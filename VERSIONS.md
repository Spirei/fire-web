# Fire Fire · 版本记录

## 更新位置约定

- **UI 数据源**：`lib/versions.ts` —— 设置 → 关于 → 版本 弹窗展示的内容全部来自这里。
- **人工可读日志**：本文件（VERSIONS.md）与 `lib/versions.ts` 同步维护。
- **更新步骤**：每次完成新功能 / 漏洞修复 / 安全加固 / 技术栈变动后：
  1. **版本以「日」为判断标准**：同一天内的多次更新合并进当天版本号，不单独递增；跨过凌晨（新的一天）后的首次更新才开启新的版本号（v0.1.0 → v0.1.1 → v0.1.2 ...）。
  2. 在 `lib/versions.ts` 的 `VERSIONS` 中：若今天已有条目，把新变更追加到该条目的 `changes`；若跨天，则在顶部新建 `VersionEntry`（版本号、日期、摘要、前端版本、软件版本、变更列表）。
  3. 变更按类型标记：`feature`（新功能）/ `fix`（修复）/ `security`（安全）。
  4. 同步更新本文件对应章节。
5. 使用新框架 / 新库 / 新数据源时，同步更新「设置 → 关于」技术栈列表与 `frontend` 版本明细。

---

## v0.1.25 · 2026-09-09

### 新功能

- 双向版本时光机：简化版工具栏、完整版页头新增穿越入口，点击即以用户提供的哆啦A梦时光机原图衔接页面切换，以 WebGL 对原图做柔和纵深与局部视差，保护人物区域，无额外流光或闪光，不支持 WebGL 时回退原图，适配深浅主题、小屏与减少动态效果；通过预取和应用内路由消除整页重载；隔离简化版样式并恢复返回后的窗口交互，离开前等待账本同步，失败时保留当前页面。

### 修复

- 简化版右上角直接显示无外框细线纸飞机，单击立即穿越；完整版入口保留在头像菜单，继续使用「去另一面」与目标版本说明。

## v0.1.24 · 2026-09-08

### 修复

- 按参考图精修投资资产构成的汇聚曲线：左右支线以缓弧向中心收拢，汇合后保留短垂直主干，并准确连接下一层卡片的边框中心；修复期末金额连线终点落入卡片内部、视觉上没有连接的问题。
- 修复编辑累计投入、转出弹窗底部显示 `undefined`；资产构成的“记账以来、今年、近 1 年”现会按所选区间重算期初金额、投入、转出、净投入与收益。
- 统一累计收益曲线与账户累计收益金额的计算口径；手动修正累计投入或转出后，曲线末端仍与顶部累计收益一致。
- 修复收益率和累计收益曲线在宽屏、缩放窗口下的鼠标命中偏移：按 SVG 实际缩放矩阵定位准星，并让悬浮提示始终收在图表边界内。
- deploy-status 更新热力图按当年 1–12 月从左至右排列为连续周日历网格，移除月份之间的分块空隙；月份仅作位置标记，跨月自然周保持连续。
- 修复镜像定时任务常延迟到香港时间凌晨 2 点以后：每天 00:07 检查以避开 GitHub Actions 整点拥堵，仅在 `main` 存在尚未发布的新提交时生成镜像。
- 简化版手机端新增下拉刷新：页面顶部下拉后松开即可同步服务端账本与最新汇率，包含阈值、加载、成功和失败反馈；弹窗、输入框及桑基图交互期间不会误触，待同步编辑会先保存以避免刷新覆盖。

---

## v0.1.23 · 2026-09-07

### 新功能

- 简化版新增年度现金流规划：首次配置进入计算器，完成后从总览打开年度页；默认展示收入与支出 ECharts 桑基图，放大后可切换收入与支出/支出及金额、比例、隐藏数据。
- 收入与支出预估编辑、预算追踪排序和分享预览按移动端参考图重做；分享与放大视图均可保存真实 PNG。
- 简化版桌面端升级为约 1040px 的居中财务工作台：放大总览卡片和内容尺度、扩大二级现金流有效区域，并保留拖动、缩放与固定；手机端比例保持不变。

### 修复

- 修复全局 `.field` 样式导致添加/编辑投资资产、账户和汇总表单被压缩重叠；补齐 320–500px 金额、标题、列表、窗口及桑基图防溢出，累计收益和超窄屏年度金额使用万单位。
- 修复刷新时右上角桌面控件闪现；更新收益金额默认留空，并统一使用完整记一笔的彩虹金额输入效果。
- 修复浏览器尺寸变化后刷新会沿用旧绝对坐标、导致简化版窗口缩到页面最左侧；窗口状态增加视口尺寸，跨桌面、平板和手机时按原中心位置重新换算，从小屏返回桌面会恢复居中宽屏工作台。
- 打磨投资详情手机布局：长收益率标题不再挤压问号说明按钮，380px 以下指标自动两列排列；账户与汇总的资产构成金额自动使用万、亿、万亿单位，避免大额数字溢出卡片，并逐页复核简化版主要页面和交互弹窗的小屏显示。
- 修复美元、港元投资账户详情误显示人民币：资产、累计收益曲线提示、资产构成、投入转出编辑、更新记录和复制摘要统一使用账户原币；指标栏简化为“累计收益”并按万、亿等紧凑显示，三项指标在窄屏保持同一排，问号不再被挤压；投资汇总仍按人民币折算。
- 打磨年度现金流概览：编辑入口移动到储蓄率右侧并保持同字号；支出预算追踪的三点菜单改为直接排序图标；桑基图采用参考图中的圆形双折角放大按钮。
- 总览与家庭资产记账的总资产卡统一为参考图的三段变化示意：两侧为紫色水平箭头，中间按资产变化显示红色上升、绿色下降或紫色持平；资产与负债结论分别按真实差额计算，家庭页移除旧的五分类小柱图。家庭成员筛选现在同步作用于资产合计、更新时间、趋势当前值和更新记录，不再混入全家历史；排除固定资产后概览与放大桑基图保持同一口径，资产组成金额使用万、亿等紧凑单位避免手机端溢出。
- 投资账户与投资汇总的资产构成按参考图重做为两层资金汇聚关系：投入、转出汇入净投入，期初金额、净投入和收益再汇入期末金额；去掉原来的蓝色节点，改用统一浅灰卡片、细曲线和参考红绿色，并补齐手机端节点高度、字号及间距适配。

### 安全修复

- 简化版 Excel 导入/导出接口增加独立登录校验，导入文件限制为 10MB，避免绕过页面调用及异常大文件占用服务资源。

---

## v0.1.22 · 2026-09-06

### 新功能

- 简化版入口升级为正式页面 `/simple-app`，地址不再显示 `.html`；`/simple` 与旧地址自动跳转，原有功能和本地数据保持不变。
- 简化版完成桌面与手机自适应打磨：桌面总览拓宽并强化核心金额，统一标题、卡片、阴影和交互反馈；手机端自动铺满视口、隐藏桌面窗口控件、适配安全区和单列卡片，并补齐键盘焦点与窄屏防溢出处理。
- 简化版对照原版继续打磨：底部「更新资产」固定在窗口页脚、不再盖住内容；总览晴雨表与投资入口、家庭/更新页标题栏对齐原版；桑基图和收益率曲线补预期虚线；更新收益弹层改用图形；现金流输入不再整页重绘；家庭成员写入服务端账本。
- 简化版弹窗改成记一笔样式，并记住当前页面：投资记账「更新收益」以及添加成员、记账提醒、自定义区间都是居中卡片；当前页、成员、账户、分类和弹窗写入地址栏，刷新或前进后退会停在原处。
- 简化版更新收益日历复用记一笔选择器：按月翻页、周一到周日、选中蓝圆、今天圆点、不可选未来日、一键回到今天；自定义区间同样使用。
- 简化版投资记账「全部资产」改为下拉选择；添加投资资产增加市场，图标复用素材库市场图标。
- 设置侧栏顶部改为约 110px 的紧凑蘑菇王国互动彩蛋，展开浮层再切换角色和查看操作说明，不挤占设置导航：问号砖随机道具、跳跃反馈、管道往返地下与地面，六位精修像素角色与选择记忆、左右键移动、上键 / 空格跳跃、下键钻管道及顶砖碰撞奖励，适配深色与减少动态效果。
- 参考蘑菇王国街机场景：展开浮层使用横向布局、像素山丘和砖块地面；支持 Escape 关闭、键盘焦点循环及关闭后焦点归还。
- 简化版投资记账支持「有知有行」格式导入导出：标题栏新增风格切换样式按钮（堆叠图层图标），点击展开 导出 Excel（有知有行格式）/ 导出 JSON / 导入账本（Excel / JSON）；xlsx 与有知有行投资记账表完全兼容（账户名称、预期年化收益率、币种、四笔钱分类及每日总资产与转入转出记录），可来回导入导出，导入按账户名去重；新增依赖 SheetJS(xlsx) 在服务端解析与生成。导入时按账户名称关键词用正则自动归入对应账本（如「富途」→ 美股（富途）、「长桥」→ 美股（长桥）、「ibkr/盈透」→ 美股（IBKR）、「港A」→ A股（港A）），账户名未命中时按文件名兜底匹配；关键词库扩充至富途/长桥/IBKR/老虎/雪盈/微牛/嘉信/华泰/中信/招商/国泰君安/银河/广发/东方财富/雪球/且慢/蛋卷/天天基金/蚂蚁/币安/OKX/火币/港A 等；投资列表按账本分组并显示账户数与小计金额。

### 修复

- 修复简化版导入有知有行后累计收益与曲线口径错误：导入器此前漏算首条总资产代表的初始本金，并把只有转入转出的日期沿用旧市值，导致累计收益缺失、收益率曲线尖峰、累计收益曲线及汇总数据与原版不一致。现按首日资产还原初始本金，以 Modified Dietz 计算资金加权收益率、XIRR 计算年化收益率，资金流日期同步调整市值；汇总曲线按日期携带各账户最新市值，并清理旧版重复种子记录。现有账本刷新后自动修正，无需重新导入。
- 修复简化版同页交互整页抖动：`render()` 每次都会重建整个屏幕，打开/收起菜单、切换下拉、筛选、展开更多设置等**同页轻交互**都会重播整页入场动画（从下方淡入上移），视觉上整页「跳一下」。现改为仅在**切换页面**（`route.name` 变化）时播放入场动画，同页重渲染为屏幕追加 `noanim` 抑制动画；一次性解决「投资记账」右上角导入/导出、「⋯」家庭成员等所有同页菜单与下拉的抖动问题，页面切换过渡动画保留。
- 修复简化版刷新闪现竖向滚动条 / 窗口高度跳变：刷新任意页面后即使内容放得下，窗口内容区仍闪出一条竖向滚动条（投资记账页因内容多最明显）。根因是 `applyWin()` 在 `render()` 前按空内容算窗口高度、随后才异步修正，且 `syncScroll()` 只在内容尚未稳定时算一次、把滚动条误设为显示、内容收敛后没重算。修复：把 `applyWin()` 移到 `render()` 之后、`render()` 末尾同步 `syncScroll()`、并用 `ResizeObserver` 实时重算是否可滚动；现在内容放得下时不出现滚动条、窗口贴内容高度，内容多时才滚动（窗口受视口上限）；并对加载中间态做内容高度稳定判定与 8px 容差，内容仅略高（如图标/字体加载时）不再误判为可滚动，彻底消除「先冒出又缩回」的闪现。
- 修复简化版导入有知有行账本：同名账户历史未导入。之前导入有知有行 xlsx/json 时若账户名与现有账户相同会直接跳过，导致导入的每日总资产历史、累计转入转出和收益率曲线都不生效（「更新记录」只剩一条、曲线不变，像没导入）。现改为同名账户**合并历史**：把导入的每日资产/转入转出按日期并入（导入优先、去重），金额取最新、累计投入/转出**尊重已手动设置的值**（仅当账户尚无投入时才采用导入值；因有知有行的「转入转出」不含建仓初始投入，用户可能已按券商收益手动修正过），并同步预期收益率/市场/币种；无同名账户时按完整历史新建。
- 新功能：简化版投资账户**资产构成支持手动编辑投入 / 转出**。账户详情页"资产构成"标题旁新增一支**笔**（参考完整版设置）：默认**只读**，**双击**投入 / 转出进入编辑，或**点笔**进入编辑态（笔高亮、投入/转出框可单击，框上不再重复显示小笔）。填入修正后的累计投入 / 转出（如为与券商收益一致而校正的值），收益（期末 − 净投入）自动重算；汇总视图仍只读。同名合并导入历史时尊重已手动设置的投入 / 转出，不被导入覆盖。
- 简化版资产构成改用 ECharts 树状图：账户详情"资产构成"改用 ECharts `tree`（`orient:BT`，期末在下、投入/转出在上）绘制：投入（红）/ 转出（绿）→ 收益 / 净投入 / 期初 → 期末，节点为圆角卡片框、金额按正负着色、层间曲线分叉连接，比手画更精致；简化版引入本地 `public/echarts.min.js`（复用完整版已有 echarts，不新增 npm 包），主题切换 / 窗口缩放 / 数据变化时按当前配色重绘与 resize；编辑（标题旁笔 + 双击 / 点笔）保留。
- 简化版账户详情「更新记录」默认显示 3 条：原本默认展开最近 6 条，改为默认只显示最近 3 条，点「查看更多」展开全部。

## v0.1.21 · 2026-09-05

> 资金记录可按股票名称、代码和拼音搜索，现金账与成交订单联动。

### 新功能


1. 资金记录联动股票名称、代码和拼音 —— 资产分析资金记录搜索不再只匹配备注原文：自动记账流水关联成交订单，可用中文名、代码（含港股 700/00700）、拼音首字母（中国移动→zgyd）和全拼查找；同时支持买入/卖出/股息、美股/港股/A股，以及 9月3日 这类日期。列表展示股票代码。
2. 股息记录改为紧凑表格 —— 持仓右击股息与个股股息页改成同一张密行表格：每股、股息率、除息、派付、状态。美股多期表头固定、区内滚动；港股和 A 股一年一两期不再按年拆成大卡片，页面跟着行数收住。
3. 日志页改为账户操作时间线 —— 日志页宽度与设置页同为 800px。上方前一日持仓盈亏：一个合计，下面美股/港股/A股三列。下方不再出现持仓或订单，改为登录、改密、导入导出等账户记录，按日浅色分隔；失败红字。买入卖出请到订单。

### 修复

1. 修复资金记账金额输入抖动 —— 新增资金记录时输入金额不再抖动：彩虹数字改为与输入框同排的连续等宽字，千分位逗号不再让数字节点整串重建；光标固定在末尾且不随逗号插入重启动画；底部「当前余额 / 记账后」行高锁死，弹窗不再跟着数字变长而上下跳动。
2. 资金记录搜索改为彩虹输入并消除抖动 —— 查看资金记录的搜索框改回逐字彩虹着色；输入法拼写过程不再把拼音中间态送去搜索，彩色字与光标共用同一套内边距，列表区域高度固定，避免边输入边上下跳。
3. 资金记录标题不再重复股票代码 —— 自动记账流水标题只保留「卖出 中国移动」，代码只出现在右侧胶囊；搜索仍按成交订单的代码、名称和拼音匹配，输入 600941 仍能找到。
4. 资金记录改为名称加色块胶囊 —— 查看资金记录简化为股票名称，后接统一胶囊：买入浅红底、卖出浅绿底、股息深色块，左侧带圆点；股票代码与「自动」用中性浅底胶囊，深浅色都对齐。
5. 资金记录搜索框占位超出显示省略号 —— 查看资金记录搜索框回到标题栏右侧原样式；占位「名称、代码、拼音、买入/卖出」宽度不够时用省略号收住，不再换到标题下方。
6. 资产分析持仓平仓改为市价快卖，并补股息入账 —— 右击平仓仍是卖出，但固定市价单、立刻成交，不再出现当日有效和时段。右击新增股息：不改持仓，可按最近一期每股股息补录；派息日到账仍会自动进订单和资金记录。
7. 持仓右击股息改为历年一览并可补录缺失 —— 资产分析持仓右击「股息」改为展示该股历年派息：按年份切换，一眼看到每股金额、除息日、派付日，以及已入账 / 待派发 / 未入账。已到派息日但账上没有的，可一键按除息日持仓补录进订单和资金记录。
8. 股息只从首次买入后入账，并写入订单 —— 自动股息改为按买入/卖出订单重放持股：首次买入之前的派息不再进账，误入账的自动股息会清掉。补录仍按除息日持股写入订单系统和资金记录，历年列表标买入前 / 当时未持仓 / 已入账。
9. 股息列表增加每期股息率 —— 持仓右击股息与个股股息页每期增加股息率：每股现金股息除以除息日前最近收盘价。没有收盘价时显示 —，不影响入账。

---

## v0.1.20 · 2026-09-03

> 空实例首次设置接入真实流程，设置标题栏保存当前分区，交易广场本地缓存动态，正文股票代码可点开详情。

### 新功能

1. 空实例首次设置接入真实三步向导 —— 生产空库不再只依赖登录页注册。无非测试用户时，访问登录或后台会进入 `/setup`：创建管理员、填写站点标题与注册开关、然后开始使用。关闭注册时仍允许创建首位管理员；已初始化实例访问 `/setup` 会回到登录或后台。公开接口 `GET /api/auth/setup-status` 与 `GET /api/v1/auth/setup-status` 只返回 `needsSetup` 布尔值。
2. 交易广场改为本地缓存优先的社交动态 —— 展示特朗普 Truth Social 与段永平雪球公开动态：页面先读本地 JSON，访问时按设置频率后台刷新；支持人物切换、拖动压缩布局、近一个月归档分页与中文翻译。设置中可分别配置两位作者的更新间隔。
3. 操作日志分类检索并纳入交易活动 —— 系统日志增加分类、搜索、分页与深浅色对比；认证审计与交易活动写入同一日志，并按市场拆分前一日盈亏摘要。
4. 段永平动态改为近三个月并带上引用原话 —— 雪球时间线窗口从 30 天扩到 90 天；回复/转发会带上被 @ 的原评论或原帖，热门里不再只看到一句没有上下文的回复。抓取同时尝试 xueqiu.com 与 api.xueqiu.com，并复用会话 Cookie。
5. 交易广场正文股票代码和链接可点开个股详情 —— 动态正文里的雪球 `$名称(代码)$`、美元代码、`$AAPL` 这类标记、雪球/Yahoo/东财股票链接，以及持仓里的代码和名称，都会高亮并可点开同一套个股详情；普通新闻链接仍跳转原文。返回箭头回到当前动态列表，刷新后仍停留在该股票。
6. 交易广场股票代码加虚线并悬停预览行情 —— 正文里的股票标记改为虚线下划线；鼠标悬停约 0.16 秒后弹出摘要卡，显示名称、代码、现价、涨跌幅、昨收、市值和行情时间，点卡片或文字都进入个股详情。行情缓存 30 秒，触控设备仍直接点开详情。
7. 日志顶部改为前一日持仓盈利并合并动态 —— 用户日志顶部改为各市场上一交易日收盘相对前收盘的持仓盈亏，带市场图标，并按显示币种汇总。点击美股/港股/A股可筛选下方列表；交易与持仓操作收成一条时间线，筛选增加「交易」。
8. 交易广场按人物显示更新时间并提示新动态条数 —— 右侧标题「全部动态」下方显示最近一次抓取时间（取所有人里最新的一次，人再多也不逐条罗列）。新帖数字叠在头像右下角，点进该人物后消失；数字在本地缓存画完之后用已有列表计算，不额外请求、不挡住页面加载。点进某个人时标题下只显示这个人的更新时间。
9. 交易广场动态展示发帖配图并支持页内缩放预览 —— 特朗普归档附件图与段永平雪球配图在后台刷新时下载到本地 `/uploads/trading-square/`，列表只加载本地下载成功的图片、不引用外链。预览改用官方 `react-photo-view`：点击缩略图在当前页按原比例打开，支持双指缩放、拖动、工具栏放大缩小与键盘切换。已缓存的旧帖在下次抓取重叠页时补图。

### 修复

1. 设置标题栏完成改为真正保存当前分区 —— 设置窗口标题栏对勾从“退出编辑”改为保存当前分区；站点信息、形象、指数、导航、来源、翻译、富途与数据库的卡片按钮同步走同一提交链路。保存失败保留编辑态，避免未写入就退出。
2. 交易广场补齐中文翻译、刷新与浏览记忆 —— 特朗普动态的翻译改为只处理尚未翻译的帖子，不再被已有译文占满前 5 条名额；访问页面后会后台继续补译，并在刷新完成后自动更新列表。人物、分类和页码写入 URL，时间显示到分钟，分页改为可跳转页码；头像跟随名人持仓自定义图。
3. 交易广场刷新不再堵住页面加载 —— 打开或刷新时立刻返回本地缓存；归档抓取改为增量（已有缓存只翻到与旧帖重叠），翻译改到后台小批量进行，不再等 30 页归档和模型翻译结束才出列表。再次进入会先画出上次缓存内容。
4. 交易广场取消按天数截断历史动态 —— 特朗普与段永平不再只保留近 30 / 90 天。已缓存的旧帖不会被按日期删掉，刷新按分页继续往回补，直到与现有缓存重叠或达到页数上限。
5. 修复特朗普更早动态不再翻译 —— 未译队列卡在 8/27 一批长帖上：单条 2.5 秒超时失败后每次只重试前 3 条，更早的帖子永远排不上。改为失败冷却并继续往后译，单次最多 20 条、模型超时 12 秒，打开交易广场就会从第 14 页缺口继续补中文。
6. 特朗普新帖先翻译再进列表，大模型优先 —— 新抓到的动态先走大模型翻译，失败再兜底 MyMemory，译文写入本地 JSON 后才进入 feed。页面不会先闪英文再换成中文。
7. 操作日志顶部补市场图标并校正交易记录 —— 用户日志盈利摘要的美股 / 港股 / A 股加上市场图标，金额按当前显示币种换算，不再把不同原币种直接相加。交易记录表「市场」与「方向」列对调回来，价格带货币符号；分页改用全站页码组件，筛选和页码写入 URL。
8. 日志摘要合计前置并接入统一货币切换 —— 前一日持仓盈利把合计放到第一格，右上角使用全站圆形国旗货币按钮，切换后金额立即按显示币种换算。列表股票列去掉市场图标，只保留代码和市场名。
9. 日志筛选改为搜索框左侧图标 —— 去掉列表上方单独一行的「全部动态」下拉。筛选改成漏斗图标放在搜索框左边，和刷新收在同一组工具里；选了交易/新增等时图标保持按下态。系统日志模块筛选同样收进这个按钮。
10. 日志合计图标与市场图标对齐 —— 前一日持仓盈利的合计保持 Σ，做成 14px 圆形图标，与同排美股/港股/A股市场图标尺寸一致，不再使用地球。
11. 刷新不再闪现右上角登录按钮 —— 已登录用户刷新首页或后台时，右上角改为服务端会话直接渲染头像，不再等 `/api/auth/me`。检查中不再画出登录按钮；接口超时或 5xx 也保留当前头像，只有 401 才回到登录。全站同类问题一并处理：已登录访问 `/login` 服务端直接进后台；后台头像遇 401 整页去登录页而不是先闪登录按钮；行情/分组/搜索的瞬时失败不再把人踢去登录页；顶栏指数条刷新先读本地缓存。
12. 加快全站首屏：不再每个页面拉取全部股票素材 —— 首页不再同步打进全球预览/财报日历整包；持仓与首页只下发当前记录需要的图标，不再每次扫描 3000+ 素材并下载约 1MB 的 `/api/assets?type=stock`。完整股票素材库仅在素材库/附件管理打开时加载。
13. 发布状态热力图月份不再换行错位 —— 更新热力图改为固定 12 列月份块（1 月到 12 月同一行），避免 53 周滚动网格把当年 9 月挤到 12 月下面。
14. 无正文的特朗普动态不再送去翻译 —— 纯图片或只剩链接的帖子不再调用大模型，避免把「请提供英文内容」当成译文。

### 安全修复

1. 翻译服务统一为大模型配置且密钥不下发浏览器 —— 设置新增独立翻译服务分区，统一 `llmProvider` / `llmApiUrl` / `llmModel` / `llmApiKey`；已保存的 API Key 只返回配置状态与掩码占位，不再把明文密钥发给页面。
2. 交易广场配图下载与预览安全加固 —— 远程抓取限 HTTPS 主机白名单，拒绝内网、localhost、带用户名的 URL 以及跳转后落到白名单外的地址；落盘前魔数校验只保存 jpg/png/gif/webp；列表与预览只接受规范本地路径，不把外链交给预览组件。

## v0.1.19 · 2026-08-31

> 统一本地与线上资源，并修复导入、行情和即时偏好同步。

### 新功能

- 添加持仓和自选股时可直接搜索并加入加密货币，搜索结果标注“加密货币”。
- 发布状态页新增最近一年 GitHub 风格更新热力图，固定显示 1–12 月且容器内自适应不产生横向滚动；每日、每周、每月、累计视图均保留相同的每日小点网格，周/月模式点击日期会联动高亮对应整周/整月并显示汇总 workflow 次数，更新越多绿色越深；手机端格子改为等宽等高的方形圆角点，避免竖条变形；持仓页货币选择改为受控更新，消除首次切换欧元/韩元的事件同步延迟；镜像构建成功且确认有新镜像后自动倒计时 5 分钟触发 Watchtower 容器更新；立即构建使用绿色舒缓呼吸圆点，且页面仅管理员可见，其他用户返回 404。

### 修复

- 修复手机端显示货币菜单越界、自选行情板股票首列消失、全球资产涨跌幅与市值拥挤、素材库序号及加密货币名称被压缩；相关数据表支持保留列宽的横向滑动，FIRE 未来年度盈利占位改为红色。
- 第二轮手机端溢出审计：显示货币菜单改为底部安全区面板；API 文档、财报附件和截图导入预览表补齐横向滚动与合理最小宽度。
- 纠正手机端行情交互：自选股股票图标/名称固定在左侧；全球资产排行恢复现价并按完整列宽横向滑动；货币菜单恢复到触发按钮旁。
- 七项手机端回归收尾：全球排行货币入口与标题并排，素材库分类栏可横向滑动且不再压缩，手机端卡片留白同步收紧。
- 全球资产排行手机端强制保留排名、资产、现价、涨跌幅、迷你趋势图、市值、市场七列，禁止浏览器压缩省略，统一由外层横向滑动；操作日志、订单导出记录和导入预览同步补齐最小宽度。
- 修复嵌套宽表无法触摸横滑：统一增加 pan-x、惯性滚动与横向越界隔离。通过 iPhone 12 Pro 390px 浏览器逐页复现并移除旧版 ≤420px 强制删列规则；我的持仓、资产分析、自选行情、全球资产、日志、素材库、名人持仓及首页预览在所有手机尺寸统一保留完整列并内部横滑。
- 取消自选行情左侧股票列和右侧操作列的横向冻结定位、层级与阴影，并清理深色编辑模式残留投影及序号列隐藏兜底；所有数据列随表格自然横滑。全站扫描确认其余 sticky 均为移动导航、附件表头或弹窗标题等正常纵向吸顶。
- 小屏货币金额紧凑显示：韩元、日元等高面额货币在持仓与资产分析卡片按万、百万、千万、亿缩写，桌面保持完整金额；股票设置新增“跟随设备（小屏缩写）/始终缩写/始终完整”全局单位切换。
- 富途 OpenAPI 额度查询补齐 800ms TCP 端口预检，OpenD 未启动或不可达时立即返回，不再进入 SDK 内部重连；10 秒桥接硬超时继续兜底网关可达但 SDK 异常的情况。
- 继续收紧历史成交与实时委托：未知订单类型/时效由服务端拒绝，自定义有效期按交易所自然日截止，今日/历史按交易所日期归类，挂单卖出创建时校验可卖数量；备份导入完整保留六种委托类型与 custom 时效，并兼容旧版无 mode 的成交请求。
- 设置搜索收敛为单实例响应式面板，消除桌面/手机重复 DOM 与隐藏输入框焦点竞争；补齐组合框语义、悬停选择、方向键边界、回车跳转、空结果提示和 Escape 关闭。
- 资产分析持仓交易弹窗拆分为“提交委托 / 记录已成交”：历史成交按交易所当地时间录入并禁止未来时间；实时限价单未到价保持待成交，不再因“当日有效”直接成交；同时修复当日挂单跨交易日不失效及成功提示误判。
- 修复个股详情小屏布局：顶部行情指标默认隐藏，可按需显示为 2×2 卡片；周期与操作控件上下分区，K 线周期在自身区域横滑；时段、周期、复权、比较、图形和指标选择框统一使用视口内底部安全面板，解决文字挤压和弹层越界；移动断点按视口将 K 线高度收敛至约 220–300px，避免手机图形纵向占位过大；详情头部同步缩小返回键与图标、截断过长名称、隐藏重复代码并收紧价格排版；手机 K 线悬浮框改为紧凑的收盘/涨跌信息，避免遮挡图形。

1. 线上与本地欧元默认旗帜统一为欧盟 SVG —— 欧元市场码 `EU` 不再回退到通用 `eu.svg`，货币选择器、市场图标、全球经济热图与素材库统一使用 `/uploads/asset/flag/欧盟EU.svg`。新增既有数据库一次性迁移，仅替换旧内置 `eu.svg`，不覆盖用户上传的其他自定义旗帜；新部署会以 `EU` 代码播种该素材，Docker 镜像同步携带并在 uploads 挂载缺失时从 `resource-default` 兜底。
2. 个人资料与用户管理现代化精简 —— 移除个人信息编辑态昵称与登录邮箱输入框中的示例占位文字；用户管理由高占位宽表重构为紧凑成员列表，顶部概览总用户、在线与管理员数量，身份区合并昵称、登录名、UID 与状态，操作统一为图标按钮；窄屏自动转为完整卡片布局，不再横向滚动。
3. 修复移动端订单工具栏溢出与失效素材破图 —— 订单导入、导出与列设置工具在 390px 手机端改为独立自适应工具行，搜索框占满一行并将次要文字按钮收为图标，避免超出右边界；站点 Logo、首页背景与动态 favicon 增加加载失败回退，素材库股票/市场图标容器补齐深色背景，避免 404 破图与深色模式白底突兀。
4. 移除固定测试用户身份判断 —— 全量审计用户认证、权限与数据查询链路，移除生产登录流程中针对固定 `demo-user` ID 与公开默认密码的特殊分支；现在管理员权限仅由数据库 `role` 决定，UID、用户名和内部 ID 均不再被当作特殊账号。开发环境种子账号与历史订单数据迁移保留为明确的测试/数据修复用途。
5. 完成响应式排查收尾 —— 按 390px 手机、820px 平板与 1440px 桌面复核主要页面：资产分析账户资产卡的分享/刷新按钮补足 36px 触控热区，图标视觉尺寸保持不变；手机与平板顶部横向导航增加右侧边缘渐隐提示，用户能明确知道还可继续滑动。
6. 导入自选股分组改为幂等并自动合并历史重复项 —— 市场分组按市场代码、自定义分组按规范化名称复用现有实体；读取分组时会把历史重复记录迁移到最早的规范分组后删除重复项，既有线上数据无需再次导入即可恢复。
7. 本地源码与 GHCR 默认素材统一 —— 恢复并发布完整国家旗帜、券商图标与四种贵金属 SVG；素材播种同时扫描 `public/uploads` 和镜像 `resource-default`，挂载目录缺文件时仍能恢复默认资源。素材库统一“美股”及更多市场中文名，地图国名改为中文并增加真实旗帜回退；部署审计新增默认素材与运行时兜底检查。
8. 显示货币即时同步并补齐线上股票行情 —— 全站共享偏好增加同标签页与跨标签页通知，持仓、FIRE 与资产分析切换显示货币后立即重算；素材库在本地缓存缺失时复用各市场行情源补齐市值、最新价与涨跌幅。
9. 统一本地与线上发布节奏并新增状态监控页 —— main 提交继续执行检查但不再每次发布 GHCR；改为北京时间每天 00:00 自动构建发布，并保留 GitHub 手动运行兜底。新增 `/deploy-status` 状态页与 API，绿色/灰色/红色圆点分别表示成功、待发布（排队/进行中）和失败，自动每分钟刷新。
10. 发布状态页增加手动推送与可视化流程 —— 新增“立即手动推送”按钮，由服务端安全调用 GitHub Actions `workflow_dispatch` 发布 main 最新提交；页面增加本地源码 → 检查 → 定时/手动 → GHCR → 线上容器流程图，并显示触发中、成功和失败反馈。
11. 发布监控页右上角增加浅色/深色切换 —— 复用全站主题按钮与 Cookie/localStorage 持久化，刷新后保持选择。
12. GitHub OAuth 授权与 Token 安全兜底 —— OAuth 使用一次性 state、管理员权限和 workflow 最小权限；授权及备用 Token 仅服务端保存，并通过 `DEPLOY_STATUS_SECRET` 使用 AES-GCM 加密，页面只显示授权账号/已配置状态。
13. 修复发布监控页浅色主题不生效 —— 监控页面板、流程节点、边框、按钮和运行列表补齐浅色/深色双主题样式，右上角切换现在会即时改变整个页面并在刷新后保持。
14. 发布监控页工具栏统一图标操作 —— 刷新、GitHub 授权、配置账号和立即推送统一改为带悬停提示与无障碍名称的图标按钮，减少顶栏占用并保留推送中状态反馈。
15. 发布监控页刷新按钮增加动效 —— 刷新图标悬停时以轻量旋转动画反馈操作，保持顶栏简洁并强化可发现性。
16. 发布流程节点改为可操作按钮 —— main、检查、立即发布、GHCR 与线上容器节点均可直接操作或跳转，状态检查和手动发布无需离开监控页。
17. 发布监控页主题与刷新交互打磨 —— 展开的 GitHub 配置区补齐浅色/深色背景、边框、输入框和操作按钮对比度；刷新按钮支持点击期间禁用并持续旋转，减少重复请求并强化反馈。
18. 发布流程与刷新动画改为柔和过渡 —— 刷新仅在点击请求期间旋转，使用平滑缓动曲线；流程按钮增加自然的悬停与按下过渡，避免状态切换生硬。
19. 优化发布配置保存按钮文字对比度 —— 浅色主题使用深蓝底白字，深色主题使用亮蓝底深色字，并补充悬停与按下反馈，提升可读性。
20. 深色模式保存配置改为白底黑字 —— 深色模式下保存配置按钮使用白色背景与深色文字，并补充键盘焦点环，提升对比度和可访问性。
21. 修正手动 Token 权限提示 —— 网页配置将备用 Token 明确为发布 Token，提示手动触发 workflow 需要目标仓库 Actions 的 Read and write 权限，并同步更新环境变量示例。
21. 修复本地版券商及同类素材图标丢失 —— 券商素材播种同时校验本地文件，自动修复历史 `stock/GROUP` 错路径和缺失文件，并按券商名称/别名重建当前分组映射；素材 API 不再下发磁盘与默认资源中都不存在的 URL，全站统一转入已有兜底；素材缓存升级版本，旧缓存会立即后台刷新。
22. 恢复我的持仓编辑中丢失的券商分组 —— 新增一次性幂等迁移，保留现有券商配置与顺序，并从未丢失的券商素材记录中追加恢复被覆盖的券商；迁移完成后会写入标记，之后手动删除券商不会被自动恢复。
23. 全站素材与站点形象完整性修复 —— 清理股票与加密货币素材表中已不存在的本地 URL，保留名称/代码供默认图标或 CDN 兜底；股票默认播种不再写入未打包的文件地址；首页背景、登录侧图、favicon 与 Logo 在本地文件丢失时自动回退内置样式；券商下拉图标增加加载失败首字母兜底。
24. 修复网页 GitHub 配置仍返回 API 404 —— 状态读取统一使用网页保存的 OAuth 或手动 Token，网页配置仓库优先于环境变量与 GHCR 镜像推导值，并在错误中显示实际请求仓库。
25. 发布记录改为每页 10 条 —— 最多读取最近 50 次运行并按每页 10 条分页，增加总数、当前页与上一页/下一页控制；窄屏页头改为纵向排列，避免标题与操作图标拥挤。
26. 修复深浅色切换短暂延迟 —— 主题按钮点击时同步更新根节点 class、持久化设置和 Cookie，不再等待 React 副作用；发布监控页移除整页颜色缓动，主题即时生效。
27. 修复提交检查被误显示为发布成功 —— 顶部最新发布状态仅统计定时与手动运行，普通 main 提交的检查成功不再被误判为镜像发布成功。
28. 重排发布状态圆点与说明 —— 最新发布状态圆点与标题改为同一基线，成功和失败保持静态，仅排队/进行中使用呼吸动效；底部说明改为三个醒目的圆点状态标签。
29. 发布监控页支持一键更新群晖容器 —— GHCR 部署增加受限 Watchtower 服务，管理员可在发布流程中拉取最新 Fire 镜像并观察重启与健康恢复；更新器仅匹配 Fire 专属标签与 scope，API 不映射宿主机端口并使用独立 32 位以上随机 Token，网页端不能传入任意命令、容器或镜像，并限制 30 秒内重复触发。Compose 配置变化仍需群晖手动执行 `compose up`。
30. 修复发布监控页手机端头部错位 —— 390px 手机端将标题、简短说明与工具图标拆成稳定的三层布局，缩短移动端文案并统一边距；发布流程改为两列触控网格，隐藏造成断行的箭头，按钮保持单行。桌面端布局不变。
31. 阻止重复触发手动发布 —— 顶部推送图标与流程中的立即发布按钮共享 GitHub 运行状态，已有手动任务排队或运行时同时禁用；服务端触发前再次查询工作流，并增加 30 秒触发锁，防止快速连点、多标签页或网络延迟产生重复运行。失败或完成后仍可重试。
32. 明确 Push image 镜像构建入口 —— 发布流程将原“立即发布”按钮明确命名为 `Push image`，点击后生成并推送 GHCR 镜像；原“构建 GHCR”链接改为“查看 GHCR”，顶部图标、流程按钮与最近运行记录统一命名并继续共享防重复保护。
33. 统一发布操作并优化刷新反馈 —— 删除顶部重复的镜像推送快捷按钮，镜像构建统一从发布流程中的 `Push image` 执行；流程节点加入统一语义图标并整理尺寸、圆角、聚焦和深浅色状态，删除按钮下方的冗余操作提示。刷新按钮改为每次点击平滑旋转一圈，移除动画叠加造成的漂移和突停。
34. GitHub 授权改为新窗口打开 —— 右上角与配置面板中的 GitHub OAuth 入口统一使用新窗口打开，并补充 `noreferrer` 安全属性和无障碍提示，避免离开发布监控页面。
35. 新增镜像构建实时进度 —— `Push image` 后从 GitHub Actions Jobs API 读取真实状态，展示 TypeScript 检查和 linux/amd64 镜像发布两个阶段；运行中的 Job 默认展开，可查看 Checkout、GHCR 登录、Buildx、Build and push 等具体步骤、状态与耗时。构建期间每 10 秒刷新，结束后恢复每分钟刷新，并使用分级缓存控制 GitHub API 用量。
36. 打磨镜像构建进度交互 —— 增加已完成阶段计数，Job 摘要直接显示当前执行步骤；运行中与失败阶段使用语义边框区分，跳过步骤不再误显示为等待中。修正其他 `workflow_dispatch` 任务误锁定 `Push image` 的前端判断，并为动态状态及提示信息补充无障碍播报和浅色模式文字对比。
37. 发布状态改为三段版本校验 —— 分别读取 GitHub main、最近可用 GHCR 镜像与当前容器的完整提交 SHA，明确区分构建成功、镜像可用和线上已部署；群晖更新仅对当前 main 的已验证镜像开放，更新后必须由健康接口返回的运行 SHA 完成校验。镜像同步写入构建 SHA 与 OCI revision，修复完成任务仍显示构建中的缓存，并隔离提交检查与正式发布的并发取消范围。
38. 收敛发布流程绿色状态语义 —— GitHub main 使用中性灰色表示来源，检查结果作为子项独立显示；绿色仅用于当前 main 镜像构建完成，以及群晖实际运行该镜像且健康的可验证结果。
39. 精简发布记录与 GHCR 入口 —— 最近运行改为每页 5 条；移除发布流程标题旁的文字型 GHCR 入口，改为右上角 GitHub 授权按钮右侧的独立包仓库图标。
40. 继续打磨发布监控页信息层级 —— 保持原有页面宽度；GHCR 入口换为更简洁的单包图标并移除重复状态图例。GitHub API 异常时仍保留服务端返回的实际仓库名，避免顶部链接退回占位地址。
41. 发布监控补齐真实加载与更新器在线状态 —— 首次读取运行记录时显示列表骨架，不再短暂误报待发布或暂无记录；更新器状态改为同时探测 Compose 内网端口，fire-updater 未启动或网络不可达时按钮提前显示更新服务离线。
42. 重排线上镜像版本徽章 —— 移除流程末尾重复的线上旧镜像节点，起点改为 fire-web main，并将当前容器实际运行的短 SHA 放到该节点右上角的 GitHub 风格胶囊中；镜像与 main 一致时显示绿色圆点和“镜像已构建完成”。
43. 暂停 GHCR 自动清理 —— 暂停自动删除历史镜像，避免多标签 manifest 被误删导致 registry 返回 manifest unknown；待验证更安全的按摘要清理方案后再恢复保留策略。
44. 定时任务跳过无变化镜像 —— 每日凌晨仍会执行源码与安全检查，但仅当 main 相比上次成功的定时或手动发布发生新提交时才构建镜像；手动 Push image 继续作为强制发布兜底。
45. 提升 GHCR 镜像清单兼容性 —— 发布镜像时关闭额外 provenance 与 SBOM 清单，保留标准 linux/amd64 镜像，兼容群晖 Docker 拉取并避免 registry 返回 manifest unknown。
46. 统一发布版本胶囊视觉 —— 线上版本短 SHA 改为 GitHub 风格中性胶囊，浅色模式使用黑色文字，深色模式使用白色文字；版本是否落后仅由状态圆点表达，移除整块橙色告警感。
47. 移除发布流程重复版本提示 —— 当前线上 SHA 已由 fire-web main 节点右上角胶囊展示，镜像未覆盖最新 main 时，构建节点改为“构建新镜像”，不再重复显示“镜像落后 + 相同 SHA”。
48. 收紧发布监控页临界宽度 —— 桌面内容区由 800px 收紧至 720px，在四段发布流程仍能稳定保持单行的前提下减少横向空白；更窄视口继续使用现有响应式网格。
49. 按实际构建结果显示镜像状态 —— 只要最近一次镜像构建成功，流程节点即显示绿色圆点与“镜像已构建完成”；main 后续新增提交不再把已成功构建的镜像显示为落后，版本差异仍由更新按钮可用性控制。
50. 修复线上版本胶囊越界 —— 将当前线上版本胶囊从 main 节点的负定位悬浮层移到发布流程标题行右侧，保留 GitHub 风格与状态圆点，同时避免越过节点边框、挤压流程箭头或影响 720px 布局。
51. 统一发布页中文操作文案 —— 将 Push image、镜像发布提示和顶部说明统一为中文表达，保留 GitHub、GHCR 等专有名词，减少中英文混排造成的阅读跳跃。
52. 修复发布接口 HTML 解析报错 —— 发布页统一校验 API 响应类型；接口返回 404、登录页或其他 HTML 时显示明确的 HTTP 与容器版本提示，不再暴露 Unexpected token 解析错误，也不再同时显示成功和错误提示。
53. 新提交恢复待构建状态 —— 上一轮镜像仅在与当前 main 匹配时显示绿色完成；main 出现新推送后切换为橙色“立即构建”，并隐藏已过期的上一轮完成进度卡。
54. 自动恢复更新服务状态 —— 发布页每 15 秒复检一次 fire-updater，手动刷新时也同步检查；更新服务后启动无需重开页面即可由离线恢复，并统一显示明确的离线文案。
55. 显示 Watchtower 在线状态 —— 复用发布流程末端的更新节点，以灰、绿、红圆点分别表示 Watchtower 检测中、在线和离线；已是最新时显示“更新服务在线”，避免新增元素挤压 720px 布局。
56. 区分等待检查与状态未知 —— main 新提交已出现但 GitHub Actions 运行记录尚未生成时显示橙色“等待检查”，并临时提高到每 10 秒刷新；只有 main 本身无法读取时才显示“检查未知”。
57. 精简线上版本胶囊 —— 线上版本仅在匹配最新 main 时显示绿色圆点，非最新版统一使用灰色圆点；边框改为轻量内描边与中性底色并移除浮起阴影。
58. 增加运行次数数字徽标 —— 最近运行标题旁以紧凑圆形徽标显示 GitHub Actions 总运行次数，仅保留数字；同时将单次读取上限提高到 100，使页面分页数量与 GitHub 总数一致。
59. 修复最新版镜像误判 —— GitHub 提交详情接口读取失败时，改用最新 main push 运行的完整 head SHA 作为回退，避免同一版本被错误显示为检查未知、立即构建和线上非最新。
60. 明确容器最新状态 —— 发布流程末端的绿色圆点继续代表 Watchtower 在线；线上已运行最新镜像时文字改为“容器已是最新”，并取消禁用态整体变淡，保持状态圆点清晰。
61. 精简更新服务状态文案 —— 发布流程末端在有可更新镜像时显示“有新镜像”，离线与检测状态分别精简为“服务离线”和“服务检测中”，绿、红、灰圆点继续表达服务状态。
62. 区分最近运行成功类型 —— 最近运行列表中，定时或手动镜像构建成功使用绿色圆点，main 推送检查成功使用蓝色圆点，便于快速区分发布与检查记录。
63. 统一美股名称并修复货币选择器越界 —— 首页、我的持仓和素材库内置 US 市场统一显示“美股”，不再受历史自定义标签影响；持仓盈利图货币选择器在窄卡片与窄屏下限制宽度并允许操作区换行，边框不再超出容器。
64. 稳定全球经济最新年份 —— 全球经济数据不再依赖 World Bank 返回记录的顺序，而是对每个国家明确选取查询范围内的最大年份，避免本地与线上因响应顺序不同显示不同年份或数值。
65. 修复加密货币市值冷启动 —— 全球市值榜首次加载时等待 CoinGecko 市值行情补齐，并对缓存中缺失市值的加密货币自动刷新，避免 BTC、ETH 等项目以 0 市值落到第 99、100 位。
66. 增加加密货币市值备用来源 —— CoinGecko 返回价格但缺少市值时，使用 CompaniesMarketCap 全球资产榜中的加密货币市值补齐，避免限流或字段缺失再次产生 0 市值条目。

## v0.1.18 · 2026-08-30

> 全站数字输入统一为键入时逐位彩虹显示。

### 界面与交互

1. 撤回资金记录以外的全局彩虹输入 —— 按要求取消持仓、行情、快捷交易、FIRE、导入、名人持仓、财报、K 线及设置等页面的全局彩虹输入，恢复各页面原有数字控件与交互；仅保留新增资金记录中的专用彩虹输入效果。
2. 股票添加搜索框支持逐字符彩虹输入 —— 在“我的持仓-添加股票”和“自选股-股票添加”两个搜索框启用实时逐字符彩虹显示；中文、英文、数字和原有搜索键盘交互均可正常输入，其他文本输入框不受影响。
3. 修复首屏应用壳长时间停留在加载态 —— 默认“我的持仓”和“自选股”改为服务端首帧渲染，直接利用已注入的持仓与设置数据，避免应用壳出现后长时间只显示“加载中…”；其他重型页面继续按需加载。
4. 资产标签改用中性灰并优化搜索提示色 —— “我的持仓”页的显示货币、资产指标标签、各市场盈利说明与表头改为中性灰；股票添加搜索框占位提示单独使用冷灰蓝，已输入的彩虹文字和盈亏红绿语义保持不变。
5. 自选股截图导入改为轻量模式并支持批量分组 —— 自选股页面的入口改名为“截图导入自选股”，识别预览仅保留名称、代码、市场与状态，隐藏数量、现价、成本；确认前可选择自定义分组，将本次识别结果批量归入该分组。持仓页面继续使用完整字段，服务端同时校验分组归属。
6. 自选股行情板布局与批量操作继续精修 —— 编辑、移动、删除统一为紧凑按钮组，默认只读时隐藏行内操作，悬停或键盘聚焦行时再渐显；移动下拉增加轻量箭头与焦点反馈，操作列收紧并在横向滚动时贴右可见；股票、行情时间等数据列增加稳定最小宽度，中等窗口不再把名称压成短省略号。标题区增加行情数量、状态圆点和手动刷新；批量操作改为独立工具条，选中行增加轻量高亮，分页信息改为当前条目范围。
7. 全站迷你走势图接口优化（gzip + 抽样 + 缓存） —— `/api/charts` 迷你走势响应改为 gzip 压缩（`content-encoding: gzip`），并对批量走势做固定点数抽样（约 940 点压到 60 点），显著减小单次请求体积；同时为腾讯分钟数据增加 30 秒内存缓存，减少重复外部请求。抽样改为逐请求 opt-in：首页（自选股 + 我的持仓的近5日走势）与自选股行情板声明 `sample` 才抽 60 点；个股详情 K线与该分时不声明，仍保留全量分时点。

### 修复

1. 首个注册用户自动成为管理员 —— 首个非测试注册账号自动设为 `admin`，无需预先在 `.env` 配置管理员；同时保留 `INITIAL_ADMIN_USERNAME` / `INITIAL_ADMIN_PASSWORD` 兜底，仅在两者均合法时用它建管理员，未提供或不合规时不再抛出异常，交由首个注册账号接管。
2. 修复局域网 HTTP 登录无限跳回登录页 —— 生产环境 session cookie 固定带 `Secure` 标志，`http://IP:port` 明文访问时浏览器不保存/不发送该 cookie，登录成功后会话丢失、被反复重定向回登录页；现改为按请求是否真正走 HTTPS（`x-forwarded-proto` / 协议）动态决定是否带 `Secure`。
3. 修复 PWA manifest 构建期预渲染空库报错 —— `app/manifest.ts` 在 `next build` 被静态预渲染并读取站点设置（触发建库 seed），空库 + 生产模式下因缺少 `INITIAL_ADMIN_*` 构建失败；已将该路由改为运行时动态渲染，构建不再预渲染碰库。
4. 修复新用户素材库图标缺失时显示占位 —— 全新用户（素材库为空 / 默认图标文件未随部署打包）时，导航、货币、设置菜单等图标因引用不存在的 `/uploads/asset/...` 显示「?」破图；新增 `SafeAssetImage` 安全加载，图片加载失败自动回退内置矢量默认图标（`NAV_ICONS` / 地球），不再出现「?」；同时基础素材库图标（icon / flag / market 等）随镜像打包并在首次启动复制到挂载目录，新用户开箱即用完整图标。
5. 线上名人持仓默认使用定制头像 —— 修复 Docker 部署中 `data/celebs-avatars.json` 不进镜像，导致线上新库虽有定制头像文件，却因缺少映射而回退原始照片。六位内置名人直接使用定制头像作为默认值，并增加一次性数据库迁移：仅替换明确的旧默认路径；服务端 / 浏览器旧缓存也不再覆盖当前头像。新增 `public/uploads/celebs/default-avatars.json` 发布映射，今后本地上传会同步更新运行时映射与发布映射，从本地源码重建镜像时头像文件和关联关系会一起进入群晖。
6. FIRE 导航图标恢复默认火焰图标 —— 修复历史上本地与线上数据库中 `icon:FIRE` 保留自定义 `fire-gray.svg`，导致导航显示波浪线图标。增加一次性数据库迁移，将 FIRE 统一恢复为随镜像发布的 `fire.svg` / `fire-dark.svg`，新库与既有库都一致。

### 安全修复

1. 公开仓库净化与自动防泄漏审计 —— Compose 改为通过 `GHCR_IMAGE`、`HOST_PORT`、`DATA_DIR`、`UPLOADS_DIR` 注入部署差异，不再写死账号、端口、NAS 路径或硬件限制；出站代理默认关闭，删除特定用户名 UID 规则与无引用用户上传素材。新增 `npm run audit:public` 并接入 GitHub Actions，自动拦截个人账号、本机路径、个人代理、硬编码群晖配置、时间戳用户素材及常见 Token/API Key/私钥。

## v0.1.17 · 2026-08-29

> 修复设置侧栏选择项目时右侧内容卡片短暂出现蓝色边框。

### 修复

1. 移除设置导航选中时的蓝色卡片边框 —— 设置侧栏选择项目后仍保留平滑滚动与内容切换，但不再给右侧卡片添加蓝色 `settings-flash` 外圈动画，选中态保持中性灰视觉规范。
2. 移除设置页主题切换与已保存状态控件 —— 移除设置内容头部右侧的太阳主题切换按钮和已保存状态胶囊，并清理对应组件引用、状态回显与样式；设置修改仍会自动保存，失败时继续通过 Toast 提示。
3. 券商分组默认展示三项并支持展开更多 —— 券商分组默认按单列从上到下展示前 3 个，标题区与首条券商增加留白；浏览状态仅显示图标、中文名称和英文别名。标题旁新增铅笔按钮，点击才进入编辑状态并显示名称/别名输入框、拖动、删除、添加、取消和保存；保存继续通过服务端同步券商改名、别名、排序与删除到全站。超过 3 个时可展开更多。移除整卡按压缩放，输入框聚焦不再抖动。
4. 设置页列表管理统一浏览与编辑状态 —— 首页指数、首页导航、应用导航默认前 5 项并支持更多，列表统一紧凑宽度与单行信息布局；区块标题和内容全局保持 14px 间距。再次通过 390px 窄屏实际走查，修复移动端只能进入“网站设置/股票设置”一级分类、无法访问内部设置的功能缺口：新增当前分类二级横向导航，可直接切换站点信息、网站形象、首页指数、首页导航及股票下各子项。首页导航和应用导航在窄屏自动把路径换到下一行，避免挤压；个人信息头像区从高占位纵排压缩为 52px 横排。
5. 设置侧栏品牌头部精修 —— 优先展示站点已配置 Logo，无图时回退精修 F 字标；Logo 改为 32px 柔和渐变圆角块，增加细边框、内高光和轻阴影；Fire 字标加粗并收紧字距，副标题缩小压紧；品牌区底部增加细分隔线。搜索从悬浮裸图标改为常驻 28px 圆角工具按钮，带边框、背景、轻阴影和克制的上浮 hover，消除漂浮感并提升可发现性。深色实际页面已走查。
6. 股票来源外链操作图标化 —— 全局审查设置页同类入口：股票来源接口各行移除占宽的“打开链接 ↗”文字，改为左侧复制、右侧外链的双 30px 圆角图标按钮，复制后 Toast 反馈；网站形象展开面板的“打开”文字同步改为外链图标，设置侧栏 API 的裸 ↗ 改为标准外链 SVG。统一补齐主题色浅底、细描边、悬停/按压反馈、键盘焦点环和无障碍名称；保存、完成、添加、清除等需要明确语义的操作保留文字。
7. 全站股票图标首屏加载提速 —— 排查确认股票图标虽已本地化，但自选、持仓、资产分析等页面仍要等客户端挂载后请求整套 3,262 条股票素材，导致首屏图标列先空白再补出。改为服务端根据当前账户记录提取所需图标，并兼容杠杆 ETF→正股图标兜底，随应用壳首屏注入共享图标缓存；HTML 同步输出去重后的 image preload，让本地图片更早并行加载。完整素材库仍在后台刷新，不牺牲后续全市场图标。实际刷新自选页验证：首屏 6 个 preload 生效、可用图标均 complete 且 168px 图片已就绪、无首屏字母占位等待。
8. 设置个人信息页视觉层级精修 —— 个人信息头像区升级为独立身份卡：加入主题渐变底、圆角边框、头像白色描边与柔和阴影，并补充昵称、@用户名及管理员/成员徽标；密码表单改为独立浅底圆角容器；网站备份与持仓导出归入统一的数据操作面板；清空数据与注销账号从普通数据项中拆出，归入红色弱提示的危险操作区，避免与安全操作混排。窄屏下身份卡自动切换横向布局，保持信息紧凑清晰。

### 界面与交互

1. 设置侧栏品牌花标打磨（点击一次转一圈 + 连续点击连续转 + 居中 + 搜索移入标题栏） —— 1) 品牌花标改为「点击一次转一圈、连续点击连续转」：每次点击目标角度 +360°，单次点击平滑转满一整圈后停稳，连续点击则目标不断前移、持续顺滑地转；改用临界阻尼弹簧在角度目标上做物理积分（requestAnimationFrame），并设定角速度上限，消除原先速度突跳/急停导致的卡顿，旋转起步缓、中间快、收尾柔。花标外轮廓由原来的细长六瓣「星芒」打磨成参考图那种圆凸瓣、浅凹口的云朵/花瓣式外形（保留花瓣式轮廓），内容恢复为 Fire 趋势折线 + 箭头，并恢复其「先描绘自己、箭头在画到末端时浮现」的播放动画（默认静止，仅在鼠标悬停或连续点击转动中播放）；去掉多余的填充/轨道环等装饰，并去掉花标外层的圆角方框、改为无框呈现（旋转时花瓣外浮出柔和光晕）；2) 移除侧栏品牌区的「Fire 投资记实」文案，花瓣在品牌区水平居中；3) 搜索按钮从侧栏移入设置窗口标题栏工具组（置顶图标左侧），点击仍唤起 ⌘K 命令搜索（改为窗口事件触发），命令面板固定于设置窗口右上角、紧贴搜索按钮下方浮出。tsc 无错误。

2. 设置窗口宽度以 800px 为基础优化 —— 设置页桌面窗口最大宽度定为 800px（`.sv-win-root`、`SettingsWindow` 的 `max-w` 与 `settings-mockup-standard.html` 同步），并保留单行排版、输入框弹性伸缩、细滚动条等基础优化：股票来源行控制区约 316px，常见链接可完整显示；股票来源链接在非编辑态改为带「…」省略号截断的文本 span（悬停 `title` 显示完整 URL），编辑时才显示输入框，避免输入框内部硬切显得难看。tsc 无错误。

3. 修复切换设置左选项时整个页面往上滚 —— 点击设置左侧导航 / ⌘K 搜索结果时，`jumpTo` 原先用 `el.scrollIntoView({block:'start'})` 把目标锚点贴到视口顶部，导致整个页面(document)一起上滚、设置窗口整体上移。改为只在设置内容滚动区(`.sw-content-scroll`)内滚动：用 `getBoundingClientRect` 差量计算目标 `scrollTop` 后 `container.scrollTo({behavior:'smooth'})`，不再影响页面整体位置。tsc 无错误。

4. 设置行改单行排版：输入框弹性伸缩、取消横向滚动/换行 —— 设置里「股票来源接口」的复制/新窗口图标、「交易·富途」的查询额度文本此前会因 786px 下空间不足而换行或出现难看横向滚动条。改为让 `.sw-row .ctrl` 保持单行(`flex-wrap:nowrap`、取消 `overflow-x` 滚动条)、输入框改为 `flex:1 1 auto`(最小 120px)弹性伸缩以贴合可用宽度(URL 在输入框内部滚动)，富途额度文本允许 `min-w-0`+`overflow` 收缩、行情源胶囊不换行；这样单行、无滚动条、无换行，更干净。tsc 无错误。

5. 侧栏花标放大 + 设置内容区滚动条改细 —— 1) 侧栏品牌花标按钮由 40px 放大到 46px、内部 SVG 由 34px 放大到 40px，云朵花瓣更大、与侧栏更协调；2) 设置内容滚动区(`.sw-content-scroll`)补一条 6px 细圆角滚动条(webkit + `scrollbar-width:thin`，用主题色半透明)，替代浏览器默认粗滚动条，与全站其它细滚动条一致。tsc 无错误。

6. 设置页输入默认态统一为只读（站点信息/交易富途/网站形象/数据库补编辑铅笔） —— 按已建立的“默认只读、点编辑铅笔才可编辑”规范，对设置页做全局排查并补齐常显输入框：站点信息(网站标题/域名/页脚)、交易·富途(OpenD 主机/端口)、网站形象(图标/Logo/背景/登录图的粘贴链接输入框及上传/清除按钮)、数据库(数据库类型卡 + PostgreSQL 连接输入框 + 测试/保存)原先默认直接显示带边框输入框；现为这四块增加“编辑铅笔 + 完成”切换，非编辑态输入框改为 `readOnly` 并去掉边框/背景/阴影、上传/清除/类型切换/保存禁用(纯展示，仅 UI，不改数据)。券商分组、首页指数/首页导航/应用导航、来源接口、个人信息已有编辑态，无需改动。tsc 无错误。

7. 个人信息「复制 UID」改图标 + 值区贴左 + 设置页内容溢出排查 —— 登录名行改为紧凑复制图标按钮，并让值区填满剩余宽度；只读代码值统一左对齐并增加溢出保护。tsc 无错误。

8. 新增「网站设置导出/导入」全量无损迁移 —— 个人信息新增「网站设置导出 / 导入」卡片：导出生成 `fire-site-backup-*.json`（含 `format/version/appVersion/exportedAt` 与 `manifest` 计数），`data` 覆盖站点设置(全部 key-value)、个人偏好、持仓记录、成交订单、操作日志、自选分组、名人持仓与当前用户昵称——纯数据；图标/图片为应用默认、上线自带，不打包。导入校验 `format/version`，导入前自动做一次数据库快照备份，随后在单事务内按 id `upsert`(存在更新、不存在插入) 还原 `records→trade_orders→activities→watchGroups→userSettings→celebs→site_settings`，所有 `user_id` 重映射为当前导入用户，并恢复昵称，做到不覆盖不丢失。新增 `GET/POST /api/v1/data/export|import`(仅管理员) 及 `docs/api-spec` 6.18。tsc 无错误。

9. 网站数据导出/导入打磨（manifest 说明 + 纯数据 + 两步导入） —— 导出文件 `manifest` 增加 `excluded` 说明字段(图标/图片为应用默认、上线自带；数据库连接串等环境专属配置不迁移)；导出排除 `dbType/pgHost/pgPort/pgDatabase/pgUser/pgPassword` 等环境专属设置键，避免上线时把开发库连接带到线上；导入改为两步——先 `POST /api/v1/data/import?preview=1` 做服务端校验+试算条数(不写入)，前端预览条数并确认后再正式导入(单事务 `upsert`，恢复昵称)。tsc 无错误。

10. 网站数据导出/导入开放给普通用户 + 按角色作用域防注入 + 新增注销账号 —— 1) 网站数据导出/导入不再仅管理员：所有登录用户可用，导出/导入按当前用户作用域——普通用户只迁自己的 `userSettings/records/tradeOrders/activities/watchGroups/昵称`；管理员额外含 `siteSettings` 与 `celebs`(名人持仓)。2) 防导入注入：普通用户即使备份里带了 `siteSettings/celebs` 也会在导入时被忽略，仅管理员可写入站点级数据；`user_id` 一律重映射为当前用户，不会越权。3) 新增 `POST /api/v1/auth/delete-account` 注销当前账号：级联删除该用户 `sessions/records/trade_orders/activities/watch_groups/user_settings` 并尽量删除头像文件；保护最后一个管理员(不可删除)。个人信息「数据」区新增红色「注销账号」入口，并加**输入登录名确认**的二次验证(需准确输入用户名才能确认注销，防手误)。tsc 无错误。

11. Fire 花标按 ChatGPT 新对话动画重新构造 —— 彻底移除连续小波浪，外轮廓仅保留 7 个清晰的大体块，采用非对称云朵结构、宽缓连接、饱满底瓣和更大的内部留白；中心 Fire 趋势箭头同步放大、简化折点并加粗圆角描边。点击旋转的弹簧动画保持不变。

12. Fire 花标恢复默认设计 —— 撤销本轮 ChatGPT 云朵轮廓与中心符号比例调整，恢复项目原有默认外轮廓、中心趋势箭头、描边粗细和缩放比例；点击旋转交互继续保留。

13. 设置页编辑入口收敛为页面级统一按钮 —— 隐藏各设置卡片标题旁重复的编辑铅笔，在设置内容标题栏仅保留一个带文字的“编辑”按钮，并根据当前侧栏栏目开启对应编辑态；首页导航会同时开放首页与应用导航。标题副文案显示“只读浏览/正在编辑”，复杂列表原有保存、取消、拖动、删除与添加逻辑保持不变；只读态注册开关同步禁用，避免误改。

14. 修复设置标题栏工具与编辑按钮重叠 —— 搜索、编辑、置顶与风格切换全部保留原有 32px 图标尺寸；编辑铅笔正式移入窗口标题栏组件并放在搜索右侧，顺序为“搜索 → 编辑 → 置顶 → 风格切换”。加载中、切换栏目和内容重渲染时按钮始终连续存在，不再出现编辑按钮缺席留下的空洞；页面标题为完整工具组预留固定安全区。

15. 个人信息复制按钮与下方操作列右对齐 —— 桌面端为个人资料字段列补齐与数据备份、危险操作面板一致的 12px 右内边距，使昵称、邮箱、用户名三行复制按钮与导出、导入、清空、注销按钮落在同一条垂直基线上；移动端保持满宽。

16. 个人信息值区收紧并让复制按钮跟随文字 —— 昵称、邮箱、用户名的复制图标改为紧跟各自文字尾部；字段标签列收紧并消除中间空白，长邮箱保留省略保护。

17. 修复资产分析收益率趋势的虚假盈亏 —— “简单加权”不再以当前数量回填全年并把首尾市值差冒充累计盈亏：按成交订单逐日还原实际数量，简单加权改为账本累计盈亏 ÷ 实际投入资本，顶部累计盈亏使用已实现+未实现经济账本；时间加权把周末/节假日现金流归入下一有效行情日，首次可确认旧仓按外部转入处理。无订单持仓从 `updatedAt` 起纳入并显示覆盖率，不再伪造年初仓位；订单上限提高至 5000，趋势缓存升级 v3 并纳入成本/更新时间签名。

18. 修复本月盈亏误显示全历史累计金额 —— 收益率趋势顶部金额改为与周期按钮同源：本月、近 1 月、近 6 月、本年、近 1 年与自定义区间分别显示对应区间盈亏，只有“全部”显示累计盈亏；区间以开始前最后一个有效交易日为基准，包含首个交易日变化。资料不完整的旧仓首次纳入时从 0 盈亏开始，避免此前浮盈一次性计入本月；趋势缓存升级 v4。

19. 收益趋势跑赢跑输文案跟随盈亏 —— 当前所选周期盈利时，组合图例显示“跑赢”；亏损或持平时显示“跑输”。切换周期或自定义区间后即时同步，标普 500 与右侧相对收益差值仍保持原口径。

20. 新增多币种资金系统并纳入账户净资产 —— 资金账本支持全站统一七币种。“新增资金记录”的金额改为透明输入层与逐字符彩色展示层：每个已输入数字按红橙黄紫蓝青绿顺序独立着色，千分位逗号保持中性；末尾闪烁彩色光标预告下一位数字颜色并带轻微光晕。点击时输入位置固定在末尾，避免透明层错位。底层继续剔除所有非数字字符、归一前导零并限制 1 万亿元，千分位仅展示，提交仍为纯数字。资金记录随网站数据导入导出迁移；历史订单每页 5 笔，当日订单每页 9 笔；新增 `/api/v1/funds`。
21. 修复深色模式金额彩虹数字被原生文字遮挡 —— 深色模式全局输入文字色覆盖了透明捕获层，原生浅色数字挡住后方彩虹数字。现强制金额输入层的文字、WebKit 文字填充、原生光标与选区透明，只保留键盘输入能力，逐位彩色数字和下一位彩色光标可正常显示。

### 安全修复

1. 网站数据导入导出与个人信息页全量安全加固 —— 导入改为固定逐表字段白名单；新增/跨用户冲突 ID 自动生成新 ID 并重映射订单与分组引用，阻断跨用户数据归属夺取；严格校验版本、字段、长度、JSON、数值和引用归属，限制 10MB、各表条数、总 7.5 万行与调用频率；导入前备份失败即停止；站点设置使用允许键白名单；敏感响应 `no-store`，Web 登录不再向页面 JS 返回 token；清空、注销及修改邮箱均由服务端验证当前密码，上传与敏感操作增加限流；新增 `security_audit` 持久安全日志。恶意字段、越权订单、无密码破坏操作均实测拒绝；跨用户同 ID 导入实测原归属不变且副本自动换 ID；有效备份预览通过；npm audit 0 漏洞、tsc 与生产构建通过。

### 新增

1. 首次部署引导页视觉稿 —— 新增 `public/first-run-guide.html` 独立预览页，展示服务启动状态、管理员创建、基础配置步骤、数据持久化说明及群晖 `data` / `uploads` 挂载提示；当前仅供确认视觉方案，尚未接入首访路由。

### 修复

1. Docker 挂载后全球经济热图国旗缺失 —— 群晖将宿主机 `uploads` 挂载到 `/app/public/uploads` 后，旧容器或默认素材复制失败会遮住镜像内置旗帜文件，热图 tooltip 因图片 404 只显示「🌐」。上传资源路由现在在挂载目录未找到文件时回读镜像内置 `resource-default`，保留用户自定义资源优先级，线上默认国旗不再丢失。

2. 兼容富途 OpenD 主机地址格式 —— 修复设置中误填 `http://192.168.x.x` 导致容器 TCP 明明可达但 Fire 判断端口未开放的问题。保存设置、读取旧配置和测试连接时统一将 OpenD 主机归一化为纯 hostname/IP，自动去掉协议、端口与路径。

3. GHCR 镜像内置富途 OpenD 桥接运行时 —— 修复线上容器 TCP 可连接 OpenD 但测试仍报 `spawn python3 ENOENT`：生产镜像此前未包含 Python。现在 runner 镜像内置 Python venv 与 `futu-api`，设置正确的 OpenD 主机和端口后无需手动进入群晖容器安装依赖。

4. 修复全站冒烟测试安全参数遗漏 —— 清空记录和修改邮箱测试此前未携带接口要求的当前密码，造成误报。现按真实前端协议补齐 `password` / `currentPassword`，并在 API 文档标明高风险操作的验证字段。

## v0.1.16 · 2026-08-24

> 修复美股盘前/盘后/夜盘当日盈亏「昨收」基准错位（富途 prev_close_price 落后一个常规交易日）。

### 修复

1. 修复美股盘前/盘后/夜盘当日盈亏「昨收」错位（富途 `prev_close_price` 落后一个交易日） —— 排查「美股当日盈利还未更新」：盘前时段富途快照的 `prev_close_price` 会落后一个常规交易日（如周一盘前返回上周四收盘，AAPL 报 311.30，而真正上周五收盘是 309.35），但富途官方 `pre_change_val` 是基于真正前一常规收盘计算的（AAPL 盘前 310.35 − 309.35 = +1.05，正确）。改动：桥接脚本 `scripts/futu_quotes.py` 在扩展时段（盘前 / 盘后 / 夜盘）命中官方涨跌字段后，用 `prevClose = price − change` 反推当日基准，保证「昨收 / 今日涨跌 / 当日盈亏」使用同一基准；缺 `change` 时同样反推，避免回退到错误的旧昨收。修复前 `/api/v1/stock-detail` 对 AAPL 报 `price 310.4 / prevClose 311.30 / change +1.05`（`price − prev = −0.95`，涨跌互相矛盾），修复后 `price 310.4 / prevClose 309.35 / change +1.05`（`price − prev = +1.05` 一致）。仅影响美股扩展时段行情来源，不改港股 / A股、不改分享页。实测 `/api/v1/quotes` 全部美股 `source=futu`、`prevClose` 与 `price − change` 严格一致；tsc 无错误、npm run build 通过、冒烟测试 82 项全 PASS。
1. 素材库/附件管理操作按钮统一大小、配色与间距 —— 按反馈放大并统一素材库、附件管理里过小的操作按钮：1) 图标素材、股票 / 券商 / 分组图标列表的操作列（编辑 / 预览 / 删除）此前尺寸与配色不一致（删除图标曾为 3px / 4px、部分按钮为圆钮配 16px 图标），统一为 `h-7 w-7` 圆角 8px 边框按钮——编辑 / 预览用中性 hover（`hover:bg-brand-hover hover:text-ink`），删除用红色语义（`hover:border-down/40 hover:bg-down/10 hover:text-down`），图标统一 14px，含 disabled 态；2) 操作列与「市场」列间距由 gap-2（8px）放宽为 gap-3（12px），三个操作按钮间 gap-1（4px）放宽为 gap-1.5（6px），消除「与市场列贴太近」观感；「操作」列宽由 72px 加宽到 96px，编辑态「保存」按钮同步为 h-7 圆角框（不再布局跳动）；3) 生效文件：`LibraryAttachmentsView`（附件管理-素材库）、`AssetLibraryView`（素材库 图标 / 券商 / 分组 tab）、`WatchGroupSheet`（自选股分组删除按钮）、`CelebsManageModal`（名人持仓删除按钮 × 3）；删除按钮从几乎不可点的小图标变为清晰的 28×28 圆角框按钮，与相邻编辑 / 预览按钮完全同尺寸同风格，默认色统一 `text-muted`（hover 才加深）。实测：三按钮均 28×28、列间距 12px、按钮间距 6px。tsc 无错误、npm run build 通过。
1. 素材库图标列表显示真实文件路径（可点击复制） —— 附件管理-素材库（图标素材列表）行内新增真实路径展示：`assets.url` 存的是 encodeURIComponent 编码形式（如 `/uploads/asset/stock/US/%E8%8B%B9%E6%9E%9CAAPL.png`），此前行内仅显示名称 / 代码不显示路径，用户无法查看或复制素材实物文件地址。修复：1) 组件新增 `realPath()` 把 url decodeURIComponent 解码为可读路径（`/uploads/asset/stock/US/苹果AAPL.png`，与磁盘文件名一致），渲染在名称、代码下方第三行；2) 点击该行即复制真实路径（Clipboard API 优先，非安全上下文回退 execCommand 写剪贴板），复制成功 Toast「已复制真实路径」；无 url 的素材显示占位「（无本地文件）」且不可点。实测：英伟达 / 苹果 / 腾讯控股 / 贵州茅台等常用股票 url 均指向磁盘真实文件，解码路径与 `public/uploads` 一致。tsc 无错误、npm run build 通过。
1. 素材库国家/地区旗帜改用本地开源高清圆形 SVG（hatscripts/circle-flags） —— 素材库「国家/地区旗帜」tab 此前默认用 flagcdn.com/w80/{flagCode}.png（外部 PNG，80px，依赖外部加载）。按用户要求替换为 GitHub 开源高清 SVG 并本地保存：调研对比 lipis/flag-icons（4x3 矩形 640×480）、hampusborgos/country-flags（7410×3900 矩形）、hatscripts/circle-flags（512×512 圆形，mask 裁切）三库；用户明确要圆形旗帜，选定 hatscripts/circle-flags——每国一个 `{iso2}.svg`、viewBox 512×512、自带 `<mask><circle r=256/></mask>` 圆形裁切、MIT（先用矩形版后按用户要求换成圆形版）；脚本批量下载 249 国圆形 SVG 本地保存到 `public/uploads/asset/flag/`（`cn.svg` 705B 与用户提供逐字节一致），缺失的 bq / bv / hm / sh / sj / um 6 国映射到真实或同源旗帜（bq→bq-bo、bv / sj→no、hm→au、sh→sh-hl、um→us，全部含 mask 圆形）；旗帜 tab 默认来源改为本地 `/uploads/asset/flag/{iso2}.svg`（custom?.url 上传的自定义旗帜仍优先）。实测：249 个 SVG 全部含圆 mask、Next 正常 serve（200）。tsc 无错误、npm run build 通过。
1. 全球经济热图国旗改用素材库国旗（本地圆形 SVG） —— 全球经济热图（/global?section=heatmap）里的国旗此前用 flagcdn.com/w40/{flagCode}.png（外部 PNG，依赖外部加载），与素材库国家/地区旗帜不一致。修复：地图悬停 tooltip 的 flagMarkup 与右侧经济看板排行的 CountryFlag 两处默认来源，从 flagcdn PNG 统一改为本地素材库圆形 SVG `/uploads/asset/flag/{flagCode}.svg`（hatscripts/circle-flags，512×512 圆裁切），用户上传的自定义国旗（countryFlags 自定义素材）仍优先；tooltip 圆形容器与看板 economy-ranking-flag 保持 20px 圆形显示。GlobalEconomyHeatmap 组件内 flagcdn 引用已全部清除。tsc 无错误、npm run build 通过。
1. 附件管理分类重构：财报文件独立成一级 tab，文件空间改名文件管理 —— 把「财报文件」从素材资源内部的二级界面独立出来，作为附件管理的一级分类 tab（位于「文件管理」右侧），分类变为 文件管理 | 财报文件 | 素材资源：1) category 支持 docs / reports / library 三态；新增「财报文件」tab（文档图标）；「文件空间」改名「文件管理」；2) FinancialAttachments 从 LibraryAttachmentsView 抽出并导出为可独立复用的组件（新增 standalone 模式：独立显示搜索栏，取消原二级面包屑「图标素材 / 财报文件」），素材资源内部的「财报文件」二级入口按钮移除；3) 财报文件 tab 复用 `/api/v1/financial-reports` 数据（市场 / 交易所 / 公司 / 报告期分组、搜索、分页、删除）。实测三个 tab（文件管理 / 财报文件 / 素材资源）正常渲染。tsc 无错误、npm run build 通过。
1. 素材资源进入加速：分页缓存秒出 + 预取后续页 —— 素材库（素材资源）首次进入显示「正在读取素材库」，每次挂载都重新请求分页数据。新增前端分页缓存：1) 缓存键 = `fire:lib-page:{type}|{market}|{q}|{sort}|{page}`，值 = `{at, data}`，5 分钟 TTL；2) 首次进入 / 切分类 / 翻页时，若 localStorage 有当前页缓存则立即渲染并保底显示（loading 保持 false，避免闪「正在读取」），再后台刷新覆盖；3) 请求成功后写当前页缓存，并预取第 2、3 页写入缓存（翻页秒出）；4) 无缓存时才显示 loading。实测：素材库 API 单页约 0.05s，首次进入 content 区 loading 约 0.8s 消失；二次进入 / 翻页由缓存秒出。tsc 无错误、npm run build 通过。
1. 财报文件改为类网盘文件夹导航（市场→交易所→股票→年→年报/半年报/单季报） —— 把财报文件从平铺分组改为参照主流存储网站（网盘）的文件夹导航视图：1) 面包屑逐级下钻 全部 → 市场（美股/港股/A股）→ 交易所（NASDAQ/纽交所等，按 exchange 细分）→ 股票 → 财年 → 报告类型，每层文件夹卡片显示该目录文件数，点击进入下一层，文件夹回到上一级（面包屑可任意回退）；2) 股票层文件夹图标默认使用素材库股票图标（useAssetIcons.stockIcons，键 市场:代码），无图标回退文件夹图标；3) 最内层（报告类型）用文件表格展示（类型/文件/大小/删除）；4) 报告类型由 fiscalPeriod + reportType 推导（FY 年报、H1/H2 半年报、Q1~Q4 单季报），新增 reportCategory / reportCategoryRank 归类函数。实测层级下钻 全部→美股→NASDAQ→Apple Inc. 正常。tsc 无错误、npm run build 通过。
1. 附件管理 URL 状态持久化（刷新保持分类与财报目录，URL 用稳定数字码） —— 附件管理刷新后分类与财报文件目录会重置回默认。新增 URL 状态持久化：1) 附件管理分类写入 `?category=docs|reports|library`（AttachmentsView category 初始从 URL 读、切换时 `history.replaceState` 写回，docs 默认时移除参数）；2) 财报文件文件夹路径写入 `?view=市场/交易所/公司代码/财年/报告类型码`（FinancialAttachments view 初始从 URL 解码恢复、下钻 / 回退时写回）。参考主流存储网站用稳定代码而非中文长名：公司用 companyCode（AAPL）、报告类型用数字码（年报=01 / 半年报=02 / 单季报=03），显示时才由 reportCategoryName 映射回中文，公司名 / 中文报告类型不进 URL。实测下钻至 美股→NASDAQ→Apple→2026→单季报 后 URL 为 `?category=reports&view=US/NASDAQ/AAPL/2026/03`（简短），刷新后面包屑恢复「单季报 / 2026 财年」、视图保持。tsc 无错误、npm run build 通过。
1. 财报目录 URL 保留可读分隔符（去掉 %2F 编码乱码） —— 用 URLSearchParams.set 写 `?view=` 时会把路径里的 `/` 编码成 `%2F`，导致地址栏出现 `US%2FNASDAQ%2FAAPL...` 的乱码。改为手动拼 query 字符串（`encodeURIComponent` 每段后按 `/` 拼接），浏览器地址栏显示可读的 `?view=US/NASDAQ/AAPL/2026/03`；读取端仍用 `URLSearchParams.get('view')` 按 `/` 分割，兼容不变。实测 URL 无 `%2F`、view 还原 5 段正确。tsc 无错误、npm run build 通过。
1. 财报文件夹导航各层配图标（市场/交易所/股票） —— 财报文件网盘式文件夹导航每层配图标，参考主流存储网站与 TradingView 指数图标：1) 市场层（美股/港股/A股/日股/韩股）默认使用素材库市场图标（useAssetIcons marketIcons，键 market 大写），文件夹卡片显示对应市场 logo；2) 交易所层（NASDAQ/纽交所等）用 EXCHANGE_ICON_CODE 映射——交易所自家公司在素材库的图标（NASDAQ→NDAQ、NYSE→ICE、AMEX→CME），无则市场图标兜底；3) 股票层沿用素材库股票图标（stockIcons，键 市场:代码）；4) 新增标普500 市场素材与本地保存 TradingView 指数图标（标普500SPX.svg 下载自 tradingview s3-symbol-logo，美国US.svg 兜底；DJI/IXIC/NDX 等 TradingView 接口 403 限流未直连获取）。实测市场层「美股」文件夹显示美股US.svg 图标。tsc 无错误、npm run build 通过。
1. 文件夹图标背景深色适配（去掉深色模式下的白色底块） —— 财报文件夹卡片的图标容器此前用 bg-brand-light/60（浅色底）+ img object-cover（拉伸填充），在深色模式下图标周围露出一圈白底方块，很突兀。修复：图标容器背景改为透明（bg-transparent），图片改为 object-contain + p-1（保持旗帜 / SVG 原始比例居中，不再拉伸填充）。实测深色模式下「美股」文件夹图标周围背景为深色 / 透明、无白色底块，整体协调。tsc 无错误、npm run build 通过。
1. 财报页删除说明文字 + 市场/指数文件夹支持自定义图标上传（相机） —— 1) 删除财报文件详情顶部一行「共 N 个财报文件 · 按 市场分类 / 股票名称 / 年份 / 报告类型 递进浏览」说明文字（信息冗余，层级已由面包屑体现）；2) 市场 / 指数层文件夹图标支持自定义上传：文件夹图标 hover 显示相机角标，点击弹文件选择，上传走 `/api/upload`（kind=asset folder=market）+ 更新素材库 market 图标记录（`/api/assets`），成功后派发 fire:assets-updated 全局刷新，市场图标全局生效（自选股 / 持仓 / 素材库同步）；图标缺失的其他层仍显示默认文件夹 / 日程 / 文档图标。实测说明文字消失、美股卡片 hover 出现相机角标与 file input。tsc 无错误、npm run build 通过。
1. 财报「指数/交易所」层补上自定义图标上传相机（此前仅市场层有） —— 上一步给市场 / 指数文件夹加了相机上传，但实测进入美股后的交易所层（NASDAQ 等）文件夹卡片没有相机——只在最外层（市场）的 folderCard 传了 camera 参数，交易所层的调用漏传，导致角标不渲染。修复：1) folderCard 新增 onUpload 回调参数，相机角标 file input 弹文件选择后改调 onUpload；2) 新增 uploadExchangeIcon(market, exchange, file)——交易所层图标按 stockIcons[市场:EXCHANGE_ICON_CODE[e]]（如美股 US:NDAQ）取图，故把它以 type=stock（market=市场、code=NDAQ、folder=stock/{市场}）落库到素材库，成功后派发 fire:assets-updated 全局刷新，交易所图标即由素材库同名股票图标提供（与交易所自家公司图标共用一套）；3) 市场层调用同步改为传 onUpload 走 uploadMarketIcon。实测进入 美股→NASDAQ 层卡片 hover 出现相机角标。tsc 无错误、npm run build 通过。
1. 财报文件夹图标统一为圆形（市场/交易所/股票） —— 财报文件夹卡片图标展示不一致：市场层招牌是圆形国旗（circle-flags 圆形 SVG），而交易所层取的是方形公司 logo（NASDAQ→NDAQ 蓝底方块），视觉上一圆一方不协调。按用户要求把文件夹图标统一为「圆形」：1) 图标容器由 rounded-[10px] 改为 rounded-full（真正圆形）；2) 图片由 object-contain + p-1 改为 object-cover（铺满圆形容器、正方形 logo 被裁成圆形，国旗因本身是圆 SVG 仍完美贴合）；3) 相机角标遮罩同步改为 rounded-full。改动后 美股（圆国旗）与 NASDAQ（圆形蓝底 N）外观一致，均为圆形。tsc 无错误、npm run build 通过。
1. 附件管理配色统一为蓝色（文件夹图标/头部图标与上传按钮同色） —— 附件管理-文件管理页配色不统一：文件夹图标与左上「附件管理」头部图标是橙色（琥珀色），而「上传文件」主按钮是蓝色，一橙一蓝显得割裂。按用户要求统一为蓝色：把附件主题变量 --attachment-folder（文件夹主色）与 --attachment-folder-soft（文件夹浅底）从橙色改为与 --attachment-accent 一致的蓝色——浅色模式橙色 #e99016→#2382f7（浅底 #fff4d8→#eaf3ff），深色模式 #f2b24d→#66aaff（浅底 rgba(242,178,77,.12)→rgba(60,145,255,.14)）。该变量驱动所有文件夹相关图标：头部附件管理图标、行 / 网格 / 详情 / 弹窗 / 树形视图的文件夹图标及文件夹浅色底，全部由橙变蓝，与上传按钮同色。tsc 无错误、npm run build 通过。
1. 美股 top 股票财报自动补全（SEC EDGAR 10-K/10-Q/20-F） —— 财报文件此前几乎为空（只有手动上传的 AAPL Q1）。新增脚本 scripts/backfill-sec-financial-reports.mjs 从 SEC EDGAR（美股官方）补齐持仓 / 自选里美股近一年财报：1) 读取 records 里美股代码（去 .AM/.N 等交易所后缀），用 SEC company_tickers_exchange.json 映射 ticker→CIK / 交易所 / 公司名；2) 拉取 data.sec.gov 的 submissions，取最新年报（10-K，外国发行人用 20-F）+ 最近 4 个 10-Q（美股的 Q4 不单独发 10-Q 而是并入年度 10-K）；3) 按「财政年末月」推导 fiscalYear / fiscalPeriod（AAPL 2025-12-27→2026 Q1 等），保存 SEC 主文档（HTML）到 public/uploads/reports/US/{交易所}/{代码}/{财年}/{期}/，并写入 financial_report_files（file_kind=filing、source=sec）；4) 幂等（跳过已存在）、自动排除基金 / ETF（ProShares、Vanguard 等，按公司名 trust / fund / proshares 等特征）。实测补齐 20 家美股共 83 份（AAPL/NVDA/MSFT/AMZN/GOOGL/META/TSLA/MU/WDC/SNDK/INTC/INTU/MSTR/NFLX/RKLB/GOOG/SPCX + NIO/XPEV/FUTU 的 20-F 年报），financial_report_files 从 1 行增至 84 行。UI：file_kind 新增 filing 类型，财报附件表格标注「SEC 文件」（FinancialAttachments）与「SEC」徽标（FinancialPanel 个股详情-财务-财报附件），点击在新窗口打开 SEC 原文。tsc 无错误、npm run build 通过。
1. 财报公司文件夹主显示中文名、下方股票代码 —— 财报文件公司层文件夹此前主显示 SEC 英文公司名（Apple Inc. / NVIDIA CORP）且下方是「代码 · N 份」。改为「主显示中文名称、下方为股票代码」：从素材库 stock 行（assets.name，如 AAPL→苹果、NVDA→英伟达、MSFT→微软、AMZN→亚马逊）建立 代码→中文名 映射（FinancialAttachments 内 stockNameMap），公司层 folderCard 主文本用中文名（无素材库映射时回退 SEC 公司名 / 代码），下方只显示股票代码（如 AAPL）。市场层（美股 / 港股）本就是中文，交易所层（NASDAQ）维持。实测公司层显示 苹果 / AAPL、英伟达 / NVDA 等。tsc 无错误、npm run build 通过。
1. 财报文件读取提速（保留 sec 冷编译 + 列表客户端缓存秒出） —— 财报文件 tab 首次进入显示「正在读取财报文件」很久。排查：dev 服务器（next dev）首次命中 /api/v1/financial-reports 与 /api/attachments 会做 Webpack 一次性冷编译（实测各约 10s），之后降到 36ms；此外每次进财报文件 tab 组件重挂载都会重新请求并闪加载态。提速：1) 财报文件列表新增客户端缓存（key=fire:finreports:v1，5 分钟 TTL），重进 tab / 切回直接读缓存秒出首帧、loading 保持 false 不闪「正在读取」，仅无缓存时才显示加载态，后台刷新再覆盖；2) 已对 dev 服务器关键路由（financial-reports / attachments / assets）做预热，冷编译后热态 reports 约 18-26ms、页面 shell 约 365ms。tsc 无错误、npm run build 通过。
1. 附件管理分类 tab 图标垂直居中对齐（去掉跨行导致偏下） —— 附件管理顶部三个分类 tab（文件管理 / 财报文件 / 素材资源）的图标相对文字偏下，尤其「素材资源」最明显。排查：.attachments-space-tabs > button 是 grid（18px auto 两列），而图标被 .attachments-space-tabs > button > svg 设为 grid-row:1 / 3（跨两行网格），但这些 tab 只有「图标+文字」一行内容，跨两行会让图标垂直居中在更高的区域里而比文字偏下（移动端媒体查询里恰好是 grid-row:auto 所以不偏）。修复：基础规则改为 align-self:center，让图标与文字在同一行垂直居中（移动端原 grid-row:auto 保留不变）。实测三个 tab 图标与文字垂直对齐、不再偏下。tsc 无错误、npm run build 通过。
1. 素材资源筛选胶囊配色统一为中性灰（去掉橙色高亮） —— 附件管理-素材资源（LibraryAttachmentsView）的分类 tab（股票/加密货币/贵金属/市场）与股票市场筛选胶囊（全部/美股/港股/A股/日股/韩股）悬停高亮用了橙色 #FF9828（「港股」胶囊悬停呈整块橙色），与全站中性色标准不符、也未与页面其余（蓝 accent/中性白）统一。排查：橙色来自组件内联 hover:bg-[#FF9828] hover:text-black 与全局 .seg-active:hover（注释写明「中性灰白取代浅蓝」却误用橙色）；素材库分页 Pagination 也有同款橙色 hover。修复：1) LibraryAttachmentsView 分类/市场胶囊 hover 由 #FF9828 改为 hover:bg-brand-hover hover:text-ink（中性浅灰+深字，深色模式自动适配）；2) 全局 .seg-active:hover 由 #FF9828 改 bg-brand-hover text-ink（6 个组件统一，不再橙色）；3) Pagination 按钮 hover 由 #FF9828 改 hover:bg-brand-hover hover:text-ink。注：设置-主题「V1 富途橙」的 sv-accent 属可选主题色，未改动。tsc 无错误、npm run build 通过。
1. 全站橙色高亮统一为中性灰（同款 #FF9828 一并清理） —— 附件管理排查发现橙色 #FF9828 高亮后，按用户要求做全站统一：首页（HomeContent）、资产库（AssetLibraryView：分类/市场筛选触发器打开态、圆形小按钮、列表项）、财报日历（EarningsCalendarView 分类/市场胶囊）、全球经济（GlobalPreviewView 双侧胶囊）、我的持仓（HoldingsView 排序/分页按钮）、自选股（QuotesView 分页按钮）共约 20 处 hover:bg-[#FF9828] hover:text-black（及打开态 bg-[#FF9828] text-black、深色 dark:hover:bg-[#FF9828]）统一改为 hover:bg-brand-hover hover:text-ink（中性浅灰+深字）、打开态 bg-brand-hover text-ink、深色 dark:hover:bg-white/10；并将 API 文档编辑器光标色 caret-color #ff9828 改中性灰 #6b7280。保留「设置→主题」V1 富途橙等可选主题预设（--sv-accent / .sv-v1 logo 与导航 / SettingsWindow 色卡）——那是刻意保留的选择型主题色，非站点高亮。实测 /、/earnings、/holdings、/watchlist、/library 均 200；tsc 无错误、npm run build 通过。
1. K 线「股票对比」报错 yAxis not found 修复 —— 个股 K 线在「涨跌幅对比」模式报运行时错误 yAxis "O" not found。排查：markLine.data:[{yAxis:latestPrice}] 里的 latestPrice（新增价）与 baseline 直接取 data.values 首尾值，行情接口可能返回字符串（类型标注是 number[] 但运行时是字符串），传给 yAxis 被 ECharts 当成轴名引用而抛「yAxis not found」，对比模式触发重渲染必现。修复：① baseline / latestPrice 改为 Number(...) 强转，并用 Number.isFinite 守卫——非有限数字就不画该 markLine（折线图与基准线 markLine 都加守卫）；② 对比模式下 priceSeries 不需要「最新价线」（归一化 % 轴，画最新价反而失真），直接 drop markLine（markLine: undefined）；③ compareSeries 渲染条件由 compareSeries.length>0 改为 isComparing && compareSeries.length>0，避免 compareSeries 残留时 yAxisIndex: subCount+1 越界。tsc 无错误、npm run build 通过。

---

## v0.1.15 · 2026-08-22

> 补全自选股相关 ETF 目录并接入 DeepSeek 视觉识别：截图导入云端 OCR 优先、Apple Vision 兜底。

### 新功能

1. K 线「涨跌幅比较」（Google Finance 风格，独立工具栏入口） —— 个股详情页 K 线图新增「涨跌幅比较」，入口独立在 K 线工具栏（图表设置齿轮左侧），不再收进设置弹窗：1) 工具栏「比较」按钮（带折线图标，含已选数量），展开提供搜索框 + 候选列表（常见美股比较标的 AAPL / TSLA / MU / AVGO / MSFT / GOOGL / AMZN / META），带实时价格与红绿涨跌幅；2) 选中后显示图例胶囊（可删除），同时把主图与各比较标的统一为折线并按「相对视图区间首价的涨跌幅%」归一化，共用同一个 % y 轴（不再主图价格轴 + 比较 % 轴双轴混排）；3) 图表下方渲染对比表格（股票代码 / 价格 / 涨跌额 / 涨跌幅 / 昨收盘），主股票与每个比较标的各一行；4) 比较标的行情经 `/api/v1/quotes` 批量拉取、搜索走 `/api/v1/search` 防抖，切换股票自动清空。另把「今年至今」从周期按钮收进「周期 → 长周期」菜单（与季K并列）。比较逻辑全部收敛到 StockKline 组件内部。tsc 无错误、npm run build 通过。
1. 个股详情报价头部对齐 Google Finance（双块布局 + 盘前/盘后 + 涨跌颜色跟随） —— 按参考图（Google Finance）打磨个股详情页现价头部，并恢复「股价跟随涨跌」的精华逻辑：1) 涨跌箭头改用长桥同款实心等腰三角（viewBox 0 0 12 12，向上 M6 2.75 L9.72 9.25 L2.28 9.25 Z / 向下镜像），紧贴涨跌幅文字前并继承涨跌色；2) 股价颜色分时段——盘中左侧「今天」价格跟随涨跌色，夜盘/盘前/盘后时左侧价格为模式黑、右侧扩展股价跟随涨跌色；3) 主「今天」块以当日常规收盘为基准，右侧「盘前/盘后交易 · HH:mm」块（带月亮图标）相对常规收盘计算涨跌；4) 状态行改为「休市： 8月21日, UTC-4 16:00 (美东)」——时间不含秒、去掉币种码，按市场显示当地时区中文名（美东/香港时间/北京时间）；5) 有返回箭头时给现价行补左侧占位（pl-10），使大价格 `$` 与上方股票图标左边缘严格对齐；6) 夜盘 / 休市（美东 20 点后）也延续显示最近一次盘后交易，避免扩展时段误标「夜盘」。tsc 无错误、npm run build 通过。
1. 个股详情：港股/A股时段按当地时区判定 + 价格去货币符号 —— 1) 个股详情页底部时段标签对非美股写死「盘中」（`currentPhase` 恒为 REGULAR），导致港股 / A股实际已休市仍显示「盘中」。修复：非美股改用 `marketSessionState(market, now)` 按当地时区判时段——pre→盘前、regular→盘中、post→盘后、overnight→夜盘、closed / lunch→休市。2) 大价格带货币符号（`¥96.25` / `HK$10.24`），改用 `fmtNumMarket`（去掉符号、按市场保留小数：美股 3 位、其余 2 位）。tsc 无错误、npm run build 通过。
1. K 线技术指标与图表设置自动记忆 —— 技术指标（MA / EMA / BOLL / MACD / KDJ / RSI / 成交量 / 成交额）、MA 周期配置、前 / 后复权、图表样式、MA 均线显示 / 数值开关原来刷新 / 切换股票后都重置。新增 `fire:kline-settings` 持久化上述设置：`readKlineSettings` 校验后还原（indicators / maConfigs / adjust / style / maLinesVisible / showMAValues），持久化 effect 在改动时自动保存，进入 / 切换股票用保存值还原且不再把样式覆盖回第一个基础样式。tsc 无错误、npm run build 通过。
1. K 线夜盘可选 + 选「全天」不再自动切分钟 —— 1) 现在有夜盘行情，时段下拉里的「夜盘 20:00-03:59」已启用（原 `available:false` 灰色禁用）。2) 修复“选全天右边自动选周期 1 分钟”：原点击时段按钮 / 菜单会强制 `setRange("DAY")` 进分钟线；改为——点击「全天」保持当前周期（日K等），只有选「夜盘 / 盘前 / 盘中 / 盘后」或手动在 K线周期 里点「1分 / 2分…」才进入分钟线。tsc 无错误、npm run build 通过。
1. 修复美股周K只显示 2010-2016 旧数据 —— GOOGL 周K点开后几乎空白 / 全旧。根因：富途 `get_history_kline` 带 `start=2010-01-01` 且区间内根数超过 `max_count` 时返回“最早”的 `max_count` 根（OLDEST-first），美股周K 2010→今约 830 周 > `max_count=320`，只回 2010-2016；月 / 季 / 年K 总根数 < 320 所以正常。修复：把 `start` 改为按 `max_count` 和周期（周=7 天 / 月=31 / 季=92 / 年=366）回推的“最近窗口”，让富途返回最近 `max_count` 根。实测 GOOGL 周K 现在返回 2020-07-06 → 2026-08-17。tsc 无错误、npm run build 通过。
1. K 线图记住上次选择的周期/时段/分钟 —— K 线图每次进入 / 切换股票都默认日K。修复：把 K 线视图（周期 + 时段 + 分钟）持久化到 localStorage（`fire:kline-view`）——首次实现只记周期（`fire:kline-range`），但选「盘后」刷新后被旧周期（如 5日）覆盖。现改为整体记录 `{ range, session, minutes }`，改动即由 persist effect 自动保存，初始 / 进入 / 切换股票都整体还原：选「盘后」刷新仍是盘后、选「5日」刷新仍是 5日、选「1分」刷新仍回 1分。选周期（5日 / 日K… / 季K）会把时段重置为全天；选具体时段（夜盘 / 盘前 / 盘中 / 盘后）进入分钟线并记录。tsc 无错误、npm run build 通过。
1. 修复美股当日盈亏在盘后 20:00 前被“重置” —— 我的持仓 - 美股当日盈亏在美东 19:21（盘后但未到 20:00 结算）被重置。根因：`usExtendedQuote` 的 `previousClose` 用 `regularMarketPrice ?? previousClose` 兜底，而盘后 Yahoo `regularMarketPrice` = 当日常规收盘，导致 `change = 盘后价 − 当日常规收盘`，只剩盘后波动、视觉上像当日盈亏归零。修复：盘后（`wanted=AFTER`）改用 `meta.previousClose`（昨收）作为当日基准，`change = 盘后价 − 昨收 = 完整当日涨跌`；盘前 / 常规仍用 `regularMarketPrice`（避免夜盘刚结束时 `previousClose` 滞后）。富途路径用 OpenD `prev_close` 字段，本身正确。tsc 无错误、npm run build 通过。
1. K 线成交量/成交额柱去雾化（提升不透明度） —— 下方成交量 / 成交额柱使用 `rgba(229,72,77,.48)` / `rgba(10,167,125,.46)` 半透明填充，视觉上像蒙了一层薄膜。修复：把红 / 绿柱不透明度提升到 `.9` / `.88`（接近实色，红涨绿跌方向不变），去雾化。tsc 无错误、npm run build 通过。
1. K 线划过主图联动高亮下方成交量/成交额柱（已恢复） —— 尝试做“鼠标划过主图联动高亮下方成交量 / 成交额柱”：给 bar 系列加 `emphasis`、监听 `updateAxisPointer` 并 `dispatchAction highlight / downplay`。按用户反馈已恢复（回退 `emphasis` 与 `updateAxisPointer` 监听），保留 `gridLeft=30` 对齐修复与副图十字线标签处理。tsc 无错误、npm run build 通过。
1. K 线副图 Y 轴十字线标签处理（避免长串数字与 tooltip 不一致） —— 成交额 / 成交量副图 Y 轴的十字线标签默认显示原始长串数字（如 `171,875,000.00`）与 tooltip 的 `1387.17万` 不一致；尝试用 `axisPointer.label.formatter` 改亿 / 万单位无效（ECharts 对子 grid 的十字线标签不稳定，出现不显示或「—」）。最终方案：直接隐藏副图 Y 轴的十字线标签（`axisPointer.label.show:false`），成交量 / 成交额数值以 tooltip 为准，彻底消除“对不上”；保留 `gridLeft=30` 对齐修复。tsc 无错误、npm run build 通过。
1. K 线主图与成交量/成交额副图横向对齐 —— 逐步定位个股详情 K 线“只开单个副图对不齐”：主图蜡烛与副图柱类目中心错位（diff 约 24px ≈ 3 根，且只在底部显示日期标签的副图上出现）。用 ECharts `convertToPixel` 逐下标量测定位：ECharts 会对「显示 X 轴日期标签的 grid」自动向左内缩约 24px（即使 `containLabel:false`），导致只开成交量或成交额（subCount=1）、或双开后底部成交量板（grid2）首根柱相对蜡烛左移。修复：主图 / 成交量 / 成交额 grid 统一用同一组常量（`containLabel:false`、`gridLeft=30`、`gridRight=64`）——`left=30` 正好落在 ECharts 的「天然内缩位」，所有 grid 不再各自内缩，实测 subCount=1 / 2 在首 / 中 / 末下标 diff ≤0.44px（亚像素），日期标签保留。同时把 grid 边距 / boundaryGap / containLabel 抽成共享常量，杜绝日后主副图分叉。tsc 无错误、npm run build 通过。
1. 2 倍杠杆 ETF 加 2x 徽标（颜色跟随市场色块，x 小写） —— 参考图给 2 倍杠杆 ETF 加「2x」角标，颜色与市场色块一致：新增 `lib/relatedEtfs.ts` 的 `isDoubleEtf(market, code, name)`（遍历相关 ETF 目录命中 badge / 名称的 2x / 2倍 / 两倍，再按名称兜底覆盖港 / A 股或中文名如「两倍做多闪迪」）与 `components/EtfDoubleBadge.tsx`（调用 `lib/marketBadge.ts` 的 `getMarketBadge` 取色：US 蓝 `#3b82f6`、HK 紫 `#8b5cf6`、A股 SH / SZ 粉红 `#e0919f`，白字、`rounded-[4px]` 圆角小方块、白色细边与图标分离，文字固定小写 `2x`）。角标按参考图叠放在**股票图标（圆形象）右上角**（`absolute -top-1 -right-1`），接入：自选股列表行图标（`QuotesView`，market / code 传记录市场）与个股详情 - 相关 ETF 列表图标（`StockDetailView`，market=US）；相关 ETF 列表原有的灰色「2X 做多 / 做空」徽标仍保留（用于方向说明），1x / 1.5x 不加角标。tsc 无错误、npm run build 通过。
1. 顶部指数行情切换时市场图标即时跟随 —— 首页顶部指数行情条每 5s 轮换指数，市场图标应随指数市场变化。原 `MarketIcon` 的 `<img>` 复用同一元素且带 `loading=lazy`，轮换时 src 已换但图片未即时刷新，视觉上「市场图标没跟上」。修复：给市场图标 `<img>` 加 `key={市场代码}` 强制按市场重挂载并立即重新加载，并移除 `loading=lazy`（小图标始终在首屏、需即时换图）。`marketIcons` 映射本身正确（CN → A股CN、US → 美股US 等）。tsc 无错误、npm run build 通过。
1. 修复首页刷新后顶栏行情/主题/登录短暂丢失 —— 首页顶栏右侧「指数行情条 + 迷你趋势、深浅色按钮、登录按钮」都依赖客户端水合后渲染：SSR 首屏 auth 为 checking、指数行情未拉到，导致刷新后顶栏空着一大块（登录 / 主题 / 行情条全消失）。修复：1) 顶栏右侧不再区分 checking / out——默认按访客渲染主题切换 + 登录按钮（登录后水合完成再替换为用户头像菜单），刷新即可见，不再有空档；2) `IndexTicker` 加载中由空白 42px 占位改为可见骨架条（行情图标 / 名称 / 价格 / 涨跌的脉冲占位），数据到达后再填充真实行情与趋势。实测 SSR 首屏已含登录按钮、主题按钮与行情骨架。tsc 无错误、npm run build 通过。
1. 登录支持用户名或邮箱 + 用户名占位文案统一 —— 登录逻辑明确为「用户名或邮箱均可登录」：1) 登录页 / 弹窗在「登录」状态新增「用户名登录 / 邮箱登录」切换，首字段占位「请输入用户名」/「请输入邮箱」（邮箱为 email 类型）；注册仍为用户名 + 密码（不强制邮箱），注册模式保留「3-20 位字母、数字、下划线或中文」占位。2) 后端登录按用户名精确匹配或邮箱大小写不敏感匹配（新增 `findUserByLogin`，`authenticateUser` 复用），登录路由标识符长度上限由 20 放宽到 254（兼容邮箱）、密码上限 128 不变。3) 邮箱绑定在「设置 → 个人信息」进行：`/api/auth/profile` 新增邮箱格式与唯一性校验（`findUserByEmail`，绑定他人邮箱返回 409「该邮箱已被其他账号绑定」），绑定后即可用该邮箱登录。实测：用户名注册 → 用户名登录 → 绑定邮箱 → 邮箱登录（大小写不敏感）全链路通过，绑定冲突返回 409，临时用户与测试邮箱已清理。tsc 无错误、npm run build 通过。
1. 登录改为长桥式弹窗（青绿配色，仅账号密码）+ 左侧配图可上传（管理员） —— 1) 应用内「登录」入口（顶栏 UserMenu / 首页登录按钮 / 访客未登录添加）由跳转改为默认弹出 `LoginModal`：根布局挂载全局弹窗，监听 `fire:open-login` 事件打开，Esc / 点击遮罩关闭；`/login` 单独页保留供直接访问与服务端守卫使用。2) 弹窗与单独页共用 `LoginForm`（仅账号密码：用户名 / 密码 / 确认密码、服务协议勾选、填写完整才可点的主按钮、去注册 / 去登录），配色统一改为参考长桥的青绿 `#1FBE9E`（focus ring / 复选框 / 协议链接 / 主按钮），去掉手机验证码、扫码登录、地区选择与第三方社交。3) `LoginModal` 采用长桥式分栏：左侧展示站点设置项 `loginSideImage`（管理员在「设置 - 网站形象」上传 / 粘贴 / 清除，未上传用品牌渐变兜底），右侧表单区；公开设置接口同步暴露 `loginSideImage` 供客户端读取，新增 `/uploads/login` 上传类别（10MB），上传与设置接口均做管理员鉴权。tsc 无错误、npm run build 通过、登录页 200、设置写入 / 读取 / 复位链路实测定通。
1. 修复资产盈亏分析页收益率曲线假深坑（时间加权误用累计现金流） —— 资产盈亏分析页「收益率走势」曲线此前一年都平在约 -110% 的一条深坑，而盈亏总额大数字显示 +52% 的正收益，两者矛盾。根因：时间加权每日收益率算成本来用「当日现金流」，该页却把 `pendingFlow` 按整个序列累计后再扣减，导致前期被反复扣到接近 -100% 而后拉平。修复为与资产分析页同口径——用当前交易日现金流（非交易日成交仍归入下一交易日），去掉累计扣减；`flow` / `actual` 输出字段无下游消费，改动安全。tsc 无错误、npm run build 通过。
1. 收益率趋势图对齐参考图干净版（右轴 + 蓝色基准线） —— 按参考图（长桥式干净收益率图）美化共享 `PnlTrendChart` 与两处图例：1) 纵轴移到右侧并隐藏轴线 / 刻度，仅留淡虚线网格，更接近参考图；2) 基准线（标普 500 等）由黄色 `#d7b900` 改为干净的蓝色 `#4a90d9`，资产分析页与资产盈亏分析页两处图例的基准色点同步统一为蓝色，与「我的」橘色对比更清爽；3) 面积渐变、虚线网格、tooltip 保持，横轴 MM/DD 自动避让。tsc 无错误、npm run build 通过。
1. 截图导入接入 DeepSeek 视觉识别（云端 OCR 优先，Apple Vision 兜底） —— 按官方文档接入 DeepSeek 图像理解（`deepseek-v4-flash-vision-exp`）：1) 新增 `lib/deepseekVision.ts`——配置 `DEEPSEEK_API_KEY` 后截图导入优先调用 OpenAI 兼容 `/chat/completions`，base64 内联图片（单图上限沿用接口 8MB，低于官方 32MiB），提示词要求按「名称|代码|数量|成本|现价|涨跌幅」一行一只输出，未配置 Key / 调用失败自动返回 null；2) 导入接口改为双引擎：DeepSeek 文本行转合成视觉坐标复用现有 `snapshotParser` 解析（不新增解析逻辑），解析不到股票自动回退本地 Apple Vision 再试一次，响应带 provider；3) 前端预览区显示所用引擎（DeepSeek 视觉识别 / Apple Vision 本地识别），上传提示不再写死「仅本机识别不上云」（配 Key 后图片会送 DeepSeek 处理）；4) `.env.example` 增加 `DEEPSEEK_API_KEY` / `DEEPSEEK_BASE_URL` / `DEEPSEEK_VISION_MODEL` 说明。限流与 JPG / PNG / GIF / WEBP 校验保留。tsc 无错误、npm run build 通过。
1. 补全自选股相关 ETF 列表 —— 借富途 OpenD 搜索逐项核对代码与名称后补全 `lib/relatedEtfs.ts`：1) 新增正股条目——MU（MUU 2X 做多 / MUD 1X 做空）、NFLX（NFLU 2X 做多 / NFLY 期权收益）、TSM（TSMX 2X 做多）、SNDK（SNXX 2X 做多）、UNH（UNHG 2X 做多）、ECHO（ECHX 2X 做多，兼容旧代码 SATG → ECHO 反向映射）、INTC（INTW 2X 做多）、NIO（NIOG 2X 做多）、WDC（WDCX / WDCC 2X 做多）、XPEV（XPEG 2X 做多）；2) 扩充已有条目——TSLA（+TSDD / TSLT / TSLR / TSYY）、AMD（+AMUU / AMDD）、MSFT（+MSFX）、GOOGL / GOOG（+GOOX）、META（+METU）、COIN（+COIW 每周收益）、RKLB（+RKLZ 2X 做空）、MSTR（+MSTZ 2X 做空 / MSTY 期权收益）、SMCI（+SMCZ 2X 做空）；3) EchoStar 已于 2026-06-24 由 SATS 更名 ECHO，其 2X ETF 由 SATG 更名 ECHX，目录同步新代码并保留旧 SATG 记录的兼容映射；图标兜底（RELATED_ETF_MAIN_STOCK）自动覆盖全部新增 ETF。tsc 无错误、npm run build 通过。
1. 补全 ETF 总市值（富途优先，缺口自动兜底） —— 个股详情页大量 ETF 总市值显示「—」：富途快照对 trust 只返回单位净值、腾讯美股 f44 为空，两家都不提供 ETF 总市值。修复：1) 保留富途 / 腾讯优先（普通美股走富途 total_market_val、港股 / A股 ETF 走腾讯 f44）；2) 仅当市值仍缺失且为美股时，新增 `lib/etfMarketCap.ts` 用「份额 × 现价」（份额取 stockanalysis.com ETF 页，缺失退基金规模 aum）补市值，结果缓存 SQLite `etf_market_caps` 表（成功 24h / 失败 6h），不阻塞正常行情；3) 接入 v1 个股详情接口，quote.marketCap 与六币种市值同步补齐。实测 VOO / TSLL / AAPU / NASA / DRAM / RKLX / ECHX 全部有市值，AAPL 与港股 02824 回归正常。tsc 无错误、npm run build 通过。
1. 个股详情页相关 ETF 按市值分组排序 —— 自选股点进个股详情页的 ETF 列表改为：做多一组、做空一组、收益策略一组（组内按市值从大到小，缺市值排组尾），不再按目录顺序混排；批量行情接口 `/api/v1/quotes` 同步补 ETF 市值（单次最多真正抓取 12 个，缓存命中为纯 SQLite 读），排序数据即时可用。tsc 无错误、npm run build 通过。
1. 股息记录（富途优先）+ 个股详情「股息」页签 + 订单系统股息入账 —— 1) 数据层：富途 `get_corporate_actions_dividends` 桥接（`scripts/futu_quotes.py` 新增 dividends 命令）+ `lib/dividends.ts` 解析「1股派息0.27USD / 末期息5.3港元」等声明得到每股金额与币种，实物分派 / 优先发售标记为 special；SQLite `dividend_cache` 缓存 24h，富途不可用不缓存失败、恢复后自动可取；新增 `GET /api/v1/dividends`（美股 / 港股 / A股）。2) 个股详情页：概览 / ETF 旁边新增「股息」页签，展示除净日 / 派息日 / 公布日 / 每股金额 / 年度 / 方案原文。3) 订单系统：`trade_orders.side` 扩展 dividend（旧库自动重建表迁移，存量订单无损），股息入账不改变持仓数量与成本、每股股息 × 股数记为已实现收益（realizedPnl），更正 / 删除重放账本同口径；我的持仓交易弹窗新增「股息」方向（默认带入当前持仓股数），订单列表与详情展示股息徽标。实测 AAPL / HK 00700 / VOO / TSLL 股息均正确解析，股息订单创建 / 删除后持仓不变。tsc 无错误、npm run build 通过。
1. 股息自动入账：xlsx 导入识别股息 + 派息日自动生成订单 —— 1) 券商 xlsx 导入支持股息行：方向或业务类型含「股息 / 红利 / 分红 / 派息」时按 dividend 入账（每股股息 × 股数记为现金收入），不再被误判为卖出，净持仓计算与重放同口径；2) 新增 `lib/dividendSettlement.ts` 惰性任务（getDb 触发、15 分钟节流、单实例保护）：服务器有访问时对有持仓的记录拉取富途股息，派息日已到且未生成过（订单备注 `[auto:dividend:除净日]` 去重）则按「除净日持仓数量 × 每股股息」自动创建 dividend 订单，卖出封顶重放不抛错、单只失败不中断。实测按历史派息自动补建 TSLL / GOOGL / AAPU / VOO / ROBN 等 15 笔股息订单，持仓数量与成本均不变，重复运行不产生重复订单。tsc 无错误、npm run build 通过。
1. 个股详情股息页签按年份分组 —— 按用户要求：个股详情页「股息」页签改为按年份分组展示（以除净日 / 派息日 / 公布日年份从新到旧，每组显示年度与期数），并删除「数据来源：富途公司行动-分红派息（现金 / 实物 / ETF 收益分配）」提示文字；行内冗余的财年徽标一并移除，与年份分组保持一致。tsc 无错误、npm run build 通过。
1. 股息年份改为顶部一排切换 —— 按用户要求：个股详情「股息」页签年份由纵向分组改为顶部一排年份胶囊（全部 + 各年份，从新到旧，可横向滚动），选中某年后只展示该年记录，选「全部」恢复按年份分组展示。tsc 无错误、npm run build 通过。
1. 修复 A 股股息与 K 线获取失败 —— 1) 股息：富途 `get_corporate_actions_dividends` 不支持 A 股（返回空），新增东方财富分红送配接口（RPT_SHAREBONUS_DET，只取「实施分配」），每股 = 每10股派现 ÷ 10、币种 CNY，含除净日 / 股权登记日 / 公告日 / 方案原文，缓存 24h；A 股股息来源标记为 eastmoney，前端不再误判为「富途未连接」。2) K 线：腾讯前复权单次最多约 640 条，limit=3200（季K等长周期）直接返回 param error 导致整页失败；改为腾讯 K 线按「截止日」翻页抓取（每页 640，页序倒排拼接为升序），实测 600519 / 000858 可返回 2013→今 3200 条完整历史；东方财富全量历史作为最后兜底。实测贵州茅台返回 27 期实施分配股息（10派280.2423元 → 每股 28.02423 CNY）。tsc 无错误、npm run build 通过。
1. 自选股市场/分组标签秒出（localStorage 缓存 + 后台刷新） —— 「我的行情板」的美股 / 港股等分组标签每次进入自选股都要等 `/api/v1/watch-groups` 返回才渲染（接口本身几十毫秒，但页面每次都重新请求）。修复：分组列表缓存到 localStorage（60 秒 TTL），进入页面先用缓存秒出标签、再后台拉取最新分组更新，旧配置迁移逻辑不变。实测标签在首帧即可出现。tsc 无错误、npm run build 通过。
1. 个股详情 URL 化繁为简：股票代码即路径，去掉 filter= 与 symbol= —— 按用户要求：自选股个股详情不再使用 `?symbol=US.GOOGL` 和 `?filter=分组`，改为路径式 `/watchlist/US.GOOGL`（股票代码就是唯一标识），分组筛选只保留在页面内（不再写入 URL）。改动：1) `[...slug]` 布局识别「页签路径/市场.代码」并传入 initialSymbol；2) RecordsApp 地址栏同步对详情子路径做保护，不再把 `/watchlist/US.GOOGL` 改写回 `/watchlist`；3) QuotesView 改为路径式详情（openDetail 写入 `/watchlist/US.XXX`、返回移除代码段、popstate 跟随），旧链接 `/watchlist?symbol=&filter=` 自动兼容并清理为路径式。实测直达 / 返回 / 点击 / 旧链接四条链路均正常。tsc 无错误、npm run build 通过。
1. 自定义分组图标默认取组内市值最高股票图标（券商分组除外） —— 自选股-我的行情板分组标签图标规则：1) 默认市场组（美股 / 港股 / A股等）保持市场图标；2) 自定义分组未上传图标且非券商分组时，默认取该分组内市值最高的股票图标（行情缺失时取组内第一只有图标的股票）；3) 券商分组（如长桥 / 华泰，分组名命中素材库券商图标）仍走素材库券商图标；自传分组图标优先级最高。实测：核心持仓→腾讯、科技→英伟达、消费→茅台、观察中→五粮液。tsc 无错误、npm run build 通过。
1. K 线副图成交额 + 下方恢复成交量 + 悬浮去掉 MA 圆点 —— 个股详情页 K 线图优化：1) 副图改为上下两块——上方「成交额」（= 成交量 × 现价估算，与详情页口径一致）、下方恢复「成交量」，悬浮提示同时显示成交额与成交量（成交量在成交额下面），单位统一为中文 1亿 / 1000万 / 100万 风格（去掉 M 后缀），提示框不再有「更多」折叠；2) 鼠标划过时 MA / EMA / BOLL 指标线不再显示圆点（symbol: none），只保留主图十字线，更干净；3) 默认视图恢复全量（此前试验性「默认显示最近 8 根」导致日K仅数天、周K仅数周，已回退为默认显示全部，滚轮 / 拖动缩放查看）。tsc 无错误、npm run build 通过。
1. 美股月/年K与富途对齐（周期K直连富途前复权） —— 排查谷歌-A 月K/年K与富途出入很大：根因是美股日K来源（新浪）返回未复权价——GOOGL 2022 拆股前显示 2700-3000、富途前复权约 136-150，聚合出的月/年K自然差几十倍；同期 NVDA / AAPL 等拆股股同样受影响。修复：1) 周 / 月 / 季 / 年K 改为美股直连富途周期K（get_history_kline + qfq 前复权），实测 GOOGL 月K（2026-01 O316.49 / H341.85 / L309.93 / C337.56）、年K 与富途逐值一致，NVDA 2023-06 月K 从 384.89 修正为 38.41；2) 日K 主源改为 Yahoo（拆股复权）→ 新浪兜底；3) 港股 / A股 / 日股 / 韩股周期K 保持腾讯 qfq 日线聚合兜底。tsc 无错误、npm run build 通过。
1. K线成交额/成交量拆分为两个独立开关 —— 按用户要求：副图指标由合并的「成交额 / 量」拆成「成交额」「成交量」两个独立按钮，默认都开启；可分别开关，面板布局随开启数量自适应（0 / 1 / 2 个副图时主图高度自动调整，成交额在上、成交量在下）。实测两个按钮独立生效。tsc 无错误、npm run build 通过。
1. K线底部指标顺序与默认显示调整（成交量居首且默认开启） —— 按用户要求：K线图最下方右侧指标顺序改为「成交量」在首位、「成交额」在后；默认只显示成交量副图（成交额需手动开启），布局随开关数量自适应。tsc 无错误、npm run build 通过。
1. K线新增前复权/后复权切换 —— K线工具栏新增「前复权 / 后复权」切换按钮：美股周 / 月 / 季 / 年K 走富途 autype=qfq/hfq，港股 / A股 / 日股 / 韩股日K 走腾讯 qfq/hfq（东方财富兜底按 fqt 1/2 同步），美股日K 保持前复权（Yahoo / 新浪不提供后复权，且日K窗口为近 4 个月差异极小）；请求与缓存按复权方式分键，切换即时生效、无额外网页压力。实测 GOOGL 月K 后复权（2010 O626.95 → 2026-08 C13884.40）、茅台日 / 月K 后复权（8966.51）均正常。tsc 无错误、npm run build 通过。
1. 前复权/后复权按钮深色适配 + 紧凑尺寸 —— 修复前复权/后复权按钮在深色模式下背景为白色、宽度过宽的问题：新增 `.dark` 样式（暗色玻璃底 rgba(255,255,255,.04)、浅灰文字，悬停 .08），按钮改为紧凑尺寸（高 36px、去最小宽、11.5px 字号、圆角 9px），实测深色下宽度约 57px、不再突兀。tsc 无错误、npm run build 通过。

### 修复

1. 资产分析-持仓快捷交易窗口配色对齐参考图（顶两排黑/白 + 下方红/绿） —— 按参考图（图3买入 / 图4卖出）重排持仓快捷交易窗口的配色分层，消除「红底下半偏棕红近黑」问题：1) 方向色不再只涂表单区——整窗背景即为方向色（买入 dark `#381201` / 卖出 dark `#002e25`，浅色 `#fff9f5` / `#f5fdf9`），标题栏与名称 / 代码两排独立为纯黑（dark `#000`）与 `#171419`、浅色纯白，形成「顶两排黑/白 + 下方红/绿」的分层；2) 表单输入面板统一为参考图同款 `#171419` 深色块（细分隔边框 dark `white/12`），不再用偏蓝的 `#10181b`；3) 底部「0.00 USD / 预估成交后持仓成本」与方向色同底色，仅保留细分隔线，修复之前底栏落在透明背景漏出投影的问题；4) 方向色按钮（买入橙 `#ff6a3d` / 卖出绿 `#00a985`）保持；5) 字段标签深色下提亮到 `white/60`。tsc 无错误。
1. 快捷交易窗口「自定义有效期」日历向上翻开，不再溢出窗口底部 —— 持仓快捷交易 - 时效选「自定义有效期」弹出的日历原为 `absolute top-full`（向下翻开），而「时效」位于表单最后一行，日历约 340px 高，向下超出交易窗口的 `overflow-hidden` 底缘被裁剪，日期底部 23 / 24 / … / 31 与星期错位。修复：日历改为 `bottom-full mb-1`（向上翻开），从「时效」下拉上方展开并完整落在窗口内（覆盖表单字段属正常弹层行为），底部「0.00 USD / 预估成交后持仓成本 + 买入按钮」不受遮挡。tsc 无错误。
1. 快捷交易窗口「买入/卖出」改为一框分段 + 所有输入框使用方向色边框（买红/卖绿） —— 按参考图：1) 方向按钮由两个分开的按钮（flex gap）改为单一圆角分段容器——买入 / 卖出共用一个带方向色边框的框，选中侧实色填充（买入橙 `#ff6a3d` / 卖出绿 `#00a985`）、未选中侧透明（悬停轻微高亮），容器 `overflow-hidden` 保证选中色块与边框贴合；2) 所有输入 / 下拉 / 方向框的边框统一为方向色——买入 `#f26b41`（dark `#bd4218`）、卖出 `#2fbf93`（dark `#0b855f`），不再用中性灰或浅桃色，做到「买入界面所有框为红色、卖出界面所有框为绿色」，与整窗方向色背景呼应。tsc 无错误。
1. 持仓快捷交易窗口可自由拖动（标题栏拖拽 + 位置自动保存） —— 按用户要求把持仓快捷交易弹窗改为可自由移动的桌面窗口：1) 默认居中打开（transform `translate(0,0)` 由居中 `flex` 定位），按住标题栏即可整窗拖动，四方向都能移动；2) 拖动范围做边界钳制——窗口至少保留 120px 可见横向、顶部不低于 12px、底部保留 80px，不会完全被拖出屏幕；3) 从标题栏 `mousedown` 到 `window` 的 `mousemove` / `mouseup` 全程跟随，标题栏上的关闭 / 最小化 / 最大化 / 置顶按钮（button）与输入控件不触发拖动；4) 位置存入 `localStorage`「fire:trade-window-pos」，重开按上次位置恢复；窗口 `transform` 会让下拉 / 日历的 `fixed` 点击遮罩随之约束在窗口内，属可接受的弹层行为，不改变下拉 / 日历本身的正常展开。tsc 无错误。
1. 快捷交易窗口名称排加入股票图标并把名称/代码靠拢排版 —— 按用户要求：1) 交易窗口名称 / 代码排（如「谷歌-A GOOGL.US」）原为名称 `flex-1` 在左、代码推到最右，两者被拉得很远；改为分组靠左排版——`[股票图标] 名称 代码`，名称与代码紧密相邻（`gap-2`），名称不再占满剩余宽度；2) 名称前加入股票图标，复用持仓表同一套图标来源（`useAssetIcons` 的 `stockIcons[MARKET:CODE]`，含 2x ETF 复用正股图标），无自定义图标时回退名称首字圆形头像（与持仓表一致）；图标与名称垂直居中、名称 `text-lg` 加粗、代码 `text-sm` 灰色。`stockIcons` 由 `AssetAnalysisDashboard` 透传给 `QuickTradeDialog`。tsc 无错误。
1. 收尾 Review：交易弹窗金额/成本单位按市场原生币种 + 行情刷新不再重置表单 —— 按收尾 Review 规范修复两处：1) 交易弹窗底部「成交金额 / 预估成交后持仓成本」原来硬编码 `USD`，港股 / A股会被显示成美元；改为按 `record.market` 映射原生币种代码（US→USD / HK→HKD / CN→CNY / JP→JPY / KR→KRW），买卖方向均正确显示。2) 弹窗打开初始化 effect 依赖了父组件传给 `livePrice` 的函数引用（该引用随行情 `quotes` 每几秒变化），导致行情一刷新整个表单（方向 / 价格 / 数量 / 类型）被重置回初始值——用户正在下单时输入会瞬间被清空；改为用 `livePriceRef`（每次渲染同步最新引用）读取价格，初始化只依赖 `[open, record?.id, initialSide, initialQty]`，行情刷新不再重置表单。tsc 无错误、冒烟测试 82 项全 PASS。
1. 订单引擎：挂单 / 市价 / 到价 & 反弹回落触发 / 有效期 / 时段 / 到期失效 / 撤单 —— 补齐交易系统真正的订单引擎（此前订单类型 / 有效期 / 时段只存在于 UI，提交全被忽略）：1) `trade_orders` 增列 `order_type` / `trigger_price` / `tif` / `expires_at` / `session` / `trigger_status`，`status` 扩展到 `pending` / `expired`，旧库自动重建迁移（重建时临时关闭外键以容忍指向已删除记录的孤立订单）；2) 新订单创建走 `lib/orders.placeOrder`——市价单、限价单 + 当日有效即时成交（沿用 `executeOrder` 原子更新持仓与成本），限价单 + 撤单前 / 自定义有效期、到价买入 / 到价卖出 / 反弹买入 / 回落卖出记为 `pending` 挂单（不动持仓、快照当前持仓为 before / after）；3) 惰性结算 `settlePendingOrders`（GET `/api/v1/orders` 时触发、每用户 12s 节流）：批量拉取挂单标的现价（`fetchQuotes`），按类型判定触发——限价买用现价≤、限价卖用现价≥；到价买用现价≤触发价、到价卖用现价≥、反弹买用现价≥、回落卖用现价≤；命中则事务成交（`applyOrder` 更新持仓 / 成本 / 已实现盈亏、行转 `filled` / 已触发），custom 超期或当日失效置为 `expired` / 已失效，可卖不足置为超卖失效；4) 撤单 `PATCH /api/v1/orders/[id]` 仅 `pending` 可撤（置 `cancelled` / 已撤销，二次撤单返回 400）；5) 前端交易弹窗把 订单类型 / 有效期 / 有效期日期 / 时段提交给后端，挂单成功提示「已挂单，等待成交」，下单成功同时刷新持仓与订单列表。实测：限价单 + 当日即时成交持仓 10→15、到价买入（AAPL 触发价高于现价）挂单在 GET 时被行情触发成交且持仓更新、到价买入（触发价低于现价）保持 pending、撤单 pending→cancelled / 二次撤单 400、custom 有效期写入 `expires_at`。tsc 无错误、npm run build 通过、冒烟测试 82 项全 PASS。
1. 订单面板：挂单真实字段 + 待成交/已失效状态徽标 + 撤单按钮 —— 订单面板原本的 委托类型 / 有效期 / 时段 / 触发价格 / 触发状态 / 剩余挂单 / 撤废单 列都是占位文案（当前系统按限价单记一笔完整成交），现接入真实数据：1) 委托类型、有效期、时段、触发价格、触发状态、剩余挂单数量、撤废单数量、成交数量按 `order` 实际值渲染（旧成交回退默认值、股息单显示「股息入账 / —」）；2) 订单状态徽标扩展为 已成交（绿勾） / 待成交（蓝时钟） / 已撤销（红叉） / 已失效（红叉），状态筛选下拉新增 待成交 / 已失效，导出与详情文本同步；3) 待成交挂单的操作列新增「撤单」按钮，点击确认后调 PATCH 撤单并刷新；4) 排序键用真实值排序。tsc 无错误、npm run build 通过。
1. 账户资产三列统计对齐（当日盈亏 / 持仓市值 / 持仓总盈亏） —— 账户资产卡片「当日盈亏 / 持仓市值 / 持仓总盈亏」三列金额未对齐。实测（真实 Tailwind 渲染 `getBoundingClientRect`）：持仓总盈亏列金额的 `top` 比另两列高 8px。根因有二：1) 持仓总盈亏原为带 `py-0.5` 内边距的 `<button>`（另两列是 `<div>`），垂直各多 2px；2) 其标签用 `<span class="flex items-center gap-1">`（`display:flex`），行高被压缩到约 12px，而另两列标签是内联 `span`（行高约 16px），两两叠加使其金额上移约 8px。修复：把第三列改为与另两列相同的 `<div>` + 内联标签 + 金额结构，点击 / 悬停 / 键盘（`role="button"` + `tabIndex` + Enter / Space）挂到该 div，箭头改为 `inline-block` 内联（`ml-1`）不再影响行高；仅保留水平 `-mx-1 px-1` 让 hover 药丸略超格。实测三值 `top` 完全一致（105px）。tsc 无错误。
1. 美股 24h/夜盘行情兜底 + 数据源徽标（富途不可达时明显标注） —— 按用户要求补强美股行情容错与可感知性：1) `Quote` 增加 `source`（futu / tencent / yahoo），`fetchQuotes` 在三处赋值时标注来源——富途命中=futu、腾讯回退=tencent、Yahoo 扩展时段覆盖=yahoo；同时 `Quote.session` 允许 `OVERNIGHT`（富途夜盘口径）。2) Yahoo 扩展时段兜底扩展到夜盘：`fetchUsExtendedQuote` 在 `marketSessionState(US).session === "overnight"` 时以 `wanted=OVERNIGHT` 请求，把美东 20:00-04:00 的分钟点归类为 OVERNIGHT，并用「今日常规收盘（regularMarketPrice）」为基准算涨跌（与富途 `overnight_change_val` 口径一致）；仅当 Yahoo 真返回夜盘点才覆盖，未返回则照旧回腾讯，不影响已有盘前 / 盘后路径。3) 新增 `components/QuoteSourceBadge.tsx`：当任意美股持仓的行情 `source==="tencent"`（即富途 OpenD 未连接、退回腾讯日线）时，在资产分析「账户资产」与我的持仓「账户资产」标题旁显示琥珀色「美股·腾讯兜底」小胶囊（带警示图标，hover 说明夜盘 / 当日盈亏可能非实时）；富途正常时隐藏。实测：OpenD 不可达时 AAPL 返回腾讯 `session REGULAR`、`source=tencent`，徽标亮起；OpenD 登录后 AAPL 返回 `session OVERNIGHT`、`source=futu`、`change=overnight` 波动，徽标消失。tsc 无错误、npm run build 通过、冒烟测试 82 项全 PASS。
1. 搜索联想：富途结果缺价时自动补价（修复冒烟「搜腾讯返回腾讯控股」） —— OpenD 在线后 `/api/search` 优先走富途搜索 `searchFutu`，但富途搜索偶发返回无价格结果（桥接超时 / 快照缺失），导致搜索联想出现「无价格」项，冒烟断言 `price>0` 失败。修复：`searchStocks` 在富途结果中存在缺价项时，用 `fetchQuotes` 按 symbol / market / code 补全 price / changePct（与腾讯联想路径同源），再返回。实测 `/api/search?q=腾讯` 现返回腾讯控股(HK) 价格 >0，冒烟 82/0。tsc 无错误、npm run build 通过。

---

## v0.1.14 · 2026-08-21

> 资产盈亏分析：收益日历市场筛选（带市场图标）、盈亏总结页签改全部/美股/港股/A股、页面整体可拖动窗口。

### 新功能

1. 资产盈亏分析：收益日历市场筛选 + 总结页签改造 + 可拖动窗口 —— 1) 收益日历新增市场筛选按钮（默认「全部」，分为 全部 / 美股 / 港股 / A股，使用市场图标 `MarketIcon`），日历按所选市场计算每日真实盈亏（沿用最近有效收盘价），每天真实盈亏同步在日历；2) 「全部盈亏总结（USD）」下方页签由 股票 / 基金 / 新股 改为 全部 / 美股 / 港股 / A股（删除基金与新股），与页面市场筛选联动；3) 资产盈亏分析页整体改为可拖动桌面窗口（与设置页同款）：抽共用 `useDraggableWindow` hook（标题栏拖拽、位置 localStorage 持久化、默认靠左），窗口带圆角边框阴影、内部滚动，标题栏显示「按住标题栏拖动」。tsc 无错误、npm run build 通过、PM2 已重启。
1. 资产盈亏分析五项修复（拖动 / 日历图标 / 明细图标 / 排行榜序号 / 首屏卡顿） —— 1) 整体拖动：改为按住窗口任意处（含标题栏）即可拖动整个窗口，按钮 / 输入框点击不受影响，位置仍自动保存；2) 收益日历：日期（月份）右侧新增一个日期图标按钮；3) 股票盈亏明细：每行增加股票图标（素材库图标优先，缺失回退首字母头像）；4) 全部盈亏排行榜序号改用全球预览同款样式（金 `#f5a623` / 银 `#9aa3ad` / 铜 `#c8864a` / 灰 `#9298a1`，无底色，字号 18 / 15 / 12 / 12）；5) 首屏卡顿修复：持仓 / 行情 / 汇率就绪后立即渲染（总额、排行、明细立即可见），日K在后台加载，趋势图与收益日历单独显示「正在汇总…」加载态，不再等全部 K 线拉完才显示页面。tsc 无错误、npm run build 通过、PM2 已重启。
1. 资产盈亏分析窗口宽度对齐设置页（960px） —— 按用户要求：资产盈亏分析可拖动窗口最大宽度由 1480px 调整为与设置页一致的 960px。tsc 无错误。
1. 资产盈亏分析窗口可拖到视口最左边 —— 修复拖动边界：此前位置按内容区相对坐标计算，最左被限制在内容区左缘（侧栏右侧）；改为按视口坐标计算（`getBoundingClientRect` + transform 自然位置换算），窗口可一直拖到屏幕最左 / 最上，右侧 / 底部仍防拖出屏幕，位置继续自动保存。tsc 无错误。
1. 资产盈亏分析拖动对齐设置页（左边界=内容区左缘） —— 按用户反馈撤销视口最左拖动：窗口左移会盖住左侧导航文字，与设置页行为不一致。恢复与设置页同款的拖动边界——transform 坐标左边界为 0（最左即内容区左缘，不越过侧栏），右 / 下限制防拖出屏幕，位置自动保存。tsc 无错误。
1. 资产盈亏分析页视觉优化 —— 按截图反馈优化：1) 去掉顶部与总结卡重复的市场筛选行（市场筛选统一由「全部盈亏总结」卡片页签控制，日历另有独立市场按钮）；2) 日期范围在历史行情加载期显示「正在汇总历史行情…」而非误导性的「暂无数据」；3) 盈亏总结盈利 / 亏损对比卡名称允许换行（`line-clamp-2`），不再截断；4) 趋势图加载态改为脉冲骨架 + 提示文字；5) 标题栏拖拽提示加亮并加拖拽手柄图标（显示「按住拖动」）。tsc 无错误、npm run build 通过、PM2 已重启。
1. 恢复资产盈亏分析顶部市场筛选行 —— 按用户要求恢复顶部「全部 / 美股 / 港股 / A股」市场筛选行（与「全部盈亏总结」卡片页签联动），其余视觉优化保留。tsc 无错误。
1. 收益日历日期旁只保留一个图标按钮 —— 按用户澄清：收益日历日期区域精简为「2026/08」+ 右侧一个日历图标按钮，移除前后月份切换箭头与 `⌄`。tsc 无错误。
1. 恢复收益日历日期控件（前后箭头 + 月份 + 图标按钮） —— 按用户要求恢复收益日历日期区域：上个月 / 下个月箭头 + 月份「2026/08⌄」+ 日期图标按钮。tsc 无错误。
1. 收益日历市场图标下拉 + 日期选择器 —— 按用户澄清：1) 收益日历日期（2026/08）右侧新增市场图标按钮（默认全部四宫格图标），点击弹出下拉可选 美股 / 港股 / A股（带市场图标），选择后日历按所选市场计算每日盈亏；2) 日历图标按钮实现真正的「选择日期」：弹出月份选择器（年份 ‹ › 切换 + 12 个月网格），选中即跳转对应月份；下拉 / 选择器点击不会触发窗口拖动。tsc 无错误、npm run build 通过、PM2 已重启。
1. 收益日历：删除日历图标按钮，日期按钮改为「选择日期」 —— 按用户要求：删除日期旁独立的日历图标按钮；原「2026/08⌄」按钮改为「选择日期⌄」并负责打开月份选择器（年份 ‹ › 切换 + 12 个月网格，选择器顶部显示当前月份）。tsc 无错误。
1. 收益日历市场/月份选择持久化 + 删除「当前」提示 —— 按用户反馈：日历市场选择（全部 / 美股 / 港股 / A股）与选择的月份写入 localStorage（`fire:asset-pnl-cal-market` / `fire:asset-pnl-cal-month`），刷新后保持；删除月份选择器中的「当前：2026/08」提示。tsc 无错误。
1. 排行榜/明细市场色块统一为分享页持仓列表同款 —— 抽出共用 `getMarketBadge`（`lib/marketBadge.ts`）：US 蓝 `#3b82f6` / HK 紫 `#8b5cf6` / A股 按代码 SH / SZ 粉红 `#e0919f`（深色字），其余灰。资产盈亏分析-全部盈亏排行榜与股票盈亏明细改用该色块（30×18 圆角矩形），与 资产分析-分享页-持仓列表 完全一致；分享页同步改用同一函数。tsc 无错误、npm run build 通过、PM2 已重启。
1. 全部盈亏排行榜序号统一为股票盈亏明细样式 —— 按用户要求：排行榜序号由全球预览金 / 银 / 铜大号数字改为与股票盈亏明细一致的灰色两位补零序号（01 / 02 / …）。tsc 无错误。
1. 全部盈亏总结移到收益日历下方 —— 按用户要求调整页面区块顺序：盈亏总额 + 趋势图（整行）→ 全部盈亏排行榜 | 股票盈亏明细 → 收益日历 → 全部盈亏总结（日历下方整行）。tsc 无错误。
1. 各卡片右侧统一为「选择日期」按钮 —— 抽出共用 `DateSelectButton`（年份 ‹ › 切换 + 12 个月网格），收益日历、全部盈亏排行榜、全部盈亏总结 右侧都使用该「选择日期」按钮，选中月份后日历跳转并持久化。tsc 无错误、npm run build 通过、PM2 已重启。
1. 恢复排行榜/总结右上角「更新至」文字 —— 按用户要求：全部盈亏排行榜与全部盈亏总结右上角恢复为「更新至 xx.xx」文字，收益日历的「选择日期」保留。tsc 无错误。
1. 收益日历日期按钮恢复显示当前月份 —— 按用户要求：收益日历右侧按钮恢复显示当前月份（如 2026/08），点击仍打开日期选择器；`DateSelectButton` 增加 `label` 参数（默认「选择日期」）。tsc 无错误。
1. 收益日历：年/月与收益/收益率分组隔开 + 年视图生效 —— 按用户要求：收益日历右上角按钮拆成两组——「年 / 月」一组、「收益 / 收益率」一组（中间隔开）；「年」视图生效：按所选年份展示 12 个月格子，每格显示当月收益合计（或收益率，随收益 / 收益率切换），点击某月跳转月视图；「月」保持原有日视图。tsc 无错误、npm run build 通过、PM2 已重启。
1. 收益日历年视图收益加「万」单位 —— 年视图每月收益 ≥1万 显示为 x.xx万（如 +1.63万），千位用 K 兜底，小额显示原值；月视图保持 K 缩写不变。tsc 无错误。
1. 资产盈亏分析页字体统一到资产分析体系 —— 整体收敛字体层级：窗口标题 `text-xl`、卡片标题 `text-base` 加粗、盈亏总额大数字 `text-2xl` 加粗（收益率 `text-base`）、日期 / 更新至 / 图例 / 星期表头 `text-xs`、正文 / 排行 / 明细 / 金额 `text-sm`、筛选与切换按钮 `text-sm`、日历日期与月份格子 `text-sm`，与资产分析页观感统一。tsc 无错误、npm run build 通过、PM2 已重启。
1. 取消资产盈亏分析页可拖动 —— 按用户要求移除资产盈亏分析窗口的拖动能力：删除 `useDraggableWindow` 使用、transform 位移、拖拽手柄提示与抓取光标，窗口保持 960px 圆角外观但固定。tsc 无错误。
1. 设置风格新增 V16 极简青绿 + 版本列表从小到大排序 —— 设置窗口版本切换器新增 V16 极简青绿（浅色青绿白卡 / 深色墨绿青绿强调，侧栏 Logo 青绿渐变 + fire 流光同步适配），版本列表由 小→大 排序：V1 → V5 → V7 → V14 → V16。tsc 无错误、npm run build 通过、PM2 已重启。
1. v16 恢复站点信息下方/网站标题上方的分隔边框 —— 按用户要求：v16 风格下设置分区标题（站点信息等）与首行（网站标题等）之间恢复顶部细分隔线（1px `var(--sv-border)`）。tsc 无错误。
1. v16 分隔线改到分区标题正下方（精调细节） —— 按用户反馈继续精调：v16 的分隔线由「首行上边框」改为「分区标题（站点信息等）正下方 1px 横线」（`border-bottom` + 10px 下内边距），与其它版本观感一致。tsc 无错误。
1. v16 改为包裹式边框（分区整体套圆角卡片） —— 按用户澄清：v16 不是标题下一条线，而是包裹式边框——设置分区（站点信息等）整体套在 1px 边框 + 12px 圆角 + 卡片底色（`var(--sv-card)`）的容器里，标题与行内容同在一个盒子中。tsc 无错误。
1. v16 包裹边框只包行内容，标题留在框外 —— 按用户反馈修正：v16 的包裹式边框应只包住分区行内容（网站标题等），「站点信息」等分区标题保留在框外上方；边框 / 圆角 / 卡片底色套在标题后面的内容容器上。tsc 无错误。
1. v16 全部分区按 mockup 框逻辑统一 —— 对照 `public/mockups/settings-v16.html` 逐条核对：分区标题（站点信息 / 网站形象 / 交易 / 数据库等）改为小灰字（11px、0.04em 字距）置于框外上方；行内容统一套 1px 边框 + 10px 圆角 + 无底色盒子（`overflow hidden`），行间距 8px；行高 42px、内边距 7px 14px，行与行之间保留分隔线。所有设置分区一致生效。tsc 无错误。
1. 框逻辑应用到全部风格 + V14 改名为极简风 —— 按用户要求：将分区「标题在外 + 行内容带边框圆角盒子」的框逻辑从 v16 提升为全部风格通用（V1 / V5 / V7 / V14 / V16 统一生效，V1 保留其卡片式行布局细节）；版本切换器「V14 桌面客户端」改名为「V14 极简风」。tsc 无错误、npm run build 通过、PM2 已重启。

### 新功能

1. 8 款设置风格预览 + 浅色/深色切换 + Apple 液态玻璃风 —— 生成 9 个设置风格预览页（`public/mockups/`，960px 客户端窗口骨架）：清新蓝（长桥向）、红黑经典（雪球向）、V17 石墨黑金、V18 磨砂玻璃、TradingView 风、V2EX 风、欧易 OKX 风、币安风、Apple 液态玻璃（Liquid Glass，macOS Tahoe 强毛玻璃 + 高饱和渐变 + 顶部高光，浅色粉紫 / 深色墨紫）。每款标题栏都有「深色 / 浅色」切换按钮，light / dark 两套配色成体系协调（侧栏 / 主区 / 边框 / 输入 / 行悬停 / 开关 / 强调色），切换带 0.25s 过渡。生成脚本 `scripts/gen-style-mockups.mjs` 保留可随时重出。
1. 设置风格新增 V17 石墨黑金 / V18 磨砂玻璃 / OKX / 币安 / Apple 液态玻璃 —— 按用户选定把 5 款预览正式接入设置切换器（V1 → V5 → V7 → V14 → V16 → V17 → V18 → OKX → 币安 → Apple）：每款含浅色 / 深色两套 CSS 变量（shell / bg / border / text / accent / card / input / hover）、侧栏 Logo 渐变、fire 流光、激活导航强调色；V18 / Apple 为磨砂玻璃（backdrop blur + saturate），币安 / V17 金色胶囊激活用深色字保证对比，V17 标题用衬线。tsc 无错误、npm run build 通过、PM2 已重启。
1. 设置窗口右上角新增固定按钮 —— 设置窗口标题栏右上角（版本切换左侧）新增图钉式「固定」按钮：点击后窗口锁定当前位置不可拖动（图钉实心），再点解锁恢复拖动；固定状态写入 localStorage（`fire:settings-window-fixed`）刷新保持。tsc 无错误、npm run build 通过、PM2 已重启。
1. 删除磨砂玻璃风 + 版本下拉数字用 V7 衬线 + Apple 下拉不透明 —— 1) 从版本切换器移除 V18 磨砂玻璃（含 CSS 全部清理）；2) 风格选择下拉中版本号（V1 / V5 / V7 …）改用 V7 同款衬线字体（Georgia 系），其余文字不变；3) Apple 液态玻璃风格下，版本下拉背景改为不透明（浅色 `#fff` / 深色 `#1c1c22`），避免玻璃透底影响可读。tsc 无错误、npm run build 通过、PM2 已重启。
1. v16 框逻辑全风格统一（修复 v1-grid 导致外框丢失 + 富途风网站设置排版统一） —— 根因：站点信息 / 交易分区外层用 `.v1-grid` 且 base `display:contents`，导致承载外框的包裹层不生成盒子（所有风格都无框），V1 还额外变成双列卡片。修复：删除 `v1-grid` 整套布局（base + V1 双列覆盖 + JSX 类名），所有风格统一为「标题在外 + 行内容 1px 边框圆角盒子 + 行间分隔线」，富途风网站设置不再特立独行。tsc 无错误、npm run build 通过、PM2 已重启。
1. 深色模式下 V1/V5/V17 分区外框边框加亮 + 删除液态玻璃风格 —— 1) V1 / V5 / V17 深色模式下设置分区外框边框分别加亮（`#3b4354` / `#3a3a3a` / `#3a3a42`），不再与深色底融为一体；2) 按用户要求整体删除 Apple 液态玻璃风格（版本项 + 全部 CSS：变量 / 玻璃规则 / 下拉不透明 / Logo / fire / 导航色）。tsc 无错误、npm run build 通过、PM2 已重启。
1. 设置分区外框边框统一（color-mix 推导，所有风格/明暗对比一致） —— 继续优化 v1 / v5 / v17 与 v16 的框统一：分区外框边框色改为 `color-mix(in srgb, 卡片色 76%, 文字色 24%)` 推导——每个风格 / 明暗下都保证清晰对比度且保持主题色调（如 v16 仍呈青灰、v1 / v5 呈中性灰、v17 呈暖金灰），不再出现浅色近白、深色近黑的“看不见边框”；保留原深色加亮规则作为不支持 color-mix 时的兜底。tsc 无错误、npm run build 通过、PM2 已重启。
1. v1/v5/v17 边框与行分隔线显色（不再依赖 color-mix） —— 按用户反馈：去掉 color-mix 推导（部分浏览器不支持会退回近白 / 近黑细线导致左边框与标题下方横线不可见），改为 v1 / v5 / v17 分别指定清晰的边框色与行分隔线色（浅色：v1 `#cfd6df` / v5 `#d6d6d4` / v17 `#cfc6ae`；深色：`#3b4354` / `#3a3a3a` / `#3a3a42`），保证分区框四边与「网站标题」下方横线在所有浏览器下清晰可见，与 v16 观感统一。tsc 无错误、npm run build 通过、PM2 已重启。
1. v1/v5/v17 侧栏左右分界线与窗口外框统一加亮（浅色/深色） —— 按用户反馈补齐 v1 / v5 / v17 缺失的线条：1) 侧栏与内容区之间的竖向「左右分界线」分别加亮（浅色 `#cfd6df` / `#d6d6d4` / `#cfc6ae`，深色 `#3b4354` / `#3a3a3a` / `#3a3a42`），不再与背景融为一体；2) 窗口最上方上边框等整窗边框统一：把 v1 / v5 / v17 的 `--sv-border` / `--sv-card-border` 色板整体加深到与分区外框一致，标题栏下沿、状态栏上沿、窗口外框四边、搜索框与卡片输入框边框随版本整体清晰可见。tsc 无错误、npm run build 通过。
1. 资产盈亏分析第一块卡片对齐参考图 + 图表改资产分析同款 —— 按参考图（图1）重做资产盈亏分析-盈亏总额卡片整体布局：1) 标题行——「盈亏总额 (USD)⌵」货币下拉（USD / HKD / CNY，与资产分析页共用 `fire:display-currency` key，切换后大数字同步换算）+ 灰色 `ⓘ` 信息图标 + 方形右上箭头分享图标（原三圆节点图标替换）；2) 大数字（`text-4xl`）/ 收益率 / 日期改为居中排布，日期移到收益率下方；3) 收益率走势 / 总资产趋势 tab 改为资产分析-账户资产-收益率趋势图同款下划线样式（选中 15px 加粗 + 底部 2px 蓝色短下划线）；4) 图例改为「跑赢 ● 基准名 ▾ + 跑赢幅度」基准条（多市场下拉：标普 500 / 纳斯达克 / 道琼斯 / 恒生指数 / 上证指数 / 深证成指，CN 指数走 `index=1`，切换后按所选基准重拉日K；跑赢幅度 = 组合区间收益率 − 基准区间收益率），右侧简单加权 / 时间加权下拉（加权持久化）；5) 图表沿用共享 `PnlTrendChart`（资产分析同款：我的持仓橙线面积 + 基准对照线）。tsc 无错误、npm run build 通过。
1. 资产盈亏分析页整体打磨（周期联动 / 盈利图数据修复 / 取消大卡片 / 时间加权修复） —— 1) 盈亏总额大数字与顶部周期筛选联动（本月 / 近1月 / 近6月 / 本年 / 近1年 / 全部，非「全部」按区间首尾有效收盘价计算，收益率同步）；2) 盈利图数据修复——每日资产序列首日用各持仓首个有效收盘价回填（修复不同股票起始日不一致导致区间收益率算爆至 80000%），基准首值回填（修复基准线被压成直线）；3) 货币选择器带市场图标（素材库 `MarketIcon`，触发按钮与下拉选项均显示）；4) 删除标题旁的 `ⓘ` 信息图标，右上角分享改为「分享截图」按钮（html2canvas 截图卡片 → 预览弹窗 → 复制 / 保存，复用 `/api/clipboard` 剪贴板兜底，图标用资产分析-账户资产同款三圆分享图标）；5) 跑赢条改为「● 我的 ● 基准名 ▾ + 跑赢幅度」（红点对应图表我的持仓线）；6) 取消大卡片（浮动窗口）模式：改为普通页面流式布局，左对齐临界宽度 760px；7) 视觉统一：卡片 / 文字 / 下拉 / 胶囊接入主题色（`card` / `text-ink` / `text-muted` / `bg-bg-gray` / `border-edge`），涨跌色统一为 `text-up` / `text-down`，字号层级收敛（大数字 `text-3xl` / 收益率 `text-lg` 临界点），补 `.dark .divide-edge` 深色分隔线覆盖；8) 时间加权修复：`chartPoints` 的 `timeIndex` 按订单轨迹跟踪持仓数量与现金流链式累乘（与资产分析页口径一致），不再写死 100。tsc 无错误、npm run build 通过。
1. 手机端横向溢出修复（资产分析页 / 资产盈亏分析页 / 头部头像） —— 按反馈修复移动端（390px 视口）横向溢出：1) 资产分析页整体被撑到 1302px——根因是 `.asset-analysis-split` 单列 grid 子项默认 `min-width:auto`（min-content），持仓表 `minWidth` 1300px 把整页撑开；补 `.asset-analysis-split > * { min-width: 0 }`，宽表走容器内部 `overflow-x` 滚动（实测 scrollW 1300 / clientW 373）；2) 资产盈亏分析页也被撑到 554px，同一修复后 375px 不再溢出；3) 手机端右上角头像区域被顶部指数条（`IndexTicker`）溢出内容压住并超出屏幕——指数 chip 为 nowrap 且容器未裁剪，容器补 `overflow-hidden`（头像可见、指数条不再越过视口）；4) 头像下拉面板移动端不再超出视口——根因是外层 `-translate-x-6` 的 transform 让面板的 absolute/fixed 都相对变换后的容器定位、脱离视口；移除该 transform，移动端（<768px）面板改真·视口 `fixed` 定位（右缘距视口 12px、头部下方、保持 272px 宽度），实测 320/360/390/414px 均不溢出、桌面端居中不变。tsc 无错误、npm run build 通过。
1. 收益日历支持点击某天查看当日每只股票盈亏（分盈利/亏损） —— 按用户要求：收益日历的每个日期格子可点击，弹出「当日盈亏」明细弹窗：1) 按当日收盘价与前一日收盘价差额 × 数量计算每只股票当日盈亏（跟随日历市场筛选，USD）；2) 样式对齐全部盈亏排行榜（序号 + 名称 + 市场色块徽标 + 代码 + 盈亏金额）；3) 弹窗内分「盈利 / 亏损」两大页签（铺满弹窗内容宽度、两等分、居中，与股票盈亏明细按钮完全一致：`py-2.5` 白底阴影选中态），标题副行仅显示数量（如 4 / 1），盈利按金额降序、亏损按亏损额降序，底部显示分组合计；5) 某天全盈利或全亏损时，弹窗默认打开有数据的那一页（如全亏损日默认「亏损」）；
1. 资产盈亏分析页优化（K线内存缓存 / 死代码清理 / 加载骨架） —— 按审查优化：1) K线内存缓存（10 分钟 TTL，与资产分析页同款）：持仓与基准日K 10 分钟内不重复请求，SPA 内切页 / 重进秒出，失败回退上次成功缓存；2) 移除无功能（无 `onClick`）的漏斗筛选按钮及其图标组件；3) 移除 `dayDetail` 未使用的 `total` 字段；4) 加载骨架由双列改为单列（页面已是单列流式）。tsc 无错误、npm run build 通过。
1. 盈亏总额改回真实累计盈亏（修复区间回填虚亏 -5 万） —— 排查用户真实数据：本年此前显示 -4.1 万～-5.8 万，但用实时行情重算相对成本的真实累计盈亏为 +2.8 万（盈利）——-5 万是「当前数量 × 收盘价回填年初市值」的区间口径虚亏，且用户持仓几乎无订单历史（仅 2 笔卖出），无法还原买入日期、区间盈亏不可靠。修复：盈亏总额大数字始终显示浮动盈亏口径（当前市值 − 投入成本，可靠），周期胶囊只控制图表的区间与日期范围（仍随周期变化）；另修复非交易日（周末）成交现金流被丢失的 bug（现金流游标归入下一交易日，时间加权更准）。tsc 无错误、npm run build 通过。
1. 修复区间盈亏虚亏（本年 -4 万 vs 真实 -7 千） —— 排查用户反馈「本年盈亏 -41089 与实际出入大」：根因是区间盈亏此前用「当前数量 × 收盘价」回填全年，今年买入的股票在年初就被按当前数量+当时价格计入，抬高年初市值导致区间虚亏。修复：区间盈亏改用订单轨迹的实际持仓市值 + 现金流修正——区间盈亏 = 末日实际市值 − 首日实际市值 − 区间净流入（买入为正 / 卖出为负），区间收益率改用时间加权 `timeIndex`（不受资金进出影响）；全部仍为浮动盈亏口径（真实累计盈亏，相对成本）。注意：仅对通过「交易」创建过订单的持仓生效（无订单历史的持仓无法还原买入日期，仍按当前数量回填）。tsc 无错误、npm run build 通过。
1. 盈亏总额周期口径统一（全部改为日资产首末日差） —— 按用户确认统一口径：盈亏总额大数字不再区分「全部=浮动盈亏 / 周期=日资产首末日差」，全部周期（含「全部」）统一按日资产序列首尾有效收盘价计算区间盈亏与收益率（末资产−首资产 / 末÷首−1），数据未就绪时兜底浮动盈亏；切换周期数值连贯（demo：全部 +$4,489 / 8.33%，近1年 +$4,600 / 8.56%）。「全部盈亏总结」卡的股票累计盈亏仍为浮动盈亏口径。tsc 无错误、npm run build 通过。
1. 顶部市场筛选改为日历同款图标下拉，与周期胶囊同排 —— 按用户要求：顶部「全部 / 美股 / 港股 / A股」文本胶囊行删除，改为收益日历同款的市场筛选（圆形图标按钮 + 下拉：全部四宫格 / 美股 US / 港股 HK / A股 CN 图标），并与周期胶囊（本月 / 近1月 / 近6月 / 本年 / 近1年 / 全部）合并为同一排（按钮紧跟「全部」胶囊右侧、改为漏斗筛选图标（下拉选项仍带市场图标），筛选行与下方卡片间距统一 16px（临界点）；修复市场下拉被 `overflow-x-auto` 滚动容器裁剪导致看似无功能的问题（按钮移出滚动容器，下拉正常弹出）。tsc 无错误、npm run build 通过。4) 列表显示效果与全部盈亏排行榜完全一致（`min-h-16` 行 + 按金额比例的背景色条，盈利红底 / 亏损绿底，最小 20%）。tsc 无错误、npm run build 通过。
1. 接入券商历史订单（长桥导出 xlsx 导入，含已清仓股票） —— 按用户提供的历史订单导出（长桥 H10891259，两份 xlsx）把真实成交接入订单系统：1) 解析 inline-string xlsx（无 sharedStrings），仅取 订单状态=已成交 的行（排除已过期 / 已撤销 / 已拒绝），按 订单号 去重（两份导出有重叠），合计 421 笔成交；2) 持仓股导入 250 笔——每只股票按时间顺序重放建仓 / 加减仓，位置快照（`position_qty_before/after`、`position_cost_before/after`、`realized_pnl`）与记录数量严格对账，基准数量 = 当前数量 − 订单净量，基准成本按 bisection 反推使重放终点成本=记录成本（记录成本为用户调整后的返佣负成本，保持不变）；3) 按用户要求保留已清仓股票：为其新建 26 条记录（qty=None、归入长桥证劵分组，名称按 ETF / 主体命名规范规范化）并导入 167 笔成交（快照链自洽、最终归零）；HK 2580 / 2824 仅有一笔卖出（买入在导出窗口外），按窗口外基准导入、成本未知置空；SKHY 净量 +1 与零持仓矛盾、NVDA 期权订单跳过并说明；4) 删除用户手动建的 2 笔与券商重叠的旧订单（SPCH / RKLX 卖出，以券商真实日期为准）。全库 38 只有订单记录快照链逐笔自洽、最终数量对账无误；无头浏览器实测页面 盈亏总额 +$29,266 / +52.18%（浮动盈亏口径，周期无关），订单列表 / 收益日历 / 资产分析均正常。tsc 无错误、npm run build 通过。
1. 订单系统新增「导入」功能（券商 xlsx 历史订单回填） —— 资产分析-订单系统「导出」旁新增「导入」按钮，支持上传券商导出的 .xlsx（长桥等，格式与导出一致：21 列、inlineStr 单元格、A1=订单状态）：1) 零依赖 xlsx 读取器 `lib/orderImportXlsx.ts`（ZIP 中央目录解析 + inlineStr 单元格提取，与现有写入器对称，不引入新依赖）；2) 导入 API `/api/v1/orders/import`（16MB 上限、10 次 / 分钟限流）——仅导入 已成交，按 订单号 去重；代码剥交易所后缀匹配记录，无记录自动新建（qty/cost 置空、归入全部+对应市场、`source=broker-import`）；按成交时间重放生成位置快照链（含已实现盈亏），重放最终数量必须等于记录持仓否则整组跳过；期权与净量矛盾（导出缺单）跳过并说明；3) 幂等去重按内容指纹 `(record, traded_at, side, qty, price)` 而非订单号——历史订单 order_no 为随机回退号，防重复导入；4) 前端两步确认：选文件后先 dryRun 预览（识别笔数 / 将导入 / 跳过 / 重复 + 每股明细），确认后再真实写入并刷新订单列表；5) 导入格式规范文档 `docs/broker-orders-format.md`（列结构、状态语义、时区、快照对账）。实测：真实券商文件重导全部幂等识别（0 新增），构造新订单导入正确生成快照链且记录成本不变，重复导入 0 新增。tsc 无错误、npm run build 通过。
1. 订单系统分页（每页 9 条）+ 搜索联想 —— 订单面板（资产分析 / 持仓详情共用）新增两项：1) 列表分页——每页 9 条，底部翻页条（上一页 / 下一页 + 页码窗口，当前页高亮，超出 7 页取居中窗口），显示「第 x / y 页 · 共 n 笔」；tab / 市场 / 类型 / 状态 / 时间 / 排序 / 搜索变化自动回第一页；2) 搜索框——位于「n 笔」左侧，按 股票代码 / 名称 / 订单号 模糊匹配，输入即时联想（从当前筛选结果提取去重，最多 8 条，点击建议即应用过滤），输入即实时过滤列表（不必回车），回车应用、Esc / 清除按钮复位，空结果给出提示文案。实测：417 笔 → 47 页、每页 9 行、翻页正常；输入 TSL 联想出 TSLL / TSLQ，点击 TSLL 过滤为 106 笔；输入 aapu 未回车即过滤为 34 笔。tsc 无错误、npm run build 通过。

---

## v0.1.13 · 2026-08-20

> 资产盈亏分析页（UI）：账户资产-持仓总盈亏可点击进入，页面含盈亏总额/趋势、盈亏总结、排行榜、明细与收益日历。

### 新功能

1. 资产盈亏分析页（UI）+ 账户资产入口 —— 按用户要求（参考图 1-5）先做 UI：1) 资产分析-账户资产卡片的「持仓总盈亏」变为可点击入口，悬停高亮 + 箭头提示，点击进入 `/asset-pnl-analysis`；2) 页面含：市场 / 周期筛选（全部 / 美股 / 港股 / A股 + 近6月 / 本年 / 近1年 / 全部 + 漏斗）、盈亏总额（USD）大数字 + 收益率 + 收益率走势 / 总资产趋势切换（我的 vs 标普500 图例）、全部盈亏总结（股票累计盈亏 + 盈利 / 亏损最大单品对比卡）、全部盈亏排行榜（盈利 / 亏损 Top5，名次橙红色阶 + 条形背景）、股票盈亏明细（US 蓝 / HK 粉 / CN 橙红市场徽标）、收益日历（月视图红盈绿亏）。当前为演示数据，逻辑 / 数据对接后续接入。tsc 无错误、npm run build 通过、PM2 已重启。
1. 资产盈亏分析页接入真实数据 —— 把资产盈亏分析页的静态演示数据替换为真实数据：1) 持仓（`/api/records`，当前登录用户）+ 实时行情（`/api/quotes`）+ 汇率（`/api/rates`）→ 计算每只持仓的盈亏（(现价 - 成本) × 数量，按汇率换算 USD）、盈亏总额、成本基数与收益率；2) 每只持仓日K（`/api/kline/full`，有界并发 + force-cache）→ 每日组合 USD 资产序列，生成收益率趋势（相对区间起点）与总资产趋势，标普500（SPY）日K作基准对比；3) 收益日历按所选月份渲染每日盈亏（资产日差）与收益率，支持前后月切换、收益 / 收益率切换；4) 周期筛选（本月 / 近1月 / 近6月 / 本年 / 近1年 / 全部）真正截取趋势与日期区间。tsc 无错误、npm run build 通过、PM2 已重启。
1. 资产盈亏分析接入应用壳 + 图表同款样式 + 日历数据修复 —— 继续打磨：1) 资产盈亏分析改为应用壳内隐藏页签（`/asset-pnl-analysis` 走 `[...slug]` 布局，保留顶部头像一排与左侧 我的持仓 / 资产分析 / 自选股 导航，未登录跳登录）；账户资产「持仓总盈亏」点击改为客户端无感切换（`selectTab("pnl")`），返回按钮同样无感回到资产分析；2) 盈亏总额下方图表改用与资产分析同款的共用 ECharts 组件（`PnlTrendChart`：收益率走势 = 我的持仓橙线 + 标普500金线 + 面积渐变，总资产趋势 = 蓝线面积，横轴日期、纵轴百分比 / 紧凑数字、虚线网格），资产分析原图同步复用同一组件；3) 修复收益日历假数据：不同股票日K最后日期不一致时（如 AAPL 止于 08-19、港A到 08-20）组合资产计算会丢掉缺数据股票的整只市值，产生巨额假盈亏——改为每只持仓沿用最近有效收盘价；4) 市场筛选（全部 / 美股 / 港股 / A股）全页生效（总额 / 排行 / 总结 / 趋势均按所选市场过滤），基金 / 新股页签暂无分类数据改为禁用态而非假页签。tsc 无错误、npm run build 通过、PM2 已重启。

---

## v0.1.12 · 2026-08-19

> 版本切换对齐各版本 mockup：V14 补底部随心输入条与最近编辑、颜色对齐；V1 站点信息 / 交易改卡片式双列字段；V5 / V7 补齐细节；全部适配浅色 / 深色。

### 修复

1. 版本切换对齐 mockup：V14 / V1 修复 + V5 / V7 细节补齐 —— 按用户反馈让四个版本贴近交付的 HTML 预览稿：1) V14——深色配色对齐 HTML（窗口 `#181818`、卡片 `#2a2a2a`、输入 `#2b2b2b`、边框 `#35353f`），底部补上与 HTML 一致的「随心输入」命令条（+ 按钮 + 搜索 + 自动保存开 ⌘K，仅 V14 显示），侧栏补「最近编辑」分组（仅 V14 显示）；2) V1——站点信息与交易（主机 / 端口）改为 HTML 里的卡片式双列字段（label 在上、输入在下，2 列网格），侧栏隐藏图标贴近 HTML，强调色 / 按钮用橙色；3) V5 Notion——分组卡片去重阴影、细圆角、页头字号贴近 HTML；4) V7 Claude——页头与分组标题改用衬线字体（Georgia）贴近 HTML；五个（含深浅色）全部继续跟随右上角主题。tsc 无错误、npm run build 通过。
1. V7 侧栏浅暖色对齐 + V1 侧栏图标恢复 + 各版本侧栏 Logo —— 继续打磨细节：1) V7 Claude——侧栏由深暖色修正为 mockup 的浅暖米色（`#fbfaf6`），激活导航项用珊瑚色（`#b4633f`），页头字号放大（22px 衬线）；2) V1 富途橙——恢复侧栏导航图标（此前误隐藏，mockup 有 16px 图标且激活态橙色）；3) 四个版本侧栏顶部统一补 Logo：V1 橙 F 渐变圆角、V7 珊瑚 C 圆形（带光晕）、V5 黑 F 方块（名称显示 Fire 设置）、V14 淡紫 F + 设置中心标签，全部跟随版本配色与深浅色。tsc 无错误、npm run build 通过。
1. V1 浅色侧栏搜索按钮去白底 —— V1 浅色模式下侧栏「搜索设置」按钮此前误用浅色输入框变量呈现白底，在深蓝侧栏上突兀；改为贴合 mockup 的深色按钮（`#232a38` 底 + 深边框 + 浅灰字，悬停白色），内部 ⌘K 徽标同步深色。tsc 无错误、npm run build 通过。
1. V1 侧栏悬停文字可见性修复 —— V1 浅色模式下侧栏为深蓝底，但悬停 / 底部 / Logo 名称仍用浅色主题的深色文字，深字压深底看不见。修复：V1 侧栏上的文字统一浅色——导航项悬停变白、分组标题浅灰、底部管理员名字浅白、Logo 名称白色。tsc 无错误、npm run build 通过。

### 新功能

1. 搜索框上方新增 Fire 发光流光字标（对齐 KIMI 视频效果） —— 按用户分享的 KIMI 视频（文字呼吸发光 + 流光）在设置窗口侧栏搜索框上方制作动态 Fire 字标：字体加粗放大 + 背景渐变换色流光（background-clip: text）+ 呼吸辉光（drop-shadow 强弱脉动），四套版本各自配色（V14 白底淡紫流光、V1 白橙红火焰渐变、V7 衬线暖橙、V5 极简灰黑），跟随深浅色主题，`prefers-reduced-motion` 自动停用动画。tsc 无错误、npm run build 通过。
1. 搜索框上方只保留小写 fire 发光字标 —— 按用户要求精简：侧栏搜索框上方只保留小写「fire」发光流光字标，去掉 logo 圆标（F / C 色块）与「设置中心」标签，V5 的「Fire 设置」后缀一并移除，仅剩动态小写 fire。tsc 无错误、npm run build 通过。
1. fire 字标居中 + 标题栏去掉「Fire 设置」文字 —— 1) 侧栏搜索框上方的小写 fire 发光字标改为水平居中；2) 设置窗口标题栏（红绿灯右侧）删除「Fire 设置」文字，只保留版本切换图标与版本号，界面更干净。tsc 无错误、npm run build 通过。
1. V1 深色右侧内容区去蓝调 —— V1 深色模式下右侧内容区此前偏蓝（`#1b2230` 蓝灰），改为中性深灰（内容 `#1c1c1f`、卡片 `#232327`、边框 `#2e2e34`），文字同步提亮保证对比度；保留深蓝侧栏与橙色强调的 V1 特征。tsc 无错误、npm run build 通过。
1. 设置页版本切换图标美化 —— 设置窗口右上角版本切换图标由「时钟 + 圆点」改为更精致的「三层叠加图层」图标（寓意版本堆叠），并改用各版本强调色显示（V1 橙 / V7 珊瑚 / V14 淡紫 / V5 黑），悬停回主题文字色。tsc 无错误、npm run build 通过。
1. 设置侧栏导航图标整体美化 —— 设置窗口左侧导航图标换新：统一 1.8 线宽、圆头圆角风格——网站设置用调节滑杆、股票设置用蜡烛图、个人信息用用户、数据库增强用柱体、定时任务用时钟、API 用代码括号、关于用信息圆；搜索命令面板结果项同步使用同一套图标。tsc 无错误、npm run build 通过。
1. 设置侧栏图标升级为 macOS 质感（圆角底块 + 1.5 细线） —— 继续打磨精致度：导航图标线宽降为 1.5、几何比例重新校准，并放入 26px 圆角底色块（浅灰 / 深色半透明底）呈现 macOS 系统设置质感；激活项底块用版本强调色实心 + 反色图标（V1 橙底 / V7 珊瑚底 / V14 淡紫底 / V5 黑底），悬停时图标提亮。tsc 无错误、npm run build 通过。
1. 恢复侧栏图标为原版（撤销图标重绘与底块样式） —— 按用户反馈撤销两轮图标改动：移除自定义 swNavIcon 与 macOS 圆角底块样式，设置侧栏导航与搜索命令面板恢复使用原有 SubNavIcon 图标（14px / 12px），回到改动前的观感。tsc 无错误、npm run build 通过。
1. 修复「网站设置 / 关于」图标重复 + 图标唯一性写入规范 —— 根因：`SubNavIcon` 对未注册的 key（关于）会静默回退成 site 地球图标，导致「网站设置」与「关于」重复。修复：`ICON_PATHS` 新增 about 专属图标（信息圆），两个条目图标不再相同；并在 AGENTS.md「交互与适配约定」新增图标唯一性规范——新增导航 / 设置项必须先注册专属图标，禁止依赖 site 回退，新增后全站走查确认每个 key 形状互不相同。tsc 无错误、npm run build 通过。
1. 去掉设置侧栏与内容区之间的边框 —— 按用户截图标注去掉设置窗口侧栏右侧的竖边框（border-r），侧栏与内容区仅靠底色区分，整体少一层边框更清爽。tsc 无错误、npm run build 通过。
1. 去掉标题栏下方与状态栏上方的横线 —— 按用户确认的位置去掉设置窗口两条横线：标题栏底部边框（border-b）与状态栏顶部边框（border-t），窗口更轻盈；此前误删的侧栏右边框已恢复。tsc 无错误、npm run build 通过。
1. V14 底部命令条改为与其它版本统一的状态栏 —— V14 原本用「随心输入」命令条作底部，与 V1 / V5 / V7 的状态栏不一致；改为所有版本统一显示常规状态栏（富途 OpenD 连接状态 · 行情源 · 修改自动保存），删除命令条相关样式。tsc 无错误、npm run build 通过。
1. 恢复标题栏下方与状态栏上方的横线 —— 按用户要求恢复设置窗口标题栏底部边框（border-b）与状态栏顶部边框（border-t）。tsc 无错误、npm run build 通过。
1. 去掉「网站设置 · 修改自动保存」下方第一个内容外框的边框 —— 按用户指认的位置（内容头部下方包着全部设置分组的大框）去掉其边框与阴影：各设置子页内容容器由 `rounded-card border bg-white shadow-card` 改为仅保留底色与内边距，内容与窗口融为一体，只保留内部分组卡片边框。tsc 无错误、npm run build 通过。

### 新功能

1. 设置子项重塑：整块面板 + 分组标题 + 细分隔行（参考 Linear / macOS） —— 按用户要求参考主流网站重新设计设置子项，解决「太乱」：1) 去掉每组分组的独立卡片边框 / 底色，内容区变成一整块面板，分组之间仅靠「小号灰色分组标题」区分（Linear / macOS 设置风格），行与行保留细分隔线；2) 分组标题图标隐藏，标题改为 12px 灰字分组标签，动作按钮（重置等）保留在行尾；3) 图片类设置项（网站图标 / Logo / 网站背景）默认收起为紧凑行（标签 + 缩略预览 + 更换按钮），点「更换」才展开 上传 / 粘贴直链 / 清除 / 打开 面板，不再默认铺开；4) 股票来源接口 URL 卡片自动识别 http(s) 并出现「打开链接」。全部跟随版本配色与深浅色。tsc 无错误、npm run build 通过。

### 新功能

1. 设置改为富途 macOS 桌面端风格（顶部 Tabs + 无侧栏 + 分组行） —— 按用户提供的富途 macOS 截图重塑设置窗口布局：1) 去掉左侧栏，子分类改为顶部横向 Tabs（网站设置 / 股票设置 / 个人信息 / 数据库增强 / 定时任务 / API / 关于），激活项文字高亮 + 强调色下划线（富途 macOS 的「常规 / 行情 / 交易 / 提醒 / 热键 / 安全」同款）；2) 顶部左侧保留发光小写 fire，右侧搜索图标（⌘K 弹层）+ 保存状态；3) 内容区去掉大面板底色，分组标题 + 紧凑行直接铺在窗口背景上，行间细分隔线，图片类设置项保持「收起 → 更换展开」；4) 移动端顶部 Tabs 横向滚动自适应。tsc 无错误、npm run build 通过。

### 修复

1. 恢复设置窗口为左侧栏布局（撤销富途 macOS Tabs 改动） —— 按用户要求撤销富途 macOS 顶部 Tabs 方案，恢复为上一版布局：左侧栏（fire 发光字标 + 搜索设置 + 分组导航 + V14 最近编辑 + 底部用户头像在线点）+ 内容头部（设置 / 子分类 · 修改自动保存 + 保存状态）+ 分组标题行式内容；删除顶部 Tabs 与搜索图标相关样式，分组行 / 媒体字段收起展开等此前优化保留。tsc 无错误、npm run build 通过。

### 修复

1. 导航菜单中「我的持仓」前面的图标删除（设置页导航菜单编辑器 + 主侧栏） —— 按用户要求删除「我的持仓」前的图标：1) 设置页「导航菜单」编辑器行内首字图标对 holdings 隐藏（保留占位列避免布局错位）；2) 主界面侧边导航与移动端标签的「我的持仓」图标同步隐藏，其它页签图标保留。tsc 无错误、npm run build 通过。
1. 导航菜单编辑器去掉全部行首字母图标 —— 按用户反馈（如「资产分析」前仍有「资」）把设置页「导航菜单」编辑器每行的首字图标整格移除：删除字母头像 span，网格由 4 列调整为 3 列（拖动手柄 + 默认圆点 + 名称 / URL），整行对齐不变。tsc 无错误、npm run build 通过、PM2 已重启生效。

### 新功能

1. API 文档页改为 V5 Notion 极简风格 —— 按用户要求把 `/api-docs` 页面重塑为 V5 Notion 极简风：去掉渐变背景 / 模糊光斑 / 大阴影 hero 卡片，改为「面包屑 + 标题 + 描述 + 右侧小统计」的轻量头部；内容区细边框（浅色 `#e9e9e7` / 深色 `#2b2b2b`）、8px 圆角、无重阴影；目录去掉虚线分隔、改为纯行；正文标题去掉装饰条、副标题用细分隔线；代码块 / 引用 / 表格 hover 全部用 Notion 中性色（`#f7f6f3` / `#262626`），文字用 `#37352f` / `#787774` 体系。tsc 无错误、npm run build 通过、PM2 已重启。

### 修复

1. API 文档第 6 章子节编号 6.1-6.14 —— 按用户要求给 `docs/api-spec.md` 第 6 章「路由清单」下的 7 个子节补编号：认证 / 资产持仓 / 行情数据 / 名人持仓 / 素材库上传 / 券商 / 设置 → 6.1-6.7；原数据模型章节 6.1-6.7 顺延为 6.8-6.14（文档内无交叉引用，目录锚点自动跟随），`/api/api-docs` 实时读取已生效。tsc 无错误。
1. API 文档左侧目录只显示一级分类 —— 按用户要求隐藏目录中的子节：左侧目录只渲染 `##` 一级章节（如 6.13 自选股分组），不再展示 / 展开 `###` 子节，去掉展开箭头与子节列表；子节仍保留在正文中（滚动高亮按一级章节匹配）。tsc 无错误、npm run build 通过、PM2 已重启。
1. API 文档目录：6.8-6.14 全部收进「6」作子项 —— 按用户要求把 6.8-6.14（券商 / 个股详情 / 公司简况 / 分时 / K 线 / 分组 / 订单）全部收进「6. 路由清单（v1）」作为子项，与 6.1-6.7 同级；目录只显示到 X.Y 一层（未编号的请求 / 响应示例等细节标题不进目录），展开「6」即可看到 6.1-6.14。tsc 无错误、npm run build 通过、PM2 已重启。

### 修复

1. API 文档补全 4 个缺失 v1 接口 + 路由表方法彩色徽标 —— 核对全量 v1 路由后发现 4 个未入文档的接口：GET / POST `/api/v1/financial-reports`（财报列表 / 上传）、DELETE `/api/v1/financial-reports/{id}`、GET `/api/v1/index-kline`（指数月 K）、GET `/api/v1/orders/export`（订单导出 xlsx）——已补进 6. 路由清单表格并新增 6.15 财务报表 / 6.16 指数月 K / 6.17 订单导出三节；页面优化：路由表首列 HTTP 方法渲染为彩色徽标（GET 绿 / POST 蓝 / PUT 黄 / DELETE 红 / PATCH 紫，深浅色适配）。tsc 无错误、npm run build 通过、PM2 已重启。

### 新功能

1. 素材库新增「icon」类目（贵金属之后，收录全站全部图标） —— 按用户要求：素材库新增「icon」页签（位于贵金属之后，页签名即英文 icon），把全站当前使用的 25 个导航 / 操作 / 设置图标全部收录为可管理素材（settings / holdings / assets / watchlist / global / earnings / celebs / activities / users / attachments / library / site / sitemanage / stocks / profile / database / api / cron / about / trade / search / plus / refresh / close / check，SVG 存于 `/uploads/asset/icon/`，首次进入自动播种）；每个图标配规范中文展示名（设置 / 持仓 / 资产 / 自选 / 全球预览 / 盈利 / 名人 / 动态 / 用户 / 附件 / 素材库 / 网站 / 网站管理 / 股票 / 个人信息 / 数据库 / 接口 / 定时 / 关于 / 交易 / 搜索 / 新增 / 刷新 / 关闭 / 完成），名称与代码均可点击复制（非安全上下文自动回退）；支持行内上传替换（全局生效）、编辑名称、恢复默认，按内置规范顺序展示，浅色 / 深色均以灰底圆角瓦片呈现。tsc 无错误、npm run build 通过、PM2 已重启。

### 修复

1. 素材库页签排序：加密货币 / 贵金属移到股票图标之后 —— 按用户要求调整素材库顶部页签顺序：股票图标 → 加密货币 → 贵金属 → 市场图标 → 券商图标 → 分组图标 → icon，便于常用类目优先展示。tsc 无错误。

### 新功能

1. 设置-导航菜单：全部类别图标可上传（相机）+ 侧栏全局生效 —— 按用户要求：设置 → 网站设置 → 导航菜单中每个类别行左侧都显示图标并内置相机浮层（我的持仓 / 资产分析 / 自选股 / 财报日历 / 名人持仓 / 用户 / 附件 / 素材库 / 日志 / 设置等），点击即可上传 / 更换；图片写入素材库 `icon:<KEY>`（与素材库 icon 类目同一份数据，全局生效），主侧栏与移动端标签同步显示自定义图标（无自定义时回退各类别默认图标，默认图标抽到 `lib/navIcons.tsx` 与侧栏共用同一套）。上传接口 icon 文件夹白名单、文件清理归类同步补齐。tsc 无错误、npm run build 通过、PM2 已重启。

### 修复

1. 素材库标题去图标 + 删除描述文字 —— 按用户要求：素材库页标题「素材库」左侧的标题图标移除，并删除下方描述「股票图标（按市值选股、分市场管理）与市场图标，唯一标识全局生效，素材保存在本地」，标题更简洁；`SettingsHeader` 增加 `hideIcon` 开关，其它设置页不受影响。tsc 无错误。

### 修复

1. 网站形象：相机改为图片内悬停浮层 + 网站图标与首页 Logo 预览统一尺寸 —— 按用户反馈修复设置 → 网站设置 → 网站形象：1) 上传相机改为图片内悬停浮层（整块预览覆盖半透明黑底 + 居中相机图标，与素材库相机交互一致），不再是贴右下角的角标；2) 首页 Logo 预览由 44×56 横条改为与网站图标一致的 44×44 方块，并补齐网站图标预览边框，两块视觉大小统一。tsc 无错误。

### 修复

1. 素材库 icon：操作列去掉相机按钮 + 删除图标放大 —— 按用户反馈：素材库 → icon 类目每行右侧「操作」列不再显示相机上传按钮（上传统一走左侧预览图悬停相机），删除 / 恢复默认图标由 4px 放大到 16px，按钮更清晰。tsc 无错误。

### 修复

1. 网站形象预览悬停去灰罩（主题色描边 + 白底相机） —— 按用户反馈：网站图标 / 首页 Logo / 网站背景预览悬停时不再盖半透明黑灰罩，改为跟随设置窗口版本主题色描一圈细边，并在图片中央浮现小白底相机图标（主题色相机），观感更干净、深浅色与四个版本主题均适配。tsc 无错误。

### 修复

1. 网站形象预览相机按图片比例缩放 —— 按用户反馈：预览图较小时（如 16×16 favicon）悬停相机圆片显得过大；改为相机圆片高度约为预览图高度的 40%、相机图标再按圆片 58% 缩放，小图小相机、大图大相机，不再盖过小图。tsc 无错误。

### 修复

1. 网站形象预览相机深色模式适配 —— 修复深色模式下相机圆片过亮、浅色主题相机看不清的问题：深色模式相机圆片由亮白改为深灰（`#2b2b31` + 白描边），相机图标继续用当前版本强调色（V14 淡紫 / V1 橙 / V7 珊瑚 / V5 浅灰），深浅色与四个版本主题均清晰。tsc 无错误。

### 修复

1. 恢复网站形象预览相机为最初角标样式 —— 按用户反馈撤销悬停浮层系列改动：网站图标 / 首页 Logo / 网站背景预览恢复为最初的右下角相机小圆标（主题色底 + 白色相机 + 卡片色描边，悬停无灰罩 / 无浮层），观感回到改动前。tsc 无错误。

### 修复

1. 恢复网站形象预览相机为「太灰」提示之前的悬停浮层 —— 按用户澄清：恢复为提出「鼠标划过背景颜色太灰」之前的状态——网站图标 / 首页 Logo / 网站背景预览悬停时整块盖半透明黑罩（`bg-black/35`）并在图片中央显示白色相机图标，不再是角标样式。tsc 无错误。

### 修复

1. 网站形象预览：悬停黑罩加深至 50% + 空预览灰底改透明 —— 按用户要求：1) 悬停半透明黑罩由 35% 加大到 50%，呈纯黑压暗而非发灰；2) 网站图标空预览的内层灰底（`bg-bg-gray`）改为透明，未上传时不再显示灰色方块，直接透出白色 / 深色瓦片。tsc 无错误。

### 修复

1. 网站形象预览悬停去黑罩（仅主题色相机图标） —— 按用户要求移除悬停黑罩：网站图标 / 首页 Logo / 网站背景预览悬停时不再压黑背景，仅在图片中央浮现主题色相机图标（跟随当前版本强调色，带轻微投影保证在浅图上也可见），观感更轻。tsc 无错误。

### 修复

1. 恢复网站形象预览 50% 黑罩 + 白色相机 —— 按用户要求撤销「去黑罩」改动：网站图标 / 首页 Logo / 网站背景预览悬停恢复为整块 50% 黑罩 + 中央白色相机图标，空预览透明底保留。tsc 无错误。

### 修复

1. 恢复至「太灰」提示之前：35% 黑罩 + 灰占位底 —— 按用户再次澄清：恢复为提出「鼠标划过背景颜色太灰」之前的状态——悬停黑罩由 50% 改回 35%，空预览占位底由透明改回 `bg-bg-gray` 灰底，白色相机居中不变。tsc 无错误。

### 新功能

1. 交易设置新增富途 OpenAPI 额度（已用 / 总额） —— 设置 → 交易新增「OpenAPI 额度」行：通过富途 OpenD 的 `query_subscription`（实时订阅额度）与 `get_history_kl_quota`（历史 K 线额度）展示 已用 / 总额 / 剩余，OpenD 在线时自动查询，也可点「查询额度」手动刷新；桥接脚本新增 `quota` 命令，新增 `POST /api/futu/quota`（管理员）。实测 OpenD 返回 实时订阅 0 / 100、历史K线 0 / 100。tsc 无错误、npm run build 通过、PM2 已重启。

### 修复

1. OpenAPI 额度改为仅手动查询 —— 按用户要求去掉打开设置页 / 刷新网页时的自动额度查询，额度只在点击「查询额度」时拉取，避免每次刷新都打一次 OpenD。tsc 无错误。

### 修复

1. 全球预览去重：贵金属 / 加密货币不再重复 —— 排查发现重复根因：CMC「资产页」本身已含 Gold / Silver / Platinum / Palladium 等贵金属（英文名），后端又追加素材库贵金属（中文名）合并，导致每种金属出现两次。修复：合并后做硬性去重——贵金属 / 加密货币按 类型 + 代码、股票按 市场 + 代码 去重，素材库版本优先，再按市值排序取前 100；实测全球预览贵金属仅剩 4 条（黄金 / 白银 / 铂金 / 钯金）、全榜无重复。tsc 无错误、npm run build 通过、PM2 已重启。

### 修复

1. 全球预览-资产列美化（图标左对齐 + 名称截断对齐） —— 按用户反馈美化全球预览「资产」列：由居中对齐改为左对齐，图标固定在列首，名称 / 代码紧随其后；名称块设置最大宽度 190px 超出省略号截断，长名称不再撑乱列宽，整列图标与文字对齐整齐。tsc 无错误。

### 修复

1. 全球预览排名改为分享页持仓列表同款（金 / 银 / 铜大号数字） —— 按用户要求把全球预览的排名序号由圆形小徽标改为 资产分析-分享页-持仓列表 同款效果：去掉圆底，第 1 / 2 / 3 名分别用金色 `#f5a623`（24px 加粗）、银色 `#9aa3ad`（20px 加粗）、铜色 `#c8864a`（17px 半粗），其余灰色 `#9298a1`（14px 半粗），跨页时按全局排名取色。tsc 无错误。

### 修复

1. 修复美股盘后当日盈亏重置（富途 `after_change_val` 字段口径错误） —— 排查「美东未到 20 点当日盈亏被重置」：富途快照的 `after_change_val` 实测为「盘后价 - 常规收盘价」（≈0），并非「盘后价 - 昨收」，脚本直接使用导致盘后 16:00-20:00 当日盈亏≈0。修复：盘后统一按 `after_price - prev_close` 计算涨跌（昨收缺失才回退官方字段）；并按「24 小时行情 + 美东 20:00 结算」口径，夜盘时段价格继续显示 `overnight_price`，但涨跌锁定为盘后最终价（`after_price - prev_close`），夜盘波动不再改动当天盈亏。实测盘后 AAPL +4.35 / NVDA -5.05 / ROBN -2.39，不再归零。

### 修复

1. 美股股价显示 3 位小数 —— 按用户要求全站统一：美股（US）股价保留 3 位小数，其它市场 2 位。`fmtPrice` 增加 `market` 参数、新增 `fmtNumMarket`；覆盖我的持仓（现价 / 成本价 / 详情）、自选股行情、股票搜索联想、资产分析持仓表、个股详情（现价 / 最高 / 最低 / 今开 / 昨收 / 52 周高低 / 均价 / 相关 ETF）、交易订单、素材库股票列表、名人持仓持股价格、全球预览 USD 价格、当日盈亏分享图持仓列表、K 线 Y 轴刻度。tsc 无错误。

### 修复

1. 修复当日盈亏刷新闪变（-81 ↔ -936） —— 排查「美股当日盈亏刷新显示 -81，多刷几次恢复 -900+」：净资产 / 持仓市值一致但涨跌不同，根因是行情快照来源不一致——旧版缓存含盘后涨跌≈0 的错误快照（修复前写入），页面先读缓存显示小数值；富途桥接进程并发连接 OpenD 会互相阻塞超时，实时请求偶尔失败时旧缓存继续生效。修复：1) 富途桥接调用全局串行化（避免并发连接超时回退腾讯）；2) 行情缓存换 `v2` 版本号并缩短有效期为 30 分钟（作废旧错误快照，跨天不再闪旧盈亏）；3) 本次请求了但未返回的标的不再用旧缓存补值（删除后显示同步中），杜绝用过期涨跌拼出假盈亏。实测连续 5 次请求美股涨跌完全一致。tsc 无错误、npm run build 通过、PM2 已重启。

### 修复

1. 修复素材库股票图标二次上传被重置 —— 排查「素材库 NASA 上传股票图标后又被重置」：DB url 仍指向旧 PNG，但磁盘上有 18:46 上传的 SVG，DB `updated_at` 也停在 18:46——说明上传的文件写成功了、DB 写入也发生了，但 url 没更新。根因：`upsertAsset` 的 `ON CONFLICT` 对 `source='manual'` 的行保护 url / name（防自动同步覆盖），但把用户自己的再次上传也挡住了（该上传路径不带 id，路由的强制更新分支不触发）。修复：upsert 逻辑改为「自动同步仍保护 manual 行，但手动写入（source=manual）允许覆盖 url / name / board」，素材库股票上传同时显式带上 id；NASA 已恢复为用户最新上传的 SVG 图标。复现验证：第二次上传不带 id 时 url 由 `/a.png` 正确更新为 `/b.svg`。tsc 无错误、npm run build 通过、PM2 已重启。

### 修复

1. 美股夜盘当日盈亏与券商对齐（昨日结算归档，今日=夜盘波动） —— 排查「有夜盘价格了但当日盈亏还是未更新 / 显示 -800 而券商不到 -100」：根因是夜盘时段（美东 20:00-04:00）被当作「闭市」，轮询跳过美股；且盈亏口径沿用旧一天的整天涨跌。按券商口径对齐：美东 20:00 为交易日分界，旧一天结算归档，夜盘属于新一天，当日盈亏 = 夜盘波动（`overnight_price - 今日常规收盘`，即富途官方 `overnight_change_val`），与券商一致（实测券商 -124，我们同口径 -151，差异为行情时点 / 持仓差异）。修复：1) `marketSessions` 新增 overnight 活跃时段，轮询继续、夜盘价格 24 小时实时更新；2) 夜盘涨跌改用富途 `overnight_change_val / overnight_change_rate`；3) 美股仅周末标记已结算；4) 行情缓存升级 `v3`，作废旧一天整日盈亏的旧快照，刷新不再闪现旧数值。tsc 无错误、PM2 已重启。

### 修复

1. 行情路由拆分：美股走富途，港股/A股走腾讯 —— 按用户要求「美股走富途、港A用更成熟的数据源」：`fetchQuotes` 把美股与港股/A股拆开——美股单独走富途 OpenAPI（保证盘前/盘后/夜盘口径与 24 小时行情），港股/A股始终直接走腾讯（成熟稳定）；「仅富途」模式下美股不回退腾讯，但港A仍走腾讯。修复了此前美股+港A混批导致富途桥接超时、整体回退腾讯变成常规收盘口径（当日盈亏回到 -800 量级）的问题。实测完整 24 只持仓请求：美股全部 OVERNIGHT 口径（AAPL 0.03），港A腾讯 REGULAR，不再整体回退。tsc 无错误、npm run build 通过、PM2 已重启。

### 修复

1. OpenAPI 额度查询保留上一次结果 —— 按用户要求：额度查询中 / 失败 / 刷新页面时保留上一次成功查询的结果——成功结果写入 localStorage（`fire:futu-quota`），页面加载直接恢复显示并标注「查询于 HH:mm」；查询中不闪空、失败不清空。tsc 无错误。

1. 个股详情页状态区分盘前/盘中/盘后/夜盘 —— 按用户要求：个股详情页市场状态由「已收盘」改为按时段区分——盘前、盘中、盘后、夜盘（美东 20:00-04:00）、休市（周末）；夜盘新增为活跃时段，详情页主行情在夜盘也每 30 秒自动刷新，状态与价格同步实时。tsc 无错误。

---

## v0.1.11 · 2026-08-18

> 左上角 FIRE 字标改用 kiro.dev 同款字体（AWS Diatype Rounded Semi Mono Bold），字体文件本地化，深浅色模式一致。

### 新功能

1. FIRE 字标换用 kiro.dev 同款字体 —— 左上角品牌字标由系统粗体换成 kiro.dev 左上角 KIRO 使用的 AWS Diatype Rounded Semi Mono Bold（700，19px 下比 Regular 更有存在感）；字体文件从 kiro.dev 本地化到 `public/fonts`（woff2，Regular 400 同时保留），不依赖外网字体服务；应用于后台 / 首页左上角 LOGO 文字（SiteLogo 与首页 Logo）及登录页桌面 / 移动端品牌文字；深浅色共用同一字体，颜色跟随主题（浅色深字、深色浅字）。tsc 无错误、npm run build 通过。
1. Logo 字体可在设置中切换 + 网站形象一键重置 —— 设置 → 网站形象 → Logo 字体新增三个选项：KIRO 同款·粗体（默认）/ KIRO 同款·常规 / 系统粗体，选择后立即预览、保存后全站左上角 LOGO（后台、首页、登录页）同步生效；新增 `logoFont` 设置项（类型、默认值、保存 / 公开接口 v1/v2 全链路）；网站形象区块新增「重置」按钮（二次确认），一键把网站图标、Logo 图片、Logo 文字、Logo 字体、网站背景恢复默认并保存。tsc 无错误。

### 修复

1. 修复美东夜盘结束后当日盈亏仍显示昨日的问题 —— 美东夜盘（20:00-04:00）结束后进入盘前，当日盈亏此前仍停留在上一交易日：1) 盘前/盘后基准价错误——应用优先采用腾讯「昨收」，而盘前刚开始时腾讯仍返回更早收盘（实测美股盘前 04:08 腾讯昨收还是上周五收盘，Yahoo regularMarketPrice 才是最近常规收盘），导致盘前涨跌把昨日涨跌混入；2) 盘前尚无成交时 Yahoo 无当日盘前点，扩展行情整批失败退回腾讯昨收，当日盈亏直接显示昨日。修复：扩展时段统一以 Yahoo regularMarketPrice（最近常规收盘）为基准；盘前暂无成交时返回「基准价 + 当日盈亏归零」，不再沿用昨日。实测 AAPL 盘前 prevClose 305.93→305.59、涨跌 +0.31→+0.51，SACH 无盘前成交显示 0。tsc 无错误、npm run build 通过。

### 新功能

1. 行情源接入富途 OpenAPI（OpenD 网关，全局优先 + 自动回退） —— 按用户要求评估并接入富途接口：安装 Futu_OpenD（GUI 10.10.7008）与 futu-api Python SDK，新增 `scripts/futu_quotes.py` 桥接脚本（get_market_snapshot / get_search_quote），Node 侧新增 `lib/futuQuotes.ts`（端口预检 + 可用性缓存 + 美股盘前/盘后/夜盘 session 提示）。行情与搜索全局改为富途优先：快照直接覆盖腾讯 + Yahoo（含 `pre_price` / `after_price` / `overnight_price` 盘前盘后夜盘三档涨跌），搜索走 `get_search_quote`；OpenD 未安装 / 未登录 / 失败时自动回退腾讯 + Yahoo + 东财，不影响现有功能。健康检查新增 `quoteSource` 与 `futuOpenD` 状态。登录 OpenD 后即为全局主行情源。tsc 无错误。
1. 行情源手动切换（自动 / 仅富途 / 腾讯 + Yahoo） —— 设置 → 股票来源接口 → 行情源新增三个选项：自动（富途优先，失败回退腾讯 + Yahoo，默认）/ 仅富途 / 腾讯 + Yahoo；新增 `quoteSource` 设置项（类型、默认值、保存接口白名单校验、加载归一化），行情与搜索均按该配置决定是否启用富途，强制富途时不再回退备用源；股票来源接口区块顶部显示富途 OpenD 连接状态（已连接走富途 / 未连接走备用源），健康检查返回 `quoteSource` 与 `effectiveSource` 两个字段。tsc 无错误、端到端实测保存 / 还原正常。
1. 富途 OpenAPI 连接配置进系统设置 + 新增「交易」区块 —— 为线上部署做准备：富途 OpenD 网关地址不再写死本机，新增 `futuHost` / `futuPort` 设置项（默认 `127.0.0.1:11111`，可指向远程 OpenD；类型、默认值、保存接口校验、加载归一化全链路）；设置 → 我的持仓（stocks）新增「交易」区块，内含两块独立卡片：「富途 OpenAPI 配置」（主机 + 端口 + 测试连接按钮，真实拉一次 AAPL 快照验证可达与登录）+「行情源」切换（自动 / 仅富途 / 腾讯 + Yahoo，从股票来源接口移入）；股票来源接口不再包含行情源；新增 `POST /api/futu/test` 连接测试接口（管理员，支持临时 host / port）；健康检查按已保存配置检测。tsc 无错误、保存 / 还原 / 测试接口实测正常。

### 修复

1. 修复富途测试连接：OpenD 绑定局域网地址 + SDK 日志污染 stdout —— OpenD 登录后设置页测试连接仍失败，排查到两个原因：1) 本机 OpenD 配置的监听地址是局域网 IP `localhost` 而非 `127.0.0.1`（OpenD.xml 中 ip / telnet_ip / websocket_ip 均绑定该地址），默认主机配置连不上——已将 `futuHost` 设置项更新为实际监听地址，并在配置说明里提示局域网地址场景；2) 富途 SDK 的 FTConsoleLog 默认把日志打到 stdout，混入桥接脚本 JSON 导致 Node 解析失败——桥接脚本静默 console logger，Node 侧解析改为只取首个 `{` 到最后一个 `}` 的 JSON 片段。修复后实测：测试连接 0.7s 返回成功（AAPL 305.59）、行情全局切富途（AAPL 盘前 307.91 / +2.32 / prevClose 305.59）、健康检查 effectiveSource=futu。tsc 无错误。

### 新功能

1. 设置页 2.0 重构：⌘K 命令搜索 + 全局自动保存 + 极简双栏布局 —— 按用户要求用前沿交互重构臃肿的设置页：1) 删除大尺寸 hero 卡、竖 / 横排切换与各模块零散保存按钮，改为紧凑顶栏（标题 + 全局保存状态胶囊 + 版本徽标）；2) 新增 ⌘K 命令搜索——输入 Logo / 富途 / 数据库 / 密码等关键词即时匹配 12 个设置项，方向键 + 回车或点击跳转到对应模块并高亮定位；3) 全局自动保存（React 19 风格）：站点信息、网站形象、首页指数、首页导航、股票来源接口、交易（富途 + 行情源）等 22 个字段修改即保存，700ms 防抖，顶栏实时显示「保存中… / 已保存 HH:MM / 保存失败·重试」，失败可一键重试；4) 左侧导航改为极简图标栏（激活态高亮），移动端保留横向胶囊；5) 券商管理因含「清空二次确认」保留独立保存按钮，数据库配置保留显式保存，其余全部自动保存。tsc 无错误、npm run build 通过。

### 新功能

1. 设置改为独立桌面窗口（弹窗式）+ 右上角齿轮入口 + 深浅色跟随主题 —— 按用户最终选定的桌面客户端布局（V16/V17）落地：1) 设置从页面页签改为独立弹窗窗口（`SettingsWindow`：960×720 居中、窗口标题栏红绿灯 + Fire 设置 + 版本、底部状态栏显示富途 OpenD / 行情源 / 自动保存），从任意页面点右上角齿轮或导航「设置」都能打开，Esc / 红点 / 点击遮罩关闭；2) 深色 / 浅色随右上角主题切换自动变化（同一份样式，不再固定单主题）；3) 窗口内布局改为紧凑桌面风格——184px 侧栏（搜索设置按钮 + 分组导航 + 底部管理员）+ 主区行式设置项（站点信息、交易 · 富途等改为「名称在左、控件在右」行式面板），⌘K 搜索保留在内容区顶部；4) 导航「设置」页签点击直接开窗，不再切换页面内容。tsc 无错误、npm run build 通过。

### 新功能

1. 设置入口改为左下角常驻头像（桌面客户端风格），移除导航「设置」页签 —— 按用户要求彻底桌面客户端化：1) 移除侧边导航与移动端导航里的「设置」页签（默认页签数组、设置页导航菜单编辑、加载归一化全部过滤掉，历史数据库里残留的设置页签也不再显示）；2) 新增左下角常驻头像入口——圆形头像（真实头像或首字 + 右下角小齿轮角标），悬浮于页面左下角，点击打开设置窗口，任意页签可用；3) 右上角头像菜单里的「设置 / 个人信息」也统一改为打开设置窗口（个人信息定位到 profile 子页），不再跳转页面；4) `/settings` 深链直接开窗并落在默认页签，设置子分类导航只在窗口内切换，不再影响主页面内容。tsc 无错误、npm run build 通过。

### 修复

1. 设置窗口搜索去重 + 左下角改齿轮图标 + 窗口内头像补真实头像与在线点 —— 细节收尾：1) 设置窗口原来有「侧栏搜索按钮 + 内容区搜索框」两个搜索，删掉内容区第二个，只保留左上角侧栏顶部一个「搜索设置」，点击展开下拉搜索面板（⌘K 快捷键同步可用）；2) 页面左下角常驻入口由头像改为设置齿轮图标（圆形按钮 + 齿轮，悬停放大），不再占用头像语义；3) 设置窗口左下角侧栏底部的用户头像改为显示真实头像（有 `user.avatar` 用图片，无则首字兜底），并在头像右下角边框处加绿色在线状态点（深浅色边框适配）。tsc 无错误、npm run build 通过。

### 修复

1. 设置由弹窗式改为页面内无感平滑过渡 —— 按用户要求去掉弹窗形态：删除 SettingsWindow 弹窗壳与齿轮弹窗入口，设置恢复为页面内页签内容（桌面客户端式紧凑侧栏 + 行式设置项保留），点左下角齿轮或头像菜单「设置 / 个人信息」时在当前页面内无感切换并带平滑淡入过渡（tab-panel 已有的 fade + 上移动画，`prefers-reduced-motion` 自动降级），不弹窗、不跳转、不刷新；`/settings` 深链直接落在设置页签。tsc 无错误、npm run build 通过。

### 修复

1. 设置页恢复客户端边框卡片（保持原有尺寸不变） —— 按用户反馈修复页面内设置页卡片视觉：1) 设置页内容区加客户端底色（浅色 `#f3f3f3` / 深色 `#1c1c1c`），让卡片与页面区分开；2) 设置分组卡片保持原有尺寸 / 内边距 / 圆角不变（撤销此前 12px 紧凑内边距、34px 输入框、按钮缩小等尺寸改动），仅加强可见性——浅色白底 + 细边框 + 轻阴影，深色深底 + 边框 + 阴影；3) 外层设置卡片容器原样保留。tsc 无错误、npm run build 通过。

### 修复

1. 澄清设置形态：仅去掉弹窗遮罩，960px 客户端窗口壳页面内嵌、宽度不变 —— 按用户纠正：之前误把设置改成铺满页面的宽布局。修正为——只移除弹窗的遮罩 / 悬浮 / 居中定位，桌面客户端窗口壳原样保留并直接嵌进页面内容区：固定 960px 宽（`max-w-[960px]` 居中）、720px 高（小屏自适应）、窗口标题栏（红绿灯 + Fire 设置 + 版本）、紧凑侧栏、行式设置卡片、底部状态栏全部不变；进入设置带平滑 pop-in 过渡，宽高与弹窗版一致，不再变宽。tsc 无错误、npm run build 通过。

### 修复

1. 设置加回导航页签 + 移除左下角设置图标 —— 按用户要求：1) 「设置」页签重新加回桌面侧边导航与移动端导航（默认页签、导航菜单编辑、服务端加载归一化均恢复，历史数据缺设置页签时自动补上），点击后仍以 960px 客户端窗口壳在页面内平滑呈现；2) 移除左下角常驻齿轮设置图标，设置入口统一走导航「设置」页签与右上角头像菜单。tsc 无错误、npm run build 通过。

### 新功能

1. 导航页签可拖动排序并自动保存，「设置」默认在最左边 —— 按用户要求：1) 桌面侧边导航页签支持鼠标拖动排序（cursor-grab，拖到目标位置松手即重排），顺序变化自动保存到 `site_settings.tabs` 并全局生效（刷新 / 换设备保持）；2) 默认顺序调整——「设置」页签排在导航最左边（默认页签数组三处、服务端缺省补齐逻辑均改为 settings 在首位，当前数据库已迁移为 settings 在第一位）；3) 移动端标签栏保持静态顺序跟随。tsc 无错误、npm run build 通过、端到端实测保存 / 还原正常。

### 修复

1. 设置窗口壳可拖动 + 默认靠左；撤销「设置页签默认最左」 —— 按用户要求调整：1) 设置 960px 客户端窗口壳支持按住标题栏拖动（pointer capture，光标变为 move，拖动过程中实时跟随、上下左右不越界），位置自动保存到 localStorage（`fire:settings-window-pos`），默认位置为最左边（x:0, y:0，不再居中）；2) 撤销上一条「设置页签默认最左边」——默认页签顺序与缺省补齐逻辑恢复为 settings 在末尾，当前数据库导航顺序已还原（holdings…activities, settings），导航页签拖动排序 + 自动保存功能保留。tsc 无错误、npm run build 通过、端到端实测顺序保存 / 还原正常。

### 新功能

1. 日志 / 附件管理 / 用户管理 / 名人持仓统一套用设置页客户端窗口风格 —— 抽出通用 `ClientWindow` 组件（960px 窗口框架 + 红绿灯标题栏 + 标题 / 徽标 + 可选底部状态栏 + 可选拖动），设置窗口改为基于它实现；日志、附件管理、用户管理、名人持仓四个视图在 RecordsApp 里全部用 `ClientWindow` 包裹，居中显示，与设置页同款边框 / 圆角 / 阴影 / 进入动画，保持桌面客户端统一观感（权限守卫逻辑不变）。tsc 无错误、npm run build 通过。

### 修复

1. 修复设置窗口可拖动与默认靠左未生效 —— 排查并修复：1) 拖动改为 mousedown + window mousemove / mouseup（弃用 pointer capture），拖动更可靠，且位置用 ref 实时镜像，松手时保存最新位置；2) 位置恢复从 useState 初始化改为挂载后 useEffect 读取，避免水合不一致导致交互失效；3) 本地位置校验（非法 / 越界自动回 0,0）；4) 存储键升级为 `fire:settings-window-pos:v2`，清除旧版残留位置——本次更新后首次打开设置窗口必然落在最左边 (0,0)，之后拖动的位置才被记住。tsc 无错误、npm run build 通过。

### 修复

1. 恢复至 20:36 状态（撤销四个视图套窗口壳） —— 按用户要求恢复到 20:36 的状态：移除通用 `ClientWindow` 组件与「日志 / 附件管理 / 用户管理 / 名人持仓」的客户端窗口壳（四个视图恢复为原有独立页面）；设置窗口恢复为独立的 960px 页面内嵌窗口壳（标题栏红绿灯 + Fire 设置 + 版本 + 底部状态栏），按住标题栏可拖动、默认靠左、位置自动保存（保留可靠版拖动实现，避免拖不动）；导航页签可拖动排序 + 自动保存、「设置」页签位于末尾、左下角无齿轮图标等均保持恢复点状态。tsc 无错误、npm run build 通过。

### 修复

1. 用户管理：按 UID 排序 + 进入秒开（缓存 + 即时渲染表壳） —— 用户列表按 UID 数字升序，测试账号固定在最后；新增短缓存与即时表壳渲染，编辑、重置密码、删除和手动刷新后强制重新拉取。tsc 无错误、npm run build 通过。

### 新功能

1. 设置窗口右上角新增版本切换（V1 / V5 / V7 / V14） —— 按用户要求在设置窗口标题栏右上角（v0.1.11 徽标旁）新增版本切换 icon（调色盘 + 时钟），点击弹出菜单含 V1 富途橙 / V5 Notion / V7 Claude / V14 桌面客户端 四个版本（每项带三段色卡预览 + 当前项对勾）；选中后设置窗口整体换肤——窗口壳（标题栏 / 侧栏 / 状态栏 / 卡片 / 输入框 / 文字 / 强调色 / 标题字体）改为 CSS 变量驱动，四套版本各自定义完整配色（V1 深蓝侧栏 + 橙、V5 白底极简黑、V7 暖米 + 陶土橙 + 衬线标题、V14 深色 + 淡紫强调），选择持久化到 localStorage（`fire:settings-version`），默认 V14。tsc 无错误、npm run build 通过。

### 修复

1. 版本切换适配浅色 / 深色主题 —— 修复版本换肤与右上角主题切换不联动的问题：四个版本（V1 / V5 / V7 / V14）各自定义完整的浅色与深色两套 CSS 变量——浅色主题下用各版本浅色系（V14 白底黑强调、V1 浅灰 + 橙、V7 暖米 + 陶土橙、V5 纯白极简），切到深色主题后自动切换各版本深色系（V14 深灰 + 淡紫、V1 深蓝 + 橙、V7 暖黑 + 陶土橙、V5 近黑极简）；窗口壳、卡片、输入框、文字、边框、强调色、下拉菜单全部跟随变量联动。tsc 无错误、npm run build 通过。

---

## v0.1.10 · 2026-08-16

> 资产分析持仓盈亏排行统一展示股票图标；修复当日盈亏分享图复制后圆角丢失、与预览不一致。

### 修复

1. 持仓盈亏排行改用股票图标 —— 资产分析-持仓盈亏排行不再显示市场国旗，统一复用持仓表的股票图标解析与 ETF 正股图标映射；图标缺失时使用股票名称首字兜底。TypeScript 检查通过。
1. 当日盈亏分享图复制后保留圆角透明背景 —— 修复资产分析-账户资产-分享当日盈亏图复制出来与弹窗预览不一致：`html2canvas` 导出时不再强制填充白底，改为透明背景，让卡片自身的 48px 圆角与深浅色背景在复制出的 PNG 中保持一致，避免导出成白色矩形。tsc 无错误、npm run build 通过。
1. 每日盈亏分享弹窗预加载头像与模板图 —— 修复点开每日盈亏分享弹窗时短暂显示默认头像/名称及右侧 5 张模板图空白：当前登录用户信息从外层 `RecordsApp` 一路传入分享弹窗，首帧即显示真实昵称与头像；同时新增模板图预加载（资产分析页挂载和点击分享时按盈亏方向预加载），避免打开后再请求图片造成闪空。tsc 无错误、npm run build 通过。
1. 分享弹窗内复制成功提示层级修复 —— 修复每日盈亏分享弹窗内点「复制图片」后成功提示被弹窗遮挡的问题：全局 `Toaster` 层级由 `z-[90]` 提升到 `z-[10001]`，高于分享弹窗的 `z-[10000]`，复制成功/失败提示现在会显示在弹窗上方。tsc 无错误、npm run build 通过。
1. 复制图片反馈提速并统一提示文案 —— 排查每日盈亏分享复制图片成功提示晚出约 1 秒的问题：`html2canvas` 改为弹窗挂载时即预加载，复制点击时先弹出「正在生成图片…」避免无反馈，完成后再提示「已复制图片」；避免首帧动态 import 和等待生成期间用户以为无响应。tsc 无错误、npm run build 通过。
1. 资产分析页签 URL 由 `/assets` 改为 `/asset-analysis` —— 资产分析从我的持仓剥离后一直沿用 `/assets`，与素材库 `/api/assets` 语义混淆；现将默认导航 URL 改为 `/asset-analysis`，并对已保存到 `site_settings.tabs` 的旧 `/assets` 做运行时兼容迁移，旧地址访问会自动回到新地址。tsc 无错误、npm run build 通过。
1. 资产分析页刷新加载优化 —— 资产分析页刷新时原为客户端动态懒加载，首屏先显示「加载中…」几秒；改为 SSR 直出（`AssetAnalysisView` dynamic `ssr:true`），服务端首帧直接渲染资产分析内容，避免刷新后整页加载中。tsc 无错误、npm run build 通过。
1. 资产分析趋势图本地缓存无感刷新 —— 资产分析刷新时趋势图和持仓盈亏排行会重新请求历史 K 线，短暂显示「正在汇总真实历史行情…」。现新增 `localStorage` 趋势缓存：刷新时若持仓、货币、基准一致，先用上次成功趋势/收盘数据立即渲染，再后台刷新更新，避免可见 loading。tsc 无错误、npm run build 通过。
1. 项目与网站品牌统一为 Fire —— 按用户要求暂时将项目/网站名称统一为 Fire，清理对外展示中的旧品牌名称：`package name`、默认站点标题/Logo、首页/后台页脚、关于/版本弹窗、桑基图分享品牌、健康检查服务名、财务与名人数据源 UA 等均改为 Fire；内部旧缓存键/事件名/数据库文件名暂不迁移，避免破坏现有数据。tsc 无错误、npm run build 通过。
1. 内部标识与数据库文件名统一迁移为 Fire —— 在品牌统一为 Fire 的基础上，继续迁移内部标识：`localStorage` 前缀、全局事件名、会话/主题 Cookie 名、备份文件名、脚本与数据库文件名均改为 `fire`；旧 `localStorage` 与 Cookie 做一次性兼容迁移（旧键自动复制到新键后清理，旧会话 Cookie 继续兼容读取），数据库旧文件已备份为 `fire-pre-migration.db` 后迁移为 `fire.db`。tsc 无错误、npm run build 通过。
1. 修复登录/注册页与后台路由之间反复刷新 —— 内部会话 Cookie 迁移后，后台布局仍只读取新 `fire_session`，导致持有旧会话 Cookie 的浏览器在 `/login` 与 `/records` 之间反复跳转刷新。修复：后台布局、改密、v1 登出等服务端直接读取会话 Cookie 的位置全部兼容旧会话 Cookie，登录/注册页不再循环刷新。tsc 无错误、npm run build 通过。
1. 修复迁移后股票图标与全球预览加密货币/贵金属缺失 —— Fire 迁移后排查两处：1) `useAssetIcons` 本地缓存若为空数组会阻止重新请求 `/api/assets`，导致自选股/持仓股票图标消失；改为空缓存不生效并强制拉取最新素材。2) 全球预览 `market=ALL` 现在只返回公司/ETF，缺少加密货币与贵金属；改为在 `top-stocks` 接口合并素材库 `crypto/metal` 并按市值排序。tsc 无错误、npm run build 通过。
1. Fire 迁移连锁问题收尾审计 —— 继续排查 Fire 迁移连锁问题：登录/注册与 v1 登出时同步清理旧会话 Cookie；历史备份目录从旧名改为 `fire-*`；测试数据文件中的旧邮箱/旧名清理；全项目源码、脚本、文档、数据配置中已无旧品牌标识。tsc 无错误、npm run build 通过。
1. 我的持仓账户资产删除当日盈亏结算提示文字 —— 按用户要求删除我的持仓-账户资产-当日盈亏卡片下方「已结算 · 日期 · 各市场当地结算时点」提示文字，保留当日盈亏金额展示。tsc 无错误、npm run build 通过。
1. 后台左侧导航选中背景改为直接跟随按钮 —— 修复我的持仓、素材库等导航项点击后白色选中背景偏移到下一项的问题：去掉根据索引 `translateY` 的绝对定位背景，改为每个按钮自身在激活态显示白色背景，避免动态导航顺序/数量变化导致背景错位。tsc 无错误、npm run build 通过。
1. 我的持仓删除按市场汇总提示文字 —— 按用户要求删除我的持仓下「按市场汇总持仓盈亏，币种按各自市场」提示文字，保留持仓盈亏汇总内容。tsc 无错误、npm run build 通过。
1. 首页自选股/我的持仓新增排序 —— 为首页前端「自选股」「我的持仓」表格增加列排序：股票名称、现价、涨跌幅可排序；我的持仓额外支持持仓盈亏排序；点击表头切换升序/降序，第三次点击恢复默认顺序。tsc 无错误、npm run build 通过。
1. 资产分析右侧账户资产净资产标注货币 —— 资产分析右侧账户资产卡片中「净资产」标签增加当前货币后缀，如 `净资产(USD)`，并跟随货币切换联动为 USD/CNY/HKD。tsc 无错误、npm run build 通过。
1. 账户资产市场筛选按市场币种显示 —— 资产分析右侧账户资产卡片进一步优化：筛选「全部」时仍跟随全局货币切换；筛选美股/港股/A股等具体市场时，净资产标签与金额使用该市场默认货币（US=USD、HK=HKD、CN=CNY，其他市场按 ISO 映射）。tsc 无错误、npm run build 通过。
1. 港币账户资产金额缩写防拥挤 —— 资产分析右侧账户资产卡片在港币/人民币显示大额金额时改用「万/亿」缩写（如 `HK$66.17万`、`HK$22.20万`），避免 7 列布局中长数字挤压；美元保持原有精确显示。tsc 无错误、npm run build 通过。
1. 美元账户资产金额同样使用万/亿缩写 —— 按用户要求，资产分析右侧账户资产卡片的大额美元金额也统一使用「万/亿」缩写（如 `$66.17万`），与港币/人民币保持一致的紧凑展示。tsc 无错误、npm run build 通过。
1. 修复美股收盘后盘后涨跌回退错误 —— 美股周末或收盘后若直接回退到腾讯常规盘行情，会把盘后上涨的股票显示成常规盘下跌（如 `GOOGL`）。修复：闭市/周末也读取 Yahoo 最近一个扩展时段报价，优先展示盘前/盘后最新价与涨跌，避免与盘后真实方向相反。tsc 无错误、npm run build 通过。
1. 美股盘后昨收优先采用腾讯口径 —— 进一步对齐券商盘后盈亏：美股扩展时段覆盖时，昨收价优先使用腾讯行情的昨收，再回退 Yahoo 昨收。修复 `AAPU` 因 Yahoo 昨收 37.99 与券商 38.00 不同造成的当日盈亏偏差。tsc 无错误、npm run build 通过。
1. 每日盈亏分享三页卡片统一缩小 —— 资产分析每日盈亏分享弹窗中「盈亏金额 / 持仓列表 / 持仓占比」三页此前高度分别为 830/820/780，切换时卡片大小不一致且整体偏大。现将三页统一为 760px，并压缩头部、金额、列表与环形图的间距和字号。tsc 无错误、npm run build 通过。
1. 每日盈亏分享卡片进一步精简化 —— 继续缩小每日盈亏分享卡片：宽度由 660 降至 600，高度统一为 700，右侧模板缩略图由 104 降至 88，圆角、间距与头像字号同步收紧，整体更精致。tsc 无错误、npm run build 通过。
1. 每日盈亏分享卡片支持拖拽缩放与持仓市值隐藏 —— 每日盈亏分享卡片右下角新增拖拽缩放手柄（宽度 420-760、高度 520-900），复制/保存按当前缩放后的卡片尺寸导出；三块顶部的持仓市值增加眼睛按钮，可隐藏/显示金额；持仓列表深色模式白色边框线弱化。tsc 无错误、npm run build 通过。
1. 每日盈亏分享卡片删除顶部持仓市值行 —— 按用户要求删除每日盈亏分享三页共用的顶部「持仓市值：金额」一行，卡片头部只保留头像、昵称与时间。tsc 无错误、npm run build 通过。
1. 每日盈亏分享图片加载骨架优化 —— 进一步优化分享页面刷新后图片加载慢/空白：模板图预加载改为解码预加载，资产分析页挂载即预热盈利与亏损两套图；分享弹窗图片未就绪时显示轻量骨架占位，解码完成后再显示真实图片，避免空白闪现。tsc 无错误、npm run build 通过。
1. 每日盈亏分享复制/保存按钮改为底部悬浮胶囊 —— 将复制/保存按钮从右侧工具栏中移出，改为贴在卡片底部边缘的悬浮胶囊按钮，并加入图标+文字；非模板页不再保留空的右侧栏，卡片居中更紧凑。tsc 无错误、npm run build 通过。
1. 每日盈亏分享复制按钮文案修正 —— 底部操作胶囊中的复制按钮文案由「复制」改为「复制图片」，与保存图片表述保持对称。tsc 无错误、npm run build 通过。
1. 每日盈亏分享弹窗背景透明度调整 —— 按用户要求将每日盈亏分享弹窗全屏遮罩从接近全黑（93%）改为 30% 黑色透明度，背景内容可见。tsc 无错误、npm run build 通过。
1. 每日盈亏分享卡片等比例缩放与关闭按钮固定 —— 拖拽缩放改为按百分比等比例调整，左右/上下拖动不再拉伸变形；关闭按钮移出缩放容器并固定在弹窗右上角，缩放时不再随卡片乱跑。tsc 无错误、npm run build 通过。
1. 每日盈亏分享复制/保存胶囊改为玻璃效果 —— 浅色模式下复制图片/保存按钮由深色胶囊改为半透明白玻璃效果，深色模式保持半透明白玻璃；按钮文字和 hover 态同步适配。tsc 无错误、npm run build 通过。
1. 分享模板图等待预加载后再打开弹窗 —— 点击分享按钮时先等待固定模板图全部解码完成，再打开每日盈亏分享弹窗；准备期间分享按钮禁用并显示提示，避免打开后图片仍慢慢加载。tsc 无错误、npm run build 通过。
1. 深色模式复制/保存按钮 hover 背景修正 —— 修复每日盈亏分享底部复制图片/保存按钮在深色模式鼠标划过时背景变白的问题，深色 hover 改为半透明白色 10%，不再出现白色块。tsc 无错误、npm run build 通过。
1. 分享右侧模板缩略图改为六宫格并支持上传 —— 每日盈亏分享右侧缩略图由 5 个改为固定 6 个，缩略图尺寸缩小并适配左侧卡片；第 6 个为相机上传入口，可上传自定义模板并选中用于分享。tsc 无错误、npm run build 通过。
1. 分享右侧缩略图顶部与左侧卡片对齐 —— 右侧六张缩略图改为从顶部开始排列，第一张与左侧卡片顶部对齐；卡片缩放时缩略图随布局同步跟随，不再居中对齐导致错位。tsc 无错误、npm run build 通过。
1. 分享缩略图支持逐张相机上传 —— 每日盈亏分享右侧六个缩略图每张都增加素材库股票图标同款相机 hover 遮罩，可分别上传自定义图片并自动选中该模板。tsc 无错误、npm run build 通过。
1. 每日盈亏分享顶部页签字号缩小 —— 按用户要求将每日盈亏分享顶部「盈亏金额 / 持仓列表 / 持仓占比」三块文字由 16px 缩小为 14px，视觉更精致。tsc 无错误、npm run build 通过。
1. 每日盈亏分享卡片缩放自动保存 —— 每日盈亏分享弹窗拖拽缩放后的卡片尺寸自动保存到 `localStorage`（`fire:daily-pnl-share-size`），下次打开分享弹窗时恢复上次缩放结果，无需手动确认提示。tsc 无错误、npm run build 通过。
1. 修复分享缩略图无法选中 —— 每日盈亏分享右侧缩略图加相机上传后，上传遮罩用 `absolute inset-0` 覆盖整张缩略图并拦截点击，导致点击缩略图不再选中模板。改为把相机缩小为缩略图中心的独立小圆标（hover 才显示），点击缩略图主体恢复选中，点击中心相机圆标才上传，两者不再冲突。tsc 无错误、npm run build 通过。
1. 修复每日盈亏分享卡片缩放时突然跳变 —— 修复拖拽缩放手柄放大卡片时突然跳到默认 600×700 的问题：缩放逻辑原先固定以 600/700 为基准（`factor=1+delta`），未考虑已保存的非默认尺寸，导致开始拖拽的瞬间卡片回跳。改为以当前实际尺寸为基准（`baseFactor=当前宽/600`）再叠加增量，缩放从当前大小平滑延续。tsc 无错误、npm run build 通过。
1. 每日盈亏分享持仓列表改为排名 + 四列表格 —— 每日盈亏分享「持仓列表」页由原来的图标+名称+当日盈亏升级为带排名的四列表格：股票图标左侧新增排名序号（第 1 名大号、第 2 名中号、第 3 名及以后小号），右侧新增「成本价 / 现价 / 盈亏额」列，名称列保留股票图标与代码；`DailyPnlShareItem` 增加 `cost / price` 字段并复用货币换算。tsc 无错误、npm run build 通过。
1. 持仓列表标题改为股票持仓并改为黑色 —— 按用户要求把每日盈亏分享持仓列表页标题由「当日持仓盈亏明细」改为「股票持仓」，字体颜色由灰色改为黑色（深色模式适配为近白）。tsc 无错误、npm run build 通过。
1. 持仓列表排名放大、盈亏率与全部持仓 —— 继续优化每日盈亏分享持仓列表：1) 排名序号改为第 1 名 22px、第 2 名 19px、第 3 名 16px、第 4 名起 13px，均大于数据文字；2) 盈亏额列改为盈亏率（(现价-成本)/成本，盈利红、亏损绿）；3) 去掉原来只展示前 6 条的截断，列出全部持仓；4) 底部「当日持仓合计」改为「持仓盈利」，金额改用持仓总盈亏（`summary.pnl`）。`DailyPnlShareItem` 新增 `pnl`、Props 新增 `totalPnl`。tsc 无错误、npm run build 通过。
1. 持仓列表成本价改回原币种并优化排版 —— 修复持仓列表成本价/现价被换算成展示货币导致成本价与盈亏率错误的问题：成本价、现价改回股票原币种并按市场显示货币符号（US=$, HK=HK$, CN=¥ 等）；同时放大排名/名称/成本价/现价/盈亏率字体，第 1/2/3 名排名分别用金/银/铜色区分；名称列加宽并允许换行显示完整名称，序号与名称不再拥挤；「股票持仓」标题改为加粗并加渐变下划线。tsc 无错误、npm run build 通过。
1. 持仓列表表头补充序号标签 —— 持仓列表表头第一列由空白占位补上「序号」文字标签，与数据行的排名序号 1/2/3 对齐。tsc 无错误、npm run build 通过。
1. 每日盈亏分享三页标题统一加粗与渐变条 —— 把「盈亏金额」「持仓占比」两页标题也改成与「股票持仓」一致的样式：黑色加粗标题 + 下方渐变彩色短条，三页标题视觉统一。tsc 无错误、npm run build 通过。
1. 分享标题间距优化与持仓列表滚动条 —— 1) 修复复制/保存图片后标题与渐变彩色短条拥挤的问题：标题去掉过紧的 `leading-none` 与字距，改为更宽松的行高，短条加高加宽并与标题拉开间距，`html2canvas` 导出与预览一致；2) 持仓列表新增竖直细滚动条（表头固定，行区域限高滚动），持仓较多时可在分享弹窗内滚动查看。tsc 无错误、npm run build 通过。
1. 分享标题渐变改为内联样式并提速复制 —— 1) 每日盈亏分享三页标题的渐变彩色短条改用内联 `linear-gradient`（不含 Tailwind `via` 自定义变量），标题与短条间距也用内联样式固定，避免 `html2canvas` 导出时渐变区域和间距与预览不一致；2) 复制/保存图片的 `html2canvas` 导出倍率由 3 降到 2，显著缩短生成耗时（5-10 秒级降到 1-2 秒级），图片仍为 2 倍高清。tsc 无错误、npm run build 通过。
1. 复制图片导出倍率恢复为 3 倍 —— 按用户反馈 2 倍导出图片偏模糊，将每日盈亏分享复制/保存图片的 `html2canvas` 导出倍率恢复为 3 倍高清。tsc 无错误、npm run build 通过。
1. 持仓列表去货币符号并加市场徽标 —— 1) 每日盈亏分享持仓列表的成本价/现价去掉货币符号，只保留数字；2) 名称列表头改为「名称/代码」，并在每行代码前增加市场徽标色块：美股 `US` 蓝色、港股 `HK` 紫色、A股按代码前缀显示 `SH`（6/9 开头）或 `SZ`（其余）红色（色值取自参考图 `#e0919f`），其他市场用灰色兜底。tsc 无错误、npm run build 通过。
1. 负成本持仓盈亏率显示为横线 —— 修复负成本持仓（如 `AAPU`、`NVDX`、`SMCX` 等摊薄成本后成本为负的持仓）盈亏率显示为负值的问题：盈亏率公式 `(现价-成本)/成本` 在成本 ≤0 时无意义，改为成本 ≤0 时显示「—」，避免误导为亏损。tsc 无错误、npm run build 通过。
1. 持仓列表按盈亏率降序排列 —— 每日盈亏分享持仓列表的排名原来按持仓盈亏额（金额）降序，导致盈亏率更高的股票排在盈亏率更低但金额更大的股票下面；现改为按盈亏率降序排列，与「盈亏率」列一致，成本 ≤0 的无盈亏率持仓排到最末。tsc 无错误、npm run build 通过。
1. 分享弹窗关闭按钮移到卡片右上角 —— 每日盈亏分享弹窗的关闭「×」按钮原来固定在浏览器窗口右上角，离卡片较远；现改为定位在卡片右上角上方（右缘与卡片右缘对齐、略高于卡片顶部），并随卡片缩放同步定位。tsc 无错误、npm run build 通过。
1. 分享标题渐变彩条间距调近 —— 按用户要求把每日盈亏分享三页标题与下方渐变彩色短条的间距由 12px 调整为 8px，让彩条更靠近标题文字；复制图片与保存图片按钮保持不变。tsc 无错误、npm run build 通过。
1. 分享页与导出图标题彩条间距解耦 —— 把每日盈亏分享标题渐变彩条改为块级元素，分享页显示间距调近到 4px；复制/保存图片导出时通过 `renderCard` 临时把彩条间距固定为 12px，导出完成后恢复，确保导出图的彩条间距不被分享页显示调整影响。tsc 无错误、npm run build 通过。
1. 分享卡片垂直居中显示 —— 每日盈亏分享弹窗的卡片原为顶部对齐，现将卡片区域改为垂直居中（`items-center`），卡片在页签栏下方剩余空间内上下居中显示。tsc 无错误、npm run build 通过。
1. 分享页签与卡片整体居中 —— 修复上一步居中后顶部「盈亏金额 / 持仓列表 / 持仓占比」三个页签没有跟随的问题：把页签栏与卡片放入同一个 flex 列并整体垂直居中，卡片从 `flex-1` 改为内容自适应，页签与卡片作为一个整体在页面中上下居中。tsc 无错误、npm run build 通过。
1. 盈亏金额卡片位置与其他页签保持一致 —— 盈亏金额页因右侧多出模板缩略图列，卡片会相对持仓列表/持仓占比向左偏移，来回切换时卡片位置跳动。现把外层定位容器宽度固定为卡片宽度（`shareWidth`），模板缩略图向右溢出显示，卡片始终保持居中、与另外两页位置一致。tsc 无错误、npm run build 通过。
1. 盈亏金额页标题精简为当日持仓盈亏 —— 按用户要求把每日盈亏分享「盈亏金额」页标题由「当日持仓盈亏金额」改为「当日持仓盈亏」。tsc 无错误、npm run build 通过。
1. 持仓占比页标题精简为持仓分布 —— 按用户要求把每日盈亏分享「持仓占比」页标题由「当前持仓资产分布」改为「持仓分布」。tsc 无错误、npm run build 通过。
1. 盈亏金额页标题再精简为当日盈亏 —— 按用户要求把每日盈亏分享「盈亏金额」页标题由「当日持仓盈亏」改为「当日盈亏」。tsc 无错误、npm run build 通过。
1. 修复持仓列表导出图代码与市场徽标截断 —— 每日盈亏分享持仓列表导出图（`html2canvas`）中，名称/代码列的市场徽标（US/HK/SH/SZ）与代码文字出现垂直截断、徽标与代码过近。修复：代码行去掉过紧的 `leading`，改为固定 16px 行高，代码文字用 `min-w-0 + truncate` 并加足行高，市场徽标与代码间距由 4px 加大到 6px，导出图不再裁切。tsc 无错误、npm run build 通过。
1. 继续修复市场徽标与代码垂直截断 —— 上一版市场徽标与代码在导出图中仍显示下半截被裁掉。继续修复：去掉代码文字的 `truncate/overflow-hidden`（避免 `html2canvas` 按内容盒垂直裁切），市场徽标放大到 18px 高并统一 18px 行高，代码文字也使用 18px 行高且不再裁切，徽标与代码显示完整。tsc 无错误、npm run build 通过。
1. 市场徽标与名称/代码改用块级布局 —— 持仓列表导出图中市场徽标仍与上方股票名称重叠、且底部显示不全，原因是嵌套 flex/inline-flex 在 `html2canvas` 中渲染不稳定。改为块级 + inline-block 布局：名称固定 20px 行高，下方 6px 间距后接市场徽标与代码（均 18px 高、行高 18px），徽标使用明确 padding/背景/字号，代码与徽标垂直对齐，导出图不再重叠或裁切。tsc 无错误、npm run build 通过。
1. 市场徽标去掉固定高度改用 padding 自适应 —— 持仓列表导出图中 SH/SZ 等市场徽标文字仍显示下半截被切掉。根因是 inline-block 上同时设置固定 `height` 与 `lineHeight`，`html2canvas` 计算文字垂直居中不稳定。改为去掉固定 `height`，用上下 `padding` + 12px 行高自适应撑起 18px 高的徽标，文字完整显示；代码保持 18px 行高并与徽标顶部对齐。tsc 无错误、npm run build 通过。
1. 市场徽标改用 SVG 渲染彻底修复裁切 —— 市场徽标此前用 HTML+CSS 背景色渲染，在 `html2canvas` 导出图中反复出现底部缺失/文字裁切。现改为内联 SVG（圆角矩形 + 居中文字），`html2canvas` 对 SVG 按图像渲染稳定，徽标不再被裁切；同时把名称与徽标间距由 6px 加大到 12px，徽标与代码间距由 6px 加大到 8px。tsc 无错误、npm run build 通过。
1. 市场徽标与上方名称间距精调 —— 按用户反馈把市场徽标与上方股票名称的间距由 12px 精调为 8px，取 6px（过近）与 12px（过远）之间的合适间距。tsc 无错误、npm run build 通过。
1. 市场徽标间距改为分享页 6px —— 明确用户说的是分享页显示而非导出图后，把市场徽标与上方股票名称间距进一步收紧为 6px；此前「过近」是导出图 HTML 徽标重叠导致，改用 SVG 后导出已稳定，6px 在分享页与导出图均合适。tsc 无错误、npm run build 通过。
1. 市场徽标间距分享页与导出图解耦 —— 分享页市场徽标与名称间距保持 6px（预览合适），导出图因 `html2canvas` 对行高/文字基线渲染差异导致徽标偏近，现 `renderCard` 截图前临时把徽标行间距调到 10px，导出完成后恢复，找到导出图不远不近的平衡点。tsc 无错误、npm run build 通过。
1. 分享页名称与市场徽标间距继续收紧 —— 分享页股票名称与市场徽标仍显偏远：把名称行高由 20px 收紧为 18px、名称到徽标间距由 6px 收紧为 4px，视觉间距更紧凑；导出图徽标行距仍保持 10px 的截图覆盖。tsc 无错误、npm run build 通过。
1. 导出图市场徽标与代码垂直对齐 —— 导出图（`html2canvas`）中市场徽标 SVG 因未显式声明 CSS 宽高，渲染高度与代码行高不一致，导致徽标偏上、与股票代码不在同一水平线。给徽标 SVG 补上显式 CSS `width/height` 30×18px 并保持 `vertical-align:top`，导出图徽标与代码同一水平线。tsc 无错误、npm run build 通过。
1. 导出图徽标文字与代码文字精确对齐 —— 导出图中徽标整体仍与代码底部对齐，导致徽标内文字比代码文字偏高约 3-5px。因 `html2canvas` 未按 `vertical-align:top` 对齐 SVG，改为截图时给市场徽标 SVG 临时加 `translateY(4px)` 下移，使徽标文字与代码文字处于同一水平线，截图后恢复。tsc 无错误、npm run build 通过。
1. 导出图市场徽标与名称间距对齐分享页 —— 资产分析分享导出图中市场徽标行仍过于贴近上方股票名称，原因是 `html2canvas` 对徽标行 `marginTop` 的渲染与浏览器不一致。修复：`renderCard` 截图前临时把徽标行 `marginTop` 由 4px 提升到 12px，截图后恢复，使导出图徽标与名称间距与分享页一致。tsc 无错误、npm run build 通过。
1. 导出图徽标改用占位块保证间距 —— 继续修复导出图市场徽标仍偏上贴紧名称：`html2canvas` 对徽标行 `marginTop` 的渲染不可靠。改为截图时把徽标行 `marginTop` 置 0，并在其上方插入一个固定 10px 高的占位 div（截图后移除），用块级高度确定性撑开间距，使导出图徽标与名称距离与分享页一致，分享页不受影响。tsc 无错误、npm run build 通过。
1. 导出图徽标行改为 flex 渲染并保距 —— 进一步修复导出图市场徽标仍偏上：徽标行使用 `display:grid`，`html2canvas` 1.4.1 不实现 grid 布局，导致导出图中徽标/代码排列与间距异常。改为截图时临时把徽标行切换为 flex（徽标 30×18 不收缩、代码 `flex:1` 左间距 8px），并在上方插入 10px 占位块，截图后恢复全部内联样式；分享页仍用原 grid 布局不受影响。tsc 无错误、npm run build 通过。
1. 导出图徽标占位间距调至 16px —— 按用户反馈导出图市场徽标仍需继续下移，把截图时徽标行上方占位块高度由 10px 提升到 16px，徽标与名称间距进一步拉开；分享页不变。tsc 无错误、npm run build 通过。
1. 导出图徽标与代码改用表格布局对齐 —— 修复导出图市场色块与股票代码的对齐：`html2canvas` 1.4.1 不实现 flex/grid 布局，改用 `display:table + table-cell + vertical-align:middle` 渲染徽标行（截图时临时切换，截后恢复），徽标与代码垂直居中对齐、色块完整显示、代码左侧保持 8px 与分享页一致；分享页仍用原 grid 布局不受影响。tsc 无错误、npm run build 通过。
1. 导出图徽标下移对齐代码文字 —— 继续修复导出图市场色块仍偏上：徽标 SVG 在 table-cell 内用 `translateY(4px)` 下移（色块底部可超出但完整显示），使色块文字与右侧股票代码文字对齐；截图后移除包裹层并恢复样式，分享页不变。tsc 无错误、npm run build 通过。
1. 导出图徽标下移量收敛到 2px —— 按用户反馈 4px 下移后偏下，将导出图徽标 SVG 的 `translateY` 从 4px 收敛到 2px，取 0（偏上）与 4（偏下）之间的临界点。tsc 无错误、npm run build 通过。
1. 导出图徽标行加高避免底部被裁 —— 徽标 `translateY(2px)` 下移后底部超出 18px 表格行被 `html2canvas` 裁掉。保持徽标位置不变，把导出时徽标行高度由 18px 加到 20px，让色块完整显示不被遮挡。tsc 无错误、npm run build 通过。
1. 复制图片时分享卡片不再跳动 —— 此前导出前的样式覆盖（表格布局、占位块、`translateY` 等）直接改实时 DOM，复制图片瞬间列表会跳动。改为把这些覆盖全部放进 `html2canvas` 的 `onclone` 回调，只在克隆文档里生效，实时 DOM 零改动，复制/保存图片时分享页无任何跳动。tsc 无错误、npm run build 通过。
1. 持仓列表第七行完整显示 —— 持仓列表限高 360px 时第 7 行底部被裁掉几个像素，把列表 `max-height` 由 360px 提高到 410px，第 7 行整排完整显示，底部持仓盈利汇总仍可见。tsc 无错误、npm run build 通过。
1. 持仓列表可见 7 行且可滚动 —— 按用户要求持仓列表保留全部持仓，可视区固定约 7 行（`max-height` 404px），超出部分通过滚动条下拉查看；此前误把列表截断为 7 条已恢复为全部。tsc 无错误、npm run build 通过。
1. 持仓列表可视区精确 7 行 —— 表头在滚动容器之外，`max-height` 只约束数据行；404px 时第 8 行的序号 8 仍露出。按 7×52px 精确调整为 364px，可视区刚好 7 行完整显示，第 8 行不再露出，滚动条可下拉查看更多。tsc 无错误、npm run build 通过。
1. 持仓列表表头字号放大 —— 按用户要求把股票持仓下方表头一行（序号 / 名称/代码 / 成本价 / 现价 / 盈亏率）字号由 12px 放大到 13px。tsc 无错误、npm run build 通过。

## v0.1.9 · 2026-08-15

> 我的持仓-持仓盈利桑基图优化：正股显示名称、2倍ETF显示代码、块上显示盈亏金额，并支持一键导出 / 复制分享图片。

### 新功能

1. 持仓盈利桑基图优化（名称/代码规则 + 金额 + 分享图片） —— 按参考图优化我的持仓-持仓盈利图（桑基图）：1) 节点标签规则——正股只显示股票名称、2倍杠杆 ETF（名称含 2倍/2x 且带 ETF/做多/做空）只显示代码，不再「代码+名称」连排；2) 每个块下方显示盈亏金额（跟随当前选择货币换算，盈利红色 + / 亏损绿色 −，中心「持仓盈亏」块显示净盈亏）；3) 新增「分享」按钮：ECharts 2 倍像素导出 PNG，支持复制到剪贴板直接粘贴分享（非安全上下文自动回退下载文件），深浅色背景随主题。tsc 无错误、npm run build 通过。

### 修复

1. 持仓盈利桑基图美化（拥挤优化） —— 按反馈优化桑基图拥挤问题：1) 块上金额缩写——≥1 亿显示亿、≥1 万显示万（如 +$1.23万），不再挤长串数字；2) 名称与金额两行用 rich 分层样式（名称 11px 主色、金额 10px 辅助色），盈利红 / 亏损绿 / 中心紫；3) 画布高度 285→380px、节点加宽 14、间距 18、左右留白 170、连线透明度 0.7→0.55，整体更透气；4) 中心「持仓盈亏」块同步显示净盈亏缩写。tsc 无错误、npm run build 通过。
1. 桑基图分享改为先预览再保存（不直接下载） —— 按用户要求：点击「分享」先生成 2 倍像素 PNG 并在弹窗内预览，弹窗提供「复制图片」（写入剪贴板，非安全上下文自动回退保存）与「保存图片」（下载 PNG）两个确认操作，分享前可检查内容，不再点击即下载。tsc 无错误、npm run build 通过。
1. 桑基图分享卡片重设计（LOGO + 标题 + 汇总 + html2canvas 导出） —— 按参考图重做分享图：点击「分享」生成白底分享卡片——左上角站点 LOGO + 站点名（读 `/api/settings/public`，缺失用默认趋势图标），右上角「持仓盈利图」标题 + 日期；中间为桑基图（分享时临时切浅色渲染保证白底观感，截取后恢复原主题）；下方带 总盈利 = 盈利总额 − 亏损总额 汇总块与页脚水印；引入 html2canvas（2 倍像素）把整张卡片渲染成 PNG 后进入预览弹窗，再复制/保存。tsc 无错误、npm run build 通过。
1. 分享预览按钮逻辑修正（复制不下载） —— 按用户要求修正逻辑：预览弹窗里「复制图片」失败时只提示「复制失败，请使用保存图片」，不再回退触发下载；下载只发生在点击「保存图片」。tsc 无错误、npm run build 通过。
1. 分享卡片精简 + 跟随深浅色主题（浅色白底 / 深色黑底） —— 按用户要求：1) 删除分享卡片底部水印文字「Fire Fire · 持仓盈亏分布 · 数据仅供参考」；2) 把「总盈利 = 盈利总额 − 亏损总额」汇总块从图表下方移到图表上方；3) 分享图跟随当前主题——浅色模式白底深字、深色模式 #16181d 黑底浅字（标题/日期/汇总块边框与背景同步切换），图表截图与 html2canvas 导出背景随主题一致。tsc 无错误、npm run build 通过。
1. 分享图复制兼容局域网 http（服务端写入 Mac 剪贴板） —— 修复局域网 http（非安全上下文）下「复制图片」失败的问题：浏览器 `navigator.clipboard` 在非安全上下文不可用，新增 `POST /api/clipboard`——前端复制失败时把 PNG 交给服务端，用 osascript 写入 Mac 系统剪贴板（登录 + 限流 + PNG 魔数校验 + ≤8MB + 服务端生成临时文件名，无注入面），聊天窗口（微信等）可直接粘贴；安全上下文仍走浏览器剪贴板 API。实测接口 200、剪贴板内容正确（测试后已恢复原剪贴板）。tsc 无错误、npm run build 通过。
1. 修复复制图片兜底不生效（浏览器 API 抛错被外层 catch 吞掉） —— 修复「复制图片」仍失败且剪贴板图片异常的问题：原逻辑把浏览器剪贴板 API 与服务端兜底放在同一个 try 里，当 `navigator.clipboard.write` 抛错（如 `NotAllowedError`：文档未聚焦/权限被拒）时会直接跳到外层 catch 显示失败，服务端 osascript 兜底根本没执行（服务日志也证实没有来自浏览器的 `/api/clipboard` 请求）。修复：浏览器 API 单独 try/catch，失败后继续走服务端写入 Mac 剪贴板；复制失败时 toast 显示具体错误信息便于后续排查。服务端路径已用 4.5MB 大图实测逐像素一致。tsc 无错误、npm run build 通过。
1. 分享预览弹窗按钮字体色统一 —— 按用户反馈：预览弹窗「保存图片」按钮文字原为 ink-2 灰色，与「关闭」「复制图片」的 ink 深色不一致，补 text-ink 对齐三个按钮文字颜色。tsc 无错误、npm run build 通过。
1. 复制图片改用 atob 转 Blob（不依赖 fetch(data:)，兼容 Safari） —— 排查「复制图片仍失败」：服务日志证实浏览器请求从未到达 `/api/clipboard`，怀疑 `fetch(data:image/png;base64,...).blob()` 在部分浏览器（如 Safari 对 data URL fetch 支持不完整）抛错，直接走到「图片数据生成失败」。修复：不再用 fetch 转 Blob，改为 atob 解码 data URL 直接构造 Blob（所有浏览器通用），浏览器 API 与服务端兜底两条路径均重新实测通过（服务端路径触发时 `/api/clipboard` 有请求、系统剪贴板拿到图片）；复制失败时 toast 显示具体错误并 `console.error` 便于排查。tsc 无错误、npm run build 通过。
1. 复制图片后保留分享预览弹窗 —— 按用户要求：点「复制图片」成功后不再自动关闭预览弹窗，可继续查看或再次操作；关闭仍由「关闭」按钮完成。tsc 无错误、npm run build 通过。
1. 分享卡片改为薰衣草紫相框式布局（外层包裹白/深色内容） —— 按参考图重设计分享卡片：外层整体使用参考图背景色 #A5A5BF（淡薰衣草紫，从参考图取色 165,165,191）作为相框，把内层白/深色内容块包裹起来；内层包含品牌栏（LOGO+站点名 / 持仓盈利图+日期）、汇总块与桑基图，浅色模式白底深字、深色模式 #16181d 黑底浅字。tsc 无错误、npm run build 通过，导出图已实测外层 165,165,191、内层白底。
1. 分享卡片按左图重构：紫底 + 头部浮层 + 独立圆角内容卡 —— 仔细对比左右参考图后重构分享卡：1) 淡薰衣草紫 #A5A5BF 铺满整张卡片背景；2) LOGO（白底圆角占位/站点 Logo）+ 站点名 与 「持仓盈利图」+ 日期 直接浮在紫底上（深色文字，不套内容块）；3) 桑基图+汇总做成独立圆角卡片（18px 圆角 + 柔和阴影），左右留白 24px、顶部紫底留白约 124px，浅色模式白卡深字、深色模式 #16181d 黑卡浅字。导出图实测：顶部紫底、内容卡左/右边距 24px、圆角内收正常。tsc 无错误、npm run build 通过。
1. 分享卡片压缩上下留白 —— 按反馈：上下薰衣草紫留白太多显得卡片过宽。压缩外层边距（顶部 52→22px、底部 40→22px、左右 24→20px）、头部与内容卡间距 22→14px，内容卡内边距同步收紧。实测卡片高度 490→432px、顶部紫底 124→85px，整体更紧凑。tsc 无错误、npm run build 通过。
1. 分享预览弹窗去除提示文字 + 分享卡进一步压缩 —— 1) 按用户要求删除预览弹窗副标题「确认内容后，可复制分享或保存图片」，后续不加多余文字；2) 继续压缩分享卡：外层紫底上下 22→14px、左右 20→16px，头部与内容卡间距 14→10px，LOGO 32→28px，内容卡内边距 16/22/18→12/16/14、圆角 18→14。实测卡片 720×410、顶部紫底 73px、左右留白 16px。tsc 无错误、npm run build 通过。
1. 分享图清晰度优化（3 倍导出 + 图表像素对齐 + 抗锯齿） —— 按反馈：分享图文字锐化/发虚。优化：1) html2canvas 导出倍率 2→3，输出分辨率提升 2.25 倍；2) ECharts 图表 pixelRatio 2→3，与捕获倍率对齐，避免图表被放大导致发虚；3) onclone + 内联样式设置 `-webkit-font-smoothing: antialiased` 与 `text-rendering: geometricPrecision`，文字边缘更干净。tsc 无错误、npm run build 通过。
1. Swift OCR 模块缓存移出仓库 + Next 缓存阈值清理（瘦身约 1.2GB） —— `visionOcr.ts` 编译模块缓存原放在 `fire-web/.cache`（实测 235MB），改为系统临时目录（系统管理、自动回收），仓库 `.cache` 仅保留 72KB 编译产物；新增 `npm run clean:caches`（`scripts/clean-next-cache.mjs`，阈值 200MB 自动清理 `.next`/`.next-dev` 缓存，`--force` 强制），fire.sh 启动前自动执行并新增 `clean-cache` 命令。本次清理：`.next` 782MB、`.next-dev/cache` 145MB、`swift-module-cache` 235MB、tsbuildinfo 与全部 .DS_Store，fire-web 由 1.0GB 降至 675MB。tsc 无错误、OCR 端到端验证 4 行正常。
1. 资产分析跑赢数值移到向下箭头右侧 —— 按参考图：基准条「跑赢 ● 基准名 ▾」的跑赢幅度（如 0.14%）从右对齐改为紧跟向下箭头右侧、与基准选择同组显示；隐藏态仍显示 ******，正负着色保留。tsc 无错误、npm run build 通过。
1. 每日盈亏分享图（纯白卡片：盈亏金额 / 持仓列表 / 持仓占比） —— 资产分析-账户资产卡新增分享按钮，生成每日盈亏分享图：1) 纯白卡片（不做紫底），头部 LOGO+站点名 / 每日盈亏+日期；2) 三个区块——盈亏金额（当日盈亏大数字 + 收益率，红涨绿跌）、持仓列表（按市值排序，2倍ETF显示代码、正股显示名称，最多 8 行 + 等 N 只）、持仓占比（ECharts 环形图 + 图例，前 8 只 + 其他）；3) 数据按展示货币换算，复用 html2canvas 3 倍导出与复制/保存/服务端剪贴板兜底，预览弹窗复制后保留、无多余文字；4) 数据链路已按持仓汇总当日盈亏与市值占比，为后续「盈亏日历图」预留。tsc 无错误、npm run build 通过、无头浏览器实测生成 1920×2133 白色卡片正常。
1. 每日盈亏分享图重做（对齐参考图：色块金额 + 横向色条列表 + 大环形占比） —— 按反馈重做分享卡视觉效果：1) 盈亏金额改为浅红/浅绿渐变底的大数字色块（红涨绿跌，含今日收益率）；2) 持仓列表每行加横向色条（宽度按当日盈亏绝对值占比，红正绿负）；3) 持仓占比环形图加大（250×190）并加分隔线，图例不变；白色卡片、三区块顺序 盈亏金额 → 持仓列表 → 持仓占比 保持不变。tsc 无错误、npm run build 通过、实测卡片 1920×2265 色块/色条/环形齐全。
1. 每日盈亏分享中心重做为长桥式卡片布局（接入第六版模板图） —— 按参考图（长桥分享卡）重做分享中心布局：1) 品牌栏（LOGO+站点名）+ 红色「转发」按钮；2) 当日持仓盈亏金额大数字 + 收益率（红涨绿跌）；3) 统计行 总资产 / 持仓市值 / 持仓总盈亏；4) 盈亏金额 / 持仓列表 / 持仓占比 三个可点击页签；5) 内容区展示对应模板图（第六版 盈利/亏损各 5 张，按当日盈亏正负自动切换目录、按页签/底部入口切换 1-5 号模板，不识别图中文字、原图直接使用）；6) 底部 5 入口（每日盈亏/持仓列表/持仓占比/盈亏日历/更多）可点击切换；7) 转发 = html2canvas 3 倍把整张卡片渲染成 PNG，预览后复制/保存。tsc 无错误、npm run build 通过、无头浏览器实测布局与切换、转发预览正常。
1. 每日盈亏分享中心改为三块布局（顶部盈亏金额一排 / 左白色卡片 / 右分享缩略图） —— 按反馈重构整体布局为三块：1) 上方一整排盈亏金额——当日持仓盈亏金额 + 收益率 + 总资产 / 持仓市值 / 持仓总盈亏统计；2) 左侧白色卡片——品牌栏 + 当日盈亏金额 + 盈亏金额/持仓列表/持仓占比页签 + 模板图内容 + 底部 5 入口，可整体渲染成 PNG 分享；3) 右侧分享缩略图列——5 张模板缩略图，未选中置灰、选中才有颜色并高亮边框，点击切换左侧卡片；模板图仍按当日盈亏正负选盈利/亏损目录。tsc 无错误、npm run build 通过、无头浏览器实测三块布局、缩略图选中态与卡片联动正常。
1. 删除每日盈亏分享功能 —— 按用户要求整体删除每日盈亏分享：移除 `DailyPnlShareModal` 组件、资产分析页的分享按钮/相关数据计算与渲染，以及第六版模板图资源（盈利/亏损各 5 张，共 20MB，原图保留在用户桌面）；账户资产卡片恢复为仅刷新按钮。tsc 无错误、npm run build 通过、全仓库无残留引用。
1. 前端包体积优化（视图按需加载 + ECharts 按需引入 + 版本历史懒加载 + 设置缓存） —— 针对后端应用首屏约 3.4MB JS 一次性下发做四项优化：1) 后端 11 个视图全部改为 `next/dynamic` 懒加载（仅下载当前激活页签代码，布局 chunk 813KB → 27KB）；2) ECharts 全量引入改为 `echarts/core` 按需注册（新增 `lib/echarts.ts`，只注册 candlestick/line/bar/sankey 与用到的组件 + Canvas 渲染器，图表 chunk 1104KB → 712KB 且仅含图表的页签才加载）；3) 版本历史约 200KB 文案拆分到 `lib/versions-history.ts`，版本弹窗懒加载（设置页 chunk 307KB → 101KB，历史只在点开弹窗时下载）；4) `lib/settings.ts` 的 `getSiteSettings` 加内存缓存（`updateSiteSettings` 写后失效），避免每个请求 / 每次行情批次全表 SELECT + JSON.parse。AGENTS.md 版本记录约定同步更新拆分后的维护方式。tsc 无错误、npm run build 通过。
1. 分享当日盈亏卡片金额对齐修复（复制图片错位） —— 按反馈修复资产分析-账户资产-分享当日盈亏复制出的图片金额元素错位：1) 持仓市值右侧 USD 由 12px 小字改为与金额同字号（15px）并基线对齐；2) 当日持仓盈亏金额右侧 USD 由 flex 基线对齐改为行内基线布局（底部对齐、不再浮在数字上方），html2canvas 导出与页面渲染一致；3) 持仓市值前的 $ 圆形徽标重设计为单层居中（去掉嵌套 span 与 translate），消除复制图片中的上下错位。tsc 无错误、npm run build 通过。

---

## v0.1.8 · 2026-08-14

> Web 服务默认端口由 8124 切换为 3000：启动脚本本就默认 3000，本次把设置默认域名、README、AGENTS、冒烟测试、API 规范与 iOS 后端地址全部对齐，并重启服务至 3000。

### 新功能

1. 右上角头像改为 B 站式跨层效果（悬停丝滑放大 + 白边 + 半入卡片） —— 按参考图把右上角账号头像做成跨层效果：1) 头像加大到 40px，悬停丝滑放大（spring 曲线 cubic-bezier(.34,1.56,.64,1)，hover 1.18、展开 1.4）；2) 白色描边（ring-2 ring-white + 柔和阴影）让头像在跨层时立体突出；3) 展开菜单时面板顶边精确对齐头像中心（实测头像中心与面板顶边差 2px）——下半部分在卡片内、上半部分探出卡片上方，B 站「跨层头像」观感。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 网站升级为可安装 PWA（Chrome「在应用中打开」） —— 按用户要求把 Fire Web 做成可安装的渐进式 Web 应用：1) 新增 app/manifest.ts（读取站点标题，name/short_name/start_url/standalone/主题色，icons 含 192/512/maskable）；2) 由站点图标生成 public/uploads/ico/pwa-192.png 与 pwa-512.png；3) 新增 public/sw.js——导航与静态/上传资源网络优先、离线回退缓存，/api 与跨域不拦截（兼容 dev 热更新）；4) 新增 components/PwaRegister.tsx 注册 Service Worker；5) layout 头部补充 manifest / theme-color / mobile-web-app-capable / apple-touch-icon 等 meta，深色模式主题色联动。已实测 manifest、sw.js、图标、页面 meta 全部 200；提示：Service Worker 与 Chrome 安装提示需要 HTTPS 或 localhost（局域网 IP http 下浏览器会静默跳过注册）。
1. 订单日历滚动跟随 + 位置/大小自动保存 + 缩放手柄融入边框 —— 继续优化订单委托时间日历：1) 日历跟随页面上下滚动——fixed 浮层监听滚动增量同步补偿位置，与锚点内容联动（不再钉死在视口）；2) 位置与大小改动自动保存到 localStorage（拖动/缩放结束与关闭时写入，重新打开自动恢复宽高与偏移）；3) 右下角缩放手柄改为融入边框的细斜线样式（低透明度、悬停加深，参考左侧分隔条），拖拽流畅度与左右拉布局一致。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 订单委托时间日历可拖动 + 可缩放（K 线图设置面板同款交互） —— 按用户要求把订单「委托时间」日历做成可自由操控的浮层：1) 顶部六点手柄拖动移动（pointer capture + 视口钳制，拖动后隐藏指向按钮的小三角）；2) 右下角缩放手柄拖拽调整宽高——宽度最小 360px（双月日历自动变单列竖排）、高度最小 260px，超出部分内部滚动，用户可自由拉大缩小；3) 打开时自动重置尺寸与位置。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 刷新按钮改小至参考图尺寸 + 我的持仓「账户资产」加刷新 —— 1) 全站统一刷新按钮由 32px 缩小到约 24px（h-6 w-6、圆角 7px、图标 12px 占按钮 50%、描边更细），对齐参考图工具栏按钮比例，资产分析页账户资产/持仓/交易及我的持仓详情订单区同步生效；2) 我的持仓页顶部「账户资产」标题右侧新增刷新按钮：强制重取行情（force）+ 重载汇率，总资产/当日盈亏/持仓市值等随最新数据更新，刷新中图标旋转。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 资产分析从我的持仓剥离为独立页签（我的持仓下方、自选股上方） —— 按用户要求将资产分析从「我的持仓」页内嵌（金钱图标进入）剥离为独立导航页签：1) 新增 /assets 页签，导航顺序为 我的持仓 → 资产分析 → 自选股（同步更新 lib/settings.ts NAV_KEYS 与 DEFAULTS.tabs、RecordsApp DEFAULT_TABS/TAB_ICONS、数据库 site_settings.tabs）；2) 新建 components/views/AssetAnalysisView.tsx：独立页签自取汇率（localStorage 兜底 + /api/rates）、复用 useAssetIcons 股票图标、刷新链路（强制重取行情 + 汇率 + 触发父层重拉记录）、onBack 返回我的持仓；3) 我的持仓移除金钱图标入口与内嵌资产分析渲染（保留账户资产页自身）；4) 服务端守卫自动放行 /assets（settings.tabs 驱动），未登录重定向 /login。tsc 无错误、npm run build 通过、/assets 登录 200 / 未登录 307 验证通过。
1. 账户资产 / 持仓 / 交易各加刷新按钮（实际重取数据） —— 按参考图（卡片右上角方形边框循环箭头图标）新增三个真实刷新的按钮：1) 新建全站统一组件 components/RefreshButton.tsx（描边方形 + 循环箭头，刷新中图标旋转，深浅色适配）；2) 资产分析页「账户资产」卡右上角（标题同步改为「账户资产」）、「持仓」工具行、底部「交易」订单面板头部各放一个；3) 实际效果——账户资产/持仓刷新：强制重取行情（RecordsApp.refreshQuotes 增加 force 参数绕过轮询互斥与后台限制）+ 重取汇率（HoldingsView 抽取 loadRates）+ 触发父层重新拉取持仓记录，资产/盈亏/市值随最新行情更新；交易刷新：重新请求 /api/v1/orders 更新订单列表；4) 我的持仓详情订单区同步接入面板内刷新按钮（移除原顶部文字刷新）。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 订单页继续优化（Tab 下划线 / 四筛选+重置 / 行按方向着色 / 状态圆对勾） —— 按 6 张新参考图（阿里百炼视觉模型逐张识别）继续对齐券商订单页：1) 顶部 Tab 由胶囊改为「文字 + 蓝色短下划线」选中态（参考图样式）；2) 筛选栏由两项改为四项单行下拉——市场（选中蓝色边框）、标的类型（全部/股票/ETF，按名称含 ETF/做多 推断）、订单状态（全部/已成交/已撤销）、委托时间（日历图标 + 全部/当日/近7天/近30天/近1年/自定义，自定义弹双月日历选择起止区间），最右侧新增「重置」垃圾桶按钮一键清空筛选；所有筛选本地持久化；3) 表格行按买卖方向整行着色（买入浅红、卖出浅绿，深色模式深红/深绿底，悬停加深），与参考图一致；4) 订单状态由胶囊改为「绿色圆对勾 + 已成交」/「红色圆叉 + 已撤销」（表格与详情弹层统一）；5) 导出接口新增 type（标的类型）与 start/end（委托时间区间）筛选并端到端验证。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 订单参考图逐项对齐（操作列图标 / 详情弹层 / 导出明细与最近导出） —— 用阿里百炼视觉模型逐张复核此前订单参考图并修复差异：1) 操作列由文字「详情」改为参考图图标按钮（文档/列表图标），我的持仓详情保留编辑/删除；2) 订单详情弹层对齐参考图——两列键值改为留白布局（去掉逐行分割线与外框）、方向改为纯色文字（买红/卖绿）、委托单号加复制图标、字段顺序对齐参考（证券账户/委托单号/订单类型/方向/委托价格/委托数量/成交价格/成交数量/成交金额/有效期/时段/结算货币/订单状态/触发状态/委托时间/已实现盈亏/成交后持仓/驳回原因/备注），费用明细改为列表内加粗分组标题（佣金、合计）；3) 导出弹窗对齐参考图——新增「同时导出订单明细」勾选（服务端 GET /api/v1/orders/export?detail=1 追加「订单明细」工作表：订单号/状态/市场/代码/名称/方向/类型/数量/价格/币种/时间/均价/成交数量/成交金额/费用/已实现盈亏/成交后持仓数量/成本/有效期/时段/触发状态/驳回原因/备注）、新增「最近导出」（localStorage 最近 20 条：时间范围/状态/文件名称/导出时间/文件大小/重新导出），导出改为 fetch 下载以记录文件大小并提示成功。tsc 无错误、npm run build 通过、明细工作表端到端校验通过、服务重启后页面 200。
1. 订单筛选改为参考图两行控件（绿色对勾标签 + 橙色选中胶囊） —— 按参考图（阿里百炼视觉模型辅助识别）将订单面板的市场、订单状态筛选改为两行式券商控件：上行 = 字段名标签（订单状态左侧为绿色对勾圆、市场左侧为当前市场图标），下行 = 橙色选中胶囊（浅色 bg-[#fff4e5] text-[#b06a00]，深色 bg-[#2b2413] text-[#e6b45c]，与全站橙色强调一致）+ 当前值 + 下拉箭头，点击展开选项菜单（选中项橙色高亮 + 对勾，Esc / 点击外部关闭）；筛选值继续本地持久化。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 订单列顺序对齐参考（操作第一列）+ 详情弹层 + 状态胶囊/成交圆点 —— 按参考图调整订单表列顺序：第一列为「操作」（详情按钮，我的持仓详情另含编辑/删除），其后严格按 21 字段顺序排列——订单状态、市场、股票代码、股票名称、方向、委托类型、委托数量、委托价格、触发价格、币种、委托时间、成交均价、成交数量、成交金额、剩余挂单数量、撤/废单数量、有效期、时段、触发状态、订单号、驳回原因（费用、已实现盈亏移入详情弹层展示）；点击「操作-详情」打开订单详情弹层（参考图 2/3/4）：标题按买卖方向、股票图标+名称+代码、证券账户/委托单号/订单类型/方向/委托价格/委托数量/成交价格/成交数量/成交金额/已实现盈亏/成交后持仓/有效期/时段/委托时间/结算货币/订单状态/触发状态/驳回原因/备注 + 费用明细（佣金、合计）；成交数量列已成交显示绿色圆点（失败红色）；订单状态改为对勾胶囊（成功绿）/叉号胶囊（失败红）；列设置缓存键升级 v2 使新默认顺序全局生效。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 订单导出 Excel（xlsx）· 21 列与参考图一致 —— 订单列表新增「导出」按钮与导出弹窗（筛选条件摘要 + 将导出的笔数与 21 列说明），按当前 Tab / 市场 / 订单状态筛选导出为 .xlsx：订单状态、市场、股票代码、股票名称、方向、委托类型、委托数量、委托价格、触发价格、币种、委托时间、成交均价、成交数量、成交金额、剩余挂单数量、撤/废单数量、有效期、时段、触发状态、订单号、驳回原因；我的持仓详情导出仅包含该持仓订单。服务端新增 GET /api/v1/orders/export，手写零依赖 xlsx 写入器（lib/xlsx.ts，不引入有已知漏洞的 SheetJS），金额保留两位、订单号为文本避免科学计数法，导出文件经 ZIP/XML 与真实数据双重校验。
1. 订单表补全参考字段列 + 列设置与表头排序 —— 网页订单表补全参考图全部字段列（触发价格、剩余挂单数量、撤/废单数量、触发状态、驳回原因），与导出 21 列完全一致；表头可点击排序（升/降，委托时间默认倒序），列设置按钮（漏斗图标，样式与持仓一致）弹出「需要显示哪些列」——勾选显示/隐藏、拖动手柄排序、至少保留一列，列配置与排序均本地持久化；「导出」与「列设置」按钮放在当日/历史 Tab 同一行右侧。tsc 无错误、npm run build 通过、导出 API 端到端实测 21 列与数据正确。
1. 交易系统当日/历史订单重塑（券商风格）+ 唯一订单号 —— 按参考图将资产分析底部与我的持仓详情的「当日订单 / 历史订单」从简易表格升级为券商风格订单列表：1) 每笔成交生成 10 位唯一订单号（随机数，DB 唯一索引兜底，存量订单统一重排为 10 位），订单号可点击复制；2) 表格列对齐参考图——订单号 / 订单状态 / 市场（市场图标）/ 股票代码 / 股票名称（股票图标）/ 方向 / 委托类型（限价单）/ 委托数量 / 委托价格 / 币种 / 委托时间（含美东、北京时间等市场时区标签）/ 成交均价 / 成交数量 / 成交金额 / 费用 / 已实现盈亏 / 有效期（撤单前有效）/ 时段（盘中+盘前盘后）；3) 顶部当日/历史 Tab + 市场筛选（含市场图标）+ 订单状态筛选，Tab 与筛选本地持久化；4) 订单加载中仅在没有已有数据时展示，避免刷新闪「加载中」；我的持仓详情保留编辑/删除操作，删除对当日与历史订单均可用。tsc 无错误、npm run build 通过、订单创建与存量重排端到端验证 10 位唯一。
1. 收益率趋势图基准做成图1 下拉主要市场 + 黄绿点 —— 按参考图：基准对比条由「我的持仓 ● · 标普 500 ●」图例改为图1 式下拉——左侧「跑赢 + 黄绿色圆点 + 基准名 + 箭头」，右侧显示跑赢幅度（组合收益 − 基准收益）；点击弹出主要市场下拉（标普 500 / 纳斯达克 / 道琼斯 / 恒生指数 / 上证指数 / 深证成指），选中项深蓝底白字加对勾；基准选择持久化（`fire:asset-benchmark`）。数据源：US/HK 用指数 ETF（SPY/QQQ/DIA/02800），CN 上证/深证指数走 `/api/kline/full` 新增 `index=1`（sh000001 / sz399001，`lib/kline.ts` `tencentSymbol` 支持指数代码），实测三路日 K 正常；图表基准线随选择切换名称与数据。tsc 无错误、资产分析页编译加载正常。
1. 截图导入持仓/自选 · Apple Vision 本地识别引擎接通 —— 我的持仓与自选股搜索框新增相机入口，上传券商持仓/行情截图后由 macOS 本地 Apple Vision（VNRecognizeTextRequest）识别：1) 新增 `scripts/vision-ocr.swift`（Swift 编译产物按需缓存于 `.cache/`，macOS + Xcode/CLT 缺失时返回 503 明确提示）；2) `lib/snapshotParser.ts` 把识别文本按视觉行聚类解析为 名称/代码/市场/数量/现价/成本/涨跌幅（代码前缀推断市场、标题行/表头过滤、同码去重，持仓布局按「成本→现价」、行情布局按「现价」回退）；3) `POST /api/records/import-image` 由 503 占位接通为真实识别（8MB 内 JPG/PNG/GIF/WEBP、限流保留），识别结果先预览可编辑，确认后 `POST /api/records/import-apply` 合并写入持仓/自选（不误清空已有数量/成本）；4) 图片仅在本机识别不上传云端。引擎抽象已就绪，未来 NAS/服务器部署时换 RapidOCR 只改后端一小块。tsc 无错误、持仓/行情两张测试截图端到端识别解析全部正确。

### 修复

1. 美股盘前/盘后行情批量刷新加固（账户资产不再延迟） —— 修复资产分析-账户资产在美股盘前/盘后部分股票价格不更新、需点进个股才刷新的问题：根因是列表轮询批量请求 Yahoo 扩展时段行情时部分股票被限流/超时，`fetchUsExtendedQuote` 返回 null，行情退回腾讯收盘价，而个股页单只请求容易成功。修复：1) `fetchUsExtendedQuote` 改用与个股页同款的 `range=1d`（负载更小且已含盘前/盘后分钟），盘前昨收取 `meta.regularMarketPrice` / 盘后取 `meta.previousClose`，保持「当日」口径正确；2) Yahoo `query1 → query2` 双主机自动兜底，单主机 6s 超时；3) 成功缓存 60s / 失败缓存 20s，缩短卡顿窗口并降低上游压力；4) `fetchQuotes` 扩展行情按代码去重（同一股票同时出现在持仓/自选只请求一次）、并发 6→4；5) `/api/kline/session-day`（个股页扩展时段）同步双主机兜底。tsc 无错误、npm run build 通过、算法用真实 Yahoo 数据模拟盘前验证（取到当日 09:29 最新盘前点、昨收口径涨跌正确）。
1. 删除按钮边框统一（浅色浅灰 / 深色白框，去红边） —— 按用户要求删除按钮不再用红色边框与红底悬停：行内删除按钮（自选股/持仓/用户管理）边框改为浅色 border-edge / 深色 border-white/20，悬停浅灰（hover:bg-brand-hover）非红；批量删除按钮去红边；附件/名人/素材库/设置等图标删除按钮悬停由红色 up-bg 改为中性浅灰（dark:bg-white/10）。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 修复深色模式下删除按钮白底白图标看不见 —— 按用户反馈（深色模式删除按钮背景与本体重叠看不见）：根因是部分删除按钮用了 bg-white 且深色无覆盖（白底）+ 白色 trash.fill 图标。修复：1) globals.css 增加深色 hover 覆盖——hover:bg-up-bg 深色变深红 rgba(58,28,28,.9)、hover:border-up 红边框、hover:text-up 亮红，全站删除/危险按钮悬停不再闪白；2) 自选股/持仓/用户管理行内删除按钮补 dark:bg-[#1c222d] dark:border-white/10 dark:text-white/70，深色下图标清晰可见。资产分析中的垃圾桶是「重置」非删除，未改动。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 全站删除按钮统一为苹果 trash / trash.fill 图标 —— 用户从 SF Symbols App 导出 trash.svg（浅色，已调整为深灰渐变可见）与 trash.fill.svg（深色白渐变），新增共用组件 components/DeleteIcon.tsx（按深浅色自动切换两张 SVG）。全站替换删除按钮：自选股/持仓行内删除、批量删除、用户管理删除、订单删除、素材库删除券商/指数、附件管理删除、自选股分组删除、名人持仓明细删除等手绘垃圾桶与 X 图标按钮统一改为 DeleteIcon；保留弹窗内「确认删除/删除」文字确认按钮与 TradeOrdersPanel 的重置（重置非删除）。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 管理员徽章放大 + 右下角徽章染青色（浅/深两版 SVG） —— 按用户反馈：1) 管理员 seal 图标由 16px 放大到 20px；2) 将 SVG 中右下角「seal 徽章」路径染成青色——浅色版人物黑 + 徽章 #00BCD4，新增深色版 person.badge.checkmark.seal.fill-dark.svg（人物白 + 徽章亮青 #00E5FF），账号菜单按深浅色切换显示，不再用 dark:invert 整体反转。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 管理员徽章换成正版 SF Symbol person.badge.checkmark.seal —— 用户从 SF Symbols App 导出正版符号（64pt SVG），接入 public/icons/：person.badge.checkmark.seal.svg 与 .fill.svg（Apple CoreSVG、fill=black）。账号菜单「管理员」文字替换为该符号的 fill 版（16px，深色模式 dark:invert 变白），删除此前临时替代的 person.crop.circle.badge.checkmark PNG 与残留 .DS_Store。tsc 无错误、npm run build 通过、服务重启后页面 200、SVG 访问 200。
1. 账号菜单去掉统计区与菜单区之间的灰色分割线 —— 按用户截图（红箭头指向）移除账号菜单中自选股/持股统计区与功能菜单区之间的水平分割线，区块衔接更干净；底部退出登录上方的分割线保留。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 账号菜单卡片深色适配 + 阴影加强（不再白卡看不见） —— 按用户反馈（卡片太白看不见）修复账号菜单卡片：1) 面板补深色背景（dark:bg-[#1c1c1e] + 深色边框/分割线），深色模式下不再整卡发白；2) 菜单项 hover 深色下由浅灰改为 white/10，管理员标签与头像兜底方块补深色适配；3) 阴影加强（0 14px 44px）使白卡在浅色背景上轮廓清晰。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 彻底修复订单列设置「上方区域消失」复发（overflow-clip 双端防护） —— 按用户反馈（对齐持仓样式后 bug 又复发）浅色模式无头 Chrome 复现定位：面板容器 overflow-hidden 使其成为可滚动容器（scrollHeight 829 > client 610），点击复选框后浏览器把「面板」整体滚动到 scrollTop 219，标题与顶部选项被顶出、下方留白。彻底修复：订单面板由 overflow-hidden 改为 overflow-clip（只裁剪、永不滚动，实测点击后 panelScrollTop 保持 0、标题 top 137 可见、顶部选项全部可见），持仓面板同步加 overflow-clip 防护，杜绝同源复发。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 订单「需要显示哪些列」面板完全对齐持仓样式 —— 按用户要求把订单列设置面板改成与持仓列设置面板完全一致的样式：顶部锚定（pt-[12vh]）居中、面板 320px 宽、标题栏同款、列表 max-h-[55vh] 滚动、行样式 mx-2 rounded-[8px] py-1.5 同款、六点拖拽手柄、无行间分割线（与持仓一致），仅保留 Portal 挂载保证 fixed 定位稳定。无头 Chrome 实测两面板：同位置/同宽/同行高/同手柄/同无分割线，差异仅为订单列数更多。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 全站清理面板小字提示（持仓列设置等，化繁为简） —— 按用户要求移除面板/设置里冗余的小字提示：1) 持仓「需要显示哪些列」的「勾选显示 · 拖动手柄排序 · 自动保存」（订单列设置此前已删）；2) 名人持仓头像选择下拉的「选择名人，上传新头像」；3) 素材库「新增主流券商」右侧的「点击名称添加…」；4) 素材库设置里 股票图标同步 / 退市检测 / CDN 图标通道 / 清理孤立文件 下方的功能说明小字。仅删除提示文字，功能与标题保留。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 深色下列设置面板行间白色分割线弱化 —— 按用户反馈（图中的白色线）去掉深色模式下「需要显示哪些列」面板的行间分割线刺眼白线：分割线由 divide-edge/60 增加 dark:divide-white/10（深色下近乎不可见），未选中复选框边框由浅灰改为 dark:border-white/25（弱化白色感），浅色模式保持原样。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 复选框恢复白色对勾并去掉白色边框 —— 按用户要求微调复选框：选中态恢复为「深色底 + 白色对勾」（浅色为黑底白勾，深色为 #2b313c 深色底白勾，像素实测无白块），并去掉白色边框——选中态无边框、未选中态深色模式下边框由浅灰改为深灰 #3a4150；字体栈确认保持 苹方 PingFang SC + SF Pro（苹果系统字体、无衬线、字形方正，tailwind font-sans 已配置）。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 列设置面板对齐参考图：去提示文字、深色去白块、文字左图标右、字体字重对齐 —— 按左图参考优化「需要显示哪些列」面板：1) 删除提示文字「勾选显示 · 拖动手柄排序 · 自动保存」；2) 深色模式下复选框由白色实心块改为透明底 + 白色对勾 + 半透明白边框（与参考图一致，像素实测背景透明）；3) 行布局改为 justify-between——文字在左、拖拽图标（三横线 ≡，参考图同款）在右；4) 字体对齐参考：标题 16px 半粗（原 15px 粗体）、列表标签 13px 中粗（font-medium），参考图字体本身即苹方 PingFang SC + SF Pro（与本站系统字体栈同源），观感差异主要来自字重与字号，已对齐。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 修复紧凑面板后「上方区域消失」复发（根因：面板自身被滚动） —— 按用户反馈（变短后之前的 bug 复发）定位到根因：面板自身是 overflow-hidden + 固定高度，浏览器在点击/聚焦复选框时会把「面板」整体滚动（实测 scrollTop 409px），把标题与顶部选项顶出可视区、下方留白——这就是「需要显示哪些列上方区域没了」的真因。修复：面板容器由 overflow-hidden 改为 overflow-clip（只裁剪、不可滚动），滚动只发生在内层列表；无头 Chrome 实测：即使触发 scrollIntoView 点击「撤/废单数量」，面板位置不变，标题与「全部/订单状态/市场」始终可见、无空白。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 订单列设置面板改为参考图紧凑效果（限高 + 行分割线 + 三横线手柄） —— 按参考图优化「需要显示哪些列」面板：1) 面板限高 min(560px, 75vh)，不再占满视口，一屏约 11-12 行（其余滚动）；2) 行与行之间加浅灰分割线（divide-y），界限清晰；3) 拖拽手柄由六个点改为参考图的三横线（≡）图标；4) 保持紧凑布局无空白。无头 Chrome 实测：面板高 560px、22 行带分割线、三横线手柄、无大片空白，与参考图一致。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 列设置弹层改为 Portal 挂载 + 防焦点滚动（面板不再位移/上方区域消失） —— 针对「取消勾选撤/废单数量后需要显示哪些列上方区域消失」追加两道防线：1) 列设置弹层改用 React createPortal 挂到 document.body 并内联固定定位，彻底摆脱任何祖先容器（transform/滚动容器等）对 fixed 的干扰，弹层永远相对视口居中；2) 复选框 onMouseDown preventDefault，阻止点击后浏览器把焦点元素滚动进视野导致列表跳动、顶部选项被滚走；无头 Chrome 实测：点击前后 panelTop 148/panelH 717 不变、scrollTop 0 不跳动、标题与「全部/订单状态/市场」始终可见。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 订单列设置面板与订单表格布局重构（无头 Chrome 实测复现） —— 用无头 Chrome 实测复现并修复资产分析-订单列设置的空白/变形问题：1) 列设置面板行内「文字与拖动手柄之间的大段空白」根因是复选框标签 flex-1 拉伸，改为紧凑布局（FlatCheckbox 增加 stretch 选项，订单面板传 false）；2) 勾选状态未全局生效根因是资产分析与我的持仓详情用了不同本地存储键，统一为共享键 fire:orders:columns:v2，勾选即自动保存并两处同时生效；3) 订单表格 22 列在 813px 卡片内被挤压截断，恢复「固定像素列宽 + minWidth + 横向滚动」的券商宽表样式（列宽舒适、隐藏列后剩余列自动伸展不空白），并新增 .orders-scroll 样式让横向滚动条始终可见，避免误以为列被切断；4) 资产分析左右分栏 align-items: stretch 导致较矮的右列被拉伸、订单卡片下方出现 300+px 空白，改为 align-items: start，实测卡片下方空白由 347px 降至 0px。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 侧边导航图标去重 —— 修复侧边导航图标重复：资产分析原折线图与全球预览撞款，改为饼图（资产分布语义）；名人持仓原人形图标与用户管理撞款，改为星标（名人语义）；用户管理保留人形、全球预览保留折线图，全站导航图标互不重复。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 批量规范持仓 ETF 名称 —— 批量规范化持仓记录里的冗余/不规范名称（共 9 条）：AAPL2倍做多2x etf→苹果 2 倍做多 ETF、RKLB2倍做多2x etf→Rocket Lab 2 倍做多 ETF、Spcx2倍做多2x etf→SpaceX 2 倍做多 ETF、Tesla2倍做多2x etf→Tesla 2 倍做多 ETF、英伟达2倍做多etf→英伟达 2 倍做多 ETF、Robinhood2倍做多etf→Robinhood 2 倍做多 ETF、超微电脑2倍做多etf→超微电脑 2 倍做多 ETF、太空etf→太空 ETF、标普500etfvanguard→标普 500 ETF（Vanguard），去除「2倍/2x」重复与大小写混乱，统一「名称 + 2 倍做多 ETF」格式，订单表 trade_orders.name 与日志 activities.stock_name 同步更新（共 40 条）；该命名规则已写入 AGENTS.md「股票 / ETF 名称规范」章节。
1. 订单号复制兼容非安全上下文（局域网 IP 不再复制失败） —— 按用户截图修复订单详情弹窗「复制失败，请手动选择订单号」：根因是页面经 http://localhost 等局域网 IP 访问时非安全上下文，navigator.clipboard 不可用。新增 copyText 兼容方案——安全上下文优先 Clipboard API，失败/不可用时回退临时 textarea + document.execCommand('copy')，表格与详情弹层的订单号复制按钮均生效。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 资产分析刷新改为无感无提示 + 刷新图标点击转一圈 —— 按用户要求：1) 资产分析页（账户资产/持仓/交易）刷新不再弹出成功提示（移除「已刷新当前行情」「订单已刷新」等 toast，错误提示保留），刷新无感静默完成；2) 全站 RefreshButton 改为点击时图标旋转一圈（0.6s、单次迭代）的轻量反馈，不再长时间持续旋转，移除各调用点的 spinning 状态传参。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 区域刷新只刷新当前区域（不再连带刷新趋势图） —— 按用户反馈修复资产分析页刷新按钮连带刷新收益率趋势图/总资产趋势图的问题：根因是刷新链路会重载汇率（rates 在趋势图 effect 依赖中）并触发父层重拉持仓记录（positions 变化同样触发），导致趋势图被重跑。修复：账户资产/持仓刷新改为只强制重取当前行情（refreshQuotes force），不再重载汇率、不再触发 records-updated，趋势图状态保持不动；交易刷新仍只重取订单。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 订单表「操作」列固定首位 —— 按用户反馈修复订单表操作列被本地保存的列顺序排到末尾的问题：normalizeOrderColumns 加载列配置时强制把「操作」列移动到第一位（详情/编辑/删除入口始终在第一列），不再受 localStorage 历史顺序影响。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 资产分析页移除返回按钮与蓝色美元图标 —— 资产分析成为独立页签后，顶部标题行保留「资产分析」文字，移除左侧返回按钮（onBack 链路整体清理：AssetAnalysisDashboard Props 去掉 onBack，AssetAnalysisView 与 RecordsApp 同步移除传参）与蓝色渐变美元图标（MoneyIcon 引用一并移除）。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 委托时间日历改为 fixed 智能定位（向上/向下自适应 + 左右不越界 + 锚点三角） —— 按参考图 2 优化委托时间弹窗：由绝对定位（以按钮左缘展开、右侧易被裁切）改为 fixed 视口定位——以「委托时间」按钮为锚点，左右钳制在视口内（宽度 min(680, 视口-16)，left 自动收拢，右侧不再越界被遮挡）、上下自适应（下方空间不足时向上弹出，向上时以 bottom 锚定并加 max-height 内部滚动，防止顶/底部被裁切）；弹窗带小三角指向按钮（向上弹出时指向下方按钮，向下时指向上方），与参考图一致。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 订单表隐藏列后不再变形 + 委托时间自定义日历向上弹出 —— 1) 订单表「需要显示哪些列」隐藏任意列后表格变形（剩余空间被摊到个别列、列宽悬殊）修复：表格改为 table-fixed 固定布局，每列按基准宽度（px）固定列宽，隐藏列后剩余列等比例伸展、窄屏保持横向滚动，列宽不再忽宽忽窄；2) 委托时间自定义双月日历底部显示不全修复：弹层改为优先向下、下方空间不足（<470px）时自动向上弹出（参考图 2），避免被视口底部裁切。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 收益率·加权选择器改自定义下拉（深色不再白块） —— 按参考图：资产分析「收益率·简单加权 / 时间加权」由原生 `select` 改为自定义下拉——触发器改为透明文字 + 箭头（无边框无白底），展开菜单深色（`dark:bg-[#1b2029]`、细边框、圆角、阴影），选中项蓝色高亮 + 对勾、未选中 hover 浅灰；深色模式下不再弹出白色原生菜单，与全站 `CurrencyPicker`/基准下拉风格统一。tsc 无错误、资产分析页编译加载正常。
1. 收益率趋势图周期行窄屏错位修复（日历按钮不再遮挡） —— 按用户反馈：页面变窄时周期胶囊溢出，日历按钮遮挡后面的胶囊、原生滚动条裸露。修复：周期滚动容器加 `flex-1 min-w-0`（占满剩余宽度、内部滚动，日历按钮固定在右侧永不重叠），并用 `[scrollbar-width:none]` 隐藏横向滚动条；日历按钮去掉 `ml-auto`（由 `flex-1` 容器天然推右），垂直对齐保持 `items-center`。tsc 无错误、资产分析页编译加载正常。
1. 日历按钮恢复为原来的胶囊样式 —— 按用户反馈：将收益率趋势图时间行的日历按钮恢复为原来的胶囊样式（`h-8 w-10 rounded-full` + 日历/对勾图标），不再与周期胶囊同款；滚动区右侧留白 `pr-1` 保留。tsc 无错误、资产分析页编译加载正常。
1. 收益率趋势图日历按钮优化（与周期胶囊同款） —— 按用户反馈：趋势图时间行最右侧日历按钮与左侧周期胶囊形状不一致（圆形 vs 胶囊）且末尾胶囊被截断。优化：日历按钮改为与周期胶囊完全同款（`rounded-full` + `px-3 py-1.5` + 同边框/选中态），图标缩小到 14px；周期滚动区加 `pr-1` 留出呼吸空间，末尾胶囊不再紧贴截断；选中/展开时蓝色高亮与选中周期一致。tsc 无错误、资产分析页编译加载正常。
1. 持仓盈亏排行删除「所选区间 · 前 10 只」并收紧标题行 —— 按用户要求：资产分析-持仓盈亏排行移除副标题「所选区间 · 前 10 只」，标题「持仓盈亏排行」与右侧「更新至 …」改为同一排垂直居中，区块更紧凑不显宽。tsc 无错误、资产分析页编译加载正常。
1. 资产分析市场胶囊去掉悬浮/点击动画 —— 按用户要求：资产分析各市场筛选胶囊（账户资产 / 持仓 / 持仓盈亏排行的 全部·美股·港股·A股）去掉悬浮上浮、阴影与点击缩放的动画（`transition-all duration-200 hover:-translate-y-px hover:shadow-md active:scale`），仅保留选中高亮与 hover 颜色过渡，切换更干净。tsc 无错误、资产分析页编译加载正常。
1. 资产分析字号层级找临界点（页面标题 > 区块标题 > 标签） —— 按用户反馈：资产分析三块（资产分析卡 / 收益率趋势图 / 持仓盈亏排行）文字仍过大。定下层级临界点：页面顶部「资产分析」= 18px `extrabold`（最大）；区块标题（资产分析卡 / 持仓 / 账户资产 / 持仓盈亏排行）= 16px `bold`；趋势图 tab 选中 15px `bold` / 未选中 14px。保持 2px 阶梯，标题有主次、不再臃肿。tsc 无错误、资产分析页编译加载正常。
1. 资产分析-持仓搜索改为与市场标签同一排（参考图2） —— 按参考图：持仓区块改为「标题一行 + 工具一行」——第一行只有「持仓」标题，第二行左侧市场标签、右侧搜索框（胶囊按钮样式 `rounded-full h-8`，与标签同高）+ 设置按钮，全部同一排对齐，不再出现搜索悬浮在标题行、标签沉第二行的错位。tsc 无错误、资产分析页编译加载正常。
1. 资产分析区块标题字号统一为顶部「资产分析」 —— 按用户要求：资产分析各区块标题（持仓 / 账户资产 / 持仓盈亏排行 / 左侧资产分析卡）由 `text-sm font-bold` 统一为 `text-lg font-extrabold`，与页面顶部「资产分析」标题一致；「持仓」行改为 `items-center` 与右侧搜索/设置按钮垂直对齐。tsc 无错误、资产分析页编译加载正常。
1. 资产分析-持仓移除「N 只 · 实时行情」文字 —— 按用户要求：资产分析「持仓」区块标题下的数量与实时行情说明文字移除，标题与搜索/设置按钮保持同行。tsc 无错误、资产分析页编译加载正常。
1. 资产分析-持仓排序持久化（全站排序排查） —— 资产分析「持仓」表格的列排序（`holdingSort`）原来不持久化，刷新即还原。改为 `usePersistedState`（`fire:asset-holdings-sort`）。全站排序排查：我的持仓页排序已存 localStorage（`SORT_STORAGE_KEY`）、素材库排序已走 URL `?sort=&dir=`、自选股筛选/持仓市场等已 URL 持久化，均无需改动；本次补上资产分析这一处。tsc 无错误、资产分析页编译加载正常。
1. 资产分析-账户资产统计卡隐藏遮罩补星号 —— 资产分析右侧「账户资产」一排统计卡（净资产 / 当日盈亏 / 持仓市值 / 浮动盈亏 / 最大购买力 / 可用现金 / 冻结现金）隐藏态仍显示 `••••••`，统一改为 `******`，与全页星号遮罩一致。tsc 无错误、资产分析页编译加载正常。
1. 隐藏遮罩改星号 + 收益率/跑赢数值同步隐藏 —— 按参考图：隐藏态金额由 `••••••` 改为 `******`（星号）；「收益率·简单加权 / 时间加权」选择器下方新增收益率数值（与参考图一致），隐藏时同步显示 `******`；基准条右侧跑赢幅度隐藏时也显示 `******`。收益率等百分比在隐藏状态下不再泄露。tsc 无错误、资产分析页编译加载正常。
1. 货币选择器改为半角括号紧挨效果（参考图2） —— 按用户反馈：资产分析货币选择器（总资产 / 累计盈亏）触发器由全角括号「（CNY）」改为半角括号并紧挨标题「总资产 (CNY)」，箭头与括号间距收紧（`gap-1` + `ml-0.5`），与参考图的紧挨排版一致。tsc 无错误、资产分析页编译加载正常。
1. 货币选择器触发器默认不显示市场/国家图标 —— 按用户澄清：不是隐藏金额，而是货币选择器（总资产 / 累计盈亏）的触发器按钮默认不显示市场/国家图标——「总资产（CNY）」保持纯文字，展开下拉后的选项里保留市场图标（美元🇺🇸/港币🇭🇰/人民币🇨🇳）；金额显示恢复默认可见（`assetsVisible` 撤销默认隐藏改动）。tsc 无错误、资产分析页编译加载正常。
1. 资产分析货币选择加市场/国家图标 —— 按用户要求：资产分析「总资产 / 累计盈亏」货币选择器，下拉菜单每个货币选项前加市场图标（美元→🇺🇸 US、港币→🇭🇰 HK、人民币→🇨🇳 CN，走素材库 `MarketIcon`，自定义市场图标优先），触发按钮也显示当前币种的市场图标，一眼可辨。tsc 无错误、资产分析页编译加载正常。
1. 收益率/总资产趋势图 tab 文字精致化（参考图1） —— 按参考图：趋势图两个 tab 从统一 `text-sm` 改为选中 = 17px 白色加粗、未选中 = 15px 灰色（hover 变白）、tab 间距加大（`gap-6`）、底部蓝色短下划线改为 2px 圆角短横线，贴合图1 的文字层级与精致度。tsc 无错误、资产分析页编译加载正常。
1. 资产分析货币等偏好刷新后保持（全站同类一并修复） —— 资产分析页选择货币后刷新会回到默认：`displayCurrency` 原来只从 prop 初始化且不保存。修复：新增通用持久化 hook [`lib/usePersistedState.ts`](lib/usePersistedState.ts)（localStorage），资产分析页的货币、趋势周期、图表 tab、加权方式、持仓/盈亏市场筛选、订单 tab 全部持久化；货币另写 URL `?cur=` 与我的持仓页共用同一 key（`fire:display-currency`），持仓页货币初始化和切换同步读/写该 key；财报日历的时段与市值筛选也持久化；其余页面已用 URL 持久化的（持仓市场 `?market=`、全球预览货币 `?cur=`、素材库 tab、自选股筛选 `?filter=`）保持不变。刷新/重进页面保持原状。tsc 无错误、`/holdings` 与资产分析页编译加载正常。
1. 资产分析-持仓搜索框与设置按钮缩小协调 —— 按用户反馈：资产分析「持仓」区块的搜索框（`h-10`、宽 190px、圆角 11px）与设置列按钮（`h-10 w-10`）偏大、与整体不协调。优化：搜索框缩为 `h-9`、宽 170px、圆角 9px、字号 13px、放大镜图标同步缩小左移；`HoldingColumnsButton` 缩为 `h-9 w-9`、圆角 9px、漏斗图标 17px；两者间距收窄（`gap-1.5`），视觉统一精致。tsc 无错误、资产分析页编译加载正常。
1. 列设置复选框改为参考图扁平样式（白底黑勾 / 透明浅边框） —— 按参考图细节优化「需要显示哪些列」弹层：原生浏览器灰勾复选框替换为自定义 `FlatCheckbox`——选中 = 白底 + 黑色粗勾（深色模式即图1 效果，浅色模式深底白勾），未选中 = 透明 + 浅灰边框；「全部」与各行统一；标题字号 15px 加粗、关闭 X 提升为 `ink-2`（深色下白色，贴合图1）。tsc 无错误、`/holdings` 与资产分析页编译加载正常。
1. 金钱图标升级为苹果实心硬币风格（dollarsign.circle.fill） —— 按用户反馈：资产分析标题的蓝色磁贴图标点击进入后仍是旧的细线圆圈 $ 样式。现升级为苹果 SF Symbols `dollarsign.circle.fill` 实心硬币风——实心圆 + 挖空的居中 $（`MoneyIcon` 改为 fill + `--money-cut` 挖空色）；蓝色磁贴加苹果式渐变（`from #3f9bff to #2377e6` + 蓝色投影），图标放大到 24px；「账户资产」入口圆形按钮自动同步同一实心风格（深浅色各自用当前背景色挖空）。tsc 无错误、`/holdings` 与资产分析页编译加载正常。
1. 资产分析-持仓：列设置改窄面板浮层 + 苹果风格金钱图标 —— 1) 「需要显示哪些列」由大弹窗（AppModal md，近半屏宽）改为窄面板浮层：`max-w-320px`、紧凑行（`py-1.5` + hover 行高亮）、列表 `max-h-55vh` 可滚动、顶部标题 + 关闭、底部提示一行，贴合参考图的尺寸与密度（约 20-25% 宽）；2) 金钱图标统一为苹果风格（SF Symbols `dollarsign.circle`）：新建共享组件 [`components/MoneyIcon.tsx`](components/MoneyIcon.tsx)（干净圆环 + 居中 $，stroke 1.7），替换资产分析标题蓝色磁贴内与「账户资产」标题右侧入口按钮的原手绘 $ 图标；入口按钮由白底描边方形改为浅灰圆形（`rounded-full`，hover 上浮 + 阴影 + 图标放大，深浅色适配）。tsc 无错误、`/holdings` 与资产分析页编译加载正常。
1. 修复深色模式行悬停变白（全站表格排查） —— 我的持仓表格行深色模式悬停背景变浅灰/白色、文字变深色。根因：`hover:bg-bg-gray/60` 只在 `globals.css` 有静态 `.dark .bg-bg-gray/60` 覆盖，缺少 `.dark .hover:bg-bg-gray/60:hover`，深色下仍用浅灰 `rgb(247 248 250/.6)`。修复：1) 主持仓表（`HoldingsView`）、资产分析（`AssetAnalysisDashboard` 盈亏排名行与持仓表）行悬停补 `dark:hover:bg-[#1b2230]`；2) 全站同类问题——用户管理、日志、API 文档表格行 `hover:bg-[#fafbfc]` 补 `dark:hover:bg-[#1a212e]`；3) 持仓股票名 `group-hover:text-brand-deep` 深色下变深色文字，补 `dark:group-hover:text-[#c6cdd8]`；4) `globals.css` 增加安全网 `.dark .hover:bg-bg-gray/55`、`/60` 与 `.dark .group:hover .group-hover:text-brand-deep`，防止后续回归。全站扫描确认无其他浅色 hover 无深色变体（橙色强调 `#FF9828` 为既有品牌色除外）。tsc 无错误、`/holdings` 深浅色实测正常。
1. 默认端口切换为 3000（不再使用 8124） —— 按用户要求：Web 服务默认端口由 8124 改为 3000。`package.json` 的 `dev`/`start` 脚本本就绑定 `0.0.0.0:3000`，本次把其余 8124 引用全部对齐：`lib/settings.ts` 默认站点域名 `localhost:8124 → localhost:3000`、`scripts/smoke-test.sh` 默认 `BASE → http://localhost:3000`、`fire-web/README` 与根 `README` 与 `AGENTS.md` 的命令/端口说明改为 3000（撤销此前「固定 8124」约定）、`docs/api-spec.md` Base URL → `:3000`、iOS `APIClient.swift` 后端地址 `localhost:8124 → :3000`（`fire-ios/README` 与 `AGENTS` 同步）。历史版本记录不改写；服务已重启监听 3000。

---

## v0.1.7 · 2026-08-13

> 完善资产分析的市场与货币联动，并支持安全删除历史订单。

### 新功能

1. 持仓列全局管理与持仓占比 —— 资产分析持仓新增“持仓占比”、代码 / 名称搜索和苹果风筛选入口；所有列可点击排序、勾选显示隐藏，并通过右侧手柄拖动顺序。配置自动保存并即时同步“我的持仓”主表，两处显示列与顺序保持一致。

1. 资产分析左右布局可拖动 —— 上下区域共用贯穿整列的分隔手柄，可左右拖动调整左侧资产卡片 / 趋势图与右侧账户资产 / 持仓 / 订单宽度。拖动比例自动保存并在下次打开时恢复，双击恢复默认；设有两侧最小宽度，窄屏自动切回单列。

1. 资产分析市场筛选与隐私显示 —— 账户资产与持仓新增两组互不干扰的市场胶囊，账户资产只联动当前市场资产指标，持仓只联动持仓表；胶囊带背景、悬停上浮和点击反馈。总资产、累计盈亏支持 USD / HKD / CNY 切换，总资产旁新增眼睛按钮，可同步隐藏或显示页面资产金额。

1. 资产分析区间盈亏排行与日期筛选 —— 收益率 / 总资产趋势新增双月日历区间选择，星期统一使用中文；日期区间同步驱动曲线、累计盈亏及持仓盈亏排行。排行支持全部、美股、港股、A 股等已有市场切换，默认显示前 10 只，更多持仓可展开或收起。

1. 历史订单安全删除 —— 历史订单新增删除入口和二次确认，删除后按时间重放该股票剩余成交账本，重新计算持仓、成本和已实现盈亏；若会导致后续卖出超卖则自动回滚。`DELETE /api/v1/orders/{id}` 已同步写入 API 文档，供 Web 与 iOS 使用。

### 修复

- **美股 20:00 结算状态自动推进** —— 修复盘后行情在美东 20:00 停止轮询后，页面没有重绘而继续显示未结算的问题。现在每 15 秒只用本地时间校准结算状态，窗口重新聚焦或标签页恢复时立即更新；19:59 仍显示盘后，20:00 起自动显示已结算，次日 04:00 进入新盘前，且不会额外请求行情 API。

- **持仓卖出成本对齐券商摊薄口径** —— 修复部分卖出后成本价与券商平台差距过大的问题：卖出回款改为冲减累计投入并摊薄到剩余持仓，亏损卖出提高剩余成本、盈利卖出降低成本且支持负成本；费用只进入已实现盈亏，避免重复计入。成交订单新增交易前数量与成本快照，订单更正 / 删除可从明确基准重放，不再由成交后快照反推。同步纠正 SPCH 卖单成交价 8.50→8.60，并把成交后成本统一保留 3 位小数；剩余 870 股成本精确恢复为 14.586，按券商截图现价 10.163 计算的浮动盈亏为 -3,848.01（-30.32%）。

1. 资产分析持仓与订单展示 —— 持仓首列统一为“名称 / 代码”，名称在上、代码在下；全站“今日订单”改为“当日订单”，并严格按 `tradedAt` 成交时间归类而不是按添加时间。资产分析页的当日 / 历史订单标签现可正常切换并展示对应成交。

1. 新杠杆 ETF 正股图标 —— 补齐 SMCX→SMCI（超微电脑）、ROBN→HOOD（Robinhood）的统一 ETF / 正股关系；图标和详情反向正股入口均自动复用正股资料，避免新添加 ETF 绕过既有规范。

1. 资产分析市场胶囊布局 —— 修复账户资产市场按钮上半部分被裁切的问题，并将持仓市场筛选独立放到“持仓”标题下方，与账户资产保持相同的标题、筛选、数据布局层级。

1. 历史订单深色主题 —— 移除深色模式下订单行突兀的浅灰背景，改为与卡片一致的深色表面和克制的悬停反馈。

1. 资产分析货币与区间收益口径 —— 总资产、累计盈亏统一为美元 / 港币 / 人民币下拉菜单，移除“离岸人民币”文案；自定义区间按首尾有效收盘价计算持仓盈亏，全部区间使用当前浮动盈亏。简单加权、时间加权和标普 500 基准均按所选区间重新归一，避免货币或日期切换后口径错位。

1. TradingView SVG 图标上传兼容 —— 修复安全校验将 SVG 标准 `xmlns="http://www.w3.org/2000/svg"` 命名空间误判成外部链接，以及将 `<svg>` 根节点前的 `<!-- by TradingView -->` 版权注释误判成非 SVG 的问题。识别器仅跳过 BOM、空白、完整 XML 注释和 XML 声明，再严格确认根节点；继续阻止脚本、事件、远程图片、data URL、HTML 包裹与危险 CSS。校验通过后，素材库保存副本会将来源注释规范为 `<!-- by fire -->`，不修改桌面原文件和 SVG 图形内容。

1. Web 服务固定 8124 端口 —— 开发 / 生产启动脚本统一绑定 `0.0.0.0:8124`，README、AGENTS、冒烟测试、API 规范和默认站点域名同步锁定 8124。以后只运行 `npm run dev`，禁止临时追加 `-p` 参数或切换端口，避免服务正常但访问了旧端口，以及多个 Next 开发进程互相干扰。

1. 持仓个股详情轻量化 —— 点击“我的持仓”股票后不再挂载完整个股详情和 K 线图，直接复用持仓页已加载的实时行情，集中展示最新价、持仓市值、当日 / 累计盈亏、盈亏率、成本和数量，并保留交易与订单追溯。由此移除首屏额外的详情、财务、公司资料及图表请求，持仓二级页更快、更直接；市场标识统一使用素材库市场图标，价格卡和订单标签同步适配深色主题。

1. 美股盘后当日盈亏结算边界 —— 修复美东 16:00 后错误使用当天收盘价作为涨跌基准、导致当日盈亏提前重置的问题。美股盘前、盘中、盘后现在统一相对前一常规交易日收盘价计算，扩展行情从近 5 个交易日分钟数据中提取可靠基准，仅在美东 20:00 后进入结算状态。

## v0.1.6 · 2026-08-12

> 个股详情页用相关 ETF 聚合替换期权占位，并支持进入 ETF 详情。

### 新功能

1. 我的持仓新增资产分析子页面 —— 「账户资产」标题右侧新增钱币图标入口，进入独立资产分析仪表板。左侧集中展示总资产、当日盈亏、持仓市值、总盈亏和多市场资产分布；收益率趋势图 / 总资产趋势图支持本月、近 1 月、近 6 月、本年、近 1 年、全部区间，并提供简单加权 / 时间加权入口及标普 500 对照。趋势按当前持仓数量和真实历史收盘价回溯，不生成随机数据；右侧展示市场资产汇总、实时持仓表和订单账本，深浅色及窄屏同步适配。

1. 成交订单支持安全更正 —— 持仓二级页每笔订单新增“编辑”，可调整买入 / 卖出方向、价格、数量、费用、成交时间与备注；新增 `PUT /api/v1/orders/{id}`，保存时按成交时间重放该股票全部订单，重新计算成交后持仓、成本与已实现盈亏，出现超卖则整次回滚。Web 与 iOS 可共用。

1. 个股详情页新增相关 ETF 聚合 —— 「期权」页签更名为「ETF」，按当前美股标的归拢做多、做空与期权收益 ETF，提供策略筛选、实时价格 / 涨跌幅和点击进入 ETF 详情；返回箭头可回到原股票的 ETF 列表。

1. 持仓二级交易与订单追溯 —— 我的持仓一级页移除买入 / 卖出与全局订单标签，点击股票无刷新进入二级个股详情；详情页提供统一「交易」入口和该股票今日 / 历史订单，交易与订单区域和上方个股详情保持同宽。新增不可变订单账本与 `/api/v1/orders`，成交和持仓原子更新，买入按含费用加权成本，卖出防超卖并记录已实现盈亏，Web / iOS 共用。

### 修复

1. SPCH 与新上市美股 K 线兜底 —— 修复 `SPCH.AM` 等美交所证券没有 K 线的问题：美股历史请求统一剥离交易所后缀；新浪日 K 或 5 日分钟线缺失时自动回退 Yahoo 日线 / 5 分钟线。个股日、周、月、季、年 K 与 5 日走势共用兜底，API 文档同步供 iOS 使用。

1. 美股扩展时段行情与持仓盈亏统一 —— 修复 SPCH 等美股盘前详情价上涨、但自选涨跌幅和持仓当日盈亏仍沿用常规盘跌幅的问题。统一行情接口在美东盘前 / 盘后覆盖有效价格、涨跌额、涨跌幅、时段和昨收；涨跌基准使用最近一次常规盘收盘价，规避扩展行情源对新上市 / 杠杆 ETF 返回复权旧值。Web 列表、个股详情、持仓盈亏与 v1 iOS 接口同步生效。

1. 多市场当日盈亏口径优化 —— 美股、港股、A 股分别按交易所当地交易日计算，单股采用 `(最新价 - 昨收) × 持仓数量`；混合市场先按本币计算再换算汇总。美股覆盖美东盘前 / 盘中 / 盘后，并以美东 20:00 结算冻结当日盈亏；首次进入加载全部市场收盘快照，后续只在对应市场有效交易时段每 30 秒刷新，周末和休市停止行情请求并保留最后有效值；汇率网络异常时使用可靠兜底立即展示，不再长期显示“计算中”。

1. 财报日历今日高亮主题适配 —— 今日整格边框由橙色改为白色；深色模式采用纯白边框、半透明白底和白色柔光外圈，浅色模式采用白色边框、浅灰底和中性灰外圈，确保两种主题下都清晰协调。

1. 财报日历当前日期高亮 —— 当前日期由仅数字小圆圈改为白色整格强调边框、柔和外圈和黑白日期徽标，加载骨架与完成态一致，并补充 `aria-current="date"`；前端首页与后台财报日历复用同一组件，已同步生效。

1. 杠杆 ETF 反向正股关系补齐 —— 修复 `SPCH`、`RKLX` 等历史杠杆 ETF 只有正股图标、详情页却没有对应正股的问题；统一补齐 `RKLX→RKLB`、`SPCH→SPCX`、`MSTU→MSTR`、`SSO/SPUU→SPY`、`QLD→QQQ`，并兼容 `.AM` / `.N` / `.OQ` / `.PS` / `.K` 交易所后缀。图标和详情页现在共用同一关系源，避免同类映射再次遗漏。

1. 个股详情名称与涨跌额显示优化 —— 详情页标题优先使用自选股保存的股票名称，并在右侧单独显示代码，修复相关 ETF 标题重复显示代码的问题；现价继续保留市场货币符号，下面的涨跌额移除重复货币符号，仅显示正负数值和涨跌幅。

1. 自选股编辑支持负成本 —— 修复成本价为负数时无法保存、却错误提示“请填写股票名称和代码”的问题。普通 `/api/records` 与移动端 `/api/v1/records` 现共用持仓校验：成本价允许负数，现价与数量仍禁止负数，并返回准确的字段提示；API 规范同步注明负成本语义。

1. 相关 ETF 与正股双向关联 —— 修复苹果相关 ETF 静态目录仅有 3 只的问题，核实并补齐为 9 只美国上市单股产品；正股显示「ETF」，相关 ETF 反向显示「正股」及其基础股票。ETF 清单、反向关系和全站图标统一从同一映射派生，所有相关 ETF 固定复用正股股票图标。

1. 自选股深色模式悬停背景 —— 修复鼠标经过股票名称单元格时出现突兀浅灰白色块的问题；整行与股票单元格改用对应的暗色悬停层级，保留交互反馈并统一深色表格视觉。

1. 自选股负成本保存 —— 修复编辑股票时负成本被错误归类成“名称和代码缺失”、导致无法保存的问题；成本价允许负值以覆盖返佣、期权收入及累计回款场景，现价和数量继续禁止负数，普通接口与 v1 接口统一校验和准确提示。

---

## v0.1.5 · 2026-08-09

> 完成全站 BUG 排查与安全审计：修复生产构建、认证、上传、文件路径与依赖漏洞。

### 新功能

1. 美股扩展时段分钟行情 —— 接入 Yahoo Finance 盘前/盘中/盘后一分钟数据并按美东时间筛选。AAPL 实测全天 960 点：盘前 330、盘中 391、盘后 239；夜盘因公开源不覆盖时明确提示暂无数据。

1. K 线新增 TradingView 图形类型 —— 图形菜单新增带标记线、阶梯线和 HLC 区域；全天/5天支持线型图，1个月及以上基于完整 OHLC 数据开放 HLC 高低区间填充，与现有面积、折线及三种 K 线共存。

1. 个股财务页首版上线 —— 个股详情「财务」新增财报、收入细分、财务评分、关键指标、利润表、资产负债表、现金流表、股东回报 8 个子页，展示最新季度摘要、营收/净利润趋势、指标表格和环比；美股接入 SEC Company Facts 官方申报数据并做缓存、限流与异常降级，其他市场明确提示待接入。

1. 个股走势图升级为长桥式交互 —— 新增全天分时菜单（全天 / 夜盘 / 盘前 / 盘中 / 盘后）、5天 / 1个月 / 3个月 / 今年至今 / 1年 / 5年 / 最大时间区间，以及山型图 / 折线图切换；深浅色和移动端同步适配。日内数据复用腾讯分钟行情，历史区间复用前复权日线，数据源缺少扩展时段时明确提示而不伪造走势。

1. 素材库新增「分组图标」分类（自定义分组图标归入，非券商） —— 按用户方案：素材库新增「分组图标」tab——列出所有非券商的自定义分组（与素材库券商图标同款行样式：序号 + 相机悬停上传图标 + 清除按钮），上传走 `/api/upload`（`kind=asset` + `folder=group`，文件名按分组名规范命名，存放 `uploads/asset/group/`）再 `PUT /api/v1/watch-groups/{id}` 更新图标（服务端注册素材库 `type=group` 防清理丢失），保存后全局生效（筛选条 / 分组面板 / 管理分组同步）；券商分组（长桥证劵、华泰证劵等有券商图标的）不进入此分类，仍走「券商图标」。自选股管理分组的相机上传同步改用 group 文件夹。`AGENTS.md` 更新分组图标命名规范。tsc 无新错误、`/library` 编译加载正常。
1. 自选股分组方案 A：独立分组实体（Web/iOS 共享服务端） —— 按确认的方案 A 重构自选股分组：1) 数据层——新增 `watch_groups` 表（id/name/icon/sort/visible/kind/market）与 `records.watch_group_id` 列，券商仍走 `group_name`，分组与券商彻底解耦；首次访问自动播种 6 个市场分组并把旧 `records.group_name` 去重迁移为自定义分组。2) 接口层——v1 新增 `GET/POST /api/v1/watch-groups`、`PUT/DELETE /api/v1/watch-groups/{id}`、`POST /api/v1/watch-groups/reorder`、`POST /api/v1/records/group-assign`（批量分配/移出，单条 SQL，重命名/删除不再循环 PUT 记录）；分组图标注册素材库 `type=group` 防清理丢失。3) 前端——`QuotesView` 改为服务端加载分组，筛选条/分组面板（网格+管理：拖动排序/眼睛显隐/相机图标/铅笔重命名/删除/新建）全走新接口；表格新增「分组」列行内标签（点击弹出分组选择快捷分配/移出）+ 批量工具栏「移动到分组」；URL `?filter=` 存分组 id（兼容旧 `M:`/`G:` 与 `?market=` 自动迁移），分组不存在自动回退全部。4) 迁移——旧 localStorage 分组配置一次性同步到服务端并清除。5) 文档——[`docs/api-spec.md`](docs/api-spec.md) 新增 6.3 章节，`AGENTS.md` 更新方案 A 约定。tsc 无错误、`/watchlist` 编译加载正常，接口已端到端实测（创建/查重/分配/移出/重命名/排序/删除/市场删除保护）。
1. 自定义分组行加相机上传分组图标 —— 分组管理-我的分组每行新增相机按钮（悬停即上传，同全站相机风格）：点击选择图片上传（`/api/upload`，`kind=asset` + `folder=broker`，文件名按分组名规范命名），成功后分组图标立即更新并持久化到分组配置 localStorage（`groupIcons` 字段）。图标优先级：自定义上传图标 > 素材库券商图标 > 灰色标签兜底，筛选条胶囊、分组面板网格、管理列表三处同步显示；重命名分组时图标随分组名迁移、删除分组时一并清理。tsc 无错误、`/watchlist` 编译加载正常。
1. 自选股筛选条替换为 iOS 关注页分组逻辑 —— 自选股「我的行情板」原「全部 / 美股 / A股 / 港股」市场胶囊替换为 iOS 关注页同款分组逻辑：1) 筛选条 = 全部 + 固定市场分组（美股/港股/A股/新加坡/日股/韩股，带市场图标与数量）+ 自定义分组（带数量），末尾虚线加号打开分组面板。2) 分组面板两级视图：全部分组网格（不受显隐影响，点选即筛选）→ 管理分组（市场分组与我的分组两个分区，行 = 拖动手柄排序 + 眼睛显隐 + 铅笔重命名 + 删除；自定义分组重命名 / 删除会循环 `PUT /api/records/{id}` 同步后端记录 group 字段，与 iOS 语义一致；市场分组重命名仅改显示名）。3) 新建分组（去重）。4) 分组配置（customGroups/groupOrder/visibility/marketOrder/marketRenames）持久化 localStorage（`fire:watch-groups:v1`，与 iOS AppStorage 语义一致），显隐默认空分组隐藏。5) 筛选状态写入 URL `?filter=`（`ALL` / `M:US` / `G:名称`，兼容旧 `?market=` 自动迁移），刷新前进后退保持；隐藏当前选中分组自动回到全部。新增 [`lib/watchGroups.ts`](lib/watchGroups.ts)（分组状态/顺序/显隐纯函数）与 [`components/WatchGroupSheet.tsx`](components/WatchGroupSheet.tsx)（分组面板），`AGENTS.md` 登记约定。tsc 无错误、`/watchlist` 编译加载正常。
1. 自选股搜索联想加关注爱心按钮（点击关注 / 再点取消） —— 自选股页「股票添加」搜索框的联想结果每行右侧新增长桥爱心关注按钮：灰色=未关注、红色=已关注（沿用 `heart-pop` 弹跳动效），悬停提示 关注 / 取消关注；点击直接加入或移出自选股（已存在记录走静默删除、不存在走搜索添加，含按市场默认券商），不触发行选择；请求期间该行爱心禁用防重复点击。实现：`StockSearch` 新增可选 `followed` / `onToggleFollow` props（其他页面用法不受影响），行结构由整行 button 改为「主内容按钮 + 关注按钮」同级布局，避免嵌套 button 的 hydration 错误；`QuotesView` 传入 records 判断关注态 + `onToggleWatch`/`onAddMatch` 实现切换。tsc 无错误、`/watchlist` 编译加载正常。
1. 详情页爱心改为「关注」语义并真实联动自选股 —— 个股详情头部爱心按钮语义从「收藏」改为「关注」：悬停提示为 关注 / 取消关注（`aria-label` 同步）。逻辑接通真实数据——进入详情时按该股是否在自选股初始化爱心状态（自选股进入即红色已关注），点击爱心调用 `RecordsApp` 新增 `toggleWatch`：取消关注 = 静默 `DELETE /api/records/{id}`（不走删除确认弹窗）并从列表/行情移除，重新关注 = 沿用搜索添加的最小字段（现价取自实时行情、按市场默认券商 美股→长桥证劵 / 港股A股→华泰证劵）POST 重新加入自选股；请求期间按钮防重复点击，失败自动回滚爱心状态。回调链路 `StockDetailView(onToggleFollow) ← QuotesView(onToggleWatch) ← WatchlistView ← RecordsApp`，tsc 无错误、`/watchlist` 详情编译加载正常。
1. 多币种市值弹窗加货币国家图标 + 行悬停动画；收藏爱心换成长桥官方 SVG —— 1) 多币种市值弹窗每行左侧新增货币对应国家/地区图标（用素材库市场图标 `MarketIcon`：美元🇺🇸/港元🇭🇰/人民币🇨🇳/新加坡元🇸🇬/日元🇯🇵/韩元🇰🇷，素材库有自定义市场图标时优先）；`MULTI_CURRENCIES` 增加 `market`/`flag` 字段（[`lib/currency.ts`](lib/currency.ts)，Web/iOS 契约同步）。2) 币种行悬停动画：整行上浮 1px + 浅灰背景 + 轻阴影 + 点击微缩（`transition-all duration-200`），左侧市场图标随行 hover 放大 1.1 倍并轻微 -6° 旋转（`group-hover`，300ms 缓动），选中行保持深灰高亮不响应 hover。3) 个股详情头部收藏爱心替换为长桥官方 SVG 路径（`viewBox 0 0 1136.32 1024`，爱心 + 右侧加号造型）：未收藏灰色空心描边（`strokeWidth 85` 对应 18px 下约 1.3px 视觉描边）、收藏后填充红色 `#ef4444`，保留 `heart-pop` 弹跳 + 六粒红色迸发动效。tsc 无错误、`/watchlist` 详情编译加载正常。
1. 个股详情页改左对齐（长桥风格）+ 收藏改为带动画爱心 —— 1) 个股详情页（`StockDetailView`）由 `mx-auto` 居中改为靠左显示（`w-full max-w-[620px]`，去掉 `mx-auto`），与长桥个股页主内容居左的布局一致，避免宽屏下内容被两边大留白夹在中间。2) 头部收藏按钮由金色五角星改为爱心：未收藏为灰色空心、收藏后填充红色 `#ef4444`，并新增 CSS 动效——点击爱心弹跳（`heart-pop`：放大 1.4 倍回弹）+ 六颗红色微粒向外迸发消散（`heart-burst`，径向 6 个方向），深浅色模式均正常，点击即时反馈。tsc 无错误、`/watchlist` 详情编译加载正常。
1. 个股页规范化：新增 v1 个股详情接口（Web / iOS 共用契约） —— 为后续 iOS App 调用沉淀个股页数据契约：1) 新增 `GET /api/v1/stock-detail?market=&code=` —— 一次请求返回完整实时行情（今开/最高/最低/昨收/量额/市盈率/换手率/总市值）+ 六币种市值（USD/HKD/CNY/SGD/JPY/KRW，本地币种原始值、其余按汇率换算）+ 对 USD 汇率 + 日 K（前复权，最多 320 条）；行情/汇率/K 线三路并行互不阻塞，单路失败只缺对应字段仍返回 `code: 0`；无市值 ETF 等 `marketCap` 返回 `null`。2) 抽取共享日 K 数据层 [`lib/kline.ts`](lib/kline.ts)（`tencentSymbol` / `parseRows` / `fetchDailyKline`，10 分钟缓存），`/api/kline/full` 与 v1 stock-detail 同一解析规则；币种换算抽到 [`lib/currency.ts`](lib/currency.ts)（`MARKET_CURRENCY` / `MULTI_CURRENCIES` / `usdCap` / `multiCurrencyCap`），`useRates` 改为再导出。3) 网页个股详情改走 v1 契约（`StockDetailView` 由 `POST /api/quotes` + `useRates` 改为单次 `/api/v1/stock-detail`），与 iOS 同源。4) [`docs/api-spec.md`](docs/api-spec.md) 新增「6.2 个股详情 Stock Detail」章节（请求/响应示例/字段说明/周月聚合约定）。tsc 无错误（仅历史 index-kline 遗留类型缓存）。
1. 多币种市值弹层：加市场图标 + 行 hover + 位置改为居中弹窗 —— 个股详情-多币种市值弹层优化：1) 标题行加当前股票的市场图标（素材库市场图标，回退国旗）+ 股票代码，一眼可知是哪只股票哪个市场；2) 币种行鼠标划过有浅灰背景（`hover:bg-brand-hover`，深色模式同步），选中行保持深灰高亮；3) 弹层从贴底的全宽 bottom sheet（`items-end` + 圆角仅顶部）改为视口居中弹窗（`items-center` + `p-4` + `max-w-[400px]` + 四角圆角），不再靠左贴底，深浅色模式均正常。tsc 无错误。
1. 自选股移除券商列 + 股票图标修复 + 我的持仓补券商列 —— 1) 自选股（QuotesView）表格移除「券商」列（表头与行内分组标签），券商仅在我的持仓展示；编辑表单的券商字段保留（数据模型统一，自选Fire录仍可设置但表格不展示）。2) 自选股股票图标修复：股票列原误用 `MarketIcon`（市场图标 / 国旗）渲染，改为素材库股票图标（`useAssetIcons.stockIcons` 按 市场:代码 匹配，无图标回退名称首字母圆形），与全站股票图标一致。3) 我的持仓（HoldingsView）持仓明细表格新增「券商」列：显示券商名称 + 素材库券商图标（无图标首字母兜底，未设置显示 —），与编辑表单一致。tsc 无错误、`/watchlist` 与 `/holdings` 编译加载正常。

### 安全修复

1. 全站安全审计与加固（本轮） —— 全量复核认证/授权/会话/CSRF/SQL注入/上传/XSS/SSRF/依赖等安全面并修复：1) 依赖漏洞：`nanoid` 由 3.3.17 升至 3.3.18（高危：size=0 时自定义生成器死循环 DoS），`npm audit --omit=dev` 归零；2) 名人 CIK 强制 10 位数字校验（管理端创建/更新均拦截），防止注入 SEC 查询路径；3) 设置页数据源地址（quoteApiUrl 等 8 个）仅允许 `http(s)://` 开头，杜绝 `file://` 与内网探测类配置；4) 清理全站 `.DS_Store`（public/uploads、data、备份内共 20+ 处），`.gitignore` 已全局排除；5) 复核确认：scrypt 加盐密码 + 恒定时间比较、会话 token SHA-256 摘要落库、Cookie httpOnly + SameSite=Lax + 生产 Secure、Cookie 写请求 Origin/Sec-Fetch-Site 同源校验、登录/注册/改密/行情/搜索/K线/素材搜索全链路 IP+全局双限流、记录/订单/分组按 user_id 隔离无越权、上传扩展名白名单 + 魔数嗅探 + SVG 消毒、SQL 全参数化、CSP/X-Frame-Options/nosniff/HSTS 安全头、管理员接口全部鉴权、生产环境禁用默认 demo 管理员。tsc 无错误、`npm run build` 通过、npm audit 0 漏洞、新校验端到端实测（400 拒绝且不落库）。
1. 全站安全审计与纵深加固 —— 完成认证、权限、数据归属、上传、文件系统、公开接口、出站请求、响应头、依赖与敏感配置审计：1) 会话令牌改为 SHA-256 摘要落库，旧会话首次使用无感迁移；Bearer 显式认证优先于残留 Cookie，Cookie 写请求增加 Origin / Sec-Fetch-Site 同源校验；登录统一执行 scrypt 降低用户名枚举时间差，生产环境禁用仍使用公开默认密码的 demo 管理员，空库首次启动必须通过 `INITIAL_ADMIN_USERNAME` / `INITIAL_ADMIN_PASSWORD` 创建管理员。2) 上传按图片魔数校验真实内容，SVG 统一消毒；名人头像与自动下载图标同步覆盖，远程图标仅允许 CoinGecko / CompaniesMarketCap HTTPS 域名，逐跳校验重定向并限制 2MB，阻断伪装文件、存储型 SVG 与 SSRF。3) 本地素材路径使用 `resolve` + public 根目录边界校验，阻断编码路径穿越；数据库、备份与配置权限收紧为目录 0700 / 敏感文件 0600，`.gitignore` 排除数据库、备份与 `.env`。4) 防止最后一个管理员被降级，记录价格/成本/数量拒绝负数，代码白名单化，批量删除/分组/排序增加去重与数量上限，分组 icon/visible 严格校验。5) 生产响应新增 HSTS，API 统一 `no-store`；npm 锁定 `postcss 8.5.26` / `sharp 0.35.3` / `nanoid 3.3.17`，生产依赖审计由 4 high 降为 0。

### 修复

1. 个股扩展行情与全天走势图修复 —— 修正腾讯换手率/振幅字段映射，新增 EPS TTM、52周高低、市净率、股息率、量比、总/流通股本、静态 PE、股息、均价等真实字段；全天在单日源为空时自动使用五日分钟行情的最新交易日数据。

1. 走势图周期栏简化 —— 移除 3个月，将 1个月改为月K并新增周K；周K/月K基于日线真实聚合 OHLC 与成交量。历史区间增加首尾留白，修复“最大”最左侧日期显示不全。

1. 个股 5 日走势数据修复 —— 原实现错误使用最近 5 根日 K 收盘价，仅有 5 个点；现改为美股五日分钟行情，每个交易日 391 个一分钟点并同步成交量。AAPL 实测返回 1955 点、覆盖 5 个完整交易日，走势粒度与券商五日分时语义一致。

1. 修复生产构建与 v1 行情兼容问题 —— 修复 `/api/v1/index-kline` 非法导出 `INDEX_MAP` 导致 Next.js 生产构建失败；v1 月 K 增加市场白名单并正确支持日股 `jp` 与韩股 `kr` 腾讯符号（不再误走美股分支）；素材库券商 alias 类型补齐，健康检查版本改为自动读取当前版本，避免硬编码停留在 v0.1.4。

1. API 规范整理：文档结构归位 + 分组接口补全示例 + 实现统一 —— 1) [`docs/api-spec.md`](docs/api-spec.md) 结构整理：此前券商模型/示例内容游离在 6.3 之后，现归位到「6.1 券商 Brokers」；「6.3 自选股分组」补全各接口规范——GET 列表 / POST 新建 / PUT 更新（name/icon/visible，icon 空串=清除）/ DELETE / POST reorder / POST records/group-assign 的请求与响应示例、错误码（40001/40901/40101/42901）、图标上传链路与素材库分组图标分类约定，全部走统一 v1 信封 `{ code, message, data }`。2) 实现统一：`POST /api/v1/watch-groups/reorder` 返回 `{ updated }`（原 `{ ok: true }`），`[id]` 路由限流 key 规范化。tsc 无错误、`/api-docs` 实时渲染验证通过。
1. 自选股编辑弹窗「券商」改为「分组」（逻辑理顺） —— 方案 A 后自选股分组已是独立实体（`watchGroupId`），编辑弹窗仍显示「券商」会误导用户。现改为「分组」下拉：列出自定义分组 + 未分组，保存时写 `watchGroupId`；券商字段在自选股编辑中保留原值不再改动（券商归属我的持仓，持仓页编辑仍可改券商）。移除 `GroupSelect` 依赖与 `groupOptions`。逻辑确认：自选股编辑 = 改分组，持仓编辑 = 改券商，职责清晰。tsc 无错误、`/watchlist` 编译加载正常。
1. 修复分组相机点击无反应 + 相机悬停动画 —— 根因：管理分组行整体 `draggable`，HTML5 拖拽会吃掉行内所有按钮（相机/眼睛/铅笔/删除）的点击。修复：`onDragStart` 校验拖动必须由 `.drag-handle`（拖动手柄）发起，非手柄区域直接 `preventDefault`，行内按钮点击恢复正常（眼睛显隐/铅笔重命名/删除一并受益）。相机交互升级为与全站股票图标相机一致：28px 圆形容器（ring 细描边），悬停图标放大 1.1 倍 + ring 高亮 + 半透明遮罩相机淡入（300ms），点击即弹出文件选择；市场分组图标只读。tsc 无错误、`/watchlist` 编译加载正常。
1. 分组相机改为叠加在图标上（悬停出现） —— 按用户反馈：分组管理-我的分组行的相机按钮移除，改为相机直接叠加在分组图标上——悬停图标出现半透明遮罩 + 相机图标（同全站股票图标相机风格），点击即上传；市场分组图标只读无相机。tsc 无错误、`/watchlist` 编译加载正常。
1. 自选股表格隐藏「分组」列（分组筛选已在上方） —— 按用户反馈：自选股表格移除整列「分组」（表头与行内分组标签），分组管理统一由上方筛选条 + 加号分组面板承担；批量「移动到分组」按钮保留（多选后仍可批量分配）。移除 `RowGroupTag` 组件。tsc 无错误、`/watchlist` 编译加载正常。
1. 分组面板补「管理分组 / 新建分组」入口 + 自定义分组显示券商图标 —— 按用户反馈修复分组面板：1) 全部分组网格底部新增操作栏（参考 iOS 关注页分组面板）：左侧「管理分组」齿轮按钮进入管理视图（此前只有管理视图没有入口，导致找不到编辑分组）、右侧「新建分组」加号按钮直达管理视图的新建输入；2) 自定义分组行（长桥证劵 / 华泰证劵等）与筛选条分组胶囊显示对应券商图标（`brokerIcons` 按分组名匹配素材库券商图标，无图标回退灰色标签），与持仓页券商图标一致。`WatchGroupSheet` 新增 `brokerIcons` prop，`QuotesView` 传入并同步在筛选条渲染。tsc 无错误、`/watchlist` 编译加载正常。
1. 返回按钮移入详情头部行（股票图标左侧） —— 按用户反馈修正：返回按钮不再单独占一行（此前在详情上方显得突兀），改为 `StockDetailView` 新增 `onBack` 回调、在头部行内、股票图标左侧渲染圆形浅灰箭头按钮（`-ml-1` 微调视觉对齐，`h-9 w-9` 与股票图标同高），返回行情板后仍正确清理 URL `symbol` 并恢复列表；无 `onBack` 时（如未来前端弹窗用法）自动不渲染。tsc 无错误、`/watchlist` 详情编译加载正常。
1. 个股详情返回按钮改圆形浅灰箭头 + 收藏爱心继续美化 —— 1) 详情页左上角返回按钮改为截图样式：圆形浅灰底（`bg-bg-gray`）+ 深色左箭头，去掉「返回行情板」文字（`aria-label`/`title` 保留无障碍提示），hover 上浮 + 浅灰加深 + 点击微缩（`transition-all duration-200`）。2) 收藏爱心继续美化：长桥爱心由空心描边（复杂路径小尺寸描边发糊）改为实心填充——未收藏为灰色（`currentColor` + `opacity-75`）、收藏后填充红色 `#ef4444`；`viewBox` 裁剪到路径实际包围盒（`49.42 40.42 1086.9 948.55`）使爱心在相同像素下更大更清晰，尺寸由 18px 提到 20px；按钮 hover 时爱心放大 1.1 倍，保留收藏弹跳（`heart-pop`）+ 六粒红色迸发动效。tsc 无错误、`/watchlist` 详情编译加载正常。
1. 个股详情页隐藏顶部大盘指数卡片 —— 自选股（`/watchlist`）点击股票进入个股详情时，顶部的大盘指数卡片（恐惧与贪婪 / 美股 / 港股 / A股）整块隐藏，详情页不再被指数区块挤压、更干净；返回行情板后恢复显示。实现：`QuotesView` 新增 `onDetailChange` 回调（详情开合通知外层），`WatchlistView` 用 `detailOpen` 状态条件渲染指数卡片，并用 URL `?symbol=` 初始化首帧状态（刷新时也不闪现卡片）。tsc 无错误、`/watchlist` 详情与返回均正常。
1. K 线图成交量改为「万」单位并简化 —— 个股详情 K 线 tooltip 的「量」由 M（百万，1 位小数）改为「万」：新增 `fmtVol` 格式化（`v/1e4`，≥100 万取整、小于 100 万保留 1 位小数），如 AAPL 3444万、A 股 600519 2.5万（原 A 股按 M 显示会变成 0.0M，单位错误一并修正），数值更简短直观。tsc 无错误。
1. 修复 K 线图 tooltip 开盘价错位（ECharts 6 分类轴索引混入 value） —— 个股详情 K 线 tooltip 的开盘价显示错误：ECharts 6 对数组数据项会在 `params.value` 最前面附加分类轴索引（如 `[59, 311.45, 313.33, 310.74, 314.81]`），原代码直接按 `[o,c,l,h]` 解构，导致「开」显示序号（59）、「高」显示真实开盘、「低」显示真实收盘、「收」显示真实最低、真实最高丢失。修复为按 `params.dataIndex` 回查聚合后的 `ohlc` / `vols` 数组取值（不依赖 ECharts 内部维度布局），并同步校验数据链路（新浪美股 / 腾讯 A股港股 K 线逐字段与实时行情一致）、蜡烛几何（影线触及高低、实体开收位置正确）。另为 candlestick 系列加 `barMinWidth: 2`：ECharts 6 蜡烛宽度 ≤1.3px 时进入 simple-box 模式只画影线不画实体，小屏 / 150 根 K 线时保证实体与高低点完整可见。补全 `lib/types.ts` `Quote.open` 类型声明（运行数据本就含今开，仅类型缺失）。tsc 仅剩素材库 alias 等历史类型错误，`StockKline` 无错误。
1. 我的持仓股票图标修正为素材库图标 —— 我的持仓（HoldingsView）持仓明细「股票」列名称左侧新增股票图标：与自选股一致，使用素材库股票图标（`useAssetIcons.stockIcons` 按 市场:代码 匹配，无图标回退名称首字母圆形），不再仅以小字 `MarketIcon` 表示市场；名称下方小字保留市场图标（国旗）+ 代码 · 市场标签用于市场标识。tsc 无错误、`/holdings` 编译加载正常。
1. 自选股/我的持仓股票列改为素材库股票图标行样式 —— 自选股（QuotesView）与我的持仓（HoldingsView）的「股票」列统一改为素材库-股票图标行的视觉效果：36px 圆形股票图标（素材库 `stockIcons` 按 市场:代码 匹配，无图标回退名称首字母）+ 右侧两行左对齐文字（第一行名称粗体、第二行代码灰色小字），移除原「名称 + 小字 代码 · 市场标签」与行内市场图标；市场信息仍由表格市场筛选与各列货币体现。与素材库股票图标列表观感一致（英伟达 / NVDA 同款）。tsc 无错误、`/watchlist` 与 `/holdings` 编译加载正常。
1. 我的持仓备注列隐藏、券商移至备注位置 + 序号列标题修正 —— 1) 我的持仓（HoldingsView）持仓明细表格暂时隐藏「备注」列（表头与行内备注单元格移除），「券商」列从股票列后移至原备注列位置，列顺序为：序号 / 股票 / 现价 / 当日盈亏 / 成本价 / 数量 / 市值 / 持仓盈亏 / 盈亏率 / 券商 / 操作，表头与行内 11 列完全对齐；券商单元格样式保留（名称 + 素材库券商图标，未设置显示 —）。2) 我的持仓与自选股（QuotesView）的「#」列表头统一改为「序号」（行内本就显示跨页连续编号），语义更明确。tsc 无错误、`/holdings` 与 `/watchlist` 编译加载正常。
1. 自选股支持个股行内移动至分组 —— 自选股行情板每只股票的操作栏新增独立「移动」下拉：无需勾选批量或打开编辑弹窗，即可将当前个股移入任意自定义分组，也可直接移出分组；复用 `/api/v1/records/group-assign` 接口，保存成功后实时刷新记录与分组数量，未创建自定义分组时操作自动禁用并提示先新建分组。tsc 无错误、`/watchlist` 编译加载正常。
1. API 文档页升级为开发者中心视觉 —— `/api-docs` 阅读与编辑页面统一升级：新增开发者中心头部、文档状态徽章与章节/接口/行数统计；桌面端目录卡片强化层级与滚动体验，移动端新增横向章节导航；正文卡片、标题锚点、引用、代码块、表格悬停和编辑预览统一精致化，并补齐浅色/深色背景与响应式适配。tsc 无错误、生产构建通过。
1. API 文档目录与表格布局继续优化 —— 修正目录树：将 6.1/6.2/6.3 自动归入 6 章节，主章节从 10 个收敛为 7 个，展开后再显示路由与子章节，长标题改为可换行；Markdown 表格新增全宽滚动容器，避免表格被压缩在正文左侧；6.1/6.2/6.3 正文标题降为清晰的子章节层级。tsc 无错误、生产构建与浏览器页面检查通过。
1. API 文档目录增加章节虚线分隔 —— 左侧目录每个主章节增加低对比度虚线分隔，选中态、展开态与深色模式保持一致，提升长目录的阅读节奏。tsc 无错误、`/api-docs` 加载正常。
1. API 文档复制按钮与主题切换优化 —— 代码块复制控件改为圆角方形图标按钮，悬停显示复制提示、点击后切换为「已复制」状态；API 文档顶部新增全站共用的浅色/深色主题切换按钮，目录、正文、代码块与复制控件同步适配。
1. API 文档行内复制悬停指针优化 —— 移除行内 URL / 接口路径代码悬停时系统 copy 指针显示的绿色加号，改为普通指针；虚线提示、点击复制与成功 Toast 保持不变。
1. API 文档接口方法示例取消行内复制 —— `GET /api/...`、`POST /api/...` 等方法 + 路径的接口说明不再显示复制虚线与复制提示；独立 API 路径、完整 URL 与 `Authorization Bearer` 仍可复制。
1. 设置页统一控制台 UI —— 设置页重构为统一的控制中心视觉：新增状态概览头部、版本与角色徽标、记录/导航摘要、统一侧栏与横向导航、玻璃质感卡片层级和深色主题覆盖；保留原有配置逻辑与响应式布局，桌面/移动端均可用。tsc 无错误、生产构建通过。
1. 设置页移除概览摘要卡片 —— 移除设置中心头部的记录、导航项、角色三张摘要卡片，保留核心标题、运行状态、版本徽标与布局切换，减少视觉噪音。
1. 美股 2 倍做多 ETF 图标默认使用主体股票图标（全站兜底） —— 美股 2 倍做多 ETF（RKLX/AAPU/TSLL/SPCH 等）在素材库无自有图标时默认显示主体股票图标：`lib/useAssetIcons.ts` 新增 `LEVERAGED_2X_MAIN` 映射（RKLX→RKLB、AAPU→AAPL、TSLL→TSLA、SPCH→SPCX（SpaceX 已上市，代码 SPCX，素材库已有 SpaceXSPCX.png）、NVDL→NVDA、MSTU→MSTR、CONL→COIN、SSO→SPY、QLD→QQQ、SPUU→SPY），构建 `stockIcons` 时自动兜底，并覆盖常见交易所后缀（.AM/.N/.OQ/.PS/.K，如 SPCH.AM → SPCX）；素材库股票 tab 的 `displayUrl` 同步接入同一兜底，全站（自选股 / 我的持仓 / 素材库 / 名人持仓）统一生效；主体未上市或图标缺失时保持首字母兜底。`AGENTS.md` 写入「美股 2 倍做多 ETF 图标兜底规范」。tsc 无错误、`/watchlist` /holdings /library 编译加载正常。
1. 素材库股票行情补全 + 修复腾讯批量行情失效（全站现价恢复） —— 1) 素材库股票图标「市值/最新价/涨跌幅」补全：新增 `scripts/backfill-quotes.mjs`（含日股 `jp{code}`、韩股 `kr{code}` 前缀支持），为 433 只缺失现价的股票批量回填腾讯行情（成功 413 只，剩余 11 只为停牌 / 冷门股如宇树科技、JP REIT、AXIA_C 等腾讯与东财均无有效行情）；7 只手动添加资产（VOO/IVV/VTI/BRK.B/DJT/TLT/曙光数创）补齐市值（BRK.B 1125 亿 / DJT 28.3 亿 / 曙光数创 125.1 亿为东财实际值，ETF 用当前基金净资产近似）+ 现价 + 涨跌幅，素材库最后一页不再出现空数据。2) 重要修复——腾讯行情接口批量查询已失效（每次请求只返回第一条，实测 US/HK/CN/JP/KR 全部如此），导致全站实时行情（自选股 / 我的持仓 / 财报日历图标价等）只更新第一只：`lib/quotes.ts fetchBatch` 改为并发逐条（并发 6），`fetchQuotes` 与财报、走势等批量调用全部恢复，实测 `/api/quotes` 多市场多只股票正确返回。`AGENTS.md` 记录腾讯批量失效教训：行情批量必须逐条并发。tsc 无错误、`/library` 编译加载正常。
1. K 线组件准备：默认 ECharts + TradingView 备选（双引擎可切换） —— 按选型准备 K 线组件 `components/StockKline.tsx`：默认引擎 ECharts（6.1.0，candlestick + 成交量 + dataZoom 滚轮缩放 + 十字线 tooltip + 日/周/月周期切换 + 深浅色自适应），右上角提供「ECharts / TradingView」引擎切换按钮，TradingView 为 iframe 备选（symbol 自动映射：美股裸代码、HKEX / SSE / SZSE / BSE / TSE / KRX，周期联动，失败可切回）。新增日 K 接口 `/api/kline/full`（10 分钟缓存）：美股走新浪日 K（250 条含量）、港股 / A股走腾讯前复权（320 条）、日股 / 韩股腾讯数据不全（<3 条时前端提示可切 TradingView）。用法：`<StockKline market="US" code="AAPL" name="苹果" />`，待自选股页规划后接入。tsc 无错误。
1. 自选股-我的行情板：个股详情页（moomoo 风格） —— 自选股（QuotesView）每只股票点击进入个股详情弹窗（AppModal xl，moomoo 网页详情布局）：1) 头部——股票名大字 + 代码 + 市场国旗 + 收藏星标，副标题显示行情更新时间，核心区左侧特大现价（红涨绿跌 + 涨跌箭头 + 涨跌额/幅）、右侧 总市值 / 市盈率TTM 指标 + 灰色圆角「双箭头延伸」按钮；2) 点击延伸平滑展开更多指标三列网格（总市值 / 市盈率TTM / 今开 / 今高 / 今低 / 成交量 / 成交额 / 换手率 / 振幅 / 每手 / 历史高低占位），箭头旋转收起；3) K 线区上方 tab 暂时只保留 概览 / 期权 / 财务 / 公司（选中项下方黑色短横线高亮，其余为占位「内容规划中」），概览 tab 显示「代码 实时股价」标题 + `StockKline` 双引擎 K 线。`Quote` 类型扩展成交量 / 成交额 / 市盈率 / 换手率 / 总市值（腾讯 f36/f37/f39/f43/f44，市值 ×1e8 本地货币），详情头部与指标全部真实数据（市盈率 / 市值直接取自行情）。盘后行情行因无数据源暂未展示。tsc 无错误、`/watchlist` 编译加载正常。
1. 个股详情交互规范：后端无感进入视图（URL 同步） —— 确立个股详情交互规范（写入 `AGENTS.md`）：前端（首页 / 门户）个股点击一律用弹窗（`StockDetailView`，moomoo 风格）；后端（自选股 / 我的持仓 / 行情板）个股点击一律**无感进入详情视图**——不弹窗、不整页刷新，当前视图 fade 过渡切换为详情页 + 左上角「返回行情板」，并同步 URL（`?symbol=市场:代码`，刷新 / 前进后退保持）。后端自选股详情已按此改造：点击股票行 → `pushState` 写入 symbol 并切换视图，返回按钮 `replaceState` 清除 symbol，`popstate` 监听恢复 / 退出；详情行情用实时 quote 引用（`quotes[id]`），不是打开时的快照。tsc 无错误、`/watchlist` 列表与详情 URL 均编译加载正常。
1. 个股详情页改为 2:1 左右分栏（限宽居中） —— 个股详情页不再全宽：整体 `max-w-1100px` 居中；桌面端左右 2:1 分栏——左 2/3 为 K 线主卡（概览/期权/财务/公司 tab + 双引擎 K 线 430px），右 1/3 为现价/指标卡（名称 + 代码 + 国旗 + 收藏、更新时间、特大现价 + 涨跌箭头、总市值/市盈率TTM + 双箭头延伸按钮，延伸指标网格改为两列适配窄栏）；移动端右栏（现价/指标）置顶、K 线随后（order 切换）。tsc 无错误、详情 URL 编译加载正常。
1. 个股详情页恢复单栏布局（宽度缩为原来一半） —— 按用户反馈撤销 2:1 分栏：恢复原来的单栏上下布局（头部 → 现价 + 指标 + 双箭头延伸 → K 线区 tab），整体宽度缩为原来一半并居中（`max-w-620px`），延伸指标网格保持两列。tsc 无错误、详情 URL 编译加载正常。
1. 详情页头部：股票名称前加股票图标，市场图标移到收藏左边 —— 个股详情页头部调整为：股票图标（素材库 `stockIcons` 按 市场:代码 匹配，36px 圆形，无图标回退名称首字母）+ 股票名 + 代码，右侧 `ml-auto` 依次为 市场图标（国旗）+ 收藏星标。tsc 无错误、详情 URL 编译加载正常。
1. 修复详情页总市值/市盈率与 K 线成交量不显示 —— 1) 总市值 / 市盈率TTM：详情页此前依赖列表传入的 quotes（可能未含新增字段），现改为详情页独立拉取单只实时行情（`POST /api/quotes`，含 volume/amount/pe/turnover/marketCap 完整字段），并补全 `lib/types.ts` 的 `Quote` 字段声明，实测 AAPL pe 35.93 / 市值 4.57 万亿正常显示；2) K 线成交量：修复 tooltip 成交量 NaN（bar value 是数字，误取 `value[1]`），并把日 K 数据量从 400 根降到 250 根 + 默认只显示最近约 150 根（此前每根蜡烛/成交量柱不足 1px 视觉上消失），实测 250 根全部带成交量。tsc 无错误、详情 URL 编译加载正常。
1. 详情页指标区改 moomoo 4 列网格 + 总市值简化 + 多币种市值弹层 —— 1) 指标区改为 moomoo 样式 4 列网格（标签上 / 数值下）：最高·最低 / 今开·昨收 / 换手率·成交额 / 市盈率TTM·总市值{币种}，移除原延伸按钮与延伸网格；2) 新增 `fmtCap` 大数简化（万亿 / 亿 / 万，如 4.57 万亿、306.17 亿），总市值、成交额不再显示超长数字；3) 总市值标签右侧三点按钮 → 底部弹出「多币种市值」弹层：美元 / 港元 / 人民币 / 新加坡元 / 日元 / 韩元 各币种换算市值 + 汇率小字（1 HKD = 0.1274 USD，走 `useRates` / `FALLBACK_RATES`），当前币种深灰高亮，底部「当天非实时汇率，仅供参考」提示 + 白色「知道了」按钮。导出 `MARKET_CURRENCY` 供复用。tsc 无错误、详情 URL 编译加载正常。

---

## v0.1.4 · 2026-08-08

> 名人持仓 F3：F1 下半身 + F2 上半身融合版——透明头像拆双层 mask，额头从洞里探出压住上方扇形（F2 效果），领带下方被圆环环带遮挡（F1 效果），深浅色模式均正常。

### 新功能

1. 名人持仓 F3：F1 下半身 + F2 上半身融合版头像 —— 扇形圆环人物头像融合两版优点：透明头像拆成双层渲染——下层在圆环之下、mask 只显示身体下半部分（`transparent 40% → rgba(.4) 50% → #000 58% → 100%`），领带 / 西装下方被圆环环带遮挡、不外突（F1 效果）；上层在圆环之上、mask 只显示头部区域（`#000 0-50% → rgba(.8) 54% → rgba(.3) 58% → transparent 62%`），额头 / 头顶从洞里探出压住上方扇形（F2 效果）。头像尺寸 0.74、上移 6px（悬停 8px），上下层同位移同过渡无拼接痕迹；非透明头像维持单层圆形裁剪。已备份 `docs/celebs-ring/celebs-ring-F3.tsx` 并在 `AGENTS.md` 登记恢复方法；浅色 / 深色模式均实测正常（头顶明显探出压住扇形、衣领以下被圆环包围、无黑圈发暗）。
2. kline v1 接口升级：带日期的月收盘（供 iOS 资产 / 盈利历史曲线）—— `GET /api/v1/kline` 新增 `months` 字段（与 `closes` 一一对应的 `YYYY-MM` 月份数组，兼容旧 `closes` 调用）；数据源统一为东方财富月 K（美股尝试 105 纳斯达克 / 106 纽交所 / 107 美交所市场代码、A股按代码分沪 `1.xxx` / 深 `0.xxx`、港股 `116.xxx`，约 2020 年至今）；东方财富不可用时降级：A股 → 新浪日线转月线、港股 → 腾讯月 K、美股 → 腾讯兜底。
3. 新增 `/api/v1/index-kline` 指数月 K（供 iOS 盈利对比曲线）—— `GET /api/v1/index-kline?key=spx` 返回 8 个基准指数月收盘（`closes` + `months`）：标普500 `spx` / 纳斯达克 `ndx` / 恒生指数 `hsi` / 上证指数 `sse` / 深证成指 `szse` / 日经225 `n225` / 韩国KOSPI `ks11` / 富时新加坡海峡 `sti`；数据源东财优先，失败按腾讯 / 雅虎兜底，10 分钟缓存。

### 修复

1. 修复圆环扇区划不进去（谷歌 / 亚马逊等小扇区） —— 1) 头像层 pointer-events 拦截：F3 上层头像 mask 只影响视觉、不影响鼠标命中，整个头像方块盖住环带内缘一圈导致左上半圆扇区从内缘划过时无法 hover——修复为三层头像全部 `pointer-events-none`，头像上浮改由圆环容器统一判定（鼠标在中心洞里才触发）；2) 基础环无事件：持仓权重悬殊（如巴菲特 Apple 占大头）时环带大部分露出无事件的基础底色环，鼠标划过没有可命中的扇区——修复为扇区 hover 不再依赖每个 path 的事件，改为容器级几何判定：鼠标在环带任意位置（含基础环露出的细线区、内缘）按角度映射到对应扇区，悬停弹出与提示全部恢复正常。实测：左上半圆 105°/135°/200° 均正确触发对应扇区弹出，洞里头像上浮、环外复位，佩洛西 / 巴菲特数据均正常。
1. 「其他持仓」聚合段不参与最大持仓突出判定 —— 最大持仓段（向外突出 + 高亮）的判定改为排除「其他持仓」聚合段：聚合权重可能略高于真实最大持仓（如木头姐 ARK：其他持仓 19.1% > 特斯拉 19%），此前会抢走突出效果导致特斯拉不突出。修复后 `maxIdx` 只在真实持仓中取最大，实测木头姐圆环特斯拉扇区正确向外突出、其他持仓保持内侧不突出。
1. 名人持仓加密货币/贵金属图标走素材库（如特朗普持仓 BTC） —— `CelebLogo` 组件此前只查股票类型素材（`stockIcons`：市场:代码），素材库里 BTC 是 crypto 类型（`assetIcons`：按代码映射），导致特朗普持仓的比特币 ETF（code=BTC, market=US）匹配不到图标、走 CDN 兜底失败后显示首字母。修复：`CelebLogo` 增加 `assetIcons` 兜底（加密货币/贵金属按代码查询），实测特朗普持仓 BTC 行正确显示素材库比特币图标 `/uploads/asset/crypto/1785666703934-8a7ad4bd.svg`，圆环扇区 Logo 同步受益。
1. 名人持仓明细股票图标悬停相机上传（自动同步素材库） —— 名人持仓展示页的持仓明细每行股票图标新增悬停相机上传（仅管理员可传，非管理员只读）：上传后经 `/api/upload` 保存文件、自动 `POST /api/assets` 注册到素材库（type=stock, 市场:代码，`upsert` 幂等不堆积），再触发 `fire:assets-updated` 全局刷新——该股票在名人持仓 / 圆环 / 素材库全站生效，无需重复上传。实测：巴菲特持仓明细 8 行均有「上传 XX 图标（全站生效）」按钮，悬停相机遮罩淡入正常。
1. API 规范化 v1（面向 Swift / Android 移动端 + 全站） —— 新增统一 API 规范层 `/api/v1/**`（17 个路由）与规范文档 [`docs/api-spec.md`](docs/api-spec.md)：1) 统一响应信封 `{ code, message, data }`，错误码分段（40001 参数 / 40101 未登录 / 40103 密码错误 / 40301 权限 / 40401 不存在 / 42901 限流 / 50002 上游失败）；2) 认证支持 `Authorization: Bearer <token>`（登录返回 token，`getAuthUser` 兼容 Cookie 与 Bearer 双通道）；3) 分页统一 `?page=&pageSize=`，响应 `meta` 带 total；4) 新增 `/api/health` 健康检查、`/api/v1/overview` 资产总览（成本/市值/盈亏/市场分布）；5) v1 覆盖：认证、records CRUD、quotes/charts/kline/rates/search/indices/earnings、celebs(+详情+收益日线)、assets 分页/注册/改名/删除、upload、settings/public；6) 上传核心逻辑抽离 `lib/upload.ts`（SVG 消毒 / 文件落盘 / 头像替换），旧接口与新 v1 共用，冒烟 82/82 通过、旧接口零回归。
1. 设置-API 开发接口书图标链接 + /api-docs 文档页 —— 设置 → API 开发接口子页标题右侧与左侧竖排导航项右侧新增书本图标，点击新开 `/api-docs` 页面；新增 `/api-docs` 路由实时渲染 `docs/api-spec.md`（`lib/markdown.ts` 轻量 Markdown 渲染：标题/表格/代码块/列表/引用/行内代码/链接，内容先转义防 XSS），文档单一数据源、改动即同步，深浅色模式适配，顶部带「返回设置」。
1. API 文档页 Markdown 双模式编辑（分屏预览）+ 可折叠目录 —— `/api-docs` 升级为双模式：1) 阅读模式——左侧目录大纲（按一级章节分组，子项自动折叠，滚动自动展开当前章节、点击一级手动展开/收起、当前章节高亮），右上角「编辑文档」按钮（仅管理员显示）；2) 编辑模式——Obsidian 风格左右分屏：左 Markdown 源码编辑（深色等宽字体）、右实时预览（随输入即时渲染），顶部保存/取消（保存写回 `docs/api-spec.md` 经 `/api/api-docs` POST 接口，仅管理员、固定白名单路径、512KB 上限）。实测：初始目录 7 项全折叠、点击「路由清单」展开 6 子项、高亮跟随滚动、编辑模式 textarea+预览+保存均正常。

### 修复

1. 取消「苹果蓝」全局配色规范 —— 按用户要求取消 `AGENTS.md` 中的「苹果色块标准」章节：品牌色不再强制苹果蓝 `#0071e3`，按钮/胶囊 hover 的苹果「购买」风格（只改背景不改边框）与旧青色禁令一并解除；保留与颜色无关的独立约定——焦点无轮廓无光晕（白框问题历史修复）、深浅色适配需补 dark 覆盖、素材库图标全局缓存机制；「苹果风格开关（Toggle）」规范（绿/灰，名人管理为基准）不受影响继续生效。现有代码仍使用 brand=#0071e3，配色体系待后续统一调整。
1. 全站苹果蓝改为浅灰边框 + 白色块状 + hover 浅灰动画 —— 全站配色体系从苹果蓝切换为中性色：1) 品牌色板改为灰（brand.DEFAULT `#0071e3→#6b7280`、hover→`#e9ebee` 浅灰、light→`#f1f3f5`、deep→`#3f4652`）；2) 87 处实心蓝块改为「白底 + 浅灰边框 + 深色文字」（`border-edge-strong bg-white text-ink-2`，深色模式 `dark:bg-[#1c1c1e]`），含主按钮、tab/分页选中态、徽标、加号图标等；3) hover 统一为明显浅灰背景 `hover:bg-brand-hover`（浅色 #e9ebee、深色 `.dark` 覆盖 #262c37）+ `transition-all` 上浮/按压动画，替换原蓝色 hover（`hover:bg-brand-hover hover:text-white` / `active:#006EDB` 全部清除）；4) 蓝色阴影、输入框 focus 蓝光环、SVG `#0071e3` 图标全部改为中性灰/currentColor，全站蓝色残留清零；5) `AGENTS.md` 登记「中性色按钮标准」。实测：设置页保存按钮浅色 hover 浅灰背景、深色模式半透明白、transition all 动画正常，冒烟 82/82 通过。
1. 胶囊/筛选按钮 hover 改为黑色背景白字（浅色模式） —— 浅色模式下胶囊按钮（全部/美股/港股/A股筛选、分页页码与上一页/下一页、首页导航 tab、财报日历市场切换、附件管理分类等）hover 由浅灰改为纯黑背景 + 白色文字，选中态胶囊 hover 同步黑色（鼠标移开恢复白底选中）；`seg-active` 全局类补充 `.seg-active:hover` 黑色（深色模式保持半透明白）。实测：首页「全部」胶囊 hover 背景 rgb(0,0,0)、文字白色、圆角胶囊，与周围白底胶囊区分强烈，冒烟 82/82 通过。
1. 胶囊 hover 由黑色调整为淡黄色 #FFE38B —— 按用户参考色块将胶囊/筛选按钮 hover 从纯黑改为淡黄 `#FFE38B`（rgb 255,227,139）+ 黑色文字，覆盖全部/美股/港股/A股筛选、分页、首页导航 tab、财报日历、附件管理分类及 `seg-active` 选中态 hover（深色模式保持半透明白）。实测：首页「全部」胶囊 hover 背景 rgb(255,227,139)、文字黑色，与参考色块完全一致，视觉柔和且区分明显，冒烟 82/82 通过。
1. 胶囊 hover 再调整为橙色 #FF9828 —— 按用户参考图（暖色环形图，红色箭头指向橙色弧段）将胶囊/筛选按钮 hover 从淡黄改为橙色 `#FF9828`（rgb 255,152,40）+ 黑色文字，覆盖全部市场筛选、分页、首页导航 tab、财报日历、附件管理分类及 `seg-active` 选中态 hover（深色模式保持半透明白）。实测：首页「全部」胶囊 hover 背景 rgb(255,152,40)、黑色文字清晰，与参考图橙色一致、区分明显，冒烟 82/82 通过。
1. 财报日历市场/市值/类型/时段胶囊补橙色 hover —— 财报日历的市场切换（美股/港股/A股/日股/韩股）与 `PillGroup`（市值/类型/时段）未选中胶囊此前只有 `hover:text-ink`（文字变色无背景），鼠标划过无橙色效果。修复：未选中分支统一补 `hover:bg-[#FF9828] hover:text-black`，与全站胶囊橙色 hover 一致（选中态 `seg-active` hover 橙色已生效）。全站排查无其他同类遗漏；实测港股按钮 hover 背景 rgb(255,152,40)、黑字，冒烟 82/82 通过。
1. api-docs 换成 GitHub 风格渲染 —— API 规范文档页（含编辑模式实时预览）改为 GitHub Flavored Markdown 渲染风格：`globals.css` 新增 `.markdown-body` 规范（浅色：白底深灰字、h1/h2 下边框线、表格完整边框 + 灰色表头 + 斑马纹、代码块 `#f6f8fa` 浅灰背景 + 边框、引用左侧灰条、链接 `#0969da`；深色：GitHub Dark 配色 `#0d1117`/`#161b22`/`#30363d`/`#4493f8`）；`lib/markdown.ts` 渲染器改为输出纯语义 HTML（移除 Tailwind 色类，样式统一交给 `.markdown-body`）；阅读与编辑预览容器套用 `markdown-body`，深色模式背景对齐 GitHub Dark。实测浅色/深色均符合 GitHub README 渲染风格。
1. iOS App（SwiftUI）MVP 骨架 —— 新增 `ios/` 目录的 SwiftUI iOS App 骨架（技术栈：Swift 5.9 + SwiftUI + Async/Await + URLSession，对接后端 `/api/v1` 统一信封 + Bearer token）：XcodeGen 工程配置（`project.yml`）、`Info.plist`（ATS 开放局域网 HTTP）、Codable 模型（用户/总览/持仓/名人）、`APIClient`（登录 / 总览 / 持仓列表 / 登出）、登录页 + 资产总览 + 持仓列表三页、`AuthStore` 全局登录态；`ios/README.md` 含构建步骤（`brew install xcodegen` → `xcode-select` 切换 → `xcodegen generate`）。全部 Swift 文件语法检查通过；等待本机安装完整 Xcode 后 `xcodebuild` 构建到模拟器验证。
1. Fire iOS 工程落地 + App 图标 + 登录解码修复 —— 1) iOS 项目独立为「Fire ios」目录（软件名 Fire：工程名 / Bundle ID `com.fire.app` / 显示名 / 登录页标题全部改为 Fire）；安装完整 Xcode 26.6 + iOS 26.5 模拟器运行时，`xcodebuild` 构建通过并在 iPhone 17 模拟器运行（首启黑屏为模拟器显示缓存问题，重启模拟器解决；XcodeGen info 需用 properties 生成 Info.plist，缺失 UILaunchScreen 导致黑屏已修复）；2) 极简 App 图标（1024px，深蓝黑底 + 橙色 `#FF9828` 上升折线 + 白色终点圆点，无文字）接入 `Assets.xcassets`，actool 编译通过、主屏显示正常；3) 登录显示「HTTP 200」修复：后端 v1 登录只返回 user.id/username，Swift `User` 模型必填字段导致 Codable 解码失败，改为可选字段；macOS 编译网络层+模型真实调后端验证：登录 / `me()` / `overview()` 均解码成功（5 持仓、总市值 187381.5）。
1. 项目目录重组：fire-web / fire-ios 独立 —— 为让项目与对话更清晰，将根目录拆分为两个独立子项目：原 `fire` → `fire-web`（Web 全栈，662M 含 node_modules/data/素材/备份整体迁移，数据库与 1262 个素材完好，服务在 `fire-web` 下重启、端口 8124 正常）；原「Fire ios」→ `fire-ios`（SwiftUI，README 构建路径同步更新）。清理根目录空 `package-lock.json` 残留与 `.DS_Store`；新增根 README（两个子项目边界与常用命令）、`fire-web/README`（关键位置指引），约定 Web 改动只进 `fire-web`、iOS 改动只进 `fire-ios`。
1. 登录页改为长桥风格（左右分栏品牌卡） —— 登录页 `/login` 重构为长桥（Longbridge）风格：940px 大圆角卡片左右分栏——左侧深色品牌区（网格纹理背景、Fire Logo 橙色上升折线 + 标语「记录投资点滴」、特性列表、底部橙色折线装饰），右侧白色表单区（大字标题「登录」、登录/注册文字 Tab 橙色下划线选中、48px 输入框、黑色实心登录按钮、无账号去注册入口）；移动端隐藏左栏、顶部显示 Fire 品牌；深浅色模式均协调（深色下登录按钮反色白底黑字，符合 iOS 最佳实践），注册开关关闭时仅显示登录、开启时显示 Tab 与去注册。冒烟 82/82 通过。
1. 素材库股票图标补齐（记录漏收 + 图标获取三处根因修复） —— 修复素材库股票不全问题，三处根因：1) 记录漏收——东财 clist 翻页遇到 f20 市值缺失/解析失败（返回 0）时被误判为低于门槛提前截断，导致 A股缺 8 只、港股缺 5 只（如中国移动/海光信息等大票）；修复为缺失行跳过不截断、翻页上限 10→30，重新同步后 CN 409→418、HK 243→248；2) 图标错配——微牛搜索股票代码可能与指数冲突（000858 命中 CSI 500 指数、000001 命中 SSE 指数），精确匹配未限定 `template=stock` 导致大票图标缺失（五粮液）；修复为精确匹配必须 `template==='stock'`，代码搜不到时用中文名兜底；3) 图标被拒——微牛部分图标 CDN 返回 `application/octet-stream`（非 `image/*`），`downloadIcon` 误拒（中国移动等）；修复为接受 octet-stream 并按文件魔数校验/推断扩展名。另新增「仅补图标」模式（`POST /api/assets/sync` body `{ mode: 'icons' }`，只对 url 为空的存量记录重试下载，快速补漏）。实测：五粮液/中国移动等大票图标补齐，素材库 A股列表 46/46 图标加载成功、0 破损；剩余 160 只为微牛/parqet 确实无图标的冷门股（覆盖率约 88%），可后续加备用图标源。
1. 素材库股票数量扩容（美股 100 亿 / 港股·A股 150 亿 / 日韩前 300） —— 按用户决策扩大素材库股票覆盖：市值门槛下调（美股 300 亿→100 亿美元、港股/A股 500 亿→150 亿本地货币）、日韩由前 100 扩到前 300（companiesmarketcap 翻页拉取 + 代码去重，韩股受源收录上限为 162 只）。同步优化：已有图标的股票复用本地 URL 不重复下载，全量同步显著加快。实测总股票 3259 只（美股 1033 / A股 1284 / 港股 480 / 日股 300 / 韩股 162）。图标补漏遇到微牛搜索接口对该 IP 的持续限流（空结果风控）：为 `searchWebull` 增加 4 次指数退避重试、同步/补漏并发降到 2-3，并新增独立补漏脚本 `scripts/backfill-icons.mjs`（串行慢速、绕开 dev server 后台任务，`node scripts/backfill-icons.mjs [N]` 分段执行）；大票（五粮液/中国移动/平安银行/中国人保等）图标已补齐，剩余 345 只中小市值图标待微牛限流恢复后由脚本继续补（当前有图标 2914/3259，覆盖率 89.4%）。
1. 素材库美股手动添加股票市场列为空修复 —— 手动添加的美股（JPM / BRK_B / BRK_A 等，source=manual）board 为空，素材库「市场」列显示 —。修复：1) 为现有 9 只手动美股硬编码补精确归属市场（JPM/BRK/VOO/IVV/VTI/TLT → 纽交所、DJT → 纳斯达克）；2) 前端 `boardFallback` 兜底：美股 board 为空时显示「美股」（避免再出现空列，精确交易所待后台同步/backfill 补齐）。实测素材库美股列表市场列全部有值（TSM 纽交所、NVDA/AAPL 纳斯达克，无空值）。
1. 素材库市场/加密货币/贵金属统一为股票式表格展示 —— 素材库 market / crypto / metal 三个 tab 由管理型输入行改为与「股票图标」一致的微牛表格并简化：1) 市场 tab 精简为三列（序号 / 市场 / 操作），图标合入市场栏（名称左侧 36px 圆形图标悬停相机上传，与股票图标同尺寸），保留恢复默认；2) 加密货币 / 贵金属 tab 与股票图标完全一致：六列（序号 / 代码名称 / 市值 / 最新价 / 涨跌幅 / 操作），图标 36px 悬停相机上传，市值 / 价格 / 涨跌幅从 assets 数据填充（手动添加暂无数据时显示 —，后续同步可填充），保留恢复默认占位（₿ / 金）；3) 行悬停浅灰、深浅色适配。实测：市场 tab 三列简洁、图标 36px；crypto tab 六列齐全、风格与股票一致；冒烟 82/82 通过。
1. 素材库股票名称/代码显示顺序反转 + 市场图标上传修复 —— 1) 素材库股票图标与加密货币/贵金属的「代码 / 名称」列显示顺序反转：名称在前（粗体）、代码在后（灰色小字），与用户习惯一致（实测股票行：英伟达 / NVDA）；2) 修复市场图标点相机上传无反应：原 onClick 依赖 `fileRefs` 间接引用（ref 回调在 React 重渲染下可能未及时绑定导致 `click()` 静默失效），改为 DOM 就近查找（`e.currentTarget.closest("div")` 内 `querySelector input[type=file]`）触发，不依赖 ref 引用；market / crypto / metal 三处图标上传统一修复，类型断言 HTMLInputElement。冒烟 82/82 通过。
1. 素材库市场图标增加全球主要市场 + 移除恢复默认 —— 1) 市场图标 tab 由 5 个市场扩展为 14 个全球主要市场：美股/港股/A股/日股/韩股/新加坡/英国/德国/法国/澳大利亚/加拿大/印度/台湾/巴西（`MARKET_META` 补全名称与国旗，flagcdn 自动圆形国旗，UK 映射 gb 修复英国国旗 broken）；2) 修复 SG 显示韩国国旗 bug：SG 自定义图标记录误存了韩国图（与 KR 同 URL），删除该错误记录恢复新加坡默认国旗；3) 按用户要求移除 market / crypto / metal 三个 tab 的「恢复默认」按钮与操作列（market 精简为 序号/市场 两列，crypto/metal 为 序号/代码名称/市值/最新价/涨跌幅 五列），后续修改直接再次上传覆盖；4) 市场上传点击修复经 CDP 实测 `input.click` 触发成功（DOM 就近查找生效）。冒烟 82/82 通过。
1. 素材库市场筛选折叠为地球仪下拉 + 市值汇率补全 + 上传覆盖修复 —— 1) 股票图标 tab 市场筛选由 14 个胶囊改为默认显示前 5 个市场 + 地球仪圆形按钮，点击弹出下拉弹层（参考左图：圆角卡片、「选择市场」标题、国旗 + 名称垂直列表、选中深灰高亮 + 对勾、悬停浅灰、点击外部关闭），折叠的 9 个市场收纳进弹层，筛选栏不再拥挤；2) 市值货币转换补全：`MARKET_CURRENCY` 增加 SG/UK/DE/FR/AU/CA/IN/TW/BR → SGD/GBP/EUR/AUD/CAD/INR/TWD/BRL，服务端 `/api/rates` 拉取新增币种，`FALLBACK_RATES` 补兜底汇率，新增市场市值不再按 1:1 误算；3) 市场上传「成功却没更新」根因修复：`upsertAsset` 对 `source=manual` 的记录不更新 URL（防同步覆盖手动图标），但手动再次上传同样被挡住；`POST /api/assets` 带 id 时改为强制更新 url（与 name 一起），市场/加密货币/贵金属再次上传立即生效并保留 toast 提示。冒烟 82/82 通过。
1. 素材与头像命名规范 + 货币换算规范 + 误删恢复 —— 1) 素材文件命名规范（写入 `AGENTS.md`）：市场图标「中文名+市场码」（美股US.svg）、加密货币/贵金属「中文名+代码」（比特币BTC.svg）、股票「中文名+代码」（苹果AAPL.png），由 `lib/upload.ts` 的 `assetFilename` 统一生成（前端上传带 name/code/market）；历史随机名文件按规范重命名并更新 `assets.url`。2) 头像命名规范：按「登录名(UID编号)」（`Admin(UID1).jpeg`），`saveUpload` 头像分支统一生成，现有头像已重命名。3) 货币换算规范（写入 `AGENTS.md`）：新增市场必须同步 `MARKET_CURRENCY` / `FALLBACK_RATES` / `/api/rates` 币种，市值统一 cap ÷ 汇率。4) 事故恢复：清理脚本路径前缀 bug（`/public/uploads` vs `/uploads`）误删素材文件，已从 `data/backups/fire-20260808-003024` 备份恢复——market 5/5、crypto 39/39、metal 3/3 全部恢复，stock 备份内 1168 只旧图标恢复；剩余 1747 只为扩容新增股票图标，待微牛限流恢复后由 `backfill-icons.mjs` 补；`AGENTS.md` 补充「素材清理注意事项」（必须用 `cleanupOrphanFiles`、URL 先 decode、路径前缀 `/uploads`）防止再犯。
1. 头像命名改为 Admin(UID1)（去冒号）+ 修复扩展名缺小数点 —— 1) 头像命名由「登录名(UID:编号)」改为「登录名(UID编号)」（`Admin(UID:1)` → `Admin(UID1)`）：去掉冒号分隔，避免上传清洗规则把冒号转成横线（`demo(UID-2)`）导致命名不一致；`AGENTS.md` 头像命名规范同步更新为「登录名(UID编号)」。2) 修复头像上传扩展名缺小数点 bug：avatar 分支此前 `${stem}${ext}` 拼接少了点号（生成 `demo(UID-2)png` 这类无扩展名分隔的文件），改为 `${stem}.${ext}`。3) 现有文件与数据库同步：Admin 头像文件与 `users.avatar` 已更新为 `/uploads/avatar/Admin(UID1).jpeg`，测试残留文件按新规范重命名为 `demo(UID2).png`。
1. 素材文件命名全量规范化（market/crypto/metal/stock）+ 名称数据修复 —— 1) 新增项目内工具 `scripts/rename-assets.mjs`（支持 `--dry-run`，只 `fs.rename` 不删除、URL 先 decode、路径前缀 `/uploads`，防误删），把全部存量素材按 `AGENTS.md` 命名规范迁移：market 5 个时间戳随机名 → 美股US.svg/港股HK.svg/A股CN.svg/日股JP.svg/韩股KR.svg（name 同步为中文标签）；crypto 39 个 → 名称+代码（比特币BTC.svg、AaveAAVE.svg 等）；metal 3 个 → 黄金GOLD.png/白银SILVER.svg/铂金PLAT.svg；stock 各市场 73 个历史时间戳文件 → 名称+代码（苹果AAPL.png、伯克希尔BRK.B.png 等），0 冲突。2) 名称数据修复：28 行 HTML 实体解码（MS&amp;AD → MS&AD，修复显示与文件名）；13 个 A 股 XD/C 前缀名修正为规范全名（XD新华保→新华保险、XD上汽集→上汽集团、C长鑫→长鑫科技、C嘉立创→嘉立创 等，无图标行仅修正名称）；6 条未编码 URL 规范化（encodeURIComponent 存库）；2 条缺扩展名 URL 修复（平安银行000001png → 平安银行000001.png）。3) 验证：素材磁盘 1215 文件与数据库完全对应、0 素材孤儿文件（清理 2 个 .DS_Store）、重命名后 URL 全部 200、tsc 无错误、冒烟 82/82 通过；剩余 1747 只股票图标仍缺（微牛限流，待 `backfill-icons.mjs` 回填，回填脚本已按规范命名）。
1. 素材库市场筛选「全部」图标更换 + 「更多市场」弹层重定位美化 —— 1) 「全部」按钮图标由地球改为九宫格（4 个圆角方块），与「更多市场」地球圆形按钮区分，不再同形重复；2) 「更多市场」下拉弹层原为相对整个筛选容器 `right-0` 定位，导致点击后出现在容器最右缘、离按钮很远——改为包裹在按钮的 `relative` 容器内、右对齐按钮展开（面板向左延伸，空间充裕不会溢出屏幕），并新增：指向按钮的小三角、`popover-in` 淡入缩放动画、标题「更多市场」+ 折叠数量徽标、打开时按钮高亮为橙色，选中态与悬停样式保留。tsc 无错误、`/library` 页面编译加载正常。
1. 素材库市场图标名称可编辑 + 加密货币/贵金属行情补全 —— 1) 市场图标 tab：每行新增铅笔按钮编辑市场名称（行内编辑 Enter 保存 / Esc 取消），保存写入 `settings.marketLabels` 全局生效（首页标签、持仓市场 chips 同步）并同步素材库 `assets.name`；行布局恢复为与加密货币 tab 一致的自然样式——序号 + 图标 + 名称/市场码两行左对齐（图标在文字左侧），铅笔保留在名称后。2) 加密货币 / 贵金属 市值 / 最新价 / 涨跌幅补全：新增 `lib/assetQuotes.ts`——39 个加密货币走 CoinGecko `simple/price`（含市值 / 现价 / 24h 涨跌幅，出站走 `proxyFetch` 代理优先），贵金属 GOLD/SILVER/PLAT 走 companiesmarketcap 全球资产榜，行情缓存到内存 + `data/asset-quotes-cache.json`（crypto 5 分钟 / metal 6 小时），`GET /api/assets` 读取时合并 + 过期后台刷新不阻塞请求；crypto / metal tab 数据未就绪时 2.2 秒静默重取一次（无骨架闪烁）。3) 验证：39 币种 + 3 贵金属实测返回真实行情（如 BTC、ETH、黄金 `$4401` / 白银 `$63.8` / 铂金 `$1757`），tsc 无错误、冒烟 82/82 通过。
1. 加密货币/贵金属 市值、最新价、涨跌幅 排序 —— 素材库 crypto / metal 两个 tab 的 市值 / 最新价 / 涨跌幅 表头改为可点击排序按钮（复用股票图标 tab 的排序机制：点击切换升/降序，当前排序列高亮 + 箭头指示，无数据行排最后），排序后序号跟随当前顺序重新编号，排序状态同步到 URL（`?sort=&dir=`），刷新 / 分享保持。tsc 无错误、`/library` 页面编译加载正常。
1. 素材库新增钯金（贵金属补全四大主流） —— 贵金属由 3 种补全为 4 种主流产品：新增 钯金 PALLAD（黄金 / 白银 / 铂金 / 钯金 四大贵金属齐全）。图标从 companiesmarketcap 下载并本地化保存（`public/uploads/asset/metal/钯金PALLAD.png`，符合命名规范），`assets` 表新增 `metal:PALLAD` 记录，行情走既有 CMC 全球资产榜数据源（实测 现价 `$1383` / 市值 `$2420 亿` / 24h `+0.41%`）；前端 `BASE_ASSETS` 同步加入，贵金属 tab 直接展示。铑 / 铱 / 钌 / 锇 等小众贵金属暂无免费行情源，如需后续接入单独处理。tsc 无错误、页面编译加载正常。
1. 素材库「搜索即添加」重塑 + 名称/代码列序修正 —— 1) 移除底部笨重的「新增股票图标」表单（市场下拉 + 代码/名称/图片链接输入 + 上传/保存），改为搜索即添加：在搜索框输入后如素材库无匹配，空态显示「未找到 XX」+「+ 添加」按钮，点击拉取腾讯行情源联想结果（名称 / 代码 / 市场），候选列表逐条「添加」；选中后新增服务端接口 `POST /api/assets/add-by-search` 自动解析并尽力下载本地图标（复用 `stockSync.resolveIcon`，微牛 / parqet，失败留空前端首字母兜底），写入素材库（source=manual）并刷新。实测 BYND「别样肉客」搜索 → 添加 → 图标自动下载为 `别样肉客BYND.png`（命名规范）全流程通过。2) 股票图标 tab 表头「代码 / 名称」改为「名称 / 代码」（与行内名称在前、代码在后一致），加密货币 / 贵金属表头与搜索框占位同步。tsc 无错误、`/library` 编译加载正常。
1. 加密货币/贵金属 搜索框 + 搜索即添加（CoinGecko） —— 1) 加密货币 / 贵金属 tab 顶部新增搜索栏（搜索名称 / 代码 + 当前数量统计），列表按关键词本地过滤；2) 搜索无本地匹配时同样显示「未找到 XX」+「+ 添加」：加密货币候选走新增服务端接口 `GET /api/assets/search?type=crypto`（CoinGecko 搜索，返回 名称/代码/大图图标，走代理），贵金属候选为内置四大贵金属按名称/代码匹配；3) `POST /api/assets/add-by-search` 扩展支持 crypto/metal：传入 `iconUrl` 自动下载本地化（crypto → `public/uploads/asset/crypto/名称代码.扩展名`，metal 同理，market=ASSET，source=manual），并修复下载命名的扩展名缺小数点 bug；4) 实测：搜 `dogwifhat` → 候选 WIF → 添加 → 图标自动下载为 `dogwifhatWIF.jpg`（规范命名），测试数据已清理；贵金属搜「钯」→ 钯金 PALLAD。tsc 无错误、`/library` crypto/metal 页编译加载正常。
1. 素材库搜索增强：东财兜底 + 北交所前缀修复 + 手动添加兜底 + 添加提速 —— 1) 搜索源兜底：腾讯联想无结果时自动切东方财富搜索联想（按全称 / 简称 / 代码，覆盖 A股 / 港股 / 美股 / 北交所），解决改名股 / 中文全称搜不到——实测「西安旅游」→ 000610 \*ST西旅、「曙光数创」→ 920808 均可搜索并添加；2) 修复北交所代码前缀：920xxx / 4xx / 8xx 此前被误判为沪市（sh920808 无行情），改为 bj 前缀，`toTencentSymbol` / `minuteQueryCodes`（分时走势）/ 东财候选三处全局生效，北交所现价与迷你走势正常；3) 手动添加兜底：行情源也无候选时，股票 tab 显示「可手动添加」表单（市场 + 代码 + 名称，名称默认搜索词），保存仍走 `add-by-search`（图标尽力下载）；4) 添加提速：`add-by-search` 图标下载加 7 秒超时保护，微牛限流重试不再阻塞添加（实测 30s → 7s，超时留空后续可上传或回填）；5) 顺带修复 `lib/versions.ts` 条目缺逗号导致的 tsc 语法错误。tsc 无错误、`/library` 编译加载正常。
1. 素材库新增「券商图标」tab（市场图标后） —— 素材库 tabs 在市场图标后新增「券商图标」：券商列表来自 设置 → 股票设置 → 券商管理（原分组管理）中的分组，每行 序号 + 图标（悬停相机上传）+ 券商名称；`assets` 新增 broker 类型（id=`broker:{分组ID}`、market=GROUP、code=分组ID、name=券商名），图标按券商名称命名存放 `public/uploads/asset/broker/`（长桥证劵.png，已写入 `AGENTS.md` 命名规范），上传后全局生效；`/api/assets` 与 `/api/upload` 支持 broker；`rename-assets.mjs` 适配 broker 文件夹（名称无代码后缀）；附件管理素材库分支自动出现「broker」目录。实测：上传长桥证劵图标 → 注册 broker 素材 → 查询返回，命名规范、测试数据已清理。
1. 「分组」全站更名「券商」+ 券商图标全局显示 —— 1) 文案更名：设置 → 股票设置 的「分组管理」改为「券商管理」（添加分组 → 添加券商、分组名称 → 券商名称、删除分组 → 删除券商、提示文案同步），我的持仓 / 行情 编辑股票表单的「分组」字段改为「券商」，行情列表表头「分组」改为「券商」，财报日历「特别关注」分组提示同步为券商；2) 券商图标显示：`GroupSelect` 由原生 select 重写为自定义下拉（选项带券商图标 + 首字母兜底 + 选中对勾 + 点击外部关闭），我的持仓 / 行情编辑表单传入 `brokerIcons`；行情列表分组标签前显示券商图标；券商管理卡片由标签占位改为显示券商图标（素材库 broker 素材，无则首字母兜底）；`useAssetIcons` 新增 `brokerIcons`（券商名称 → 图标）供全局复用。tsc 无错误、`/library` /holdings /records /settings 编译加载正常。
1. 修复券商图标上传后不显示（分组 ID 大小写匹配 bug） —— 根因：券商素材存库时 code 被 `toUpperCase`（g178… → G178…），而前端按分组 id 精确 `===` 匹配全部落空，导致图标不显示、再次上传也找不到已有行无法强制更新 URL。修复：素材库券商 tab（`brokerIconOf` / `saveBrokerIcon` 查找）与券商管理（`SettingsView.brokerIconOf`）统一改为大小写不敏感匹配（`toLowerCase` 比对）；并在保存分组设置时同步素材库 broker 素材名称（`UPPER(code)=分组ID`，券商改名后按名称映射的图标仍全局生效）。实测：IBKR 两次上传换图 URL 正确覆盖更新，测试数据已清理；用户已上传的长桥证劵图标文件与记录完好，刷新即可显示。tsc 无错误、相关页面编译加载正常。
1. 修复券商分组被清空（保存空列表误清记录）+ 数据恢复 + 防误清保护 —— 根因：保存「券商管理」时若提交空 / 未加载完成的券商列表，服务端会把被移除分组的全部持仓记录分组清空（`clearRecordGroup`），导致我的持仓所有记录的券商丢失（设置里的 3 个券商仍在）。修复：1) 数据恢复——Admin 持仓按规则恢复：美股 8 条 → 长桥证劵、港股 5 条 + A股 3 条 → 华泰证劵；2) 防误清——券商管理保存按钮在券商列表加载完成前禁用（显示「加载中…」），且券商列表为空而当前已有券商时保存需二次确认（“将删除全部券商并清空所有持仓记录的券商”），避免异步加载竞态或误操作清空；3) 排查全局：仅「保存分组设置」会触发记录分组清空（rename/clear 同步），已由上述保护覆盖，其他设置保存均为局部字段不涉及记录。tsc 无错误、设置页编译加载正常。
1. 券商数据规范化（面向 iOS / Android） —— 1) 新增 `lib/brokers.ts` 统一券商数据模型 `{ id, name, icon }`：列表 = 设置券商分组（`site_settings.groups`，顺序即展示顺序）+ 素材库券商图标（`assets` type=broker，code=分组ID，大小写不敏感匹配），并抽出 `syncRecordGroups`（改名/删除同步持仓记录）供设置路由与 v1 共用；2) 新增 v1 券商接口：`GET /api/v1/brokers`（登录可读列表）、`POST /api/v1/brokers`（管理员全量保存，覆盖式）、`DELETE /api/v1/brokers?id=`（管理员删除并同步清空对应持仓记录的券商）；3) 更新 `docs/api-spec.md` 新增「券商 Brokers」章节（模型字段 / 响应示例 / 请求示例 / 增删改约定，图标走素材库上传自动合并）与路由清单，`AGENTS.md` 写入券商数据规范；4) 实测：GET 返回 3 券商含图标（长桥证劵 / IBKR / 华泰证劵），POST 原样保存 code 0，文档内容接口已含新章节，`/api-docs` 渲染正常。tsc 无错误。
1. 素材库券商图标：主流券商候选一键添加 + 行内删除 —— 1) 券商图标 tab 新增「新增主流券商」区：A股（华泰证券 / 东方财富 / 同花顺 / 中信证券 / 国泰君安 / 招商证券 等 15 家）、美股（IBKR / 嘉信理财 / 罗宾汉 / 富途 / 老虎 / 微牛 / 第一证券 等 12 家）、港股（富途 / 老虎 / 华盛 / 辉立 / 耀才 / 中银国际 / 海通国际 / 招证国际 等 15 家）分类切换，点击名称一键添加（生成分组 ID 写入券商管理，图标随后在行内上传），已存在（忽略证劵/证券差异）显示 ✓ 置灰；2) 每行右侧新增删除按钮（垃圾桶，hover 红，confirm 确认），删除走 `v1 DELETE /api/v1/brokers`：移除券商 + 同步清空该券商名下持仓记录 + 删除图标素材（assets 行与本地文件）；3) 实测：添加「同花顺」→ 4 家，删除 → 恢复 3 家，数据无损；页面编译正常。tsc 无错误。
1. 修复主流券商候选重复逻辑（IBKR = 盈透证券） —— 候选列表曾把同一券商的不同写法当作两个条目（如美股同时列出 IBKR 与 盈透证券、嘉信理财与 Schwab 等），且去重只比对字面名称，已添加 IBKR 后仍可再添加「盈透证券」造成重复。修复：`MAIN_BROKERS` 改为「规范名 + aliases」结构——同一券商只保留一个规范名（盈透证券 / 嘉信理财 / 罗宾汉 / 微牛证券 等），英文名与常见写法（IBKR / Interactive Brokers / Schwab / Charles Schwab / 嘉信 / Robinhood / Webull / Futu / Tiger / Firstrade 等）放 `aliases`；新增 `brokerNameKey`（证劵/证券 + 大小写归一化）与 `brokerExists`（规范名 + 别名集合比对），候选 ✓ 置灰与添加拦截统一走该逻辑，跨市场同名（富途 / 老虎 / 微牛）同样只允许添加一次；全表排查无其他同类重复（中金/CICC、亿创/E*TRADE、德美利/TD Ameritrade 等已归并）。`AGENTS.md` 写入候选去重规范。tsc 无错误、页面编译正常。
1. 修复新增主流券商候选深色模式下文字不可见 —— 深色模式下候选券商按钮（如微牛证券）文字呈深灰、与深色背景对比度极低（截图标红确认），已存在/选中态与 hover 也有相同隐患。修复：候选按钮深色模式显式浅色文字（`dark:text-[#e5e7eb]`，已存在态 `dark:text-[#99a3b2]`），hover 深色显式橙底黑字（`dark:hover:bg-[#FF9828] dark:hover:text-black`），不再依赖全局 `text-ink-2` 覆盖；全站排查 `hover:text-black` 均搭配橙色底（黑字在橙底可见），无其他同类隐患。tsc 无错误、`/library` 编译加载正常。
1. 券商别名（IBKR 改为盈透证券）+ 券商行可拖动排序 —— 1) 券商模型新增 `alias` 别名：IBKR 分组更名为「盈透证券」（持仓记录与 broker 素材名称同步），现有券商补齐别名（长桥证劵 → Longbridge、盈透证券 → IBKR、华泰证劵 → HTSC），素材库券商行改为两行显示（名称粗体 + 别名小字灰色，与股票图标「英伟达 / NVDA」同款），无别名不显示第二行；`GroupConfig` 增加 alias，券商管理卡片新增别名输入框，v1 `GET/POST /api/v1/brokers` 返回/接收 alias，`docs/api-spec.md` 与 `AGENTS.md` 同步（模型含 alias、请求响应示例更新）；2) 素材库券商行可拖动排序：行首拖动手柄 + 整行 draggable，松开后自动把新顺序 POST 到 v1 保存（toast「券商顺序已保存」），顺序即展示顺序（券商管理 / iOS 列表一致）。实测：v1 返回 长桥证劵(Longbridge) / 盈透证券(IBKR, 含图标) / 华泰证劵(HTSC)，页面编译正常。tsc 无错误。
1. 修复券商分组被迁移脚本覆盖丢失（数据恢复） —— 别名迁移脚本误用硬编码 3 个券商覆盖 `site_settings.groups`，导致用户此前通过素材库添加的券商分组（同花顺 / 东方财富 / 老虎证券 / 嘉信理财 / 罗宾汉 / 富途证券 等 6 个）从列表消失（broker 图标素材仍在）。修复：1) 从 `assets`（type=broker）与 `records.group_name` 反推被删分组，恢复完整券商列表 9 个——长桥证劵 / 盈透证券 / 华泰证劵 / 罗宾汉 / 富途证券 / 老虎证券 / 东方财富 / 同花顺 / 嘉信理财，别名补齐（Longbridge / IBKR / HTSC / Robinhood / Futu / Tiger / 东财 / Schwab）；2) 罗宾汉 / 富途证券（覆盖后重新添加的新分组 ID）的图标素材从旧分组 ID 迁移到新分组 ID，9 个券商图标全部恢复；3) `AGENTS.md` 写入「券商数据只读基准」：修改 groups 必须基于当前完整列表增量操作、禁止硬编码覆盖，恢复时可从素材与记录反推。实测：v1 返回 9 券商全部带图标，页面编译正常。

---

## v0.1.3 · 2026-08-07

> 图片文件唯一化：替换 / 删除素材时后台同步删除旧文件，并清理历史孤立文件，uploads 目录不再不断堆积（ico / 背景 / Logo / 头像 / 股票图标统一生效）。

### 新功能

1. 名人收益分析改为日线（CSV 存附件名人文件夹） —— 收益分析 K 线图由月粒度改为日粒度：服务端生成近 5 年日线（1826 天，每天一点，月序列插值平滑），持久化为 CSV（`data/attachments/{名人}/收益/收益日线.csv`，列 date / 组合收益 / 各对比指数），新增 `/api/celebs/returns` 接口读取；前端按区间取日线（近3月 92 天 / 近1年 365 天 / 近5年 1826 天，区间起点归零），悬停与 X 轴显示具体日期（年/月/日）。实测 1 年区间 365 点、轴标签 2025/08/08。
1. 名人收益曲线改为富途形态（涨跌有致 + 锚点精确对齐） —— 收益分析 K 线不再单调向上：改用「控制点样条（Catmull-Rom）+ AR(1) 均值回归噪声 + 锚点精确对齐」生成近 5 年日线——曲线含 2022 年回撤（组合抗跌、标普 -24.27% 对应参考图 -24.24%）、近 3 年 / 近 1 年锚点、1 年窗口中部深 V、末段冲高回落（巴菲特 5 年最高 84.05 / 终点 76，比例对应富途 85.7 / 77.06）；近 1 年 / 近 5 年终点与顶部数值完全一致（+26.54% / +76.00%），噪声在锚点处归零、无放大失真；CSV 升级 v3（版本标记强制重新生成）；X 轴只保留首尾日期（富途样式）；基准指数 5 年终点改用加法公式（负收益的名人不再爆值）。
1. 附件管理素材库分支：市值排序 + 删除同步 —— 附件管理素材库分支新增：1) 市值列可点击排序（按换算后的美元市值升序/降序切换）；2) 每行新增删除按钮（灰→悬停红，confirm 确认），删除后通过 `/api/assets` 同步移除素材库记录并刷新附件管理（素材库新增/删除与附件管理双向同步，实时读取 + `assets-updated` 事件）。实测：删除测试素材后素材库无记录、附件管理列表同步消失。
1. 附件管理分门别类（财报文件 / 素材库） —— 附件管理新增分类 tab：1) 财报文件——原有 13F 目录树；2) 素材库——按 股票（美股/港股/A股/日股/韩股子分支）/ 加密货币 / 贵金属 / 市场 分组展示，实时读取素材库数据，本地文件发生变化自动同步；支持图片预览（lightbox 大图）与修改名称（同步素材库 `assets.name`，`/api/assets` 主动改名强制生效）。实测 1358 个素材全部渲染、改名后素材库同步更新。
1. 素材库新增搜索与归属市场列 —— 1) 市场筛选行新增搜索框：全部搜全部、美股搜美股等（按当前市场过滤代码 / 名称，关键字写入 `?q=` 防刷新）；2) 表格新增「归属市场」列（可点击排序）：美股按交易所显示纳斯达克 / 纽交所 / 美交所（东方财富 `f13`），A股按代码规则显示科创板 / 创业板 / 北交所 / 沪主板 / 深主板，港股显示主板 / 创业板；同步与 backfill 接口补齐存量数据（实测更新 1154 行，NVDA→纳斯达克、TSM→纽交所、688825→科创板、300750→创业板、00700→主板）。

### 修复

1. 恢复名人持仓 F2 版本 —— 用户对 mask 实心化调整不满意，按约定用 `docs/celebs-ring/celebs-ring-F2.tsx` 整体替换 `CelebsView.tsx` 恢复：头像置顶于扇形之上（圆环在人物之下、悬停块状不覆盖头像）、mask 58% 起淡出。
1. 名人持仓头像身体实心化（领带下方不再透明） —— 透明头像的 mask 底部淡出范围收窄（58% 起 → 78% 起，底部仅 3% 柔化），西装/衬衫主体保持不透明，完整遮挡圆环内圈；领带下方不再透出后面的扇形与 Logo，观感与参考图（身体实心、遮挡圆环）一致。
1. 命名名人持仓 F2 版本并备份 —— 将当前版本（头像置顶于扇形之上：圆环统一在人物之下、悬停块状不覆盖头像）命名为「名人持仓 F2」，备份至 `docs/celebs-ring/celebs-ring-F2.tsx`，并在 `AGENTS.md` 登记恢复方法。
1. 名人持仓圆环：头像置顶于扇形之上（悬停块状不再覆盖人物） —— 修复悬停扇区（左上/右上等）弹出时覆盖人物头像的问题：圆环 SVG 统一渲染在人物之下（不再按透明检测分支压在人物之上），头像始终位于最顶层、完整可见（含肩膀衣领），悬停弹出的块状从头像后面穿过；与参考图「头像在扇形之上、扇形从后面经过」效果一致。
1. 恢复名人持仓 F1 版本 —— 用户对 F2（额头置顶于扇形之上）不满意，按约定用 `docs/celebs-ring/celebs-ring-F1.tsx` 整体替换 `CelebsView.tsx` 恢复改造前版本：圆环恢复条件层级（透明头像在上 / 非透明在下）、头像尺寸 0.7/0.66、上移 3px（悬停 5px）、mask 58% 起淡出；实测头像尺寸 97px 确认恢复到位。
1. 名人持仓圆环：额头置顶于扇形之上（F2） —— 圆环统一改为压在人物之上（不再依赖透明检测分支）：头部从洞里探出、额头贴近/压住上方扇形，肩膀/衣领被圆环扇形明显遮挡；透明头像尺寸 0.70→0.74、上移 3→6px（悬停 8px）、肩膀 mask 淡出起点 58%→52%。改造前版本已备份为「名人持仓 F1」（`docs/celebs-ring/celebs-ring-F1.tsx`），不理想可随时恢复。
1. 确立「苹果风格」规范（以名人管理开关为基准） —— 将名人持仓-名人管理的启用开关确立为「苹果风格」基准并写入 `AGENTS.md` 项目规范：开启苹果绿 `#34c759`、关闭浅灰 `#e9e9ea`（深色 `#3a3a3c`）、圆点必须内联纯白（禁止 `bg-white` 类，防深色模式被 `.dark .bg-white` 覆盖）、36×20 基准；动画规范：轨道颜色 300ms ease-out、圆点滑动 300ms + iOS 缓动 `cubic-bezier(.32,.72,0,1)`、乐观更新不抖动、禁止额外弹跳缩放闪烁。此后用户说「苹果风格」即指该样式，全站开关类控件统一遵守。
1. 修复开关圆点深色模式变深（统一纯白） —— 全局暗黑规则 `.dark .bg-white` 会把开关白色圆点覆盖成深色（#151a26），导致素材库 CDN、注册、自动备份开关在深色模式下圆点发暗、与名人持仓-名人管理开关不一致。修复：三处开关圆点改为内联白色（`style backgroundColor #fff`），深浅色模式均为纯白；关闭轨道统一 `#e9e9ea` / 深色 `#3a3a3c`。实测深色模式下圆点 rgb(255,255,255)。
1. 全站开关统一苹果 iOS 风格配色 —— 素材库 CDN 通道开关、网站设置注册开关、数据库增强自动备份开关统一为苹果 iOS 开关配色：开启苹果绿 `#34c759`，关闭浅灰 `#e9e9ea`（深色模式 `#3a3a3c`），白色圆点 + 平滑过渡，与名人持仓-名人管理启用开关完全一致。实测关闭色 rgb(233,233,234)。
1. 素材库加载骨架（消除「暂无股票图标」误提示）+ CDN 开关苹果风格 —— 1) 素材库进入时 assets 尚未加载完成会误显示「暂无股票图标，可点击右上角设置同步」，现改为加载中显示骨架屏（表头 + 10 行占位），加载完成后才显示列表、真正为空才显示提示；2) 素材库设置弹窗 CDN 图标通道开关改为苹果 iOS 风格：开启为苹果绿 `#34c759` + 白色圆点，关闭为浅灰轨道（深色模式 `#3a3a3c`），平滑过渡。
1. 版本弹窗版本 tab 加变更数徽标 —— 设置 → 关于 → 版本弹窗的每个版本号右上角新增圆形数字徽标，显示该版本变更总数（如 v0.1.3 右上角圆内 14），选中态白底蓝字、未选中蓝底白字，一目了然。
1. 设置页化繁为简（删除多余文字提示） —— 站点信息 / 网站形象卡片及全站设置卡片、子页面删除副标题说明文字（desc）、保存后的行内提示块（保留 Toast 反馈）、注册开关等说明小字；`SettingsHeader` 的 `desc` 改为可选。实测设置页仅保留卡片标题与表单字段，界面更简洁。
1. 收益分析悬停十字光标顺滑优化（零 React 重渲染逐帧跟随） —— 修复鼠标快速来回滑动时十字光标又肉又卡的问题：原实现每次 mousemove 都触发 React 重渲染（365~1826 点 polyline 每帧重建）+ 150ms left/top 过渡追赶。现改为 `React.memo` 冻结图表（悬停移动零重渲染，实测 400 次滑动 SVG polyline 节点不变、0 次 DOM 变更）+ ref 直接更新十字线 / 圆点 / 日期 / 数值 / 浮窗 + `requestAnimationFrame` 合并到每帧一次，光标逐帧精确跟随鼠标；进入 / 离开时仅一次状态更新（主线变粗 + 200ms 淡入淡出），来回滑动平滑无感。
1. 修复站点信息被测试覆盖丢失（冒烟不再写站点设置 + 恢复备份） —— 根因：冒烟测试「保存网站设置」会把站点标题 / 域名 / 图标 / 背景覆盖成测试占位值（如 `/uploads/ico/test.png`），而「替换删旧文件」逻辑把用户真实图标 / 背景文件删除；测试中途中断时末尾还原未执行，残留测试值。修复：1) 冒烟测试移除对 site settings 的全部写操作（不再污染用户数据），首页标题断言改为对比备份值（82/82 通过且测试后设置保持不变）；2) settings 保存删旧文件逻辑加保护——新值为本地但文件不存在（测试占位 / 异常写入）时不删旧文件；3) 孤立文件清理增加 24 小时安全期，防误删刚上传未保存的文件；4) 从每日备份恢复用户原始的网站图标 / 背景文件与站点设置（ico/background 文件恢复并可访问，标题域名保持用户当前值）。
1. 修复手动保存覆盖市值 + 附件管理刷新秒出 —— 1) `upsertAsset` 的市值 / 价格 / 涨跌幅改为仅在传入有效值时更新：此前素材库手动添加 / 编辑（不带市值）会把同步股票的市值覆盖为 0（如平安银行 000001 因此无市值）；backfill 接口同时补归属市场与市值，实测 000001 恢复 2187 亿人民币、A股全部有市值。2) 附件管理素材库分支增加 60 秒本地缓存秒出（刷新先显示上次数据再后台拉最新），接口本身本地约 40ms。
1. 修复港股/A股/日韩市值无数据（市场-货币映射） —— 市值换算误用 `rates[市场代码]`（如 HK），而汇率键是货币代码（HKD/CNY/JPY/KRW），导致只有美股有市值。修复：增加市场→货币映射（US→USD / HK→HKD / CN→CNY / JP→JPY / KR→KRW），本地货币市值 ÷ 对应汇率换算为美元。实测：腾讯控股 00700 市值 $554.8B（港币 4352 亿 ÷ 7.8446），素材库与附件管理同步修复。
1. 素材库市场列右对齐统一（附件管理同步） —— 市场列表头与数据行内容改为右对齐，与市值 / 最新价 / 涨跌幅三列对齐方式统一；列宽调整：序号 36 / 代码名称自适应 / 市值 80 / 最新价 72 / 涨跌幅 72 / 市场 140，市场列空间充足、与涨跌幅不再拥挤；附件管理素材库分支同步。实测表头与行右边缘一致。
1. 分页精简（移除首页/末页按钮） —— 智能分页去掉「首页 / 末页」按钮（与页码 1 / 尾页功能重复），保留 上一页 / 页码（首尾 + 当前附近 + 省略号）/ 下一页 / 「共 N 页 · 跳至」输入框，控件更简洁；素材库与附件管理统一生效。
1. 素材库市场列加市场图标并拉开间距（附件管理同步） —— 素材库股票列表「市场」列加入市场图标（素材库市场图标，与筛选栏一致），列宽 84→98px 拉开与涨跌幅的间距；附件管理素材库分支同步更新（股票条目市场列显示市场图标）。实测列宽：序号 20 / 代码名称 215 / 市值 92 / 最新价 84 / 涨跌幅 84 / 市场 98（含图标）。
1. 分页优化 / 表头调整 / 市值货币换算 / 附件管理分页 —— 1) 新增智能分页组件（Pagination）：页码首尾 + 当前页附近 + 省略号，含首页 / 上一页 / 下一页 / 末页 / 跳转输入框，替换原先只有「上一页 / 下一页」的分页，素材库与附件管理素材库分支统一使用；2) 素材库表头「归属市场」改名「市场」，并与市值列互换位置（顺序：序号 / 代码与名称 / 市值 / 最新价 / 涨跌幅 / 市场）；3) 市值按汇率统一换算为美元显示（本地货币市值 ÷ `/api/rates` 汇率，避免港股/A股/日韩市值直接混排）；4) 附件管理素材库分支每页 20 条分页展示（原一次性渲染 1358 条）。实测：素材库 68 页智能页码、附件管理 20 条/页、NVDA 市值 $5.29T 正确换算。
1. 素材库文件分类归档（市场 / 加密货币 / 贵金属 / 股票分文件夹） —— `uploads/asset` 目录按类型分文件夹：market / crypto / metal / stock/{市场}。上传接口支持 `folder` 参数（前端按类型自动归类），清理接口先迁移历史根目录文件再清理孤立文件；实测 66 个历史文件迁移到对应文件夹、20 个孤立文件清理，`assets.url` 全部更新、根目录零残留。
1. 素材库股票同步数量修正（翻页全量拉取 + 美股 300 亿门槛） —— 根因：东方财富 clist 接口单页上限 100 条（`pz=300` 也只返回 100 条），此前美股/港股/A股各只同步到前 100 名。修复：按市值降序翻页拉全量（每页 100，直到市值低于门槛），美股门槛调整为 300 亿美元、港股/A股保持 500 亿本地货币。实测：美股 107→506、港股 100→243、A股 100→409、日股/韩股保持前 100 大市值。
1. 图片文件唯一化（替换 / 删除同步清理 + 孤立文件清理） —— 1) 素材库图标：更新 / 删除资产时后台同步删除旧本地文件（仅当无其他引用时删除，避免误删共享图片），同一条素材多次上传不堆积；2) 网站设置：网站图标 / 背景 / 首页 Logo 替换保存后自动删除旧文件；3) 个人头像、名人头像上传替换后自动删除旧文件；4) 素材库设置弹窗新增「清理孤立文件」按钮，扫描 `public/uploads` 删除未被任何记录引用的历史残留文件——首次执行清理 778 个文件（旧同步随机名图标、旧背景 / Logo / 头像等），被引用文件全部保留。

---

## v0.1.2 · 2026-08-06

> 名人管理头像上传化繁为简：去掉头像旁的铅笔按钮，鼠标划过头像直接显示相机即可上传，与持仓股票图标交互统一，全站排查同类冗余按钮。

### 新功能

1. 素材库与股票图标重塑（分市场同步 / 微牛图标源 / 微牛样式列表） —— 1) 股票图标首次全量同步：美股 >500 亿美元、A股 >500 亿人民币、港股 >500 亿港币（东方财富市值排行），日股/韩股前 100 大市值（companiesmarketcap）；仅首次自动拉取，之后手动触发（素材库设置弹窗「立即同步」）。2) 图标源：美股/港股/A股用微牛（搜索接口拿 tickerId → 168x168 PNG），日股/韩股用 parqet；图标下载到本地 `public/uploads/asset/stock/{市场}/`，按「中文名+代码」命名（苹果AAPL / 寒武纪688256 / 腾讯控股00700），全站秒开无外部慢请求。3) 素材库股票 tab 重塑为微牛样式：白色卡片 + 表头（序号 / 代码与名称 / 最新价 / 涨跌幅 / 市值，可点击排序，排序写入 `?sort=&dir=` 防刷新）+ 圆形图标行（悬停相机上传）+ 分页（每页 20，`?page=`）；数据直接读本地素材库，不再依赖外部 top-stocks 接口。4) 素材库标题右侧新增设置入口（齿轮，与名人持仓一致）：股票同步（进度/上次同步时间）、退市检测（连续两次失败判定删除）、CDN 图标通道开关（默认关闭，开启后缺失图标尝试外部 CDN 兜底）。5) `assets` 表新增 `market_cap/price/change_pct/source/last_checked_at`，自动同步不覆盖手动上传图标（source=manual 优先）。6) 退市检测接口比对东方财富行情，连续两次失败才删除防停牌误删；日韩暂未接入检测源。
1. 名人持仓股票图标为最高优先级并全局生效（迁移 + 缓存修复） —— 名人持仓上传的股票图标作为最高优先级图标源：上传即写入素材库（`/api/assets`，type=stock，按「市场:代码」如 `US:GOOGL`），全站（所有名人圆环、持仓明细、编辑表单）自动共用，无需重复上传。1) 历史「名人专属」图标已迁移到素材库并清空专属记录（巴菲特 GOOGL/AXP/KO/BAC/CVX/OXY/CB、段永平 PDD/GOOGL 共 9 个），素材库成为唯一图标源，圆环/明细不再读取名人专属 override；2) 修复 useAssetIcons 会话级缓存不刷新的问题：挂载时先用缓存秒出、再后台强制拉取最新素材库，新增/迁移的图标立即全局生效（此前旧缓存会一直回退默认图）；3) 圆环/明细图标查找按「持仓市场:代码」优先、`US:代码` 兜底，兼容非美股。
2. 圆环中心头像透明自由轮廓 + 圆环压人物头从洞中探出 + 悬停跃然而出（重做） —— 根据反馈重做头像：1) 透明 PNG 头像改为自由轮廓渲染（不再被圆形蒙版硬切，完整保留肩膀/衣领），人物背后加深蓝色圆盘衬托，按图片透明度自动分流（canvas 检测 alpha），非透明头像（默认 JPG）保持圆形裁剪；2) 图层顺序调整：圆环置于透明人物之上，圆环压住人物肩膀/衣领，头部从洞中探出（参考图「头从洞里探出、衣领被扇形遮挡」）；人物 0.64、上移 3px（悬停 5px），头大居中；3) 悬停不再单纯放大图片，改为「上浮 + 深色投影加深 + 蓝色光环淡入」，320ms 平滑过渡；4) 深色圆盘保留克制的呼吸光晕。
2. 收益走势图升级为 moomoo 分时风格 —— 收益分析走势图按 moomoo 分时图风格重做：组合收益主线下新增蓝色渐变面积填充（线条处半透明 → 底部透明）；主线增加霓虹发光（双层描边：底层宽光晕 + 上层细线），悬停时主线自动变粗（2.4 → 3.8）且光晕增强，200ms 平滑过渡；悬停新增右侧深色信息浮窗（毛玻璃）：日期 + 组合收益 + 全部对比指数数值（红涨绿跌）；图表新增竖向点状网格（四分位）与 X 轴首/中/尾三个日期标签，整体更接近专业金融终端。
3. 名人持仓收益分析重设计（参考富途）+ 详情页 URL —— 详情页收益分析卡片按参考图重做：顶部三大时段大数值（近1年 / 近3年 / 近5年，红涨绿跌）；近3月 / 近1年 / 近5年区间切换，走势图按区间生成（近3月取末尾 3 个月归零、近1年用月序列、近5年平滑合成终点对齐 y5）；图例「组合收益」（蓝）+ 对比指数（灰）数值随鼠标联动，悬停显示白色虚线十字光标、圆点、左侧数值标签与底部日期标签；底部「收起 / 展开全部」折叠（收起后只保留三大数值）；卡片整体缩小为约原 1/4（实测 576×352，图表 534×123），横向网格改为淡点状虚线，十字光标用 CSS 过渡平滑滑动、淡入淡出。详情页增加 URL（`/celebs?celeb=<id>`）：深链直达、刷新保持、返回 / 前进后退无感切换列表与详情。
4. 对比基准指数可扩展（benchmarks 数组） —— 收益对比不再硬编码标普500：`returns` 新增 `benchmarks` 数组（code / name / y1 / points），后续加纳指（IXIC）、上证（SH000001）、恒生（HSI）等只需追加一条数据即可同时出现在图例与图表（灰系自动取色）。存储层、名人管理表单、SEC 数据管道、服务端缓存与前端 localStorage 全链路兼容：旧数据没有 benchmarks 时用 spxPoints 自动兜底归一为标普500，刷新不丢、不闪现。

### 安全修复

1. 后端路由服务端鉴权守卫（修复未登录展示后台布局） —— 严重安全修复：未登录直接输入后台地址（`/watchlist`、`/records`、`/celebs` 等）会展示完整后台布局。根因是后台布局完全依赖客户端 `router.replace('/login')` 跳转（实测未生效），服务端无鉴权。修复：1) 后台布局（`[...slug]`）增加服务端守卫——未登录一律 307 重定向到纯净的 `/login`（无多余后缀），后台布局/数据绝不外泄；2) 客户端兜底守卫——鉴权完成且未登录时渲染「请先登录」占位并引导登录，不渲染后台界面；3) 登录/注册/改密限流适度放宽（登录 50 / 注册 20 / 改密 20 次每 15 分钟，配合全局预算兜底，保留防爆破能力且正常使用/冒烟测试不误触），冒烟测试同步新增「未登录后台跳登录页」断言。实测：未登录访问 `/watchlist` 立即跳 `/login`，登录用户访问后台正常。
2. 全站安全加固（响应头 / 限流 / 输入校验 / 密码策略） —— 根据全站安全审计结果完成加固：1) 新增安全响应头：CSP（生产收紧：无 `unsafe-eval`、`connect-src` 仅 self；开发放行 HMR ws）、`X-Frame-Options: DENY`、`X-Content-Type-Options: nosniff`、`Referrer-Policy`、`Permissions-Policy`，关闭 `X-Powered-By`（不再泄露 Next.js）；实测页面无 CSP 违规、外链图片正常。2) 公开行情类接口（quotes / search / kline / charts / ticker / indices / earnings / top-stocks）新增按 IP + 全局双限流（实测搜索 60 次/分钟后返回 429）；认证接口（登录 / 注册 / 改密）增加全局预算，配合按 IP 限流，即使伪造 `X-Forwarded-For` 也无法无限爆破（IP 值增加格式校验）。3) kline 股票代码白名单 + `encodeURIComponent`，URL 参数注入被拒（实测 400）。4) SVG 上传消毒强化：必须为 SVG 且扩展黑名单（事件处理器全类目、`<a`/`<use`/`<image`/动画、`data:`/`javascript:`/外部引用 href/xlink），恶意 SVG 被拒。5) 公开接口错误信息收敛，不再回显 `err.message`（防泄露数据源内部细节）。6) 密码策略提升：至少 8 位且同时含字母与数字（注册 / 改密 / 管理员重置均校验），前端提示同步更新，冒烟测试账号密码同步调整。
1. 限流升级为 SQLite 共享持久化存储 —— 限流计数由进程内存态升级为 SQLite 持久化（`rate_limit` 表）：同一主机多进程 / 多 worker 共享计数、服务重启不清零，配合既有按 IP + 全局双限流，防爆破与滥用更可靠；环境变量 `RATE_LIMIT_STORE=memory` 可回退到纯内存态（单实例最快），SQLite 不可用时自动回退内存不影响请求；过期桶与过期行定期清理，防止无限增长。实测搜索接口计满 60 次/分钟后返回 429，计数已落库（search 与 global:search 行均持久化）。
2. 全站安全加固（响应头 / 限流 / 输入校验 / 密码策略） —— 根据全站安全审计结果完成加固：1) 新增安全响应头：CSP（生产收紧：无 `unsafe-eval`、`connect-src` 仅 self；开发放行 HMR ws）、`X-Frame-Options: DENY`、`X-Content-Type-Options: nosniff`、`Referrer-Policy`、`Permissions-Policy`，关闭 `X-Powered-By`（不再泄露 Next.js）；实测页面无 CSP 违规、外链图片正常。2) 公开行情类接口（quotes / search / kline / charts / ticker / indices / earnings / top-stocks）新增按 IP + 全局双限流（实测搜索 60 次/分钟后返回 429）；认证接口（登录 / 注册 / 改密）增加全局预算，配合按 IP 限流，即使伪造 `X-Forwarded-For` 也无法无限爆破（IP 值增加格式校验）。3) kline 股票代码白名单 + `encodeURIComponent`，URL 参数注入被拒（实测 400）。4) SVG 上传消毒强化：必须为 SVG 且扩展黑名单（事件处理器全类目、`<a`/`<use`/`<image`/动画、`data:`/`javascript:`/外部引用 href/xlink），恶意 SVG 被拒。5) 公开接口错误信息收敛，不再回显 `err.message`（防泄露数据源内部细节）。6) 密码策略提升：至少 8 位且同时含字母与数字（注册 / 改密 / 管理员重置均校验），前端提示同步更新，冒烟测试账号密码同步调整。

### 修复

1. 修复名人持仓刷新闪现中文名字占位 —— 上一版为防头像空白加了首字母垫底，导致图片加载完成前显示「巴」「黄」等汉字。改为：1) 占位改无文字的低调底色（非透明头像浅灰圆盘、透明头像留空），头像图片加载完成直接覆盖，不再闪现汉字；2) `/celebs` 页面 SSR HTML 增加头像 preload link（`<link rel="preload" as="image">`），浏览器解析阶段即并行下载自定义头像，人物更快出现。实测：6 个头像全部加载成功，圆环中心直接显示人物照片、无文字占位。
1. 全站刷新性能优化（图标请求去重 / 外部图标超时降级 / 指数轮询后台暂停） —— 排查「网页刷新慢」实测定位三类问题并修复：1) `useAssetIcons` 模块级请求去重——此前每个图标组件（名人圆环每个股票 logo、市场 / 资产图标等）都独立 fetch `/api/assets`，一次刷新会发几十次重复请求（局域网 / 手机端更明显），改为全站共享一次请求，刷新后素材图标请求从几十次降到 1-2 次；2) 外部公司图标（`g.foolcdn.com`）国内访问约 800-1500ms/个，素材库没有的股票图标 1.5s 未加载即降级为首字母占位并写入 `sessionStorage`，本次会话刷新不再重复请求慢 CDN；3) 首页指数栏在页面隐藏 / 睡眠时暂停 60s 轮询，避免多标签页堆积行情请求。另排查确认：`/api/celebs` 有缓存时秒出（约 50ms），冷启动 / 代码变更后的 15-25s 为 Next dev 按需编译，生产构建无此问题。
1. 修复名人持仓刷新/睡眠后人物头像短暂消失 + 移除页面副标题 —— 1) 扇形中心头像刷新消失的根因是 SSR 首帧只渲染静态示例头像，客户端再切到缓存的自定义头像，期间 img 无占位、新图（部分自定义 PNG 达 1MB+）加载慢导致人物区域空白。修复：后台布局（`[...slug]`）服务端读取自定义头像（`celebs-avatars.json`）并传入，SSR 首帧即渲染最新头像，刷新无示例→自定义的切换；头像区域改为首字母占位始终垫底、图片加载完成后 320ms 淡入覆盖，加载/切换期间人物区域不空白；挂载后预加载全部名人头像，刷新 / 睡眠唤醒后尽早并行加载。2) 名人持仓页副标题「追踪知名投资人的 250 日持仓表现与最新操作 · 样式参考富途「聪明钱」」按用户要求删除。
1. 移除「开发模式来源」后台可视化配置（恢复 next.config.mjs 手动方式） —— 按决策移除整个功能及所有残留：后台卡片、保存 / 重启服务 / 删除按钮、`/api/system/restart` 与 `/api/system/dev-origins` 接口、`scripts/restart-server.mjs`、settings `devOrigins` 字段与数据库配置均已清除；`next.config.mjs` 恢复为原始方式（`DEV_ORIGINS` 环境变量或直接改代码配置 `allowedDevOrigins`）。
1. 开发配置：allowedDevOrigins 支持局域网访问 —— `next.config` 增加 `allowedDevOrigins`（localhost / 127.0.0.1 / localhost），消除局域网 IP 访问开发资源的 cross-origin 警告，并为 Next.js 未来大版本强制要求该配置提前兼容；配置变更后服务已自动重启验证。
2. 圆环中心优化：去深色圆盘 / 衣领柔化 / SEC 徽标统一灰色 —— 1) 圆环中心人物头像背后的深色圆盘移除（改为透明）：浅色模式下洞内直接显示页面背景，鼠标悬停最大持仓时不再出现突兀的黑色圆；2) 透明人物的衣领/肩膀底部增加 CSS mask 线性渐变向下淡出（58% 实 → 100% 透明），悬停/切换持仓时衣领不再生硬突出，与圆环自然融合；3) 数据来源徽标（SEC 13F / SEC Form 4 / 示例数据）颜色统一为中性灰方案（`bg-bg-gray` / 深色 `white/5`），不再用蓝色区分，深浅色一致。
2. 名人专属股票图标为最高优先级并全局生效（迁移 + 缓存修复） —— 名人持仓上传的股票图标作为最高优先级图标源：上传即写入素材库（`/api/assets`，type=stock，按「市场:代码」如 `US:GOOGL`），全站（所有名人圆环、持仓明细、编辑表单）自动共用，无需重复上传。1) 历史「名人专属」图标已迁移到素材库并清空专属记录（巴菲特 GOOGL/AXP/KO/BAC/CVX/OXY/CB、段永平 PDD/GOOGL 共 9 个），素材库成为唯一图标源，圆环/明细不再读取名人专属 override；2) 修复 useAssetIcons 会话级缓存不刷新的问题：挂载时先用缓存秒出、再后台强制拉取最新素材库，新增/迁移的图标立即全局生效（此前旧缓存会一直回退默认图）；3) 圆环/明细图标查找按「持仓市场:代码」优先、`US:代码` 兜底，兼容非美股。
1. 名人专属股票图标上传即生效（不再依赖点保存） —— 修复名人管理-编辑-持仓股票图标上传后重新打开会重置、圆环不同步的问题：根因是旧逻辑上传只更新了编辑表单的本地草稿，必须再点「保存」才写入数据库，而「上传成功」提示容易误导。新增 `/api/celebs/stock-icon` 接口：已存在的名人上传图标后立即持久化到 `celebs.stock_icons_json`（该名人专属、不写全局素材库），并同步前台圆环；新建名人仍随「创建」一起保存。验证：上传 KO 测试图标后不点保存，接口立即返回生效；测试文件未注册进素材库（素材库 US:CODE 仍作为兜底）；已恢复用户真实上传的 GOOGL 图标并确认编辑表单与圆环同步显示。
2. 圆环悬停提示回归方向定位 + 圆环左移 + 残留修复 —— 1) 提示文字恢复按扇区方向定位（右/左/上/下外侧），移除视口翻转逻辑——最大持仓的提示不再被翻到左上方；2) 名人持仓列表卡片右侧内边距 112px → 144px（圆环再左移 32px，实测右缘 1152 → 1120），右侧扇区（如美国运通 · 12.8%）的提示完整显示在圆环右侧（x 1128–1244 < 页面 1280），不再贴边/溢出；3) 圆环容器增加 `onMouseLeave` 兜底：指针离开圆环时强制清空扇区与头像悬停，避免提示文字残留。
3. 圆环悬停提示外侧智能定位 + 股票 Logo 跟随外倾 —— 1) 悬停提示不再遮挡中心人物头像：改为放在圆环容器外侧，按扇区主导方向自动选择上/下/左/右（如右侧扇区提示出现在圆环右侧外缘，垂直对齐扇区中部），实测巴菲特（苹果 24.6%）与黄仁勋（英伟达 46.8%）的提示均在圆环外侧空白区，头像与圆环完全不被遮挡，位置自然协调。2) 股票 Logo 悬停联动：鼠标划过扇区时，该扇区 Logo 与色块同步向外平移（同外弹向量）+ 按扇区角度轻微外倾旋转（±7°）+ 放大 1.08，320ms 平滑过渡，不再呆板。
3. 修复圆环最大持仓段右下显示不全（三处根因） —— 名人持仓圆环图最大持仓段右下角显示不全的完整根因与修复：1) 最大段默认「外扩」外缘到半径 49.6（viewBox 0~100 仅余 0.4），悬停再沿中角外弹 2.4px（外缘 52）超出视口，被 SVG 默认 `overflow:hidden` 裁出直线缺口——圆环 SVG 改为 `overflow-visible`；2) 悬停外弹使最大段与相邻段分离，露出深色楔形缝隙（视觉上像「被挡住」）——悬停段改为最后绘制，并按扇形跨度动态扩展边界角度，把缝隙填满，圆环保持闭合；3) 相邻路径间的亚像素抗锯齿细线被明暗反差放大——每个扇形段加与自身同色的发丝描边盖住接缝。实测悬停/非悬停状态各交界处像素级无缝相接（视觉模型误读的「缝隙」实为悬停时相邻段变暗 20% 的正常强调反差）。
4. 修复弹窗被整页高度带偏（tab-panel transform 包含块） —— 名人管理弹窗原本固定定位却随页面高度下移：根因是后台 tab 切换动画（`tab-in`）在 transform 上做位移，动画结束后浏览器仍以矩阵值（恒等矩阵）保留包含块，导致弹窗的 `fixed` 定位退化成相对整个页面（文档）居中——名人越多页面越长，弹窗就越靠下。修复：tab 切换动画改为仅淡入（去掉 transform 位移），弹窗回归视口垂直居中（实测 900px 视口下面板顶部 178px、底部 723px，正中 450px），内容完整可见；同时修复了 tab 面板内所有 fixed 元素（弹窗 / 抽屉 / 提示）的同类定位问题。
5. 名人数据标签改为显示来源标签 —— 名人持仓列表与详情页的数据徽标不再显示自动生成的类型文字（示例数据 / SEC 13F），改为直接显示名人管理里配置的「来源标签」（如「SEC EDGAR 13F · 伯克希尔」「SEC EDGAR Form 4 · 佩洛西」），缺失时按来源类型兜底；接口返回的 detail 映射统一补齐每位名人的来源标签。数据质量仍用颜色区分：真实数据（SEC 13F / Form 4）浅蓝底、示例兜底灰底；悬停提示补充说明（如「当前为示例兜底数据（SEC 暂不可达）」），手动维护名人（公开披露 · 示例数据）不追加该提示。
6. 名人持仓详情刷新修复 + 缓存防污染 + 管理弹窗秒开 —— 1) 详情页深链（`/celebs?celeb=<id>`）刷新不再停留在原始头像与示例数据：首帧用示例+自定义头像占位后，缓存/接口真实数据到位会自动替换 active（此前 pendingId 被提前清空导致永远停在示例对象）；自定义头像同时应用到详情视图。2) 缓存持久化修复：SEC 拉取失败时不再把示例数据写进缓存文件（此前示例数据会冒充「缓存数据（24h 内）」并覆盖真实数据），只持久化真实（SEC 成功）或手动维护数据，被污染的旧缓存自动清理，失败条目下次继续重试；实测缓存仅保留真实（cathie/SEC 13F）与手动（trump）条目。3) 名人管理弹窗增加本地缓存：再次打开秒出列表，不再闪现「加载中…」；首次无缓存时用骨架屏占位。
7. 名人管理头像上传化繁为简 —— 编辑表单头像右下角的铅笔按钮删除：头像本身改为可点击按钮，鼠标划过显示相机遮罩 + 放大 + 灰色描边，与持仓明细股票图标的交互完全一致，点击即打开文件选择器上传新头像。全站排查同类问题：名人管理列表的「编辑」铅笔按钮与名人持仓页标题旁的「修改头像」快捷入口（弹出名人选择列表）属独立功能保留；个人设置头像（相机按钮）与素材库上传（整块点击 + 文字）已是简化形态，无需重复按钮。
8. 名人持仓卡片布局微调 + 头像选择列表悬停相机 —— 1) 圆环图再左移：卡片右侧内边距由 80px 增至 112px，圆环右缘不再贴边，右侧留白更从容；2) 右上角数据来源徽标（如「缓存数据（24h 内）」）由紧贴边缘改为留出 16px 内容边距（距视口右缘约 31px），靠近但不贴边，与卡片内边距协调；3) 标题旁铅笔弹出的「选择名人，上传新头像」列表：头像悬停时放大 + 淡入相机遮罩 + 行背景高亮（与编辑表单头像交互统一），点击整行即可上传。

---

## v0.1.1 · 2026-08-05

> 出站代理支持（本地代理）、名人持仓 UI 重构 + SEC EDGAR 真实数据源、名人头像本地化抓取、未登录行情补货币符号、版本记录弹窗重设计为「总结 + 详细更新」。

### 新功能

1. 名人管理界面优化 —— 1) 管理列表头像与展示页同步（合并 `data/celebs-avatars.json` 的自定义头像覆盖）；2) 「启用」改为 iOS 风格滑块开关（平滑过渡），并移除编辑表单里重复的启用项（启停统一在列表页控制）；3) 编辑/新增表单顶部新增「返回列表」按钮；4) 持仓明细与近期操作编辑器补全表头（图标 / 股票代码 / 股票名称 / 市场 / 占比% / 目标价；涨跌幅% / 操作），市场列带市场图标（素材库优先）；5) 每行持仓新增可编辑股票图标：素材库兜底，上传后仅在本名人生效（`celebs` 表 `stock_icons_json` 记录，不写全局素材库），前台圆环与详情页优先使用名人专属图标。
2. 名人持仓可维护化（新增 / 编辑 / 排序） —— 名人与持仓从硬编码改为数据库维护（SQLite `celebs` 表：基本信息 / 启停 / 排序 / 数据来源配置 / 持仓 / 近期操作 / 收益 JSON）。后台「名人持仓」标题旁新增齿轮「名人管理」（管理员）：新增 / 编辑 / 删除 / 启停 / 拖动排序（HTML5 拖拽 + 保存提示），表单含唯一 ID、名称、头衔、头像上传、来源类型（SEC 13F / Form 4 / 示例数据）+ CIK / 申报主体、持仓明细与近期操作编辑器、收益字段；SEC EDGAR 真实数据管道改为按每条记录的来源配置工作，手动数据作为兜底；增删改后自动清除 24h 缓存并刷新前台，立即生效。
3. 名人持仓头像与股票图标可编辑 —— 名人持仓圆环中心头像右下角新增铅笔按钮（管理员可见）：点击后头像进入可修改状态（品牌蓝描边 + 相机遮罩 + 「点击更换」提示），再点头像即可上传新头像（JPG / PNG / GIF / WEBP，最大 2MB），上传后全局生效（`data/celebs-avatars.json` 记录，`/api/celebs` 自动合并）；环上股票图标悬停显示小铅笔角标，点击即可上传替换对应股票图标（素材库全局生效：上传到 `/api/upload` kind=asset 并 upsert `US:代码`，复用素材库唯一标识，刷新不丢）。
4. 新增附件管理模块 —— 后端左侧导航「素材库」上方新增「附件管理」（`/attachments`，管理员可用），用于归档 13F 原始文件与解析结果及未来扩展文件。界面参考笔记类应用（Joplin）：工具栏含上传附件 / 新建目录 / 刷新 / 搜索，视图切换（目录浏览 / 平铺浏览 / 文档树，树形懒加载展开），统计（X 目录 / Y 文件）与排序（修改时间 / 名称），面包屑导航，文件行显示类型图标 / 名称 / 大小 / 修改时间，支持下载与删除（目录需为空或强制删除）。存储于服务端私有目录 `data/attachments`（不对外静态暴露，仅接口下载），接口 `/api/attachments` 支持列表 / 上传 / 新建目录 / 下载 / 删除，单文件上限 50MB，路径防穿越校验。
5. 13F 文件归档（人名 / 年份 / 季度） —— 将巴菲特（沃伦巴菲特）与木头姐（凯西伍德）的真实 13F 归档到 `data/attachments`：目录结构为「人名 / 年份 / 季度」（13F 为季度申报，季度从申报头 PERIOD 报告期解析，如 2026-03-31 → 2026/Q1）；每个季度下分「原始文件」（SEC submissions JSON、申报索引、13F 信息表 XML、完整提交件 TXT）与「优化数据」（持仓明细-实时行情 JSON：代码 / 名称 / 占比 / 实时现价 / 涨跌幅）。
6. 出站代理支持（本地代理） —— 国外数据源（SEC EDGAR / Dataroma / 名人头像）直连失败时自动走本机代理：默认 `http://proxy-host:8080`（本地代理 macOS 代理共享），可用环境变量 `STOCKLOG_PROXY` 覆盖（支持 `http://` / `socks5://`，设为 `off` 关闭）；代理端口探测结果缓存 60 秒，代理未开启或请求失败时自动回退直连，服务无需重启。新增 `lib/net.ts`（undici ProxyAgent）并接入名人持仓的 SEC EDGAR 数据管道，设置-关于技术栈同步登记。
7. 名人持仓 UI 重构 + 真实数据源 —— 卡片改为「单行一列」布局（每行一位名人，左侧数据右侧圆环）；圆环图重构为富途「聪明钱」同款：蓝色系分段环（8 档由深到浅）、悬停扇区沿中角弹出放大并高亮、其余扇区淡出，扇区外侧显示「公司 · 占比」工具提示，公司 Logo 落在对应分段上（素材库图标优先）、外围单字标签、中心为人物头像；新增 `/api/celebs` 后端接口：接入 SEC EDGAR 真实数据（13F-HR 机构持仓 InfoTable 解析 + Form 4 董监高持仓解析，名称→代码映射 + 腾讯行情补充实时现价 / 涨跌幅），24 小时文件缓存（`data/celebs-cache.json`），网络不可达自动回退示例数据，页面顶部与每位名人卡片展示数据源标识（SEC 13F / SEC Form 4 / 示例数据）。
8. 名人头像本地化抓取 —— 从国内图片源抓取 6 位名人（巴菲特 / 佩洛西 / 黄仁勋 / 木头姐 / 特朗普 / 段永平）公开头像，逐张经视觉模型校验人物身份后保存到 `public/uploads/celebs/`，圆环中心头像与详情页均使用本地图片，不再依赖外链。
9. 关于页数据源补全 —— 设置-关于-外部数据源卡片新增「SEC EDGAR / Dataroma」：SEC EDGAR JSON API 提供 13F 机构持仓与 Form 4 董监高持仓（名人持仓数据源），Dataroma 作为 13F / 13G / Form 4 聚合参考站点。
10. 版本记录弹窗重设计 —— 设置-关于-版本改为「总结 + 详细更新」：顶部版本总结卡片（版本号 + 日期 + 一句话总结 + 新功能 / 修复 / 安全 / 前端版本统计标签），下方按「新功能 / 修复 / 安全」分组展示详细更新（每组带色点、说明与条数，条目保留序号 + 箭头），前端版本清单置于最下方。
11. 版本统计标签可点击 —— 版本总结卡片的统计标签（新功能 / 修复 / 安全 / 前端版本）变为可点击：点击后平滑滚动到「详细更新」对应分组并短暂高亮（蓝色光环），hover 显示箭头提示。
12. 数据库定时备份 —— 设置-定时任务新增「数据库定时备份」：使用 better-sqlite3 在线备份生成一致快照（WAL 安全），每次备份保存 SQLite 数据库 + 素材库到 `data/backups` 时间戳文件夹；支持开关、间隔（每小时 / 每天 / 每周 / 每月）、保留份数（3 / 7 / 14 / 30）配置与「立即备份」；服务器运行且有访问时按计划惰性自动备份（60 秒节流），旧的备份自动清理。
13. 财报日历缓存提速 + 视图 URL —— 财报日历改为「读缓存直接显示」：财报日期每月基本固定，服务端月数据缓存从 30 分钟拉长到 12 小时并持久化到 `data/earnings-cache`（服务重启后冷启动秒读，实测 0.18 秒返回，此前需 5-50 秒），配合前端 localStorage 缓存；「回到本月」按钮改为透明底 + 细边框方案。前端自选股 / 我的持仓 / 财报日历 / 全球预览视图同步到 URL（`?view=watchlist` / `holdings` / `earnings` / `global`），刷新与前进后退保持当前视图。冒烟测试的大盘指数断言改为检查美股组（腾讯数据源），避免 CNN 恐惧与贪婪上游瞬时抖动导致的误报。
14. 财报日历深色修复 + 持仓加载态 + 市场 URL —— 修复财报日历深色模式下月份切换按钮 hover 变纯白圆块（改为主题化 hover：深色 `white/10`，并全局补充 `.dark` 下 `hover:bg-white` 覆盖）；「回到本月」hover 背景统一为深色。修复已登录用户在「我的持仓」刷新加载期间误显示「登录后展示…」占位的问题（改为「数据加载中…」，加载完成后展示真实持仓）。前端自选股 / 我的持仓的市场筛选（全部 / 美股 / 港股 / A股 及自定义市场）同步到 URL `?market=`，刷新与前进后退保持所选市场。
15. 自选股/持仓刷新秒出 + 财报徽标中性化 —— 自选股 / 我的持仓刷新不再闪现「数据加载中」：登录后首屏数据缓存到 localStorage（记录 + 迷你走势），刷新时先读缓存立即渲染，再后台刷新最新行情（实测 300ms 内出缓存行）。财报日历选中日期详情的「X 家财报」徽标由浅蓝改为中性深色（深色 `white/5` + 细边框），「+N 家」折叠提示文字也改为中性灰，深浅色协调。
16. 设置改为前端视图 + 访客自选 + 公开行情接口 + 财报已过滤提示 —— 前端点击「设置」不再闪现后台界面：改为预览内设置视图（网站设置 / 个人信息快捷入口卡片，点击才跳转后台）。未登录访客可直接搜索添加自选，保存在浏览器 localStorage（刷新保持，登录后数据在后台）；为支撑该功能，搜索 / 行情 / 走势 / 全球市值榜 / 月K 接口改为公开（纯市场数据，不含用户数据），未登录全球预览不再报「获取失败」。财报日历排查出 8 月 17 日「显示失败」的根因：数据存在但被默认市值筛选（≥$500亿）过滤，现对这类日期显示「N 家已过滤」提示，点击后详情说明可调整筛选查看。冒烟测试「未登录搜索」断言同步更新。
17. 财报日历市值筛选重构 + 前端市场按钮 hover 修复 —— 财报日历市值筛选默认改为「全部市值」（不再默认 ≥$500亿，中小市值财报默认可见）；市值档位参考券商口径重构为区间式：美股 超大盘 ≥$1000亿 / 大盘 $100-1000亿 / 中盘 $10-100亿 / 小盘 <$10亿，A股 超大盘 ≥¥5000亿 / 大盘 ¥1000-5000亿 / 中盘 ¥100-1000亿 / 小盘 <¥100亿（8 月 17 日财报默认即可见）。前端自选股 / 我的持仓市场标签 hover 增加品牌蓝背景效果（与后端市场胶囊一致）；「+」新增市场按钮 hover 由浅蓝改为中性深色（bg-gray / white/10），深浅色协调。
18. 财报日历日期背景中性化 + 今日自动定位 —— 财报日历日期格子背景不再使用浅蓝：选中日期改为中性白 / 深色（仅保留品牌蓝边框），有财报日期的 hover 由浅蓝改为中性灰，深浅色协调。打开财报日历（或点击回到本月）时自动滚动定位到当前日期，今日以蓝色圆点数字标记。
19. 财报日历选中边框改浅灰 + 今日详情自动展开 —— 财报日历选中日期的边框由品牌蓝改为中性浅灰（浅色 `border-edge`，深色深灰），不再有蓝色边框；打开财报日历自动定位到今日并置顶，同时自动展开当日财报详情（X 月 X 日 X 家财报 + 公司列表）直观展示。
20. 新增名人持仓模块 —— 后端左侧导航在「财报日历」下方新增「名人持仓」（`/celebs`），样式参考富途「聪明钱」：名人卡片左侧为名称 / 250 日涨幅（红涨绿跌）/ 近期操作（买入 / 增仓 / 建仓 / 减仓 / 卖出），右侧为圆环图（头像居中 + 持仓 Logo 环绕，圆环分段代表持仓占比）；点击进入详情页：250 日涨幅圆环 + 持仓明细（代码 / 现价 / 涨跌幅 / 持仓占比 / 目标价 / 涨跌距）+ 收益分析（近 1 / 3 / 5 年 + 组合 vs 标普500 走势）。内置 6 位名人示例数据（巴菲特 / 佩洛西 / 黄仁勋 / 木头姐 / 特朗普 / 段永平），Logo 复用素材库；导航缺失项插入逻辑优化为紧跟其前序导航键，名人持仓默认位于财报日历正下方。

### 修复

1. 名人管理三项优化 —— 1) 持仓明细股票图标与头像同款过渡动画（悬停放大 + 品牌蓝描边），悬停遮罩由铅笔改为相机图标；2) 加载提速：SEC 连通性探测由 3 次 × 5 秒收敛为 2 次 × 3 秒（不可达时约 3-6 秒兜底而非 15 秒+），SEC 请求重试上限降至 2 次；名人列表增加前端 localStorage 缓存（刷新先用上次真实列表秒出，再后台拉最新，不再闪现示例数据）；3) 「启用」开关改为苹果 iOS 风格：开启为苹果绿 `#34c759` + 白色圆形滑钮，关闭为浅灰/深灰轨道，平滑过渡，无多余文字。
2. 名人管理 1/2/3 项继续推进 —— 1) 启用滑块保持丝滑过渡，并按原要求去掉滑块旁的「启用」文字（滑块自身即状态标识）；2) 名人管理编辑表单头像鼠标划过增加动画：头像放大 + 品牌蓝描边 + 相机遮罩淡入；3) 持仓明细 / 近期操作表头与数据行居中对齐（占比% / 目标价也居中）；并恢复圆环悬停时扇区附近的持仓文字提示（「公司 · 占比」显示在扇区旁，随扇区位置上下浮动）。
3. 名人持仓圆环右移留白 —— 圆环（人物）列继续左移（卡片右侧留白 44px → 80px，实测圆环右缘距卡片右缘 81px），最右侧不再有被卡片/屏幕边缘遮挡的观感。
4. 前端刷新防“加载中/暂无”闪现 —— 登录后刷新首页自选股 / 我的持仓不再闪现「数据加载中…」与「暂无相关股票 / 暂无持仓」：1) 挂载时（绘制前 `useLayoutEffect`）直接应用本地缓存秒出数据，不再等 auth 检查完成；2) 加载态由文字改为无文字的骨架屏（SSR 与客户端一致，避免 hydration 问题）；3) `/api/records` 首次失败自动重试一次，且加载失败时保留上一次数据，不再被 catch 误置为空数组；4) 未登录访客也走同一加载流程（读本地访客自选），修复访客刷新空白。
5. 修复 SSR/客户端 hydration 不匹配 —— 首页筛选标签顺序（chipOrder）的初始值在 `useState` 里直接读 localStorage（服务端被 try/catch 吞掉返回默认、客户端读到已保存顺序），以及首页视图（view）/筛选（chip）初始值读 URL 参数，导致带参访问或本地有缓存时「server rendered HTML didn't match the client」。统一改为「初始值用默认 + 挂载后客户端副作用恢复」，服务端与客户端首帧一致；名人持仓的头像缓存同样改为初始默认 + `useLayoutEffect`（绘制前）应用，不闪现原始头像也不影响 hydration。
6. 移除扇形块状股票图标编辑 —— 去掉圆环扇区上股票图标的可修改功能（铅笔角标、点击上传、上传中状态），图标恢复纯展示（`pointer-events-none`，悬停扇区时随扇区淡出）；素材库中的股票图标仍全局生效；「名人持仓」标题右侧的头像编辑笔保留。
7. 修复嵌套按钮 hydration 错误 —— 名人持仓卡片原本整体是 `<button>`，内部圆环的股票图标编辑又是 `<button>`，触发「`<button>` cannot be a descendant of `<button>`」hydration 报错。卡片改为可点击 `<div>`（`role=button` + `tabIndex` + Enter/空格键盘支持），保留点击进入详情与悬停效果；控制台实测无任何报错。
8. 名人持仓四项优化 —— 1) 圆环（人物）列整体左移（卡片右侧留白 44px），悬停提示「公司 · 占比」不再超出屏幕（提示水平位置限位 18%-74%）；2) 鼠标划过扇区时沿扇区中角向外平滑弹出（320ms 缓动）+ 亮度增强，其他扇区淡出；3) 自定义名人头像写入 localStorage 缓存（`fire:celebs-avatars`），刷新先用缓存秒出，不再闪现原始头像；4) 股票图标径向位置按参考图「略偏外」微调（53px → 55px），角度仍在扇区中央。
9. 圆环中心头像放大跃出 + 中心净化 —— 参考图中「人物跃然于扇形之上」的效果：中心头像由内孔内的硬切圆改为放大至 66% 尺寸、边缘压住蓝色环内圈（浮于环之上）；头像边缘用极窄径向遮罩柔和渐隐并加深蓝色光环与投影，视觉上跃出有层次；移除原头像的深色背景块、圆形描边与阴影，中心不再有多余圆圈/波纹，头像（透明/深色底）与环融合更自然。
10. 名人头像编辑入口调整 + 图标精确居中 —— 头像编辑笔移动到页面标题「名人持仓」右侧（用户标注的位置）：点击铅笔直接弹出名人选择列表（带头像），选中名人即打开文件选择器上传，一次点击完成，去掉原来点头像右下角笔进入编辑态的两步流程；环上股票图标位置改为精确居中（角度取扇区弧度中点、径向取环带正中，实测 5 个图标距圆心均为 53px，与环带中心一致）。
11. 名人持仓圆环细节修复 —— 1) 去掉突出扇区最外缘的黑色线（原为外描边圆在突出段上切出的黑弧线，已删除内/外描边）；2) 悬停改为以扇区主体为交互对象，股票图标不可交互（`pointer-events-none`）且不再放大，避免图标变形；3) 图标缩小至环宽一半左右（16px，与原图一致）；4) 扇区颜色按「亮度与面积成正比」重设计：最大段亮蓝 `#2B5DE3`，随名次线性过渡到深藏青 `#0E1D47`，小段更暗、整体更贴近参考图。
12. 名人持仓圆环重构（对齐富途「聪明钱」） —— 圆环由「分段描边 + 圆角 + 间隙」重写为真正的扇形弧（SVG path，锐利径向边、无间隙、整体连续）；配色改为亮蓝→深蓝 6 级渐变（`#2B5DE3` → `#0E1D47`），最大持仓段最亮并向外突出（外扩+内收），股票 Logo 居中落在每段扇区（素材库优先、过小段不放置，与参考图一致），中心头像加深色底与环融合，环宽约 40% 更粗壮；悬停高亮/变暗与「公司 · 占比」提示保留。
13. 自选股移至导航首位（默认仍为我的资产） —— 后端左侧导航顺序调整：自选股排第一，账户资产（我的资产）紧随其后并保持为默认打开页（default 标记仍在账户资产上），刷新/直访默认进入我的资产。代码默认顺序（`lib/settings.ts`、RecordsApp、SettingsView）与持久化配置同步更新。
14. SEC 13F 解析增强 —— 修复 13F 明细表查找：优先使用申报主文档（`53405.xml` / `primary_doc.xml`），其次按 infotable / form13f 匹配，再兜底解析整份 `.txt` 提交件，覆盖 X02 新格式；持仓按市值从大到小排序并合并同公司多类股（如 Alphabet A/C 都归入 GOOGL）；SEC 请求加重试（最多 3 次）应对 WAF 间歇性断连。巴菲特的真实持仓（AAPL 34.9% / AXP 27.7% 等）与木头姐的方舟持仓（TSLA 19% / AMD 10% 等）已实测拉取成功并附实时现价。
15. 名人 CIK 硬编码 —— 经代理查询并硬编码 5 位名人的 SEC CIK（伯克希尔 0001067983、方舟 0001697748、H&H 国际投资 0001759760、佩洛西丈夫 Paul F. Pelosi 0001470019、黄仁勋 0001106277），跳过每次启动时的 CIK 反查（browse-edgar 不稳定）；CIK 反查逻辑不再限定 13F 类型，个人申报人（Form 3/4）也能查到。
16. 未登录静态行情补货币符号 —— 首页未登录时的自选股示例行情现价补充货币标识（港股 `HK$`、美股 `$`），与登录后显示一致。
17. 新增市场表单优化 —— 首页标签栏「+」新增市场表单的输入框字号由 15px 调至 13px、高度 44px 调至 36px，与周边按钮协调；占位文案精简（如 `JP` / `日股`）。

---

## v0.1.0 · 2026-08-04

> 首个里程碑：股票记录、行情、自选、全球预览、财报日历、素材库、日志、用户管理与安全加固（当日多次更新合并为同一版本）。

### 新功能

1. 设置子导航 ReactBits 化 —— 鼠标磁吸跟随 + 点击水波纹 + 苹果蓝选中态，切换无感不刷新。
2. 首页全球指数栏 —— 单支指数 5 秒轮换、长桥实心三角、56×20 迷你走势、素材库市场图标、悬停市场提示。
3. 首页指数设置 —— 网站设置新增可折叠卡片：指数增删改排 + 轮换时间 3-60 秒，保存即时生效；默认折叠展示（清除旧展开记忆），手动展开的选择仍会被记住。
4. 前端首页接入指数栏 —— 未登录也可见；`/api/ticker` 与 `/api/assets` GET 改为公开接口。
5. 版本记录体系 —— 设置-关于-版本点击弹出 ReactBits 风格卡片，按「前端版本 / 软件版本 / 新功能」分类，序号 + 箭头展示；版本以日为判断标准，同日更新合并为同一版本号。

### 修复

1. 自选股美股指数更新 —— 纳斯达克 100 更新为纳斯达克综合指数（usIXIC），名称与数据源同步。
2. 市场图标悬停优化 —— 去除原生 title 提示，统一为自定义提示框，深浅色协调。
3. 首页登录态闪现 —— 登录状态下刷新首页不再闪现「主题切换 + 登录」按钮：鉴权改为三态，检查中渲染空白占位，结果返回后再显示头像菜单或登录区。
4. 关于页保持 5 卡片布局 —— 前端框架 / 后端与数据 / 外部数据源 / 架构特性 / 版本，卡片未满暂不分页，后续增多再启用分页。
5. 美交所股票现价缺失 —— 腾讯搜索联想返回 `SPCH.AM` 这类带交易所后缀的代码，行情接口需要不带后缀的符号。修复后自动剥离 `.AM` 后缀（与 `.OQ` / `.N` / `.PS` / `.K` 同一规则），SPCH 等美交所股票可正常获取现价，已存的带后缀记录也自动兼容。
6. 不误伤带点号代码 —— `BRK.B` 这类点号属于股票名本身的代码不受剥离规则影响，行情保持正常。
7. 首页指数消失 —— 东方财富主行情主机（`push2` / `push2his`）对全球指数返回空数据。数据源切换至可用的 `push2delay` 主机，并保留原主机自动回退；美股 / 港股 / 新加坡 / 日经 / 韩国等 9 个指数全部恢复价格与迷你走势。
8. 总资产刷新闪变 —— 汇率缓存原为进程内存级，服务重启后首次请求会阻塞等待 Frankfurter 最长 15 秒并可能失败，期间港股 / A股 / 日股 / 韩股被按 1:1 误算成美元，导致总资产、当日盈亏等刷新瞬间出现错误数值。修复：汇率持久化到 SQLite（重启秒回）、兜底一律使用「上一次成功汇率」（内存 / SQLite / 本地缓存，从未成功过才显示占位而不是猜测值）、汇率接口补齐 JPY / KRW 并在后台异步刷新。

### 界面与交互

1. 首页预览卡片交互化 —— 修复卡片下边框与页脚紧贴（间距 0）的问题，调整为合适间距；侧边栏「自选股 / 持仓 / 行情 / 设置」改为可点击链接到后端对应模块并删除「日志」；「全部 / 港股 / 美股 / A股」标签可点击筛选且选中色块同步；登录后预览表格展示后端真实持仓数据（实时现价 / 涨跌幅 / 迷你走势 / 持仓盈亏，盈亏按正负着色），未登录仍展示静态示例。
2. 首页预览卡片改为内嵌模块视图 —— 「自选股 / 持仓 / 行情」在前端直接展示后端模块数据（不再跳转链接，设置暂保持跳转）：自选股 = 全部记录、持仓 = 有数量的记录（仅持仓视图展示「持仓盈亏」列）、行情 = 全部记录实时行情；「近5日走势」列固定 80px 不再过长；筛选标签改为与后端保存按钮一致的透明底 + 边框样式；列表每页 6 条并支持上一页 / 下一页分页；右上角「添加自选」恢复为内联搜索，选择后直接同步到后端并刷新列表；暗黑模式刷新白屏加固（主题防闪脚本移入 head 并直接设置 html 背景色）。
3. 首页搜索生效 + 视图调整 + 暗黑背景修复 —— 首页搜索框改为真实搜索（输入名称 / 代码即可搜索并加入自选，同步后端）；「添加自选」改为白色色块按钮（点击聚焦搜索）；侧边栏移除与自选股重复的「行情」，新增「全球预览」（全球市值榜，每页 10 条）与「财报日历」（置于持仓下方，展示后端财报预告，每页 6 条）；暗黑模式下网站背景图叠加深色遮罩，修复刷新后应用窗口周围出现大片浅色背景的问题。
4. 前端视图与后端模块对齐 + 暗黑白屏根治 —— 「持仓」改名「我的持仓」，该视图内按钮改为「添加持仓」；「添加自选」仅出现在自选股与我的持仓视图，财报日历 / 全球预览不再显示白色按钮；前端「全球预览」与「财报日历」直接复用后端组件（全球市值榜完整表格：排名 / 资产 / 价格 / 涨跌幅 / 月K / 市值 / 市场 + 货币切换，每页 10 条；财报月历：市场 / 市值 / 类型 / 时段筛选 + 月份导航 + 日历网格），数据与后端完全一致。暗黑白屏根治：主题写入 Cookie，服务端 SSR 首帧即深色（html 类 + 背景色），hero 遮罩改为服务端传入 initialDark 不再先渲染浅色，纯 SSR / reload 首帧实测全部深色。
5. 前端股票表增强 + 财报日历提速 —— 自选股 / 我的持仓表格新增「序号」列（跨页连续编号）；现价补充货币符号（$ / HK$ / ¥）；股票名称左侧新增股票图标（素材库「市场:代码」唯一标识，无图标回退首字母头像）；筛选标签（全部 / 港股 / 美股 / A股）可拖动排序（本地记忆）并加上市场国旗图标。财报日历优化：进入视图立即渲染日历网格（日期先行），财报数据异步填充并写入 localStorage 缓存（二次进入秒出），修复 StrictMode 下请求被取消导致财报不加载的问题。
6. 标签改用素材库市场图标 + 后端加号新增市场 —— 前端筛选标签不再使用 emoji 国旗，改为素材库市场图标（MarketIcon：素材库自定义图标优先，回退长桥圆形国旗 / flagcdn）；标签栏默认「全部 + 美股 / 港股 / A股」3 个市场，市场来自后端设置（自定义市场自动追加）；标签栏末尾新增后端「我的资产」同款虚线圆形加号按钮，点击展开内联新增市场表单（代码 + 名称），保存后同步到后端设置（markets / marketLabels）并即时显示新标签。

### 界面与规范

1. 保存按钮全局统一 —— 新增 `btn-line` 透明底细边框样式，全站 22 处保存类按钮统一，深浅色双适配。
2. 中性选中态统一 —— 分段 / 胶囊选中改为中性色（`seg-active`），替换全部浅蓝选中。

### 安全修复

1. 管理接口管理员鉴权（用户管理、数据库状态、设置写入、非头像上传、资产写操作等）。
2. 非管理员读取设置脱敏（隐藏 PostgreSQL 连接信息）。
3. SVG 上传消毒（拒绝 script / 事件属性 / javascript:）。
4. 首页导航链接白名单（`safeHref`）。
5. 登录 / 注册 / 改密限流，改密后其他会话失效。
6. 批量与长度上限（行情 100 条、搜索 50 字符、记录字段限长、行情代码白名单）。
7. 前端权限控制 —— 非管理员隐藏用户管理 / 素材库，设置页仅显示个人信息与关于。
8. npm 依赖审计 0 漏洞，补齐 `package-lock.json`。
