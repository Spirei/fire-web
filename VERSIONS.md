# Fire Fire · 版本记录

## 更新位置约定

- **UI 数据源**：`lib/versions.ts` —— 设置 → 关于 → 版本 弹窗展示的内容全部来自这里。
- **人工可读日志**：本文件（VERSIONS.md）与 `lib/versions.ts` 同步维护。
- **更新步骤**：每次完成新功能 / 漏洞修复 / 安全加固 / 技术栈变动后：
  1. **版本以「日」为判断标准**：同一天内的多次更新合并进当天版本号，不单独递增；跨过凌晨（新的一天）后的首次更新才开启新的版本号（v0.1.0 → v0.1.1 → v0.1.2 ...）。
  2. 在 `lib/versions.ts` 的 `VERSIONS` 中：若今天已有条目，把新变更追加到该条目的 `changes`；若跨天，则在顶部新建 `VersionEntry`（版本号、日期、摘要、前端版本、软件版本、变更列表）。
  3. 变更按类型标记：`feature`（新功能）/ `fix`（修复）/ `security`（安全）。
  4. 同步更新本文件对应章节。
5. 使用新框架 / 新库 / 新数据源时，同步更新「设置 → 关于」技术栈列表与 `frontend` 版本明细。

---

## v0.1.34 · 2026-09-18

### 安全

#### 登录二次验证（TOTP）
- 在设置 → 二次验证中扫描本地生成的二维码绑定验证器（密钥不经过第三方图片接口）。
- 输入 6 位数字后开启，并一次性给出 8 条备用码。
- 开启后网页与 iOS 登录在密码正确时只返回 5 分钟有效的 ticket，因此需要再提交验证码或备用码才能登录。
- 同一验证码不能重复使用；管理员重置密码会同时关闭该用户的二次验证，用户管理页也可以单独关闭。

#### 二次验证改为独立设置项
- 从设置 → 个人信息中拆分出来，在账号分组下单独成项，地址为 ?sub=totp。
- 个人信息仍然支持在改密时填写验证码。

#### 二次验证安全加固
- 备用码加长到 64 位熵并用数据密钥做 HMAC 存储，TOTP 密钥以 AES-GCM 加密后写入数据库。
- 登录 ticket 与绑定过程改存 SQLite，因此多进程不会丢失；同一账号只保留最新一张 ticket，验证码连续错 8 次即作废。
- 开启二次验证会注销其它会话；改密与管理员关闭二次验证都需要再次验证身份。
- 用户列表不再查询密钥字段。

### 新功能

#### 交易广场新增「新」角标
- 判断口径与作者卡片的未读数共用 lib/tradingSquareSeen.ts 的同一个纯函数，因此两处不会出现不一致。
- 角标按进入页面时的已读快照计算，所以浏览过程中不会消失。
- 评论时间同时改为相对时间（刚刚 / N 分钟前 / 昨天 HH:MM 等），点赞前增加拇指图标，二级评论页移除内层滚动条。

#### 评论按数量分流显示
- 不超过 10 条时在帖子下直接展开，超过 10 条时「评论 N」改为入口，点击后进入二级评论页（原帖 + 返回按钮 + 完整评论列表），因此一级页面保持简洁。
- 评论抓取从只取第一页改为评论超过 20 条的帖子最多取 3 页（60 条）。
- 同时修复评论头像与名字行没有对齐的问题。

#### 评论改为回复流样式
- 评论不再套用圆角卡片，改为主流社交网站（X / Reddit / YouTube）的回复流。
- 顶部一条细分隔线加「评论 N · 全部评论 ↗」，每条评论为「头像 + 名字 · 时间 + 正文 + 赞数」，楼中楼标出「回复 @某人」。
- 一级页面仍然只显示「评论 N」按钮，点击后才展开。

#### 交易广场新增帖子评论与评论者头像
- 新增 lib/tradingSquareComments.ts，用配置中的雪球 Cookie 抓取评论并按帖子 id 存入 data/duan-comments.json，评论数与缓存时间都没有变化时跳过。
- 一级页面只在帖子底部增加一个「评论 N」按钮。
- 同时修复评论者头像取不到、被引用的帖子没有作者头像、正文中的「回复@某人:」被清除后缺少上下文这三处问题。

### 修复

#### 版本弹窗的详细更新改为一行一条
- 详细更新原本用编号圆点加箭头标注，说明文字又是一个整段，与仓库版本记录「一行一条、前缀一横」的写法不一致。
- 现在每条记录的第一行是改动标题、行首一横，说明缩进一级并逐行列出，因此记录可以多行也便于扫读，内容不删减。

#### 修复深色模式下左侧导航滚动条常显
- 左侧导航的默认隐藏规则只写了一个类名，它与全站 `.dark *` 规则的特异性相同（都是 0-1-0）且位置在前，因此在深色模式下被覆盖成白色 26%，滚动条一直可见。
- 加上 `.dark` 前缀后恢复为默认透明，只有悬停与聚焦时才显示。
- 已用无头 Chrome 加载真实样式表验证修复前后的计算值，并在 AGENTS.md 中记录这条约定。

#### 后台页签恢复按需加载
- 持仓、自选股与资产分析仍然随外壳同步渲染（服务端首帧即有内容）。
- 其余页签改回 next/dynamic 按需下载，并且不设置 loading 挡板，因此刷新时不会再把已经画出的页面覆盖成「加载中…」。
- 首屏空闲后再预取其余页签，首次进入也更快。

#### 四色门图片压缩体积
- 窗户 window.png 保持 838×447 像素，只做无损压缩。
- 转盘 dial.png 从 2088×2068 改为 414×410（侧栏显示尺寸的 3 倍，覆盖三倍屏），使用 Lanczos 缩放并保留透明通道。
- 指针与手型图本来就小，未作改动。

#### 图表与浮动助手改为按需加载
- 核心图表包去掉 MapChart 与默认捆绑的 SVG 渲染器，全球经济热图单独走 lib/echarts-map.ts，因此进入持仓与自选股时不再加载世界地图。
- 浮动助手改为 next/dynamic，首屏空闲后再挂载，不占用持仓首屏时间。
- 图表与助手外观不变。

#### 未读分界标签改为压在帖间分隔线上
- 上一版的左对齐红色文字与横贯整行的红色细线，在深色模式下位于帖间两条分隔线之间，观感杂乱。
- 现在改用微信「以下是新消息」与 Telegram 的写法：把一行 10px 中性灰小字居中压在原有的帖子分隔线中间（容器高度为 0，标签用卡片底色遮住线条）。
- 因此分界处只有一条线，红色只保留在单条未读圆点上。

#### 新动态标记改为主流做法
- 单条新动态在最前面显示 6px 圆点（浅色 #e23d3d、深色 #ff8a8a，与头像未读计数同色）。
- 在新动态与上次看过的帖子交界处显示一次「以上 N 条为新动态」分隔线，因此不再逐条添加文字角标。
- 分界位置由 lib/tradingSquareSeen.ts 的 unseenBoundaryIndex 计算，整页都是新动态或全部看过时不显示分隔线。

#### 新动态角标改用站内未读色
- 原来的实心绿胶囊使用 #4caf58，这个颜色不在站内色板内，而且被 ml-auto 推到行尾后与发布时间之间留有大段空白。
- 改为站内未读色的软底小胶囊（浅色 bg-up-bg 配 text-up，深色 #3a1c1c 配 #ff8a8a），紧跟在发布时间之后。

#### 修复交易广场更新延迟与刷新回退到旧帖
- 段永平转发别人的帖子时正文只有一个 emoji 图片，被 mapDuanStatus 判定为空而丢弃，因此保留条件改为「正文 / 图片 / 引用 有一个就保留」，并在去标签前取出 emoji 的 alt（显示为 [很赞]）。
- 刷新时客户端曾用 localStorage 缓存整份替换服务端首帧数据，因此先显示旧帖再跳回新帖。
- 现改为合并，服务端首帧优先。

#### 修复特朗普动态不再更新
- 归档站改版后日期改为包在 <time datetime> 中、正文容器增加属性，两处旧正则因此静默失效，整页解析为 0 条。
- 现在日期优先读取 datetime 属性，正文改为按 div 深度配对提取。
- 单页超时从 4 秒放宽到 8 秒并按页容错，因此单页失败不会导致整次刷新没有任何写入。
- 实测缓存从 291 条增至 311 条。

#### 四色门两套样式各用各的音效
- 样式 2 珐琅盘使用八音盒音效（转动前 C-G-C 拇指琴、转动中五声音阶、停下时 C-G 五度）。
- 样式 1 原插画转盘改回棘轮声（三角波 690→410Hz，停下时为 G6 谐波钟加 4.2kHz 轻响）。
- 切换样式后下一次转动改用对应音效，挂钩与停止位置不变。

#### 发布状态热力图按全年提交计数
- 此前只统计最近 100 条提交，因此八月与九月前半被截断。
- 现在按页读取全年 main 提交：1–7 月没有提交，8 月 30–31 日共 73 次，9 月 1–18 日共 767 次。
- 格子外观仍与线上版一致，按周排列。

#### 交易广场正文清除抓取带入的页面文案
- 新增 stripTradingSquareChrome() 并在 normalizeTradingText() 中统一调用，因此「回复@某人:」「查看图片」「网页链接」这类链接文字不再作为正文显示（275 条段永平帖中有 171 条带该前缀）。
- 正文中间的「//@某某:」转发链与「[已修改]」等原帖标记保留。
- 判断是否为回复改为在清洗前记录 DuanPost.reply 标记。

#### 交易广场卡片不再自行移动到中间
- 面板由 mx-auto 居中并用 translate 偏移，位置保存在 localStorage。
- 原逻辑会把限制到内容区后的偏移写回存储，因此内容区变窄（更换显示器、窗口缩小）后卡片永久停在中间。
- 现在限制只作用于渲染，只有拖动结束才写入位置，并增加窗口 resize 与内容区 ResizeObserver 监听。

---

## v0.1.33 · 2026-09-17

### 新功能

#### 四色门新增样式 2，棘轮声跟随转动
- 原插画转盘保留为样式 1，珐琅金边转盘作为样式 2。
- 左上角四色圆标切换两套外观（偏好写入 fire:four-door-style），右上角仍是静音开关。
- 转动时按真实转角每 15° 发一记棘轮声，转速越快越密越尖，停止时补一声停止音；静音立即切断。
- 窗口、挂钩、手型光标与原有缓动时长不变。

### 修复

#### 四色门转动更顺，停止音改为叮
- 转盘旋转从每帧重绘的 drop-shadow 改为圆形 box-shadow，避免滤镜跟着旋转造成卡顿。
- 短转 820ms、随机长转 1760ms，缓动时间加长。
- 停止音去掉低频咚，改成 C7 高亮叮（三角波加泛音），棘轮声仍按转角触发。

#### 珐琅转盘红区箭头改回旧图样式
- 样式 2 红色扇区的箭头改为与样式 1 插画转盘相同的左指弯箭头：右端圆头、弧线呈笑嘴、左端实心三角。
- 箭头位于红区中下部。

#### 四色门一次转到终点，不再转回起点
- 随机转此前多转几圈后终点与真正打开的工作区可能不一致，转完又被 CSS 把 -810° 收成 -90°，看起来像倒转回起点。
- 现在随机转两整圈后直接停在目标色块，转完不加动画地把角度收进一圈。
- 阴影改在不旋转的槽上，挂钩不再跟着抖；棘轮声按缓动曲线推算，不再每帧读取样式。

#### 四色门音效变柔和
- 停止音去掉 C7 三角波与 4kHz 泛音，改成 G5 正弦钟（约 0.6 秒衰减、20ms 起音）。
- 棘轮声从 1.2–1.6kHz 降到约 540–620Hz，转得越快越轻越低。
- 增加 28ms 最小间隔与 2.4kHz 低通，因此一帧内不会叠成尖啸。

#### 四色门音效改成分三段的合成风铃
- 对照原片四色盘「开始转、转动中、停下」的听感重做合成，不取样原片。
- 起手是上行的 G-B-D 碎晶，转动中按五声音阶轮换风铃并保留余响，停止时加一段 E-B-G-D 下行收束，约 1 秒余响。
- 全部使用正弦与柔起音，避免出现干脆的电子哔声。

#### 四色门音效改为纸牌拨动感
- 风铃合成与原片相比仍有差距：原片的转动更像一叠卡片被快速拨过。
- 因此改为噪声带通的纸牌拨动：起手两下 fwip，转动中每 15° 一记沙沙（越快越密越薄），停止时一记卡片贴合加很轻的木质摩擦音。
- 不取样原片。

#### 四色门音效按原片频谱改为宽带脉冲
- 对原片转盘片段做了频谱：转动是约 10–12Hz 的竖直宽带脉冲（能量从几百赫兹到 8kHz），不是音高钟声。
- 合成改为短噪声闪击（中频 2.2kHz 加塑料感 5.2kHz），起手相隔 80ms 两记，转动最小间隔 65ms，以免连成拉链声。
- 停止时只加一记更实的拨动，去掉全部正弦叮；不把原片音频写入仓库。

#### 四色门音效改回风铃碎晶，对齐原片花园那段
- 宽带卡片版听感不如之前的风铃。
- 从原片「See that new color on the dial」的 02 秒处抽取音轨：转前是 4.1–4.4kHz 一串碎晶（约 40ms 一记），转动中是 2.4–3.8kHz 更轻的重叠风铃，停止时是 4.2→3.5→4.7kHz 下行收束。
- 合成改回正弦风铃三段（基音、微失谐、低八度），不再使用噪声拨片；不把原片音频写入仓库。

#### 四色门转动中保留五声音阶风铃滑动
- 转前碎晶与停止时的下行仍与原片花园那段对齐。
- 转动过程改回 G-B-D-E-G 五声音阶风铃（带轻八度），随转速变密变轻，不再使用偏高的碎晶叠音。

#### 四色门音效改回线上棘轮，停止音变柔和
- 风铃与纸牌几轮都不理想，因此音效先改回线上那套三角波棘轮（约 690→410Hz、高通 360Hz、45ms）。
- 转动仍按转角触发同一套滑动，转前补上线上那记 0.1 秒延迟的第一下。
- 停止时保留线上 G6/G7/D8 钟与 4.2kHz 轻响，去掉最刺耳的部分；样式 1/2、挂钩与终点不变。

#### 四色门音效改为珐琅八音盒
- 转前是 C-G-C 三记拇指琴加一口气声，转动中以上线棘轮作为齿轮底、叠加 C 五声音阶滑动，越慢琴声越长。
- 停止时是 C-G 五度打开后收到 E、C。
- 低通约 3.6kHz，没有刺耳的高频，也不是纸牌声；样式与挂钩不变。

---

## v0.1.32 · 2026-09-16

### 新功能

#### 助手接入站内实时数据，回答不再一律「我无法联网」
- 以前助手只会收到系统提示与对话本身：服务端算出的页面上下文与页面数据快照只被本地兜底回答使用，模型那边一条都没有拿到，因此问「最近有什么大新闻」只能回答「我没有联网、没有你的页面数据」。
- 现在新增 lib/assistantLiveData.ts，把站内本来就实时抓取的数据整理成快照随提问一起下发：交易广场最近动态（特朗普 Truth Social 与段永平雪球，含中文译文摘要、作者与时间，可当作站内新闻源引用），以及我的持仓与自选行情（原币现价与涨跌幅，仅在助手「账户数据」范围内且提问与行情相关时才抓取，2.5 秒硬超时、失败即跳过）。
- 同时把页面、个股、筛选、页面数据快照、账户记录数组成「站点上下文」块，作为独立 system 消息真正发给模型，并明确引用口径：可据此回答并注明抓取时间与来源，块外仍不得编造，也不得声称联网搜索过；问不到时直接说明「站内暂无可用的××数据」并指向对应页面。
- 没有配置大模型时的本地兜底回答同样会把交易广场动态展开回复，不再只回一句「大模型尚未配置」。

#### 助手回答支持表格与可点链接
- 回答渲染此前只识别标题、列表、代码与粗体：模型最常用来报数字的 Markdown 表格会整段以裸竖线显示，正文里的链接也只能当纯文本看。
- 现在把 Markdown 解析抽到 lib/assistantMarkdown.ts（纯函数，便于回归测试），补齐表格、引用块、分隔线三类块与链接、行内代码、粗体三类行内样式：表格带表头与隔行底纹，外层可横向滚动，手机上也不会把对话撑宽。
- 链接只在 http/https 协议下渲染为可点链接（javascript: 与 data: 一律按纯文本显示），沿用交易广场的雪球蓝并带下划线，在新标签页打开。
- 回归用例覆盖表格解析结果、非法协议被拒绝，以及「渲染层确实接上了这两类块」。

#### 长对话补上「回到最新」并收紧回答长度
- 对话变长后向上翻看时，此前没有回到末尾的入口，只能拖动滚动条。
- 现在消息列表在离开底部时显示一颗「回到最新」胶囊（贴底状态由滚动、发送、切换对话统一维护，只在状态真的翻转时才触发渲染），点击平滑回到底部并自动隐藏。
- 系统提示同步补上回答风格约束：默认短、先给结论、整套控制在 8 行以内、不要复述问题、不要用免责声明开场、不要罗列能力清单、需要比较多组数字时使用短表格或一行一项的短列表。

### 修复

#### 智能助手分隔线与资产分析同色
- 智能助手工作台里侧栏与对话区之间的竖线此前由两层叠加：侧栏自带的 1px 边框与可拖动分隔条，重叠处比资产分析那根更亮，并多出半像素宽度。
- 现在 768px 以上把分隔线完全交给分隔条绘制：浅色 rgba(122,132,146,.24)、深色 rgba(255,255,255,.15)，与资产分析的分隔条取值相同，悬停变宽变蓝的交互保留。
- 手机端侧栏是覆盖式抽屉，仍然保留自身边框。

#### 智能助手对话列收窄居中
- 宽屏下对话正文此前横向铺满整个工作区，单行过长，阅读吃力。
- 现在消息列收窄到 768px 并水平居中，底部输入卡片与用量统计行同步收窄到同一宽度，三者左右对齐。
- 轨迹视图仍按原设计通栏铺满，手机与窄屏不受影响。

#### 轮次短线对齐主流大模型的对话轨道
- 对话右侧的轮次短线按「从上到下由旧到新」排列，但打开对话时高亮固定在第一条（最上面），看起来像最新一轮在上面；现在打开对话或新增消息后，高亮自动移到最新一轮。
- 样式与动效参考主流大模型软件的对话轨道：右对齐短横线，每根长度按 8 根一组变化（12–20px），当前轮更亮且只比邻条长 4px（悬停多 8px），静止时不动，只在悬停与切换时走 150ms 过渡。
- 悬停预览同步收窄：卡片由 360px 单行改为 260px、正文两行截断，字号与内边距收紧；预览从按钮左侧展开，轨道不做裁剪。

#### 交易广场刷新先出内容、头像不再丢
- 交易广场此前完全依赖浏览器本地缓存与接口返回：缓存不可用（无痕、配额满）或接口较慢时，刷新会先渲染一整屏骨架，两位作者的帖子头像也要等接口才出现。
- 现在把服务端已有的帖子快照（每位作者最近 40 条，含中文译文与本地化图片）随首屏下发，刷新时列表与头像立即就位。
- 名人头像同时在 HTML 阶段并行预加载，不再先显示首字母；接口返回后照旧合并最新内容并写回本地缓存，筛选、翻页与未读角标逻辑不变。

#### 上传素材改为先缓存、再静默更新
- 名人头像、股票图标等上传素材此前带 Cache-Control: max-age=0，每次刷新都要重新校验或重新传输（名人头像单张接近 1MB），页面上会先显示首字母占位。
- 现在 /uploads/* 统一返回 public, max-age=3600, stale-while-revalidate=86400：一小时内直接复用浏览器里上次成功加载的副本，刷新瞬间显示图片；过期后先显示旧图、后台静默校验，有更新再自动替换。
- 上传文件名带时间戳，更换头像本身也会更换地址；静态命中与运行期上传两条服务路径都已覆盖。

#### 图片组件不再先闪一次占位
- SafeAssetImage（导航、市场、货币、股票图标与名人头像统一使用的图片组件）此前把 <img> 初始设为 opacity-0，等 onLoad 后才切换为可见，即使图片已在浏览器缓存里也要多等一帧。
- 现在图片始终参与绘制、占位垫底：命中缓存时首帧就是真图，尚未加载完成时图片透明、占位自然显示；加载失败仍回退占位，alt 为空不会出现破图图标。
- 交易广场与名人持仓的名人头像预加载同时加上 fetchPriority=high，尽早开始取图。

#### 交易广场股票提及改为雪球样式
- 正文里的股票提及按雪球习惯渲染为蓝色链接 $名称(代码)$：原文里的「西方石油」、单独的「OXY」，以及雪球原文自带的 $西方石油(OXY)$，统一显示为 $西方石油(OXY)$，颜色取雪球蓝 #3774e0（深色模式 #5b91f0）。
- 原有的虚实线下划线设定保留：线下划线的颜色仍按实时涨跌显示绿或红，未取到行情时使用品牌灰。
- 悬停只提亮文字，不改变虚线下划线，点击仍打开行情卡片与个股详情。

#### 交易广场刷新不再闪掉筛选行
- 交易广场的筛选状态（看谁 / 分类 / 页码 / 个股）此前只写在 URL 里，刷新时服务端首帧只能画出「全部动态」，水合后才补上「段永平 + 全部 / 热门 / 原发 / 长文」那排标签。
- 现在把同一份状态镜像到 cookie（fire_trading_square_filter），服务端首帧就用它渲染，标题、条数、标签行、页码与个股详情刷新后原位不变。
- URL 仍是最终依据，cookie 只作为首屏提示，清除 cookie 即回到默认。

#### 修复股票提及误匹配与重复链接
- 特朗普那条帖子的中文译文里「纽约州总检察长和曼哈顿地区检察官」被识别成港股「长和(00001)」，正文凭空多出一个股票链接，根源是两字中文名匹配时没有边界判断。
- 现在两字中文名要求左右至少一侧不是汉字才算提及：「我买了长和」「长和(00001)」照旧命中，「检察长和曼哈顿」不再命中；雪球原文的 $名称(代码)$ 写法不受影响，四字及以上中文名保持原行为。
- 同时修掉裸写「名称(代码)」会连出两个重复链接的问题：相邻且同标的的名称与代码命中会合并为一个提及，并去掉多余的右括号，输出恒为 $长和(00001)$。

#### 启动脚本自检挂载目录权限，种子失败不再阻塞启动
- 线上（群晖）升级到非 root 镜像后出现过容器无限重启：DATA_DIR / UPLOADS_DIR 是宿主机 bind mount，属主不是容器内的 uid 1000（node 用户），启动脚本写公开缓存种子时报 EACCES，而 entrypoint 带 set -e 会直接退出，被 restart: unless-stopped 反复拉起。
- 现在 entrypoint 启动前先自检两个挂载目录的写权限：数据目录不可写时打印明确的中文提示（含 chown -R 1000:1000 data uploads 这行命令）后再退出；上传目录不可写只告警，服务照常启动；公开缓存种子合并失败一律只告警。
- 部署文档补上「挂载目录属主必须是 1000:1000」的前置步骤，以及「升级后容器无限重启、日志只有 EACCES」的排查章节。
- 部署一致性审计改为校验「镜像内路径必须被启动脚本引用」这一不变式，并补了一条回归用例同时覆盖「数据目录不可写必须报错退出」与「种子失败不阻塞启动」。

#### 四色门侧栏素材随仓库与镜像提供
- 四色门侧栏依赖的 4 个素材（窗户 / 转盘 / 指针 / 手型光标）此前排除在仓库与镜像之外，只在开发机本地存在；线上或新克隆环境里 window.png 会返回 404 并触发组件的 onError 回退，侧栏入口先闪一下再整块消失。
- 现在把这 4 个文件纳入版本管理（.gitignore 精确放行，其余 uploads 与二进制素材仍然禁止入库），构建上下文与镜像一并携带：.dockerignore 同步放行，进入 /app/resource-default 后由 entrypoint 播种到 uploads 卷，老部署重建容器即可补齐。
- 公开仓库审计对这份白名单放行，其他规则不变；ASSETS.md 与群晖部署文档同步说明。未使用的 room.png 仍留在本地不入库。

#### 弹窗底部按钮统一为胶囊样式
- 弹窗底部按钮此前只挂了 .btn（纯布局）与一个并不存在的 btn-secondary 类，取消按钮因此没有任何底色与文字色，深色模式下几乎不可见；删除类确认按钮也缺少内边距。
- 现在在 globals.css 制定共享的弹窗胶囊配方：.dialog-actions（右对齐 + 10px 间距）与 .dialog-btn 三档语义 —— ghost（取消 / 次要，透明底、主题自适应文字色、悬停浅灰）、neutral（确认 / 保存，白底浅灰边框，深色模式深灰底白字）、danger（删除，实心红底白字），统一 38px 高、全圆角。
- AppDialogHost、导入截图弹窗、名人管理弹窗、卡包与卡面弹窗以及助手「重命名 / 新建空间」弹窗全部改用这套配方，深浅色对比度一致；同时比对源码用到的 btn-* 类与 globals.css 定义，确认不再有「用了但没有定义」的按钮类。

#### 修复上传素材在运行期 404（模型服务图标上传不生效）
- 安全加固时给 /uploads/[...path] 兜底路由加错了判断：非 reports 的路径被要求必须位于镜像内置素材目录（resource-default）之内，而用户新上传的素材（模型服务图标、头像、卡面等）写在 uploads 卷里，于是静态命中不到时一律被判非法并返回 404，表现为上传提示成功、图标始终是破图。
- 现在兜底路由只在「回退到镜像内置素材」那一支校验 resource-default 归属，主分支只保留「必须仍在 uploads 卷内」的越权防护，reports 仍不参与回退。
- 回归用例：上传 PNG 素材后经该路由取回必须是 200 与 image/png，缺失文件仍为 404。线上更新到本版后，此前上传成功却显示破图的图标会直接恢复，无需重新上传。

#### 修复局域网 HTTP 下删除对话报 crypto.randomUUID 不存在
- 弹窗模块（appDialog）此前用 crypto.randomUUID 生成请求 id，而这个 API 只在安全上下文（https 与 localhost）提供：通过局域网 HTTP 地址访问时，助手侧栏「删除会话」等操作会直接抛出 TypeError: crypto.randomUUID is not a function。
- 现在新增 lib/randomId.ts 统一生成客户端随机 id：使用任何上下文都可用的 crypto.getRandomValues，没有 crypto 时退回 Math.random，输出恒为 24 位小写十六进制，因此 ac-<24hex> 这类服务端格式校验继续成立；appDialog 与助手的会话 id 都改为使用它。
- 回归用例除模拟非安全上下文（randomUUID 缺失、getRandomValues 抛错）外，还会扫描全部「use client」文件，禁止再出现 crypto.randomUUID(。

#### 智能助手窄屏适配打磨
- 手机与平板（<1024px）下助手工作区高度此前写死 calc(100dvh - 112px)，而窄屏顶部真实占用是顶栏 60px（手机）/ 72px（平板）+ sticky 移动端标签栏约 56px + 下边距 14px，面板底部连同输入框会被推出可视区。
- 现在改为 flex 撑满「顶栏 + 标签栏」之外的剩余高度（app-shell-main → records-app → records-content → assistant-page → assistant-workspace 全链路，覆盖 :has 选择器并在手机上按 60px 顶栏口径补 min-height），此后顶栏或标签栏高度调整都不需要再同步常量。
- 手机上还把输入区与消息列表两侧留白由 24–31px 收到 16px，与输入框左右对齐；「轨迹」工具栏按钮与搜索框在窄屏会超出边界，改为整条横向滚动、不换行不裁剪。
- 触屏没有悬停，轮次短线轨道（仅悬停出预览、还会覆盖正文）在 <768px 隐藏；对话超过 24 轮时轨道只保留最近 24 条，避免垂直居中的轨道把最新一轮挤出可视区。

#### 浮动入口补齐管理类页面上下文
- 用户管理 / 附件管理 / 日志此前不在助手页面白名单里（客户端 PAGE_COPY 与服务端 normalizeAssistantContext 都缺少），在这些页面打开浮动助手只会得到「当前页面上下文为空」。
- 现在两处白名单都补上这三个页面，并各自给出页面数据快照：附件管理（根目录文件与目录数、最近 5 个文件）、用户管理（用户总数与角色分布，不含任何凭据）、日志（最近 5 条安全事件的时间与类型）。
- 同时补上交易广场 / 全球经济 / 设置的快捷提问，让浮动入口在这些页面也能直接问出有用的内容。

#### 修复线上 FIRE 页面「小丑鱼不见了」
- 线上 FIRE 页面那对游动的小丑鱼（父子鱼）与礁石贴图整体消失，页面本身不报错。
- 根因是插图 / 字体 / 图标 / 分享图按素材授权约定既不进 Git 仓库也不进镜像（.gitignore 与 .dockerignore 排除），而 Compose 只挂了 data 与 uploads，部署素材没有进入运行时的通道，线上 /images/fire/clownfish-family.png 必然是 404；本地 dev 直接读源码目录下的 public/，所以本地一切正常，容易误判成代码问题。
- 现在 Compose 补上四个只读挂载（./images、./fonts、./icons、./share → /app/public/*，可用 IMAGES_DIR 等变量覆盖），启动脚本在素材缺失时打印一行中文提示，服务照常启动。
- 部署文档新增「1d. 挂载插图 / 字体 / 图标素材」与「FIRE 页面的小丑鱼不见了 / 图片、字体、图标 404」排查章节；部署一致性审计把四个挂载与启动提示列为必需项，回归用例覆盖「缺失时有提示、补齐后安静」。

#### 回退持仓盈亏图的排版与配色改动
- 按要求把「我的持仓 → 持仓盈亏图」这一轮的改动整体回退到调整前的状态：删除按宽度自适应的排版模块（lib/sankeyLayout.ts）与图表配置模块（lib/sankeyModel.ts），components/HoldingsPnlSankey.tsx 与 app/globals.css 恢复为原版本。
- 手机端回到「左右各留 82px、柱宽 10px、两行 9px 标签、画布高 392px」的原始排版，配色仍是纯色盈利红 #e5484d / 亏损绿 #0aa77d / 中间汇总紫 #7567b9，以及单色半透明连线（曲率 .5、透明度 .55）。
- 上一轮为此新增的 4 条版本记录（宽度自适应、渐变观感重做、去压扁、柱体加宽）与 2 个回归用例一并撤销，回归套件回到 27 条；工作区与调整前的提交逐文件比对已无差异。

#### 恢复相关 ETF 角标为改动前的样式
- 按要求撤回这一轮对角标的全部改动，恢复到用户提出「角标太压抑」之前的状态：components/EtfDoubleBadge.tsx 还原为原来的 19px 深灰实心圆角标（#4b5563 底、8px 加粗白字、1.5px 表面色描边、轻投影，定位在图标右下 -bottom-2 -right-0.5，2x 保留小写 x）。
- 8 个页面共 10 处调用点也回到「标记放在图标容器内」的写法；期间尝试过的浅色小胶囊，以及「名称下方代码行内联标签」两版一并撤销，回归用例里针对内联标签的那条断言同步删除。
- 工作区与提出该需求前的提交逐文件比对已无差异，只剩这条版本说明。

#### 相关 ETF 圆点改年轻配色（形状不变）
- 按要求保留现有 19px 圆形角标（右下错位、白描边、8px 加粗白字），只改配色，去掉偏压抑的深灰 #4b5563。
- 两倍做多与两倍做空都显示 2x，用颜色区分：做多西瓜红 #ff4d78、做空薄荷绿 #12c48b；1x/1.5x 反向仍为「反」（同绿色）、收益橙 #e8890c、非 2x 做多蓝 #3b93ff，投影改成同色轻晕。
- 调用点与尺寸位置不变。

### 安全修复

#### 补齐公开历史里的个人标识清理
- 上一轮公开仓库清理只覆盖工作区与部分规则，本轮把可达历史补齐：版本记录、部署文档、构建说明与用户表迁移脚本里残留的个人标识（GitHub 账号与镜像路径、登录名、本机代理端口与本地代理软件名、NAS 硬件型号）统一替换为中性占位。
- 本地与远端 main 的提交历史一并重写，旧提交不再被任何分支或标签引用；仓库对象库从 1.3GB 降到 16MB，本地 2257 个可回收对象与 13 个游离对象全部清除。
- 工作区文件、提交信息与全历史 blob 扫描均无命中；公开仓库上清理前上传的 44 个构建记录（Actions artifact）已删除，容器镜像包仍不可匿名拉取。
- 遗留说明：GitHub 对不再被引用的旧提交可能短期保留按 SHA 的缓存视图，需要时可向 GitHub Support 申请清除。

---

## v0.1.31 · 2026-09-15

### 新功能

#### 四色门工作区入口迁入主站
- 窗户、转盘、固定指针、手势与声音开关迁入桌面端主导航顶部。
- 账户资产、资产分析、FIRE 与全球经济可从色块、转盘或原导航进入；转盘使用连续缓动，并同步棘轮声与停止提示音。
- 静音偏好通过全站偏好机制持久化，关闭时立即停止当前声音，重新开启不会自动试听。
- 素材限制在侧栏内部，窄高窗口自动缩放，其余导航继续滚动可达。

#### 业务页面恢复原有内容结构
- 移除业务内容上方重复的页面名称、分隔线与四色门状态顶栏，各页面直接从自身内容开始，保留持仓宽表、资产分析分栏与管理页面的原有空间。
- 矮窗口补足手势与侧栏分隔线的间距。
- 智能助手按顶部栏以下的真实视口高度自适应，助手页不再渲染站点页脚；桌面端与移动端都完整保留对话与输入区域。

### 修复

#### 四色门音效按钮去掉绿色状态标识
- 移除音效开关右下角的绿色状态圆点（含静音时的灰色圆点）。
- 同时取消开启态的绿色基调：深色模式下按钮不再有绿色背景块与绿色描边，浅色模式也不再使用墨绿色图标。
- 开启与关闭仍通过喇叭图标区分（关闭为带叉喇叭），深浅色表现一致。

#### 智能助手工作区与左侧导航等高
- 桌面端智能助手页面此前把工作区高度固定在 680px，窗口较高时右侧面板下方会空出一大块，与左侧主导航底部对不齐。
- 现在 1024px 以上改用与左侧导航相同的视口高度（100vh - 112px，顶部起点同为 88px），两侧面板上下边缘对齐，对话区与输入框随窗口高度自适应。
- 对话过长时仍在面板内部滚动，不产生整页滚动；手机与平板宽度保持原有紧凑高度。

#### 主导航分组名改为「资产」
- 左侧主导航第一个分组标题由「工作区」改为「资产」，与分组内的持仓、资产分析、自选股、FIRE、全球经济保持一致。
- 下方「管理」分组不变，智能助手侧栏的对话分组标题不受影响。

#### 深色模式智能助手输入框配色修正
- 深色模式下全局表单聚焦填充（#151a26 蓝黑）会覆盖智能助手的输入框，工作台底部的输入框因此变成与卡片底色（#2b2b2b）不协调的深蓝色块。
- 现在助手内的输入控件聚焦时保留自身底色：底部输入框与卡片一致，带独立底色的会话改名与空间名输入框、模型编辑器字段维持各自配色。
- 悬浮面板与深色助手主题一并生效，浅色模式不受影响。

#### 用户消息胶囊统一为发送按钮绿
- 对话里的用户消息气泡改用与发送按钮相同的实心绿（#4caf58），描边同色并保留同色轻投影，文字改为白色，与按钮上的白色箭头呼应。
- 编辑消息时的取消与保存图标同步改为白色系。
- 工作台与悬浮面板、深浅色模式表现一致，并显式覆盖助手自带主题里把用户气泡固定为灰底浅字的两条规则。

### 安全修复

#### 公开仓库安全加固与素材分离
- SVG 使用 XML 解析白名单，上传路径与真实图片格式校验，请求体流式限额，财报权限保护。
- 首次安装令牌，容器以非 root 运行（保留图片优化缓存的写入权限），兼容历史清理后的提交检查。
- 代码采用 MIT，公开仓库不再包含部署素材，由部署者自行提供。

---

## v0.1.30 · 2026-09-14

### 新功能

#### 智能助手补齐附件、空间与调用审计
- 图片支持「仅本次」与「持久保留」两种模式；保留的文件进入用户隔离的私有数据目录，只能通过登录鉴权接口查看，删除归档时同步清理记录与文件。
- 对话可归入自建空间。
- 新增模型调用账本，记录实际服务、模型、状态、延迟、错误与提供商返回的 Token；归档内展示调用与失败统计，价格未知时不虚报费用。

#### 智能助手补齐模型控制与流式对话
- 输入区新增自动或指定模型选择、三档数据发送范围与临时对话。
- 兼容 OpenAI SSE 的服务逐段显示回答并可随时停止，不支持流式时继续使用完整响应与顺序回退。
- 每条回答显示实际服务、模型及是否发生回退，并支持复制、重新回答、编辑问题后建立分支与导出当前对话。

#### 智能助手增加可控记忆与服务健康状态
- 对话归档内新增默认关闭的跨对话记忆，可查看、编辑或一键清空；只有开启后才把用户填写的记忆注入模型上下文。
- 新增模型目录接口与进程内健康状态：正常问答和连接测试都会记录最近成功、失败、耗时与检查时间，为自动回退和故障排查提供依据。

#### 智能助手图片支持点击预览
- 待发送图片的缩略图支持点击或键盘打开全屏原图预览，点击遮罩、关闭按钮或按 Escape 可退出。
- 删除按钮继续独立工作，不会误触预览。
- 预览层通过顶层 Portal 展示，适配手机端与深浅色页面。

#### 智能助手支持多模态图片提问
- 回答输入框支持粘贴图片、拖拽图片与文件选择，发送前展示可单独移除的缩略图。
- 图片以 OpenAI 兼容的 image_url 内容随当次问题发送给多模态模型，不写入长期对话数据库。
- 接受所有 image/* 格式，不限张数，单次提问的图片总量上限为 100MB。

#### 收敛智能助手配色与加载动画
- 用户消息、归档选中态与回答加载改为冷调浅灰，仅保留品牌图标的小面积青色点缀。
- 发送按钮参考 ChatGPT：可发送时显示绿色圆形上箭头，生成中切换为可点击的白色停止方块；回答中输入新内容会立即恢复上箭头，提交时停止当前请求并发送新问题。
- 三点动画改为 1.8 秒低幅度缩放节奏。

#### 重新设计对话归档
- 对话归档改为占满助手内容区的独立页，移除无意义的左侧黑色遮罩。
- 新增对话数量、搜索、当前会话状态与克制的空状态，删除操作收起到列表行内，并同步适配深浅色。

#### 账户助手更名并适配深浅色
- 全站入口、面板标题与无障碍标签统一由「账户助手」改为「智能助手」。
- 面板、归档遮罩与抽屉、会话气泡、错误与操作卡片、输入发送区补齐深色配色，浅色模式保持原有白色视觉。

#### 账户助手打开后保留入口
- 账户助手面板打开后，页面底部原来的圆形入口继续保留在用户放置的位置，并提高到遮罩之上保证可点击。
- 再次点击入口可直接收起面板，拖动位置继续持久化。

#### 对话历史改为归档并长期保留
- 账户助手标题栏始终显示历史对话与新建按钮。
- 历史抽屉改为对话归档，不再按数量截断会话，已有和后续会话全部保留，可单独删除。
- 单个会话仍限制消息数量与大小，避免异常内容占用过多空间。

#### 账户助手入口与面板支持自由移动
- 关闭状态的圆形入口与打开后的账户助手面板均可直接拖动：入口拖动不会误触打开，面板通过标题栏移动且标题栏按钮仍可正常操作。
- 两种状态分别按用户保存在浏览器，刷新和开关后恢复上次位置；视口尺寸改变或保存位置越界时自动收回可见区域，手机与桌面都可使用。
- 标题栏新增持久化图钉，置顶后点击面板外部不会收起。

#### 模型配置增加逐项连接测试
- 每个模型行新增独立测试入口，直接显示连接成功耗时或可理解的失败原因，保存前即可确认 API 地址、密钥与模型 ID 是否匹配。
- 测试接口仅管理员可用，限制频率与请求大小，已保存密钥由服务端按服务 ID 读取，不会回传浏览器；自定义图标可一键恢复提供方默认图标。

#### 模型服务支持自定义图标与多级回退
- 模型服务从单项配置升级为有序服务列表：每个提供方可自定义显示名称并上传本地图标，一个服务可添加多个模型 ID。
- 服务与模型都能调整优先级，第 1 项调用失败、超时或返回空内容时，账户助手与内容翻译会依次回退。
- 新增服务默认作为其他 OpenAI 兼容提供方，不再把「自定义服务」当成固定品牌；密钥逐服务保存且只向浏览器返回配置状态，旧版单模型配置首次编辑时无损迁移。
- 素材清理会保留仍被模型服务引用的图标。

#### 设置页重塑为模型服务
- 将容易误解为单一翻译能力的「翻译配置」更名为「模型服务」，并独立归入智能服务。
- 页面按 DeepSeek、OpenAI 与自定义 OpenAI 兼容服务展示品牌图标、配置状态、当前模型与使用范围；编辑态改为提供方选择卡、API 地址、模型 ID 与密钥保护状态，手机端自动改为单列。
- 模型配置与股票数据源分开保存，服务端同时限制提供方、URL 协议与模型 ID 长度。

#### 新增股票优先从 TradingView 自动补图
- 新增自选或持仓后，如果素材库没有该证券图标，会在后台查询对应 TradingView 标的页，读取页面实际使用的 s3-symbol-logo 官方 SVG，完成安全校验后保存到本地素材库并刷新全站图标。
- 升级前已经加入且仍显示首字母的股票也会以双 worker 在后台逐只补齐；已有手动图标不会被覆盖，TradingView 不可用或无图时继续回退微牛与 Parqet，补图失败也不阻塞股票添加。
- 美股、港股、A股、日股与韩股均支持；普通用户仅可触发缺图补全，素材库的显式修改仍限管理员。

#### 新增全站上下文账户助手
- 全站右下角新增账户助手：桌面使用不遮挡主内容的右侧浮动面板，手机使用底部面板。
- 自动识别账户资产、资产分析、资产总盈亏、自选股、FIRE、财报日历、卡面库与素材库等当前页面，并提供对应快捷问题。
- 服务端复用设置中的大模型提供商、API 地址、模型与密钥，只向模型提供当前用户的受控持仓摘要，密钥不会下发浏览器，模型不能直接读写数据库；未配置模型时仍可在本地完成持仓概览与基础数据诊断。
- 回答明确区分事实、限制与下一步。

#### 账户助手支持页面操作、分组整理与交易预览
- 可用自然语言打开功能页、筛选美股/港股/A股自选，创建自选分组并将多个代码批量移入指定分组。
- 买入或卖出指令先生成包含股票、数量、价格与预计金额的结构化预览，用户再次确认才会写入订单；分组移动与交易入账完成后均可在原对话中撤销，底层复用现有归属校验、持仓重算与订单账本。
- 账户分析补充按市场集中度、重复代码、资料缺项及超过 7 天未更新记录的诊断；分组操作后会即时刷新行情板。同时修复助手跳转到市场筛选后刷新出现首帧跳动与 hydration 警告。

#### 账户助手支持多会话历史
- 每次新对话生成独立会话 ID，服务端按用户最多保存 20 个会话并自动迁移旧版单会话历史。
- 标题栏新增历史图标，抽屉中展示标题、短 ID 与更新时间，可切换或删除单个会话；新建对话不再清空旧记录，刷新后恢复最近使用的会话。
- 发送按钮参考素材库市场徽标改为浅色圆盘与青色核心点缀，可发送时显示低频扩散光环，悬停时箭头轻微上移，并尊重系统减少动态效果设置。

#### 智能助手新增 Harness 风格模型轨迹
- 参照 DeepSeek Harness 开源源码移植完整助手界面：独立工作台采用固定会话侧栏、单一会话画布与底部输入坞。
- 输入框复刻 22px 圆角、浮层模型与数据菜单、工具行、运行统计、状态按钮及弹出动画；等待模型首段输出时以低对比度文字流光呈现思考状态，正文开始输出后自动消失，并适配深浅色与减少动态效果。
- 浮动入口恢复最初的轻量面板，底部使用横向铺满的单行输入胶囊，仅保留输入与发送按钮；发送按钮始终使用 ChatGPT 风格实心绿色，回答中显示纯白停止方块，继续输入时恢复白色箭头。
- 用户消息默认隐藏操作图标，双击后直接在原气泡内修改并重新回答，编辑态仅显示取消与保存；气泡使用更轻的 0.5px 描边与紧凑内边距。
- 工作台侧栏恢复 Harness 的工作区搜索行与收窄按钮，右上角三点菜单集中管理对话记录与归档；主输入区移除重复的调用、Token、服务状态、数据范围、临时对话、发送内容与导出控件，仅保留附件、模型与发送，浮层点击外部自动关闭。
- 侧栏复用资产分析的蓝色可拖动分隔条，可在 220–420px 间调宽、双击恢复默认并按账户保存；宽度持久化在浏览器存储满额或不可用时安全降级为 Cookie，拖动期间关闭列宽过渡以消除 280ms 延迟。
- 工作台或助手设置中当前选择的模型写入账户偏好，并同步用于各页面浮动入口；会话右侧增加轮次短线轨道。
- 新增 800×800 设置中心，包含通用设置、对话与记忆、模型、插件与助手预设；空间、记忆与模型调用统计集中进入设置，空间选择采用自定义浮层。
- 会话列表支持重命名、归档、恢复与删除，手动标题不会被后续保存覆盖；工作台、浮动面板、设置中心及其浮层统一服从助手自身主题。
- 模型服务可在当前弹窗内选择和编辑，全程不跳转；轨迹页保留 Input / Model / Tools 三泳道、高密度调用列表及右侧详情。
- 记录按用户和对话隔离，删除对话同步清理，且不保存 API Key、请求头、账户摘要、问题正文、回答正文或图片内容。

#### 新增智能助手独立工作台
- 左侧导航在财报日历下方新增智能助手入口，并参考主流 AI 工作台重构为固定会话侧栏与右侧单一会话画布。
- 新建、搜索、空间、记忆与调用统计常驻左侧，输入区固定在右侧底部，模型选择与发送按钮位于输入框右下角，数据范围、临时对话、发送内容、图片策略与导出收为次级控件。
- 原右下角可拖动入口保持轻量化；独立页完整适配深浅色、窄屏抽屉与手机宽度，当前页不重复挂载第二个浮动实例，避免会话状态竞争。

### 修复

#### 智能助手输入区移除附件按钮
- 按常见对话产品的交互精简输入区，移除回形针附件按钮与隐藏的文件选择器。
- 图片改为直接粘贴或拖入输入区域，缩略图删除、点击查看原图与 100MB 总量限制保持不变。

#### 修复局域网环境粘贴图片报错
- 智能助手图片预览标识改为组件内本地序号，不再依赖仅在安全上下文提供的 crypto.randomUUID。
- 通过局域网 HTTP 地址访问时，复制粘贴、拖拽与选择图片均可以正常生成预览并发送。
- 图片仍只随当次模型请求使用，不写入对话归档或附件目录，因此删除归档对话不会遗留附件文件。

#### 精简账户助手标题栏
- 移除会把面板压窄并造成标题换行变形的最小化按钮，标题栏只保留对话归档、新建、置顶与关闭四个操作，并收紧移动端间距。
- 归档抽屉删除重复的新建对话大按钮，统一从标题栏加号新建；标题栏按钮独立隔离拖拽事件。
- 置顶复用设置页相同的竖直图钉 SVG，与加号统一为相同尺寸、圆形背景与交互效果，置顶后以同色实心图钉表示状态。

#### 修复账户助手图钉点击
- 图钉按钮增加独立的指针事件隔离与层级，点击不再被标题栏拖动处理截获。
- 置顶与取消置顶均可正常切换，标题栏拖拽仍保持可用。

#### 收敛账户助手图钉颜色
- 图钉按钮与最小化、关闭按钮统一使用中性灰色，移除置顶状态下的青色背景与高对比图标。
- 置顶功能仍通过按钮提示与无障碍状态保留，不再抢标题栏的视觉焦点。

#### 修复模型服务保存与图标隔离
- 模型服务允许先保存尚未填写 API 密钥的待完善配置，密钥继续只在真正调用时作为可用条件。
- 上传的服务图标同步显示在当前提供方选择卡；切换到 DeepSeek、OpenAI 或自定义提供方时恢复各自的正确默认图标，不再沿用上一个提供方的上传图。
- 每次上传按服务与批次生成独立文件地址，避免其他服务误用 DeepSeek 图标或浏览器命中旧缓存。

#### 修复模型图标上传并调整排序交互
- 修复选择模型图标后异步上传完成时文件输入节点已释放而触发空引用报错的问题。
- 模型图标改为按原图透明背景、完整比例直接展示，不再额外套品牌色底板、边框或圆角。
- 服务优先级调整移到模型服务只读列表，配置多个服务后可直接拖动卡片并立即持久化；编辑页移除服务上下移动按钮，只保留配置、模型内部顺序与删除操作。

#### 完善刷新、导入适配和分组权限
- 自选首次进入立即加载报价，手动刷新可获取休市快照，定时刷新遵守持久化周期，后台返回仅到期补刷；超过 100 只按 API 上限分批。
- 手机导入弹窗可滚动至底部操作。
- 普通用户通过归属校验的独立接口上传分组图标，公共素材权限保持不变；财报全站排序仅管理员可改，失败回滚并提示。
- 版本列表补齐当前与上一版，本地腾讯提示不再误报富途故障，并增加隔离数据库回归检查。

#### 补齐导入返回入口并让分组胶囊按内容自适应
- 导入股票弹窗新增「返回编辑分组」胶囊，点击关闭导入并恢复原编辑工作台，不再只能点关闭或取消。
- 行情板默认只显示 5 个分组胶囊（含「全部」），其余收进紧随第五个胶囊的更多分组菜单；从菜单选择的分组会进入第五位，确保当前筛选始终可见。
- 胶囊移除统一的 125px 固定宽度，按图标、完整名称与数量自然撑开；轨道按内容收缩并受可用空间约束，避免截断或把菜单推到远端。
- 更多菜单入口增加与胶囊一致的圆形轮廓、主题背景与交互反馈。

#### 修复自选行情刷新间隔被标签页焦点触发
- 实测一分钟间隔连续两轮约每 60 秒更新，确认自选页定时器正常；不一致来自外层的 visibilitychange 监听：用户切回浏览器标签页时会无条件刷新，从而让「每 1 小时」等设定提前触发。
- 现在自选页获得焦点时不再走这条全局刷新旁路，只由右侧间隔控件或手动刷新按钮触发行情更新；其他页面继续保留回到前台自动刷新。
- 成功刷新同时记录毫秒时间戳，为后续刷新节奏与状态展示提供统一依据。

#### 相关 ETF 图标强制跟随正股
- 修复两倍做多等相关 ETF 在素材库已有发行商图标时不跟随正股的问题。
- 统一图标层现在无条件以关系表中的正股图标覆盖 ETF 及 .AM、.N、.OQ、.PS、.K 后缀代码，客户端全量素材与服务端首屏预载使用同一规则。
- 正股图标缺失时回退名称首字母，不再显示 ETF 发行商图标；SPCH.AM、TSLL、NVDX、ROBN、AAPU、RKLX、SMCX 等均已覆盖。

#### 自选分组筛选刷新后保持
- 修复我的行情板选择分组后只更新组件内存、刷新即返回全部的问题。
- 顶部胶囊、更多分组菜单与分组面板现在统一把所选分组写入 URL 的 filter 参数；进入股票详情会保留当前分组，返回列表继续显示原筛选，浏览器前进后退同步恢复。
- 分组删除或链接参数失效时自动清理并回到全部。

#### 自选分组 URL 改为短标识
- 自选分组筛选地址不再暴露 wg- 开头的内部随机 id：市场分组使用小写市场码（us、cn、hk、sg、jp、kr 等），自定义分组按当前排序使用 1、2、3……。
- 分组重排后当前地址同步更新序号。
- 旧内部 id、M:市场与 G:名称格式不再兼容，遇到时直接清除参数并显示全部，不执行转换或跳转。

#### 全站货币国旗首屏提速
- 货币国旗继续全部以素材库为配置来源，但不再由每个 CurrencyFlag 实例触发完整的 250 个国家/地区素材请求。
- 服务端按素材库记录读取固定 7 种展示货币对应的本地文件，并以内联图片随首屏 HTML 下发，刷新第一次绘制不再依赖第二个 /uploads 请求；远程自定义素材仍预载，资金系统的扩展币种只在组件实际出现时按 ISO 代码请求。
- 我的持仓、资产分析、资金系统、FIRE、名人持仓与卡面库统一受益，素材库替换国旗后仍全站生效；全球经济因需要完整国家目录，继续按需读取全部国旗。

#### 账户总览资金项按视图显示
- 账户总览的资金口径按视图拆清：银行卡现金与冻结现金只在「全部」下显示，且银行卡余额按当前展示货币折算后计入全部视图的可用现金与净资产。
- 美股、港股、A股只保留净资产、当日盈亏、持仓市值、浮动盈亏、可用现金 5 项，并继续使用各市场自己的资金余额。
- 响应式断点从 xl 前移到实际面板可用的 lg，桌面下「全部」7 项与单市场 5 项都保持一排；指标区增加最大宽度，宽屏下不再被拉得过散。
- 货币国旗的服务端映射同时移至通用模块，避免开发环境服务端布局读取客户端常量引发运行时错误。

#### 素材图标刷新首帧不再空白
- 修复通用 SafeAssetImage 在素材图片加载与解码完成前只输出空图片框的问题。
- 现在先用内置 SVG 保持尺寸与可见内容，图片完成后再淡入替换；内联国旗直接在服务端与客户端首帧显示，当前导航的自定义图标也从素材库读取后随 HTML 下发并预载。
- 我的持仓显示货币与左侧导航刷新时不再出现空圆、地球占位或图标延迟，市场图标、搜索结果等共用场景同步受益。

#### 导航图标首帧水合一致
- 修复我的持仓刷新后出现 Recoverable Error：服务端没有某个导航素材、客户端模块缓存却已有该素材时，SafeAssetImage 两端分别输出默认 SVG 与图片节点，触发 React hydration mismatch。
- 导航图标现在在服务端 HTML 与客户端水合首帧统一使用服务端素材快照，本地缓存只在水合完成后补充。

#### 账户总览市场切换位置稳定
- 账户总览的全部与单市场视图统一使用固定 7 列轨道。
- 美股、港股、A股的 5 项指标固定占据与全部视图相同的前 5 列，右侧两列留空。
- 切回全部时只在后两列补出银行卡现金与冻结现金，已有指标不再重新均分宽度或横向跳动。

#### SPCH 代码统一并清理重复记录
- 将 SpaceX 两倍做多 ETF 从搜索源遗留代码 SPCH.AM 统一为实际代码 SPCH。
- 数据库迁移保留有 604 股及 13 笔历史订单的真实持仓，删除截图导入产生的空仓重复记录，并同步订单、活动与素材代码。
- 新增和编辑记录也统一经过美股交易所后缀规范化，文件与截图导入沿用同一规则，后续不会再次生成 SPCH.AM。

#### 卡面库首屏卡面稳定加载
- 修复卡面库刷新后图片区域先全部为空、随后集中出现造成抖动的问题。
- 服务端按当前用户持有清单预载「我的卡」首屏最多 12 张，客户端首批 32 张立即加载且前 8 张提高优先级。
- 卡槽始终保持银行卡标准比例，后续卡面继续懒加载，不会为首屏一次请求完整 433 张素材。

#### 导航图标与头像刷新不再闪空
- 左侧导航当前需要的本地素材图标改为服务端内联进首屏 HTML，头像也在服务端读取本地文件并以内联图片交给账号菜单。
- 客户端重新确认登录态时保留同一首屏头像，不再把已显示图片换回待下载地址。
- 通用素材替换同时取消 100ms 透明度过渡，刷新时默认图标、定制图标与头像均连续可见。

#### 素材库行情后台更新且排序列稳定
- 素材库股票页的默认首屏直接从服务端数据库输出上次持久化结果，不再先显示整页骨架。
- 浏览器同时保存最后一次成功的筛选页面，刷新先恢复原表格再静默校验；任一证券缺少行情时只给热缓存极短的合并窗口，冷缓存继续在后台更新；翻页、筛选与排序期间保留现有表格。
- 所有排序标题永久预留箭头宽度，点击市值等列时后续标题与市场列保持原位。

#### 素材库 URL 状态首帧水合一致
- 修复带 sort、market、page、q 或卡片视图参数刷新素材库时，服务端默认状态与客户端直接读取 URL 后的状态不同而触发左下角 Next.js Issue。
- 分类、市场、排序、页码、搜索与卡片布局现在先以统一状态完成水合，再在首次绘制前恢复 URL 参数，保留持久化体验且不再产生 hydration mismatch。

#### 手机端全局壳层与高频操作区适配
- 收紧手机端顶栏、页面上下留白与页脚，窄屏隐藏占位较大的指数条；十多个主入口改为单行图标文字胶囊并支持横滑、自动居中与吸顶。
- 素材库分类与市场筛选支持稳定横滑，搜索框独占一行避免挤压。
- 自选分组管理在手机上改为分组栏与成员区两段可滚动布局，操作按钮可横滑，避免固定桌面高度导致内容或底部动作被裁掉。

#### 修复手机收益日历金额溢出
- 资产分析与资产盈亏分析共用的收益日历新增手机专用窄金额格式：千位起使用千、万、亿单位，小额去除无意义小数。
- 375px 月视图采用更紧凑的字号与字距，日期格限制自身溢出。
- 收益率根据数值长度自适应小数位，桌面仍保留原有精度与显示方式。

#### 调整手机收益日历比例
- 手机月历不再沿用桌面端 80px 日期格高度：窄屏日期格收紧为 52px，格间距与金额上下间距同步缩小，圆角按尺寸调整。
- 整月高度明显降低，单格比例更接近移动端日历卡片，桌面布局保持不变。

#### 排查并修复手机端七列日历比例
- 系统检查收益日历、财报日历、日期区间选择器、订单日期筛选、资金日期选择器与交易有效期日历。
- 收益日历加载骨架与年视图改为匹配手机实际高度，避免加载前后跳动；财报日历手机日期格从 88px 收紧为 52px，只显示日期与财报数量，完整公司信息使用页面已有的下方日期详情区，避免在窄格中重复堆叠。
- 桌面端继续显示公司摘要与悬停详情，其余日期选择器本身已采用方形日期格，无需改动。

#### 资产总盈亏二级页即时打开
- 修复资产分析点击持仓总盈亏后长时间显示骨架的问题。
- 主页面与二级页的组合历史及基准 K 线窗口统一为 330 天，使主页面已经完成或正在进行的请求可直接命中同一份内存缓存。
- 二级页首帧直接复用父页面现成的持仓、行情、组合收盘序列与订单，不再等待自身的请求，趋势图与收益日历可直接出现；客户端缓存有效期按实际接收时间计算，避免服务端数据时间较早导致刚请求完就被判定过期。
- 独立地址进入时仍保留完整取数流程。浏览器实测点击约 260ms 即显示总盈亏、排行与明细。

#### 账户助手刷新后恢复上次对话
- 账户助手按用户在服务端保存最近 30 条消息，刷新页面、切换功能或换设备后重新打开仍可继续上次对话。
- 旧版浏览器历史会自动迁移并清除，不同用户的历史互相隔离。
- 标题栏新增「开始新对话」按钮，可随时清空历史重新提问；异常历史自动降级，不影响当前会话。

#### 账户助手真实场景稳定性加固
- 按刷新、浏览器前进后退、同页切换市场、快速连点与请求中重开会话等真实路径回归：助手从美股直接切港股时以新 URL 为准，不再被旧筛选状态改回。
- 交易与分组确认预览增加 15 分钟有效期，旧版无时间戳预览自动失效；执行中刷新后的交易标记为待核对，避免网络结果不明时重复入账。
- 确认与撤销统一串行并拦截双击；新对话会取消未完成回答并隔离旧响应；前端与服务端都只保留最近 30 条，服务端严格清洗持久化动作与撤销结构。
- 跨市场同代码不再默认取第一只，要求用 US: / HK: / CN: 明确市场，交易预览同步显示市场；批量分组遇到未识别代码时整体停止，不再只移动其中一部分。

#### 账户助手操作支持安全重试
- 交易、建组与移动分组统一改由服务端幂等操作入口执行，每个预览使用固定操作标识并在 SQLite 事务内完成业务写入与结果登记。
- 网络中断或刷新后用同一标识重试只返回首次结果，不会重复下单、建组或移动；标识被篡改后拒绝执行，业务失败会整体回滚。
- 撤销也使用独立固定标识并支持安全重试，超过 15 分钟后明确禁用；对话保存失败自动重试三次并显示可手动重试状态。
- 手机底部面板补齐圆角与点击遮罩关闭，桌面自动聚焦输入框，支持 Escape 关闭与中文输入法回车保护；以 390px 视口实测刷新、历史恢复、面板滚动与关闭，未执行真实交易或分组变更。

#### 账户助手入口采用长桥配色
- 参考长桥 Olo 青的使用层级重做右下角账户助手入口：按钮使用沉稳石墨黑作为主体，白色星芒负责识别，仅在角落保留一颗带柔光的 Olo 青亮点。
- 悬停只轻微提亮深色底与阴影，不再铺满高饱和青绿色。

#### 账户助手面板统一青绿色视觉
- 打开后的工作面板固定使用清爽白底，在深色站点中也保持一致。
- 长桥 Olo 青缩减为点睛色：用户消息接近白色，只保留极浅青边，文字恢复中性深灰；发送按钮改为浅薄荷底与青色箭头，去掉大面积实心青色。
- 标题图标保持与入口完全一致的石墨黑圆底、白色主星与青色小星，只按标题栏缩小尺寸。

#### 账户助手换用长桥风格星芒图标
- 移除线条偏碎的通用 Sparkles 图标，改为专门绘制的饱满四角白色主星与右上 Olo 青小星组合。
- 入口与面板标题共用同一个图标组件与同一套石墨黑圆底配色，只通过尺寸适配不同场景，确保两处长期一致。

#### 账户助手隐藏页面识别状态
- 标题栏移除「正在查看：财报日历」等当前页面状态，只保留账户助手名称。
- 输入框下方同时移除示例命令与数据用途两行常驻说明，保存正常时保持留白，仅在失败时显示可点击的重试提示。
- 页面上下文仍仅在发起提问时作为受控信息用于生成相关回答。

#### 市场与国家图标移除 Emoji 回退
- 修复素材库市场图标在自定义素材失效或默认文件缺失时退回 Emoji 的问题。
- 全站 MarketIcon、素材库国家/地区旗帜与全球经济地图统一改用本地素材优先、内置矢量地球兜底；持仓市场编辑器移除 Emoji 国旗输入与默认值，新市场图标只从素材库解析。
- 新增回归约束，防止界面再次引入 Emoji 图标回退。

#### 账户助手补齐真实页面上下文与失败恢复
- 账户助手在卡面库与 FIRE 页面读取真实数据：卡面库读取当前用户持卡数与按原币汇总的录入金额，并说明金额可能含信用卡额度；FIRE 只读取允许的规划参数；素材库检查分类数量与本地文件缺失项，敏感卡号与安全码不会进入上下文。
- 回答支持标题、列表、粗体与行内代码排版；输入框随内容增高；用户上滑阅读时新回复不再强制拉到底部。
- 请求失败显示可持久化的重新回答按钮；删除会话会等待保存队列并阻止旧请求把已删除会话重新写回，Escape 优先关闭历史抽屉。

### 安全修复

#### 模型发送内容透明化
- 输入区新增发送内容面板，在请求前明确显示将使用的第三方模型服务，以及文字、图片、当前页面摘要、账户摘要和记忆中哪些内容会被发送。
- 同时提示第三方服务可能按自身条款处理或保留请求，提醒不要输入密码、Cookie 或 API Key。

#### 模型密钥改为服务端加密存储
- 模型服务密钥、旧版大模型密钥、雪球 Cookie 与数据库密码写入 SQLite 前统一使用 AES-256-GCM 加密，读取时仅在服务端解密。
- 支持 FIRE_ENCRYPTION_KEY，未配置时生成权限为 0600 的持久化实例密钥。
- 现有明文配置保持兼容，并在下次保存时自动迁移为密文。

#### 智能助手与模型服务边界加固
- 连续粘贴或拖拽图片时原子预留容量，避免并发绕过 100MB 总量；图片读取失败给出可恢复提示。
- 服务端精确核算原始图片字节，并明确拒绝损坏或超限数据，不再静默丢图。
- 停止生成会同步取消上游模型请求，模型回答与连通性测试响应增加硬性体积上限；设置写入改为有界读取，避免异常服务或超大请求持续占用内存。

#### 全站审查安全与数据修复
- 设置接口统一按白名单返回，管理员与普通用户均不再收到 LLM、数据库密码或雪球 Cookie；保存空密码保留已配置值。
- 文件与截图导入共用市场与代码规则，保留 SHOP/USO 等合法证券代码、识别 CSV 引号与显式市场；跨市场同代码不再覆盖原持仓，歧义返回明确错误并回滚。
- 资产总览按统一汇率折算为美元或请求的币种，并补充报价来源与估值时间。

#### 账户助手安全边界加固
- 问答、会话保存与确认操作改为流式限长读取请求体，阻断超大 JSON 在解析前占满内存。
- 页面名称改由服务端白名单确定，股票与筛选字段严格限长限字符，账户数据以不可信 JSON 区块交给模型并明确忽略其中的提示注入。
- 模型地址只允许无凭据的 HTTP/HTTPS，拦截云元数据与链路本地地址；供应商错误不再透传，回答统一限制 4000 字。
- 交易、分组与撤销补齐标识长度及数量、价格、费用、名义金额上限，未知内部错误统一收敛；Next.js 与 Sharp 升级至安全版本，移除存在原型污染与 ReDoS 且无修复版本的 SheetJS，账本 Excel 导入导出迁移到 ExcelJS，并在解压前限制压缩包条目、膨胀率与解压总量。

---

## v0.1.29 · 2026-09-13

### 新功能

#### 补齐富途式自选分组管理工作台
- 「编辑分组」升级为完整的左右分栏工作台，并统一深浅色：左侧加入固定的「全部」，市场分组与自建分组合并为一条可自由拖拽排序的列表，数量统一右对齐，操作图标仅在悬停时覆盖显示，侧栏收窄到仍可完整显示最长名称的 240px；分组图标与行情板复用同一份解析结果。
- 新增与重命名改为一致的居中弹窗与卡面库风格胶囊按钮，新增入口紧跟在分组列表末尾。
- 右侧补齐 min-height 约束并形成独立滚动区，73 条等长列表可从固定表头下滚动到底，切换长短列表不再导致窗口跳动；同时修复暗色模式下半透明浅色背景把侧栏和弹窗染成灰白块的问题。
- 详情顶部改为全市场股票联想，选中后直接加入当前自定义分组；明细支持全选、跨分组移动、多选移出、上移/下移/图钉置顶/删除及服务端持久化排序，置顶支持多只股票并连续排列。
- 四个操作默认隐藏，悬停或键盘聚焦行时显示；去掉文字徽标、左侧强调线与整行灰底，四个图标（含已置顶图钉）离开行后全部隐藏，划过时已置顶图钉以实心圆底区别于普通图钉。
- 明细行增至约 56px，刷新后保留置顶状态；市场列由重复文字改为与分组栏一致的 24px 圆形市场图标，并保留悬停名称。
- 勾选框改为 17px 中性色圆角控件，选中使用深灰底与白色对勾，深浅色不再出现系统蓝色；批量移动点击后立即更新当前列表，失败才回滚。
- 全部和市场分组恢复上移、下移、置顶与删除操作：排序按全局或当前市场持久化，删除会从自选股中移除；导出可用态改为高对比胶囊，导入在全部及市场分组中同样可以打开并默认导入全部。
- 行情板分组条固定显示 5 个完整分组并支持横向滑动，三横杠菜单选中其他分组后会自动滚动，并以能完整容纳当前项的最早分组为左边界，避免首枚胶囊被标题侧裁切；主题切换不再对胶囊背景色做过渡闪帧。
- 导出改为富途兼容的制表符 TXT；新增 TXT/CSV/SEL/EBK/JSON 文件导入、列表预览与目标分组选择，并兼容 GBK 券商文件，股票默认进入全部自选并附加到所选分组。
- 指定分组导入改为哈希索引匹配、预编译 SQL 与单事务批量写入，移除逐条全表读取造成的等待。
- 文件选择按钮固定为 44px，底部取消与导入固定为 40px，统一恢复正常内边距与文字居中；移除重复的「编辑行情板」模式，更多菜单中的编辑入口改为低强调样式。

#### 按富途逻辑重构自选分组入口
- 顶部保留常用分组，原「更多」改为三横杠图标。
- 菜单集中列出全部、市场与自定义分组，显示图标、数量与当前项勾选，底部固定蓝色「编辑分组」。
- 点击编辑直接进入排序、显隐、重命名、新建与删除界面，并移除功能重复的独立加号入口。

#### 优化自选股更多分组交互
- 「更多」改为不带数量的固定聚合按钮，点击后从按钮下方弹出其余分组，常用分组不再被替换，整排布局保持稳定。
- 修复分组横向滚动容器裁掉弹层的问题，展开时解除裁剪并提高控制栏层级。
- 弹层显示分组图标、名称、数量及当前选中标记；选中后按钮显示当前分组，点击外部自动关闭。

#### 相关 ETF 添加支持名称联想搜索
- 手动添加相关 ETF 的代码与名称输入合并为证券联想搜索，可按代码、英文名或中文名查找。
- 候选项展示图标、市场、名称、代码与行情，选择后自动填写规范代码和名称。
- 搜索不到时仍可把输入内容作为代码添加，产品类型与默认正股关联保持不变。

#### 相关 ETF 支持手动添加兜底关系
- 相关 ETF 列表无论为空或已有产品，底部都显示圆形添加入口。
- 支持填写股票代码、可选名称与做多、做空、收益策略类型，并默认关联当前正股；关系双向保存在本机，新 ETF 详情可反查正股并沿用正股图标。
- 补齐英特尔的 LINT 做多与 INYY 收益策略产品，同时删除财报日历标题下方重复的市场范围与操作说明。

### 修复

#### 补齐线下版美股列表行情
- 修复线下版没有富途 OpenD 时，腾讯未收录的新股、冷门 ETF 与特殊代码在自选列表只显示走势图，现价、涨跌幅、最高、最低与市值全部为横线的问题。
- 批量行情现在会对腾讯漏项逐只使用 Yahoo 常规行情补齐，并在从线上带回「仅富途」配置但本机 OpenD 整批为空时自动启用公开源。
- 浏览器实测 UFO、ECHO、ASTS、VSAT 六列行情完整，ECHO 列表现价与详情页一致。

#### 简化自选股移动分组
- 批量移动由「先点按钮、再操作第二个下拉框」合并为单个目标分组菜单，点击即可直接选择并执行，完成后自动清除选择状态。
- 批量与单股菜单统一按「移入分组 / 其他」组织选项，「移出当前分组」固定放在末尾。

#### 补齐更多分组选中态图标
- 从「更多」弹层选择分组后，聚合按钮同步显示该分组的市场、自定义、券商或组内股票兜底图标，尺寸与弹层列表保持一致。
- 适度增加分组栏上下留白、胶囊高度与间距，并同步放大管理加号，改善横向工具栏过扁的比例。

#### 统一全部分组图标
- 「全部分组」面板中的「全部」由地球图标改为与「我的行情板」分组栏一致的四宫格图标，统一同一筛选项在两个入口中的视觉含义。

#### 彻底修复自选股刷新分组闪失
- 自选股分组现在与自选记录一起由服务端注入首屏 HTML，刷新首帧即可显示完整的市场与自定义分组，不再依赖 localStorage 是否存在，客户端只负责静默校准最新配置。
- 同时删除「我的行情板」标题旁与「全部」数量重复的总数徽标。

#### 修复自选股刷新时分组短暂消失
- 自选股分组改为在浏览器绘制前恢复最后一次成功快照，不再因快照超过一分钟而先渲染只有「全部」的中间状态。
- 服务端随后静默刷新，空响应或请求失败时继续保留已有分组；旧分组迁移后的结果也同步更新快照。

#### 补齐全站稳定数据快照优先加载
- 复核资产分析、订单、财报日历、全球预览、名人持仓、用户、附件与素材相关页面的加载路径；已有快照或服务端首屏数据的页面继续静默刷新。
- 为遗漏的资产盈亏分析补充持仓、行情、汇率、订单与历史收盘快照，刷新时不再整页显示骨架。

#### 移除个股财务页财报附件功能
- 删除财务页财报模块中的财报附件入口、上传归档界面、请求与本地缓存逻辑，以及仅服务于该界面的样式。
- 摘要与收入明细保留，独立附件管理功能不受影响。

#### 卡面库 NEW 角标美化
- 左上角新品标记改为直径 26px 的银白渐变圆形徽标，去掉星芒图标，仅保留居中的 NEW 文字，搭配细边框与深灰字。
- 深色模式使用石墨灰底与浅色字，保留新入库三天内展示的规则。

#### 卡面库右下角操作按钮统一圆形
- 右下角操作保持 32px 圆形与 44px 点击热区。
- 全部卡面中已加入的默认显示勾，未加入的悬停显示加号；我的卡默认隐藏，悬停显示减号用于移除。
- 按钮悬停即时提示加入我的卡／移出我的卡，触屏常显操作入口，保留详情中的加入和移除功能。

#### 卡面操作图标与提示统一中性色
- 勾号、加号与减号统一中性灰，操作提示改为 22px 高的浅灰小标签，简化为加入／移出。
- 保留完整无障碍名称、即时提示与既有显隐逻辑，深色模式同步适配。

#### 卡面库记住我的卡／全部卡面选择
- 卡面库模式改用统一偏好存储，选择我的卡或全部卡面后刷新、离开再返回都会恢复上次选择。
- 偏好同步到 cookie，服务端与客户端首帧一致，避免先闪回默认模式。

#### 卡面详情去除重复 NEW 标记
- NEW 已在卡面库列表中展示，打开卡面详情后不再在大图左上角重复显示，避免遮挡卡面内容。

#### 提交标题增加自动格式检查
- GitHub Actions 新增提交标题检查，每次推送要求使用英文类型前缀、半角冒号与简明中文摘要。
- 同一轮界面微调尽量合并后推送，交付回执单独报告自动检查状态，减少无效排队并让部署状态页更易读。

#### 顶部指数走势图基准线细节优化
- 指数迷你走势图为昨收基准线增加上下留白，避免贴住画布边缘。
- 涨跌色带改为只填充到基准线，虚线进一步变细变淡，走势图左右内收并完整显示末端圆点。

#### 顶部指数迷你走势图按像素抽样
- 顶部走势图按 56px 显示宽度将数百个分钟点压缩到最多 56 个代表点，每个区间保留局部最高点与最低点，并固定保留首尾点，避免等距抽样漏掉短暂尖峰。
- 完整分钟序列继续用于行情推进判断与收盘后富途对账。

#### 全站列表趋势图统一微牛样式
- 公开首页预览与后台自选行情板共用昨收虚线火花图：去除面积填充与末端圆点，按昨收决定涨跌颜色，并以区间高低点抽样保留短暂尖峰。
- 分时源暂时不可用时继续显示浏览器或服务端保存的最后一次成功走势，并用灰色明确标记；从未成功获取时显示灰色虚线占位。顶部指数迷你走势图保持不变。

#### 自选行情板序号统一正文颜色
- 自选股的我的行情板中，序号列数字由浅灰色改为与股票名称一致的正文颜色，提升列表层级的一致性，并自动适配深浅色模式。

#### 恢复贵金属与缺失加密货币图标
- 移除 8 月 31 日为补齐镜像素材而加入的统一黑底金属锭 SVG。
- 全量核对市场、加密货币与券商素材，补回 BNB、DASH、DOGE、DOT、ETC、LTC、TRX、VET、XLM、XMR、XRP 共 11 个缺失的默认图标。
- 初始化逻辑也会在数据库地址为空或文件丢失时恢复内置素材，不覆盖有效的用户上传图标。

#### 按 TradingView 修正四种贵金属图标
- 逐一核对 TradingView 的 TVC:GOLD、TVC:SILVER、TVC:PLATINUM 与 TVC:PALLADIUM 页面，使用页面实际采用的官方 SVG 替换黄金、白银、铂金与钯金图标。
- 贵金属行情回退数据中的图标地址也统一改为 TradingView 来源。

#### 素材库改为按分类按页加载
- 修复素材库每次进入同时请求全站全部分类、重复拉取 3000 多条股票素材后才在浏览器分页的问题。
- 股票图标改由服务端按当前页、市场、搜索与排序读取，每次只返回 10 条；其他分类仅在实际进入时加载，市场筛选单独读取十几条轻量数据，卡面继续独立加载。

#### 修复素材库快速操作请求竞态
- 补充请求顺序保护，快速切换分类、市场或页码时只允许最后一次请求更新界面，避免较慢的旧响应覆盖当前页面。
- 服务端分页后的序号按全局位置连续计算；搜索时立即回到第 1 页，删除末页最后一条后自动收敛到有效页码，接口失败时保留现有内容并明确提示。

#### 修复本地版页面反复重载与无法点击
- 本地开发服务器不再监听行情、财报缓存与上传素材等运行时文件。
- 后台刷新数据写入磁盘时不会反复触发整页热重载，避免旧页面继续请求已失效的 JavaScript 分块而出现 ChunkLoadError、页面可见却无法点击。

#### 修复刷新时 Favicon 短暂显示地球
- 新增浏览器可在刷新最早阶段直接读取的静态多尺寸 favicon.ico，避免等待页面 metadata 与约 492KB 的上传原图时先显示默认地球。
- 后台配置的自定义网站图标继续保留，加载完成后仍按设置展示。

#### 精简账户资产页标题栏
- 按反馈移除账户资产标题旁的「本页金额 · USD / 智能缩写」胶囊，仅保留行情来源状态与刷新操作，减少重复信息。

#### 彻底消除重页面刷新时的地球图标
- 确认地球图标主要出现在首屏较重的我的持仓与资产分析：浏览器会越过轻量 favicon.ico，继续等待约 492KB 的后台上传原图。
- 现在服务端首帧只声明多尺寸静态网站图标，后台自定义图标在页面就绪且图片完整加载后再无缝替换；设置中更换图标仍会即时生效，加载失败则始终保留网站图标。

#### 消除设置页刷新时的滚动条闪现
- 设置页恢复 URL 子分类前，真实内容由仅隐藏可见性改为完全退出布局，避免其内部滚动容器透过骨架屏提前绘制右侧滚动条。
- 状态就绪后再正常显示对应设置内容，不影响页面滚动。

#### 修复我的持仓刷新与切页闪变
- 查明 18,611 是数据库旧价格计算的持仓市值，57,515 是恢复实时行情后仍未计现金的中间值，8,020 是首屏提前注入现金后暴露出的仅现金中间值，67,056 才是完整净资产。
- 现在资金与卡片现金余额随服务端首屏注入，最后一次完整行情在绘制前恢复，并以完整估值状态控制金额展示；快照就绪前保留页面结构但不展示不完整数字。
- 总资产市场状态与持仓列配置统一首帧并在绘制前恢复，消除水合重建；显示货币与市场盈亏不再短暂消失，原「各市场盈利」同步更名为「市场盈亏」。

#### 加快股票搜索联想
- 股票名称和代码优先查询本地素材库，常用标的不再等待外部接口；股票已命中时不再串行等待 CoinGecko。
- 前端搜索防抖由 260ms 缩短为 140ms，并取消连续输入产生的过期请求，最近搜索结果缓存 60 秒；保留比特币、以太坊等加密货币联想。

#### 收敛加密货币搜索结果
- 参考 OKX 与 Binance 的高市值、高成交量市场范围，建立 21 种主流加密货币白名单及中英文名称、代码别名正则。
- 命中比特币、BTC、以太坊、SOL 等查询时只显示加密货币本体，过滤同名 ETF、储备公司、策略基金与非主流币；素材库的加密货币搜索同步使用相同白名单。

#### 补全 RAM 新上市 ETF 搜索
- 为尚未被腾讯联想与本地素材库收录的 RAM 增加受控别名映射。
- 搜索 RAM、DRAM 2倍做多、2X DRAM 或 DRAM long 均直接返回「DRAM 2 倍做多 ETF（RAM）」。

#### 搜索结果优先展示正股及相关 ETF
- 搜索 AAPL、苹果、NVDA、英伟达等正股代码或名称时，第一条固定展示正股，随后按全站正股—ETF 关系表补充做多、做空与收益策略 ETF。
- 相关 ETF 批量读取本地素材与行情，避免逐只查询；当前规则覆盖关系表内全部正股。

#### 完善股票搜索结果图标与 ETF 标识
- 股票搜索联想新增本地股票图标；相关 ETF 复用对应正股图标，并在图标右下角用紧凑的 2x、反向、收益标记区分产品类型。
- 结果出现后按需批量补齐图标，不拉取完整素材库，正股与 ETF 的名称、代码与价格列保持对齐。

#### 将相关 ETF 角标调整为圆形
- 相关 ETF 图标右下角统一改为 19px 固定圆形角标，2x 保留原文，反向与收益分别缩写为「反」「收」，悬停仍显示完整含义。
- 角标向下错开并增强边缘分隔，避免被文字撑成长方形或遮住主图标。

#### 统一全站相关 ETF 圆形角标
- 搜索联想、自选股、我的持仓、个股详情头部与相关 ETF 列表统一使用同一个关系角标组件，并从全站 ETF 关系表识别 2x、反向、收益与做多产品。
- 全部采用 19px 圆标与右下下移位置，完整类型保留在悬停说明中。

#### 修复详情页相关 ETF 图标白底
- 股票详情的相关 ETF 列表统一复用对应正股的圆形图标，不再优先显示带白色方底的 ETF 独立素材。
- ETF 详情反向展示正股时仍使用该正股自身图标，圆形角标与搜索和列表保持一致。

#### 持久保存股息并增加数据源回退
- 股票详情与资产分析共用 SQLite 股息快照：已获取的历史派息永久合并保留，近期及未来记录每日更新，纯历史记录每周检查；数据源暂时失败时继续展示最后一次有效结果，不再误显示 0 期。
- 线上启用富途时优先读取富途，本地开发态直接使用东方财富 A 股与 Yahoo 美港股公开数据，富途不可用时也会自动回退公开源；旧版误写的空缓存自动失效并重新获取。

#### 相关 ETF 行情优先显示上次缓存
- 股票详情的相关 ETF 页签保存每只产品最后一次成功行情，刷新或重新进入时在首屏绘制前恢复价格、涨跌幅与市值，同时后台获取最新行情。
- 本次请求缺少个别标的或整体失败时保留旧值，不再清空列表并长时间显示「正在更新行情」。

#### 补齐资产分析全链路 ETF 角标
- 资产分析的持仓分布、持仓盈亏排行、订单列表与订单详情、收益日历明细，以及资产盈亏分析的股票盈亏明细，全部接入统一 ETF 关系角标。
- 2 倍做多、反向与收益产品均按同一关系表识别，图片缺失使用字母占位时也保留角标。

#### 修复个股详情刷新闪页并缓存概览
- 概览、ETF、股息、财务与公司标签刷新时直接渲染当前个股详情，不再先闪现自选股首页。
- 概览行情与走势图优先展示最后一次成功快照，再于后台静默更新，失败时保留旧值；全天改为直接打开完整的盘前、盘中、盘后分时 K 线，夜盘等单独移入时段菜单。

#### 精简详情说明并优化 K 线周期菜单
- 删除 ETF 与股息标题下方重复说明，并复核全站同类文案，仅保留包含数据口径、范围或操作风险的信息。
- 修复年 K 与分钟周期按钮挤压错位，增加独立分隔与横向溢出保护，周期菜单改为更紧凑的分钟、小时、长周期分区布局。

#### 统一 K 线周期控件视觉
- 移除周期按钮突兀的黄色边框、底色与箭头，统一为相邻控件使用的中性灰色交互状态。
- 分钟、小时与长周期分别使用圆角虚线边框包裹，增强分组层次；弹层从周期按钮左缘向右展开并限制最大宽度，不再越过 K 线卡片左边界。

#### 完善 K 线工具菜单收起与指标样式
- 前复权、比较和指标菜单点击工具栏外区域后自动收起，打开其中一个菜单时同步关闭其他菜单，避免浮层残留或叠加。
- 指标设置中的 MA 标题与复选框改为统一的中性灰白样式。

#### 恢复全部 K 线图形设置
- 修正全天与 5 日周期错误隐藏高级图形的条件，面积图、折线图、带标记线、阶梯线、HLC 区域、基准线、实心 K 线、空心 K 线与 OHLC 在所有周期始终可选。
- 继续保留用户的常用排序和当前样式。

#### 减轻全天选中状态
- K 线工具栏的全天选中态改为轻透明背景与低对比细描边，并收敛文字权重，在保持当前状态清楚可辨的同时与相邻周期按钮更协调。

#### 修复周期按钮遮挡并增加切换动画
- 为 5 日、日 K、周 K、月 K 与年 K 分组补充两侧安全留白，切换后自动将当前周期完整移入可视区，避免选中背景或文字被横向容器裁切；选中态改为在同组按钮间平滑滑动。
- 横向拖动会完整捕获并释放指针，松手后不再粘连移动。

#### 聚合复权与指标设置
- 移除工具栏独立的前复权按钮，将前复权、不复权和后复权作为紧凑分段选项并入指标面板。
- 聚合入口继续使用原指标图标，面板标题与分区标题统一左对齐，下方均线与显示设置保持原有功能。

#### 解耦全天视图与分钟周期
- 全天作为独立视图，不再读取、改写或高亮分钟与小时周期，并默认不叠加 MA；面积图和折线图使用完整原始分时数据，蜡烛类图形仅在内部按屏幕可见密度聚合，图形类型仍可自由切换。
- 修复周期按钮因过早捕获指针而无法点击、关闭全部指标后持久化设置被默认值覆盖，以及指标切换时反复销毁图表实例导致主价格图消失的问题。

#### 精简 K 线跟随框并自动避让
- 参考富途压缩全天分时跟随框的信息密度，展示价格、估算均价、涨跌、成交量与成交额；估算均价按可用分钟成交量加权，无有效成交量时显示横线，避免把当前价误当均价。
- 5 日及日 K、周 K、月 K、年 K 等大周期继续完整展示开高低收与成交信息。
- 所有周期的触点在图表左半区时跟随框固定右上角，越过中线后切换到左上角，并在主价格折线或 K 线柱收盘位置显示跟随圆点。

#### 恢复 K 线周期持久化
- 刷新或切换股票时恢复最后选择的全天、分时、5 日、日 K、周 K、月 K、季 K、年 K 或今年以来视图，同时保存全天与自定义分钟的区别。
- 首次恢复完成前禁止默认状态写回本地缓存，避免已保存周期被全天覆盖；图形样式的服务端与浏览器首帧统一使用面积图占位，挂载后再恢复偏好，避免本地偏好造成 hydration 错误。

#### 优化 K 线跟随点层级
- 完整恢复蓝色实心圆点、白色描边与顶层显示；K 线图仅将圆点定位到开盘价与收盘价之间的实体柱中点，避开上下影线，折线和面积图仍精确落在价格线上。
- 图形样式、复权和指标恢复完成前禁止默认面积图写回设置，实心 K 线等选择刷新后保持不变。

#### 股息记录改为持久快照优先
- 全面统一股票详情缓存优先逻辑：概览 K 线、股息、财务、公司以及资产分析持仓股息弹窗都在绘制前恢复最后一次成功快照，进入和刷新时直接显示已有内容，再静默更新。
- 后台请求返回空值、失败或上游暂时不可用时不再清空旧数据；修复日 K 等长周期已有缓存却仍被加载层遮住的问题，并在 URL 页签恢复前隐藏错误的概览内容。
- 股息服务端 SQLite 缓存过期后也先返回历史快照，以单标的去重任务后台回源；近期记录每 3 天、纯历史记录每 30 天检查一次。

---

## v0.1.28 · 2026-09-12

### 新功能

#### 财报日历接入港股（雪球财报日历）
- 港股此前只有「该市场财报数据源暂未接入」的占位。
- 现在接通雪球财报日历：接口是 /v5/stock/screener/earnings_calendar/hk/list.json，必须带登录 Cookie（匿名请求直接 400），参数用 begin_date / end_date 取区间，并带上 extend=all 才会返回整段区间。
- 返回按天分组，组内再分「已发布 / 盘前 / 盘后 / 当日」四个桶，正好对上现有的时段口径。
- lib/earnings.ts 新增 fetchHkMonth()：每月一次请求，按 symbol 与日期去重，用东方财富 push2 的 116. secid 批量补市值、现价、涨跌与简体名，再按「单日市值前 5」收敛，与美股、A股同一套规则。
- EarningsMarket 增加 HK，/api/earnings 放行 market=HK；前端港股进入常驻市场，港股单独一套 HK$ 市值档位，「业绩类型」列显示「2026 中报」这类标签，已发布条目显示「已发布」而不猜测盘前或盘后，空档月给出港股业绩季说明；设置页新增「港股财报」接口地址，关于页数据源补上雪球。
- 实测：2026-08 上游返回 2093 条事件（已发布 2032 / 盘前 3 / 盘后 14 / 当日 44），按单日市值前 5 收敛后 111 条；2026-09 为 38 条，均带回市值与现价。

#### 资产分析页新增「盈亏日历」模块与模块拖拽排序
- 把资产盈亏分析页的「收益日历」抽成共享实现：数据口径抽到 lib/pnlCalendar.ts（含按订单轨迹推进数量、现金流归入下一交易日、最近有效收盘价回填等既有算法），UI 抽到 components/PnlCalendar.tsx，资产盈亏分析页改为引用同一份实现（该文件净减约 450 行），资产分析页右栏底部新增同款模块。
- 两个页面共用一套算法，因此同一天的盈亏不会出现两个数字。
- 模块拖拽排序：左右两栏各自可拖（左侧 账户资产 / 收益率趋势图 / 持仓盈亏排行 / 资金系统；右侧 账户总览 / 持仓分布 / 订单 / 盈亏日历），鼠标悬停卡片左侧出现手柄，按住手柄才可拖动（避免选文字或拖输入框时误触发），落下即写回 site_settings.assetAnalysisOrder，成功提示「模块顺序已保存」并派发 fire:settings-updated，失败提示重试。
- 服务端保存的顺序与默认顺序合并，之后新增模块自动补在末尾；两栏容器从 space-y 改为 flex + gap 以支持 order 排序。

#### 资产分析页性能优化：组合数据一次取回与图表按需加载
- 先量后改：SSR 热态 0.08–0.6s 并不慢，慢在水合之后 —— 页面一挂载就由浏览器按持仓逐只请求 /api/kline/full（17 只约 17 次请求、约 520KB），外加订单 5000 全量、资金、汇率与行情，8 个模块全在等这批数据。
- 新增 GET /api/v1/portfolio-series?days=330：服务端按持仓并发（6）取日 K，一次返回「recordId → { d, c } 序列 + 订单」，浏览器从 N+3 个请求降到 1+1（基准单独一个小请求，切换基准不用重拉全部），同规模体积 5 只 153KB → 58KB，服务端日 K 缓存命中时实测 0.03s；资产盈亏分析页同步改用这份聚合数据并共享浏览器缓存。
- 首屏让路：聚合取数放到 requestIdleCallback（超时 600ms，兜底 setTimeout 150ms）之后，首屏先渲染账户资产与总览这些用已有行情就能画出的模块。
- echarts 趋势图改为 next/dynamic 按需加载（带骨架），并删除两个页面里已失效的逐只 kline 缓存与有界并发工具（约 2.6KB 死代码）。

#### 资产分析性能优化：聚合接口加 ETag
- 给 /api/v1/portfolio-series 加 ETag 条件请求：指纹只按内容算（closes 先按 recordId 排序再序列化，避免并发取回导致键顺序变化、304 永不命中；订单只取影响序列结果的字段），响应改为 Cache-Control: private, max-age=30, must-revalidate。
- 顺带为 next.config.mjs 里 /api/:path* 的全局 no-store 加了一条更具体的例外。
- 客户端 fetchPortfolioBundle 默认走浏览器 HTTP 缓存，只有强制刷新才 no-store；实测连续三次请求 ETag 稳定，带 If-None-Match 命中 304、下载 0 字节（此前每次重传约 58KB）。
- 「订单」模块的刷新从 limit=500 对齐到 5000，避免刷新后列表从 5000 缩到 500。

#### 新增「卡面库」页面与卡面素材
- 导航新增「卡面库」（素材库下方、日志上方，写进 NAV_KEYS 与默认页签，老用户的导航由 parseTabs 自动补位）。
- 素材：scripts/fetch-card-assets.mjs 从 GitHub HarukaKinen/Cardentify 的 main 分支 Cards/ 抓取（该仓库默认分支已变成 DMCA，脚本固定用 main），107 家银行的 436 张卡面统一转 WebP（最长边 1000px、质量 82），312MB 原图压缩到约 20MB，落地 public/uploads/cards/<国家地区>/<卡类型>/<银行>/<卡名>.webp 并生成 manifest.json；脚本可重复运行（默认跳过已存在，支持 --force / --limit / --bank）。
- 分类按上游 card.type 分 借记卡 251 / 信用卡 172 / 预付卡 1 / 其他 10，映射表另预留签账卡 / 取现卡 / 交通卡 / 礼品卡 / 虚拟卡；清单里同时记录卡组织（银联 / Mastercard / Visa / AMEX / JCB / Mir）与等级（普卡 / 金卡 / 白金 / 世界 / 无限…）。
- 页面 /cards：地区与类型两级胶囊筛选（带数量）、搜索（卡名 / 银行 / 卡组织）、响应式卡面网格（图片懒加载），点击卡面弹出大图与元信息，不含下载按钮；弹窗底部预留「录入金额」输入框与保存按钮（本版置灰占位）。
- 接口 /api/cards 登录后读取清单（60 秒内存缓存）；public/uploads/cards/ 加进 .gitignore，卡面属于本地素材，不进仓库、不随镜像分发。

#### 卡面库上线录入金额与更多筛选
- 录入金额：新增 card_amounts 表（user_id + cardKey 主键，金额 / 币种 / 备注 / 更新时间）与 lib/cardAmounts.ts，接口 PUT/DELETE /api/cards/amounts（校验金额 ≥0 且 ≤1e12、卡面标识 ≤300 字符、备注 ≤100 字符，并带限流），GET /api/cards 顺带回当前用户已录入的金额。
- 弹窗里的金额输入框与币种（按地区自动预选，如中国内地→CNY、中国香港→HKD、美国→USD）、备注已可用，保存后卡片左下角显示金额胶囊、顶部「已录入 N」可一键只看已录入；cardKey 在服务端做分段 URL 编码归一化（幂等）。
- 筛选与打磨：地区与类型常驻，「更多筛选」展开后是卡组织 / 等级 / 主题；主题标签按卡名关键词自动生成（联名 IP、虚拟卡、生肖、纪念限定、校园青年、商旅航空、动物萌系、数字银行、金卡白金以上），卡片上最多显示 3 个、弹窗显示全部；卡面悬停有上浮、图片轻微放大、渐变压暗与右上角类型角标。

#### 卡面库新增金额汇总、我的标签与分页加载
- 金额汇总：卡片列表上方新增一条汇总 —— 按币种分别合计，再用当前汇率折算成显示货币给出「≈ ¥xx」；汇率沿用全站链路（首帧 FALLBACK_RATES、挂载前合并本地缓存、随后 /api/rates 刷新）。ECB 不发布 MOP，因此给澳门元补了「与港币锚定 1 HKD ≈ 1.03 MOP」的兜底汇率，RUB / KZT 没有可用汇率就单独标注「未计入折算」。
- 我的标签：新增 card_tags 表（user_id + cardKey + tag 主键）与 PUT /api/cards/tags（覆盖式保存、去重、去空格、单个 ≤12 字、最多 10 个，带限流），GET /api/cards 一并返回；弹窗底部可增删标签（回车添加，并给出常用建议），我的标签用蓝色区分于自动主题标签，卡片上最多显示 3 个，筛选区新增「我的标签」一组。
- 网格分页加载：一次只渲染 60 张，底部「加载更多（剩余 N 张）」按钮继续追加（筛选条件变化自动回到第一屏），434 张不再一次性铺满 DOM。

#### 卡面库改为原图直存
- 把抓取脚本的默认格式从「转 WebP 压缩」改成「原图直存」：新增 --format=original|webp（默认 original），original 模式不再走 sharp 转换，按上游原始字节与原始扩展名保存（.jpeg 统一成 .jpg）。
- 重抓结果：436 张全部成功、0 失败，占用 313MB，格式分布与上游完全一致（PNG 395 / JPG 20 / SVG 19 / WebP 2）；顺带修好了 WebP 模式里因超出 XML 解析上限而失败的那张玉山银行超大 SVG。
- 清单为每张卡补了 bytes（原图字节数），卡面弹窗底部新增一行「原图 PNG · 1.3 MB · 中国内地 / 借记卡」；日后想换回轻量版可运行 --format=webp（约 20MB，网格更快）。

#### 卡面库改为「我的卡包」
- 新增 card_holdings 表（user_id + card_key 主键）与 PUT /api/cards/holdings（加入 / 移出，带限流），GET /api/cards 一并返回持有的卡。
- 页面右上角是分段切换「我的卡 N / 全部卡面 436」，默认进「我的卡」——只显示自己持有的卡，没有则显示「还没有添加卡片」并给出「去全部卡面挑一张」的入口。
- 在「全部卡面」模式下，每张卡右下角有「+ 加入」（已持有的显示「移出」）、左上角有蓝色「我的卡」角标；弹窗底部也有一键「加入我的卡 / 移出我的卡」。
- 录入金额的卡会自动视为持有（服务端处理），避免出现「录了金额却在默认列表里看不到」。

#### 卡面库筛选区按参考站重构
- 「类型」胶囊移到最上面一行（保留计数显示）。
- 下面改成一排下拉框 —— 地区 / 银行 / 卡组织 / 等级 / 主题（有自定义标签时再加「我的标签」），样式对齐参考图（浅色圆角 + 右侧箭头，深浅色都适配）；银行列表跟随所选国家地区，选「全部地区」时按地区分组用 optgroup 展示，切换地区会重置已选银行，银行取值用银行文件夹名保证唯一。
- 搜索框独立成一行，不再挤在标题右侧。
- 胶囊样式换成参考图的实心选中态：选中 = 深色实心 + 白字（深色模式反相为白底深字），未选中 = 白底细边框 + 深字，并去掉原来包住胶囊的边框盒子；「已录入 N」与「我的卡 / 全部卡面」切换统一成同一套胶囊。该实心选中态是本页按参考图定制的，未改动全站「中性色按钮标准」。

#### 卡面库地区下拉按洲分组排序
- 地区下拉原按卡面数量排，中国内地 / 台湾 / 香港 / 澳门被拆散夹在美日英中间，改为按洲分组（optgroup）排序：亚洲（中国内地 → 中国台湾 → 中国香港 → 中国澳门 → 日本 → 新加坡 → 哈萨克斯坦）→ 欧洲（英国 → 俄罗斯 → 德国 → 爱尔兰）→ 北美洲（美国 → 加拿大）→ 大洋洲（澳大利亚）。
- 洲内规则是「中国各地优先，再按卡面数量」；银行下拉在下探到「全部地区」时同样按这个顺序分组，组名带洲前缀（如「亚洲 · 中国内地」）。

#### 卡面库新增「我的卡」总览条
- 在搜索框下方加了一条「我的卡」总览：我的卡 N 张、各类型张数（按数量排）、额度合计 ≈ ¥xx（沿用按币种合计 + 汇率折算的口径，没有录入时显示「未录入（打开卡片可录入金额）」）。
- 第二行保留按币种的明细胶囊（CNY 12,000 ×3）与「无汇率未折算」提示；原来那条只按币种的「金额汇总」合并进这条总览，信息不再重复。
- 顺带修一处不一致：筛选区的计数原来一律按全量 436 张统计，在「我的卡」模式下会出现「我的卡只有 3 张、类型却写着借记卡 251」这种矛盾；现在所有计数都跟随当前模式，银行下拉也只列当前作用域里出现过的银行。

#### 卡面库筛选支持多选
- 每个维度从单选改成多选：类型胶囊直接多选（点「全部」清空选择），地区 / 银行 / 卡组织 / 等级 / 主题 / 我的标签换成自定义多选面板（按钮显示已选摘要，展开后逐项勾选，带计数与洲 / 地区分组标题）。
- 筛选语义：同一维度内 OR、跨维度 AND。
- 分面计数同步适配多选：统计某维度时排除该维度、其余条件生效，因此勾了「借记卡」后地区与银行下拉里的数字会自动变成借记卡的分布。
- 另加「清空全部筛选」按钮（有任一条件时出现，连带清空搜索词）；银行下拉在选了多个地区时用「洲 · 地区」分组标注避免同名混淆；顺手删掉改用多选后不再需要的单选下拉组件。

#### 卡面库地区下拉加国旗
- 地区多选面板里每个地区前面加上对应国旗：新增「地区 → ISO 二字码」映射（中国内地 CN / 中国香港 HK / 中国澳门 MO / 中国台湾 TW / 日本 JP / 新加坡 SG / 哈萨克斯坦 KZ / 英国 GB / 德国 DE / 爱尔兰 IE / 俄罗斯 RU / 美国 US / 加拿大 CA / 澳大利亚 AU），用现成的 CurrencyFlag 渲染。
- 它优先取素材库里用户上传的自定义旗帜、没有再回退内置的本地 SVG（public/uploads/asset/flag/*.svg，14 个码全部存在），不依赖任何远程 CDN；国旗同时出现在多选面板的每一行与选中后的按钮摘要里。

#### 卡面库新增「卡包」
- 标题旁新增「卡包」入口（堆叠图标按钮），点开进入整屏深色堆叠视图（components/CardWalletStack.tsx）：GSAP 驱动上下滑动切卡 —— 拖动跟手、松手按距离与速度判定翻页（首尾有阻尼回弹），滚轮 / 方向键 / PageUp·Down / 空格 / 点按非顶卡都能切换，卡片按银行 / 余额 / 卡名排序（排序偏好 localStorage 持久化）。
- 每张卡都有正反两面：点按最上面的卡进详情，卡片可 3D 翻转，反面按真实银行卡画了磁条、签名栏、安全码、有效期与尾号（卡号 / 有效期 / 安全码只存本地数据库，详情里可一键显示 / 隐藏）；详情里能编辑卡号 / 有效期 / 安全码 / 备注 / 币种。
- 余额历史：每张卡记「存钱 / 取钱 / 余额调整」流水（含日期与备注），余额 = 首笔发生前的余额 + 逐笔重放，删除任意一笔会自动重算后续余额，与卡面库金额胶囊、总览条是同一份数据。
- 落地：新增 lib/cardWallet.ts、lib/cardCurrencies.ts（14 种币种 / 地区默认币种 / 金额与卡号格式化）、/api/cards/wallet（GET 卡背与流水 / PUT 卡背 / POST 记流水 / DELETE 删流水，均带登录鉴权、限流与卡面 key 归一化）；数据库新增 card_details 与 card_balance_history 两张表，cardLibraryForUser 首屏一并注入卡背信息。

#### 卡包余额联动资产分析
- 新增 cardCashByCurrency()（lib/cardLibrary.ts）：只统计持有中的借记卡与预付卡余额 —— 信用卡的「金额」是额度不是余额，一起相加会让净资产虚高；卡币种取值顺序与卡包显示完全一致（金额上的币种 → 卡背币种 → 地区默认币种），保证「卡包上看到的这个余额」和「算进现金的这个余额」是同一笔；没有汇率的币种宁可不算，也绝不按 1:1 当成美元。
- /api/v1/funds 把它并进对应币种的 balances 并额外返回 cardCash 明细，于是资产分析的可用现金与净资产、资金系统的期末总资产、我的持仓的总资产都是同一份现金口径。
- 同时并进 summaries[币种].otherNetFlow：资金系统的等式是「盈亏额 = 期末总资产 − 期初总资产 − 当期净投入」，卡里的钱只进期末、不进净投入会被算成投资收益，并进其他净流入后等式依旧成立。
- 资产分析「账户资产」新增银行卡现金统计卡（按显示货币折算，悬停说明「信用卡额度不计」），资金系统的温馨提示与卡包详情底部各补一条口径说明；顺带修一处会吃掉卡余额的边界：简化版账本按「该市场总资产 − 持仓」反推现金时会整块覆盖该币种余额，现在把卡余额加回去。

#### 资金系统币种扩到全币种
- 上一版卡包的「券商账户」联动只在资金系统的 7 个币种下出现，台币 / 澳门元 / 英镑这类卡没法用，因此把资金系统的币种扩到所有能折算的币种。
- 新增 lib/fundCurrencies.ts（客户端与服务端共用，不引入服务端依赖）：FUND_CURRENCIES 共 14 个 —— 7 个主币种，加上卡面素材里实际会出现的台币 / 澳门元 / 英镑 / 澳元 / 加元，以及有汇率但暂无卡面的印度卢比 / 巴西雷亚尔；口径是能折算才收（每个币种在 FALLBACK_RATES 里都有兜底），卢布 / 坚戈不进清单，避免跨币种汇总时按 1:1 当成美元误算；同时提供 label / 符号 / 国旗二字码、isFundCurrency()、emptyFundBalances()、fundCurrencyOptions()。
- 数据库：fund_transactions 的 currency 约束从硬编码币种列表改成通用的「三字母大写」约束；旧库检测到硬编码 IN 列表即无损重建（连索引一起重建），以后再加币种不必再动表。
- 后端：fundBalances() / fundSummaries() 按新清单初始化，并给清单外的历史脏币种加 || 0 兜底（否则会让整个币种余额变成 NaN）；/api/v1/funds 的校验改走 isFundCurrency() 并统一转大写；数据备份导入导出的币种白名单同步。
- 前端：CurrencySelect 新增 options 参数（默认仍是全站 7 个展示币种，顺手删掉从未使用的 allowedCodes），资金系统面板与「记一笔」弹窗改用 14 币种清单（带国旗）；资金余额相关类型从 CurrencyCode 换成 FundCurrency，把「展示币种」与「资金币种」分开，卡包的存钱 / 取钱券商开关改用 isFundCurrency() 判定。
- 验证：实测 GBP 资金流水写入成功（说明数据库约束已放宽）、RUB 被正确拒绝；再把一张卡临时改成 GBP 做联动存钱，返回 brokerLinked: true 且生成的 card-link 流水币种为 GBP，随后删流水并还原币种。

#### 卡面库支持上传自定义卡面
- 数据库 card_details 新增 image 字段（空 = 用清单原图，非空 = 自定义卡面地址）。
- 上传接口放行 folder=card，落盘到 public/uploads/asset/card/，命名按「银行名 + 卡名 + 地区码」（如 中国银行长城借记卡CN.png，符合素材命名规范）。
- /api/cards/wallet 的 PUT 接受 image，并只接受本站 /uploads/ 下的地址（空字符串 = 恢复原图）；外链、data: 与 javascript: 一律 400 拒绝。
- 卡面库弹窗新增「卡面」一行：显示当前是「清单原图」还是「自定义照片」，并提供「上传卡面」按钮（建议标准卡面比例 1.586:1、JPG/PNG/WEBP、最大 2MB）与「恢复原图」。
- 展示端统一走 cardCover()：卡面库网格、弹窗大图、卡包堆叠视图与卡背正面都会使用自定义卡面；自定义卡面是每用户每张卡独立的覆盖。
- 顺带修一处会误删上传文件的隐患：lib/fileCleanup.ts 的引用判断与孤立文件清理此前不认 card_details.image，清理一次就会把刚上传的卡面当孤立文件删掉，现在两处都把它计入引用。

#### 素材库新增「卡片」分类并统一卡面来源
- 素材库新增「卡片」分类：清单里的 436 张卡面全部登记成 assets.type = "card"（id 约定 card:{卡面文件}，大小写敏感、不走 upsertAsset 的大写化），进类目时幂等补齐、已换过图的卡绝不覆盖回去；列表按地区与卡名排序、支持搜索与分页，点缩略图即可换图，换过图的卡多一个「恢复原图」。
- 列表与网格（卡面墙）两种展示方式可切换；展示方式按约定走 URL（?view=grid，list 为默认值不写进地址栏），刷新、前进后退与分享都保持，切换时回到第 1 页；分页改成按「排」算而不是固定 10 条 —— 列表 6 排、网格 3 排，列数跟随 sm / lg / xl 断点实时重算（436 张：列表 18 条/页、网格 15 条/页，页数从 44 页降到 25 / 30 页）；换图入口去掉文字，悬停只留一层很淡的暗色蒙层，上传中显示转圈；列表与网格切换图标改为 1.5 描边、方格圆角 + 2.8 单位间隙，未选中态从 text-muted 收到 text-faint。
- 卡面统一到素材库：此前卡面有清单原图与个人覆盖两套来源，会出现「在素材库换了图、卡面库没变」；现在卡面库网格、弹窗大图、卡包堆叠与卡背正面统一读 /api/cards 新下发的 covers，卡面库弹窗里的「上传卡面」也改成写这条素材，card_details.image 那套个人覆盖随之废弃并停止写入。
- 卡包详情也能换卡面（与素材库·卡片是同一条）；卡号与安全码的显示切换从文字按钮改成卡片详情右下角的眼睛图标（睁眼 = 已显示、闭眼加斜杠 = 已隐藏，按下时高亮），文案位置只留一个状态提示，避免与图标重复。

### 修复

#### 财报日历文案订正
- 页脚原文「美股数据来自 Nasdaq，A股数据来自东方财富预约披露，行情实时更新，仅供参考」把财报口径写成了行情，改为「财报实时更新」，并按新接入的港股补上「港股数据来自雪球财报日历」。
- 页面副标题从「美股 / A股财报」改为「美股 / 港股 / A股财报」。

#### 财报日历明细列序调整
- 明细表把「市值」与「现价」两列互换：现在是 公司 / 现价 / 时段 / EPS 预期（A股为报告期、港股为业绩类型）/ 市值，现价紧跟公司名，市值退到最后；列宽同步调整为现价 112px、市值 112px，md 档位保持 公司 / 现价 / 时段 / 预期 四列。
- 「现价」表头原来居中、数值右对齐，表头改为右对齐，与数值落在同一条竖线上。
- 日历里点日期弹出的紧凑卡片去掉币种符号（$1.05T → 1.05T、$951.08 → 951.08）：fmtCapByMarket / fmtPriceByMarket 增加 withSymbol 开关，明细表里仍保留符号；顺带把港股现价补上 HK$ 前缀。

#### 修复全站股票图标刷新后闪现首字母
- 现象：刷新页面后，持仓 / 自选 / 资产分析等列表里的股票图标先显示首字母占位（S / T / 英 / 标…），随后才切换成真实图标。
- 根因：layout 已经按当前记录算好并在服务端注入了图标表 initialStockIcons，但 RecordsApp 只在 useLayoutEffect 里调用 primeStockIconCache，而子组件的首次渲染发生在父组件渲染阶段、早于父组件的 layout effect，那时共享缓存还是空的。
- 修复：把预热提前到渲染期（useMemo(() => primeStockIconCache(initialStockIcons), [initialStockIcons])，该函数只写缓存、不通知订阅者，因此不会打断水合），并把 useAssetIcons 里「读 localStorage 缓存」从 useEffect 移到 useLayoutEffect。
- 验证：对 /asset-analysis 的首屏 HTML 做 A/B 对比 —— 修复前 3 个 <img>、0 个股票图标，修复后 13 个 <img>、其中 10 个是股票图标。

#### 收益日历偏好持久化与市场图标随首屏下发
- 收益日历的市场选择此前只在资产盈亏分析页做了持久化，资产分析页新增的日历模块是纯 useState，刷新就回到「全部」；现在把日历偏好（市场 / 月份 / 年视图 / 收益-收益率）抽成 lib/pnlCalendar 的 readPnlCalendarPrefs / savePnlCalendarPref，两个页面共用同一组 localStorage key，刷新与跨页都保持。
- 市场图标（素材库 type=market）改为随首屏 HTML 下发：新增 getMarketIconMap() 与 primeMarketIconCache()，layout 把 6 个市场图标一并放进 preload，RecordsApp 与 HomeContent 在渲染期预热共享缓存，市场下拉与筛选按钮首帧就是真实图标。
- 「当日盈亏」明细弹窗每一行加上股票图标（沿用全站 stockIcons，缓存未命中时回退首字母）。

#### 修掉输入框与下拉的蓝色聚焦框
- 现象：卡面库的搜索框与下拉一聚焦就出现一圈深蓝色描边，和全站的中性风格不搭。
- 根因：globals.css 的「点击 / 激活 / 聚焦一律无轮廓、无光晕」只覆盖了 button / a / [role=button] / input[type=button|submit]，文本输入、文本域与下拉没有被覆盖，保留了浏览器默认的 focus ring。
- 修复：把 input:not([type=checkbox]):not([type=radio])、textarea、select 的 :focus 与 :focus-visible 一并补进全局 outline: none 规则（复选框与单选框保留自己的焦点样式，避免误伤行情板等已有设计）；同时给卡面库的搜索框、下拉、金额 / 备注 / 标签输入补上全站同款的中性灰柔光焦点反馈。

#### 卡面库筛选计数改为分面联动
- 现象：搜「招商」后列表只剩招商银行的卡，但「类型」胶囊仍写着 全部 436 / 借记卡 253 / 信用卡 172 / 预付卡 1 / 其他 10。
- 根因：各维度的计数是拿「当前模式的全量集合」直接算的，没有参考搜索词与其他筛选。
- 改为标准的分面统计：统计某个维度时把这个维度自己排除在外、其余条件全部生效 —— 搜「招商」时类型胶囊显示 全部 85 / 借记卡 60 / 信用卡 24 / 其他 1，卡组织下拉只剩 UnionPay 83 / MasterCard 1，银行下拉只剩招商银行。
- 地区与银行的下拉选项列表本身也随条件收敛，不会出现「选了条件却列出一堆必然为 0 的选项」。

#### 「我的卡」总览条只在我的卡模式显示
- 上一条总览在切到「全部卡面」挑选时也会出现，而那时页面统计的是全量 436 张，两套数字并排容易让人以为统计错了。
- 现在总览条只在我的卡模式下显示；「全部卡面」模式的规模信息由标题下那行（全部卡面 436 张 · 14 个地区）承担。

#### 卡面格子改为「卡片底托」
- 现象：只有自带透明圆角的素材看起来像一张实体卡，满幅矩形素材贴到格子底边就成了直角、和下面的标题区连成一片。
- 实测素材本身没有问题：436 张里 435 张都是 1.57–1.62 的标准卡比例，sharp trim 也确认没有留白，差别只在素材自身有没有做圆角。
- 改法：给每张卡加一层「卡片底托」——图片外层留 10px 内边距并铺浅灰底（深色模式为白色 4% 透明度），图片本身用 10px 圆角裁切、一圈极淡描边与轻阴影；悬停放大、金额胶囊、我的卡角标、类型角标与「+ 加入」全部保留在卡面内。

#### 修复多选下拉勾选后面板关不掉
- 现象：多选面板里勾一项之后面板关不掉、页面其他操作也点不动。
- 根因：面板外层用的是 <label>，而 label 会把内部任意点击转发给它关联的控件（这里是第一个可聚焦元素，即开合按钮），于是「点选项 → 面板关闭 → label 再把这次点击交给按钮 → 又打开」。
- 修复：把多选组件的外层 <label> 换成 <div>（自定义下拉不需要 label 语义，无障碍属性由按钮的 title 与文案承担）；同页其余 label 都包着原生输入控件，属于正确用法，保持不变。

#### 卡面库首屏改服务端注入
- 现象：刷新 /cards 后要等一会儿卡片才出现。实测接口本身只要 11–30ms（清单 230KB、JSON 解析 0.9ms），真正的开销是串行链路 —— 首屏 HTML 里完全不含卡面数据，必须等 JS 下载与水合完成才能发起 /api/cards。
- 改法沿用股票图标与市场图标的同款做法：布局在服务端鉴权后直接读清单与该用户的持有 / 金额 / 标签（新增 lib/cardLibrary.ts，清单 60 秒内存缓存），只在访问卡面库这一页时注入 initialCardLibrary，RecordsApp 透传给组件并在 useState 初始化时使用。
- 服务端与客户端首帧一致，水合不打架；挂载后仍会静默刷新一次 /api/cards，有注入时不再显示骨架屏，请求失败也不打扰。
- 验证：给 demo 临时加入 3 张卡后抓 /cards 的 HTML，首屏直接含 3 个 /uploads/cards/ 的 <img>（修复前为 0）。顺带把卡面 key 的长度上限从 300 提到 600 字符。

#### 下拉面板滚动条改细
- 卡面库的多选面板用的是浏览器默认滚动条，在深色背景下又宽又抢眼。
- 新增全局工具类 .thin-scrollbar：滚动条宽 4px、轨道透明、滑块用 999px 圆角与半透明灰（悬停略深），scrollbar-width: thin 让 Firefox 同样细，深色模式滑块换成 26% 白色。

#### 修复多选面板相邻已选项的底色粘连
- 现象：在多选面板里连着勾两项，两行的选中底色上下贴死，中间的圆角被吃掉，看起来像一整块色带。
- 根因：面板容器只有 p-1 的内边距，各选项行之间没有任何垂直间距，两个 rounded-lg 的相邻底色相接时圆角叠在一起。
- 修复：给面板容器加 space-y-1（相邻行之间留 4px 间距），每行各自保留独立圆角；六个多选面板共用同一个组件，一处改动全部生效。
- 验证：在本地开发服务器上用无头 Chrome（430px 宽、深色模式、勾选中国内地 + 中国台湾）抓了修复前后两张对照图。

#### 券商与卡转账联动记账
- 重复计算：卡包「存钱 / 取钱」记的那笔钱如果本来就在券商账户里，券商账本不会自动少一笔，卡余额被算进现金、券商那边的钱也还在，总现金直接翻倍。
- 现在卡包的存钱 / 取钱弹窗加了一个苹果风格开关「这笔钱是从券商账户转来的 / 转回券商账户」（只在资金系统支持的币种下出现）：打开后 addCardBalanceEntry 在同一个数据库事务里再写一笔方向相反的资金流水，两边永远对称；流水 id 用 card-link-{卡包流水 id}，删卡包那笔时联动流水一起删，资金记录里标为「自动」且不可单独删。
- 实测：CNY 卡存 100 后券商账本 -100、卡余额 +100，总额仍是 10,719.50（改动前会变成 10,819.50）；取回 100 反向对称；两笔都删掉后完全回到初始状态。
- 总览口径：卡面库「我的卡」总览原来只有一个「额度合计」，而「金额」在借记卡上是余额、在信用卡上是额度；现在拆成余额合计（借记卡 / 预付卡）、额度合计（信用卡）、其他合计三档，各自换算成显示货币并标注缺汇率的币种，悬停说明「余额合计就是计入可用现金的那一份、额度是可透支上限不计入」。
- 顺带修 createFundTransaction 的返回值：原来用「最近一条」回读，补录旧日期的流水时返回的不是本条，改为按 id 回读。

#### 大陆借记卡不显示安全码
- 现象：卡包详情里，中国大陆的借记卡卡背同样画着 CVV 白方块、卡片信息里也列着「安全码」一行，而大陆借记卡背面本来就没有安全码。
- 修复：新增 hasSecurityCode(card) 规则（中国内地 + 借记卡 → 不显示；其他地区与卡种一律保持原样，规则集中在一处），三处保持一致：卡背不再画 CVV 方块（签名栏顺势拉满）、卡片信息不列「安全码」、编辑弹窗不出现安全码输入框。
- 「隐藏卡号 / 安全码」文案按是否有安全码自适应成「隐藏卡号」，底部说明补一句原因；保存时这类卡不再提交 cvv 字段（接口按「只覆盖传入字段」处理，历史残留值既不会被写回、也不会被这次保存改掉）。

#### 清掉无安全码卡片上残留的 CVV
- 上一条把大陆借记卡的安全码字段藏起来之后，早期数据里已经填过的值既显示不出来、也改不掉。
- 新增 sweepLegacyCardCvv()（lib/cardLibrary.ts）：按 hasSecurityCode() 找出「本来就没有安全码」的卡，把它们 card_details.cvv 非空的行清空；它是幂等的（只 UPDATE 非空行，跑完再跑没有匹配），且每个进程只在第一次读卡面库时跑一次；清单还没抓到、拿不到卡类型时直接跳过。
- 同时把「有没有安全码」的规则抽到新文件 lib/cardSecurity.ts，客户端展示与服务端清洗共用同一份判断。实测 demo 库里大陆借记卡的 516 已清空，香港借记卡（327）与信用卡（812）按规则保留。

#### 修复换卡面会删掉清单原图
- 现象：在素材库「卡片」里把某张卡换成自己的照片后，那张卡的清单原图文件被删掉了，再点「恢复原图」会指向一个不存在的文件。
- 根因：upsertAsset 在 url 变化时会调用 removeFileIfUnused(旧 url)，而 urlReferenced() 只认 assets / 网站设置 / 用户头像 / 名人头像 —— 卡面清单原图（public/uploads/cards/**）由卡面库直接从 manifest.json 读取，数据库里没有任何字段引用它，于是被判成孤立文件删掉。
- 修复两处：urlReferenced() 对 /uploads/cards/ 下的文件直接返回 true（清单原图永久保留，它不是用户上传物，整个目录由抓取脚本管理）；cleanupOrphanFiles() 扫描时整棵跳过 public/uploads/cards/（否则一次孤立文件清理会把 436 张原图全清掉）。
- 验证：复现「换图 → 恢复原图」两步，修复前原图文件消失、修复后原图仍在且 /uploads/cards/... 返回 200；测试中被误删的那张原图已从 git 恢复。

---

## v0.1.27 · 2026-09-11

### 新功能

#### 指数栏以东方财富为主并与富途低频对账
- 数据源定案：指数栏继续以东方财富为准（覆盖全部指数、无连接与额度约束、实测 0.17–0.21 秒），富途只做低频校对 —— 实测富途 OpenAPI 只支持恒指 HK.800000、恒生科技 HK.800700、上证 SH.000001、深证 SZ.399001、日经 JP..N225，美股指数接口明确「暂不支持」、新加坡指数无权限、韩国市场 OpenAPI 没有。
- 对账实现：每个交易日、每个指数收盘 10 分钟后（当日分时已定型，开市时两边延迟不同会误报）拉一次富途 1 分钟收盘序列，与东财逐根按当日第 N 根对齐比较（不按时间戳，避免时区差异），错位超过 20% 根数判为口径漂移并打 warn；结果写入 /api/ticker 与 /api/health 的 tickerCrossCheck。
- 实测：收盘口径 0 根不一致、不误报；把字段换回开盘口径（修复前的 bug）则 299/331 根不一致、最大差 65.6 点，能被准确抓住。
- 轮询退避补齐到四档：开市 60 秒 → 刚收市 5 分钟 → 隔夜 15 分钟 → 跨夜与周末、长假 30 分钟；并修掉「上游全部取不到时被误判成收市」的漏洞（取不到一律保持 60 秒尽快恢复）。scripts/futu_quotes.py 的代码映射补上 SH / SZ / JP（原来只认 US / HK / CN）。

#### 设置-站点信息改为常驻可编辑
- 按主流做法（macOS 系统设置、Linear、Vercel 的自动保存）收掉重复操作：站点信息不再分「只读浏览」与「正在编辑」两态，字段常驻可编辑，改动仍由已有的全局自动保存落库（700ms 防抖与胶囊提示，失败变红）。
- 因此去掉分区里的「编辑」铅笔与「保存」按钮，标题栏的铅笔与对勾在这个分区也隐藏（其余分区仍需要它做完提交），头部状态文字在该分区显示「修改自动保存」。

#### 设置分区折叠优化
- 设置已按侧栏拆成独立条目，但首页指数设置、首页导航、应用导航菜单仍是「默认收起」，每次进来都要手点一次。改为默认展开，并保留「手动收起后记住」的行为（storageKey 升到 v2，让新默认对老用户也生效）。
- 股票来源接口字段多且低频，保持默认收起但同样记住展开。
- 另外补一个通用改动：SettingsSection 新增 reveal —— 点分区里的「编辑」时自动展开，避免对着收起的分区进入编辑态却看不到内容（四个分区都接上了）。

#### 交易广场内容口径定案
- 动态以「现有内容 + 往后持续追加新帖」为准 —— 不设时间窗（不做「只留最近一个月」）、不截断历史（作者上限设为不截断）、也不往回抓历史，段永平与特朗普一视同仁。
- 客户端首屏缓存仍单独收敛：每位作者最多缓存 400 条、2.5MB 上限，超限跳过写入并 console.warn（列表展示不受影响，只是下次刷新先走骨架）。
- 实测接口返回 491 条（特朗普 291 + 段永平 200），最早 2026-07-19、最新 2026-09-10，日期与排序正常。

#### 交易广场首屏缓存加保护
- 取消接口上限后历史会一直增长，客户端 localStorage 缓存有写爆风险（写失败会导致下次刷新先看到加载态）。
- 缓存层单独限制为每位作者最近 400 条，并加 2.5MB 体积上限：超限则本次跳过写入并 console.warn，列表展示不受影响（列表本身仍是全部历史）。

#### 交易广场取消每位作者 200 条上限
- 此前列表接口每位作者最多返回 200 条（为修水合崩溃加的），界面上没有说明。
- 按要求取消限制：TRADING_SQUARE_AUTHOR_LIMIT 设为不截断，接口改为返回全部历史；同时给 takeNewestByAuthor 补上显式时间倒序（原先只有截断时才会排序，取消上限后必须自己排，否则本地并集合并会把旧帖追加到末尾打乱顺序）。
- 实测接口由 400 条变为 491 条（特朗普 291 + 段永平 200）。

#### 设置编辑态补上「取消」
- 原来编辑态只有「保存」，没有回退入口，主流做法（GitHub 与 Stripe 的行内编辑、iOS 编辑页）都是「取消 + 保存」成对出现。
- 做法：新增 lastSavedRef 记录「上次保存成功」的完整设置深拷贝（含 tabs 等独立状态，tabsRef 同步避免闭包取旧值），编辑态的分区头部在「保存」左边渲染一个次要的「取消」；点取消即回滚 site 与 tabs、重新对齐自动保存基线（避免回滚本身触发自动保存）、退出编辑态并提示「已取消未保存的修改」。
- 覆盖首页指数、首页导航、应用导航菜单、网站形象、股票来源接口、翻译服务、交易广场、数据库等所有编辑态分区。

### 修复

#### 修复金额水合不一致
- 现象：资产分析页与我的持仓页刷新后开发环境弹出 hydration 报错，持仓总市值与现金在服务端渲染 51,392.88、客户端首帧 52,546.83。
- 根因：AssetAnalysisView.tsx 与 HoldingsView.tsx 的汇率 state 用 useState 初始化函数直接读 localStorage 的 fire:rates —— 服务端读不到、只能用 FALLBACK_RATES，客户端首帧读到实时汇率，跨币种金额必然不同。
- 同类写法还有：持仓页的 ratesReady 与各市场盈利卡片顺序、盈亏分析页的日历市场 / 基准 / 加权 / 日历月份四个本地偏好、全球预览页的迷你 K 线缓存。
- 修复：新增 lib/ratesCache.ts 统一读写汇率缓存（注释写明只能挂载后读），上述读取一律改为「首帧用默认值 + useLayoutEffect 恢复」—— useLayoutEffect 先于绘制，因此既不会水合报错，也看不到兜底值闪烁。

#### 修复交易广场段永平不更新
- 加了雪球登录 Cookie 后段永平仍停在 9 月 2 日。排查结论：不是 Cookie 没带上，而是抓取走了 /v4/statuses/user_timeline.json?type=0 —— xueqiu.com 被 WAF 返回 HTML 挑战页，api.xueqiu.com 的 v4 与 type=0 只给较早的原创帖，新回复与转发不会出现，刷新却被当成成功并写回旧缓存。
- 现在优先请求 api.xueqiu.com 的 /statuses/user_timeline.json（完整时间线，实测含 9 月 10 日新帖），并拉长超时、按数字判断 error_code（避免字符串 "0" 被当成失败）。
- 设置保存雪球 Cookie 时忽略空串与 ********，防止 GET 脱敏把已保存会话覆盖掉。

#### 修复交易广场页水合报错
- 雪球完整时间线修好后段永平缓存从约 200 条涨到 700 条以上，/api/trading-square/feed 约 700KB 以上。
- 交易广场首帧从 localStorage 读这份缓存，服务端只能渲染空列表，React 19 水合对不上导致整页报错；4 秒超时也容易把大包打断。
- 修复：首帧用空列表对齐服务端渲染，缓存与 URL 筛选在 useLayoutEffect 恢复；拉接口超时改为 12 秒；列表按作者各取最近 200 条，磁盘仍保留完整历史供翻页重叠。

#### 修复刷新时闪现市场色块
- 现象：设置 → 股票设置 → 市场色块「默认显示」关掉后，刷新任意页面仍会先显示几块市场色块再消失。
- 根因：色块读的是 lib/marketBadge.ts 的模块级 store，初始值固定为「显示 + 默认配色」，而服务端设置只在 RecordsApp 的 useLayoutEffect 里应用 —— effect 只在水合之后运行，因此服务端渲染出的 HTML 里本来就有色块。
- 修复：新增 primeMarketBadges()（渲染期写入模块状态、不通知订阅者，避免打断水合；值没变时不操作），RecordsApp 在渲染一开始就按服务端设置初始化；applyMarketBadges() 改为仅在值变化时通知，自定义配色的闪动也一并消失。
- 验证：同一账号（色块关闭）对 /holdings 的服务端 HTML 做前后对比 —— 修复前含 1 处色块，修复后 0 处。

#### 修复顶部指数栏趋势线取错价格
- 现象：顶部指数栏的迷你趋势线尾部与旁边的现价对不上（如恒指线尾 24956.94 而现价 24954.47）。
- 根因：lib/ticker.ts 解析东财分时数据时取了第 2 列（每分钟开盘价），应取第 3 列（每分钟收盘价）。
- 量化比对（恒生指数 2026-09-10，东财 331 根与富途 1 分钟 K 线 331 根）：开盘口径平均差 4.74 点、最大差 65.60 点、仅 1 根完全一致；收盘口径 331/331 完全一致。
- 顺带记录：富途里恒指代码是 HK.800000（不是 HK.HSI，应用的 _to_futu_code 会把 HSI 补成 00HSI 而查不到）。

#### 交易广场贴文可读性优化
- 段永平长回复里会露出 &#34;、**加粗**、行首 *；引用里的 @用户名没有高亮；Gemini 长文把一屏撑满；8 月 31 日 13:24 泡泡玛特配图被存成雪球 !thumb.jpg（60×80，约 3KB），原文是 2048×2731。
- 修复：展示与落盘前解码 HTML 实体并去掉轻量 markdown；@提及用雪球蓝字高亮并链接到主页；正文默认 5 行、引用 4 行，超出显示「展开」；下载配图时去掉 !thumb 与 !custom 后缀取原图，已存的缩略图下次刷新会重拉。

#### 交易广场股票名按涨跌着色
- 贴文里的股票名虚线原先固定灰色，改为挂载时拉行情：涨（changePct ≥ 0）用红色虚线，跌用绿色虚线，字体颜色不变；行情未返回前虚线仍用灰色。
- 同一代码多处出现只请求一次。

#### 交易广场股票卡片显示交易状态
- 鼠标划过贴文里的股票名，行情卡片恢复原排版：名称与市场色块、代码旁灰色时段（盘前交易 / 盘中交易 / 盘后交易 / 夜盘交易，周末休市）、现价涨跌、昨收与市值、行情时间，并去掉「点击查看详情」。
- 行情未返回时不再显示「正在获取行情…」，改为无文字骨架。

#### 个股详情补素材库图标
- 素材库有泡泡玛特（HK:09992）图标，但从交易广场打开个股详情只预热了持仓与自选图标，未持仓标的就变成首字母。
- 改为按市场与代码单独查素材库，港股 9992 与 09992 都能对上，不拉 3000 条以上全量。

#### 手机端财报日历日期格显示股票图标
- 手机端财报日历格子宽高不变：小屏日期靠左、右侧两排圆形图标与名称（过长省略），超过两家时格内上下滑动；较大手机仍是日期在上、两排公司。

#### 指数栏轮询改自适应
- 原来指数栏固定每 60 秒轮询一次，全部收市时段也在刷。改为按市场状态自适应，且不维护开市时间表（夏令时、节假日、半天市、交易所改时间都不用跟），让数据自证：服务端记下上一轮各指数的「最后一根分时时间 + 点数」，有任何一项前进就返回 pollSec=60，全部不动则返回 300。
- 客户端按返回的间隔轮询，同时保留「页面重新可见立刻刷新」与「后台暂停」。
- 踩过的坑：最初想按「分时时间与市场本地时间」判断开市，实测东财给美股的时间戳是北京时间（道指末行 03:34 对应当地 15:34），按时区算会把开市误判成收市，改成比较上一轮增量后完全不依赖时区。
- 验证：单元层面 5 组用例（首次拉取 / 有新数据 / 完全不动 / 新增指数 / 空列表）全部符合预期；实测美股开市期间间隔 62 秒取两次均返回 60 秒，点数 357 → 367 在增长。

#### 修复交易广场刷新后帖子消失
- 现象：交易广场刷新后列表变空，只剩骨架屏与「0 条」，左侧「全部动态」显示「— 条」。
- 根因：客户端合并函数 mergeFeedPosts 以「服务端本次返回为准」——只遍历 incoming，本地有而本次没返回的帖子直接消失；而服务端列表是会变短的（某位作者这次没抓到、源站分页变短、或新帖还没翻译被过滤），缩水后的结果还会被写回 localStorage 缓存。
- 修复：合并改成并集 —— 新数据照旧覆盖并回填译文与本地图片，同时保留「本地有、本次没返回」的帖子，顺序与条数仍由 takeNewestByAuthor 按作者截取最新 200 条，不会无限增长；真被源站删除的帖子会在本地多留一段时间，随 200 条上限自然淘汰。
- 另外把缓存写入失败改成 console.warn（配额满或隐私模式时能看出是缓存没写成）。

#### 交易广场刷新体验优化
- 刷新时页面会显示「尚未同步」「0 条」「— 条」这类文字，看上去像发文被清空了 —— 现在加载完成前这些位置一律留空，只保留静默的占位骨架；有数据后才显示条数与更新时间，并彻底去掉「尚未同步」文案。
- 拖动过交易广场窗口后，刷新会先画在默认位置再跳到保存位置 —— 根因是位置由 useDraggableWindow 的 useEffect（绘制之后）恢复；迁移到 CSS 变量后可以在首屏同步脚本里恢复：app/layout.tsx 的 head 脚本读取 fire:trading-square-window-pos，绘制前写进 :root，组件只在已知非零位置时才用 inline 覆盖，并给 var() 加 0px 兜底。

#### 设置页标题栏铅笔只负责进入编辑
- 现象：进入编辑态后标题栏的对勾与分区头部的「保存」都能提交，两个都像保存按钮，容易点错。
- 改为：标题栏那颗图标永远是铅笔（只表示「进入编辑」，不再变成对勾），编辑中它高亮成蓝色且禁用，悬停提示「正在编辑，改完点分区里的『保存』」；全站唯一的保存入口就是分区头部那颗「保存」。站点信息是常驻可编辑与自动保存，本来就不需要这个入口，标题栏对它继续隐藏。

#### 修复货币首屏 cookie 读取
- 上一版把货币偏好写进 cookie 并由布局注入，但实测无效：布局里从 @/lib/currencyPrefs 导入的 DISPLAY_CURRENCY_COOKIE 在服务端渲染时为 undefined，于是匹配永远失败，cookie 读出来是空的。
- 改为在布局里使用字面量常量名后立即生效：带 fire-display-currency=CNY 请求 /global，服务端输出 显示货币：人民币与 flag/cn.svg（不带 cookie 时仍是美元）。
- 老用户第一次加载会由客户端补写一次 cookie，之后不再闪默认值。

#### 修复刷新时闪现默认货币
- 现象：选了港元或人民币等展示货币，刷新时先闪一下美元（列表里第一个）再切回来。
- 根因：货币走 localStorage 与 usePersistedState，设计上「服务端与客户端首帧一律用默认值、挂载后恢复」，因此首屏 HTML 里没有用户偏好；全站 13 个文件用这个 hook，属于全局现象。
- 修复：偏好同时写一份 cookie（fire-display-currency，一年有效、samesite=lax），服务端布局读 cookie 并通过新的 CurrencyProvider 注入，useDisplayCurrency 的首帧值优先用它；切换货币时自动写 cookie，老用户没有 cookie 仍按默认美元。已接入 /[…slug] 与门户首页。

#### 首页导航显示状态改用眼睛图标
- 原来的「勾选框 + 显示」占一行且语义绕（勾上 = 显示），改为眼睛图标按钮：显示态睁眼、隐藏态闭眼（带斜线），悬停提示「已显示，点击隐藏 / 已隐藏，点击显示」，无障碍属性用 aria-pressed。
- 按钮固定 28px 宽、靠右对齐，和左侧拖拽手柄分列两端，多行之间自然对齐。

#### 手机端财报日历与桌面统一
- 手机端财报日历改成与桌面完全一致：格子尺寸、内边距、圆角、列间距、日期字号与「3 家 + 「+N 家」」的截断逻辑全部沿用桌面同一套，并删掉移动样式表里针对财报日历的 46px 小格子、隐藏公司行、隐藏悬浮卡片、隐藏「+N 家」等所有手机专属覆盖。
- 点日期弹出的公司卡片也与桌面同一套（同一个格内悬浮卡片、列表内部滚动），手机与桌面看到的是同一个组件与同一套交互。

#### 修复财报日历星期错一位
- 现象：点开日期（或自动展开今天）后，详情头部的星期与日历列对不上 —— 2026-09-11 的格子就在「周五」列，头部却写「周六」。
- 根因：WEEKDAY_ZH 数组是「周一…周日」的口径，而 new Date(...).getDay() 是「周日=0…周六=6」，直接拿它当下标等于整体错一位。
- 修复：新增 weekdayZh(y, m, d) 助手统一做 (getDay()+6)%7 换算（与日历网格首列算法同源），并加注释写明两种下标口径。

#### 全站排查并修复「日期错一天」
- 以财报日历为起点把全站日期写法过了一遍：getDay() 共 11 处，除财报日历那一处外其余口径都正确（4 个日历组件的周首日算法、个股 K 线的星期文案、周 K 聚合）。
- 查出同类的第二类写法 —— 用 new Date().toISOString().slice(0, 10) 取「今天」：它按 UTC 计算，东八区 00:00–08:00 会得到昨天。受影响的是资金「记一笔」的默认发生日期与日期选择器上限，以及当日盈亏、盈亏总额、持仓盈利图分享图与订单导出的文件名日期。
- 新增 lib/format.ts 的 localDateKey()（本地日历日期）替换这 6 处；周 K、财报与行情区间里的 toISOString 保持原样（那些是按 UTC 日期串对齐数据源）。

#### 财报日历头部调整
- 「实时」胶囊前的小圆点由中性灰改成苹果绿（#34c759，与全站开关同色），实时状态一眼可辨。
- 市场筛选常驻只留真有财报数据源的美股与 A股，港股 / 日股 / 韩股收进「更多」（chevron 展开与收起，避免点进去只看到「数据源暂未接入」的死入口），展开时三个市场插在 A股之后、「更多」始终排在最后一位，当前选中的市场始终保留在常驻位，拖动排序索引仍按完整市场顺序计算。
- 「共 N 家」从标题行最右侧移到「实时」右边，与月份导航、回到本月、实时组成一组状态胶囊，数字仍与筛选后的家数同源。

#### 财报日历当天明细改紧凑表格
- 现象：点开某天的公司明细一行一张独立卡片，时段 / EPS / 现价三块显示在行中段，右侧还空着一整列 —— 根因是桌面网格写了 5 列却只有 4 个内容块，现价落在第 4 列，最后一列永远是空白。
- 改为与素材库同一套表格写法：外层一个圆角容器 + 列头（公司 / 时段 / EPS 预期（A股为报告期）/ 现价），数据列固定 88 / 132 / 150px 与列头严格对齐，公司列占剩余宽度，行高 py-3 → py-2，去掉卡片阴影与悬浮位移，改用分隔线加行悬停底色。
- 同一列的数字纵向对齐成列；手机端维持原来的三列（公司 / 时段 / 现价），当天被筛选隐藏时不再渲染空的表头框。

#### 财报日历当天明细列宽调整
- 表格化之后仍有两个问题：公司列吃掉所有剩余宽度（约 60%），市值 / 时段 / EPS / 现价被推到最右边并平均分摊，列与列之间仍有十几到二十几个百分点的空档。
- 这一版：市值从公司名下方那行拆出来，在 lg（≥1024px）起单独成列放在公司右边；四个数据列全部改成固定窄宽（市值 96 / 时段 80 / EPS 112 / 现价 112）并紧挨着排（列间距 16px），公司列只占剩余宽度（约 350px）。
- 明细区（日期 + 表头 + 行）最大宽度收到 880px，右侧空白从「表格内部的列与列之间」挪到「表格外面」；md（768–1023px）保持四列（无市值列），市值继续跟在代码后面，手机端仍是三列。

#### 财报日历时段圆点重新配色
- 原来三个圆点是盘前琥珀 #e6a23c、盘后中性灰 brand、盘中浅灰 #9aa1ab —— 盘后与盘中两个点几乎分不出来。
- 按「一天的时间线」重配：盘前琥珀 #e6a23c（开盘前）、盘中蓝 #3297f6（交易中，沿用全站日历选中态的蓝）、盘后紫 #8b5cf6（收盘后），三个色相拉开；同时避开涨红 #e23d3d 与跌绿 #0fa07b，避免小圆点被误读成涨跌方向，未识别时段仍用中性灰 #9aa1ab。

#### 财报日历时段配色统一
- 圆点换色后，明细表里的「盘前 / 盘中 / 盘后」胶囊文字还是原来的琥珀与两种灰，筛选区那排标签也全是中性灰，跟圆点对不上。
- 现在全部对齐到同一份配色（TIME_DOT 为单一来源）：明细胶囊改成同色系浅底与同色系深色文字（盘前 #fff4e5 / #b06a00、盘中 #e9f3fe / #1b6fc9、盘后 #f2ecfd / #6a41d6），深色模式在 globals.css 补覆盖。
- 时段筛选的每个标签前面补一个对应颜色的小圆点（全部为中性灰），文字也用同色系颜色，选中的那颗用 ! 覆盖 .seg-active 的文字色，深色模式换成提亮版。

#### 财报日历当天明细区改回满宽
- 上一轮为了收紧列距给明细区加了 880px 宽度上限，在约 968px 视口下它的右边缘比上方日历网格短约 30px，看着像没对齐。
- 这一版去掉宽度上限，明细区（日期 + 表头 + 行）铺满卡片内容宽度、与日历网格左右对齐；同时把公司列从「吃满剩余宽度」改成封顶 420px，数据列继续固定窄宽紧挨着排，因此去掉上限也不会把四列重新拉开。

#### 财报日历当天明细表贴边
- 上一版放开宽度后，明细区的外框与日历网格对齐了，但表格本身还套在一层带内边距的面板里（面板 mx-5 + p-4），左右各再缩进 16px，与上方日期卡片的边框仍差一条线。
- 这一版把明细面板合并成整块：面板不再留内边距，日期标题与提示单独用 px-3 与 px-4 排版，表格（列头 + 数据行）改为通栏贴边、去掉自己的圆角与左右边框，由面板外框统一收边；日期标题与表格之间用一条分隔线过渡，底部圆角由面板的 overflow-hidden 裁切。

#### A 股 9 月无财报的原因说明
- 现象：A股切到 9 月显示「该月暂无财报数据」、共 0 家。
- 排查：直接按 FIRST_APPOINT_DATE 查东方财富预约披露接口，上游对 9 月返回 code 9201（返回数据为空）；对照查 8 月（有数据）、4 月（有数据）、去年同期 10 月（有数据）、今年 10 月（上游同样为空，因为三季报预约披露日期还没到发布时点）——A股半年报集中在 7–8 月、三季报预约披露要到临近 10 月才发布，9 月天然是空档，接口本身正常。
- 修复：空月份不再只显示一句「暂无数据」，改成说明空档原因并给出「查看上月（8 月）」的入口；空月份缓存 TTL 由 12 小时缩到 30 分钟，新财报季一发布预约日期就能很快显示出来。

#### 财报数据不再被空响应清空
- 把数据留在本地，防止上游失败或返回空时把整月清成空。
- 缓存记录拆成 items/at（最后一次真正拿到数据的内容与时间，updatedAt 用它展示）与 checkedAt/ttl（上次问上游的时间与有效期）：这一轮上游返回空时只更新 checkedAt，磁盘上那份好数据不覆盖，接口继续返回上次抓到的财报（30 分钟后再问一次），进程重启后也能直接读到。
- 抓取结构异常（HTML 挑战页、风控、字段改名）不再静默当成「这天没有财报」，改为抛错走失败分支，避免把空结果写进缓存；前端 localStorage 同理，服务端这轮返回空而本地已有当月数据时保留本地那份。
- 演练验证：把 8 月的 127 家快照伪装成「已过期」放到 CN:2026-10 目录，请求接口先返回这 127 家，触发上游刷新（上游对 10 月返回空）后磁盘文件仍是 127 家；随后清理演练数据。

---

## v0.1.26 · 2026-09-10

### 新功能

#### 设置-股票新增市场色块
- 全站市场徽标颜色与文字改为可配置：设置 → 股票设置 → 市场色块，可修改美股 / 港股 / 上证 / 深证 / 加密及其他市场的底色、文字色与缩写；默认显示改用与站点信息「允许新用户注册」相同的开关，只读浏览也可直接拨动。
- 保存后持仓、搜索、分享页与盈亏分析等处同步生效；股票来源接口移到股票类别末尾。

#### 默认券商分组与图标纳入素材
- 把已配置的九家券商（长桥、华泰、盈透、富途、同花顺、东方财富、老虎、罗宾汉、嘉信）写入默认设置，并与素材库 public/uploads/asset/broker 的图标按名称与别名对应。
- 新部署无需再手动添加券商，素材库券商图标随仓库同步。

#### 设置页接入 Orca 设计语言
- 设置窗口按 Orca 的设置语言重排：默认中性灰色板、248px 分组侧栏、侧栏内搜索（⌘K）、大标题与说明加圆角内容卡片、左侧文案右侧控件的行语法；开关仍保留绿色圆点。
- 原有风格切换（富途橙 / Notion / Claude 等）可继续换肤。

#### 台币接实时汇率并扩充冒烟检查
- 台币（TWD）过去只能一直用静态兜底汇率：汇率源富兰克福是 ECB 口径，不含台币（实测混在批量里被静默忽略、单独查返回 404），台湾市场换算偏差约 1–3%。
- 现在 lib/rates.ts 对上游缺失的币种改用腾讯外汇补齐（whUSDTWD，与行情同主机、走设置里的 quoteApiUrl，无需 Referer、境内可直连），实测 TWD 由固定 0.031 变为实时 31.517；补齐失败仍回退上次成功值与静态兜底，不影响主流程。
- 行情降级提示判定放宽并接入自选股：有持仓的美股降级必报，只看自选股时至少两只走腾讯兜底才报（富途本就不提供美股 OTC 行情，单只 OTC 不再误报）。
- 冒烟测试补 9 项公开接口检查（交易广场 feed、段永平、特朗普、行情、美股五日分时、个股详情、汇率含台币），测试项由 101 增至 110；删除已被 app/api-docs/page.tsx 取代的 components/views/ApiDocsView.tsx（179 行死代码）。

### 修复

#### 修复资产分析刷新时左侧布局闪扩
- 刷新资产分析时左侧分栏会先扩大再收回。根因有两层：分栏比例默认 29%，等 useEffect 读完 localStorage 才跳回保存值；CSS minmax(340px, 29%) 在容器宽度未定时把百分比当成内容上限，左栏先被图表与表格撑开。
- 修复：在 head 同步脚本里于首屏绘制前写入 fr 比例；组件首帧不写 inline 默认值；桌面分栏改用 fr，并给网格 width:100% 与 contain:inline-size，宽度只跟父级走。

#### 修复资产分析刷新闪现「加载中」
- 资产分析虽已做服务端渲染，但仍用 next/dynamic 的 loading 组件；水合时异步块未就绪，整页会被 TabLoading「加载中…」覆盖。
- 改为随 RecordsApp 同步引入资产分析视图；趋势图缓存改为绘制前恢复，有缓存不再先显示「正在汇总真实历史行情…」，无缓存改为无文字骨架。

#### 全站消除刷新闪「加载中」
- 后台所有页签不再使用 next/dynamic 的 TabLoading：持仓、自选、FIRE、设置、全球预览、财报日历、名人持仓、交易广场、用户管理、素材库、附件、盈亏分析刷新时不再整页闪「加载中…」。
- 登录表单去掉鉴权检查的加载中挡板（服务端已拦截已登录访问）；用户管理与全球市值榜在绘制前恢复本地缓存；盈亏分析与财报日历的加载文案改为无文字骨架。

#### 设置开关在只读浏览下也可拨动
- 站点信息「允许新用户注册」、市场色块「默认显示」以及定时备份开关统一为同一款绿色圆点开关；只读浏览时不再禁用，拨动后自动保存并弹出胶囊提示。

#### 修复市场色块设置触发编译报错
- 服务端 settings 误引入带 React Hook 的市场色块模块，开发编译出现 Plugin is not supported with current @swc/core。
- 已把 Hook 拆到独立客户端模块，服务端只引用纯函数。

#### 修复美股当日盈亏不更新
- 现象：线上版美股当日盈亏整天不动，本地版正常。排查结论：持仓与自选里的软银（ADR）SFTBY 属于美股 OTC 市场，富途快照接口不支持它，而桥接脚本是整批一次请求 —— 一只不被支持就让整批 44 只美股全部拿不到富途行情。
- 随后走腾讯兜底，但腾讯对美股只提供常规盘口径（盘前 / 盘后 / 夜盘仍停在上一交易日收盘），当日盈亏因此冻结在昨天；代码里本还有 Yahoo 扩展时段兜底，本机可用、线上容器取不到。
- 量化：同一时刻线上口径合计 −941.63 USD（昨日常规盘涨跌），富途口径 −55.85 USD（今日盘前）。
- 修复：scripts/futu_quotes.py 的批量快照改为容错 —— 整批失败时先剔除报错点名的代码重试，仍失败再二分定位，不支持的代码记入 skipped 由调用方继续走兜底源；实测含 SFTBY 的 45 只由「0 只返回」变为「44 只实盘 + 1 只跳过」。

#### 恢复美股行情降级提示与空市场隐藏
- 富途不可用退回腾讯时，我的持仓与资产分析的「账户资产」标题旁重新显示琥珀色「美股·腾讯兜底」胶囊（悬停说明盘前 / 盘后 / 夜盘的最新价与当日盈亏可能停在上一交易日收盘），行情正常时隐藏 —— 此前该提示组件在源码中缺失，降级时页面没有迹象。
- 我的持仓市场标签改为「有持仓或本页添加记录才显示」：设置里的 markets 只负责顺序，空市场标签自动隐藏（与自选股空分组隐藏一致），日股等无记录市场不再出现「日股 0」；新增该市场记录后标签自动出现，当前停留的空市场自动回到总资产。

#### 美股行情兜底再加固
- lib/usExtendedQuote.ts 新增 Yahoo 双主机熔断：出现服务级失败（连不上、403 限流、非 200）后 60 秒内直接判定不可用，不再逐只等待超时 —— 含 44 只持仓的行情请求由 17.6 秒回落到 2.4 秒；熔断只对服务级失败生效，单只标的在 Yahoo 查不到（200 无 result，如下市或 OTC）不触发，避免一只坏标的关掉整批兜底。
- scripts/futu_quotes.py 的快照调用补异常容错：网络抖动与 SDK 抛错同样按「这批失败」处理，走剔除与二分重试。
- 我的持仓市场编辑面板对无记录市场标注「无记录 · 自动隐藏」，说明标签为何不显示；AGENTS.md 记录富途整批快照与美股扩展时段降级两条经验。

#### 修复交易广场 500 与 FIRE 跨年口径不一致
- 交易广场整页 500：TradingSquareView 的 readSeen() 在 useState 初始化时读取 localStorage，该行没有被 try 包住，服务端渲染直接 ReferenceError；已改为显式判断浏览器环境，并用 TypeScript 编译器 API 扫过全部 JSX 确认没有按钮嵌套按钮，其余初始化期的浏览器 API 均已保护。
- FIRE 跨年落账口径不一致：年末快照的「当前资产」用 currentAssets（不含手动覆盖），而上方面板、水球与今年这一行用 effCurUsd（含手动覆盖），跨年后冻结值与页面显示会对不上 —— 统一为 effCurUsd 并补齐依赖项。
- 冒烟测试新增登录后页面巡检（16 个页面逐一检查 200），旧的 82 项检查覆盖不到页面渲染，正是 /trading 500 漏网的原因；测试项由 82 增至 101。

#### 修复出站代理失效
- 排查「配了 STOCKLOG_PROXY 线上仍取不到 Yahoo 扩展行情」：lib/net.ts 用 Node 自带的全局 fetch 承载 npm 安装的 undici ProxyAgent，两者不是同一份实现，请求必定抛 invalid onRequestStart method (UND_ERR_INVALID_ARG) 并被 catch 静默回退直连 —— 因此代理看起来开着，其实从未生效。
- 修复：代理分支改用 undici 包自己的 fetch 与 ProxyAgent 配套，未启用代理或代理失败仍回退全局 fetch 直连。
- 同时把仍走裸 fetch 的 Yahoo 调用全部接入 proxyFetch：lib/usExtendedQuote.ts（盘前 / 盘后 / 夜盘报价）、lib/quotes.ts（美股分时迷你图）、lib/kline.ts（美股日 K）、/api/kline/five-day、/api/kline/session-day、/api/v1/index-kline；境内源（腾讯 / 新浪 / 东财）保持直连。
- 实测：带代理启动的隔离实例上 /api/kline/session-day 由 502 变为 200 并返回盘前点（AAPL 04:00 起），未经代理时仍直连不受影响；线上群晖的 STOCKLOG_PROXY 已由 off 改为 http://192.168.28.55:1088（下次重建容器生效）。

#### 补齐日韩实时行情与本地 OpenD 策略
- 自选股与持仓行情接口补上腾讯日股 jp{} 与韩股 kr{}，分时与搜索联想同步；台股、新加坡、欧澳加印巴等无源市场行内标明「暂不支持实时行情」。
- 本地 next dev 默认不连 OpenD（STOCKLOG_FUTU=on 可开），线上生产继续使用唯一连接。
- 富途正常时单只 OTC 在 Web 行内与 iOS 列表显示「无扩展行情」，整批降级仍用「美股·腾讯兜底」；Yahoo 扩展行情可用 STOCKLOG_EXTENDED_QUOTE=off 关闭。
- 仓库只保留 docker-compose.ghcr.yml，审计不再读取已删除的本地 compose；CI 在构建后运行公开短冒烟（含日韩现价），并提供 .githooks/pre-push。

#### 打磨行情提示位置与降级判定
- 行内「暂无实时行情 / 无扩展行情」收到股票代码同一行，资产分析持仓表与盈亏排行同步显示。
- 整批「美股·腾讯兜底」把 Yahoo 扩展行情也视为在线，避免本地跳过 OpenD 后单只 OTC 误报整批降级；设置页补充 OpenD 本地跳过说明，东财搜索联想覆盖日股与韩股。

### 安全修复

#### 代理加 no_proxy 保护
- 在 proxyFetch 里补上 no_proxy 判定：命中内置私网（10/8、172.16-31、192.168/16、127/8、169.254/16、100.64/10、.local 与 .lan、.internal、localhost、IPv6 回环与 ULA）或 NO_PROXY / no_proxy 环境变量（支持 *、域名后缀、IPv4 与 IPv4/掩码）时直接直连，不看代理是否可用。
- 这样富途 OpenD、NAS 接口、体检探针等内网请求不会发给第三方代理；另加 STOCKLOG_PROXY_DEBUG=1 开关，按请求打印「走代理 / 直连（内网或 NO_PROXY）/ 代理失败回退直连」。
- 实测：带代理时 127.0.0.1 与 192.168.28.5 判定直连、Yahoo 走代理返回 200。注意 Node 的 fetch 不读容器里的 http_proxy / https_proxy / no_proxy（那是给 curl、python、npm 的），本应用只认 STOCKLOG_PROXY 与这套 no_proxy 规则。

---

## v0.1.25 · 2026-09-09

### 新功能

#### 双向版本时光机
- 简化版工具栏与完整版页头新增版本穿越入口，点击后以用户提供的哆啦A梦时光机原图衔接页面切换，并以 WebGL 对原图做柔和纵深与局部视差，保护人物区域，不添加额外流光或闪光。
- 不支持 WebGL 时回退原图；支持深浅主题、手机小屏与减少动态效果。
- 通过预取与应用内路由消除整页重载；隔离简化版样式并恢复返回后的窗口交互，离开前等待账本同步，失败时保留当前页面。

### 修复

#### 版本穿越入口布局
- 简化版右上角直接显示无外框细线纸飞机，单击立即穿越。
- 完整版入口保留在头像菜单，配合「去另一面」及目标版本说明。

#### 交易广场段永平刷新支持雪球登录 Cookie
- 雪球上线阿里云 WAF 反爬后，程序化拉取段永平发文被拦截，刷新拿不到新帖。
- 为交易广场新增「雪球 Cookie」设置项，服务端改用登录会话请求，绕过 WAF 并正常获取新发文。

---

## v0.1.24 · 2026-09-08

### 新功能

#### 简化版手机端支持下拉刷新
- 在简化版页面顶部下拉并越过阈值后松开，可重新同步服务端账本与最新汇率，并显示下拉、松开、加载、成功或失败状态。
- 刷新前先补写尚未同步的本地编辑，弹窗、输入控件、桑基图与全屏图表区域会阻止误触；汇率拉取完成后再统一重绘当前页面。

### 修复

#### 资产构成汇聚曲线精确连接节点边框
- 按参考图重新校准投入、转出、期初金额、净投入、收益与期末金额之间的曲线：左右支线使用缓弧向中心收拢，汇合后保留短垂直主干，并精确停在下一层卡片的上下边框中心，解决期末金额连线被卡片遮挡、看起来没有连接的问题。

#### 资产构成时间范围可用并清理编辑弹窗异常文案
- 资产构成的记账以来、今年、近 1 年现在会按真实历史记录重算期初金额、区间投入、转出、净投入与收益。
- 同时修复编辑累计投入、转出弹窗底部出现 undefined 的问题。

#### 累计收益曲线与当前累计收益保持一致
- 统一累计收益曲线与账户顶部累计收益的资金口径。
- 手动修正累计投入或转出后，历史曲线会同步校准，末端金额与当前累计收益一致。

#### 曲线鼠标定位适配页面缩放与宽屏
- 收益率与累计收益曲线改用 SVG 实际屏幕变换矩阵计算鼠标命中位置，不再把两侧缩放留白误认为绘图区。
- 悬浮提示按真实宽度自动翻转并限制在图表边界内，修复滑至末端时指针与 K 线明显偏离的问题。

#### 发布状态热力图改为连续周网格
- deploy-status 热力图不再把 12 个月分别生成独立网格，而是把当年 1 月至 12 月从左到右按自然周连续排列。
- 月份文字只标记所在周，跨月每日块与周统计保持连续，消除月份之间不规则的大段空隙。

#### 每日镜像避开整点调度延迟
- GitHub Actions 改为香港时间每天 00:07 检查，避开整点调度高峰导致任务延迟到凌晨 2 点以后。
- 仅在 main 存在尚未发布的新提交时生成镜像，没有新提交会跳过重复构建。

---

## v0.1.23 · 2026-09-07

### 新功能

#### 简化版新增年度现金流规划与 ECharts 桑基图
- 年度现金流仅在首次配置时进入计算器，完成后从总览卡片直接打开年度页。
- 默认展示收入与支出桑基图，放大后可切换「收入与支出 / 仅支出」以及金额、比例、隐藏数据。
- 收支、分类支出与年度结余均由同一年度口径计算。

#### 现金流编辑、预算追踪与分享视图按移动端参考图重做
- 收入和支出预估采用底部编辑页、彩虹金额输入、频率与支出类型选择。
- 支出预算追踪支持排序。
- 分享预览精简为图表、数据标签和保存图片，放大图下载按钮也可直接导出真实 PNG。

#### 简化版桌面端升级为宽屏财务工作台
- 手机端比例保持不变；桌面端首次升级自动迁移到约 1040px 的居中工作台，仍可自由拖动、缩放和固定。
- 总览放大主资产卡和两张功能卡的内容尺度，二级现金流页面扩大有效内容区并统一桌面留白，解决宽屏下像缩小手机模拟器的问题。

### 修复

#### 修复简化版小屏金额溢出与表单挤压
- 修复全局 field 样式把添加与编辑投资资产、账户和汇总表单容器压成 44px 导致字段重叠的问题。
- 补齐 320–500px 的窗口、金额、标题、列表和桑基图响应式规则；累计收益与超窄屏年度金额使用万单位。

#### 修复简化版刷新控件闪现与金额初值
- 页面准备完成前隐藏简化版窗口，避免刷新时右上角桌面控件闪现。
- 更新收益金额默认留空，所有同类彩虹金额输入沿用完整版「记一笔」的输入效果。

#### 修复简化版刷新后窗口缩到左侧
- 窗口位置与尺寸现在同时记录保存时的浏览器视口；桌面、平板和手机尺寸发生变化后按原中心位置重新换算并限制在可视区域。
- 从小屏返回桌面时自动恢复约 1040px 的居中工作台，避免刷新后沿用旧绝对坐标而收缩到页面最左侧。

#### 打磨简化版投资详情手机布局与金额单位
- 资金加权收益率与年化收益率标题在普通手机保持紧凑排列，380px 以下自动改为两列布局，问号说明按钮不再受长标题挤压。
- 账户及汇总的资产构成金额按数值自动使用万、亿、万亿单位并保留正负含义，避免完整大额数字溢出卡片。
- 同步逐页检查投资列表、详情、汇总、添加资产、设置、家庭账本、更新资产、成员、月历、晴雨表和年度现金流的小屏显示。

#### 修复简化版外币投资账户详情误显示人民币
- 美元和港元投资账户详情改为始终使用账户原币：资产、累计收益曲线提示、资产构成、投入转出编辑、更新记录和复制摘要统一显示美元或港元，不再乘汇率后仍写成元。
- 指标栏的累计收益省略重复币种并按万、亿等紧凑显示，三项指标在窄屏保持同一排，问号不再被挤压；投资汇总继续按人民币折算。

#### 打磨年度现金流概览操作布局
- 编辑入口移动到储蓄率右侧并与标题保持相同字号。
- 支出预算追踪去掉多余的三点菜单，改为直接切换排序的图标按钮。
- 桑基图右下角改为参考图中的圆形双折角放大按钮。

#### 家庭总资产变化示意图对齐参考逻辑
- 总览与家庭资产记账的总资产卡统一为三段状态示意：两侧使用紫色水平箭头，中间按资产差额显示红色上升、绿色下降或紫色持平，并保留右侧资产与负债各自按真实差额计算的变化结论；家庭页移除旧的五分类小柱图。
- 成员筛选同步作用于资产合计、更新时间、趋势当前值和更新记录，成员视图不再误用全家历史快照；排除固定资产时，概览与放大桑基图也使用相同口径。
- 资产组成金额统一采用万、亿等紧凑单位，避免手机端溢出。

#### 投资资产构成改为资金汇聚关系图
- 投资账户与投资汇总的资产构成按参考图重做为两层资金汇聚：投入、转出先汇入净投入，期初金额、净投入和收益再汇入期末金额；移除原有蓝色结果块，统一浅灰卡片、细曲线和圆角，仅用参考红色与绿色区分流入和转出。
- 桌面保持完整留白，手机端同步缩短节点高度、字号与间距，金额继续使用万、亿等紧凑单位避免挤压。

### 安全修复

#### 加固简化版账本导入导出接口
- Excel 导入与导出接口增加独立登录校验，不能绕过简化版页面直接调用。
- 上传解析增加 10MB 文件上限，避免异常大文件占用服务资源。

---

## v0.1.22 · 2026-09-06

### 新功能

#### 简化版升级为正式无后缀页面
- 简化版入口改为 /simple-app，浏览器地址不再显示 .html。
- /simple 与旧 /simple-app.html 自动跳转到新页面，原有账本、导入导出、主题与窗口交互完整保留。

#### 简化版完成桌面与手机自适应打磨
- 对照家庭资产与投资记账参考图重新梳理视觉层级：桌面总览拓宽并强化核心金额，卡片、标题、阴影与交互反馈统一。
- 手机端自动铺满视口、隐藏桌面窗口控件、适配安全区和单列卡片，同时补齐键盘焦点与窄屏防溢出处理。

#### 简化版对照原版继续打磨交互与版式
- 底部「更新资产」改为窗口页脚固定、不再盖住内容。
- 总览晴雨表、投资入口和家庭与更新页标题栏对齐原版；桑基图与收益率曲线补预期虚线；更新收益弹层改用图形而不是表情；现金流输入不再整页重绘；家庭成员写入服务端账本，Escape 可关闭弹层。

#### 简化版弹窗改成记一笔样式，并记住当前页面
- 投资记账「更新收益」以及添加成员、记账提醒、自定义区间都改成居中卡片弹窗：标题说明、分段选择、彩虹金额、底部取消与确认，风格仍跟简化版一致。
- 当前页、成员、账户、分类和弹窗写入地址栏，刷新或前进后退会停在原处。

#### 简化版更新收益日历复用记一笔选择器
- 更新收益和自定义区间不再用系统日期框，改为与完整版资金「记一笔」同一套日历：按月翻页、周一到周日、选中蓝圆、今天圆点、不可选未来日、一键回到今天。

#### 简化版投资记账加市场并改用下拉筛选
- 投资记账「全部资产」改为下拉选择活钱、稳健、长期。
- 添加投资资产增加市场（美股 / 港股 / A股等），图标复用素材库市场图标；列表、详情和设置同步显示，选美股或港股会带上对应币种。

#### 简化版投资记账支持「有知有行」格式导入导出
- 「投资记账」页标题栏新增完整版风格切换样式的按钮（堆叠图层图标），点击展开导入与导出菜单：导出 Excel（有知有行格式）、导出 JSON、导入账本（Excel 或 JSON）。
- 导出的 xlsx 与有知有行投资记账表完全兼容（账户名称、预期年化收益率、币种、四笔钱分类，以及每日总资产与转入转出记录），可来回导入导出；导入按账户名去重、避免重复账户；新增依赖 SheetJS(xlsx) 在服务端解析与生成。
- 导入时按账户名称关键词用正则自动归入对应账本（如「富途」→ 美股（富途）、「长桥」→ 美股（长桥）、「ibkr/盈透」→ 美股（IBKR）、「港A」→ A股（港A）），账户名未命中时可按文件名兜底匹配（如「有知有行_华泰_xxx.xlsx」→ A股（华泰））；关键词库扩充至富途、长桥、IBKR、老虎、雪盈、微牛、嘉信、华泰、中信、招商、国泰君安、银河、广发、东方财富、雪球、且慢、蛋卷、天天基金、蚂蚁、币安、OKX、火币、港A 等；投资列表按账本分组并显示账户数与小计金额。

#### 简化版投资账户支持手动编辑投入与转出
- 账户详情页的「资产构成」标题旁新增一支笔：默认只读，双击投入或转出进入编辑，或点笔进入编辑态（笔高亮、投入与转出框可单击，框上不再重复显示小笔）。
- 填入修正后的累计投入与转出（如为与券商收益一致而校正的值），收益（期末 − 净投入）会自动重算；汇总视图仍为只读；同名合并导入历史时尊重已手动设置的投入与转出。

#### 简化版资产构成改用资金系统网格样式
- 账户详情「资产构成」改为完整版资金系统汇总图的网格样式（fund-flow-grid，3 列）：投入 / 期初金额 / 转出 / 净投入 / 期末金额 / 收益 六张圆角卡片，期初、净投入与收益使用浅蓝 flow 底，投入与转出为普通卡，期末金额使用蓝色渐变 result 高亮。
- 卡片间用 --muted 折线（bracket 与 center-line）连接，金额按正负红绿着色；编辑保留；去掉了此前引入的 ECharts（该网格为纯 HTML 与 CSS）。

#### 简化版按账本分组导入导出
- 「投资记账」每个账本分组表头右侧新增设置按钮，点开显示该账本菜单：导出 Excel、导出 JSON、导入到该账本。
- 导入到某账本即把文件里的账户归入该账本（group 由用户指定），不再依赖关键词或名称匹配，组内同名账户自动合并历史、不会再生重复；导出只导出该账本的账户，右上角全局导入导出保留。
- 另：「+ 添加投资资产」文字居中；同组重复去重时保留与账本组名完全一致的规范账户（如保留 美股（长桥）、删掉 长桥（美股）），按每个用户实际账本名匹配、不写死任何市场规则。

### 修复

#### 修复有知有行累计收益与曲线计算口径
- 导入时补回首条总资产代表的初始本金，资金流日期同步调整市值。
- 累计收益按期末资产减净投入计算，资金加权收益率改用 Modified Dietz，年化收益率改用 XIRR，汇总曲线按日期携带各账户最新市值。
- 旧版重复种子记录和错误本金会在刷新后自动修正。

#### 修复简化版同页交互整页抖动
- 简化版 render() 每次都会重建整个屏幕，因此打开与收起菜单、切换下拉、筛选、展开更多设置等同页轻交互都会重新播放整页入场动画。
- 修复：仅在切换页面（route.name 变化）时播放入场动画，同页重渲染为屏幕追加 noanim 抑制动画；一次性解决投资记账右上角导入导出、家庭成员等所有同页菜单与下拉的抖动问题，页面切换过渡动画保留。

#### 修复简化版刷新闪现竖向滚动条
- 现象：刷新任意页面后，即使内容能放下，窗口内容区仍闪出一条竖向滚动条。
- 根因：applyWin() 在 render() 前执行，窗口高度按空内容计算、随后才异步修正；syncScroll() 只在首帧计算一次（此时内容尚未稳定、scrollHeight 偏大），can-y 被误设为 true，等内容收敛到刚好放下时没有重算。
- 修复：把 applyWin() 移到 render() 之后（首帧窗口高度即贴合真实内容），render() 末尾同步调用 syncScroll()，并用 ResizeObserver 监听内容区尺寸实时重算是否可滚动；现在内容放得下时不出现滚动条、窗口高度贴合内容，内容多时才出现滚动条。
- 并对加载中间态做内容高度稳定判定与 8px 容差：内容仅略高（如图标与字体加载时）不再判定为可滚动，消除「先冒出又缩回」的闪现。

#### 修复简化版导入同名账户历史未生效
- 现象：导入有知有行 xlsx 或 json 时，若账户名与现有账户相同，mergeInvest 直接跳过，导入的每日总资产历史、累计转入转出与收益率曲线都不生效。
- 修复：同名账户改为合并历史 —— 把导入的每日资产与转入转出按日期并入（导入优先、去重），amount 取最新，累计投入与转出尊重已手动设置的值（仅当账户尚无投入时才采用导入值），同步更新预期收益率、市场与币种；无同名账户时仍按完整历史新建。

#### 修复导入时同关键词重复生成账户
- 现象：导入时按账户名精确去重，导致有知有行的「长桥（美股）」与已有的「美股（长桥）」因名字不同被当成新账户，同组里出现两个同款（ibkr 同理）。
- 修复：去重改为按账本组（关键词）匹配 —— 命中同一关键词即视为同一账户，合并历史、不再新建；新增 dedupeInvestGroups，导入后自动合并同一账本组里的重复账户（保留首个、其余历史并入）。
- 重新导入一次即可把「长桥（美股）」与「ibkr」合并进「美股（长桥）」与「美股（IBKR）」。

#### 简化版更新记录默认显示 3 条
- 投资账户详情的「更新记录」原本默认展开最近 6 条，改为默认只显示最近 3 条，需要更多时点「查看更多」展开全部，列表更紧凑。

---

## v0.1.21 · 2026-09-05

### 新功能

#### 资金记录联动股票名称、代码和拼音
- 资产分析资金记录搜索不再只匹配备注原文：自动记账流水关联成交订单，可用中文名、代码（含港股 700 与 00700）、拼音首字母（中国移动 → zgyd）和全拼查找。
- 同时支持买入 / 卖出 / 股息、美股 / 港股 / A股，以及「9月3日」这类日期；列表展示股票代码。

#### 持仓右击股息改为历年一览
- 资产分析持仓右击「股息」改为展示该股历年派息：按年份切换，可以看到每股金额、除息日、派付日，以及已入账 / 待派发 / 未入账。
- 已到派息日但账上没有的，可一键按除息日持仓补录进订单与资金记录。

#### 股息列表增加每期股息率
- 持仓右击股息与个股股息页每期增加股息率：每股现金股息除以除息日前最近收盘价；没有收盘价时显示 —，不影响入账。

#### 股息记录改为紧凑表格
- 持仓右击股息与个股股息页改成同一张密行表格：每股、股息率、除息、派付、状态。
- 美股多期表头固定、区内滚动；港股与 A股一年一两期不再按年拆成大卡片，页面跟着行数收住。

#### 日志页改为账户操作时间线
- 日志页宽度与设置页同为 800px；上方是前一日持仓盈亏：一个合计，下面美股 / 港股 / A股三列。
- 下方不再出现持仓或订单，改为登录、改密、导入导出等账户记录，按日浅色分隔，失败标红；买卖请到订单页。

### 修复

#### 修复资金记账金额输入抖动
- 新增资金记录时输入金额不再抖动：彩虹数字改为与输入框同排的连续等宽字，千分位逗号不再让数字节点整串重建。
- 光标固定在末尾且不随逗号插入重启动画；底部「当前余额 / 记账后」行高锁死，弹窗不再跟着数字变长而上下跳动。

#### 资金记录搜索改为彩虹输入并消除抖动
- 查看资金记录的搜索框改回逐字彩虹着色。
- 输入法拼写过程不再把拼音中间态送去搜索，彩色字与光标共用同一套内边距，列表区域高度固定，避免边输入边上下跳。

#### 资金记录标题不再重复股票代码
- 自动记账流水标题只保留「卖出 中国移动」，代码只出现在右侧胶囊。
- 搜索仍按成交订单的代码、名称与拼音匹配，输入 600941 仍能找到。

#### 资金记录改为名称加色块胶囊
- 查看资金记录简化为股票名称，后接统一胶囊：买入浅红底、卖出浅绿底、股息深色块，左侧带圆点。
- 股票代码与「自动」使用中性浅底胶囊，深浅色都对齐。

#### 资金记录搜索框占位超出显示省略号
- 查看资金记录搜索框回到标题栏右侧原样式；占位「名称、代码、拼音、买入/卖出」宽度不够时用省略号收住，不再换到标题下方。

#### 资产分析持仓平仓改为市价快卖
- 右击平仓仍是卖出，但固定市价单、立刻成交，不再出现当日有效和时段。
- 右击新增股息：不改持仓，可按最近一期每股股息补录；派息日到账仍会自动进订单和资金记录。

#### 股息只从首次买入后入账
- 自动股息改为按买入与卖出订单重放持股：首次买入之前的派息不再入账，误入账的自动股息会清掉。
- 补录仍按除息日持股写入订单系统与资金记录，历年列表标出买入前 / 当时未持仓 / 已入账。

---

## v0.1.20 · 2026-09-03

### 新功能

#### 空实例首次设置接入真实三步向导
- 生产空库不再只依赖登录页注册：没有非测试用户时，访问登录或后台会进入 /setup —— 创建管理员、填写站点标题与注册开关、然后开始使用。
- 关闭注册时仍允许创建首位管理员；已初始化实例访问 /setup 会回到登录或后台。公开接口 GET /api/auth/setup-status 与 /api/v1/auth/setup-status 只返回 needsSetup 布尔值。

#### 交易广场改为本地缓存优先的社交动态
- 交易广场展示特朗普 Truth Social 与段永平雪球的公开动态：页面先读本地 JSON，访问时按设置频率后台刷新。
- 支持人物切换、拖动压缩布局、近一个月归档分页与中文翻译；设置中可分别配置两位作者的更新间隔。

#### 段永平动态改为近三个月并带上引用原话
- 雪球时间线窗口从 30 天扩到 90 天；回复与转发会带上被 @ 的原评论或原帖，热门里不再只看到一句没有上下文的回复。
- 抓取同时尝试 xueqiu.com 与 api.xueqiu.com，并复用会话 Cookie。

#### 操作日志分类检索并纳入交易活动
- 系统日志增加分类、搜索、分页与深浅色对比。
- 认证审计与交易活动写入同一日志，并按市场拆分前一日盈亏摘要。

#### 交易广场正文股票代码和链接可点开个股详情
- 动态正文里的雪球 $名称(代码)$、美元代码、$AAPL 这类标记，雪球 / Yahoo / 东财股票链接，以及持仓里的代码和名称，都会高亮并可点开同一套个股详情；普通新闻链接仍跳转原文。
- 返回箭头回到当前动态列表，刷新后仍停留在该股票。

#### 交易广场股票代码加虚线并悬停预览行情
- 正文里的股票标记改为虚线下划线；鼠标悬停约 0.16 秒后弹出摘要卡，显示名称、代码、现价、涨跌幅、昨收、市值与行情时间，点卡片或文字都进入个股详情。
- 行情走 /api/v1/quotes 并缓存 30 秒，触控设备仍直接点开详情。

#### 日志顶部改为前一日持仓盈利并合并动态
- 用户日志顶部改为各市场上一交易日收盘相对前收盘的持仓盈亏，带市场图标，并按显示币种汇总。
- 点击美股 / 港股 / A股可筛选下方列表；交易与持仓操作收成一条时间线，筛选增加「交易」。

#### 交易广场按人物显示更新时间并提示新动态条数
- 右侧标题「全部动态」下方显示最近一次抓取时间（取所有人里最新的一次，人再多也不逐条罗列）。
- 新帖数字叠在头像右下角，点进该人物后消失；数字在本地缓存画完之后用已有列表计算，不额外请求、不挡住页面加载；点进某个人时标题下只显示这个人的更新时间。

#### 交易广场动态展示发帖配图并支持页内缩放预览
- 特朗普归档附件图与段永平雪球配图在后台刷新时下载到本地 /uploads/trading-square/，列表只加载本地下载成功的图片，不引用外链。
- 预览改用官方 react-photo-view：点击缩略图在当前页按原比例打开，支持双指缩放、拖动、工具栏放大缩小与键盘切换，不新开标签、不挡住列表首次加载；已缓存的旧帖在下次抓取重叠页时补图。

### 修复

#### 设置标题栏完成改为真正保存当前分区
- 设置窗口标题栏的对勾从「退出编辑」改为保存当前分区；站点信息、形象、指数、导航、来源、翻译、富途与数据库的卡片按钮同步走同一提交链路。
- 保存失败时保留编辑态，避免未写入就退出。

#### 交易广场补齐中文翻译、刷新与浏览记忆
- 特朗普动态的翻译改为只处理尚未翻译的帖子，不再被已有译文占满前 5 条名额；访问页面后会后台继续补译，并在刷新完成后自动更新列表。
- 人物、分类和页码写入 URL，时间显示到分钟，分页改为可跳转页码；头像跟随名人持仓的自定义图。

#### 交易广场刷新不再堵住页面加载
- 打开或刷新交易广场时立刻返回本地缓存；归档抓取改为增量（已有缓存只翻到与旧帖重叠），翻译改到后台小批量进行，不再等 30 页归档与模型翻译结束才出列表。
- 再次进入时会先画出上次缓存内容。

#### 交易广场取消按天数截断历史动态
- 特朗普与段永平不再只保留近 30 天与 90 天；已缓存的旧帖不会被按日期删掉，刷新按分页继续往回补，直到与现有缓存重叠或达到页数上限。

#### 修复特朗普更早动态不再翻译
- 未译队列卡在 8/27 一批长帖上：单条 2.5 秒超时失败后每次只重试前 3 条，更早的帖子永远排不上。
- 改为失败冷却并继续往后翻译，单次最多 20 条、模型超时 12 秒，打开交易广场就会从第 14 页的缺口继续补中文。

#### 特朗普新帖先翻译再进列表，大模型优先
- 新抓到的特朗普动态先走大模型翻译，失败再兜底 MyMemory，译文写入本地 JSON 后才进入 feed。
- 页面不会先闪英文再换成中文；已显示的中文在刷新时也不会被原文顶掉。

#### 操作日志顶部补市场图标并校正交易记录
- 用户日志盈利摘要的美股 / 港股 / A股加上市场图标，金额按当前显示币种换算，不再把不同原币种直接相加。
- 交易记录表的「市场」与「方向」列对调回来，价格带货币符号；分页改用全站页码组件，筛选和页码写入 URL。

#### 日志摘要合计前置并接入统一货币切换
- 前一日持仓盈利把合计放到第一格，右上角使用全站圆形国旗货币按钮，切换后金额立即按显示币种换算。
- 列表股票列去掉市场图标，只保留代码和市场名。

#### 日志筛选改为搜索框左侧图标
- 去掉列表上方单独一行的「全部动态」下拉；筛选改成漏斗图标放在搜索框左边，和刷新收在同一组工具里，选了交易或新增等时图标保持按下态。
- 系统日志模块筛选同样收进这个按钮。

#### 日志合计图标与市场图标对齐
- 前一日持仓盈利的合计保持 Σ，做成 14px 圆形图标，与同排美股 / 港股 / A股市场图标尺寸一致，不再使用地球。

#### 刷新不再闪现右上角登录按钮
- 已登录用户刷新首页或后台时，右上角改为服务端会话直接渲染头像，不再等 /api/auth/me；检查中不再画出登录按钮，接口超时或 5xx 也保留当前头像，只有 401 才回到登录。
- 同类问题一并处理：已登录访问 /login 由服务端直接进后台；后台头像遇 401 整页去登录页；行情、分组与搜索的瞬时失败不再把人踢去登录页；顶栏指数条刷新先读本地缓存。

#### 加快全站首屏：不再每页拉取全部股票素材
- 首页不再同步打包全球预览与财报日历整包；持仓与首页只下发当前记录需要的图标，不再每次扫描 3000 条以上素材并下载约 1MB 的 /api/assets?type=stock。
- 完整股票素材库仅在素材库与附件管理打开时加载；素材文件存在性检查加内存缓存，播种只执行一次；指数栏缓存 60 秒；自动备份推迟到首屏之后。

#### 发布状态热力图月份不再换行错位
- 更新热力图改为固定 12 列月份块（1 月到 12 月同一行），每个月用自己的周列，避免 53 周滚动网格把当年 9 月挤到 12 月下面。

#### 无正文的特朗普动态不再送去翻译
- 纯图片或只剩链接的帖子不再调用大模型与翻译接口，避免模型把「请提供英文内容」这类回复当成译文；已有这类无效译文会在刷新时清掉。

### 安全修复

#### 翻译服务统一为大模型配置且密钥不下发浏览器
- 设置新增独立翻译服务分区，统一 llmProvider / llmApiUrl / llmModel / llmApiKey。
- 已保存的 API Key 只返回配置状态与掩码占位，不再把明文密钥发给页面。

#### 交易广场配图下载与预览安全加固
- 配图远程抓取改为 HTTPS 主机白名单（雪球与 Truth Social 归档附件域），拒绝内网、localhost、带用户名的 URL 以及跳转后落到白名单外的地址。
- 落盘前用文件魔数校验，只保存 jpg/png/gif/webp，拒绝 svg/ico；列表与预览只接受 /uploads/trading-square/{作者}/{16位哈希}.{扩展名}，不把外链交给 react-photo-view；npm audit 对新增依赖 0 漏洞。

---

## v0.1.19 · 2026-08-31

### 新功能

#### 统一本地与线上发布节奏并新增状态监控页
- main 提交继续执行公开仓库、部署一致性与 TypeScript 检查，但不再每次提交都发布 GHCR；工作流改为北京时间每天 00:00 自动构建发布，并保留 workflow_dispatch 手动兜底。
- 新增 /deploy-status 与 /api/deploy-status，展示最近运行的提交、触发方式和时间，绿色圆点表示成功、灰色表示排队或进行中、红色表示失败，页面每分钟自动刷新。

#### 发布状态页增加手动推送与可视化流程
- 状态页新增「立即手动推送」操作，服务端安全调用 GitHub Actions workflow_dispatch 发布 main 最新提交，浏览器不接触 GitHub Token。
- 增加本地源码、检查、定时或手动、GHCR、线上容器五段流程图，并提供触发中、成功和失败反馈。

#### 发布流程节点改为可操作按钮
- 流程图中的 main、检查、立即发布、GHCR 与线上容器节点均可直接操作或跳转，状态检查和手动发布无需离开监控页。

#### 明确 Push image 镜像构建入口
- 发布流程将原先含义模糊的立即发布明确命名为 Push image，点击后生成并推送 GHCR 镜像；原构建 GHCR 链接改为查看 GHCR，顶部图标、流程按钮与最近运行记录使用一致名称并继续共享防重复保护。

#### 新增镜像构建实时进度
- Push image 后从 GitHub Actions Jobs API 读取真实构建状态，在发布页展示 TypeScript 检查与 linux/amd64 镜像发布两个阶段；运行中的 Job 默认展开，可继续查看 Checkout、GHCR 登录、Buildx 与 Build and push 等步骤、状态和耗时。
- 构建期间每 10 秒刷新，完成后恢复每分钟刷新，并对 Job 数据分级缓存以控制 GitHub API 用量。

#### 自选股与持仓支持加密货币搜索
- 添加持仓和自选股时，统一搜索入口同步返回 CoinGecko 加密货币候选，可直接保存为加密货币记录并在结果中明确标注资产类型；股票搜索源不可用时不受影响。

#### 替换 BTC/ETH 高清开源图标
- 素材库比对多个开源集合后，采用 cryptocurrency-icons 的 CC0 彩色 SVG 替换比特币与以太坊图标，统一圆角容器下的清晰度与视觉风格。

#### 搜索结果补充市场色块
- 我的持仓与自选股搜索候选使用与资产分析持仓明细一致的市场色块，股票与加密货币市场标识更直观。

#### 加密货币增加专属色块
- 市场色块统一为琥珀色「币」标识，加密货币在搜索结果、持仓与自选股中与股票市场清晰区分。

#### 发布状态页增加更新热力图与权限守卫
- 发布状态页新增最近一年 GitHub 风格更新热力图，固定显示 1–12 月且在容器内自适应不产生横向滚动；每日、每周、每月、累计视图都保留相同的每日小点网格，周与月模式点击日期会联动高亮对应整周或整月并显示汇总 workflow 次数，更新越多绿色越深；手机端格子改为等宽等高的方形圆角点，避免竖条变形。
- 持仓页货币选择改为受控更新，消除首次切换欧元与韩元的事件同步延迟；镜像构建成功且确认有新镜像后自动倒计时 5 分钟触发 Watchtower 容器更新；立即构建改为舒缓绿色呼吸圆点，手机端工具按钮右对齐；页面增加服务端管理员守卫，非管理员统一返回 404。

#### 小屏货币金额紧凑显示与全局单位设置
- 韩元、日元等高面额货币在小屏持仓与资产分析卡片中自动按万、百万、千万、亿缩写，避免净资产、当日盈亏和持仓市值溢出；桌面默认保持原完整金额。
- 股票设置新增「跟随设备（小屏缩写）/ 始终缩写 / 始终完整」切换，并在同标签页即时同步。

### 修复

#### 线上与本地欧元默认旗帜统一为欧盟 SVG
- 欧元市场码 EU 不再回退到通用 eu.svg，货币选择器、市场图标、全球经济热图与素材库统一使用 /uploads/asset/flag/欧盟EU.svg。
- 新增既有数据库一次性迁移，只替换旧内置 eu.svg，不覆盖用户上传的其他自定义旗帜；新部署会以 EU 代码播种该素材，Docker 镜像同步携带，并在 uploads 挂载缺失时从 resource-default 兜底。

#### 个人资料与用户管理现代化精简
- 移除个人信息编辑态中昵称与登录邮箱输入框的示例占位文字，避免把提示误当成已有资料。
- 用户管理由高占位宽表重构为紧凑成员列表，顶部概览总用户、在线与管理员数量，身份区合并昵称、登录名、UID 与状态，操作统一为带无障碍说明的图标按钮；窄屏自动转为完整卡片布局，不再依赖横向滚动。

#### 修复移动端订单工具栏溢出与失效素材破图
- 订单导入、导出与列设置工具在 390px 手机端改为独立自适应工具行，搜索框占满一行并将次要文字按钮收为图标，避免超出右边界。
- 站点 Logo、首页背景与动态 favicon 增加加载失败回退，素材库股票与市场图标容器补齐深色背景，避免 404 破图与深色模式白底突兀。

#### 完成响应式排查收尾
- 按 390px 手机、820px 平板与 1440px 桌面复核主要页面：资产分析账户资产卡的分享与刷新按钮补足 36px 触控热区，图标视觉尺寸保持不变。
- 手机与平板顶部横向导航增加右侧边缘渐隐提示，让用户明确知道还可以继续滑动。

#### 导入自选股分组改为幂等并自动合并历史重复项
- 网站数据导入不再为每个备份分组无条件生成新 ID：市场分组按市场代码、自定义分组按规范化名称复用现有实体，同一备份中的重复项也只保留一份。
- 读取分组时会把历史重复记录迁移到最早的规范分组后删除重复项，既有线上数据无需再次导入即可恢复整洁。

#### 本地源码与 GHCR 默认素材统一
- 恢复并发布完整国家旗帜、券商图标与四种贵金属 SVG；素材播种同时扫描 public/uploads 与镜像 resource-default，持久化目录缺文件时仍能从镜像默认资源恢复。
- 素材库统一显示美股及更多市场的中文名，地图国名改为中文并在 SVG 不可用时回退真实旗帜 emoji；部署审计新增旗帜、券商、贵金属数量与运行时兜底检查。

#### 显示货币即时同步并补齐线上股票行情
- 全站共享偏好增加同标签页与跨标签页变更通知，持仓、FIRE 与资产分析切换显示货币后立即重算，无需刷新页面；清理旧的原始 localStorage 写法，统一 JSON 持久化格式。
- 素材库股票行情在本地缓存数据缺失时按市场复用全球预览行情源补齐市值、最新价与涨跌幅，解决干净线上数据库只显示横线的问题。

#### 发布监控页右上角增加浅色/深色切换
- 复用全站 ThemeToggle，在 /deploy-status 页面右上角提供太阳与月亮图标，主题选择通过现有 localStorage 与 Cookie 持久化，刷新后保持一致。

#### 修复发布监控页浅色主题不生效
- 监控页面板、流程节点、边框、按钮和运行列表补齐浅色与深色双主题样式，右上角 ThemeToggle 现在会即时改变整个页面并在刷新后保持。

#### 发布监控页工具栏统一图标操作
- 刷新、GitHub 授权、配置账号与立即推送统一改为带悬停提示与无障碍名称的图标按钮，减少顶栏占用并保留推送中状态反馈。

#### 修复本地版券商及同类素材图标丢失
- 券商素材播种同时校验本地文件，自动修复历史 stock 与 GROUP 错路径和缺失文件，并按券商名称与别名重建当前分组映射。
- 素材 API 不再下发磁盘与默认资源中都不存在的 URL，全站统一转入已有兜底；素材缓存升级版本，旧缓存会立即后台刷新。

#### 恢复我的持仓编辑中丢失的券商分组
- 新增一次性幂等迁移，保留现有券商配置与顺序，并从未丢失的券商素材记录中追加恢复被覆盖的券商。
- 迁移完成后会写入标记，之后手动删除券商不会被自动恢复。

#### 全站素材与站点形象完整性修复
- 清理股票与加密货币素材表中已不存在的本地 URL，保留名称与代码供默认图标或 CDN 兜底；股票默认播种不再写入未打包的文件地址。
- 首页背景、登录侧图、favicon 与 Logo 在本地文件丢失时自动回退内置样式；券商下拉图标增加加载失败首字母兜底。

#### 发布监控页刷新按钮增加动效
- 刷新图标悬停时以轻量旋转动画反馈操作，保持顶栏简洁并强化可发现性。

#### 发布监控页主题与刷新交互打磨
- 展开的 GitHub 配置区补齐浅色与深色背景、边框、输入框和操作按钮对比度。
- 刷新按钮支持点击期间禁用并持续旋转，减少重复请求并强化反馈。

#### 发布流程与刷新动画改为柔和过渡
- 刷新仅在点击请求期间旋转，使用平滑缓动曲线；流程按钮增加自然的悬停与按下过渡，避免状态切换生硬。

#### 优化发布配置保存按钮文字对比度
- 保存配置按钮在浅色主题使用深蓝底白字，在深色主题使用亮蓝底深色字，并补充悬停与按下反馈，提升可读性。

#### 深色模式保存配置改为白底黑字
- 深色模式下保存配置按钮使用白色背景与深色文字，并补充键盘焦点环，提升对比度和可访问性。

#### 修正手动 Token 权限提示
- 网页配置将备用 Token 明确为发布 Token，提示手动触发 workflow 需要目标仓库 Actions 的 Read and write 权限，并同步更新环境变量示例。

#### 修复网页 GitHub 配置仍返回 API 404
- 发布状态读取统一使用网页保存的 OAuth 或手动 Token，网页配置的仓库优先于环境变量与 GHCR 镜像推导值；错误信息同时显示实际请求仓库，解决私有仓库和占位环境变量导致的 404。

#### 发布记录改为每页 10 条
- 发布监控页最多读取最近 50 次运行并按每页 10 条分页，增加总数、当前页与上一页和下一页控制；窄屏页头改为纵向排列，避免标题与操作图标拥挤。

#### 修复深浅色切换短暂延迟
- 主题按钮点击时同步更新根节点 class、持久化设置和 Cookie，不再等待 React 副作用；发布监控页移除整页颜色缓动，主题即时生效。

#### 修复提交检查被误显示为发布成功
- 顶部最新发布状态仅统计定时 schedule 与手动 workflow_dispatch 运行，普通 main 提交的检查成功不再被误判为镜像发布成功。

#### 重排发布状态圆点与说明
- 最新发布状态圆点与标题改为同一基线，提交与时间独立缩进；成功和失败保持静态，仅排队与进行中使用呼吸动效，底部文字说明改为三个醒目的圆点状态标签。

#### 修复发布监控页手机端头部错位
- 390px 手机端将发布状态标题、简短说明与工具图标拆成稳定的三层布局，缩短移动端文案并统一左右边距；发布流程改为两列触控网格，隐藏造成断行的箭头，按钮保持单行且不再互相挤压。
- 桌面端完整标题、说明与横向流程保持不变。

#### 阻止重复触发手动发布
- 顶部推送图标与流程中的立即发布按钮共享 GitHub 运行状态：已有 workflow_dispatch 排队或运行时同时禁用并显示发布进行中。
- 服务端触发前再次查询当前工作流，并增加 30 秒触发锁，防止快速连点、多标签页或网络延迟产生重复运行；失败或完成后仍可正常重试。

#### 统一发布操作并优化刷新反馈
- 删除顶部重复的镜像推送快捷按钮，镜像构建统一从发布流程中的 Push image 执行；流程节点加入同一套语义图标并统一尺寸、圆角、聚焦与深浅色状态，删除按钮下方的冗余操作提示。
- 刷新按钮改为点击后平滑旋转一圈，移除叠加动画导致的漂移和突停。

#### GitHub 授权改为新窗口打开
- 右上角与配置面板中的 GitHub OAuth 入口统一使用新窗口打开，并补充安全的 noreferrer 属性和无障碍提示，避免离开发布监控页面。

#### 打磨镜像构建进度交互
- 构建进度增加已完成阶段计数，Job 摘要直接显示当前执行步骤；运行中与失败阶段使用克制的语义边框区分，跳过步骤不再误显示为等待中。
- 修正其他 workflow_dispatch 任务误锁定 Push image 的前端判断，并为动态状态、成功提示和错误提示补充无障碍播报与浅色模式文字对比。

#### 收敛发布流程绿色状态语义
- GitHub main 改为中性灰色来源节点，检查结果作为子项独立显示；绿色仅用于当前 main 镜像构建完成，以及群晖实际运行该镜像且健康的可验证结果，避免把源码存在误读为发布成功。

#### 精简发布记录与 GHCR 入口
- 最近运行由每页 10 条收紧为 5 条；移除发布流程标题旁的文字型 GHCR 入口，改为右上角 GitHub 授权按钮右侧的独立包仓库图标，减少重复文字与视觉干扰。

#### 继续打磨发布监控页信息层级
- 发布流程保持原有页面宽度；GHCR 入口换为更简洁的单包图标，移除重复状态图例。
- GitHub API 异常时仍保留服务端返回的实际仓库名，避免顶部链接退回 owner/repository 占位地址。

#### 发布监控补齐真实加载与更新器在线状态
- 首次读取 GitHub 运行记录时显示与最终列表结构一致的骨架行，不再短暂误报待发布或暂无记录。
- 群晖更新器状态由仅检查 Token 改为同时探测 Compose 内网端口，fire-updater 未启动或网络不可达时按钮提前显示更新服务离线，不再等点击后才报错。

#### 重排线上镜像版本徽章
- 移除流程末尾重复的线上旧镜像节点，起点改为 fire-web main，并将当前容器实际运行的短 SHA 放到该节点右上角的 GitHub 风格胶囊中；镜像与 main 一致时显示绿色圆点和「镜像已构建完成」。

#### 暂停 GHCR 自动清理
- 暂停自动删除历史镜像，避免多标签 manifest 被误删导致 registry 返回 manifest unknown；待验证更安全的按摘要清理方案后再恢复保留策略。

#### 定时任务跳过无变化镜像
- 每日凌晨仍会执行源码与安全检查，但仅当 main 相比上次成功的定时或手动发布发生新提交时才构建镜像；手动 Push image 继续作为强制发布兜底。

#### 提升 GHCR 镜像清单兼容性
- 发布镜像时关闭额外 provenance 与 SBOM 清单，保留标准 linux/amd64 镜像，兼容群晖 Docker 拉取并避免 registry 返回 manifest unknown。

#### 统一发布版本胶囊视觉
- 线上版本短 SHA 改为 GitHub 风格中性胶囊，浅色模式使用黑色文字，深色模式使用白色文字；版本是否落后仅由状态圆点表达，移除整块橙色告警感。

#### 移除发布流程重复版本提示
- 当前线上 SHA 已由 fire-web main 节点右上角胶囊展示，镜像未覆盖最新 main 时，构建节点改为「构建新镜像」，不再重复显示「镜像落后 + 相同 SHA」。

#### 收紧发布监控页临界宽度
- 桌面内容区由 800px 收紧至 720px，在四段发布流程仍能稳定保持单行的前提下减少横向空白；更窄视口继续使用现有响应式网格。

#### 按实际构建结果显示镜像状态
- 只要最近一次镜像构建成功，流程节点即显示绿色圆点与「镜像已构建完成」；main 后续新增提交不再把已成功构建的镜像显示为落后，版本差异仍由更新按钮可用性控制。

#### 修复线上版本胶囊越界
- 将当前线上版本胶囊从 main 节点的负定位悬浮层移到发布流程标题行右侧，保留 GitHub 风格与状态圆点，同时避免越过节点边框、挤压流程箭头或影响 720px 布局。

#### 统一发布页中文操作文案
- 将 Push image、镜像发布提示和顶部说明统一为中文表达，保留 GitHub、GHCR 等专有名词，减少中英文混排造成的阅读跳跃。

#### 修复发布接口 HTML 解析报错
- 发布页统一校验 API 响应类型；接口返回 404、登录页或其他 HTML 时显示明确的 HTTP 与容器版本提示，不再暴露 Unexpected token 解析错误，也不再同时显示成功和错误提示。

#### 新提交恢复待构建状态
- 上一轮镜像仅在与当前 main 匹配时显示绿色完成；main 出现新推送后切换为橙色「立即构建」，并隐藏已过期的上一轮完成进度卡。

#### 自动恢复更新服务状态
- 发布页每 15 秒复检一次 fire-updater，手动刷新时也同步检查；更新服务后启动无需重开页面即可由离线恢复，并统一显示明确的离线文案。

#### 显示 Watchtower 在线状态
- 复用发布流程末端的更新节点，以灰、绿、红圆点分别表示 Watchtower 检测中、在线和离线；已是最新时显示「更新服务在线」，避免新增元素挤压 720px 布局。

#### 区分等待检查与状态未知
- main 新提交已出现但 GitHub Actions 运行记录尚未生成时显示橙色「等待检查」，并临时提高到每 10 秒刷新；只有 main 本身无法读取时才显示「检查未知」。

#### 精简线上版本胶囊
- 线上版本仅在匹配最新 main 时显示绿色圆点，非最新版统一使用灰色圆点；边框改为轻量内描边与中性底色并移除浮起阴影。

#### 增加运行次数数字徽标
- 最近运行标题旁以紧凑圆形徽标显示 GitHub Actions 总运行次数，仅保留数字；同时将单次读取上限提高到 100，使页面分页数量与 GitHub 总数一致。

#### 修复最新版镜像误判
- GitHub 提交详情接口读取失败时，改用最新 main push 运行的完整 head SHA 作为回退，避免同一版本被错误显示为检查未知、立即构建和线上非最新。

#### 明确容器最新状态
- 发布流程末端的绿色圆点继续代表 Watchtower 在线；线上已运行最新镜像时文字改为「容器已是最新」，并取消禁用态整体变淡，保持状态圆点清晰。

#### 精简更新服务状态文案
- 发布流程末端在有可更新镜像时显示「有新镜像」，离线与检测状态分别精简为「服务离线」和「服务检测中」，绿、红、灰圆点继续表达服务状态。

#### 区分最近运行成功类型
- 最近运行列表中，定时或手动镜像构建成功使用绿色圆点，main 推送检查成功使用蓝色圆点，便于快速区分发布与检查记录。

#### 统一美股名称并修复货币选择器越界
- 首页、我的持仓和素材库内置 US 市场统一显示「美股」，不再受历史自定义标签影响。
- 持仓盈利图货币选择器在窄卡片与窄屏下限制宽度并允许操作区换行，边框不再超出容器。

#### 稳定全球经济最新年份
- 全球经济数据不再依赖 World Bank 返回记录的顺序，而是对每个国家明确选取查询范围内的最大年份，避免本地与线上因响应顺序不同显示不同年份或数值。

#### 修复加密货币市值冷启动
- 全球市值榜首次加载时等待 CoinGecko 市值行情补齐，并对缓存中缺失市值的加密货币自动刷新，避免 BTC、ETH 等项目以 0 市值落到第 99、100 位。

#### 增加加密货币市值备用来源
- CoinGecko 返回价格但缺少市值时，使用 CompaniesMarketCap 全球资产榜中的加密货币市值补齐，避免限流或字段缺失再次产生 0 市值条目。

#### 补齐加密货币 K 线
- 加密货币详情页 K 线改用 CoinGecko 历史价格数据，支持 BTC、ETH、SOL 等主流币种的日 K 展示。

#### 修复加密货币图标错配
- 素材播种会修复历史上 ETH 指向比特币图标的错误映射，同时保留用户手动上传的自定义图标不被覆盖。

#### 补齐 ADA/BCH 加密货币图标
- 补充艾达币与比特币现金的高清开源 SVG，避免缺少素材时错误回退为比特币符号。

#### 行情板复用加密货币素材图标
- 自选股与我的持仓列表不再只查股票图标；ASSET 加密货币记录改为复用素材库 crypto 图标。

#### 搜索结果紧凑化
- 市场色块统一为紧凑固定尺寸，股票名称与代码设置最大宽度并截断，长名称不会撑破搜索结果行或挤压价格区域。

#### 优化加密货币色块文字对比
- 加密货币琥珀色标签改用更干净的深墨色文字，提升小尺寸色块的可读性与 GitHub 风格一致性。

#### 加密货币色块改用白色字体
- 搜索框及持仓相关市场色块统一使用白色「币」文字，搭配深琥珀底色，避免黑字观感不一致。

#### 全站数据表手机端横向浏览修复
- 统一修复持仓与全球排行货币菜单贴边溢出、自选行情板股票首列消失、全球资产涨跌幅与市值拥挤、素材库股票序号及加密货币名称被压缩等问题；数据表保留合理最小列宽并支持横向滑动，FIRE 未来年度盈利占位统一为红色。

#### 手机端溢出审计第二轮
- 显示货币菜单在手机端统一改为底部安全区面板，彻底避免左右边缘越界；API 文档、财报附件与截图导入预览表补齐最小列宽和横向滚动，不再压缩长路径、文件名或金额列。

#### 纠正手机端行情表与货币菜单交互
- 自选行情板将股票图标与名称列固定在手机端左侧，横向查看价格与走势时身份信息不再消失；全球资产排行明确恢复现价列并用固定列宽完整横滑；显示货币菜单恢复到触发按钮旁，不再远离当前操作。

#### 完成七项手机端回归检查
- 全球资产排行的货币入口与标题并排显示，弹层紧邻入口；素材库分类标签改为不压缩的横向滑动栏并收紧手机端卡片留白，避免分类被裁切，同时复核发布页、持仓、自选股、FIRE 与数据表横滑规则。

#### 全球资产排行强制保留完整七列
- 手机端不再依赖原生 table 自动分配宽度，改为固定七列 Grid：排名、资产、现价、涨跌幅、迷你趋势图、市值、市场全部保留，由外层统一横向滑动。
- 同步复核全站宽表，并为操作日志、订单导出记录和导入预览补齐明确最小宽度与独立滚动容器。

#### 修复嵌套分栏内宽表无法触摸横滑
- 定位资产分析持仓表虽有 overflow-x 但触摸手势被外层分栏接管的问题；新增全站 data-table-scroll 手势规范与 pan-x 惯性滚动，覆盖持仓、自选、全球排行、FIRE、日志、订单、API 文档及所有 overflow-x-auto 区域。
- 通过 iPhone 12 Pro 390px 浏览器逐页复现并移除旧版 ≤420px 强制删列规则：我的持仓、资产分析、自选行情、全球资产、日志、素材库、名人持仓及首页预览在所有手机尺寸统一保留完整列并内部横滑。

#### 取消宽表横向冻结列
- 通过 iPhone 12 Pro 视口实测并全站扫描横向 sticky 列；取消自选行情左侧股票列和右侧操作列的冻结定位、层级与阴影，清理深色编辑模式残留的冻结投影及序号列隐藏兜底，所有列随表格自然横滑。
- 保留移动导航、附件表头和弹窗标题等正常的纵向吸顶交互。

#### 修复个股详情图表工具栏文字挤压
- 通过 iPhone 12 Pro 视口定位图表工具栏左侧周期组被 Flex 压缩至 41px 的问题；手机端改为周期与图表操作上下分区，K 线周期在自身区域平滑横滑，复权、比较、图形和指标按钮保持完整文字，极窄屏允许操作组自然换行。
- 顶部行情指标改为默认隐藏，可按需显示为 2×2 卡片；时段、周期、复权、比较、图形和指标选择框统一使用视口内底部安全面板，解决弹层越界；移动断点按视口将 K 线高度收敛至约 220–300px；详情头部同步缩小返回键与图标、截断过长名称、隐藏重复代码并收紧价格排版；手机 K 线悬浮框改为紧凑的收盘与涨跌信息，避免遮挡图形。

#### 富途额度查询增加 OpenD 快速预检
- 查询富途 OpenAPI 额度前先进行 800ms TCP 端口检测；OpenD 未启动或地址不可达时立即返回，不再启动 futu-api 并等待内部重连。
- 桥接进程原有的 10 秒硬超时继续作为网关可达但 SDK 异常时的第二层保护。

#### 区分历史成交记录与实时委托
- 资产分析持仓交易弹窗新增「提交委托 / 记录已成交」双模式：历史成交按交易所当地时间录入，美股明确使用美东时间并禁止未来时间；实时限价单不再因当日有效而直接成交，未到价保持待成交并由实时行情触发。
- 同步修复当日有效挂单跨交易日不失效，以及挂单成功提示误判为已成交的问题。

#### 收紧委托有效期、交易所日期与导入兼容
- 订单服务拒绝未知订单类型与时效，自定义有效期按交易所自然日截止并禁止选择过去日期，挂单卖出在创建阶段校验可卖数量；今日与历史订单按各交易所当地日期归类。
- 历史成交补录增加夏令时不存在时刻校验，旧版无 mode 请求继续按当前时间成交；备份导入完整保留六种委托类型与 custom 时效。

#### 设置搜索统一为单实例响应式面板
- 移除桌面侧栏与手机内容区重复渲染的两套搜索面板，解决共用 ref 导致桌面焦点可能落到隐藏输入框的问题。
- 统一面板按视口自适应宽度，并补齐组合框与列表框语义、悬停选择、空结果提示、方向键边界、回车跳转和 Escape 关闭。

### 安全修复

#### 移除固定测试用户身份判断
- 全量审计用户认证、权限与数据查询链路，移除生产登录流程中针对固定 demo-user ID 与公开默认密码的特殊分支。
- 现在管理员权限仅由数据库 role 决定，UID、用户名与内部 ID 都不再被当作特殊账号；开发环境种子账号与历史订单数据迁移作为明确的测试与数据修复用途保留。

#### GitHub OAuth 授权与 Token 安全兜底
- 发布监控页同时支持 GitHub OAuth 授权和手动 Token 兜底；OAuth 使用一次性 state、管理员权限和 workflow 最小权限。
- 授权及备用 Token 仅服务端保存，并通过 DEPLOY_STATUS_SECRET 使用 AES-GCM 加密，页面只显示授权账号与已配置状态。

#### 发布监控页支持一键更新群晖容器
- GHCR 部署增加受限 Watchtower 服务，发布流程可由管理员直接拉取最新 Fire 镜像并观察重启与健康恢复。
- 更新器仅匹配 Fire 专属 enable 标签与 scope，API 只在 Compose 内网开放并使用 32 位以上随机 Token；网页服务端固定调用更新端点，不接收任意命令、容器或镜像参数，并限制 30 秒内重复触发；Compose 配置变化仍需群晖手动执行 compose up。

#### 发布状态改为三段版本校验
- 发布监控不再把单次构建成功等同于上线完成：页面分别读取 GitHub main、最近可用 GHCR 镜像与当前容器的完整提交 SHA，明确显示检查中、构建失败、镜像落后、待更新和版本一致。
- 群晖更新仅对当前 main 的已验证镜像开放，更新后必须以健康接口返回的运行 SHA 完成校验；镜像写入构建 SHA 与 OCI revision，修复完成任务仍显示构建中的缓存，并隔离提交检查与正式发布的并发取消范围。

---

## v0.1.18 · 2026-08-30

### 新功能

#### 股票添加搜索框支持逐字符彩虹输入
- 在「我的持仓-添加股票」和「自选股-股票添加」两个搜索框启用实时逐字符彩虹显示；中文、英文、数字和原有搜索键盘交互均可正常输入，其他文本输入框不受影响。

#### 自选股截图导入改为轻量模式并支持批量分组
- 自选股页面的截图导入入口改名为「截图导入自选股」，预览表隐藏数量、现价、成本等持仓字段，仅保留名称、代码、市场与状态。
- 确认导入前可选择自定义分组，将本次识别结果一次性归入该分组；持仓页面继续保留完整持仓字段，服务端校验分组归属后再写入。

#### 全站迷你走势图接口优化
- /api/charts 迷你走势响应改为 gzip 压缩，并对批量走势做固定点数抽样（约 940 点压到 60 点），显著减小单次请求体积；同时为腾讯分钟数据增加 30 秒内存缓存，减少重复外部请求。
- 抽样改为逐请求 opt-in：首页（自选股与我的持仓近 5 日走势）与自选股行情板声明 sample 才抽 60 点；个股详情 K 线与分时不声明，仍保留全量分时点。

#### 首个注册用户自动成为管理员
- 注册逻辑改为：首个非测试注册账号自动成为管理员（role=admin），无需预先在 .env 配置管理员。
- 同时保留 INITIAL_ADMIN_USERNAME 与 INITIAL_ADMIN_PASSWORD 兜底：生产环境仅在两者都合法（用户名 3–20 位、密码 8–128 位含字母与数字）时才用它建管理员；未提供或不合规时不再抛出「必须配置管理员」错误，改为交由首个注册账号接管。

#### 新增首次部署引导页视觉稿
- 新增独立的首次部署引导页 HTML 视觉稿，展示服务状态、管理员创建、基础配置步骤及 data/uploads 持久化提示；当前仅供预览，尚未接入首访路由。

### 修复

#### 撤回资金记录以外的全局彩虹输入
- 按要求取消持仓、行情、快捷交易、FIRE、导入、名人持仓、财报、K 线及设置等页面的全局彩虹输入，恢复各页面原有数字控件与交互；仅保留新增资金记录中的专用彩虹输入效果。

#### 修复首屏应用壳长时间停留在加载态
- 排查发现默认「我的持仓」和「自选股」虽已有服务端注入的持仓与设置数据，却仍使用 ssr:false 动态加载，首屏只能显示应用壳和「加载中…」。
- 现改为服务端首帧渲染这两个高频页面，其他重型页面继续按需加载，减少白屏式等待。

#### 资产标签改用中性灰并优化搜索提示色
- 我的持仓页的显示货币、资产指标标签、各市场盈利说明与表头改为中性灰，减少冷色灰带来的视觉偏移；股票添加搜索框占位提示单独改为冷灰蓝，已输入的加粗彩虹文字和盈亏红绿语义保持不变。

#### 自选股行情板布局与批量操作继续精修
- 自选股行情表的编辑、移动、删除操作统一为紧凑按钮组，默认只读时隐藏行内操作，编辑模式下稳定显示；操作列收紧并在横向滚动时贴右可见。
- 移动下拉改为轻量自定义箭头与焦点反馈，股票、现价、涨跌幅、走势图与行情时间增加稳定最小宽度，中等窗口不再把名称压成短省略号；标题区新增行情数量、状态圆点、最后刷新时间和手动刷新，刷新间隔控件同步收紧。
- 勾选股票后，批量移动与删除改为独立工具条，不再挤压刷新区；选中行增加轻量高亮，分页信息改为当前条目范围。

#### 修复局域网 HTTP 登录后无限跳回登录页
- 生产环境 session cookie 固定带 Secure 标志，局域网 http://IP:port 明文访问时浏览器不保存与不发送该 cookie，导致登录成功后会话丢失、被反复重定向回登录页。
- 现改为按请求是否真正走 HTTPS（x-forwarded-proto 或 url 协议）动态决定是否带 Secure，HTTP 访问可正常登录并保持会话。

#### 修复 PWA manifest 构建期预渲染空库报错
- app/manifest.ts 在 next build 时会被静态预渲染并读取站点设置（触发建库 seed），空库与生产模式下因缺少 INITIAL_ADMIN_* 导致构建失败。
- 已将 manifest 路由标记为运行时动态（force-dynamic），构建不再预渲染碰库，改为请求时读取真实站点标题。

#### 修复新用户素材库图标缺失时显示占位
- 全新用户（素材库为空或默认图标文件未随部署打包）时，导航、货币、设置菜单等图标因引用不存在的 /uploads/asset/... 而显示「?」破图。
- 新增 SafeAssetImage 安全加载：图片加载失败自动回退内置矢量默认图标（NAV_ICONS 与地球），不再出现「?」；同时基础素材库图标（icon / flag / market 等）随镜像打包并在首次启动复制到挂载目录。

#### 线上名人持仓默认使用定制头像
- 修复 Docker 部署中 data/celebs-avatars.json 不进镜像，导致线上新库虽有定制头像文件，却因缺少映射而回退原始照片的问题。
- 六位内置名人现直接使用定制头像作为默认值，并增加一次性数据库迁移，仅替换明确的旧默认路径；服务端与浏览器旧缓存也不再覆盖当前头像。新增 public/uploads/celebs/default-avatars.json 发布映射，今后本地上传会同步更新运行时映射与发布映射。

#### FIRE 导航图标恢复默认火焰图标
- 修复历史上本地与线上数据库中 icon:FIRE 保留自定义 fire-gray.svg、导致导航显示波浪线图标的问题。
- 增加一次性数据库迁移，将 FIRE 统一恢复为随镜像发布的 fire.svg 与 fire-dark.svg，新库与既有库都一致。

#### 修复 Docker 挂载后全球经济热图国旗缺失
- 群晖将宿主机 uploads 挂载到 /app/public/uploads 后，旧容器或默认素材复制失败会遮住镜像内置旗帜文件，热图 tooltip 因图片 404 只显示地球符号。
- 上传资源路由现在在挂载目录未找到文件时回读镜像内置 resource-default，保留用户自定义资源优先级，线上默认国旗不再丢失。

#### 兼容富途 OpenD 主机地址格式
- 修复设置中误填 http://192.168.x.x 导致容器 TCP 明明可达但 Fire 判断端口未开放的问题。
- 保存设置、读取旧配置和测试连接时统一将 OpenD 主机归一化为纯 hostname 或 IP，自动去掉协议、端口与路径。

#### GHCR 镜像内置富途 OpenD 桥接运行时
- 修复线上容器 TCP 可连接 OpenD 但测试仍报 spawn python3 ENOENT：生产镜像此前未包含 Python。
- 现在 runner 镜像内置 Python venv 与 futu-api，设置正确的 OpenD 主机和端口后无需手动进入群晖容器安装依赖。

#### 修复全站冒烟测试安全参数遗漏
- 冒烟测试此前清空记录和修改邮箱时未携带接口要求的当前密码，误报线上功能失败。
- 现按真实前端安全协议补齐 password 与 currentPassword，并在 API 文档明确高风险操作的验证字段，全套测试恢复通过。

### 安全修复

#### 公开仓库净化与自动防泄漏审计
- 部署配置改为通过 GHCR_IMAGE、HOST_PORT、DATA_DIR、UPLOADS_DIR 环境变量注入，不再写死账号、端口、NAS 路径或硬件限制；出站代理默认关闭，移除数据库初始化中的特定用户名 UID 规则及无引用的用户上传站点素材。
- 新增 npm run audit:public 并接入 GitHub Actions，自动拦截个人账号、本机路径、个人代理、硬编码群晖配置、时间戳用户素材及常见 Token、API Key 与私钥。

---

## v0.1.17 · 2026-08-29

### 新功能

#### 设置侧栏品牌花标打磨
- 品牌花标改为「点击一次转一圈、连续点击连续转」：每次点击目标角度加 360°，改用临界阻尼弹簧在角度目标上做物理积分（requestAnimationFrame），并设定角速度上限，消除速度突跳与急停导致的卡顿。
- 花标外轮廓由细长六瓣星芒打磨为圆凸瓣、浅凹口的云朵与花瓣式外形，内容恢复为 Fire 趋势折线加箭头，并恢复「先描绘自己、箭头在画到末端时浮现」的播放动画（默认静止，仅在悬停或连续点击转动中播放）；去掉多余装饰与外层圆角方框，旋转时花瓣外浮出柔和光晕。
- 移除侧栏品牌区的「Fire 投资记实」文案，花瓣在品牌区水平居中；搜索按钮从侧栏移入设置窗口标题栏工具组（置顶图标左侧），点击仍唤起 ⌘K 命令搜索（改为窗口事件触发），命令面板固定在设置窗口右上角、紧贴搜索按钮下方。

#### 侧栏花标放大 + 设置内容区滚动条改细
- 侧栏品牌花标按钮由 40px 放大到 46px、内部 SVG 由 34px 放大到 40px，云朵花瓣更大、与侧栏更协调。
- 设置内容滚动区（.sw-content-scroll）补一条 6px 细圆角滚动条（webkit 与 scrollbar-width:thin，用主题色半透明），替代浏览器默认粗滚动条。

#### 设置页输入默认态统一为只读
- 按已建立的「默认只读、点编辑铅笔才可编辑」规范做全局排查：站点信息（网站标题、域名、页脚）、交易·富途（OpenD 主机、端口）、网站形象（图标、Logo、背景、登录图的粘贴链接输入框及上传与清除按钮）、数据库（数据库类型卡、PostgreSQL 连接输入框、测试与保存）原先默认直接显示带边框输入框。
- 现为这四块增加「编辑铅笔 + 完成」切换，非编辑态输入框改为 readOnly 并去掉边框、背景与阴影，上传、清除、类型切换与保存禁用（纯展示，仅 UI，不改数据）；券商分组、首页指数与导航、来源接口、个人信息已有编辑态，无需改动。

#### 新增网站设置导出与导入
- 个人信息新增「网站设置导出 / 导入」卡片：导出生成 fire-site-backup-*.json（含 format、version、appVersion、exportedAt 与 manifest 计数），data 覆盖站点设置、个人偏好、持仓记录、成交订单、操作日志、自选分组、名人持仓与当前用户昵称，均为纯数据；图标与图片为应用默认、上线自带，不打包。
- 导入校验 format 与 version，导入前自动做一次数据库快照备份，随后在单事务内按 id upsert（存在更新、不存在插入）还原 records → trade_orders → activities → watchGroups → userSettings → celebs → site_settings，所有 user_id 重映射为当前导入用户，并恢复昵称，做到不覆盖不丢失。
- 新增 GET 与 POST /api/v1/data/export 与 /import（仅管理员）及 docs/api-spec 6.18。

#### 网站数据导出与导入打磨
- 导出文件 manifest 增加 excluded 说明字段（图标与图片为应用默认上线自带；数据库连接串等环境专属配置不迁移）；导出排除 dbType、pgHost、pgPort、pgDatabase、pgUser、pgPassword 等环境专属设置键，避免把开发库连接带到线上。
- 导入改为两步：先 POST /api/v1/data/import?preview=1 做服务端校验与试算条数（不写入），前端预览条数并确认后再正式导入（单事务 upsert，恢复昵称）。

#### 数据导入导出开放给普通用户并新增注销账号
- 网站数据导出与导入不再仅管理员，所有登录用户可用，且按当前用户作用域：普通用户只迁移自己的 userSettings、records、tradeOrders、activities、watchGroups 与昵称；管理员额外包含 siteSettings 与 celebs。
- 防导入注入：普通用户即使备份里带了 siteSettings 或 celebs 也会在导入时被忽略，仅管理员可写入站点级数据；user_id 一律重映射为当前用户。
- 新增 POST /api/v1/auth/delete-account 注销当前账号：级联删除该用户的 sessions、records、trade_orders、activities、watch_groups 与 user_settings，并尽量删除头像文件；保护最后一个管理员。个人信息「数据」区新增红色「注销账号」入口，并要求输入登录名确认。

#### 设置个人信息页视觉层级精修
- 个人信息头像区升级为独立身份卡：加入主题渐变底、圆角边框、头像白色描边与柔和阴影，并补充昵称、@用户名及管理员或成员徽标。
- 密码表单改为独立浅底圆角容器；网站备份与持仓导出归入统一的数据操作面板；清空数据与注销账号从普通数据项中拆出，归入红色弱提示的危险操作区。窄屏下身份卡自动切换横向布局。

#### 设置页编辑入口收敛为页面级统一按钮
- 解决设置卡片标题旁铅笔过多造成的视觉噪声：隐藏站点信息、网站形象、首页指数与导航、应用导航、券商分组、股票来源、富途连接、个人信息和数据库各自重复的编辑铅笔，在设置内容标题栏仅保留一个带文字的「编辑」按钮。
- 按钮按当前侧栏栏目准确开启对应编辑态，首页导航会同时开放首页与应用导航；标题副文案同步显示「只读浏览 / 正在编辑」，复杂列表原有保存、取消、拖动、删除与添加逻辑保持不变；站点注册开关在只读态同步禁用。

#### 新增多币种资金系统并纳入账户净资产
- 资产分析新增全站统一七币种资金系统，数据库、API、备份导入及账户现金汇总同步扩展，旧账本无损迁移；主体采用三列响应式资金流转图。
- 「新增资金记录」持续精修：方向使用分段状态，类型使用可视化卡片，日期使用站内自定义日历；金额输入采用「透明原生输入层 + 逐字符彩色展示层」，每个已键入数字按红、橙、黄、紫、蓝、青、绿顺序独立着色，千分位逗号保持中性灰，末尾自绘闪烁彩色光标使用下一位数字即将采用的颜色并带轻微光晕；点击金额区始终把输入位置保持在末尾。
- 底层仍剔除一切非数字字符、归一前导零并限制 1 万亿元，千分位只用于显示，提交为纯数字；资金记录随网站备份导入导出；历史订单每页 5 笔，当日订单每页 9 笔；新增规范 v1 资金 API。

### 修复

#### 移除设置导航选中时的蓝色卡片边框
- 设置侧栏选择项目后仍保留平滑滚动与内容切换，但不再给右侧卡片添加蓝色 settings-flash 外圈动画，选中态保持中性灰视觉规范。

#### 移除设置页主题切换与已保存状态控件
- 移除设置内容头部右侧的太阳主题切换按钮和已保存状态胶囊，并清理对应组件引用、状态回显与样式；设置修改仍会自动保存，失败时继续通过 Toast 提示。

#### 券商分组默认展示三项并支持展开更多
- 券商分组默认按单列从上到下展示前 3 个，标题区与首条券商增加留白；浏览状态仅显示图标、中文名称和英文别名。
- 标题旁新增铅笔按钮，点击才进入编辑状态并显示名称与别名输入框、拖动、删除、添加、取消和保存；保存继续通过服务端同步券商改名、别名、排序与删除到全站，超过 3 个时可展开更多。移除整卡按压缩放，输入框聚焦不再抖动。

#### 设置页列表管理统一浏览与编辑状态
- 设置页列表与只读、编辑态统一：首页指数、首页导航、应用导航默认前 5 项并支持更多，列表统一紧凑宽度与单行信息布局；区块标题和内容全局保持 14px 间距。
- 再次通过 390px 窄屏走查，修复移动端只能进入「网站设置 / 股票设置」一级分类、无法访问内部设置的问题：新增当前分类二级横向导航，可直接切换站点信息、网站形象、首页指数、首页导航及股票下各子项；首页导航和应用导航在窄屏自动把路径换到下一行，个人信息头像区从高占位纵排压缩为 52px 横排。

#### 设置侧栏品牌头部精修
- 按截图重新平衡设置侧栏顶部品牌区：优先展示站点已配置 Logo，无图时回退精修 F 字标；Logo 改为 32px 柔和渐变圆角块，增加细边框、内高光和轻阴影；Fire 字标加粗并收紧字距，副标题缩小压紧；品牌区底部增加细分隔线。
- 搜索从悬浮裸图标改为常驻 28px 圆角工具按钮，带边框、背景、轻阴影和克制的上浮悬停效果，消除漂浮感并提升可发现性。

#### 股票来源外链操作图标化
- 全局审查设置页同类入口：股票来源接口各行移除占宽的「打开链接 ↗」文字，改为左侧复制、右侧外链的双 30px 圆角图标按钮，复制后 Toast 反馈；网站形象展开面板的「打开」文字同步改为外链图标，设置侧栏 API 的裸箭头改为标准外链 SVG。
- 统一补齐主题色浅底、细描边、悬停与按压反馈、键盘焦点环和无障碍名称；保存、完成、添加、清除等需要明确语义的操作保留文字。

#### 全站股票图标首屏加载提速
- 排查确认股票图标虽已本地化，但自选、持仓、资产分析等页面仍要等客户端挂载后请求整套 3,262 条股票素材，导致首屏图标列先空白再补出。
- 改为服务端根据当前账户记录提取所需图标，并兼容杠杆 ETF 到正股图标的兜底，随应用壳首屏注入共享图标缓存；HTML 同步输出去重后的 image preload，让本地图片更早并行加载，完整素材库仍在后台刷新。
- 实际刷新自选页验证：首屏 6 个 preload 生效、可用图标均 complete、无首屏字母占位等待。

#### 设置窗口宽度以 800px 为基础优化
- 设置页桌面窗口最大宽度定为 800px（.sv-win-root、SettingsWindow 的 max-w 与静态 mockup 同步），并保留单行排版、输入框弹性伸缩与细滚动条：股票来源行控制区约 316px，常见链接可完整显示。
- 股票来源链接在非编辑态改为带省略号截断的文本 span（悬停 title 显示完整 URL），编辑时才显示输入框，避免输入框内部硬切。

#### 修复切换设置左选项时整个页面往上滚
- 点击设置左侧导航或 ⌘K 搜索结果时，jumpTo 原先用 el.scrollIntoView({block:'start'}) 把目标锚点贴到视口顶部，导致整个页面一起上滚、设置窗口整体上移。
- 改为只在设置内容滚动区（.sw-content-scroll）内滚动：用 getBoundingClientRect 差量计算目标 scrollTop 后 container.scrollTo({behavior:'smooth'})，不再影响页面整体位置。

#### 设置行改单行排版
- 设置里「股票来源接口」的复制与新窗口图标、「交易·富途」的查询额度文本此前会因 786px 下空间不足而换行或出现横向滚动条。
- 改为让 .sw-row .ctrl 保持单行（flex-wrap:nowrap、取消 overflow-x 滚动条）、输入框改为 flex:1 1 auto（最小 120px）弹性伸缩，URL 在输入框内部滚动；富途额度文本允许 min-w-0 与 overflow 收缩、行情源胶囊不换行，达到单行、无滚动条、无换行的效果。

#### 个人信息复制改为图标按钮
- 个人信息的昵称、登录邮箱、登录名三行统一结构：值区改为 flex:1 容器、内容左对齐，各加一个 30px 圆角复制图标在最右（分别复制昵称、邮箱与 UID，禁用时降低透明度），复制图标按钮不再超宽，三行值起点与复制按钮位置一致对齐。
- 同时给 .settings-code-value 去掉边框与背景、左对齐纯文本并加 flex:1 1 auto 填充（带 max-width:100% 与省略号、nowrap 防长值撑宽），使 admin、/api/v1 等只读代码值与其它只读展示一致。

#### Fire 花标外轮廓精确改为圆润云朵形
- 按 ChatGPT 新对话动画参考重新构造设置侧栏 Fire 花标：彻底移除连续小波浪，外轮廓仅保留 7 个清晰的大体块，采用非对称云朵结构、宽缓连接、饱满底瓣和更大的内部留白；中心 Fire 趋势箭头同步放大、简化折点并加粗圆角描边，解决原符号过小过碎的问题。点击一次一圈、连续点击连续旋转的弹簧动画保持不变。

#### Fire 花标恢复默认设计
- 按要求撤销本轮 ChatGPT 云朵轮廓与中心符号比例调整，恢复项目原有默认外轮廓、中心趋势箭头、描边粗细和缩放比例；点击旋转交互继续保留。

#### 修复设置标题栏工具与编辑按钮重叠
- 修复搜索、置顶、风格切换三个悬浮方块与页面级编辑按钮占用同一坐标、遮住栏目标题的问题：四者全部保留原有 32px 图标尺寸；编辑铅笔正式移入窗口标题栏组件并放在搜索右侧，顺序为「搜索 → 编辑 → 置顶 → 风格切换」。
- 不再依赖内容页绝对定位，因此加载中、切换栏目和内容重渲染时按钮始终连续存在，不会留下空洞；页面标题为完整工具组预留固定安全区。

#### 个人信息复制按钮与下方操作列右对齐
- 修复昵称、登录邮箱、用户名三行复制图标使用字段网格右边线，而数据备份与危险操作按钮使用卡片内缩右边线导致的错位；桌面端为个人资料字段列补齐与下方操作面板一致的 12px 右内边距，使复制、导出、导入、清空和注销按钮落在同一条垂直基线上。移动端恢复满宽，避免重复内缩。

#### 个人信息值区收紧并让复制按钮跟随文字
- 按文字边界重排个人资料三行：撤销将复制按钮吸附卡片右侧的操作列方案，昵称、邮箱、用户名的复制图标改为紧跟各自文字尾部；字段标签列由 180px 收紧到 142px 临界宽度，让三行内容整体左移并消除中间大块空白。长邮箱仍保留省略号与最大宽度保护，移动端标签列进一步收紧为 112px。

#### 修复资产分析收益率趋势的虚假盈亏
- 排查确认「简单加权」此前使用当前持仓数量乘历史收盘价，等同假设今天全部仓位年初已持有，并把期末市值减期初市值误标为累计盈亏；今年大量买卖时会制造截图中 -30.25% 与 -$34,257 的虚亏。
- 现在按成交订单逐日还原 positionQtyAfter，简单加权改为（账本累计盈亏 ÷ 实际投入资本）指数，顶部累计盈亏改用 buildPortfolioLedger 的已实现加未实现经济盈亏。
- 时间加权将周末与节假日成交现金流通过二分查找归入下一有效行情日，并对首次可确认的旧仓按当日市值视为外部转入；无订单持仓从 updatedAt 首个可确认日才纳入，不再回填年初，页面显示订单覆盖数提示；订单请求上限由 500 提至 5000，趋势缓存升级 v3 并纳入成本和更新时间签名。

#### 修复本月盈亏误显示全历史累计金额
- 修复收益率趋势切换到「本月」后顶部仍显示约 3.3 万美元全历史累计盈亏的问题：金额现与周期按钮使用同一趋势账本，本月、近 1 月、近 6 月、本年、近 1 年与自定义区间分别显示对应区间盈亏，只有「全部」显示累计盈亏。
- 计算以区间开始前最后一个有效交易日为基准，并包含首个交易日的变化；资料不完整的旧仓在首次可确认日将盈亏基线归零，避免历史浮盈一次性落入当月；趋势缓存升级 v4。

#### 收益趋势跑赢跑输文案跟随盈亏
- 收益率趋势图的组合图例不再固定显示「跑赢」：当前所选周期金额盈利时显示「跑赢」，亏损或持平时显示「跑输」，切换本月、本年、全部或自定义区间后即时同步；标普 500 名称及右侧相对收益差值保持原有口径。

#### 修复深色模式金额彩虹数字被原生文字遮挡
- 排查确认深色模式的全局 input 文字颜色规则覆盖了资金金额透明捕获层，导致原生浅色数字位于逐字符彩虹层上方。
- 现对 fund-amount-input 的 color、-webkit-text-fill-color、caret-color 与 selection 使用高优先级透明覆盖，保留透明输入层的键盘能力，同时让后方逐位彩色数字与下一位彩色光标正常显示。

### 安全修复

#### 网站数据导入导出与个人信息页全量安全加固
- 导入固定逐表字段白名单，彻底移除不可信列名拼 SQL；导入 ID 与当前用户已有数据冲突时才更新，新增或跨用户冲突全部生成新 ID，并重映射持仓到订单、分组引用，阻断跨用户数据归属夺取。
- 严格校验格式版本、字段、JSON、长度、数值、引用归属与站点设置键白名单，限制 10MB、各表条数与总 7.5 万行及调用频率；导入前备份失败即停止，预览与正式导入执行同一归属检查，历史孤立订单不再进入备份。
- 导出与个人资料响应 no-store，Web 登录不再向 JS 返回 token，移动端 token 保留在独立 v1 登录；清空持仓和注销账号均由服务端验证当前密码并限流，清空使用事务同步处理订单；修改邮箱也要求当前密码；上传增加用户与全局限流。
- 新增 security_audit 持久安全审计日志，记录导入、导出、上传、改密、清空、注销与拒绝事件。恶意字段、越权订单、过新版本、无密码清空与注销均实测拒绝；跨用户同 ID 正式导入实测原归属不变且副本自动换 ID。

---

## v0.1.16 · 2026-08-24

### 新功能

#### 素材库图标列表显示真实文件路径
- assets.url 存的是 encodeURIComponent 编码形式，此前行内仅显示名称与代码，用户无法查看或复制素材实物文件地址。
- 修复：组件新增 realPath() 把 url 解码为可读路径（与磁盘文件名一致），渲染在名称与代码下方第三行；点击该行即复制真实路径（Clipboard API 优先，非安全上下文回退 execCommand），复制成功提示「已复制真实路径」；无 url 的素材显示「（无本地文件）」且不可点。

#### 素材库国家旗帜改用本地圆形 SVG
- 素材库「国家/地区旗帜」tab 此前默认用 flagcdn.com 的外部 PNG（80px，依赖外部加载），按用户要求替换为 GitHub 开源高清 SVG 并本地保存。
- 调研对比 lipis/flag-icons（4x3 矩形）、hampusborgos/country-flags（7410×3900 矩形）与 hatscripts/circle-flags（512×512 圆形、mask 裁切）后，按用户要求选定圆形版：每国一个 {iso2}.svg，自带圆形 mask 裁切，MIT 许可。
- 脚本批量下载 249 国圆形 SVG 到 public/uploads/asset/flag/，缺失的 bq、bv、hm、sh、sj、um 六个代码映射到真实或同源旗帜；旗帜 tab 默认来源改为本地路径，用户上传的自定义旗帜仍优先。

#### 全球经济热图国旗改用本地素材库
- 全球经济热图里的国旗此前用 flagcdn.com 的外部 PNG，与素材库国家/地区旗帜不一致。
- 修复：地图悬停 tooltip 的 flagMarkup 与右侧经济看板排行的 CountryFlag 两处默认来源，统一改为本地素材库圆形 SVG，用户上传的自定义国旗仍优先；tooltip 圆形容器与看板国旗保持 20px 圆形显示，组件内的 flagcdn 引用已全部清除。

#### 附件管理分类重构
- 把「财报文件」从素材资源内部的二级界面独立出来，作为附件管理的一级分类 tab（位于「文件管理」右侧），分类变为 文件管理、财报文件、素材资源三态。
- FinancialAttachments 从 LibraryAttachmentsView 抽出并导出为可独立复用的组件（新增 standalone 模式：独立显示搜索栏，取消原二级面包屑），素材资源内部的「财报文件」二级入口按钮移除；财报文件 tab 复用 /api/v1/financial-reports 数据（市场、交易所、公司、报告期分组，搜索、分页、删除）。

#### 财报文件改为类网盘文件夹导航
- 把财报文件从平铺分组改为参照主流存储网站的文件夹导航：面包屑逐级下钻 全部 → 市场（美股 / 港股 / A股）→ 交易所 → 股票 → 财年 → 报告类型，每层文件夹卡片显示该目录文件数，点击进入下一层，面包屑可任意回退。
- 股票层文件夹图标默认使用素材库股票图标（键为 市场:代码），无图标回退文件夹图标；最内层（报告类型）用文件表格展示（类型、文件、大小、删除）。
- 报告类型由 fiscalPeriod 与 reportType 推导（FY 年报、H1 与 H2 半年报、Q1–Q4 单季报），新增 reportCategory 与 reportCategoryRank 归类函数。

#### 财报文件夹导航各层配图标
- 财报文件网盘式导航每层配图标，参考主流存储网站与 TradingView 指数图标：市场层（美股 / 港股 / A股 / 日股 / 韩股）默认使用素材库市场图标。
- 交易所层用映射表取交易所自家公司在素材库的图标（NASDAQ→NDAQ、NYSE→ICE、AMEX→CME），无则用市场图标兜底；股票层沿用素材库股票图标。
- 新增标普 500 市场素材并本地保存 TradingView 指数图标（DJI、IXIC、NDX 等因 TradingView 接口 403 限流未直连获取）。

#### 财报页市场文件夹支持自定义图标
- 删除财报文件详情顶部一行「共 N 个财报文件 · 按 市场分类 / 股票名称 / 年份 / 报告类型 递进浏览」说明文字，层级已由面包屑体现。
- 市场与指数层文件夹图标支持自定义上传：文件夹图标悬停显示相机角标，点击弹文件选择，上传走 /api/upload（kind=asset、folder=market）并更新素材库 market 图标记录，成功后派发 fire:assets-updated 全局刷新，市场图标在自选股、持仓与素材库同步生效。

#### 交易所层补上自定义图标上传
- 上一步给市场与指数文件夹加了相机上传，但实测进入美股后的交易所层文件夹卡片没有相机 —— 只在最外层传了 uploadableKey，交易所层的调用漏传。
- 修复：folderCard 新增 onUpload 回调参数；新增 uploadExchangeIcon(market, exchange, file)，交易所层图标按 stockIcons[市场:映射代码] 取图，因此以 type=stock（market 与 code 都指定）落库到素材库，成功后派发 fire:assets-updated 全局刷新；市场层调用同步改为传 onUpload。

#### 美股财报自动补全（SEC EDGAR）
- 财报文件此前几乎为空（只有手动上传的 AAPL Q1），因此新增脚本 scripts/backfill-sec-financial-reports.mjs，从 SEC EDGAR 补齐持仓与自选里美股近一年财报。
- 脚本用 SEC 的 company_tickers_exchange.json 映射 ticker 到 CIK、交易所与公司名，拉取 submissions 取最新年报（10-K，外国发行人用 20-F）与最近 4 个 10-Q（美股的 Q4 并入年度 10-K），并按财政年末月推导 fiscalYear 与 fiscalPeriod，保存 SEC 主文档到 public/uploads/reports/ 对应目录，写入 financial_report_files（file_kind=filing、source=sec）。
- 脚本幂等（跳过已存在）并自动排除基金与 ETF（按公司名特征）。实测补齐 20 家美股共 83 份，数据库记录从 1 行增至 84 行；UI 新增 filing 类型，财报附件表格标注「SEC 文件」与「SEC」徽标，点击在新窗口打开原文。

### 修复

#### 修复美股扩展时段昨收错位
- 排查「美股当日盈利还未更新」：盘前时段富途快照的 prev_close_price 会落后一个常规交易日（如周一盘前返回上周四收盘，AAPL 报 311.30，而真正上周五收盘是 309.35），但官方 pre_change_val 是基于真正前一常规收盘计算的（AAPL 盘前 310.35 − 309.35 = +1.05，正确）。
- 改动：桥接脚本 scripts/futu_quotes.py 在扩展时段（PRE / AFTER / OVERNIGHT）命中官方涨跌字段后，用 prevClose = price − change 反推当日基准，保证昨收、今日涨跌与当日盈亏使用同一基准；缺 change 时同样反推，避免回退到错误的旧昨收。
- 修复前 /api/v1/stock-detail 对 AAPL 报 price 310.4、prevClose 311.30、change +1.05（price − prev = −0.95，互相矛盾），修复后为 price 310.4、prevClose 309.35、change +1.05（一致）。仅影响美股扩展时段行情来源，不改港股与 A股、不改分享页。

#### 素材库与附件管理操作按钮统一
- 图标素材、股票、券商与分组图标列表的操作列（编辑 / 预览 / 删除）此前尺寸与配色不一致，统一为 h-7 w-7 圆角 8px 边框按钮：编辑与预览用中性悬停，删除用红色语义，图标统一 14px，含禁用态。
- 操作列与「市场」列间距由 8px 放宽为 12px，三个操作按钮之间由 4px 放宽为 6px，操作列宽由 72px 加宽到 96px，编辑态的「保存」按钮同步为 h-7 圆角框，不再布局跳动。
- 生效文件：LibraryAttachmentsView、AssetLibraryView、WatchGroupSheet、CelebsManageModal；删除按钮从几乎不可点的小图标变为清晰的 28×28 圆角框按钮，默认色统一 text-muted（悬停才加深）。

#### 素材资源进入加速
- 素材库（素材资源）首次进入显示「正在读取素材库」，每次挂载都重新请求分页数据。
- 新增前端分页缓存：缓存键按类型、市场、搜索、排序与页码组合，值含时间戳与数据，5 分钟 TTL；有缓存时立即渲染并保持 loading 为 false，再后台刷新覆盖；请求成功后写入当前页缓存并预取第 2、3 页，无缓存时才显示加载态。
- 实测：素材库 API 单页约 0.05s，首次进入 loading 约 0.8s 消失，二次进入与翻页由缓存秒出。

#### 附件管理 URL 状态持久化
- 附件管理刷新后分类与财报文件目录会重置回默认，因此新增 URL 状态持久化：分类写入 ?category=docs|reports|library（默认 docs 时移除参数）。
- 财报文件文件夹路径写入 ?view=市场/交易所/公司代码/财年/报告类型码，参考主流存储网站使用稳定代码而非中文长名：公司用 companyCode、报告类型用数字码（年报 01、半年报 02、单季报 03），显示时才映射回中文。
- 实测下钻至 美股 → NASDAQ → Apple → 2026 → 单季报 后地址为 ?category=reports&view=US/NASDAQ/AAPL/2026/03，刷新后面包屑与视图保持。

#### 财报目录 URL 去掉编码乱码
- 用 URLSearchParams.set 写 ?view= 时会把路径里的斜杠编码成 %2F，地址栏出现 US%2FNASDAQ%2FAAPL 的乱码。
- 改为手动拼 query 字符串（每段 encodeURIComponent 后按斜杠拼接），地址栏显示可读的 ?view=US/NASDAQ/AAPL/2026/03；读取端仍按斜杠分割，兼容不变。

#### 文件夹图标深色模式适配
- 财报文件夹卡片的图标容器此前用浅色底加 object-cover 拉伸填充，在深色模式下图标周围露出一圈白底方块。
- 修复：图标容器背景改为透明，图片改为 object-contain 加 p-1，保持旗帜与 SVG 原始比例居中，不再拉伸填充。

#### 财报文件夹图标统一为圆形
- 财报文件夹卡片图标展示不一致：市场层是圆形国旗，而交易所层取的是方形公司 logo，视觉上一圆一方不协调。
- 按用户要求统一为圆形：图标容器由圆角方框改为 rounded-full，图片由 object-contain 加内边距改为 object-cover（方形 logo 被裁成圆形，圆形国旗仍贴合），相机角标遮罩同步改为圆形。

#### 附件管理配色统一为蓝色
- 附件管理-文件管理页配色不统一：文件夹图标与左上「附件管理」头部图标是橙色，而「上传文件」主按钮是蓝色。
- 按用户要求统一为蓝色：把附件主题变量中的文件夹主色与浅底从橙色改为与 accent 一致的蓝色（浅色模式主色与浅底、深色模式对应色一并替换）。该变量驱动所有文件夹相关图标与浅底，因此头部图标、行、网格、详情、弹窗与树形视图的文件夹图标全部由橙变蓝。

#### 财报公司文件夹显示中文名
- 财报文件公司层文件夹此前主显示 SEC 英文公司名，下方是「代码 · N 份」。
- 改为主显示中文名称、下方为股票代码：从素材库 stock 行建立代码到中文名的映射，公司层主文本用中文名（无映射时回退 SEC 公司名或代码），下方只显示股票代码；市场层本就是中文，交易所层维持。

#### 财报文件读取提速
- 财报文件 tab 首次进入会显示「正在读取财报文件」很久：开发服务器首次命中相关接口会做一次性冷编译（各约 10s），之后降到 36ms；此外每次进 tab 组件重挂载都会重新请求并闪加载态。
- 提速：财报文件列表新增客户端缓存（5 分钟 TTL），重进 tab 或切回时直接读缓存秒出首帧、保持 loading 为 false，仅无缓存时显示加载态，后台刷新再覆盖；并对开发服务器的关键路由做预热。

#### 附件管理分类 tab 图标对齐
- 附件管理顶部三个分类 tab 的图标相对文字偏下，尤其「素材资源」最明显。
- 排查：tab 按钮是两列网格，而图标被设为跨两行网格，但这些 tab 只有一行内容，跨两行会让图标在更高区域垂直居中而比文字偏下（移动端媒体查询恰好是自动行高，所以不偏）。修复：基础规则改为 align-self:center，让图标与文字在同一行垂直居中。

#### 素材资源筛选胶囊改为中性灰
- 附件管理-素材资源的分类 tab 与股票市场筛选胶囊悬停高亮用了橙色，与全站中性色标准不符，也未与页面其余部分统一。
- 排查：橙色来自组件内联样式与全局分段控件悬停规则（注释写明应用中性灰白却误用橙色），素材库分页按钮也有同款。修复：分类与市场胶囊悬停改为中性浅灰加深字（深色模式自动适配），全局分段控件悬停同样改为中性灰（6 个组件统一），分页按钮同步修改。设置中的「V1 富途橙」属可选主题色，未改动。

#### 全站橙色高亮统一为中性灰
- 按用户要求做全站统一：首页、资产库（分类与市场筛选触发器打开态、圆形小按钮、列表项）、财报日历（分类与市场胶囊）、全球经济（双侧胶囊）、我的持仓（排序与分页按钮）、自选股（分页按钮）共约 20 处橙色悬停与打开态，统一改为中性浅灰加深字（深色模式为白色 10%），并把 API 文档编辑器光标色改为中性灰。
- 保留「设置 → 主题」中 V1 富途橙等可选主题预设 —— 那是刻意保留的选择型主题色，不是站点高亮。

#### 修复 K 线对比模式报错
- 个股 K 线在「涨跌幅对比」模式报运行时错误 yAxis "O" not found。
- 排查：标记线数据里的最新价与基准值直接取自行情序列首尾，接口可能返回字符串（类型标注为数字数组但运行时是字符串），传给 yAxis 后被 ECharts 当成轴名引用而抛错；对比模式触发重渲染必现。
- 修复：基准值与最新价改为 Number(...) 强转并用 Number.isFinite 守卫，非有限数字就不画该标记线；对比模式下价格序列不需要最新价线（归一化百分比轴，画最新价反而失真），直接去掉标记线；对比序列的渲染条件加上「正在对比」判断，避免残留时轴索引越界。

---

## v0.1.15 · 2026-08-22

### 新功能

#### 补全自选股相关 ETF 列表
- 借富途 OpenD 搜索逐项核对代码与名称后补全 lib/relatedEtfs.ts：新增 MU、NFLX、TSM、SNDK、UNH、ECHO、INTC、NIO、WDC、XPEV 等正股条目（含对应的 2 倍做多、1 倍做空与期权收益产品）。
- 扩充已有条目：TSLA 增加 TSDD、TSLT、TSLR、TSYY，AMD 增加 AMUU、AMDD，MSFT 增加 MSFX，GOOGL 与 GOOG 增加 GOOX，META 增加 METU，COIN 增加 COIW，RKLB 增加 RKLZ，MSTR 增加 MSTZ 与 MSTY，SMCI 增加 SMCZ。
- EchoStar 已于 2026-06-24 由 SATS 更名 ECHO，其 2 倍 ETF 由 SATG 更名 ECHX，目录同步新代码并保留旧 SATG 记录的兼容映射；图标兜底关系自动覆盖全部新增 ETF。

#### 新增股息记录与股息入账
- 数据层：富途公司行动分红接口桥接（bridge 脚本新增 dividends 命令），并解析「1 股派息 0.27USD」「末期息 5.3 港元」等声明得到每股金额与币种，实物分派与优先发售标记为 special；SQLite 缓存 24 小时，富途不可用时不缓存失败，恢复后自动可取；新增 GET /api/v1/dividends（美股、港股、A股）。
- 个股详情页：在概览与 ETF 旁新增「股息」页签，展示除净日、派息日、公布日、每股金额、年度与方案原文。
- 订单系统：trade_orders.side 扩展出 dividend（旧库自动重建表迁移，存量 420 笔订单无损），股息入账不改变持仓数量与成本，按每股股息乘股数记为已实现收益，更正与删除时重放账本同口径；我的持仓交易弹窗新增「股息」方向（默认带入当前持仓股数），订单列表与详情展示股息徽标。

#### 股息自动入账
- 券商 xlsx 导入支持股息行：方向或业务类型含「股息 / 红利 / 分红 / 派息」时按股息入账（每股股息乘股数记为现金收入），不再被误判为卖出，净持仓计算与重放同口径。
- 新增惰性任务（getDb 触发、15 分钟节流、单实例保护）：服务器有访问时对有持仓的记录拉取富途股息，派息日已到且未生成过（按订单备注去重）则按「除净日持仓数量 × 每股股息」自动创建股息订单，卖出封顶重放不抛错，单只失败不中断。
- 实测按历史派息自动补建 TSLL、GOOGL、AAPU、VOO、ROBN 等 15 笔股息订单，持仓数量与成本均不变，重复运行不产生重复订单。

#### K线新增前复权/后复权切换
- K 线工具栏新增「前复权 / 后复权」切换：美股周、月、季、年 K 走富途前复权与后复权，港股、A股、日股、韩股日 K 走腾讯前复权与后复权（东方财富兜底同步），美股日 K 保持前复权（Yahoo 与新浪不提供后复权，且日 K 窗口为近 4 个月，差异极小）。
- 请求与缓存按复权方式分键，切换即时生效。实测 GOOGL 月 K 后复权与茅台日 K、月 K 后复权均正常。

#### 截图导入接入 DeepSeek 视觉识别
- 按官方文档接入 DeepSeek 图像理解：新增 lib/deepseekVision.ts —— 配置 DEEPSEEK_API_KEY 后截图导入优先调用 OpenAI 兼容接口，base64 内联图片（单图上限沿用接口 8MB），提示词要求按「名称|代码|数量|成本|现价|涨跌幅」一行一只输出，未配置密钥或调用失败自动返回空。
- 导入接口改为双引擎：DeepSeek 文本行转合成视觉坐标后复用现有解析逻辑（不新增解析分支），解析不到股票时自动回退本地 Apple Vision 再试一次，响应带 provider。
- 前端预览区显示所用引擎，上传提示不再写死「仅本机识别不上云」；环境变量示例新增 DEEPSEEK_API_KEY、DEEPSEEK_BASE_URL 与 DEEPSEEK_VISION_MODEL 说明，限流与 JPG、PNG、GIF、WEBP 校验保留。

#### 登录改为长桥式弹窗
- 应用内「登录」入口由跳转改为默认弹出 LoginModal：根布局挂载全局弹窗，监听打开事件，Esc 与点击遮罩关闭；/login 单独页保留供直接访问与服务端守卫使用。
- 弹窗与单独页共用登录表单（仅账号密码：用户名、密码、确认密码、服务协议勾选、填写完整才可点的主按钮、去注册与去登录），配色统一改为长桥的青绿，去掉手机验证码、扫码登录、地区选择与第三方社交。
- 弹窗采用分栏：左侧展示站点设置的登录侧图（管理员在「设置-网站形象」上传、粘贴或清除，未上传用品牌渐变兜底），右侧为表单区；公开设置接口同步暴露该字段，新增 /uploads/login 上传类别（10MB），上传与设置接口均做管理员鉴权。

#### 登录支持用户名或邮箱
- 登录页与弹窗在「登录」状态新增「用户名登录 / 邮箱登录」切换，首字段占位分别为「请输入用户名」与「请输入邮箱」（邮箱为 email 类型）；注册仍为用户名加密码，注册模式保留用户名规则占位说明。
- 后端登录按用户名精确匹配或邮箱大小写不敏感匹配（新增 findUserByLogin），登录标识符长度上限由 20 放宽到 254（兼容邮箱），密码上限 128 不变。
- 邮箱绑定在「设置 → 个人信息」进行：接口新增邮箱格式与唯一性校验（绑定他人邮箱返回 409），绑定后即可用该邮箱登录。实测用户名注册、用户名登录、绑定邮箱、邮箱登录（大小写不敏感）全链路通过。

#### 2 倍杠杆 ETF 加 2x 徽标
- 参考图给 2 倍杠杆 ETF 加「2x」角标，颜色与市场色块一致：新增 isDoubleEtf(market, code, name)（遍历相关 ETF 目录命中 2x、2倍、两倍，再按名称兜底覆盖港股、A股或中文名）与 EtfDoubleBadge 组件（调用市场色块取色，白色细边与柔和投影，圆角小胶囊、白字、文字固定小写 2x）。
- 角标按参考图叠放在股票图标右上角，接入自选股列表行图标与个股详情相关 ETF 列表图标；相关 ETF 列表原有的灰色方向徽标仍保留，1x 与 1.5x 不加角标。

#### K 线划过主图联动高亮下方成交量/成交额柱
- 尝试实现「鼠标划过主图联动高亮下方成交量与成交额柱」（给柱系列加强调并监听坐标轴指针事件）。
- 按用户反馈已回退，保留主副图对齐修复与副图十字线标签处理。

#### K 线图记住上次选择的周期
- K 线图每次进入或切换股票都默认日 K。修复：把 K 线视图（周期、时段、分钟）持久化到 localStorage —— 首次实现只记周期，导致选「盘后」刷新后被旧周期覆盖。
- 现改为整体记录周期、时段与分钟，改动即自动保存，初始、进入与切换股票都整体还原；选周期会把时段重置为全天，选具体时段则进入分钟线并记录。

#### K 线技术指标与图表设置自动记忆
- 技术指标（均线、指数均线、布林线、MACD、KDJ、RSI、成交量、成交额）、均线周期配置、前后复权、图表样式与均线显示开关原来在刷新或切换股票后都会重置。
- 新增持久化：读取时校验后还原上述设置，改动时自动保存，进入与切换股票时用保存值还原，且不再把样式覆盖回第一个基础样式。

#### 个股详情报价头部对齐 Google Finance
- 涨跌箭头改用长桥同款实心等腰三角，紧贴涨跌幅文字前并继承涨跌色；股价颜色分时段 —— 盘中左侧「今天」价格跟随涨跌色，夜盘、盘前、盘后时左侧价格为模式黑、右侧扩展股价跟随涨跌色。
- 主「今天」块以当日常规收盘为基准，右侧「盘前 / 盘后交易」块带月亮图标，相对常规收盘计算涨跌；状态行改为「休市：8月21日, UTC-4 16:00 (美东)」，时间不含秒、去掉币种码，按市场显示当地时区中文名。
- 有返回箭头时给现价行补左侧占位，使大价格与上方股票图标左边缘对齐；夜盘与休市（美东 20 点后）也延续显示最近一次盘后交易，避免扩展时段误标「夜盘」。

#### K 线新增涨跌幅比较
- 个股详情页 K 线图新增「涨跌幅比较」，入口独立在工具栏（图表设置齿轮左侧）：展开提供搜索框与候选列表（常见美股比较标的），带实时价格与红绿涨跌幅。
- 选中后显示可删除的图例胶囊，同时把主图与各比较标的统一为折线并按「相对视图区间首价的涨跌幅」归一化，共用同一个百分比轴；图表下方渲染对比表格（代码、价格、涨跌额、涨跌幅、昨收盘），主股票与每个比较标的各一行。
- 比较标的行情经批量行情接口拉取、搜索走搜索接口并防抖，切换股票自动清空；另把「今年至今」从周期按钮收进「周期 → 长周期」菜单。

#### 快捷交易窗口支持拖动
- 持仓快捷交易弹窗改为可自由移动的桌面窗口：默认居中打开，按住标题栏即可整窗拖动，四个方向都能移动。
- 拖动范围做边界限制 —— 至少保留 120px 可见横向、顶部不低于 12px、底部保留 80px，不会完全被拖出屏幕；标题栏按钮与输入控件不触发拖动；位置存入 localStorage，重开按上次位置恢复。

#### 快捷交易窗口名称排版调整
- 交易窗口的名称与代码排原先名称靠左、代码推到最右，两者距离很远；改为分组靠左排版 —— 股票图标、名称、代码紧密相邻，名称不再占满剩余宽度。
- 名称前加入股票图标，复用持仓表同一套图标来源（含 2 倍 ETF 复用正股图标），无自定义图标时回退名称首字圆形头像；图标与名称垂直居中。

#### 补齐订单引擎
- trade_orders 增列订单类型、触发价、有效期、到期时间、时段与触发状态，status 扩展到「待成交」与「已失效」，旧库自动重建迁移（重建时临时关闭外键以容忍孤立订单）。
- 新订单创建走统一入口：市价单、限价单加当日有效立即成交（沿用原子更新持仓与成本），限价单加撤单前或自定义有效期、到价买入、到价卖出、反弹买入、回落卖出记为挂单（不动持仓，快照当前持仓）。
- 惰性结算在读取订单时触发（每用户 12 秒节流）：批量拉取挂单标的现价，按类型判定触发（限价买用价格小于等于、限价卖用大于等于；到价买用现价小于等于触发价、到价卖大于等于、反弹买大于等于、回落卖小于等于），命中则事务成交并更新持仓、成本与已实现盈亏；自定义超期或当日失效置为已失效，可卖不足置为超卖失效。
- 撤单接口仅待成交可撤，二次撤单返回 400；前端交易弹窗把订单类型、有效期、有效期日期与时段提交给后端，挂单成功提示「已挂单，等待成交」，下单成功同时刷新持仓与订单列表。实测限价当日成交、到价触发成交、到价未触发保持待成交、撤单与二次撤单、自定义有效期写入均符合预期。

#### 订单面板接入真实挂单字段
- 订单面板原先的委托类型、有效期、时段、触发价格、触发状态、剩余挂单、撤废单等列都是占位文案，现接入真实数据：按订单实际值渲染，旧成交回退默认值，股息单显示「股息入账」与破折号。
- 订单状态徽标扩展为已成交（绿勾）、待成交（蓝时钟）、已撤销（红叉）、已失效（红叉），状态筛选新增待成交与已失效，导出与详情文本同步；待成交挂单的操作列新增「撤单」按钮，点击确认后撤单并刷新；排序键改用真实值排序。

### 修复

#### 补全 ETF 总市值
- 个股详情页大量 ETF 总市值显示「—」：富途快照对 trust 只返回单位净值，腾讯美股字段为空，两家都不提供 ETF 总市值。
- 修复：保留富途与腾讯优先（普通美股走富途 total_market_val、港股与 A股 ETF 走腾讯），仅当市值仍缺失且为美股时，用「份额 × 现价」补齐（份额取自 stockanalysis.com ETF 页，缺失退基金规模），结果缓存到 SQLite（成功 24 小时、失败 6 小时），不阻塞正常行情。
- 接入 v1 个股详情接口后，quote.marketCap 与六币种市值同步补齐。实测 VOO、TSLL、AAPU、NASA、DRAM、RKLX、ECHX 全部有市值，AAPL 与港股 02824 回归正常。

#### 个股详情页相关 ETF 按市值分组排序
- 自选股点进个股详情的 ETF 列表改为做多、做空、收益策略三组，组内按市值从大到小，缺市值排组尾，不再按目录顺序混排。
- 批量行情接口 /api/v1/quotes 同步补 ETF 市值（单次最多真正抓取 12 个，缓存命中为纯 SQLite 读），排序数据即时可用。

#### 个股详情股息页签按年份分组
- 按用户要求，个股详情页「股息」页签改为按年份分组展示（以除净日、派息日、公布日年份从新到旧分组，每组显示年度与期数）。
- 同时删除「数据来源：富途公司行动-分红派息」提示文字，行内冗余的财年徽标一并移除，与年份分组保持一致。

#### 股息年份改为顶部一排切换
- 按用户要求，个股详情「股息」页签的年份由纵向分组改为顶部一排年份胶囊（全部加各年份，从新到旧，可横向滚动）。
- 选中某年后只展示该年记录，选「全部」恢复按年份分组展示。

#### 修复 A 股股息与 K 线获取失败
- 股息：富途公司行动接口不支持 A股（返回空），新增东方财富分红送配接口（只取「实施分配」），每股金额按每 10 股派现除以 10、币种为 CNY，含除净日、股权登记日、公告日与方案原文，缓存 24 小时；A股股息来源标记为 eastmoney，前端不再误判为「富途未连接」。
- K 线：腾讯前复权单次最多约 640 条，请求 3200 条（季 K 等长周期）会返回参数错误导致整页失败；改为按截止日翻页抓取（每页 640，页序倒排拼接为升序），实测 600519 与 000858 可返回 2013 年至今 3200 条完整历史，东方财富全量历史作为最后兜底。

#### 自选股分组标签秒出
- 「我的行情板」的美股、港股等分组标签每次进入自选股都要等分组接口返回才渲染，而页面每次都重新请求。
- 修复：分组列表缓存到 localStorage（60 秒 TTL），进入页面先用缓存秒出标签，再后台拉取最新分组更新，旧配置迁移逻辑不变。

#### 个股详情改为路径式 URL
- 自选股个股详情不再使用 ?symbol= 与 ?filter= 查询参数，改为路径式 /watchlist/US.GOOGL（股票代码就是唯一标识），分组筛选只保留在页面内。
- 改动：布局识别「页签路径 / 市场.代码」并传入初始代码；RecordsApp 地址栏同步对详情子路径做保护，不再把 /watchlist/US.GOOGL 改回 /watchlist；QuotesView 改为路径式详情（打开时写入路径、返回移除代码段、popstate 跟随），旧链接自动兼容并清理为路径式。

#### 自定义分组图标默认取组内最高市值股票
- 自选股分组标签图标规则：默认市场组保持市场图标；自定义分组未上传图标且非券商分组时，默认取该分组内市值最高的股票图标（行情缺失时取组内第一只有图标的股票）。
- 券商分组仍走素材库券商图标，自传分组图标优先级最高。实测：核心持仓→腾讯、科技→英伟达、消费→茅台、观察中→五粮液。

#### K 线副图与悬浮提示优化
- 副图改为上下两块：上方「成交额」（按成交量乘现价估算，与详情页口径一致）、下方恢复「成交量」，悬浮提示同时显示成交额与成交量，单位统一为中文「1亿 / 1000万 / 100万」风格，提示框不再有「更多」折叠。
- 鼠标划过时均线与布林线不再显示圆点，只保留主图十字线；默认视图恢复全量（此前试验性的「默认显示最近 8 根」已回退），滚轮与拖动缩放查看。

#### 美股周期 K 线直连富途前复权
- 排查谷歌-A 月 K 与年 K 和富途出入很大：根因是美股日 K 来源（新浪）返回未复权价 —— GOOGL 2022 拆股前显示 2700–3000，而富途前复权约 136–150，聚合出的月 K 与年 K 自然差几十倍；同期 NVDA、AAPL 等拆股股同样受影响。
- 修复：周、月、季、年 K 改为美股直连富途周期 K（前复权），实测 GOOGL 月 K 与年 K 和富途逐值一致，NVDA 2023-06 月 K 从 384.89 修正为 38.41；日 K 主源改为 Yahoo（含拆股复权），新浪兜底；港股、A股、日股、韩股周期 K 保持腾讯前复权日线聚合兜底。

#### K线成交额/成交量拆分为两个独立开关
- 按用户要求，副图指标由合并的「成交额 / 量」拆成「成交额」与「成交量」两个独立按钮，默认都开启。
- 可分别开关，面板布局随开启数量自适应（0、1、2 个副图时主图高度自动调整，成交额在上、成交量在下）。

#### K线底部指标顺序调整
- 按用户要求，K 线图最下方右侧指标顺序改为「成交量」在首位、「成交额」在后；默认只显示成交量副图（成交额需手动开启），布局随开关数量自适应。

#### 前复权/后复权按钮深色适配 + 紧凑尺寸
- 修复前复权与后复权按钮在深色模式下背景为白色、宽度过宽的问题：新增深色样式（暗色玻璃底、浅灰文字，悬停加深），按钮改为紧凑尺寸（高 36px、去掉最小宽、11.5px 字号、圆角 9px）。

#### 修复收益率曲线假深坑
- 资产盈亏分析页「收益率走势」曲线此前一年都平在约 -110% 的深坑，而盈亏总额大数字显示 +52% 的正收益，两者矛盾。
- 根因：时间加权每日收益率算成本来用「当日现金流」，该页却把 pendingFlow 按整个序列累计后再扣减，导致前期被反复扣到接近 -100% 而后拉平。
- 修复为与资产分析页同口径 —— 使用当前交易日现金流（非交易日成交仍归入下一交易日），去掉累计扣减。

#### 收益率趋势图对齐参考图
- 按参考图美化共享的收益率趋势图与两处图例：纵轴移到右侧并隐藏轴线与刻度，仅留淡虚线网格；基准线（标普 500 等）由黄色改为干净的蓝色，资产分析页与盈亏分析页两处图例的基准色点同步统一。
- 面积渐变、虚线网格与提示框保持，横轴日期自动避让。

#### 修复首页顶栏刷新后短暂丢失
- 首页顶栏右侧的指数行情条与迷你趋势、深浅色按钮、登录按钮都依赖客户端水合后渲染，导致刷新后顶栏空着一大块。
- 修复：顶栏右侧不再区分检查中与未登录，默认按访客渲染主题切换与登录按钮（登录后水合完成再替换为用户头像菜单）；指数行情加载中由空白占位改为可见骨架条（行情图标、名称、价格、涨跌的脉冲占位），数据到达后再填充真实行情与趋势。

#### 顶部指数行情切换时市场图标即时跟随
- 首页顶部指数行情条每 5 秒轮换指数，市场图标应随指数市场变化，但原图片元素复用同一节点且带懒加载，切换时地址已换而图片未即时刷新。
- 修复：给市场图标加按市场代码的 key 强制重挂载并立即重新加载，并移除懒加载（小图标始终在首屏、需即时换图）。

#### K 线主副图横向对齐
- 逐步定位个股详情 K 线「只开单个副图对不齐」：主图蜡烛与副图柱类目中心错位（约 24px，且只在底部显示日期标签的副图上出现）。
- 用 ECharts 坐标换算逐下标量测定位：ECharts 会对显示 X 轴日期标签的网格自动向左内缩约 24px（即使关闭标签包含），导致只开成交量或成交额时首根柱相对蜡烛左移。
- 修复：主图、成交量与成交额网格统一使用同一组常量（关闭标签包含、统一左右边距），left 正好落在 ECharts 的天然内缩位置，所有网格不再各自内缩，实测单副图与双副图在首、中、末下标误差不超过 0.44px；网格边距与类目间距抽成共享常量，避免日后主副图分叉。

#### K 线副图十字线标签处理
- 成交额与成交量副图 Y 轴的十字线标签默认显示原始长串数字（如 171,875,000.00），与提示框的「1387.17万」不一致；尝试用标签格式化改成亿与万单位无效（ECharts 对子网格的十字线标签不稳定，会出现不显示或显示破折号）。
- 最终方案：直接隐藏副图 Y 轴的十字线标签，成交量与成交额数值以提示框为准，彻底消除对不上；保留主副图对齐修复。

#### K 线副图柱去雾化
- 下方成交量与成交额柱使用半透明填充，视觉上像蒙了一层薄膜；修复：把红绿柱不透明度提升到接近实色（红涨绿跌方向不变），达到去雾化效果。

#### 修复美股盘后当日盈亏被重置
- 我的持仓美股当日盈亏在美东 19:21（盘后但未到 20:00 结算）被重置。
- 根因：美股扩展行情的昨收用「常规盘价或昨收」兜底，而盘后该字段等于当日常规收盘，导致涨跌只剩盘后波动，视觉上像当日盈亏归零。
- 修复：盘后改用昨收作为当日基准，涨跌即盘后价减昨收，得到完整当日涨跌；盘前与常规仍用当日常规收盘口径，避免夜盘刚结束时昨收滞后；富途路径本身正确，未改动。

#### 修复美股周K只显示 2010-2016 旧数据
- GOOGL 周 K 点开后几乎空白或全是旧数据。根因：富途历史 K 线带起始日期且区间内根数超过单次上限时返回最早的一批，美股周 K 从 2010 年至今约 830 周超过上限 320，只返回 2010–2016；月、季、年 K 总根数小于上限所以正常。
- 修复：把起始日期改为按单次上限与周期长度回推的「最近窗口」，让富途返回最近一批；实测 GOOGL 周 K 现在返回 2020-07-06 至 2026-08-17。

#### K 线夜盘可选 + 选「全天」不再自动切分钟
- 时段下拉里的「夜盘」已启用（原先为灰色禁用）。
- 修复「选全天右边自动选 1 分钟」：原点击时段按钮会强制切到分钟线；改为点击「全天」保持当前周期，只有选夜盘、盘前、盘中、盘后或手动在周期里选分钟才进入分钟线。

#### 个股详情港股与 A股时段判定
- 个股详情页底部时段标签对非美股写死「盘中」，导致港股与 A股实际已休市仍显示「盘中」；修复：非美股改用按时区判定的时段状态（盘前、盘中、盘后、夜盘、休市）。
- 大价格带货币符号，改用按市场格式化的数字（去掉符号、按市场保留小数：美股 3 位、其余 2 位）。

#### 快捷交易窗口配色对齐参考图
- 按参考图重排持仓快捷交易窗口的配色分层：方向色不再只涂表单区，整窗背景即为方向色（买入深棕、卖出深绿，浅色为对应浅底），标题栏与名称、代码两排独立为纯黑与近黑，浅色为纯白，形成「顶两排黑或白 + 下方红或绿」的分层。
- 表单输入面板统一为参考图同款深色块（细分隔边框），不再用偏蓝的色值；底部「0.00 USD / 预估成交后持仓成本」与方向色同底色，仅保留细分隔线；方向按钮颜色保持；字段标签在深色下提亮。

#### 快捷交易窗口日历向上展开
- 持仓快捷交易的「自定义有效期」日历原为向下翻开，而「时效」位于表单最后一行，日历约 340px 高，向下会被交易窗口的溢出裁剪，日期底部与星期错位。
- 修复：日历改为向上翻开，从「时效」下拉上方展开并完整落在窗口内，底部金额与买入按钮不受遮挡。

#### 快捷交易窗口方向色边框
- 方向按钮由两个分开的按钮改为单一圆角分段容器 —— 买入与卖出共用一个带方向色边框的框，选中侧实色填充、未选中侧透明（悬停轻微高亮），容器裁切保证选中色块与边框贴合。
- 所有输入、下拉与方向框的边框统一为方向色（买入红、卖出绿），不再用中性灰或浅桃色，做到买入界面所有框为红色、卖出界面所有框为绿色。

#### 交易弹窗币种与表单稳定性修复
- 交易弹窗底部「成交金额 / 预估成交后持仓成本」原先硬编码 USD，港股与 A股会被显示成美元；改为按记录市场映射原生币种代码（美股、港股、A股、日股、韩股），买卖方向均正确显示。
- 弹窗打开初始化依赖了父组件传入的实时价格函数引用（随行情每几秒变化），导致行情一刷新整个表单被重置、用户输入被清空；改为用引用读取最新价格，初始化只依赖打开状态、记录、方向与初始数量，行情刷新不再重置表单。

#### 账户资产三列统计对齐
- 账户资产卡片「当日盈亏 / 持仓市值 / 持仓总盈亏」三列金额未对齐，实测持仓总盈亏列金额比另两列高 8px。
- 根因有二：持仓总盈亏原为带内边距的按钮（另两列是普通容器），垂直各多 2px；其标签使用弹性布局，行高被压缩，与另两列的内联标签相比两两叠加使其金额上移约 8px。
- 修复：第三列改为与另两列相同的容器结构与内联标签，点击、悬停与键盘事件挂到该容器，箭头改为内联；实测三列金额顶部完全一致。

#### 美股夜盘行情兜底与数据源徽标
- 行情数据增加来源标记（富途、腾讯、Yahoo），并在富途命中、腾讯回退与 Yahoo 扩展时段覆盖三处分别标注；行情时段允许「夜盘」（富途口径）。
- Yahoo 扩展时段兜底扩展到夜盘：市场处于夜盘时以夜盘口径请求，把美东 20:00–04:00 的分钟点归类为夜盘，并用当日常规收盘为基准计算涨跌（与富途夜盘口径一致）；仅在 Yahoo 真返回夜盘点时才覆盖，未返回则照旧回腾讯。
- 新增行情来源徽标组件：任意美股持仓的行情来源为腾讯（即富途未连接、退回腾讯日线）时，在资产分析与我的持仓的「账户资产」标题旁显示琥珀色「美股·腾讯兜底」小胶囊（带警示图标，悬停说明夜盘与当日盈亏可能非实时），富途正常时隐藏。

#### 搜索联想自动补价
- OpenD 在线后搜索优先走富途，但富途搜索偶发返回无价格结果（桥接超时或快照缺失），导致搜索联想出现无价格项，冒烟断言价格大于 0 失败。
- 修复：搜索结果中存在缺价项时，用批量行情按市场与代码补全价格与涨跌幅（与腾讯联想路径同源）后再返回。实测搜索「腾讯」返回腾讯控股（港股）且价格大于 0，冒烟 82 项全通过。

---

## v0.1.14 · 2026-08-21

### 新功能

#### 资产盈亏分析：日历市场筛选与可拖动窗口
- 收益日历新增市场筛选按钮（默认「全部」，分为全部、美股、港股、A股，使用市场图标），日历按所选市场计算每日真实盈亏（沿用最近有效收盘价）。
- 「全部盈亏总结」下方页签由股票、基金、新股改为全部、美股、港股、A股，与页面市场筛选联动。
- 资产盈亏分析页整体改为可拖动桌面窗口（与设置页同款）：抽出共用拖动 hook（标题栏拖拽、位置持久化、默认靠左），窗口带圆角边框阴影与内部滚动。

#### 收益日历市场图标下拉 + 日期选择器
- 收益日历日期右侧新增市场图标按钮（默认全部四宫格图标），点击弹出下拉可选美股、港股、A股（带市场图标），选择后日历按所选市场计算每日盈亏。
- 日期图标按钮实现真正的选择日期：弹出月份选择器（年份前后切换加 12 个月网格），选中即跳转对应月份；下拉与选择器点击不会触发窗口拖动。

#### 各卡片右侧统一为「选择日期」按钮
- 抽出共用的日期选择按钮（年份切换加 12 个月网格），收益日历、全部盈亏排行榜与全部盈亏总结右侧统一使用该按钮，选中月份后日历跳转并持久化。

#### 收益日历年视图生效
- 收益日历右上角按钮拆成两组 —— 「年 / 月」一组、「收益 / 收益率」一组（中间隔开）。
- 「年」视图生效：按所选年份展示 12 个月格子，每格显示当月收益合计（或收益率，随切换），点击某月跳转月视图；「月」保持原有日视图。

#### 设置风格新增 V16 极简青绿
- 设置窗口版本切换器新增 V16 极简青绿（浅色青绿白卡、深色墨绿青绿强调，侧栏 Logo 与 fire 流光同步适配）。
- 版本列表由小到大排序：V1 → V5 → V7 → V14 → V16。

#### 生成 8 款设置风格预览页
- 按用户要求生成 8 个设置风格预览页（沿用 960px 客户端窗口骨架：站点信息、网站形象、交易、数据库分区加侧栏与状态栏）：清新蓝、红黑经典、V17 石墨黑金、V18 磨砂玻璃、TradingView 风、V2EX 风、OKX 风与币安风，并生成脚本便于后续微调重出。

#### 预览页新增深浅色切换与 Apple 液态玻璃风
- 8 款风格预览每款加入标题栏深浅色切换，并为每款设计两套配色（侧栏、主区、边框、输入、行悬停、开关、强调色成体系，切换带 0.25s 过渡）。
- 新增第 9 款 Apple 液态玻璃风：macOS 风格强毛玻璃（模糊 24px 加饱和度 1.8）加高饱和渐变背景、顶部高光描边与玻璃圆角面板，浅色粉紫渐变、深色墨紫渐变，开关用苹果绿。

#### 设置风格新增 5 款
- 按用户选定把 5 款预览正式接入设置切换器（顺序为 V1、V5、V7、V14、V16、V17、V18、OKX、币安、Apple）。
- 每款含浅色与深色两套变量（外壳、背景、边框、文字、强调色、卡片、输入、悬停）、侧栏 Logo 渐变、fire 流光与激活导航强调色；V18 与 Apple 为磨砂玻璃，币安与 V17 的金色胶囊激活使用深色字保证对比，V17 标题用衬线字体。

#### 设置窗口右上角新增固定按钮
- 设置窗口标题栏右上角（版本切换左侧）新增图钉式固定按钮：点击后窗口锁定当前位置不可拖动（图钉变实心），再点解锁恢复拖动；固定状态写入 localStorage，刷新保持。

#### 盈亏总额卡片对齐参考图
- 标题行改为「盈亏总额（货币）下拉」加灰色信息图标与方形分享图标；大数字、收益率与日期改为居中排布，日期移到收益率下方。
- 收益率走势与总资产趋势的页签改为资产分析同款下划线样式；图例改为「跑赢、基准名下拉、跑赢幅度」基准条（多市场基准可选，切换后按所选基准重拉日 K，跑赢幅度为组合与基准区间收益率之差），右侧保留简单加权与时间加权下拉并持久化；图表沿用共享趋势图组件。

#### 资产盈亏分析页整体打磨
- 盈亏总额大数字与顶部周期筛选联动（非「全部」按区间首尾有效收盘价计算，收益率同步）。
- 盈利图数据修复：每日资产序列首日用各持仓首个有效收盘价回填（修复区间收益率异常放大），基准首值同样回填（修复基准线被压成直线）；货币选择器带市场图标；删除标题旁信息图标，右上角改为「分享截图」按钮（截图后预览并复制或保存，复用服务端剪贴板兜底）。
- 跑赢条改为「我的、基准名下拉、跑赢幅度」；取消浮动大卡片，改为普通页面流式布局并左对齐临界宽度 760px；视觉统一接入主题色与涨跌色，字号层级收敛，并补深色分隔线覆盖。
- 时间加权修复：图表点的持仓数量与现金流按订单轨迹链式累乘，与资产分析页口径一致，不再写死 100。

#### 收益日历支持查看当日个股盈亏
- 收益日历的日期格子可点击，弹出「当日盈亏」明细：按当日收盘价与前一日收盘价差额乘数量计算每只股票当日盈亏（跟随日历市场筛选）。
- 样式对齐全部盈亏排行榜（序号、名称、市场色块徽标、代码、盈亏金额），弹窗内分盈利与亏损两大页签（两等分居中，与股票盈亏明细按钮一致），标题副行只显示数量，盈利按金额降序、亏损按亏损额降序，底部显示分组合计；某天全盈利或全亏损时默认打开有数据的那一页。
- 列表展示与排行榜一致（行高与按金额比例的背景色条，盈利红底、亏损绿底，最小 20%）。

#### 接入券商历史订单
- 按用户提供的历史订单导出接入真实成交：解析无共享字符串的 xlsx，只取「已成交」的行（排除已过期、已撤销与已拒绝），按订单号去重，合计 421 笔成交。
- 持仓股导入 250 笔：每只股票按时间顺序重放建仓与加减仓，位置快照与记录数量严格对账，基准数量为当前数量减订单净量，基准成本按二分反推使重放终点等于记录成本（用户调整后的负成本保持不变）。
- 按用户要求保留已清仓股票：新建 26 条记录并导入 167 笔成交（快照链自洽、最终归零）；仅有一笔卖出而买入在导出窗口外的标的按窗口外基准导入、成本置空；净量与零持仓矛盾或期权订单跳过并说明；删除与券商重叠的旧手动订单。
- 全库 38 只有订单记录的快照链逐笔自洽、最终数量对账无误；无头浏览器实测盈亏总额与各页面均正常。

#### 订单系统新增导入功能
- 资产分析-订单系统的「导出」旁新增「导入」按钮，支持上传券商导出的 xlsx（格式与导出一致）：新增零依赖的 xlsx 读取器（解析压缩包中央目录与内联字符串单元格，与现有写入器对称，不引入新依赖）。
- 导入接口仅导入已成交、按订单号去重；代码剥交易所后缀匹配记录，无记录自动新建；按成交时间重放生成位置快照链，重放最终数量必须等于记录持仓否则整组跳过；期权与净量矛盾跳过并说明。
- 幂等去重按内容指纹（记录、时间、方向、数量、价格）而非订单号，避免随机回退单号造成重复导入。
- 前端两步确认：选文件后先做预览（识别笔数、将导入、跳过、重复与每股明细），确认后再写入并刷新订单列表；同时补充导入格式规范文档。实测真实文件重导幂等识别、构造新订单导入正确、重复导入零新增。

#### 订单系统分页与搜索联想
- 订单面板新增分页：每页 9 条，底部翻页条（上一页、下一页与页码窗口，当前页高亮，超出 7 页取居中窗口），显示「第 x / y 页 · 共 n 笔」；tab、市场、类型、状态、时间、排序与搜索变化自动回到第一页。
- 新增搜索框（位于条数左侧），按股票代码、名称与订单号模糊匹配，输入即时联想（从当前筛选结果提取去重，最多 8 条，点击建议即应用过滤），输入即实时过滤、回车应用、Esc 或清除按钮复位，空结果给出提示。实测 417 笔分 47 页、翻页正常，搜索联想与即时过滤均符合预期。

### 修复

#### 资产盈亏分析五项修复
- 整体拖动改为按住窗口任意处即可拖动，按钮与输入框点击不受影响，位置仍自动保存。
- 收益日历的月份右侧新增日期图标按钮；股票盈亏明细每行增加股票图标（素材库图标优先，缺失回退首字母头像）。
- 全部盈亏排行榜序号改用全球预览同款样式（金、银、铜、灰四档，无底色）；首屏卡顿修复 —— 持仓、行情与汇率就绪后立即渲染总额、排行与明细，日 K 在后台加载，趋势图与收益日历单独显示加载态。

#### 资产盈亏分析窗口宽度对齐设置页（960px）
- 按用户要求，资产盈亏分析可拖动窗口的最大宽度由 1480px 调整为与设置页一致的 960px。

#### 资产盈亏分析窗口可拖到视口最左边
- 修复拖动边界：此前位置按内容区相对坐标计算，最左被限制在内容区左缘；改为按视口坐标计算，窗口可一直拖到屏幕最左与最上，右侧与底部仍防拖出屏幕，位置继续自动保存。

#### 资产盈亏分析拖动边界与设置页一致
- 按用户反馈撤销视口最左拖动：窗口左移会盖住左侧导航文字，与设置页行为不一致。
- 恢复与设置页同款的拖动边界 —— 左边界为内容区左缘，右侧与底部限制防拖出屏幕，位置自动保存。

#### 资产盈亏分析页视觉优化
- 去掉顶部与总结卡重复的市场筛选行（市场筛选统一由总结卡片页签控制，日历另有独立市场按钮）；日期范围在历史行情加载期显示「正在汇总历史行情…」而非「暂无数据」。
- 盈亏总结的盈利与亏损对比卡名称允许换行，不再截断；趋势图加载态改为脉冲骨架加提示文字；标题栏拖拽提示加亮并加拖拽手柄图标。

#### 恢复资产盈亏分析顶部市场筛选行
- 按用户要求恢复顶部「全部 / 美股 / 港股 / A股」市场筛选行（与总结卡片页签联动），其余视觉优化保留。

#### 收益日历日期旁只保留一个图标按钮
- 按用户澄清，收益日历日期区域精简为「2026/08」加右侧一个日历图标按钮，移除前后月份切换箭头与下拉箭头。

#### 恢复收益日历日期控件（前后箭头 + 月份 + 图标按钮）
- 按用户要求恢复收益日历日期区域：上个月与下个月箭头、月份按钮加下拉箭头、日期图标按钮。

#### 收益日历：删除日历图标按钮，日期按钮改为「选择日期」
- 按用户要求删除日期旁独立的日历图标按钮；原月份按钮改为「选择日期」并负责打开月份选择器（年份切换加 12 个月网格，顶部显示当前月份）。

#### 收益日历市场与月份持久化
- 日历市场选择（全部 / 美股 / 港股 / A股）与所选月份写入 localStorage，刷新后保持；删除月份选择器中的「当前：2026/08」提示。

#### 排行榜与明细市场色块统一
- 抽出共用市场色块函数：美股蓝、港股紫、A股按代码分红粉（深色字），其余灰。
- 资产盈亏分析的排行榜与股票盈亏明细改用该色块（圆角矩形），与资产分析分享页的持仓列表完全一致，分享页同步改用同一函数。

#### 全部盈亏排行榜序号统一为股票盈亏明细样式
- 按用户要求，排行榜序号由全球预览的金银铜大号数字改为与股票盈亏明细一致的灰色两位补零序号（01、02…）。

#### 全部盈亏总结移到收益日历下方
- 按用户要求调整页面区块顺序：盈亏总额与趋势图整行，然后是排行榜与明细，然后是收益日历，最后是全部盈亏总结（日历下方整行）。

#### 恢复排行榜/总结右上角「更新至」文字
- 按用户要求，排行榜与全部盈亏总结右上角恢复为「更新至 xx.xx」文字，收益日历的「选择日期」保留。

#### 收益日历日期按钮恢复显示当前月份
- 按用户要求，收益日历右侧按钮恢复显示当前月份（如 2026/08），点击仍打开日期选择器；日期选择按钮增加文案参数。

#### 收益日历年视图收益加「万」单位
- 年视图每月收益达到 1 万显示为「x.xx万」，千位用 K 兜底，小额显示原值；月视图保持 K 缩写不变。

#### 资产盈亏分析页字体统一到资产分析体系
- 整体收敛字体层级：窗口标题、卡片标题、盈亏总额大数字与收益率、日期与图例与星期表头、正文与排行与明细与金额、筛选与切换按钮、日历日期与月份格子，全部对齐资产分析页的字号体系。

#### 取消资产盈亏分析页可拖动
- 按用户要求移除资产盈亏分析窗口的拖动能力：删除拖动 hook、位移变换、拖拽手柄提示与抓取光标，窗口保持 960px 圆角外观但位置固定。

#### v16 恢复站点信息下方/网站标题上方的分隔边框
- 按用户要求，v16 风格下设置分区标题与首行之间恢复顶部细分隔线。

#### v16 分隔线改到分区标题正下方（精调细节）
- 按用户反馈继续精调：v16 的分隔线由「首行上边框」改为「分区标题正下方 1px 横线」，与其它版本观感一致。

#### v16 改为包裹式边框（分区整体套圆角卡片）
- 按用户澄清，v16 不是标题下一条线，而是包裹式边框 —— 设置分区整体套在 1px 边框加圆角与卡片底色的容器里，标题与行内容同在一个盒子中。

#### v16 包裹边框只包行内容，标题留在框外
- 按用户反馈修正：v16 的包裹式边框只包住分区行内容，分区标题保留在框外上方；边框、圆角与卡片底色套在标题后面的内容容器上。

#### v16 全部分区按 mockup 框逻辑统一
- 对照设置风格预览页逐条核对：分区标题改为小灰字（11px、0.04em 字距）置于框外上方；行内容统一套 1px 边框加圆角与无底色盒子，行间距 8px；行高 42px、内边距 7px 与 14px，行与行之间保留分隔线。所有设置分区一致生效。

#### 框逻辑应用到全部风格 + V14 改名为极简风
- 按用户要求，将分区「标题在外、行内容带边框圆角盒子」的框逻辑从 v16 提升为全部风格通用（V1、V5、V7、V14、V16 统一生效，V1 保留其卡片式行布局细节）。
- 版本切换器的「V14 桌面客户端」改名为「V14 极简风」。

#### 删除磨砂玻璃风并调整版本下拉
- 从版本切换器移除 V18 磨砂玻璃（含样式全部清理）。
- 风格选择下拉中的版本号改用 V7 同款衬线字体，其余文字不变；Apple 液态玻璃风格下版本下拉背景改为不透明（浅色纯白、深色近黑），避免玻璃透底影响可读。

#### 框逻辑全风格统一
- 根因：站点信息与交易分区外层使用了双列网格类且基础样式为 display:contents，导致承载外框的包裹层不生成盒子（所有风格都无框），V1 还额外变成双列卡片。
- 修复：删除该布局（基础规则、V1 双列覆盖与 JSX 类名），所有风格统一为「标题在外 + 行内容 1px 边框圆角盒子 + 行间分隔线」。

#### 深色模式分区外框加亮并删除液态玻璃
- V1、V5、V17 深色模式下设置分区外框边框分别加亮（#3b4354、#3a3a3a、#3a3a42），不再与深色底融为一体。
- 按用户要求整体删除 Apple 液态玻璃风格（版本项与全部样式：变量、玻璃规则、下拉不透明、Logo、fire 与导航色）。

#### 设置分区外框边框推算
- 继续统一各风格的分区外框：边框色改为按卡片色与文字色推导，保证每个风格与明暗模式下都有清晰对比度并保持主题色调，不再出现浅色近白、深色近黑的看不见边框；保留深色加亮规则作为不支持该色函数的兜底。

#### v1/v5/v17 边框与分隔线显色
- 按用户反馈去掉颜色推导（部分浏览器不支持时会退回近白或近黑细线，导致左边框与标题下方横线不可见），改为 V1、V5、V17 分别指定清晰的边框色与行分隔线色（浅色、深色各一套），保证分区框四边与「网站标题」下方横线在所有浏览器下清晰可见，与 V16 观感统一。

#### v1/v5/v17 侧栏分界线与窗口外框加亮
- 按用户反馈补齐缺失的线条：侧栏与内容区之间的竖线分别加亮（浅色与深色各一套），不再与背景融为一体。
- 窗口整体边框统一：把这三套风格的边框色板整体加深到与分区外框一致，标题栏下沿、状态栏上沿、窗口四边、搜索框与卡片输入框边框随版本整体清晰可见。

#### 手机端横向溢出修复
- 资产分析页整体被撑到 1302px —— 根因是分栏单列网格子项默认为最小内容宽度，持仓表把整页撑开；补上子项最小宽度为 0，宽表改为容器内部横向滚动。
- 资产盈亏分析页同样被撑到 554px，同一修复后 375px 视口不再溢出。
- 手机端右上角头像区域被顶部指数条溢出内容压住 —— 指数胶囊不换行且容器未裁剪，容器补上溢出隐藏。
- 头像下拉面板在移动端不再超出视口：移除导致定位基准偏移的位移变换，移动端面板改为视口固定定位（右缘距视口 12px、位于头部下方、保持 272px 宽度），实测 320 至 414px 均不溢出，桌面端居中不变。

#### 资产盈亏分析页性能优化
- K 线内存缓存（10 分钟 TTL，与资产分析页同款）：持仓与基准日 K 在 10 分钟内不重复请求，页内切换或重进秒出，失败回退上次成功缓存。
- 移除无点击行为的漏斗筛选按钮及其图标组件，移除未使用的明细总计字段；加载骨架由双列改为单列（页面已是单列流式）。

#### 顶部市场筛选改为图标下拉
- 删除顶部「全部 / 美股 / 港股 / A股」文本胶囊行，改为收益日历同款的市场筛选（圆形图标按钮加下拉），并与周期胶囊合并为同一排。
- 修复市场下拉被横向滚动容器裁剪导致看似无功能的问题（按钮移出滚动容器，下拉正常弹出），筛选行与下方卡片间距统一。

#### 盈亏总额周期口径统一
- 按用户确认统一口径：盈亏总额大数字不再区分「全部用浮动盈亏、周期用日资产首末日差」，所有周期（含全部）统一按日资产序列首尾有效收盘价计算区间盈亏与收益率，数据未就绪时兜底浮动盈亏，切换周期数值连贯。
- 「全部盈亏总结」卡的股票累计盈亏仍为浮动盈亏口径。

#### 修复区间盈亏虚亏
- 排查用户反馈「本年盈亏 -41089 与实际出入大」：根因是区间盈亏此前用「当前数量 × 收盘价」回填全年，今年买入的股票在年初就按当前数量计入，抬高年初市值导致区间虚亏。
- 修复：区间盈亏改用订单轨迹的实际持仓市值加现金流修正 —— 区间盈亏 = 末日实际市值 − 首日实际市值 − 区间净流入，区间收益率改用时间加权指标；全部仍为浮动盈亏口径。
- 注意：仅对通过交易创建过订单的持仓生效，无订单历史的持仓无法还原买入日期，仍按当前数量回填。

#### 盈亏总额改回真实累计盈亏
- 排查用户真实数据：本年此前显示 -4.1 万至 -5.8 万，而用实时行情重算相对成本的真实累计盈亏为 +2.8 万，负值是「当前数量乘收盘价回填年初市值」的区间口径虚亏，且该用户几乎无订单历史，区间盈亏不可靠。
- 修复：盈亏总额大数字始终显示浮动盈亏口径（当前市值减投入成本，可靠），周期胶囊只控制图表的区间与日期范围；另修复非交易日（周末）成交现金流被丢失的问题，时间加权更准确。

---

## v0.1.13 · 2026-08-20

### 新功能

#### 资产盈亏分析页界面与入口
- 资产分析-账户资产卡片的「持仓总盈亏」变为可点击入口，悬停高亮并显示箭头，点击进入 /asset-pnl-analysis。
- 页面含市场与周期筛选（全部、美股、港股、A股，近 6 月、本年、近 1 年、全部，以及漏斗筛选）、盈亏总额大数字与收益率、收益率走势与总资产趋势切换（我的与标普 500 图例）、全部盈亏总结（股票累计盈亏加盈利与亏损最大单品对比卡）、全部盈亏排行榜（盈利与亏损 Top5，名次色阶与条形背景）、股票盈亏明细（带市场徽标）与收益日历（月视图红盈绿亏）。
- 本版为演示数据，逻辑与数据对接在后续版本接入。

#### 资产盈亏分析页接入真实数据
- 把静态演示数据替换为真实数据：持仓记录、实时行情与汇率共同计算每只持仓的盈亏（现价减成本乘数量，按汇率换算为美元）、盈亏总额、成本基数与收益率。
- 每只持仓的日 K（有界并发加强缓存）生成每日组合资产序列，得到收益率趋势（相对区间起点）与总资产趋势，并以标普 500 日 K 作为基准对比。
- 收益日历按所选月份渲染每日盈亏（资产日差）与收益率，支持前后月切换与收益、收益率切换；周期筛选（本月、近 1 月、近 6 月、本年、近 1 年、全部）真正截取趋势与日期区间。

### 修复

#### 资产盈亏分析接入应用壳并修复日历数据
- 资产盈亏分析改为应用壳内的隐藏页签（保留顶部头像与左侧导航，未登录跳登录）；账户资产的「持仓总盈亏」点击改为客户端无感切换，返回按钮同样无感回到资产分析。
- 盈亏总额下方图表改用与资产分析同款的共用组件（收益率走势为我的持仓线与标普 500 线加面积渐变，总资产趋势为面积线），资产分析原图同步复用同一组件。
- 修复收益日历的假数据：不同股票日 K 最后日期不一致时，组合资产计算会丢掉缺数据股票的整只市值，产生巨额假盈亏，改为每只持仓沿用最近有效收盘价。
- 市场筛选（全部、美股、港股、A股）全页生效，总额、排行、总结与趋势均按所选市场过滤；基金与新股页签因暂无分类数据改为禁用态。

---

## v0.1.12 · 2026-08-19

### 新功能

#### 搜索框上方新增发光字标
- 按用户分享的视频效果，在设置窗口侧栏搜索框上方制作动态 Fire 字标：字体加粗放大、背景渐变换色流光、呼吸辉光脉动。
- 四套版本各自配色（V14 白底淡紫、V1 白橙红火焰、V7 衬线暖橙、V5 极简灰黑），跟随深浅色主题，并尊重系统的减少动态效果设置。

#### 设置子项重塑
- 按要求参考主流设置页解决「太乱」：去掉每个分组的独立卡片边框与底色，内容区变成一整块面板，分组之间仅靠小号灰色分组标题区分，行与行保留细分隔线；分组标题图标隐藏，标题改为 12px 灰字，重置等动作按钮保留在行尾。
- 图片类设置项（网站图标、Logo、网站背景）默认收起为紧凑行（标签、缩略预览、更换按钮），点击「更换」才展开上传、粘贴直链、清除与打开面板；股票来源接口地址自动识别协议并出现「打开链接」。

#### 设置改为顶部标签布局
- 按用户提供的截图重塑设置窗口布局：去掉左侧栏，子分类改为顶部横向标签（网站设置、股票设置、个人信息、数据库增强、定时任务、接口、关于），激活项文字高亮加强调色下划线。
- 顶部左侧保留发光小写 fire，右侧为搜索图标与保存状态；内容区去掉大面板底色，分组标题与紧凑行直接铺在窗口背景上，行间细分隔线，图片类设置项保持收起与展开；移动端顶部标签横向滚动自适应。

#### 接口文档页改为极简风格
- 按要求把接口文档页重塑为极简风：去掉渐变背景、模糊光斑与大阴影头部卡片，改为面包屑、标题、描述与右侧小统计的轻量头部；内容区细边框、8px 圆角、无重阴影；目录去掉虚线分隔，正文标题去掉装饰条，代码块、引用与表格悬停全部使用中性色体系。

#### 素材库新增 icon 类目
- 素材库新增「icon」页签（位于贵金属之后），把全站当前使用的 25 个导航、操作与设置图标全部收录为可管理素材，首次进入自动播种。
- 每个图标配规范中文展示名，名称与代码均可点击复制；支持行内上传替换（全局生效）、编辑名称与恢复默认，按内置规范顺序展示，深浅色均以灰底圆角瓦片呈现。

#### 导航菜单类别图标可上传
- 设置 → 网站设置 → 导航菜单中每个类别行左侧都显示图标并内置相机浮层，点击即可上传或更换。
- 图片写入素材库 icon 类目（与素材库同一份数据，全局生效），主侧栏与移动端标签同步显示自定义图标，无自定义时回退各类别默认图标（默认图标抽取到共享模块，与侧栏共用同一套）；上传接口的文件夹白名单与文件清理归类同步补齐。

#### 交易设置新增接口额度显示
- 设置 → 交易新增「OpenAPI 额度」行：通过富途 OpenD 的实时订阅额度与历史 K 线额度接口展示已用、总额与剩余，OpenD 在线时自动查询，也可点击「查询额度」手动刷新；桥接脚本新增额度命令与管理员接口。

### 修复

#### 设置风格对齐预览稿
- 按用户反馈让四个版本贴近交付的预览稿：V14 深色配色对齐（窗口、卡片、输入与边框色），底部补上与预览稿一致的命令条（加号、搜索、自动保存，仅 V14 显示），侧栏补「最近编辑」分组。
- V1 的站点信息与交易（主机与端口）改为预览稿里的卡片式双列字段（标签在上、输入在下），侧栏隐藏图标，强调色与按钮使用橙色；V5 的分组卡片去掉重复阴影、改用细圆角并调整页头字号；V7 的页头与分组标题改用衬线字体。五套风格（含深浅色）继续跟随右上角主题。

#### 侧栏配色与 Logo 对齐
- V7 侧栏由深暖色修正为预览稿的浅暖米色，激活导航项用珊瑚色，页头字号放大为 22px 衬线。
- V1 富途橙恢复侧栏导航图标（此前误隐藏），激活态为橙色；四个版本侧栏顶部统一补 Logo（V1 橙色渐变圆角、V7 珊瑚圆形带光晕、V5 黑色方块、V14 淡紫方块），全部跟随版本配色与深浅色。

#### V1 浅色侧栏搜索按钮去白底
- V1 浅色模式下侧栏「搜索设置」按钮此前误用浅色输入框变量而呈现白底，在深蓝侧栏上突兀；改为贴合预览稿的深色按钮（深底加深边框加浅灰字，悬停转白），内部快捷键徽标同步深色。

#### V1 侧栏悬停文字可见性修复
- V1 浅色模式下侧栏为深蓝底，但悬停、底部与 Logo 名称仍使用浅色主题的深色文字，深字压深底看不见；修复为 V1 侧栏上的文字统一浅色 —— 导航项悬停变白、分组标题浅灰、底部管理员名字浅白、Logo 名称白色。

#### 搜索框上方只保留小写 fire 发光字标
- 按用户要求精简：侧栏搜索框上方只保留小写「fire」发光流光字标，去掉 Logo 圆标与「设置中心」标签，V5 的「Fire 设置」后缀一并移除。

#### fire 字标居中 + 标题栏去掉「Fire 设置」文字
- 侧栏搜索框上方的小写 fire 发光字标改为水平居中。
- 设置窗口标题栏删除「Fire 设置」文字，只保留版本切换图标与版本号。

#### V1 深色右侧内容区去蓝调
- V1 深色模式下右侧内容区此前偏蓝，改为中性深灰（内容、卡片与边框色同步调整），文字提亮保证对比度；保留深蓝侧栏与橙色强调的 V1 特征。

#### 设置页版本切换图标美化
- 设置窗口右上角版本切换图标由「时钟加圆点」改为更精致的「三层叠加图层」图标，并使用各版本强调色显示，悬停回到主题文字色。

#### 设置侧栏导航图标整体美化
- 设置窗口左侧导航图标换新：统一 1.8 线宽与圆头圆角风格 —— 网站设置用调节滑杆、股票设置用蜡烛图、个人信息用用户、数据库增强用柱体、定时任务用时钟、接口用代码括号、关于用信息圆；搜索命令面板结果项同步使用同一套图标。

#### 设置侧栏图标升级为系统质感
- 继续打磨精致度：导航图标线宽降为 1.5、几何比例重新校准，并放入 26px 圆角底色块呈现系统设置质感；激活项底块用版本强调色实心加反色图标，悬停时图标提亮。

#### 恢复侧栏图标为原版
- 按用户反馈撤销两轮图标改动：移除自定义导航图标与圆角底块样式，设置侧栏导航与搜索命令面板恢复使用原有图标，回到改动前的观感。

#### 修复设置图标重复并写入规范
- 根因：图标组件对未注册的 key（关于）会静默回退成网站设置的地球图标，导致两个条目重复；修复：为关于新增专属图标，两个条目不再相同。
- 并在 AGENTS.md 的交互与适配约定中新增图标唯一性规范 —— 新增导航或设置项必须先注册专属图标，禁止依赖回退。

#### 去掉设置侧栏与内容区之间的边框
- 按用户截图标注去掉设置窗口侧栏右侧的竖边框，侧栏与内容区仅靠底色区分，整体少一层边框。

#### 去掉标题栏下方与状态栏上方的横线
- 按用户确认的位置去掉设置窗口的两条横线：标题栏底部边框与状态栏顶部边框，窗口更轻盈；此前误删的侧栏右边框已恢复。

#### V14 底部命令条改为与其它版本统一的状态栏
- V14 原本用命令条作底部，与其它版本的状态栏不一致；改为所有版本统一显示常规状态栏（富途连接状态、行情源、修改自动保存），删除命令条相关样式。

#### 恢复标题栏下方与状态栏上方的横线
- 按用户要求恢复设置窗口标题栏底部边框与状态栏顶部边框。

#### 去掉内容区大外框边框
- 按用户指认的位置去掉内容头部下方包着全部设置分组的大框的边框与阴影：各设置子页内容容器只保留底色与内边距，内容与窗口融为一体，只保留内部分组卡片的边框。

#### 恢复设置窗口左侧栏布局
- 按用户要求撤销顶部标签方案，恢复上一版布局：左侧栏（发光字标、搜索设置、分组导航、最近编辑、底部用户头像与在线点）加内容头部与分组行式内容；删除顶部标签与搜索图标相关样式，分组行与媒体字段的收起展开优化保留。

#### 删除我的持仓前的图标
- 设置页「导航菜单」编辑器行内首字图标对持仓页隐藏（保留占位列避免布局错位）；主界面侧边导航与移动端标签的持仓图标同步隐藏，其它页签图标保留。

#### 导航菜单编辑器去掉全部行首字母图标
- 按用户反馈把设置页「导航菜单」编辑器每行的首字图标整格移除：删除字母头像元素，网格由四列调整为三列（拖动手柄、默认圆点、名称与地址），整行对齐不变。

#### API 文档第 6 章子节编号 6.1-6.14
- 按用户要求给接口文档第 6 章「路由清单」下的 7 个子节补编号（认证、资产持仓、行情数据、名人持仓、素材库上传、券商、设置编号为 6.1 至 6.7），原数据模型章节顺延为 6.8 至 6.14，页面实时读取已生效。

#### API 文档左侧目录只显示一级分类
- 按用户要求隐藏目录中的子节：左侧目录只渲染一级章节，不再展示与展开子节，去掉展开箭头与子节列表；子节仍保留在正文中，滚动高亮按一级章节匹配。

#### API 文档目录：6.8-6.14 全部收进「6」作子项
- 按用户要求把 6.8 至 6.14（券商、个股详情、公司简况、分时、K 线、分组、订单）全部收进「6. 路由清单」作为子项，与 6.1 至 6.7 同级；目录只显示到两级编号，未编号的示例细节不进目录。

#### 补全接口文档并加方法徽标
- 核对全量 v1 路由后发现 4 个未入文档的接口：财报列表与上传、财报删除、指数月 K、订单导出，已补进路由清单表格并新增三节说明。
- 页面优化：路由表首列的请求方法渲染为彩色徽标（查询绿、新增蓝、修改黄、删除红、补丁紫，深浅色适配）。

#### 素材库页签排序：加密货币 / 贵金属移到股票图标之后
- 按用户要求调整素材库顶部页签顺序：股票图标、加密货币、贵金属、市场图标、券商图标、分组图标、icon，便于常用类目优先展示。

#### 素材库标题去图标 + 删除描述文字
- 按用户要求移除素材库页标题左侧的标题图标，并删除下方描述文字；设置页头组件增加隐藏图标的开关，其它设置页不受影响。

#### 网站形象预览与相机交互调整
- 上传相机改为图片内悬停浮层（整块预览覆盖半透明黑底与居中相机图标，与素材库相机交互一致），不再是贴右下角的角标。
- 首页 Logo 预览由横向长条改为与网站图标一致的方块尺寸，并补齐网站图标预览边框，两块视觉大小统一。

#### 素材库 icon：操作列去掉相机按钮 + 删除图标放大
- 素材库 icon 类目每行右侧操作列不再显示相机上传按钮（上传统一走左侧预览图悬停相机），删除与恢复默认图标放大到 16px，按钮更清晰。

#### 网站形象预览悬停样式调整
- 网站图标、首页 Logo 与网站背景预览悬停时不再盖半透明黑灰罩，改为跟随设置窗口版本主题色描一圈细边，并在图片中央浮现小白底相机图标，深浅色与四套主题均适配。

#### 网站形象预览相机按图片比例缩放
- 预览图较小时（如 16×16 图标）悬停相机圆片显得过大；改为相机圆片高度约为预览图高度的 40%、相机图标再按圆片 58% 缩放，小图小相机、大图大相机。

#### 网站形象预览相机深色模式适配
- 修复深色模式下相机圆片过亮、浅色主题相机看不清的问题：深色模式相机圆片改为深灰加白描边，相机图标继续用当前版本强调色，深浅色与四套主题均清晰。

#### 恢复网站形象预览相机为最初角标样式
- 按用户反馈撤销悬停浮层系列改动：网站图标、首页 Logo 与网站背景预览恢复为最初的右下角相机小圆标（主题色底、白色相机、卡片色描边，悬停无灰罩与浮层）。

#### 网站形象预览恢复悬停浮层
- 按用户澄清，恢复为提出「鼠标划过背景颜色太灰」之前的状态 —— 预览悬停时整块盖半透明黑罩并在图片中央显示白色相机图标，不再使用角标样式。

#### 网站形象预览悬停黑罩加深
- 悬停半透明黑罩由 35% 加大到 50%，呈纯黑压暗而非发灰；网站图标空预览的内层灰底改为透明，未上传时不再显示灰色方块，直接透出瓦片底色。

#### 网站形象预览悬停去黑罩（仅主题色相机图标）
- 按用户要求移除悬停黑罩：预览悬停时不再压黑背景，仅在图片中央浮现主题色相机图标（跟随当前版本强调色，带轻微投影保证在浅色图上可见）。

#### 恢复网站形象预览 50% 黑罩 + 白色相机
- 按用户要求撤销去黑罩改动：预览悬停恢复为整块 50% 黑罩加中央白色相机图标，空预览透明底保留。

#### 恢复至「太灰」提示之前：35% 黑罩 + 灰占位底
- 按用户再次澄清，恢复为提出「鼠标划过背景颜色太灰」之前的状态 —— 悬停黑罩改回 35%，空预览占位底改回灰底，白色相机居中不变。

#### OpenAPI 额度改为仅手动查询
- 按用户要求去掉打开设置页与刷新网页时的自动额度查询，额度只在点击「查询额度」时拉取，避免每次刷新都请求一次 OpenD。

#### 全球预览去重
- 排查发现重复根因：上游资产页本身已包含贵金属（英文名），后端又追加素材库贵金属（中文名）合并，导致每种金属出现两次。
- 修复：合并后做硬性去重 —— 贵金属与加密货币按类型加代码、股票按市场加代码去重，素材库版本优先，再按市值排序取前 100。实测贵金属仅剩 4 条，全榜无重复。

#### 全球预览资产列美化
- 全球预览「资产」列由居中对齐改为左对齐，图标固定在列首，名称与代码紧随其后；名称块设置最大宽度并在超出时显示省略号，长名称不再撑乱列宽。

#### 全球预览排名样式统一
- 按要求把全球预览的排名序号由圆形小徽标改为分享页持仓列表同款效果：去掉圆底，前三名分别用金、银、铜色大号数字，其余为灰色小号数字，跨页时按全局排名取色。

#### 修复美股盘后当日盈亏重置
- 排查「美东未到 20 点当日盈亏被重置」：富途快照的盘后涨跌字段实测为「盘后价减常规收盘价」（接近 0），并非「盘后价减昨收」，脚本直接使用导致盘后 16:00 至 20:00 当日盈亏接近 0。
- 修复：盘后统一按盘后价减昨收计算涨跌（昨收缺失才回退官方字段）；并按「24 小时行情加美东 20:00 结算」口径，夜盘时段价格继续显示夜盘价，但涨跌锁定为盘后最终价减昨收，夜盘波动不再改动当天盈亏。

#### 美股股价显示 3 位小数
- 按用户要求全站统一：美股股价保留 3 位小数，其它市场 2 位。覆盖面包括我的持仓（现价、成本价、详情）、自选股行情、股票搜索联想、资产分析持仓表、个股详情（现价、最高、最低、今开、昨收、52 周高低、均价、相关 ETF）、交易订单、素材库股票列表、名人持仓持股价格、全球预览美元价格、当日盈亏分享图持仓列表与 K 线 Y 轴刻度。

#### 修复当日盈亏刷新闪变
- 排查「美股当日盈亏刷新显示 -81，多刷几次恢复 -900 以上」：净资产与持仓市值一致而涨跌不同，根因是行情快照来源不一致 —— 旧版缓存含盘后涨跌接近 0 的错误快照，页面先读缓存显示小数值；富途桥接进程并发连接 OpenD 会互相阻塞超时，实时请求偶尔失败时旧缓存继续生效。
- 修复：富途桥接调用全局串行化（避免并发连接超时后回退腾讯）；行情缓存换版本号并缩短有效期为 30 分钟（作废旧错误快照，跨天不再闪旧盈亏）；本次请求了但未返回的标的不再用旧缓存补值，改为显示同步中，杜绝用过期涨跌拼出假盈亏。实测连续 5 次请求美股涨跌完全一致。

#### 修复股票图标二次上传被重置
- 排查「素材库某股票上传图标后又被重置」：数据库地址仍指向旧文件，而磁盘上已有新上传的 SVG，时间戳也吻合 —— 说明文件写入与数据库写入都发生了，但地址没有更新。
- 根因：素材写入对「手动上传」的行保护了地址与名称（防止自动同步覆盖），但把用户自己的再次上传也挡住了。
- 修复：写入逻辑改为自动同步仍保护手动行，但手动写入允许覆盖地址与名称；素材库股票上传同时显式带上记录 id，已恢复为最新上传的图标。

#### 美股夜盘当日盈亏持续更新
- 排查「有夜盘价格了但当日盈亏还是未更新」：根因是夜盘时段被当作闭市，轮询跳过美股，且盈亏口径沿用旧一天的整天涨跌。
- 按券商口径对齐：美东 20:00 为交易日分界，旧一天结算归档，夜盘属于新一天，当日盈亏等于夜盘波动（夜盘价减今日常规收盘，与券商一致）。
- 修复：市场时段新增夜盘活跃区间，轮询继续、夜盘价格 24 小时实时更新；夜盘涨跌改用富途夜盘涨跌字段；美股仅周末标记已结算；行情缓存升级版本，作废旧一天整日盈亏的旧快照。

#### 个股详情页状态区分盘前/盘中/盘后/夜盘
- 按用户要求，个股详情页市场状态由「已收盘」改为按时段区分：盘前、盘中、盘后、夜盘（美东 20:00 至次日 04:00）与休市（周末）；夜盘为活跃时段，详情页主行情在夜盘也每 30 秒自动刷新。

#### 行情源按市场拆分
- 按用户要求把行情源拆开：美股单独走富途（保证盘前、盘后与夜盘口径以及 24 小时行情），港股与 A股始终直接走腾讯；「仅富途」模式下美股不回退腾讯，但港股与 A股仍走腾讯。
- 这修复了此前美股与港股 A股混批导致富途桥接超时、整体回退腾讯变成常规收盘口径（当日盈亏回到错误量级）的问题。实测 24 只持仓请求中，美股全部为夜盘口径，港股与 A股为常规口径，不再整体回退。

#### OpenAPI 额度查询保留上一次结果
- 按用户要求，额度查询中、失败或刷新页面时保留上一次成功查询的结果：成功结果写入 localStorage，页面加载直接恢复显示并标注查询时间；查询中不闪空，失败不清空。

---

## v0.1.11 · 2026-08-18

### 新功能

#### 品牌字标换用同款字体
- 按参考把左上角品牌字标由系统粗体换成与参考站左上角相同的圆角等宽字体（700 字重，19px 下比常规字重更有存在感）：字体文件本地化到 public/fonts（常规字重同时保留），样式表新增字体声明，Tailwind 增加对应字体族。
- 应用范围：后台与首页左上角 LOGO 文字、登录页桌面与移动端品牌文字；深浅色共用同一字体，颜色仍跟随主题文字色。

#### Logo 字体可切换并支持一键重置
- 设置 → 网站形象 → Logo 字体新增三个选项：同款粗体（默认）、同款常规、系统粗体，选择后立即预览，保存后全站左上角 LOGO 同步生效；新增字体设置项（类型、默认值、保存与公开接口全链路）。
- 网站形象区块新增「重置」按钮（二次确认），一键把网站图标、Logo 图片、Logo 文字、Logo 字体与网站背景恢复默认并保存。

#### 行情源接入富途 OpenAPI
- 按用户要求评估并接入富途接口：安装 OpenD 与 Python SDK，新增桥接脚本（市场快照与搜索报价两个命令），Node 侧新增封装（端口预检、可用性缓存与美股盘前盘后夜盘时段提示）。
- 行情与搜索全局改为富途优先：快照直接覆盖腾讯与 Yahoo（含盘前、盘后、夜盘三档涨跌），搜索走富途报价；OpenD 未安装、未登录或失败时自动回退腾讯加 Yahoo 加东财，不影响现有功能。健康检查新增行情源与 OpenD 状态，登录后即为全局主行情源。

#### 行情源支持手动切换
- 按用户要求把备用切换做成可配置：设置 → 股票来源接口 → 行情源新增三个选项 —— 自动（富途优先，失败回退腾讯与 Yahoo，默认）、仅富途、腾讯与 Yahoo；新增配置项（类型、默认值、保存白名单校验与加载归一化），行情与搜索都按该配置决定是否启用富途，强制富途时不再回退备用源。
- 股票来源接口区块顶部显示 OpenD 连接状态（已连接走富途、未连接走备用源），健康检查返回配置源与实际生效源两个字段。

#### 富途连接配置进入系统设置
- 为线上部署做准备：OpenD 网关地址不再写死本机，新增主机与端口设置项（默认本机与默认端口，可指向远程 OpenD，含类型、默认值、保存校验与加载归一化）。
- 设置 → 我的持仓新增「交易」区块，内含两块独立卡片 —— 富途连接配置（主机、端口、测试连接按钮，真实拉一次快照验证可达与登录）与行情源切换（从股票来源接口移入）；新增管理员连接测试接口并支持临时主机端口，健康检查按已保存配置检测。

#### 设置页重构为命令搜索与自动保存
- 按用户要求重构臃肿的设置页：删除大尺寸头部卡片、排版切换与各模块零散保存按钮，改为紧凑顶栏（标题、全局保存状态胶囊与版本徽标）。
- 新增命令搜索 —— 输入关键词即时匹配 12 个设置项，方向键加回车或点击跳转到对应模块并高亮定位；引入全局自动保存，站点信息、网站形象、首页指数、首页导航、股票来源接口与交易等 22 个字段修改即保存（700ms 防抖），顶栏实时显示保存中、已保存时间与保存失败可重试。
- 左侧导航改为极简图标栏（激活态高亮），移动端保留横向胶囊；券商管理因含清空二次确认保留独立保存按钮，数据库配置保留显式保存，其余全部自动保存。

#### 设置改为独立桌面窗口
- 按用户选定的桌面客户端布局落地：设置从页面页签改为独立弹窗窗口（960×720 居中，窗口标题栏含红绿灯、标题与版本，底部状态栏显示 OpenD、行情源与自动保存），从任意页面点右上角齿轮或导航「设置」都能打开，Esc、红点或点击遮罩关闭。
- 深色与浅色随主题切换自动变化；窗口内为紧凑桌面风格 —— 184px 侧栏（搜索设置按钮、分组导航、底部管理员）加主区行式设置项，命令搜索保留在内容区顶部；导航「设置」页签点击直接开窗，不再切换页面内容。

#### 设置入口改为左下角常驻入口
- 按要求彻底桌面客户端化：移除侧边导航与移动端导航里的「设置」页签（默认页签数组、导航菜单编辑与服务端加载归一化全部过滤，历史数据库残留的设置页签也不再显示）。
- 新增左下角常驻头像入口 —— 圆形头像（真实头像或首字兜底，右下角带小齿轮角标），悬浮于页面左下角，点击打开设置窗口，任意页签可用；右上角头像菜单里的设置与个人信息也统一改为打开设置窗口（个人信息定位到对应子页）；深链 /settings 直接开窗并落在默认页签。

#### 导航页签可拖动排序
- 桌面侧边导航页签支持鼠标拖动排序（拖到目标位置松手即重排），顺序变化自动保存并全局生效（刷新与换设备保持）。
- 默认顺序调整 —— 「设置」页签排在导航最左边（默认页签数组与服务端缺省补齐逻辑均调整，当前数据库已迁移）。

#### 四个管理页套用客户端窗口风格
- 抽出通用客户端窗口组件（960px 窗口框架、标题栏、标题与徽标、可选底部状态栏、可选拖动），设置窗口改为基于它实现。
- 日志、附件管理、用户管理与名人持仓四个视图在应用壳内全部用该组件包裹，居中显示，与设置页同款边框、圆角、阴影与进入动画，保持桌面客户端统一观感（权限守卫逻辑不变）。

#### 设置窗口新增版本切换
- 在设置窗口标题栏右上角新增版本切换入口，点击弹出菜单含 V1 富途橙、V5 Notion、V7 Claude 与 V14 桌面客户端四个版本（每项带三段色卡预览与当前项标记）。
- 选中后设置窗口整体换肤 —— 窗口壳、侧栏、状态栏、卡片、输入框、文字、强调色与标题字体改为变量驱动，四套版本各自定义完整配色，选择持久化，默认 V14。

### 修复

#### 修复夜盘结束后当日盈亏显示昨日
- 美东夜盘结束后进入盘前时，当日盈亏仍停留在上一交易日，原因有两处：盘前与盘后的基准价错误 —— 应用优先采用腾讯昨收，而盘前刚开始时腾讯仍返回更早的收盘；盘前尚无成交时 Yahoo 没有当日盘前点，扩展行情整批失败后退回腾讯昨收，当日盈亏直接显示昨日。
- 修复：扩展时段统一以 Yahoo 的最近常规收盘为基准；盘前暂无成交时返回「基准价加当日盈亏归零」，不再沿用昨日。实测 AAPL 昨收与涨跌修正正确，无盘前成交的标的显示 0。

#### 修复富途连接测试失败
- OpenD 登录后设置页测试连接仍失败，排查到两个原因：本机 OpenD 监听的地址不是回环地址，默认主机配置连不上（已把设置项更新为实际监听地址，并在说明中提示局域网地址场景）；富途 SDK 默认把日志打到标准输出，混入桥接脚本的 JSON 导致解析失败（桥接脚本静默日志，Node 侧解析改为只取首个左花括号到最后一个右花括号的片段）。
- 修复后实测测试连接约 0.7 秒返回成功，行情全局切换到富途，健康检查显示实际生效源为富途。

#### 设置窗口细节收尾
- 设置窗口原有侧栏搜索按钮与内容区搜索框两个入口，删掉内容区第二个，只保留左上角「搜索设置」，点击展开下拉搜索面板（快捷键同步可用）。
- 页面左下角常驻入口由头像改为设置齿轮图标（圆形按钮加齿轮，悬停放大）；设置窗口左下角侧栏底部的用户头像改为显示真实头像（无则首字兜底），并在头像右下角加绿色在线状态点（深浅色边框适配）。

#### 设置改为页面内平滑过渡
- 按用户要求去掉弹窗形态：删除弹窗壳与齿轮弹窗入口，设置恢复为页面内页签内容（桌面客户端式紧凑侧栏与行式设置项保留），点左下角齿轮或头像菜单的设置与个人信息时在当前页面内无感切换并带平滑淡入过渡，不弹窗、不跳转、不刷新；深链 /settings 直接落在设置页签。

#### 设置页恢复边框卡片
- 设置页内容区加客户端底色，让卡片与页面区分开。
- 设置分组卡片保持原有尺寸、内边距与圆角不变（撤销此前的紧凑内边距、输入框与按钮缩小等尺寸改动），仅加强可见性 —— 浅色白底加细边框与轻阴影，深色深底加边框与阴影；外层容器原样保留。

#### 设置窗口壳改为页面内嵌
- 按用户纠正修正为 —— 只移除弹窗的遮罩、悬浮与居中定位，桌面客户端窗口壳原样保留并直接嵌进页面内容区：固定 960px 宽居中、720px 高（小屏自适应）、窗口标题栏（红绿灯、标题与版本）、紧凑侧栏、行式设置卡片与底部状态栏全部不变；进入设置带平滑过渡，宽高与弹窗版一致。

#### 设置加回导航页签
- 「设置」页签重新加回桌面侧边导航与移动端导航（默认页签、导航菜单编辑与服务端加载归一化均恢复，历史数据缺设置页签时自动补上），点击后仍以 960px 客户端窗口壳在页面内平滑呈现。
- 移除左下角常驻齿轮设置图标，设置入口统一走导航「设置」页签与右上角头像菜单。

#### 设置窗口壳可拖动并默认靠左
- 设置窗口壳支持按住标题栏拖动（实时跟随、上下左右不越界），位置自动保存到 localStorage，默认位置为最左边。
- 撤销上一条「设置页签默认最左」：默认页签顺序与缺省补齐逻辑恢复为设置页签在末尾，当前数据库导航顺序已还原，导航页签拖动排序与自动保存功能保留。

#### 修复设置窗口拖动失效
- 排查并修复：拖动改为原生鼠标事件（弃用指针捕获），拖动更可靠，位置用引用实时镜像，松手时保存最新位置；位置恢复从初始化改为挂载后读取，避免水合不一致导致交互失效；本地位置非法或越界时自动回到左上角。
- 存储键升级并清除旧版残留位置 —— 更新后首次打开设置窗口必然落在最左边，之后拖动的位置才被记住。

#### 恢复四个管理页为独立页面
- 按用户要求恢复到指定时点：移除通用客户端窗口组件与四个管理页的窗口壳（日志、附件管理、用户管理、名人持仓恢复为原有独立页面）。
- 设置窗口恢复为独立的 960px 页面内嵌窗口壳（标题栏、标题与版本、底部状态栏），按住标题栏可拖动、默认靠左、位置自动保存（保留可靠版拖动实现）；导航页签拖动排序与自动保存、设置页签位于末尾、左下角无齿轮图标等均保持恢复点状态。

#### 用户管理排序与加载优化
- 排序由创建时间升序改为按 UID 数字升序，测试账号（无 UID）固定排在最后。
- 加载优化：进入用户管理不再整页显示加载中 —— 新增模块级短缓存（切页秒开，先渲染表壳再后台刷新），首次加载也在卡片内显示小型加载行；编辑、重置密码、删除与手动刷新后强制重新拉取。

#### 版本切换适配浅色 / 深色主题
- 修复版本换肤与主题切换不联动的问题：四个版本各自定义完整的浅色与深色两套变量 —— 浅色主题下使用各版本浅色系，切到深色主题后自动切换到对应深色系；窗口壳、卡片、输入框、文字、边框、强调色与下拉菜单全部跟随变量联动。

---

## v0.1.10 · 2026-08-16

### 新功能

#### 首页自选股/我的持仓新增排序
- 为首页前端的自选股与我的持仓表格增加列排序：股票名称、现价与涨跌幅可排序，我的持仓额外支持持仓盈亏排序；点击表头切换升序与降序，第三次点击恢复默认顺序。

#### 分享卡片支持缩放与市值隐藏
- 每日盈亏分享卡片右下角新增拖拽缩放手柄（宽度与高度各有范围），复制与保存按当前缩放后的尺寸导出。
- 三块顶部的持仓市值增加眼睛按钮，可隐藏或显示金额；持仓列表在深色模式下的白色边框线弱化。

#### 分享模板缩略图改为六宫格
- 每日盈亏分享右侧缩略图由 5 个改为固定 6 个，尺寸缩小并适配左侧卡片；第 6 个为相机上传入口，可上传自定义模板并选中用于分享。

#### 分享缩略图支持逐张相机上传
- 每日盈亏分享右侧六个缩略图每张都增加与素材库股票图标同款的相机悬停遮罩，可分别上传自定义图片并自动选中该模板。

#### 每日盈亏分享卡片缩放自动保存
- 每日盈亏分享弹窗拖拽缩放后的卡片尺寸自动保存到本地存储，下次打开分享弹窗时恢复上次缩放结果，无需手动确认提示。

#### 分享持仓列表改为四列表格
- 每日盈亏分享的持仓列表由图标加名称加当日盈亏升级为带排名的四列表格：股票图标左侧新增排名序号，右侧新增成本价、现价与盈亏额列，名称列保留股票图标与代码；数据项增加成本与现价字段并复用货币换算。

### 修复

#### 持仓盈亏排行改用股票图标
- 资产分析-持仓盈亏排行不再显示市场国旗，统一复用持仓表的股票图标解析与 ETF 正股图标映射；图标缺失时使用股票名称首字兜底。

#### 当日盈亏分享图复制后保留圆角透明背景
- 修复资产分析-账户资产的分享当日盈亏图复制出来与弹窗预览不一致的问题：导出时不再强制填充白底，改为透明背景，让卡片自身的圆角与深浅色背景在复制出的图片中保持一致。

#### 每日盈亏分享弹窗预加载头像与模板图
- 修复点开每日盈亏分享弹窗时短暂显示默认头像与名称、右侧模板图空白的问题：当前登录用户信息从外层一路传入分享弹窗，首帧即显示真实昵称与头像；同时新增模板图预加载（资产分析页挂载与点击分享时按盈亏方向预加载）。

#### 分享弹窗内复制成功提示层级修复
- 修复每日盈亏分享弹窗内点「复制图片」后成功提示被弹窗遮挡的问题：全局提示层级提高，高于分享弹窗，复制成功与失败提示现在显示在弹窗上方。

#### 复制图片反馈提速并统一提示文案
- 排查每日盈亏分享复制图片成功提示晚出约 1 秒的问题：图片导出库改为弹窗挂载时即预加载，复制点击时先提示「正在生成图片…」，完成后再提示「已复制图片」，避免等待期间用户以为无响应。

#### 资产分析页签地址改为 /asset-analysis
- 资产分析从我的持仓剥离后一直沿用 /assets，与素材库接口语义混淆；现将默认导航地址改为 /asset-analysis，并对已保存到站点设置里的旧地址做运行时兼容迁移，旧地址访问自动回到新地址。

#### 资产分析页刷新加载优化
- 资产分析页刷新时原为客户端动态懒加载，首屏先显示「加载中…」数秒；改为服务端直出，首帧直接渲染资产分析内容。

#### 资产分析趋势图本地缓存无感刷新
- 资产分析刷新时趋势图与持仓盈亏排行会重新请求历史 K 线，短暂显示「正在汇总真实历史行情…」。
- 现新增本地趋势缓存：刷新时若持仓、货币与基准一致，先用上次成功的趋势与收盘数据立即渲染，再后台刷新更新，避免可见的加载态。

#### 项目与网站品牌统一为 Fire
- 按用户要求暂时将项目与网站名称统一为 Fire，清理对外展示中的旧品牌名称：包名、默认站点标题与 Logo、首页与后台页脚、关于与版本弹窗、桑基图分享品牌、健康检查服务名、财务与名人数据源标识等均改为 Fire；内部旧缓存键、事件名与数据库文件名暂不迁移，避免破坏现有数据。

#### 内部标识与数据库文件名迁移
- 在品牌统一为 Fire 的基础上继续迁移内部标识：本地存储前缀、全局事件名、会话与主题 Cookie 名、备份文件名、脚本与数据库文件名均改为 fire。
- 旧本地存储与 Cookie 做一次性兼容迁移（旧键自动复制到新键后清理，旧会话 Cookie 继续兼容读取），数据库旧文件备份后迁移为新名称。

#### 修复登录页与后台反复跳转
- 内部会话 Cookie 迁移后，后台布局仍只读取新会话 Cookie，导致持有旧会话 Cookie 的浏览器在登录页与后台之间反复跳转刷新。
- 修复：后台布局、改密与服务端登出等直接读取会话的位置全部兼容旧会话 Cookie，登录与注册页不再循环刷新。

#### 修复迁移后图标与全球预览缺失
- 排查两处：素材图标本地缓存若为空数组会阻止重新请求素材接口，导致自选股与持仓的股票图标消失，改为空缓存不生效并强制拉取最新素材。
- 全球预览只返回公司类标的，缺少加密货币与贵金属；改为在榜单接口合并素材库的加密货币与贵金属并按市值排序。

#### Fire 迁移连锁问题收尾审计
- 继续排查迁移连锁问题：登录、注册与登出时同步清理旧会话 Cookie；历史备份目录改用新前缀；测试数据文件中的旧邮箱与旧名称清理；全项目源码、脚本、文档与数据配置中已无旧品牌标识。

#### 删除当日盈亏结算提示文字
- 按用户要求删除我的持仓-账户资产-当日盈亏卡片下方「已结算 · 日期 · 各市场当地结算时点」提示文字，保留当日盈亏金额展示。

#### 修复导航选中背景错位
- 修复我的持仓、素材库等导航项点击后白色选中背景偏移到下一项的问题：去掉按索引位移的绝对定位背景，改为每个按钮在激活态显示自身背景，避免动态导航顺序与数量变化导致错位。

#### 我的持仓删除按市场汇总提示文字
- 按用户要求删除我的持仓下「按市场汇总持仓盈亏，币种按各自市场」提示文字，保留持仓盈亏汇总内容。

#### 资产分析右侧账户资产净资产标注货币
- 资产分析右侧账户资产卡片中「净资产」标签增加当前货币后缀，并跟随货币切换联动。

#### 账户资产市场筛选按市场币种显示
- 资产分析右侧账户资产卡片进一步优化：筛选「全部」时仍跟随全局货币切换；筛选美股、港股、A股等具体市场时，净资产标签与金额使用该市场默认货币（按市场映射）。

#### 港币账户资产金额缩写防拥挤
- 资产分析右侧账户资产卡片在港币与人民币显示大额金额时改用万、亿缩写，避免多列布局中长数字挤压；美元保持原有精确显示。

#### 美元账户资产金额同样使用万/亿缩写
- 按用户要求，资产分析右侧账户资产卡片的大额美元金额也统一使用万、亿缩写，与港币、人民币保持一致的紧凑展示。

#### 修复美股收盘后盘后涨跌回退错误
- 美股周末或收盘后若直接回退到腾讯常规盘行情，会把盘后上涨的股票显示成常规盘下跌。
- 修复：闭市与周末也读取最近一个扩展时段报价，优先展示盘前或盘后的最新价与涨跌，避免与盘后真实方向相反。

#### 美股盘后昨收优先采用腾讯口径
- 进一步对齐券商盘后盈亏：美股扩展时段覆盖时，昨收价优先使用腾讯行情的昨收，再回退其他来源，修复因两家昨收差异造成的当日盈亏偏差。

#### 每日盈亏分享三页卡片统一缩小
- 资产分析每日盈亏分享弹窗中「盈亏金额 / 持仓列表 / 持仓占比」三页此前高度不一致且整体偏大，现统一为同一高度，并压缩头部、金额、列表与环形图的间距和字号。

#### 每日盈亏分享卡片进一步精简化
- 继续缩小每日盈亏分享卡片：宽度、高度、右侧模板缩略图尺寸与圆角、间距、头像字号同步收紧，整体更精致。

#### 每日盈亏分享卡片删除顶部持仓市值行
- 按用户要求删除每日盈亏分享三页共用的顶部持仓市值行，卡片头部只保留头像、昵称与时间。

#### 每日盈亏分享图片加载骨架优化
- 进一步优化分享页刷新后图片加载慢与空白的问题：模板图预加载改为解码预加载，资产分析页挂载即预热盈利与亏损两套图；分享弹窗图片未就绪时显示轻量骨架占位，解码完成后再显示真实图片。

#### 分享按钮改为底部悬浮胶囊
- 将复制与保存按钮从右侧工具栏移出，改为贴在卡片底部边缘的悬浮胶囊按钮，并加入图标与文字；非模板页不再保留空的右侧栏，卡片居中更紧凑。

#### 每日盈亏分享复制按钮文案修正
- 底部操作胶囊中的复制按钮文案由「复制」改为「复制图片」，与「保存图片」表述对称。

#### 每日盈亏分享弹窗背景透明度调整
- 按用户要求将每日盈亏分享弹窗的全屏遮罩由接近全黑改为 30% 黑色透明度，背景内容可见。

#### 每日盈亏分享卡片缩放联动全部内容
- 修复分享卡片拖拽缩放时只有图片跟随变化、文字与盈亏金额不变的问题：卡片内容统一放入基准画布并按当前宽高做双轴缩放，拖拽调整卡片大小时文字、金额与图片同步缩放。

#### 分享卡片改为等比例缩放
- 拖拽缩放改为按百分比等比例调整，左右与上下拖动不再拉伸变形。
- 关闭按钮移出缩放容器并固定在弹窗右上角，缩放时不再随卡片移动。

#### 每日盈亏分享复制/保存胶囊改为玻璃效果
- 浅色模式下复制图片与保存按钮由深色胶囊改为半透明白玻璃效果，深色模式保持半透明白玻璃，按钮文字与悬停态同步适配。

#### 分享模板图等待预加载后再打开弹窗
- 点击分享按钮时先等待固定模板图全部解码完成，再打开每日盈亏分享弹窗；准备期间分享按钮禁用并显示提示，避免打开后图片仍在加载。

#### 深色模式复制/保存按钮 hover 背景修正
- 修复每日盈亏分享底部复制图片与保存按钮在深色模式鼠标划过时背景变白的问题，改为半透明白色 10%。

#### 分享右侧缩略图顶部与左侧卡片对齐
- 右侧六张缩略图改为从顶部开始排列，第一张与左侧卡片顶部对齐；卡片缩放时缩略图随布局同步跟随，不再因居中对齐而错位。

#### 每日盈亏分享顶部页签字号缩小
- 按用户要求将每日盈亏分享顶部三个页签文字由 16px 缩小为 14px，视觉更精致。

#### 修复分享缩略图无法选中
- 每日盈亏分享右侧缩略图加相机上传后，上传遮罩覆盖整张缩略图并拦截点击，导致点击缩略图不再选中模板。
- 改为把相机缩小为缩略图中心的小圆标（悬停才显示），点击缩略图主体恢复选中，点击中心相机圆标才上传。

#### 修复每日盈亏分享卡片缩放时突然跳变
- 修复拖拽缩放手柄放大卡片时突然跳到默认尺寸的问题：缩放逻辑原先固定以默认尺寸为基准，未考虑已保存的非默认尺寸，导致开始拖拽瞬间回跳；改为以当前实际尺寸为基准再叠加增量，缩放平滑延续。

#### 持仓列表标题改为股票持仓并改为黑色
- 按用户要求把每日盈亏分享持仓列表页标题改为「股票持仓」，字体颜色由灰色改为黑色（深色模式适配为近白）。

#### 分享持仓列表显示全部持仓与盈亏率
- 排名序号按名次递减字号且均大于数据文字；盈亏额列改为盈亏率（盈利红、亏损绿）。
- 去掉只展示前 6 条的截断，列出全部持仓；底部合计改为「持仓盈利」，金额使用持仓总盈亏。

#### 分享持仓列表成本价改回原币种
- 修复持仓列表成本价与现价被换算成展示货币导致成本价与盈亏率错误的问题：两者改回股票原币种并按市场显示货币符号。
- 同时放大排名、名称、成本价、现价与盈亏率字体，前三名排名分别用金、银、铜色区分；名称列加宽并允许换行显示完整名称，「股票持仓」标题改为加粗并加渐变下划线。

#### 持仓列表表头补充序号标签
- 持仓列表表头第一列由空白占位补上「序号」文字标签，与数据行的排名序号对齐。

#### 每日盈亏分享三页标题统一加粗与渐变条
- 把「盈亏金额」与「持仓占比」两页标题改成与「股票持仓」一致的样式：黑色加粗标题加下方渐变彩色短条，三页标题视觉统一。

#### 分享标题间距与持仓列表滚动
- 修复复制与保存图片后标题与渐变短条拥挤的问题：标题去掉过紧的行高与字距，改为更宽松的行高，短条加高加宽并与标题拉开间距，导出与预览一致。
- 持仓列表新增竖直细滚动条（表头固定，行区域限高滚动），持仓较多时可在弹窗内滚动查看。

#### 分享标题渐变改为内联样式
- 每日盈亏分享三页标题的渐变短条改用内联渐变，标题与短条间距也用内联样式固定，避免导出时渐变区域与间距和预览不一致。
- 复制与保存图片的导出倍率由 3 降到 2，显著缩短生成耗时，图片仍保持 2 倍高清。

#### 复制图片导出倍率恢复为 3 倍
- 按用户反馈 2 倍导出图片偏模糊，将每日盈亏分享复制与保存图片的导出倍率恢复为 3 倍高清。

#### 持仓列表去货币符号并加市场徽标
- 每日盈亏分享持仓列表的成本价与现价去掉货币符号，只保留数字。
- 名称列表头改为「名称/代码」，并在每行代码前增加市场徽标色块（美股蓝、港股紫、A股按代码前缀显示，其他市场灰色兜底）。

#### 负成本持仓盈亏率显示为横线
- 修复负成本持仓（摊薄后成本为负）盈亏率显示为负值的问题：成本小于等于 0 时盈亏率无意义，改为显示横线，避免误导为亏损。

#### 持仓列表按盈亏率降序排列
- 每日盈亏分享持仓列表的排名原按盈亏金额降序，导致盈亏率更高的股票排在金额更大但盈亏率更低的股票之后；现改为按盈亏率降序排列，与盈亏率列一致，无盈亏率的持仓排到最末。

#### 分享弹窗关闭按钮移到卡片右上角
- 每日盈亏分享弹窗的关闭按钮原来固定在浏览器窗口右上角，离卡片较远；现改为定位在卡片右上角上方（右缘与卡片对齐），并随卡片缩放同步定位。

#### 分享标题渐变彩条间距调近
- 按用户要求把每日盈亏分享三页标题与下方渐变短条的间距由 12px 调整为 8px，让短条更靠近标题文字；复制与保存按钮保持不变。

#### 分享页与导出图标题彩条间距解耦
- 把每日盈亏分享标题渐变短条改为块级元素，分享页显示间距调近到 4px；复制与保存图片导出时临时把间距固定为 12px，导出完成后恢复，确保导出图的间距不受分享页调整影响。

#### 分享卡片垂直居中显示
- 每日盈亏分享弹窗的卡片原为顶部对齐，现将卡片区域改为垂直居中，卡片在页签栏下方剩余空间内上下居中显示。

#### 分享页签与卡片整体居中
- 修复上一步居中后顶部三个页签没有跟随的问题：把页签栏与卡片放入同一个纵向容器并整体垂直居中，卡片改为内容自适应高度。

#### 盈亏金额卡片位置与其他页签保持一致
- 盈亏金额页因右侧多出模板缩略图列，卡片会相对另外两页向左偏移，来回切换时位置跳动；现把外层定位容器宽度固定为卡片宽度，模板缩略图向右溢出显示，卡片始终居中。

#### 盈亏金额页标题精简为当日持仓盈亏
- 按用户要求把每日盈亏分享「盈亏金额」页标题由「当日持仓盈亏金额」改为「当日持仓盈亏」。

#### 持仓占比页标题精简为持仓分布
- 按用户要求把每日盈亏分享「持仓占比」页标题由「当前持仓资产分布」改为「持仓分布」。

#### 盈亏金额页标题再精简为当日盈亏
- 按用户要求把每日盈亏分享「盈亏金额」页标题由「当日持仓盈亏」改为「当日盈亏」。

#### 修复导出图徽标与代码截断
- 每日盈亏分享持仓列表导出图中，名称与代码列的市场徽标与代码文字出现垂直截断、徽标与代码过近；修复：代码行去掉过紧的行高，改为固定行高并加足行高，徽标与代码间距加大，导出图不再裁切。

#### 继续修复市场徽标与代码垂直截断
- 上一版导出图中市场徽标与代码仍显示下半截被裁掉；继续修复：去掉代码文字的截断与溢出隐藏（避免按内容盒垂直裁切），市场徽标放大并统一行高，代码行高同步，徽标与代码完整显示。

#### 市场徽标改用块级布局
- 导出图中市场徽标仍与上方股票名称重叠且底部显示不全，原因是嵌套弹性布局在导出渲染中不稳定。
- 改为块级加行内块布局：名称固定行高，下方留出间距后接市场徽标与代码（统一高度与行高），徽标使用明确的背景与字号，导出图不再重叠或裁切。

#### 市场徽标改用内边距自适应
- 导出图中市场徽标文字仍显示下半截被切掉，根因是在行内块上同时设置固定高度与行高，导出渲染对文字垂直居中计算不稳定。
- 改为去掉固定高度，用上下内边距加行高自适应撑起高度，文字完整显示；代码保持统一行高并与徽标顶部对齐。

#### 市场徽标改用 SVG 渲染
- 市场徽标此前用样式背景色渲染，在导出图中反复出现底部缺失与文字裁切；现改为内联 SVG（圆角矩形加居中文字），导出渲染稳定，徽标不再被裁切；同时把名称与徽标、徽标与代码的间距分别调大。

#### 市场徽标与上方名称间距精调
- 按用户反馈把市场徽标与上方股票名称的间距由 12px 精调为 8px，取过近与过远之间的合适值。

#### 市场徽标间距改为分享页 6px
- 明确用户指的是分享页显示而非导出图后，把市场徽标与上方股票名称的间距收紧为 6px；此前「过近」是导出图中徽标重叠导致，改用 SVG 后导出已稳定，6px 在分享页与导出图均合适。

#### 市场徽标间距分享页与导出图解耦
- 分享页市场徽标与名称间距保持 6px（预览合适），导出时因渲染差异导致徽标偏近，现导出前临时把徽标行间距调到 10px，导出完成后恢复，找到导出图不远不近的平衡点。

#### 分享页名称与市场徽标间距继续收紧
- 分享页股票名称与市场徽标仍显偏远：把名称行高收紧为 18px、名称到徽标间距收紧为 4px，视觉更紧凑；导出图徽标行距仍保持截图时的覆盖值。

#### 导出图市场徽标与代码垂直对齐
- 导出图中市场徽标 SVG 因未显式声明尺寸，渲染高度与代码行高不一致，导致徽标偏上；给徽标补上显式宽高并保持顶部对齐，导出图徽标与代码落在同一水平线。

#### 导出图徽标文字与代码文字精确对齐
- 导出图中徽标整体仍与代码底部对齐，导致徽标内文字比代码文字偏高；改为截图时给徽标 SVG 临时下移，使两者文字处于同一水平线，截图后恢复。

#### 导出图市场徽标与名称间距对齐分享页
- 导出图中市场徽标行仍过于贴近上方股票名称，原因是导出渲染对间距的处理与浏览器不一致；修复：截图前临时把徽标行间距提升到与分享页一致的数值，截图后恢复。

#### 导出图徽标改用占位块保证间距
- 继续修复导出图徽标偏上贴紧名称：导出渲染对间距的渲染不可靠，改为截图时把徽标行间距置零，并在其上方插入固定高度的占位块（截图后移除），用块级高度确定性地撑开间距，分享页不受影响。

#### 导出图徽标行改用弹性布局渲染
- 进一步修复导出图徽标偏上：徽标行使用网格布局，而导出库不实现网格布局，导致导出图中徽标与代码排列与间距异常。
- 改为截图时临时把徽标行切换为弹性布局（徽标不收缩、代码占据剩余宽度并保持左间距），并在上方插入占位块，截图后恢复；分享页仍用原布局。

#### 导出图徽标占位间距调至 16px
- 按用户反馈导出图市场徽标仍需继续下移，把截图时徽标行上方占位块高度由 10px 提升到 16px，徽标与名称间距进一步拉开；分享页不变。

#### 导出图徽标改用表格布局对齐
- 修复导出图市场色块与股票代码的对齐：导出库不实现弹性与网格布局，改为截图时临时用表格布局渲染徽标行（截后恢复），徽标与代码垂直居中对齐、色块完整显示、代码左侧间距与分享页一致。

#### 导出图徽标下移对齐代码文字
- 继续修复导出图市场色块偏上：徽标在表格单元格内临时下移，使色块文字与右侧股票代码文字对齐；截图后移除包裹层并恢复样式，分享页不变。

#### 导出图徽标下移量收敛到 2px
- 按用户反馈下移 4px 后偏下，将导出图徽标的下移量收敛到 2px，取偏上与偏下之间的临界点。

#### 导出图徽标行加高避免底部被裁
- 徽标下移后底部超出表格行被导出渲染裁掉；保持徽标位置不变，把导出时徽标行高度加大，让色块完整显示。

#### 复制图片时分享卡片不再跳动
- 此前导出前的样式覆盖直接改实时页面，复制图片瞬间列表会跳动；改为把这些覆盖全部放进导出库的克隆回调，只在克隆文档中生效，实时页面零改动，复制与保存时分享页无任何跳动。

#### 持仓列表第七行完整显示
- 持仓列表限高时第 7 行底部被裁掉几个像素，把列表最大高度提高，第 7 行整排完整显示，底部持仓盈利汇总仍可见。

#### 持仓列表可见 7 行且可滚动
- 按用户要求持仓列表保留全部持仓，可视区固定约 7 行，超出部分通过滚动条查看；此前误把列表截断为 7 条已恢复为全部。

#### 持仓列表可视区精确 7 行
- 表头在滚动容器之外，最大高度只约束数据行；原先第 8 行的序号仍会露出，按行高精确调整后可视区刚好 7 行完整显示，第 8 行不再露出，滚动条可下拉查看更多。

#### 持仓列表表头字号放大
- 按用户要求把股票持仓下方表头一行（序号、名称/代码、成本价、现价、盈亏率）的字号由 12px 放大到 13px。

---

## v0.1.9 · 2026-08-15

> 我的持仓-持仓盈利桑基图优化：正股显示名称、2倍ETF显示代码、块上显示盈亏金额，并支持一键导出 / 复制分享图片。

### 新功能

1. 持仓盈利桑基图优化（名称/代码规则 + 金额 + 分享图片） —— 按参考图优化我的持仓-持仓盈利图（桑基图）：1) 节点标签规则——正股只显示股票名称、2倍杠杆 ETF（名称含 2倍/2x 且带 ETF/做多/做空）只显示代码，不再「代码+名称」连排；2) 每个块下方显示盈亏金额（跟随当前选择货币换算，盈利红色 + / 亏损绿色 −，中心「持仓盈亏」块显示净盈亏）；3) 新增「分享」按钮：ECharts 2 倍像素导出 PNG，支持复制到剪贴板直接粘贴分享（非安全上下文自动回退下载文件），深浅色背景随主题。tsc 无错误、npm run build 通过。

### 修复

1. 持仓盈利桑基图美化（拥挤优化） —— 按反馈优化桑基图拥挤问题：1) 块上金额缩写——≥1 亿显示亿、≥1 万显示万（如 +$1.23万），不再挤长串数字；2) 名称与金额两行用 rich 分层样式（名称 11px 主色、金额 10px 辅助色），盈利红 / 亏损绿 / 中心紫；3) 画布高度 285→380px、节点加宽 14、间距 18、左右留白 170、连线透明度 0.7→0.55，整体更透气；4) 中心「持仓盈亏」块同步显示净盈亏缩写。tsc 无错误、npm run build 通过。
1. 桑基图分享改为先预览再保存（不直接下载） —— 按用户要求：点击「分享」先生成 2 倍像素 PNG 并在弹窗内预览，弹窗提供「复制图片」（写入剪贴板，非安全上下文自动回退保存）与「保存图片」（下载 PNG）两个确认操作，分享前可检查内容，不再点击即下载。tsc 无错误、npm run build 通过。
1. 桑基图分享卡片重设计（LOGO + 标题 + 汇总 + html2canvas 导出） —— 按参考图重做分享图：点击「分享」生成白底分享卡片——左上角站点 LOGO + 站点名（读 `/api/settings/public`，缺失用默认趋势图标），右上角「持仓盈利图」标题 + 日期；中间为桑基图（分享时临时切浅色渲染保证白底观感，截取后恢复原主题）；下方带 总盈利 = 盈利总额 − 亏损总额 汇总块与页脚水印；引入 html2canvas（2 倍像素）把整张卡片渲染成 PNG 后进入预览弹窗，再复制/保存。tsc 无错误、npm run build 通过。
1. 分享预览按钮逻辑修正（复制不下载） —— 按用户要求修正逻辑：预览弹窗里「复制图片」失败时只提示「复制失败，请使用保存图片」，不再回退触发下载；下载只发生在点击「保存图片」。tsc 无错误、npm run build 通过。
1. 分享卡片精简 + 跟随深浅色主题（浅色白底 / 深色黑底） —— 按用户要求：1) 删除分享卡片底部水印文字「Fire Fire · 持仓盈亏分布 · 数据仅供参考」；2) 把「总盈利 = 盈利总额 − 亏损总额」汇总块从图表下方移到图表上方；3) 分享图跟随当前主题——浅色模式白底深字、深色模式 #16181d 黑底浅字（标题/日期/汇总块边框与背景同步切换），图表截图与 html2canvas 导出背景随主题一致。tsc 无错误、npm run build 通过。
1. 分享图复制兼容局域网 http（服务端写入 Mac 剪贴板） —— 修复局域网 http（非安全上下文）下「复制图片」失败的问题：浏览器 `navigator.clipboard` 在非安全上下文不可用，新增 `POST /api/clipboard`——前端复制失败时把 PNG 交给服务端，用 osascript 写入 Mac 系统剪贴板（登录 + 限流 + PNG 魔数校验 + ≤8MB + 服务端生成临时文件名，无注入面），聊天窗口（微信等）可直接粘贴；安全上下文仍走浏览器剪贴板 API。实测接口 200、剪贴板内容正确（测试后已恢复原剪贴板）。tsc 无错误、npm run build 通过。
1. 修复复制图片兜底不生效（浏览器 API 抛错被外层 catch 吞掉） —— 修复「复制图片」仍失败且剪贴板图片异常的问题：原逻辑把浏览器剪贴板 API 与服务端兜底放在同一个 try 里，当 `navigator.clipboard.write` 抛错（如 `NotAllowedError`：文档未聚焦/权限被拒）时会直接跳到外层 catch 显示失败，服务端 osascript 兜底根本没执行（服务日志也证实没有来自浏览器的 `/api/clipboard` 请求）。修复：浏览器 API 单独 try/catch，失败后继续走服务端写入 Mac 剪贴板；复制失败时 toast 显示具体错误信息便于后续排查。服务端路径已用 4.5MB 大图实测逐像素一致。tsc 无错误、npm run build 通过。
1. 分享预览弹窗按钮字体色统一 —— 按用户反馈：预览弹窗「保存图片」按钮文字原为 ink-2 灰色，与「关闭」「复制图片」的 ink 深色不一致，补 text-ink 对齐三个按钮文字颜色。tsc 无错误、npm run build 通过。
1. 复制图片改用 atob 转 Blob（不依赖 fetch(data:)，兼容 Safari） —— 排查「复制图片仍失败」：服务日志证实浏览器请求从未到达 `/api/clipboard`，怀疑 `fetch(data:image/png;base64,...).blob()` 在部分浏览器（如 Safari 对 data URL fetch 支持不完整）抛错，直接走到「图片数据生成失败」。修复：不再用 fetch 转 Blob，改为 atob 解码 data URL 直接构造 Blob（所有浏览器通用），浏览器 API 与服务端兜底两条路径均重新实测通过（服务端路径触发时 `/api/clipboard` 有请求、系统剪贴板拿到图片）；复制失败时 toast 显示具体错误并 `console.error` 便于排查。tsc 无错误、npm run build 通过。
1. 复制图片后保留分享预览弹窗 —— 按用户要求：点「复制图片」成功后不再自动关闭预览弹窗，可继续查看或再次操作；关闭仍由「关闭」按钮完成。tsc 无错误、npm run build 通过。
1. 分享卡片改为薰衣草紫相框式布局（外层包裹白/深色内容） —— 按参考图重设计分享卡片：外层整体使用参考图背景色 #A5A5BF（淡薰衣草紫，从参考图取色 165,165,191）作为相框，把内层白/深色内容块包裹起来；内层包含品牌栏（LOGO+站点名 / 持仓盈利图+日期）、汇总块与桑基图，浅色模式白底深字、深色模式 #16181d 黑底浅字。tsc 无错误、npm run build 通过，导出图已实测外层 165,165,191、内层白底。
1. 分享卡片按左图重构：紫底 + 头部浮层 + 独立圆角内容卡 —— 仔细对比左右参考图后重构分享卡：1) 淡薰衣草紫 #A5A5BF 铺满整张卡片背景；2) LOGO（白底圆角占位/站点 Logo）+ 站点名 与 「持仓盈利图」+ 日期 直接浮在紫底上（深色文字，不套内容块）；3) 桑基图+汇总做成独立圆角卡片（18px 圆角 + 柔和阴影），左右留白 24px、顶部紫底留白约 124px，浅色模式白卡深字、深色模式 #16181d 黑卡浅字。导出图实测：顶部紫底、内容卡左/右边距 24px、圆角内收正常。tsc 无错误、npm run build 通过。
1. 分享卡片压缩上下留白 —— 按反馈：上下薰衣草紫留白太多显得卡片过宽。压缩外层边距（顶部 52→22px、底部 40→22px、左右 24→20px）、头部与内容卡间距 22→14px，内容卡内边距同步收紧。实测卡片高度 490→432px、顶部紫底 124→85px，整体更紧凑。tsc 无错误、npm run build 通过。
1. 分享预览弹窗去除提示文字 + 分享卡进一步压缩 —— 1) 按用户要求删除预览弹窗副标题「确认内容后，可复制分享或保存图片」，后续不加多余文字；2) 继续压缩分享卡：外层紫底上下 22→14px、左右 20→16px，头部与内容卡间距 14→10px，LOGO 32→28px，内容卡内边距 16/22/18→12/16/14、圆角 18→14。实测卡片 720×410、顶部紫底 73px、左右留白 16px。tsc 无错误、npm run build 通过。
1. 分享图清晰度优化（3 倍导出 + 图表像素对齐 + 抗锯齿） —— 按反馈：分享图文字锐化/发虚。优化：1) html2canvas 导出倍率 2→3，输出分辨率提升 2.25 倍；2) ECharts 图表 pixelRatio 2→3，与捕获倍率对齐，避免图表被放大导致发虚；3) onclone + 内联样式设置 `-webkit-font-smoothing: antialiased` 与 `text-rendering: geometricPrecision`，文字边缘更干净。tsc 无错误、npm run build 通过。
1. Swift OCR 模块缓存移出仓库 + Next 缓存阈值清理（瘦身约 1.2GB） —— `visionOcr.ts` 编译模块缓存原放在 `fire-web/.cache`（实测 235MB），改为系统临时目录（系统管理、自动回收），仓库 `.cache` 仅保留 72KB 编译产物；新增 `npm run clean:caches`（`scripts/clean-next-cache.mjs`，阈值 200MB 自动清理 `.next`/`.next-dev` 缓存，`--force` 强制），fire.sh 启动前自动执行并新增 `clean-cache` 命令。本次清理：`.next` 782MB、`.next-dev/cache` 145MB、`swift-module-cache` 235MB、tsbuildinfo 与全部 .DS_Store，fire-web 由 1.0GB 降至 675MB。tsc 无错误、OCR 端到端验证 4 行正常。
1. 资产分析跑赢数值移到向下箭头右侧 —— 按参考图：基准条「跑赢 ● 基准名 ▾」的跑赢幅度（如 0.14%）从右对齐改为紧跟向下箭头右侧、与基准选择同组显示；隐藏态仍显示 ******，正负着色保留。tsc 无错误、npm run build 通过。
1. 每日盈亏分享图（纯白卡片：盈亏金额 / 持仓列表 / 持仓占比） —— 资产分析-账户资产卡新增分享按钮，生成每日盈亏分享图：1) 纯白卡片（不做紫底），头部 LOGO+站点名 / 每日盈亏+日期；2) 三个区块——盈亏金额（当日盈亏大数字 + 收益率，红涨绿跌）、持仓列表（按市值排序，2倍ETF显示代码、正股显示名称，最多 8 行 + 等 N 只）、持仓占比（ECharts 环形图 + 图例，前 8 只 + 其他）；3) 数据按展示货币换算，复用 html2canvas 3 倍导出与复制/保存/服务端剪贴板兜底，预览弹窗复制后保留、无多余文字；4) 数据链路已按持仓汇总当日盈亏与市值占比，为后续「盈亏日历图」预留。tsc 无错误、npm run build 通过、无头浏览器实测生成 1920×2133 白色卡片正常。
1. 每日盈亏分享图重做（对齐参考图：色块金额 + 横向色条列表 + 大环形占比） —— 按反馈重做分享卡视觉效果：1) 盈亏金额改为浅红/浅绿渐变底的大数字色块（红涨绿跌，含今日收益率）；2) 持仓列表每行加横向色条（宽度按当日盈亏绝对值占比，红正绿负）；3) 持仓占比环形图加大（250×190）并加分隔线，图例不变；白色卡片、三区块顺序 盈亏金额 → 持仓列表 → 持仓占比 保持不变。tsc 无错误、npm run build 通过、实测卡片 1920×2265 色块/色条/环形齐全。
1. 每日盈亏分享中心重做为长桥式卡片布局（接入第六版模板图） —— 按参考图（长桥分享卡）重做分享中心布局：1) 品牌栏（LOGO+站点名）+ 红色「转发」按钮；2) 当日持仓盈亏金额大数字 + 收益率（红涨绿跌）；3) 统计行 总资产 / 持仓市值 / 持仓总盈亏；4) 盈亏金额 / 持仓列表 / 持仓占比 三个可点击页签；5) 内容区展示对应模板图（第六版 盈利/亏损各 5 张，按当日盈亏正负自动切换目录、按页签/底部入口切换 1-5 号模板，不识别图中文字、原图直接使用）；6) 底部 5 入口（每日盈亏/持仓列表/持仓占比/盈亏日历/更多）可点击切换；7) 转发 = html2canvas 3 倍把整张卡片渲染成 PNG，预览后复制/保存。tsc 无错误、npm run build 通过、无头浏览器实测布局与切换、转发预览正常。
1. 每日盈亏分享中心改为三块布局（顶部盈亏金额一排 / 左白色卡片 / 右分享缩略图） —— 按反馈重构整体布局为三块：1) 上方一整排盈亏金额——当日持仓盈亏金额 + 收益率 + 总资产 / 持仓市值 / 持仓总盈亏统计；2) 左侧白色卡片——品牌栏 + 当日盈亏金额 + 盈亏金额/持仓列表/持仓占比页签 + 模板图内容 + 底部 5 入口，可整体渲染成 PNG 分享；3) 右侧分享缩略图列——5 张模板缩略图，未选中置灰、选中才有颜色并高亮边框，点击切换左侧卡片；模板图仍按当日盈亏正负选盈利/亏损目录。tsc 无错误、npm run build 通过、无头浏览器实测三块布局、缩略图选中态与卡片联动正常。
1. 删除每日盈亏分享功能 —— 按用户要求整体删除每日盈亏分享：移除 `DailyPnlShareModal` 组件、资产分析页的分享按钮/相关数据计算与渲染，以及第六版模板图资源（盈利/亏损各 5 张，共 20MB，原图保留在用户桌面）；账户资产卡片恢复为仅刷新按钮。tsc 无错误、npm run build 通过、全仓库无残留引用。
1. 前端包体积优化（视图按需加载 + ECharts 按需引入 + 版本历史懒加载 + 设置缓存） —— 针对后端应用首屏约 3.4MB JS 一次性下发做四项优化：1) 后端 11 个视图全部改为 `next/dynamic` 懒加载（仅下载当前激活页签代码，布局 chunk 813KB → 27KB）；2) ECharts 全量引入改为 `echarts/core` 按需注册（新增 `lib/echarts.ts`，只注册 candlestick/line/bar/sankey 与用到的组件 + Canvas 渲染器，图表 chunk 1104KB → 712KB 且仅含图表的页签才加载）；3) 版本历史约 200KB 文案拆分到 `lib/versions-history.ts`，版本弹窗懒加载（设置页 chunk 307KB → 101KB，历史只在点开弹窗时下载）；4) `lib/settings.ts` 的 `getSiteSettings` 加内存缓存（`updateSiteSettings` 写后失效），避免每个请求 / 每次行情批次全表 SELECT + JSON.parse。AGENTS.md 版本记录约定同步更新拆分后的维护方式。tsc 无错误、npm run build 通过。
1. 分享当日盈亏卡片金额对齐修复（复制图片错位） —— 按反馈修复资产分析-账户资产-分享当日盈亏复制出的图片金额元素错位：1) 持仓市值右侧 USD 由 12px 小字改为与金额同字号（15px）并基线对齐；2) 当日持仓盈亏金额右侧 USD 由 flex 基线对齐改为行内基线布局（底部对齐、不再浮在数字上方），html2canvas 导出与页面渲染一致；3) 持仓市值前的 $ 圆形徽标重设计为单层居中（去掉嵌套 span 与 translate），消除复制图片中的上下错位。tsc 无错误、npm run build 通过。

---

## v0.1.8 · 2026-08-14

> Web 服务默认端口由 8124 切换为 3000：启动脚本本就默认 3000，本次把设置默认域名、README、AGENTS、冒烟测试、API 规范与 iOS 后端地址全部对齐，并重启服务至 3000。

### 新功能

1. 右上角头像改为 B 站式跨层效果（悬停丝滑放大 + 白边 + 半入卡片） —— 按参考图把右上角账号头像做成跨层效果：1) 头像加大到 40px，悬停丝滑放大（spring 曲线 cubic-bezier(.34,1.56,.64,1)，hover 1.18、展开 1.4）；2) 白色描边（ring-2 ring-white + 柔和阴影）让头像在跨层时立体突出；3) 展开菜单时面板顶边精确对齐头像中心（实测头像中心与面板顶边差 2px）——下半部分在卡片内、上半部分探出卡片上方，B 站「跨层头像」观感。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 网站升级为可安装 PWA（Chrome「在应用中打开」） —— 按用户要求把 Fire Web 做成可安装的渐进式 Web 应用：1) 新增 app/manifest.ts（读取站点标题，name/short_name/start_url/standalone/主题色，icons 含 192/512/maskable）；2) 由站点图标生成 public/uploads/ico/pwa-192.png 与 pwa-512.png；3) 新增 public/sw.js——导航与静态/上传资源网络优先、离线回退缓存，/api 与跨域不拦截（兼容 dev 热更新）；4) 新增 components/PwaRegister.tsx 注册 Service Worker；5) layout 头部补充 manifest / theme-color / mobile-web-app-capable / apple-touch-icon 等 meta，深色模式主题色联动。已实测 manifest、sw.js、图标、页面 meta 全部 200；提示：Service Worker 与 Chrome 安装提示需要 HTTPS 或 localhost（局域网 IP http 下浏览器会静默跳过注册）。
1. 订单日历滚动跟随 + 位置/大小自动保存 + 缩放手柄融入边框 —— 继续优化订单委托时间日历：1) 日历跟随页面上下滚动——fixed 浮层监听滚动增量同步补偿位置，与锚点内容联动（不再钉死在视口）；2) 位置与大小改动自动保存到 localStorage（拖动/缩放结束与关闭时写入，重新打开自动恢复宽高与偏移）；3) 右下角缩放手柄改为融入边框的细斜线样式（低透明度、悬停加深，参考左侧分隔条），拖拽流畅度与左右拉布局一致。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 订单委托时间日历可拖动 + 可缩放（K 线图设置面板同款交互） —— 按用户要求把订单「委托时间」日历做成可自由操控的浮层：1) 顶部六点手柄拖动移动（pointer capture + 视口钳制，拖动后隐藏指向按钮的小三角）；2) 右下角缩放手柄拖拽调整宽高——宽度最小 360px（双月日历自动变单列竖排）、高度最小 260px，超出部分内部滚动，用户可自由拉大缩小；3) 打开时自动重置尺寸与位置。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 刷新按钮改小至参考图尺寸 + 我的持仓「账户资产」加刷新 —— 1) 全站统一刷新按钮由 32px 缩小到约 24px（h-6 w-6、圆角 7px、图标 12px 占按钮 50%、描边更细），对齐参考图工具栏按钮比例，资产分析页账户资产/持仓/交易及我的持仓详情订单区同步生效；2) 我的持仓页顶部「账户资产」标题右侧新增刷新按钮：强制重取行情（force）+ 重载汇率，总资产/当日盈亏/持仓市值等随最新数据更新，刷新中图标旋转。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 资产分析从我的持仓剥离为独立页签（我的持仓下方、自选股上方） —— 按用户要求将资产分析从「我的持仓」页内嵌（金钱图标进入）剥离为独立导航页签：1) 新增 /assets 页签，导航顺序为 我的持仓 → 资产分析 → 自选股（同步更新 lib/settings.ts NAV_KEYS 与 DEFAULTS.tabs、RecordsApp DEFAULT_TABS/TAB_ICONS、数据库 site_settings.tabs）；2) 新建 components/views/AssetAnalysisView.tsx：独立页签自取汇率（localStorage 兜底 + /api/rates）、复用 useAssetIcons 股票图标、刷新链路（强制重取行情 + 汇率 + 触发父层重拉记录）、onBack 返回我的持仓；3) 我的持仓移除金钱图标入口与内嵌资产分析渲染（保留账户资产页自身）；4) 服务端守卫自动放行 /assets（settings.tabs 驱动），未登录重定向 /login。tsc 无错误、npm run build 通过、/assets 登录 200 / 未登录 307 验证通过。
1. 账户资产 / 持仓 / 交易各加刷新按钮（实际重取数据） —— 按参考图（卡片右上角方形边框循环箭头图标）新增三个真实刷新的按钮：1) 新建全站统一组件 components/RefreshButton.tsx（描边方形 + 循环箭头，刷新中图标旋转，深浅色适配）；2) 资产分析页「账户资产」卡右上角（标题同步改为「账户资产」）、「持仓」工具行、底部「交易」订单面板头部各放一个；3) 实际效果——账户资产/持仓刷新：强制重取行情（RecordsApp.refreshQuotes 增加 force 参数绕过轮询互斥与后台限制）+ 重取汇率（HoldingsView 抽取 loadRates）+ 触发父层重新拉取持仓记录，资产/盈亏/市值随最新行情更新；交易刷新：重新请求 /api/v1/orders 更新订单列表；4) 我的持仓详情订单区同步接入面板内刷新按钮（移除原顶部文字刷新）。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 订单页继续优化（Tab 下划线 / 四筛选+重置 / 行按方向着色 / 状态圆对勾） —— 按 6 张新参考图（阿里百炼视觉模型逐张识别）继续对齐券商订单页：1) 顶部 Tab 由胶囊改为「文字 + 蓝色短下划线」选中态（参考图样式）；2) 筛选栏由两项改为四项单行下拉——市场（选中蓝色边框）、标的类型（全部/股票/ETF，按名称含 ETF/做多 推断）、订单状态（全部/已成交/已撤销）、委托时间（日历图标 + 全部/当日/近7天/近30天/近1年/自定义，自定义弹双月日历选择起止区间），最右侧新增「重置」垃圾桶按钮一键清空筛选；所有筛选本地持久化；3) 表格行按买卖方向整行着色（买入浅红、卖出浅绿，深色模式深红/深绿底，悬停加深），与参考图一致；4) 订单状态由胶囊改为「绿色圆对勾 + 已成交」/「红色圆叉 + 已撤销」（表格与详情弹层统一）；5) 导出接口新增 type（标的类型）与 start/end（委托时间区间）筛选并端到端验证。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 订单参考图逐项对齐（操作列图标 / 详情弹层 / 导出明细与最近导出） —— 用阿里百炼视觉模型逐张复核此前订单参考图并修复差异：1) 操作列由文字「详情」改为参考图图标按钮（文档/列表图标），我的持仓详情保留编辑/删除；2) 订单详情弹层对齐参考图——两列键值改为留白布局（去掉逐行分割线与外框）、方向改为纯色文字（买红/卖绿）、委托单号加复制图标、字段顺序对齐参考（证券账户/委托单号/订单类型/方向/委托价格/委托数量/成交价格/成交数量/成交金额/有效期/时段/结算货币/订单状态/触发状态/委托时间/已实现盈亏/成交后持仓/驳回原因/备注），费用明细改为列表内加粗分组标题（佣金、合计）；3) 导出弹窗对齐参考图——新增「同时导出订单明细」勾选（服务端 GET /api/v1/orders/export?detail=1 追加「订单明细」工作表：订单号/状态/市场/代码/名称/方向/类型/数量/价格/币种/时间/均价/成交数量/成交金额/费用/已实现盈亏/成交后持仓数量/成本/有效期/时段/触发状态/驳回原因/备注）、新增「最近导出」（localStorage 最近 20 条：时间范围/状态/文件名称/导出时间/文件大小/重新导出），导出改为 fetch 下载以记录文件大小并提示成功。tsc 无错误、npm run build 通过、明细工作表端到端校验通过、服务重启后页面 200。
1. 订单筛选改为参考图两行控件（绿色对勾标签 + 橙色选中胶囊） —— 按参考图（阿里百炼视觉模型辅助识别）将订单面板的市场、订单状态筛选改为两行式券商控件：上行 = 字段名标签（订单状态左侧为绿色对勾圆、市场左侧为当前市场图标），下行 = 橙色选中胶囊（浅色 bg-[#fff4e5] text-[#b06a00]，深色 bg-[#2b2413] text-[#e6b45c]，与全站橙色强调一致）+ 当前值 + 下拉箭头，点击展开选项菜单（选中项橙色高亮 + 对勾，Esc / 点击外部关闭）；筛选值继续本地持久化。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 订单列顺序对齐参考（操作第一列）+ 详情弹层 + 状态胶囊/成交圆点 —— 按参考图调整订单表列顺序：第一列为「操作」（详情按钮，我的持仓详情另含编辑/删除），其后严格按 21 字段顺序排列——订单状态、市场、股票代码、股票名称、方向、委托类型、委托数量、委托价格、触发价格、币种、委托时间、成交均价、成交数量、成交金额、剩余挂单数量、撤/废单数量、有效期、时段、触发状态、订单号、驳回原因（费用、已实现盈亏移入详情弹层展示）；点击「操作-详情」打开订单详情弹层（参考图 2/3/4）：标题按买卖方向、股票图标+名称+代码、证券账户/委托单号/订单类型/方向/委托价格/委托数量/成交价格/成交数量/成交金额/已实现盈亏/成交后持仓/有效期/时段/委托时间/结算货币/订单状态/触发状态/驳回原因/备注 + 费用明细（佣金、合计）；成交数量列已成交显示绿色圆点（失败红色）；订单状态改为对勾胶囊（成功绿）/叉号胶囊（失败红）；列设置缓存键升级 v2 使新默认顺序全局生效。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 订单导出 Excel（xlsx）· 21 列与参考图一致 —— 订单列表新增「导出」按钮与导出弹窗（筛选条件摘要 + 将导出的笔数与 21 列说明），按当前 Tab / 市场 / 订单状态筛选导出为 .xlsx：订单状态、市场、股票代码、股票名称、方向、委托类型、委托数量、委托价格、触发价格、币种、委托时间、成交均价、成交数量、成交金额、剩余挂单数量、撤/废单数量、有效期、时段、触发状态、订单号、驳回原因；我的持仓详情导出仅包含该持仓订单。服务端新增 GET /api/v1/orders/export，手写零依赖 xlsx 写入器（lib/xlsx.ts，不引入有已知漏洞的 SheetJS），金额保留两位、订单号为文本避免科学计数法，导出文件经 ZIP/XML 与真实数据双重校验。
1. 订单表补全参考字段列 + 列设置与表头排序 —— 网页订单表补全参考图全部字段列（触发价格、剩余挂单数量、撤/废单数量、触发状态、驳回原因），与导出 21 列完全一致；表头可点击排序（升/降，委托时间默认倒序），列设置按钮（漏斗图标，样式与持仓一致）弹出「需要显示哪些列」——勾选显示/隐藏、拖动手柄排序、至少保留一列，列配置与排序均本地持久化；「导出」与「列设置」按钮放在当日/历史 Tab 同一行右侧。tsc 无错误、npm run build 通过、导出 API 端到端实测 21 列与数据正确。
1. 交易系统当日/历史订单重塑（券商风格）+ 唯一订单号 —— 按参考图将资产分析底部与我的持仓详情的「当日订单 / 历史订单」从简易表格升级为券商风格订单列表：1) 每笔成交生成 10 位唯一订单号（随机数，DB 唯一索引兜底，存量订单统一重排为 10 位），订单号可点击复制；2) 表格列对齐参考图——订单号 / 订单状态 / 市场（市场图标）/ 股票代码 / 股票名称（股票图标）/ 方向 / 委托类型（限价单）/ 委托数量 / 委托价格 / 币种 / 委托时间（含美东、北京时间等市场时区标签）/ 成交均价 / 成交数量 / 成交金额 / 费用 / 已实现盈亏 / 有效期（撤单前有效）/ 时段（盘中+盘前盘后）；3) 顶部当日/历史 Tab + 市场筛选（含市场图标）+ 订单状态筛选，Tab 与筛选本地持久化；4) 订单加载中仅在没有已有数据时展示，避免刷新闪「加载中」；我的持仓详情保留编辑/删除操作，删除对当日与历史订单均可用。tsc 无错误、npm run build 通过、订单创建与存量重排端到端验证 10 位唯一。
1. 收益率趋势图基准做成图1 下拉主要市场 + 黄绿点 —— 按参考图：基准对比条由「我的持仓 ● · 标普 500 ●」图例改为图1 式下拉——左侧「跑赢 + 黄绿色圆点 + 基准名 + 箭头」，右侧显示跑赢幅度（组合收益 − 基准收益）；点击弹出主要市场下拉（标普 500 / 纳斯达克 / 道琼斯 / 恒生指数 / 上证指数 / 深证成指），选中项深蓝底白字加对勾；基准选择持久化（`fire:asset-benchmark`）。数据源：US/HK 用指数 ETF（SPY/QQQ/DIA/02800），CN 上证/深证指数走 `/api/kline/full` 新增 `index=1`（sh000001 / sz399001，`lib/kline.ts` `tencentSymbol` 支持指数代码），实测三路日 K 正常；图表基准线随选择切换名称与数据。tsc 无错误、资产分析页编译加载正常。
1. 截图导入持仓/自选 · Apple Vision 本地识别引擎接通 —— 我的持仓与自选股搜索框新增相机入口，上传券商持仓/行情截图后由 macOS 本地 Apple Vision（VNRecognizeTextRequest）识别：1) 新增 `scripts/vision-ocr.swift`（Swift 编译产物按需缓存于 `.cache/`，macOS + Xcode/CLT 缺失时返回 503 明确提示）；2) `lib/snapshotParser.ts` 把识别文本按视觉行聚类解析为 名称/代码/市场/数量/现价/成本/涨跌幅（代码前缀推断市场、标题行/表头过滤、同码去重，持仓布局按「成本→现价」、行情布局按「现价」回退）；3) `POST /api/records/import-image` 由 503 占位接通为真实识别（8MB 内 JPG/PNG/GIF/WEBP、限流保留），识别结果先预览可编辑，确认后 `POST /api/records/import-apply` 合并写入持仓/自选（不误清空已有数量/成本）；4) 图片仅在本机识别不上传云端。引擎抽象已就绪，未来 NAS/服务器部署时换 RapidOCR 只改后端一小块。tsc 无错误、持仓/行情两张测试截图端到端识别解析全部正确。

### 修复

1. 美股盘前/盘后行情批量刷新加固（账户资产不再延迟） —— 修复资产分析-账户资产在美股盘前/盘后部分股票价格不更新、需点进个股才刷新的问题：根因是列表轮询批量请求 Yahoo 扩展时段行情时部分股票被限流/超时，`fetchUsExtendedQuote` 返回 null，行情退回腾讯收盘价，而个股页单只请求容易成功。修复：1) `fetchUsExtendedQuote` 改用与个股页同款的 `range=1d`（负载更小且已含盘前/盘后分钟），盘前昨收取 `meta.regularMarketPrice` / 盘后取 `meta.previousClose`，保持「当日」口径正确；2) Yahoo `query1 → query2` 双主机自动兜底，单主机 6s 超时；3) 成功缓存 60s / 失败缓存 20s，缩短卡顿窗口并降低上游压力；4) `fetchQuotes` 扩展行情按代码去重（同一股票同时出现在持仓/自选只请求一次）、并发 6→4；5) `/api/kline/session-day`（个股页扩展时段）同步双主机兜底。tsc 无错误、npm run build 通过、算法用真实 Yahoo 数据模拟盘前验证（取到当日 09:29 最新盘前点、昨收口径涨跌正确）。
1. 删除按钮边框统一（浅色浅灰 / 深色白框，去红边） —— 按用户要求删除按钮不再用红色边框与红底悬停：行内删除按钮（自选股/持仓/用户管理）边框改为浅色 border-edge / 深色 border-white/20，悬停浅灰（hover:bg-brand-hover）非红；批量删除按钮去红边；附件/名人/素材库/设置等图标删除按钮悬停由红色 up-bg 改为中性浅灰（dark:bg-white/10）。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 修复深色模式下删除按钮白底白图标看不见 —— 按用户反馈（深色模式删除按钮背景与本体重叠看不见）：根因是部分删除按钮用了 bg-white 且深色无覆盖（白底）+ 白色 trash.fill 图标。修复：1) globals.css 增加深色 hover 覆盖——hover:bg-up-bg 深色变深红 rgba(58,28,28,.9)、hover:border-up 红边框、hover:text-up 亮红，全站删除/危险按钮悬停不再闪白；2) 自选股/持仓/用户管理行内删除按钮补 dark:bg-[#1c222d] dark:border-white/10 dark:text-white/70，深色下图标清晰可见。资产分析中的垃圾桶是「重置」非删除，未改动。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 全站删除按钮统一为苹果 trash / trash.fill 图标 —— 用户从 SF Symbols App 导出 trash.svg（浅色，已调整为深灰渐变可见）与 trash.fill.svg（深色白渐变），新增共用组件 components/DeleteIcon.tsx（按深浅色自动切换两张 SVG）。全站替换删除按钮：自选股/持仓行内删除、批量删除、用户管理删除、订单删除、素材库删除券商/指数、附件管理删除、自选股分组删除、名人持仓明细删除等手绘垃圾桶与 X 图标按钮统一改为 DeleteIcon；保留弹窗内「确认删除/删除」文字确认按钮与 TradeOrdersPanel 的重置（重置非删除）。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 管理员徽章放大 + 右下角徽章染青色（浅/深两版 SVG） —— 按用户反馈：1) 管理员 seal 图标由 16px 放大到 20px；2) 将 SVG 中右下角「seal 徽章」路径染成青色——浅色版人物黑 + 徽章 #00BCD4，新增深色版 person.badge.checkmark.seal.fill-dark.svg（人物白 + 徽章亮青 #00E5FF），账号菜单按深浅色切换显示，不再用 dark:invert 整体反转。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 管理员徽章换成正版 SF Symbol person.badge.checkmark.seal —— 用户从 SF Symbols App 导出正版符号（64pt SVG），接入 public/icons/：person.badge.checkmark.seal.svg 与 .fill.svg（Apple CoreSVG、fill=black）。账号菜单「管理员」文字替换为该符号的 fill 版（16px，深色模式 dark:invert 变白），删除此前临时替代的 person.crop.circle.badge.checkmark PNG 与残留 .DS_Store。tsc 无错误、npm run build 通过、服务重启后页面 200、SVG 访问 200。
1. 账号菜单去掉统计区与菜单区之间的灰色分割线 —— 按用户截图（红箭头指向）移除账号菜单中自选股/持股统计区与功能菜单区之间的水平分割线，区块衔接更干净；底部退出登录上方的分割线保留。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 账号菜单卡片深色适配 + 阴影加强（不再白卡看不见） —— 按用户反馈（卡片太白看不见）修复账号菜单卡片：1) 面板补深色背景（dark:bg-[#1c1c1e] + 深色边框/分割线），深色模式下不再整卡发白；2) 菜单项 hover 深色下由浅灰改为 white/10，管理员标签与头像兜底方块补深色适配；3) 阴影加强（0 14px 44px）使白卡在浅色背景上轮廓清晰。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 彻底修复订单列设置「上方区域消失」复发（overflow-clip 双端防护） —— 按用户反馈（对齐持仓样式后 bug 又复发）浅色模式无头 Chrome 复现定位：面板容器 overflow-hidden 使其成为可滚动容器（scrollHeight 829 > client 610），点击复选框后浏览器把「面板」整体滚动到 scrollTop 219，标题与顶部选项被顶出、下方留白。彻底修复：订单面板由 overflow-hidden 改为 overflow-clip（只裁剪、永不滚动，实测点击后 panelScrollTop 保持 0、标题 top 137 可见、顶部选项全部可见），持仓面板同步加 overflow-clip 防护，杜绝同源复发。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 订单「需要显示哪些列」面板完全对齐持仓样式 —— 按用户要求把订单列设置面板改成与持仓列设置面板完全一致的样式：顶部锚定（pt-[12vh]）居中、面板 320px 宽、标题栏同款、列表 max-h-[55vh] 滚动、行样式 mx-2 rounded-[8px] py-1.5 同款、六点拖拽手柄、无行间分割线（与持仓一致），仅保留 Portal 挂载保证 fixed 定位稳定。无头 Chrome 实测两面板：同位置/同宽/同行高/同手柄/同无分割线，差异仅为订单列数更多。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 全站清理面板小字提示（持仓列设置等，化繁为简） —— 按用户要求移除面板/设置里冗余的小字提示：1) 持仓「需要显示哪些列」的「勾选显示 · 拖动手柄排序 · 自动保存」（订单列设置此前已删）；2) 名人持仓头像选择下拉的「选择名人，上传新头像」；3) 素材库「新增主流券商」右侧的「点击名称添加…」；4) 素材库设置里 股票图标同步 / 退市检测 / CDN 图标通道 / 清理孤立文件 下方的功能说明小字。仅删除提示文字，功能与标题保留。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 深色下列设置面板行间白色分割线弱化 —— 按用户反馈（图中的白色线）去掉深色模式下「需要显示哪些列」面板的行间分割线刺眼白线：分割线由 divide-edge/60 增加 dark:divide-white/10（深色下近乎不可见），未选中复选框边框由浅灰改为 dark:border-white/25（弱化白色感），浅色模式保持原样。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 复选框恢复白色对勾并去掉白色边框 —— 按用户要求微调复选框：选中态恢复为「深色底 + 白色对勾」（浅色为黑底白勾，深色为 #2b313c 深色底白勾，像素实测无白块），并去掉白色边框——选中态无边框、未选中态深色模式下边框由浅灰改为深灰 #3a4150；字体栈确认保持 苹方 PingFang SC + SF Pro（苹果系统字体、无衬线、字形方正，tailwind font-sans 已配置）。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 列设置面板对齐参考图：去提示文字、深色去白块、文字左图标右、字体字重对齐 —— 按左图参考优化「需要显示哪些列」面板：1) 删除提示文字「勾选显示 · 拖动手柄排序 · 自动保存」；2) 深色模式下复选框由白色实心块改为透明底 + 白色对勾 + 半透明白边框（与参考图一致，像素实测背景透明）；3) 行布局改为 justify-between——文字在左、拖拽图标（三横线 ≡，参考图同款）在右；4) 字体对齐参考：标题 16px 半粗（原 15px 粗体）、列表标签 13px 中粗（font-medium），参考图字体本身即苹方 PingFang SC + SF Pro（与本站系统字体栈同源），观感差异主要来自字重与字号，已对齐。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 修复紧凑面板后「上方区域消失」复发（根因：面板自身被滚动） —— 按用户反馈（变短后之前的 bug 复发）定位到根因：面板自身是 overflow-hidden + 固定高度，浏览器在点击/聚焦复选框时会把「面板」整体滚动（实测 scrollTop 409px），把标题与顶部选项顶出可视区、下方留白——这就是「需要显示哪些列上方区域没了」的真因。修复：面板容器由 overflow-hidden 改为 overflow-clip（只裁剪、不可滚动），滚动只发生在内层列表；无头 Chrome 实测：即使触发 scrollIntoView 点击「撤/废单数量」，面板位置不变，标题与「全部/订单状态/市场」始终可见、无空白。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 订单列设置面板改为参考图紧凑效果（限高 + 行分割线 + 三横线手柄） —— 按参考图优化「需要显示哪些列」面板：1) 面板限高 min(560px, 75vh)，不再占满视口，一屏约 11-12 行（其余滚动）；2) 行与行之间加浅灰分割线（divide-y），界限清晰；3) 拖拽手柄由六个点改为参考图的三横线（≡）图标；4) 保持紧凑布局无空白。无头 Chrome 实测：面板高 560px、22 行带分割线、三横线手柄、无大片空白，与参考图一致。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 列设置弹层改为 Portal 挂载 + 防焦点滚动（面板不再位移/上方区域消失） —— 针对「取消勾选撤/废单数量后需要显示哪些列上方区域消失」追加两道防线：1) 列设置弹层改用 React createPortal 挂到 document.body 并内联固定定位，彻底摆脱任何祖先容器（transform/滚动容器等）对 fixed 的干扰，弹层永远相对视口居中；2) 复选框 onMouseDown preventDefault，阻止点击后浏览器把焦点元素滚动进视野导致列表跳动、顶部选项被滚走；无头 Chrome 实测：点击前后 panelTop 148/panelH 717 不变、scrollTop 0 不跳动、标题与「全部/订单状态/市场」始终可见。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 订单列设置面板与订单表格布局重构（无头 Chrome 实测复现） —— 用无头 Chrome 实测复现并修复资产分析-订单列设置的空白/变形问题：1) 列设置面板行内「文字与拖动手柄之间的大段空白」根因是复选框标签 flex-1 拉伸，改为紧凑布局（FlatCheckbox 增加 stretch 选项，订单面板传 false）；2) 勾选状态未全局生效根因是资产分析与我的持仓详情用了不同本地存储键，统一为共享键 fire:orders:columns:v2，勾选即自动保存并两处同时生效；3) 订单表格 22 列在 813px 卡片内被挤压截断，恢复「固定像素列宽 + minWidth + 横向滚动」的券商宽表样式（列宽舒适、隐藏列后剩余列自动伸展不空白），并新增 .orders-scroll 样式让横向滚动条始终可见，避免误以为列被切断；4) 资产分析左右分栏 align-items: stretch 导致较矮的右列被拉伸、订单卡片下方出现 300+px 空白，改为 align-items: start，实测卡片下方空白由 347px 降至 0px。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 侧边导航图标去重 —— 修复侧边导航图标重复：资产分析原折线图与全球预览撞款，改为饼图（资产分布语义）；名人持仓原人形图标与用户管理撞款，改为星标（名人语义）；用户管理保留人形、全球预览保留折线图，全站导航图标互不重复。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 批量规范持仓 ETF 名称 —— 批量规范化持仓记录里的冗余/不规范名称（共 9 条）：AAPL2倍做多2x etf→苹果 2 倍做多 ETF、RKLB2倍做多2x etf→Rocket Lab 2 倍做多 ETF、Spcx2倍做多2x etf→SpaceX 2 倍做多 ETF、Tesla2倍做多2x etf→Tesla 2 倍做多 ETF、英伟达2倍做多etf→英伟达 2 倍做多 ETF、Robinhood2倍做多etf→Robinhood 2 倍做多 ETF、超微电脑2倍做多etf→超微电脑 2 倍做多 ETF、太空etf→太空 ETF、标普500etfvanguard→标普 500 ETF（Vanguard），去除「2倍/2x」重复与大小写混乱，统一「名称 + 2 倍做多 ETF」格式，订单表 trade_orders.name 与日志 activities.stock_name 同步更新（共 40 条）；该命名规则已写入 AGENTS.md「股票 / ETF 名称规范」章节。
1. 订单号复制兼容非安全上下文（局域网 IP 不再复制失败） —— 按用户截图修复订单详情弹窗「复制失败，请手动选择订单号」：根因是页面经 http://localhost 等局域网 IP 访问时非安全上下文，navigator.clipboard 不可用。新增 copyText 兼容方案——安全上下文优先 Clipboard API，失败/不可用时回退临时 textarea + document.execCommand('copy')，表格与详情弹层的订单号复制按钮均生效。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 资产分析刷新改为无感无提示 + 刷新图标点击转一圈 —— 按用户要求：1) 资产分析页（账户资产/持仓/交易）刷新不再弹出成功提示（移除「已刷新当前行情」「订单已刷新」等 toast，错误提示保留），刷新无感静默完成；2) 全站 RefreshButton 改为点击时图标旋转一圈（0.6s、单次迭代）的轻量反馈，不再长时间持续旋转，移除各调用点的 spinning 状态传参。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 区域刷新只刷新当前区域（不再连带刷新趋势图） —— 按用户反馈修复资产分析页刷新按钮连带刷新收益率趋势图/总资产趋势图的问题：根因是刷新链路会重载汇率（rates 在趋势图 effect 依赖中）并触发父层重拉持仓记录（positions 变化同样触发），导致趋势图被重跑。修复：账户资产/持仓刷新改为只强制重取当前行情（refreshQuotes force），不再重载汇率、不再触发 records-updated，趋势图状态保持不动；交易刷新仍只重取订单。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 订单表「操作」列固定首位 —— 按用户反馈修复订单表操作列被本地保存的列顺序排到末尾的问题：normalizeOrderColumns 加载列配置时强制把「操作」列移动到第一位（详情/编辑/删除入口始终在第一列），不再受 localStorage 历史顺序影响。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 资产分析页移除返回按钮与蓝色美元图标 —— 资产分析成为独立页签后，顶部标题行保留「资产分析」文字，移除左侧返回按钮（onBack 链路整体清理：AssetAnalysisDashboard Props 去掉 onBack，AssetAnalysisView 与 RecordsApp 同步移除传参）与蓝色渐变美元图标（MoneyIcon 引用一并移除）。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 委托时间日历改为 fixed 智能定位（向上/向下自适应 + 左右不越界 + 锚点三角） —— 按参考图 2 优化委托时间弹窗：由绝对定位（以按钮左缘展开、右侧易被裁切）改为 fixed 视口定位——以「委托时间」按钮为锚点，左右钳制在视口内（宽度 min(680, 视口-16)，left 自动收拢，右侧不再越界被遮挡）、上下自适应（下方空间不足时向上弹出，向上时以 bottom 锚定并加 max-height 内部滚动，防止顶/底部被裁切）；弹窗带小三角指向按钮（向上弹出时指向下方按钮，向下时指向上方），与参考图一致。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 订单表隐藏列后不再变形 + 委托时间自定义日历向上弹出 —— 1) 订单表「需要显示哪些列」隐藏任意列后表格变形（剩余空间被摊到个别列、列宽悬殊）修复：表格改为 table-fixed 固定布局，每列按基准宽度（px）固定列宽，隐藏列后剩余列等比例伸展、窄屏保持横向滚动，列宽不再忽宽忽窄；2) 委托时间自定义双月日历底部显示不全修复：弹层改为优先向下、下方空间不足（<470px）时自动向上弹出（参考图 2），避免被视口底部裁切。tsc 无错误、npm run build 通过、服务重启后页面 200。
1. 收益率·加权选择器改自定义下拉（深色不再白块） —— 按参考图：资产分析「收益率·简单加权 / 时间加权」由原生 `select` 改为自定义下拉——触发器改为透明文字 + 箭头（无边框无白底），展开菜单深色（`dark:bg-[#1b2029]`、细边框、圆角、阴影），选中项蓝色高亮 + 对勾、未选中 hover 浅灰；深色模式下不再弹出白色原生菜单，与全站 `CurrencyPicker`/基准下拉风格统一。tsc 无错误、资产分析页编译加载正常。
1. 收益率趋势图周期行窄屏错位修复（日历按钮不再遮挡） —— 按用户反馈：页面变窄时周期胶囊溢出，日历按钮遮挡后面的胶囊、原生滚动条裸露。修复：周期滚动容器加 `flex-1 min-w-0`（占满剩余宽度、内部滚动，日历按钮固定在右侧永不重叠），并用 `[scrollbar-width:none]` 隐藏横向滚动条；日历按钮去掉 `ml-auto`（由 `flex-1` 容器天然推右），垂直对齐保持 `items-center`。tsc 无错误、资产分析页编译加载正常。
1. 日历按钮恢复为原来的胶囊样式 —— 按用户反馈：将收益率趋势图时间行的日历按钮恢复为原来的胶囊样式（`h-8 w-10 rounded-full` + 日历/对勾图标），不再与周期胶囊同款；滚动区右侧留白 `pr-1` 保留。tsc 无错误、资产分析页编译加载正常。
1. 收益率趋势图日历按钮优化（与周期胶囊同款） —— 按用户反馈：趋势图时间行最右侧日历按钮与左侧周期胶囊形状不一致（圆形 vs 胶囊）且末尾胶囊被截断。优化：日历按钮改为与周期胶囊完全同款（`rounded-full` + `px-3 py-1.5` + 同边框/选中态），图标缩小到 14px；周期滚动区加 `pr-1` 留出呼吸空间，末尾胶囊不再紧贴截断；选中/展开时蓝色高亮与选中周期一致。tsc 无错误、资产分析页编译加载正常。
1. 持仓盈亏排行删除「所选区间 · 前 10 只」并收紧标题行 —— 按用户要求：资产分析-持仓盈亏排行移除副标题「所选区间 · 前 10 只」，标题「持仓盈亏排行」与右侧「更新至 …」改为同一排垂直居中，区块更紧凑不显宽。tsc 无错误、资产分析页编译加载正常。
1. 资产分析市场胶囊去掉悬浮/点击动画 —— 按用户要求：资产分析各市场筛选胶囊（账户资产 / 持仓 / 持仓盈亏排行的 全部·美股·港股·A股）去掉悬浮上浮、阴影与点击缩放的动画（`transition-all duration-200 hover:-translate-y-px hover:shadow-md active:scale`），仅保留选中高亮与 hover 颜色过渡，切换更干净。tsc 无错误、资产分析页编译加载正常。
1. 资产分析字号层级找临界点（页面标题 > 区块标题 > 标签） —— 按用户反馈：资产分析三块（资产分析卡 / 收益率趋势图 / 持仓盈亏排行）文字仍过大。定下层级临界点：页面顶部「资产分析」= 18px `extrabold`（最大）；区块标题（资产分析卡 / 持仓 / 账户资产 / 持仓盈亏排行）= 16px `bold`；趋势图 tab 选中 15px `bold` / 未选中 14px。保持 2px 阶梯，标题有主次、不再臃肿。tsc 无错误、资产分析页编译加载正常。
1. 资产分析-持仓搜索改为与市场标签同一排（参考图2） —— 按参考图：持仓区块改为「标题一行 + 工具一行」——第一行只有「持仓」标题，第二行左侧市场标签、右侧搜索框（胶囊按钮样式 `rounded-full h-8`，与标签同高）+ 设置按钮，全部同一排对齐，不再出现搜索悬浮在标题行、标签沉第二行的错位。tsc 无错误、资产分析页编译加载正常。
1. 资产分析区块标题字号统一为顶部「资产分析」 —— 按用户要求：资产分析各区块标题（持仓 / 账户资产 / 持仓盈亏排行 / 左侧资产分析卡）由 `text-sm font-bold` 统一为 `text-lg font-extrabold`，与页面顶部「资产分析」标题一致；「持仓」行改为 `items-center` 与右侧搜索/设置按钮垂直对齐。tsc 无错误、资产分析页编译加载正常。
1. 资产分析-持仓移除「N 只 · 实时行情」文字 —— 按用户要求：资产分析「持仓」区块标题下的数量与实时行情说明文字移除，标题与搜索/设置按钮保持同行。tsc 无错误、资产分析页编译加载正常。
1. 资产分析-持仓排序持久化（全站排序排查） —— 资产分析「持仓」表格的列排序（`holdingSort`）原来不持久化，刷新即还原。改为 `usePersistedState`（`fire:asset-holdings-sort`）。全站排序排查：我的持仓页排序已存 localStorage（`SORT_STORAGE_KEY`）、素材库排序已走 URL `?sort=&dir=`、自选股筛选/持仓市场等已 URL 持久化，均无需改动；本次补上资产分析这一处。tsc 无错误、资产分析页编译加载正常。
1. 资产分析-账户资产统计卡隐藏遮罩补星号 —— 资产分析右侧「账户资产」一排统计卡（净资产 / 当日盈亏 / 持仓市值 / 浮动盈亏 / 最大购买力 / 可用现金 / 冻结现金）隐藏态仍显示 `••••••`，统一改为 `******`，与全页星号遮罩一致。tsc 无错误、资产分析页编译加载正常。
1. 隐藏遮罩改星号 + 收益率/跑赢数值同步隐藏 —— 按参考图：隐藏态金额由 `••••••` 改为 `******`（星号）；「收益率·简单加权 / 时间加权」选择器下方新增收益率数值（与参考图一致），隐藏时同步显示 `******`；基准条右侧跑赢幅度隐藏时也显示 `******`。收益率等百分比在隐藏状态下不再泄露。tsc 无错误、资产分析页编译加载正常。
1. 货币选择器改为半角括号紧挨效果（参考图2） —— 按用户反馈：资产分析货币选择器（总资产 / 累计盈亏）触发器由全角括号「（CNY）」改为半角括号并紧挨标题「总资产 (CNY)」，箭头与括号间距收紧（`gap-1` + `ml-0.5`），与参考图的紧挨排版一致。tsc 无错误、资产分析页编译加载正常。
1. 货币选择器触发器默认不显示市场/国家图标 —— 按用户澄清：不是隐藏金额，而是货币选择器（总资产 / 累计盈亏）的触发器按钮默认不显示市场/国家图标——「总资产（CNY）」保持纯文字，展开下拉后的选项里保留市场图标（美元🇺🇸/港币🇭🇰/人民币🇨🇳）；金额显示恢复默认可见（`assetsVisible` 撤销默认隐藏改动）。tsc 无错误、资产分析页编译加载正常。
1. 资产分析货币选择加市场/国家图标 —— 按用户要求：资产分析「总资产 / 累计盈亏」货币选择器，下拉菜单每个货币选项前加市场图标（美元→🇺🇸 US、港币→🇭🇰 HK、人民币→🇨🇳 CN，走素材库 `MarketIcon`，自定义市场图标优先），触发按钮也显示当前币种的市场图标，一眼可辨。tsc 无错误、资产分析页编译加载正常。
1. 收益率/总资产趋势图 tab 文字精致化（参考图1） —— 按参考图：趋势图两个 tab 从统一 `text-sm` 改为选中 = 17px 白色加粗、未选中 = 15px 灰色（hover 变白）、tab 间距加大（`gap-6`）、底部蓝色短下划线改为 2px 圆角短横线，贴合图1 的文字层级与精致度。tsc 无错误、资产分析页编译加载正常。
1. 资产分析货币等偏好刷新后保持（全站同类一并修复） —— 资产分析页选择货币后刷新会回到默认：`displayCurrency` 原来只从 prop 初始化且不保存。修复：新增通用持久化 hook [`lib/usePersistedState.ts`](lib/usePersistedState.ts)（localStorage），资产分析页的货币、趋势周期、图表 tab、加权方式、持仓/盈亏市场筛选、订单 tab 全部持久化；货币另写 URL `?cur=` 与我的持仓页共用同一 key（`fire:display-currency`），持仓页货币初始化和切换同步读/写该 key；财报日历的时段与市值筛选也持久化；其余页面已用 URL 持久化的（持仓市场 `?market=`、全球预览货币 `?cur=`、素材库 tab、自选股筛选 `?filter=`）保持不变。刷新/重进页面保持原状。tsc 无错误、`/holdings` 与资产分析页编译加载正常。
1. 资产分析-持仓搜索框与设置按钮缩小协调 —— 按用户反馈：资产分析「持仓」区块的搜索框（`h-10`、宽 190px、圆角 11px）与设置列按钮（`h-10 w-10`）偏大、与整体不协调。优化：搜索框缩为 `h-9`、宽 170px、圆角 9px、字号 13px、放大镜图标同步缩小左移；`HoldingColumnsButton` 缩为 `h-9 w-9`、圆角 9px、漏斗图标 17px；两者间距收窄（`gap-1.5`），视觉统一精致。tsc 无错误、资产分析页编译加载正常。
1. 列设置复选框改为参考图扁平样式（白底黑勾 / 透明浅边框） —— 按参考图细节优化「需要显示哪些列」弹层：原生浏览器灰勾复选框替换为自定义 `FlatCheckbox`——选中 = 白底 + 黑色粗勾（深色模式即图1 效果，浅色模式深底白勾），未选中 = 透明 + 浅灰边框；「全部」与各行统一；标题字号 15px 加粗、关闭 X 提升为 `ink-2`（深色下白色，贴合图1）。tsc 无错误、`/holdings` 与资产分析页编译加载正常。
1. 金钱图标升级为苹果实心硬币风格（dollarsign.circle.fill） —— 按用户反馈：资产分析标题的蓝色磁贴图标点击进入后仍是旧的细线圆圈 $ 样式。现升级为苹果 SF Symbols `dollarsign.circle.fill` 实心硬币风——实心圆 + 挖空的居中 $（`MoneyIcon` 改为 fill + `--money-cut` 挖空色）；蓝色磁贴加苹果式渐变（`from #3f9bff to #2377e6` + 蓝色投影），图标放大到 24px；「账户资产」入口圆形按钮自动同步同一实心风格（深浅色各自用当前背景色挖空）。tsc 无错误、`/holdings` 与资产分析页编译加载正常。
1. 资产分析-持仓：列设置改窄面板浮层 + 苹果风格金钱图标 —— 1) 「需要显示哪些列」由大弹窗（AppModal md，近半屏宽）改为窄面板浮层：`max-w-320px`、紧凑行（`py-1.5` + hover 行高亮）、列表 `max-h-55vh` 可滚动、顶部标题 + 关闭、底部提示一行，贴合参考图的尺寸与密度（约 20-25% 宽）；2) 金钱图标统一为苹果风格（SF Symbols `dollarsign.circle`）：新建共享组件 [`components/MoneyIcon.tsx`](components/MoneyIcon.tsx)（干净圆环 + 居中 $，stroke 1.7），替换资产分析标题蓝色磁贴内与「账户资产」标题右侧入口按钮的原手绘 $ 图标；入口按钮由白底描边方形改为浅灰圆形（`rounded-full`，hover 上浮 + 阴影 + 图标放大，深浅色适配）。tsc 无错误、`/holdings` 与资产分析页编译加载正常。
1. 修复深色模式行悬停变白（全站表格排查） —— 我的持仓表格行深色模式悬停背景变浅灰/白色、文字变深色。根因：`hover:bg-bg-gray/60` 只在 `globals.css` 有静态 `.dark .bg-bg-gray/60` 覆盖，缺少 `.dark .hover:bg-bg-gray/60:hover`，深色下仍用浅灰 `rgb(247 248 250/.6)`。修复：1) 主持仓表（`HoldingsView`）、资产分析（`AssetAnalysisDashboard` 盈亏排名行与持仓表）行悬停补 `dark:hover:bg-[#1b2230]`；2) 全站同类问题——用户管理、日志、API 文档表格行 `hover:bg-[#fafbfc]` 补 `dark:hover:bg-[#1a212e]`；3) 持仓股票名 `group-hover:text-brand-deep` 深色下变深色文字，补 `dark:group-hover:text-[#c6cdd8]`；4) `globals.css` 增加安全网 `.dark .hover:bg-bg-gray/55`、`/60` 与 `.dark .group:hover .group-hover:text-brand-deep`，防止后续回归。全站扫描确认无其他浅色 hover 无深色变体（橙色强调 `#FF9828` 为既有品牌色除外）。tsc 无错误、`/holdings` 深浅色实测正常。
1. 默认端口切换为 3000（不再使用 8124） —— 按用户要求：Web 服务默认端口由 8124 改为 3000。`package.json` 的 `dev`/`start` 脚本本就绑定 `0.0.0.0:3000`，本次把其余 8124 引用全部对齐：`lib/settings.ts` 默认站点域名 `localhost:8124 → localhost:3000`、`scripts/smoke-test.sh` 默认 `BASE → http://localhost:3000`、`fire-web/README` 与根 `README` 与 `AGENTS.md` 的命令/端口说明改为 3000（撤销此前「固定 8124」约定）、`docs/api-spec.md` Base URL → `:3000`、iOS `APIClient.swift` 后端地址 `localhost:8124 → :3000`（`fire-ios/README` 与 `AGENTS` 同步）。历史版本记录不改写；服务已重启监听 3000。

---

## v0.1.7 · 2026-08-13

> 完善资产分析的市场与货币联动，并支持安全删除历史订单。

### 新功能

1. 持仓列全局管理与持仓占比 —— 资产分析持仓新增“持仓占比”、代码 / 名称搜索和苹果风筛选入口；所有列可点击排序、勾选显示隐藏，并通过右侧手柄拖动顺序。配置自动保存并即时同步“我的持仓”主表，两处显示列与顺序保持一致。

1. 资产分析左右布局可拖动 —— 上下区域共用贯穿整列的分隔手柄，可左右拖动调整左侧资产卡片 / 趋势图与右侧账户资产 / 持仓 / 订单宽度。拖动比例自动保存并在下次打开时恢复，双击恢复默认；设有两侧最小宽度，窄屏自动切回单列。

1. 资产分析市场筛选与隐私显示 —— 账户资产与持仓新增两组互不干扰的市场胶囊，账户资产只联动当前市场资产指标，持仓只联动持仓表；胶囊带背景、悬停上浮和点击反馈。总资产、累计盈亏支持 USD / HKD / CNY 切换，总资产旁新增眼睛按钮，可同步隐藏或显示页面资产金额。

1. 资产分析区间盈亏排行与日期筛选 —— 收益率 / 总资产趋势新增双月日历区间选择，星期统一使用中文；日期区间同步驱动曲线、累计盈亏及持仓盈亏排行。排行支持全部、美股、港股、A 股等已有市场切换，默认显示前 10 只，更多持仓可展开或收起。

1. 历史订单安全删除 —— 历史订单新增删除入口和二次确认，删除后按时间重放该股票剩余成交账本，重新计算持仓、成本和已实现盈亏；若会导致后续卖出超卖则自动回滚。`DELETE /api/v1/orders/{id}` 已同步写入 API 文档，供 Web 与 iOS 使用。

### 修复

- **美股 20:00 结算状态自动推进** —— 修复盘后行情在美东 20:00 停止轮询后，页面没有重绘而继续显示未结算的问题。现在每 15 秒只用本地时间校准结算状态，窗口重新聚焦或标签页恢复时立即更新；19:59 仍显示盘后，20:00 起自动显示已结算，次日 04:00 进入新盘前，且不会额外请求行情 API。

- **持仓卖出成本对齐券商摊薄口径** —— 修复部分卖出后成本价与券商平台差距过大的问题：卖出回款改为冲减累计投入并摊薄到剩余持仓，亏损卖出提高剩余成本、盈利卖出降低成本且支持负成本；费用只进入已实现盈亏，避免重复计入。成交订单新增交易前数量与成本快照，订单更正 / 删除可从明确基准重放，不再由成交后快照反推。同步纠正 SPCH 卖单成交价 8.50→8.60，并把成交后成本统一保留 3 位小数；剩余 870 股成本精确恢复为 14.586，按券商截图现价 10.163 计算的浮动盈亏为 -3,848.01（-30.32%）。

1. 资产分析持仓与订单展示 —— 持仓首列统一为“名称 / 代码”，名称在上、代码在下；全站“今日订单”改为“当日订单”，并严格按 `tradedAt` 成交时间归类而不是按添加时间。资产分析页的当日 / 历史订单标签现可正常切换并展示对应成交。

1. 新杠杆 ETF 正股图标 —— 补齐 SMCX→SMCI（超微电脑）、ROBN→HOOD（Robinhood）的统一 ETF / 正股关系；图标和详情反向正股入口均自动复用正股资料，避免新添加 ETF 绕过既有规范。

1. 资产分析市场胶囊布局 —— 修复账户资产市场按钮上半部分被裁切的问题，并将持仓市场筛选独立放到“持仓”标题下方，与账户资产保持相同的标题、筛选、数据布局层级。

1. 历史订单深色主题 —— 移除深色模式下订单行突兀的浅灰背景，改为与卡片一致的深色表面和克制的悬停反馈。

1. 资产分析货币与区间收益口径 —— 总资产、累计盈亏统一为美元 / 港币 / 人民币下拉菜单，移除“离岸人民币”文案；自定义区间按首尾有效收盘价计算持仓盈亏，全部区间使用当前浮动盈亏。简单加权、时间加权和标普 500 基准均按所选区间重新归一，避免货币或日期切换后口径错位。

1. TradingView SVG 图标上传兼容 —— 修复安全校验将 SVG 标准 `xmlns="http://www.w3.org/2000/svg"` 命名空间误判成外部链接，以及将 `<svg>` 根节点前的 `<!-- by TradingView -->` 版权注释误判成非 SVG 的问题。识别器仅跳过 BOM、空白、完整 XML 注释和 XML 声明，再严格确认根节点；继续阻止脚本、事件、远程图片、data URL、HTML 包裹与危险 CSS。校验通过后，素材库保存副本会将来源注释规范为 `<!-- by fire -->`，不修改桌面原文件和 SVG 图形内容。

1. Web 服务固定 8124 端口 —— 开发 / 生产启动脚本统一绑定 `0.0.0.0:8124`，README、AGENTS、冒烟测试、API 规范和默认站点域名同步锁定 8124。以后只运行 `npm run dev`，禁止临时追加 `-p` 参数或切换端口，避免服务正常但访问了旧端口，以及多个 Next 开发进程互相干扰。

1. 持仓个股详情轻量化 —— 点击“我的持仓”股票后不再挂载完整个股详情和 K 线图，直接复用持仓页已加载的实时行情，集中展示最新价、持仓市值、当日 / 累计盈亏、盈亏率、成本和数量，并保留交易与订单追溯。由此移除首屏额外的详情、财务、公司资料及图表请求，持仓二级页更快、更直接；市场标识统一使用素材库市场图标，价格卡和订单标签同步适配深色主题。

1. 美股盘后当日盈亏结算边界 —— 修复美东 16:00 后错误使用当天收盘价作为涨跌基准、导致当日盈亏提前重置的问题。美股盘前、盘中、盘后现在统一相对前一常规交易日收盘价计算，扩展行情从近 5 个交易日分钟数据中提取可靠基准，仅在美东 20:00 后进入结算状态。

## v0.1.6 · 2026-08-12

> 个股详情页用相关 ETF 聚合替换期权占位，并支持进入 ETF 详情。

### 新功能

1. 我的持仓新增资产分析子页面 —— 「账户资产」标题右侧新增钱币图标入口，进入独立资产分析仪表板。左侧集中展示总资产、当日盈亏、持仓市值、总盈亏和多市场资产分布；收益率趋势图 / 总资产趋势图支持本月、近 1 月、近 6 月、本年、近 1 年、全部区间，并提供简单加权 / 时间加权入口及标普 500 对照。趋势按当前持仓数量和真实历史收盘价回溯，不生成随机数据；右侧展示市场资产汇总、实时持仓表和订单账本，深浅色及窄屏同步适配。

1. 成交订单支持安全更正 —— 持仓二级页每笔订单新增“编辑”，可调整买入 / 卖出方向、价格、数量、费用、成交时间与备注；新增 `PUT /api/v1/orders/{id}`，保存时按成交时间重放该股票全部订单，重新计算成交后持仓、成本与已实现盈亏，出现超卖则整次回滚。Web 与 iOS 可共用。

1. 个股详情页新增相关 ETF 聚合 —— 「期权」页签更名为「ETF」，按当前美股标的归拢做多、做空与期权收益 ETF，提供策略筛选、实时价格 / 涨跌幅和点击进入 ETF 详情；返回箭头可回到原股票的 ETF 列表。

1. 持仓二级交易与订单追溯 —— 我的持仓一级页移除买入 / 卖出与全局订单标签，点击股票无刷新进入二级个股详情；详情页提供统一「交易」入口和该股票今日 / 历史订单，交易与订单区域和上方个股详情保持同宽。新增不可变订单账本与 `/api/v1/orders`，成交和持仓原子更新，买入按含费用加权成本，卖出防超卖并记录已实现盈亏，Web / iOS 共用。

### 修复

1. SPCH 与新上市美股 K 线兜底 —— 修复 `SPCH.AM` 等美交所证券没有 K 线的问题：美股历史请求统一剥离交易所后缀；新浪日 K 或 5 日分钟线缺失时自动回退 Yahoo 日线 / 5 分钟线。个股日、周、月、季、年 K 与 5 日走势共用兜底，API 文档同步供 iOS 使用。

1. 美股扩展时段行情与持仓盈亏统一 —— 修复 SPCH 等美股盘前详情价上涨、但自选涨跌幅和持仓当日盈亏仍沿用常规盘跌幅的问题。统一行情接口在美东盘前 / 盘后覆盖有效价格、涨跌额、涨跌幅、时段和昨收；涨跌基准使用最近一次常规盘收盘价，规避扩展行情源对新上市 / 杠杆 ETF 返回复权旧值。Web 列表、个股详情、持仓盈亏与 v1 iOS 接口同步生效。

1. 多市场当日盈亏口径优化 —— 美股、港股、A 股分别按交易所当地交易日计算，单股采用 `(最新价 - 昨收) × 持仓数量`；混合市场先按本币计算再换算汇总。美股覆盖美东盘前 / 盘中 / 盘后，并以美东 20:00 结算冻结当日盈亏；首次进入加载全部市场收盘快照，后续只在对应市场有效交易时段每 30 秒刷新，周末和休市停止行情请求并保留最后有效值；汇率网络异常时使用可靠兜底立即展示，不再长期显示“计算中”。

1. 财报日历今日高亮主题适配 —— 今日整格边框由橙色改为白色；深色模式采用纯白边框、半透明白底和白色柔光外圈，浅色模式采用白色边框、浅灰底和中性灰外圈，确保两种主题下都清晰协调。

1. 财报日历当前日期高亮 —— 当前日期由仅数字小圆圈改为白色整格强调边框、柔和外圈和黑白日期徽标，加载骨架与完成态一致，并补充 `aria-current="date"`；前端首页与后台财报日历复用同一组件，已同步生效。

1. 杠杆 ETF 反向正股关系补齐 —— 修复 `SPCH`、`RKLX` 等历史杠杆 ETF 只有正股图标、详情页却没有对应正股的问题；统一补齐 `RKLX→RKLB`、`SPCH→SPCX`、`MSTU→MSTR`、`SSO/SPUU→SPY`、`QLD→QQQ`，并兼容 `.AM` / `.N` / `.OQ` / `.PS` / `.K` 交易所后缀。图标和详情页现在共用同一关系源，避免同类映射再次遗漏。

1. 个股详情名称与涨跌额显示优化 —— 详情页标题优先使用自选股保存的股票名称，并在右侧单独显示代码，修复相关 ETF 标题重复显示代码的问题；现价继续保留市场货币符号，下面的涨跌额移除重复货币符号，仅显示正负数值和涨跌幅。

1. 自选股编辑支持负成本 —— 修复成本价为负数时无法保存、却错误提示“请填写股票名称和代码”的问题。普通 `/api/records` 与移动端 `/api/v1/records` 现共用持仓校验：成本价允许负数，现价与数量仍禁止负数，并返回准确的字段提示；API 规范同步注明负成本语义。

1. 相关 ETF 与正股双向关联 —— 修复苹果相关 ETF 静态目录仅有 3 只的问题，核实并补齐为 9 只美国上市单股产品；正股显示「ETF」，相关 ETF 反向显示「正股」及其基础股票。ETF 清单、反向关系和全站图标统一从同一映射派生，所有相关 ETF 固定复用正股股票图标。

1. 自选股深色模式悬停背景 —— 修复鼠标经过股票名称单元格时出现突兀浅灰白色块的问题；整行与股票单元格改用对应的暗色悬停层级，保留交互反馈并统一深色表格视觉。

1. 自选股负成本保存 —— 修复编辑股票时负成本被错误归类成“名称和代码缺失”、导致无法保存的问题；成本价允许负值以覆盖返佣、期权收入及累计回款场景，现价和数量继续禁止负数，普通接口与 v1 接口统一校验和准确提示。

---

## v0.1.5 · 2026-08-09

> 完成全站 BUG 排查与安全审计：修复生产构建、认证、上传、文件路径与依赖漏洞。

### 新功能

1. 美股扩展时段分钟行情 —— 接入 Yahoo Finance 盘前/盘中/盘后一分钟数据并按美东时间筛选。AAPL 实测全天 960 点：盘前 330、盘中 391、盘后 239；夜盘因公开源不覆盖时明确提示暂无数据。

1. K 线新增 TradingView 图形类型 —— 图形菜单新增带标记线、阶梯线和 HLC 区域；全天/5天支持线型图，1个月及以上基于完整 OHLC 数据开放 HLC 高低区间填充，与现有面积、折线及三种 K 线共存。

1. 个股财务页首版上线 —— 个股详情「财务」新增财报、收入细分、财务评分、关键指标、利润表、资产负债表、现金流表、股东回报 8 个子页，展示最新季度摘要、营收/净利润趋势、指标表格和环比；美股接入 SEC Company Facts 官方申报数据并做缓存、限流与异常降级，其他市场明确提示待接入。

1. 个股走势图升级为长桥式交互 —— 新增全天分时菜单（全天 / 夜盘 / 盘前 / 盘中 / 盘后）、5天 / 1个月 / 3个月 / 今年至今 / 1年 / 5年 / 最大时间区间，以及山型图 / 折线图切换；深浅色和移动端同步适配。日内数据复用腾讯分钟行情，历史区间复用前复权日线，数据源缺少扩展时段时明确提示而不伪造走势。

1. 素材库新增「分组图标」分类（自定义分组图标归入，非券商） —— 按用户方案：素材库新增「分组图标」tab——列出所有非券商的自定义分组（与素材库券商图标同款行样式：序号 + 相机悬停上传图标 + 清除按钮），上传走 `/api/upload`（`kind=asset` + `folder=group`，文件名按分组名规范命名，存放 `uploads/asset/group/`）再 `PUT /api/v1/watch-groups/{id}` 更新图标（服务端注册素材库 `type=group` 防清理丢失），保存后全局生效（筛选条 / 分组面板 / 管理分组同步）；券商分组（长桥证劵、华泰证劵等有券商图标的）不进入此分类，仍走「券商图标」。自选股管理分组的相机上传同步改用 group 文件夹。`AGENTS.md` 更新分组图标命名规范。tsc 无新错误、`/library` 编译加载正常。
1. 自选股分组方案 A：独立分组实体（Web/iOS 共享服务端） —— 按确认的方案 A 重构自选股分组：1) 数据层——新增 `watch_groups` 表（id/name/icon/sort/visible/kind/market）与 `records.watch_group_id` 列，券商仍走 `group_name`，分组与券商彻底解耦；首次访问自动播种 6 个市场分组并把旧 `records.group_name` 去重迁移为自定义分组。2) 接口层——v1 新增 `GET/POST /api/v1/watch-groups`、`PUT/DELETE /api/v1/watch-groups/{id}`、`POST /api/v1/watch-groups/reorder`、`POST /api/v1/records/group-assign`（批量分配/移出，单条 SQL，重命名/删除不再循环 PUT 记录）；分组图标注册素材库 `type=group` 防清理丢失。3) 前端——`QuotesView` 改为服务端加载分组，筛选条/分组面板（网格+管理：拖动排序/眼睛显隐/相机图标/铅笔重命名/删除/新建）全走新接口；表格新增「分组」列行内标签（点击弹出分组选择快捷分配/移出）+ 批量工具栏「移动到分组」；URL `?filter=` 存分组 id（兼容旧 `M:`/`G:` 与 `?market=` 自动迁移），分组不存在自动回退全部。4) 迁移——旧 localStorage 分组配置一次性同步到服务端并清除。5) 文档——[`docs/api-spec.md`](docs/api-spec.md) 新增 6.3 章节，`AGENTS.md` 更新方案 A 约定。tsc 无错误、`/watchlist` 编译加载正常，接口已端到端实测（创建/查重/分配/移出/重命名/排序/删除/市场删除保护）。
1. 自定义分组行加相机上传分组图标 —— 分组管理-我的分组每行新增相机按钮（悬停即上传，同全站相机风格）：点击选择图片上传（`/api/upload`，`kind=asset` + `folder=broker`，文件名按分组名规范命名），成功后分组图标立即更新并持久化到分组配置 localStorage（`groupIcons` 字段）。图标优先级：自定义上传图标 > 素材库券商图标 > 灰色标签兜底，筛选条胶囊、分组面板网格、管理列表三处同步显示；重命名分组时图标随分组名迁移、删除分组时一并清理。tsc 无错误、`/watchlist` 编译加载正常。
1. 自选股筛选条替换为 iOS 关注页分组逻辑 —— 自选股「我的行情板」原「全部 / 美股 / A股 / 港股」市场胶囊替换为 iOS 关注页同款分组逻辑：1) 筛选条 = 全部 + 固定市场分组（美股/港股/A股/新加坡/日股/韩股，带市场图标与数量）+ 自定义分组（带数量），末尾虚线加号打开分组面板。2) 分组面板两级视图：全部分组网格（不受显隐影响，点选即筛选）→ 管理分组（市场分组与我的分组两个分区，行 = 拖动手柄排序 + 眼睛显隐 + 铅笔重命名 + 删除；自定义分组重命名 / 删除会循环 `PUT /api/records/{id}` 同步后端记录 group 字段，与 iOS 语义一致；市场分组重命名仅改显示名）。3) 新建分组（去重）。4) 分组配置（customGroups/groupOrder/visibility/marketOrder/marketRenames）持久化 localStorage（`fire:watch-groups:v1`，与 iOS AppStorage 语义一致），显隐默认空分组隐藏。5) 筛选状态写入 URL `?filter=`（`ALL` / `M:US` / `G:名称`，兼容旧 `?market=` 自动迁移），刷新前进后退保持；隐藏当前选中分组自动回到全部。新增 [`lib/watchGroups.ts`](lib/watchGroups.ts)（分组状态/顺序/显隐纯函数）与 [`components/WatchGroupSheet.tsx`](components/WatchGroupSheet.tsx)（分组面板），`AGENTS.md` 登记约定。tsc 无错误、`/watchlist` 编译加载正常。
1. 自选股搜索联想加关注爱心按钮（点击关注 / 再点取消） —— 自选股页「股票添加」搜索框的联想结果每行右侧新增长桥爱心关注按钮：灰色=未关注、红色=已关注（沿用 `heart-pop` 弹跳动效），悬停提示 关注 / 取消关注；点击直接加入或移出自选股（已存在记录走静默删除、不存在走搜索添加，含按市场默认券商），不触发行选择；请求期间该行爱心禁用防重复点击。实现：`StockSearch` 新增可选 `followed` / `onToggleFollow` props（其他页面用法不受影响），行结构由整行 button 改为「主内容按钮 + 关注按钮」同级布局，避免嵌套 button 的 hydration 错误；`QuotesView` 传入 records 判断关注态 + `onToggleWatch`/`onAddMatch` 实现切换。tsc 无错误、`/watchlist` 编译加载正常。
1. 详情页爱心改为「关注」语义并真实联动自选股 —— 个股详情头部爱心按钮语义从「收藏」改为「关注」：悬停提示为 关注 / 取消关注（`aria-label` 同步）。逻辑接通真实数据——进入详情时按该股是否在自选股初始化爱心状态（自选股进入即红色已关注），点击爱心调用 `RecordsApp` 新增 `toggleWatch`：取消关注 = 静默 `DELETE /api/records/{id}`（不走删除确认弹窗）并从列表/行情移除，重新关注 = 沿用搜索添加的最小字段（现价取自实时行情、按市场默认券商 美股→长桥证劵 / 港股A股→华泰证劵）POST 重新加入自选股；请求期间按钮防重复点击，失败自动回滚爱心状态。回调链路 `StockDetailView(onToggleFollow) ← QuotesView(onToggleWatch) ← WatchlistView ← RecordsApp`，tsc 无错误、`/watchlist` 详情编译加载正常。
1. 多币种市值弹窗加货币国家图标 + 行悬停动画；收藏爱心换成长桥官方 SVG —— 1) 多币种市值弹窗每行左侧新增货币对应国家/地区图标（用素材库市场图标 `MarketIcon`：美元🇺🇸/港元🇭🇰/人民币🇨🇳/新加坡元🇸🇬/日元🇯🇵/韩元🇰🇷，素材库有自定义市场图标时优先）；`MULTI_CURRENCIES` 增加 `market`/`flag` 字段（[`lib/currency.ts`](lib/currency.ts)，Web/iOS 契约同步）。2) 币种行悬停动画：整行上浮 1px + 浅灰背景 + 轻阴影 + 点击微缩（`transition-all duration-200`），左侧市场图标随行 hover 放大 1.1 倍并轻微 -6° 旋转（`group-hover`，300ms 缓动），选中行保持深灰高亮不响应 hover。3) 个股详情头部收藏爱心替换为长桥官方 SVG 路径（`viewBox 0 0 1136.32 1024`，爱心 + 右侧加号造型）：未收藏灰色空心描边（`strokeWidth 85` 对应 18px 下约 1.3px 视觉描边）、收藏后填充红色 `#ef4444`，保留 `heart-pop` 弹跳 + 六粒红色迸发动效。tsc 无错误、`/watchlist` 详情编译加载正常。
1. 个股详情页改左对齐（长桥风格）+ 收藏改为带动画爱心 —— 1) 个股详情页（`StockDetailView`）由 `mx-auto` 居中改为靠左显示（`w-full max-w-[620px]`，去掉 `mx-auto`），与长桥个股页主内容居左的布局一致，避免宽屏下内容被两边大留白夹在中间。2) 头部收藏按钮由金色五角星改为爱心：未收藏为灰色空心、收藏后填充红色 `#ef4444`，并新增 CSS 动效——点击爱心弹跳（`heart-pop`：放大 1.4 倍回弹）+ 六颗红色微粒向外迸发消散（`heart-burst`，径向 6 个方向），深浅色模式均正常，点击即时反馈。tsc 无错误、`/watchlist` 详情编译加载正常。
1. 个股页规范化：新增 v1 个股详情接口（Web / iOS 共用契约） —— 为后续 iOS App 调用沉淀个股页数据契约：1) 新增 `GET /api/v1/stock-detail?market=&code=` —— 一次请求返回完整实时行情（今开/最高/最低/昨收/量额/市盈率/换手率/总市值）+ 六币种市值（USD/HKD/CNY/SGD/JPY/KRW，本地币种原始值、其余按汇率换算）+ 对 USD 汇率 + 日 K（前复权，最多 320 条）；行情/汇率/K 线三路并行互不阻塞，单路失败只缺对应字段仍返回 `code: 0`；无市值 ETF 等 `marketCap` 返回 `null`。2) 抽取共享日 K 数据层 [`lib/kline.ts`](lib/kline.ts)（`tencentSymbol` / `parseRows` / `fetchDailyKline`，10 分钟缓存），`/api/kline/full` 与 v1 stock-detail 同一解析规则；币种换算抽到 [`lib/currency.ts`](lib/currency.ts)（`MARKET_CURRENCY` / `MULTI_CURRENCIES` / `usdCap` / `multiCurrencyCap`），`useRates` 改为再导出。3) 网页个股详情改走 v1 契约（`StockDetailView` 由 `POST /api/quotes` + `useRates` 改为单次 `/api/v1/stock-detail`），与 iOS 同源。4) [`docs/api-spec.md`](docs/api-spec.md) 新增「6.2 个股详情 Stock Detail」章节（请求/响应示例/字段说明/周月聚合约定）。tsc 无错误（仅历史 index-kline 遗留类型缓存）。
1. 多币种市值弹层：加市场图标 + 行 hover + 位置改为居中弹窗 —— 个股详情-多币种市值弹层优化：1) 标题行加当前股票的市场图标（素材库市场图标，回退国旗）+ 股票代码，一眼可知是哪只股票哪个市场；2) 币种行鼠标划过有浅灰背景（`hover:bg-brand-hover`，深色模式同步），选中行保持深灰高亮；3) 弹层从贴底的全宽 bottom sheet（`items-end` + 圆角仅顶部）改为视口居中弹窗（`items-center` + `p-4` + `max-w-[400px]` + 四角圆角），不再靠左贴底，深浅色模式均正常。tsc 无错误。
1. 自选股移除券商列 + 股票图标修复 + 我的持仓补券商列 —— 1) 自选股（QuotesView）表格移除「券商」列（表头与行内分组标签），券商仅在我的持仓展示；编辑表单的券商字段保留（数据模型统一，自选Fire录仍可设置但表格不展示）。2) 自选股股票图标修复：股票列原误用 `MarketIcon`（市场图标 / 国旗）渲染，改为素材库股票图标（`useAssetIcons.stockIcons` 按 市场:代码 匹配，无图标回退名称首字母圆形），与全站股票图标一致。3) 我的持仓（HoldingsView）持仓明细表格新增「券商」列：显示券商名称 + 素材库券商图标（无图标首字母兜底，未设置显示 —），与编辑表单一致。tsc 无错误、`/watchlist` 与 `/holdings` 编译加载正常。

### 安全修复

1. 全站安全审计与加固（本轮） —— 全量复核认证/授权/会话/CSRF/SQL注入/上传/XSS/SSRF/依赖等安全面并修复：1) 依赖漏洞：`nanoid` 由 3.3.17 升至 3.3.18（高危：size=0 时自定义生成器死循环 DoS），`npm audit --omit=dev` 归零；2) 名人 CIK 强制 10 位数字校验（管理端创建/更新均拦截），防止注入 SEC 查询路径；3) 设置页数据源地址（quoteApiUrl 等 8 个）仅允许 `http(s)://` 开头，杜绝 `file://` 与内网探测类配置；4) 清理全站 `.DS_Store`（public/uploads、data、备份内共 20+ 处），`.gitignore` 已全局排除；5) 复核确认：scrypt 加盐密码 + 恒定时间比较、会话 token SHA-256 摘要落库、Cookie httpOnly + SameSite=Lax + 生产 Secure、Cookie 写请求 Origin/Sec-Fetch-Site 同源校验、登录/注册/改密/行情/搜索/K线/素材搜索全链路 IP+全局双限流、记录/订单/分组按 user_id 隔离无越权、上传扩展名白名单 + 魔数嗅探 + SVG 消毒、SQL 全参数化、CSP/X-Frame-Options/nosniff/HSTS 安全头、管理员接口全部鉴权、生产环境禁用默认 demo 管理员。tsc 无错误、`npm run build` 通过、npm audit 0 漏洞、新校验端到端实测（400 拒绝且不落库）。
1. 全站安全审计与纵深加固 —— 完成认证、权限、数据归属、上传、文件系统、公开接口、出站请求、响应头、依赖与敏感配置审计：1) 会话令牌改为 SHA-256 摘要落库，旧会话首次使用无感迁移；Bearer 显式认证优先于残留 Cookie，Cookie 写请求增加 Origin / Sec-Fetch-Site 同源校验；登录统一执行 scrypt 降低用户名枚举时间差，生产环境禁用仍使用公开默认密码的 demo 管理员，空库首次启动必须通过 `INITIAL_ADMIN_USERNAME` / `INITIAL_ADMIN_PASSWORD` 创建管理员。2) 上传按图片魔数校验真实内容，SVG 统一消毒；名人头像与自动下载图标同步覆盖，远程图标仅允许 CoinGecko / CompaniesMarketCap HTTPS 域名，逐跳校验重定向并限制 2MB，阻断伪装文件、存储型 SVG 与 SSRF。3) 本地素材路径使用 `resolve` + public 根目录边界校验，阻断编码路径穿越；数据库、备份与配置权限收紧为目录 0700 / 敏感文件 0600，`.gitignore` 排除数据库、备份与 `.env`。4) 防止最后一个管理员被降级，记录价格/成本/数量拒绝负数，代码白名单化，批量删除/分组/排序增加去重与数量上限，分组 icon/visible 严格校验。5) 生产响应新增 HSTS，API 统一 `no-store`；npm 锁定 `postcss 8.5.26` / `sharp 0.35.3` / `nanoid 3.3.17`，生产依赖审计由 4 high 降为 0。

### 修复

1. 个股扩展行情与全天走势图修复 —— 修正腾讯换手率/振幅字段映射，新增 EPS TTM、52周高低、市净率、股息率、量比、总/流通股本、静态 PE、股息、均价等真实字段；全天在单日源为空时自动使用五日分钟行情的最新交易日数据。

1. 走势图周期栏简化 —— 移除 3个月，将 1个月改为月K并新增周K；周K/月K基于日线真实聚合 OHLC 与成交量。历史区间增加首尾留白，修复“最大”最左侧日期显示不全。

1. 个股 5 日走势数据修复 —— 原实现错误使用最近 5 根日 K 收盘价，仅有 5 个点；现改为美股五日分钟行情，每个交易日 391 个一分钟点并同步成交量。AAPL 实测返回 1955 点、覆盖 5 个完整交易日，走势粒度与券商五日分时语义一致。

1. 修复生产构建与 v1 行情兼容问题 —— 修复 `/api/v1/index-kline` 非法导出 `INDEX_MAP` 导致 Next.js 生产构建失败；v1 月 K 增加市场白名单并正确支持日股 `jp` 与韩股 `kr` 腾讯符号（不再误走美股分支）；素材库券商 alias 类型补齐，健康检查版本改为自动读取当前版本，避免硬编码停留在 v0.1.4。

1. API 规范整理：文档结构归位 + 分组接口补全示例 + 实现统一 —— 1) [`docs/api-spec.md`](docs/api-spec.md) 结构整理：此前券商模型/示例内容游离在 6.3 之后，现归位到「6.1 券商 Brokers」；「6.3 自选股分组」补全各接口规范——GET 列表 / POST 新建 / PUT 更新（name/icon/visible，icon 空串=清除）/ DELETE / POST reorder / POST records/group-assign 的请求与响应示例、错误码（40001/40901/40101/42901）、图标上传链路与素材库分组图标分类约定，全部走统一 v1 信封 `{ code, message, data }`。2) 实现统一：`POST /api/v1/watch-groups/reorder` 返回 `{ updated }`（原 `{ ok: true }`），`[id]` 路由限流 key 规范化。tsc 无错误、`/api-docs` 实时渲染验证通过。
1. 自选股编辑弹窗「券商」改为「分组」（逻辑理顺） —— 方案 A 后自选股分组已是独立实体（`watchGroupId`），编辑弹窗仍显示「券商」会误导用户。现改为「分组」下拉：列出自定义分组 + 未分组，保存时写 `watchGroupId`；券商字段在自选股编辑中保留原值不再改动（券商归属我的持仓，持仓页编辑仍可改券商）。移除 `GroupSelect` 依赖与 `groupOptions`。逻辑确认：自选股编辑 = 改分组，持仓编辑 = 改券商，职责清晰。tsc 无错误、`/watchlist` 编译加载正常。
1. 修复分组相机点击无反应 + 相机悬停动画 —— 根因：管理分组行整体 `draggable`，HTML5 拖拽会吃掉行内所有按钮（相机/眼睛/铅笔/删除）的点击。修复：`onDragStart` 校验拖动必须由 `.drag-handle`（拖动手柄）发起，非手柄区域直接 `preventDefault`，行内按钮点击恢复正常（眼睛显隐/铅笔重命名/删除一并受益）。相机交互升级为与全站股票图标相机一致：28px 圆形容器（ring 细描边），悬停图标放大 1.1 倍 + ring 高亮 + 半透明遮罩相机淡入（300ms），点击即弹出文件选择；市场分组图标只读。tsc 无错误、`/watchlist` 编译加载正常。
1. 分组相机改为叠加在图标上（悬停出现） —— 按用户反馈：分组管理-我的分组行的相机按钮移除，改为相机直接叠加在分组图标上——悬停图标出现半透明遮罩 + 相机图标（同全站股票图标相机风格），点击即上传；市场分组图标只读无相机。tsc 无错误、`/watchlist` 编译加载正常。
1. 自选股表格隐藏「分组」列（分组筛选已在上方） —— 按用户反馈：自选股表格移除整列「分组」（表头与行内分组标签），分组管理统一由上方筛选条 + 加号分组面板承担；批量「移动到分组」按钮保留（多选后仍可批量分配）。移除 `RowGroupTag` 组件。tsc 无错误、`/watchlist` 编译加载正常。
1. 分组面板补「管理分组 / 新建分组」入口 + 自定义分组显示券商图标 —— 按用户反馈修复分组面板：1) 全部分组网格底部新增操作栏（参考 iOS 关注页分组面板）：左侧「管理分组」齿轮按钮进入管理视图（此前只有管理视图没有入口，导致找不到编辑分组）、右侧「新建分组」加号按钮直达管理视图的新建输入；2) 自定义分组行（长桥证劵 / 华泰证劵等）与筛选条分组胶囊显示对应券商图标（`brokerIcons` 按分组名匹配素材库券商图标，无图标回退灰色标签），与持仓页券商图标一致。`WatchGroupSheet` 新增 `brokerIcons` prop，`QuotesView` 传入并同步在筛选条渲染。tsc 无错误、`/watchlist` 编译加载正常。
1. 返回按钮移入详情头部行（股票图标左侧） —— 按用户反馈修正：返回按钮不再单独占一行（此前在详情上方显得突兀），改为 `StockDetailView` 新增 `onBack` 回调、在头部行内、股票图标左侧渲染圆形浅灰箭头按钮（`-ml-1` 微调视觉对齐，`h-9 w-9` 与股票图标同高），返回行情板后仍正确清理 URL `symbol` 并恢复列表；无 `onBack` 时（如未来前端弹窗用法）自动不渲染。tsc 无错误、`/watchlist` 详情编译加载正常。
1. 个股详情返回按钮改圆形浅灰箭头 + 收藏爱心继续美化 —— 1) 详情页左上角返回按钮改为截图样式：圆形浅灰底（`bg-bg-gray`）+ 深色左箭头，去掉「返回行情板」文字（`aria-label`/`title` 保留无障碍提示），hover 上浮 + 浅灰加深 + 点击微缩（`transition-all duration-200`）。2) 收藏爱心继续美化：长桥爱心由空心描边（复杂路径小尺寸描边发糊）改为实心填充——未收藏为灰色（`currentColor` + `opacity-75`）、收藏后填充红色 `#ef4444`；`viewBox` 裁剪到路径实际包围盒（`49.42 40.42 1086.9 948.55`）使爱心在相同像素下更大更清晰，尺寸由 18px 提到 20px；按钮 hover 时爱心放大 1.1 倍，保留收藏弹跳（`heart-pop`）+ 六粒红色迸发动效。tsc 无错误、`/watchlist` 详情编译加载正常。
1. 个股详情页隐藏顶部大盘指数卡片 —— 自选股（`/watchlist`）点击股票进入个股详情时，顶部的大盘指数卡片（恐惧与贪婪 / 美股 / 港股 / A股）整块隐藏，详情页不再被指数区块挤压、更干净；返回行情板后恢复显示。实现：`QuotesView` 新增 `onDetailChange` 回调（详情开合通知外层），`WatchlistView` 用 `detailOpen` 状态条件渲染指数卡片，并用 URL `?symbol=` 初始化首帧状态（刷新时也不闪现卡片）。tsc 无错误、`/watchlist` 详情与返回均正常。
1. K 线图成交量改为「万」单位并简化 —— 个股详情 K 线 tooltip 的「量」由 M（百万，1 位小数）改为「万」：新增 `fmtVol` 格式化（`v/1e4`，≥100 万取整、小于 100 万保留 1 位小数），如 AAPL 3444万、A 股 600519 2.5万（原 A 股按 M 显示会变成 0.0M，单位错误一并修正），数值更简短直观。tsc 无错误。
1. 修复 K 线图 tooltip 开盘价错位（ECharts 6 分类轴索引混入 value） —— 个股详情 K 线 tooltip 的开盘价显示错误：ECharts 6 对数组数据项会在 `params.value` 最前面附加分类轴索引（如 `[59, 311.45, 313.33, 310.74, 314.81]`），原代码直接按 `[o,c,l,h]` 解构，导致「开」显示序号（59）、「高」显示真实开盘、「低」显示真实收盘、「收」显示真实最低、真实最高丢失。修复为按 `params.dataIndex` 回查聚合后的 `ohlc` / `vols` 数组取值（不依赖 ECharts 内部维度布局），并同步校验数据链路（新浪美股 / 腾讯 A股港股 K 线逐字段与实时行情一致）、蜡烛几何（影线触及高低、实体开收位置正确）。另为 candlestick 系列加 `barMinWidth: 2`：ECharts 6 蜡烛宽度 ≤1.3px 时进入 simple-box 模式只画影线不画实体，小屏 / 150 根 K 线时保证实体与高低点完整可见。补全 `lib/types.ts` `Quote.open` 类型声明（运行数据本就含今开，仅类型缺失）。tsc 仅剩素材库 alias 等历史类型错误，`StockKline` 无错误。
1. 我的持仓股票图标修正为素材库图标 —— 我的持仓（HoldingsView）持仓明细「股票」列名称左侧新增股票图标：与自选股一致，使用素材库股票图标（`useAssetIcons.stockIcons` 按 市场:代码 匹配，无图标回退名称首字母圆形），不再仅以小字 `MarketIcon` 表示市场；名称下方小字保留市场图标（国旗）+ 代码 · 市场标签用于市场标识。tsc 无错误、`/holdings` 编译加载正常。
1. 自选股/我的持仓股票列改为素材库股票图标行样式 —— 自选股（QuotesView）与我的持仓（HoldingsView）的「股票」列统一改为素材库-股票图标行的视觉效果：36px 圆形股票图标（素材库 `stockIcons` 按 市场:代码 匹配，无图标回退名称首字母）+ 右侧两行左对齐文字（第一行名称粗体、第二行代码灰色小字），移除原「名称 + 小字 代码 · 市场标签」与行内市场图标；市场信息仍由表格市场筛选与各列货币体现。与素材库股票图标列表观感一致（英伟达 / NVDA 同款）。tsc 无错误、`/watchlist` 与 `/holdings` 编译加载正常。
1. 我的持仓备注列隐藏、券商移至备注位置 + 序号列标题修正 —— 1) 我的持仓（HoldingsView）持仓明细表格暂时隐藏「备注」列（表头与行内备注单元格移除），「券商」列从股票列后移至原备注列位置，列顺序为：序号 / 股票 / 现价 / 当日盈亏 / 成本价 / 数量 / 市值 / 持仓盈亏 / 盈亏率 / 券商 / 操作，表头与行内 11 列完全对齐；券商单元格样式保留（名称 + 素材库券商图标，未设置显示 —）。2) 我的持仓与自选股（QuotesView）的「#」列表头统一改为「序号」（行内本就显示跨页连续编号），语义更明确。tsc 无错误、`/holdings` 与 `/watchlist` 编译加载正常。
1. 自选股支持个股行内移动至分组 —— 自选股行情板每只股票的操作栏新增独立「移动」下拉：无需勾选批量或打开编辑弹窗，即可将当前个股移入任意自定义分组，也可直接移出分组；复用 `/api/v1/records/group-assign` 接口，保存成功后实时刷新记录与分组数量，未创建自定义分组时操作自动禁用并提示先新建分组。tsc 无错误、`/watchlist` 编译加载正常。
1. API 文档页升级为开发者中心视觉 —— `/api-docs` 阅读与编辑页面统一升级：新增开发者中心头部、文档状态徽章与章节/接口/行数统计；桌面端目录卡片强化层级与滚动体验，移动端新增横向章节导航；正文卡片、标题锚点、引用、代码块、表格悬停和编辑预览统一精致化，并补齐浅色/深色背景与响应式适配。tsc 无错误、生产构建通过。
1. API 文档目录与表格布局继续优化 —— 修正目录树：将 6.1/6.2/6.3 自动归入 6 章节，主章节从 10 个收敛为 7 个，展开后再显示路由与子章节，长标题改为可换行；Markdown 表格新增全宽滚动容器，避免表格被压缩在正文左侧；6.1/6.2/6.3 正文标题降为清晰的子章节层级。tsc 无错误、生产构建与浏览器页面检查通过。
1. API 文档目录增加章节虚线分隔 —— 左侧目录每个主章节增加低对比度虚线分隔，选中态、展开态与深色模式保持一致，提升长目录的阅读节奏。tsc 无错误、`/api-docs` 加载正常。
1. API 文档复制按钮与主题切换优化 —— 代码块复制控件改为圆角方形图标按钮，悬停显示复制提示、点击后切换为「已复制」状态；API 文档顶部新增全站共用的浅色/深色主题切换按钮，目录、正文、代码块与复制控件同步适配。
1. API 文档行内复制悬停指针优化 —— 移除行内 URL / 接口路径代码悬停时系统 copy 指针显示的绿色加号，改为普通指针；虚线提示、点击复制与成功 Toast 保持不变。
1. API 文档接口方法示例取消行内复制 —— `GET /api/...`、`POST /api/...` 等方法 + 路径的接口说明不再显示复制虚线与复制提示；独立 API 路径、完整 URL 与 `Authorization Bearer` 仍可复制。
1. 设置页统一控制台 UI —— 设置页重构为统一的控制中心视觉：新增状态概览头部、版本与角色徽标、记录/导航摘要、统一侧栏与横向导航、玻璃质感卡片层级和深色主题覆盖；保留原有配置逻辑与响应式布局，桌面/移动端均可用。tsc 无错误、生产构建通过。
1. 设置页移除概览摘要卡片 —— 移除设置中心头部的记录、导航项、角色三张摘要卡片，保留核心标题、运行状态、版本徽标与布局切换，减少视觉噪音。
1. 美股 2 倍做多 ETF 图标默认使用主体股票图标（全站兜底） —— 美股 2 倍做多 ETF（RKLX/AAPU/TSLL/SPCH 等）在素材库无自有图标时默认显示主体股票图标：`lib/useAssetIcons.ts` 新增 `LEVERAGED_2X_MAIN` 映射（RKLX→RKLB、AAPU→AAPL、TSLL→TSLA、SPCH→SPCX（SpaceX 已上市，代码 SPCX，素材库已有 SpaceXSPCX.png）、NVDL→NVDA、MSTU→MSTR、CONL→COIN、SSO→SPY、QLD→QQQ、SPUU→SPY），构建 `stockIcons` 时自动兜底，并覆盖常见交易所后缀（.AM/.N/.OQ/.PS/.K，如 SPCH.AM → SPCX）；素材库股票 tab 的 `displayUrl` 同步接入同一兜底，全站（自选股 / 我的持仓 / 素材库 / 名人持仓）统一生效；主体未上市或图标缺失时保持首字母兜底。`AGENTS.md` 写入「美股 2 倍做多 ETF 图标兜底规范」。tsc 无错误、`/watchlist` /holdings /library 编译加载正常。
1. 素材库股票行情补全 + 修复腾讯批量行情失效（全站现价恢复） —— 1) 素材库股票图标「市值/最新价/涨跌幅」补全：新增 `scripts/backfill-quotes.mjs`（含日股 `jp{code}`、韩股 `kr{code}` 前缀支持），为 433 只缺失现价的股票批量回填腾讯行情（成功 413 只，剩余 11 只为停牌 / 冷门股如宇树科技、JP REIT、AXIA_C 等腾讯与东财均无有效行情）；7 只手动添加资产（VOO/IVV/VTI/BRK.B/DJT/TLT/曙光数创）补齐市值（BRK.B 1125 亿 / DJT 28.3 亿 / 曙光数创 125.1 亿为东财实际值，ETF 用当前基金净资产近似）+ 现价 + 涨跌幅，素材库最后一页不再出现空数据。2) 重要修复——腾讯行情接口批量查询已失效（每次请求只返回第一条，实测 US/HK/CN/JP/KR 全部如此），导致全站实时行情（自选股 / 我的持仓 / 财报日历图标价等）只更新第一只：`lib/quotes.ts fetchBatch` 改为并发逐条（并发 6），`fetchQuotes` 与财报、走势等批量调用全部恢复，实测 `/api/quotes` 多市场多只股票正确返回。`AGENTS.md` 记录腾讯批量失效教训：行情批量必须逐条并发。tsc 无错误、`/library` 编译加载正常。
1. K 线组件准备：默认 ECharts + TradingView 备选（双引擎可切换） —— 按选型准备 K 线组件 `components/StockKline.tsx`：默认引擎 ECharts（6.1.0，candlestick + 成交量 + dataZoom 滚轮缩放 + 十字线 tooltip + 日/周/月周期切换 + 深浅色自适应），右上角提供「ECharts / TradingView」引擎切换按钮，TradingView 为 iframe 备选（symbol 自动映射：美股裸代码、HKEX / SSE / SZSE / BSE / TSE / KRX，周期联动，失败可切回）。新增日 K 接口 `/api/kline/full`（10 分钟缓存）：美股走新浪日 K（250 条含量）、港股 / A股走腾讯前复权（320 条）、日股 / 韩股腾讯数据不全（<3 条时前端提示可切 TradingView）。用法：`<StockKline market="US" code="AAPL" name="苹果" />`，待自选股页规划后接入。tsc 无错误。
1. 自选股-我的行情板：个股详情页（moomoo 风格） —— 自选股（QuotesView）每只股票点击进入个股详情弹窗（AppModal xl，moomoo 网页详情布局）：1) 头部——股票名大字 + 代码 + 市场国旗 + 收藏星标，副标题显示行情更新时间，核心区左侧特大现价（红涨绿跌 + 涨跌箭头 + 涨跌额/幅）、右侧 总市值 / 市盈率TTM 指标 + 灰色圆角「双箭头延伸」按钮；2) 点击延伸平滑展开更多指标三列网格（总市值 / 市盈率TTM / 今开 / 今高 / 今低 / 成交量 / 成交额 / 换手率 / 振幅 / 每手 / 历史高低占位），箭头旋转收起；3) K 线区上方 tab 暂时只保留 概览 / 期权 / 财务 / 公司（选中项下方黑色短横线高亮，其余为占位「内容规划中」），概览 tab 显示「代码 实时股价」标题 + `StockKline` 双引擎 K 线。`Quote` 类型扩展成交量 / 成交额 / 市盈率 / 换手率 / 总市值（腾讯 f36/f37/f39/f43/f44，市值 ×1e8 本地货币），详情头部与指标全部真实数据（市盈率 / 市值直接取自行情）。盘后行情行因无数据源暂未展示。tsc 无错误、`/watchlist` 编译加载正常。
1. 个股详情交互规范：后端无感进入视图（URL 同步） —— 确立个股详情交互规范（写入 `AGENTS.md`）：前端（首页 / 门户）个股点击一律用弹窗（`StockDetailView`，moomoo 风格）；后端（自选股 / 我的持仓 / 行情板）个股点击一律**无感进入详情视图**——不弹窗、不整页刷新，当前视图 fade 过渡切换为详情页 + 左上角「返回行情板」，并同步 URL（`?symbol=市场:代码`，刷新 / 前进后退保持）。后端自选股详情已按此改造：点击股票行 → `pushState` 写入 symbol 并切换视图，返回按钮 `replaceState` 清除 symbol，`popstate` 监听恢复 / 退出；详情行情用实时 quote 引用（`quotes[id]`），不是打开时的快照。tsc 无错误、`/watchlist` 列表与详情 URL 均编译加载正常。
1. 个股详情页改为 2:1 左右分栏（限宽居中） —— 个股详情页不再全宽：整体 `max-w-1100px` 居中；桌面端左右 2:1 分栏——左 2/3 为 K 线主卡（概览/期权/财务/公司 tab + 双引擎 K 线 430px），右 1/3 为现价/指标卡（名称 + 代码 + 国旗 + 收藏、更新时间、特大现价 + 涨跌箭头、总市值/市盈率TTM + 双箭头延伸按钮，延伸指标网格改为两列适配窄栏）；移动端右栏（现价/指标）置顶、K 线随后（order 切换）。tsc 无错误、详情 URL 编译加载正常。
1. 个股详情页恢复单栏布局（宽度缩为原来一半） —— 按用户反馈撤销 2:1 分栏：恢复原来的单栏上下布局（头部 → 现价 + 指标 + 双箭头延伸 → K 线区 tab），整体宽度缩为原来一半并居中（`max-w-620px`），延伸指标网格保持两列。tsc 无错误、详情 URL 编译加载正常。
1. 详情页头部：股票名称前加股票图标，市场图标移到收藏左边 —— 个股详情页头部调整为：股票图标（素材库 `stockIcons` 按 市场:代码 匹配，36px 圆形，无图标回退名称首字母）+ 股票名 + 代码，右侧 `ml-auto` 依次为 市场图标（国旗）+ 收藏星标。tsc 无错误、详情 URL 编译加载正常。
1. 修复详情页总市值/市盈率与 K 线成交量不显示 —— 1) 总市值 / 市盈率TTM：详情页此前依赖列表传入的 quotes（可能未含新增字段），现改为详情页独立拉取单只实时行情（`POST /api/quotes`，含 volume/amount/pe/turnover/marketCap 完整字段），并补全 `lib/types.ts` 的 `Quote` 字段声明，实测 AAPL pe 35.93 / 市值 4.57 万亿正常显示；2) K 线成交量：修复 tooltip 成交量 NaN（bar value 是数字，误取 `value[1]`），并把日 K 数据量从 400 根降到 250 根 + 默认只显示最近约 150 根（此前每根蜡烛/成交量柱不足 1px 视觉上消失），实测 250 根全部带成交量。tsc 无错误、详情 URL 编译加载正常。
1. 详情页指标区改 moomoo 4 列网格 + 总市值简化 + 多币种市值弹层 —— 1) 指标区改为 moomoo 样式 4 列网格（标签上 / 数值下）：最高·最低 / 今开·昨收 / 换手率·成交额 / 市盈率TTM·总市值{币种}，移除原延伸按钮与延伸网格；2) 新增 `fmtCap` 大数简化（万亿 / 亿 / 万，如 4.57 万亿、306.17 亿），总市值、成交额不再显示超长数字；3) 总市值标签右侧三点按钮 → 底部弹出「多币种市值」弹层：美元 / 港元 / 人民币 / 新加坡元 / 日元 / 韩元 各币种换算市值 + 汇率小字（1 HKD = 0.1274 USD，走 `useRates` / `FALLBACK_RATES`），当前币种深灰高亮，底部「当天非实时汇率，仅供参考」提示 + 白色「知道了」按钮。导出 `MARKET_CURRENCY` 供复用。tsc 无错误、详情 URL 编译加载正常。

---

## v0.1.4 · 2026-08-08

> 名人持仓 F3：F1 下半身 + F2 上半身融合版——透明头像拆双层 mask，额头从洞里探出压住上方扇形（F2 效果），领带下方被圆环环带遮挡（F1 效果），深浅色模式均正常。

### 新功能

1. 名人持仓 F3：F1 下半身 + F2 上半身融合版头像 —— 扇形圆环人物头像融合两版优点：透明头像拆成双层渲染——下层在圆环之下、mask 只显示身体下半部分（`transparent 40% → rgba(.4) 50% → #000 58% → 100%`），领带 / 西装下方被圆环环带遮挡、不外突（F1 效果）；上层在圆环之上、mask 只显示头部区域（`#000 0-50% → rgba(.8) 54% → rgba(.3) 58% → transparent 62%`），额头 / 头顶从洞里探出压住上方扇形（F2 效果）。头像尺寸 0.74、上移 6px（悬停 8px），上下层同位移同过渡无拼接痕迹；非透明头像维持单层圆形裁剪。已备份 `docs/celebs-ring/celebs-ring-F3.tsx` 并在 `AGENTS.md` 登记恢复方法；浅色 / 深色模式均实测正常（头顶明显探出压住扇形、衣领以下被圆环包围、无黑圈发暗）。
2. kline v1 接口升级：带日期的月收盘（供 iOS 资产 / 盈利历史曲线）—— `GET /api/v1/kline` 新增 `months` 字段（与 `closes` 一一对应的 `YYYY-MM` 月份数组，兼容旧 `closes` 调用）；数据源统一为东方财富月 K（美股尝试 105 纳斯达克 / 106 纽交所 / 107 美交所市场代码、A股按代码分沪 `1.xxx` / 深 `0.xxx`、港股 `116.xxx`，约 2020 年至今）；东方财富不可用时降级：A股 → 新浪日线转月线、港股 → 腾讯月 K、美股 → 腾讯兜底。
3. 新增 `/api/v1/index-kline` 指数月 K（供 iOS 盈利对比曲线）—— `GET /api/v1/index-kline?key=spx` 返回 8 个基准指数月收盘（`closes` + `months`）：标普500 `spx` / 纳斯达克 `ndx` / 恒生指数 `hsi` / 上证指数 `sse` / 深证成指 `szse` / 日经225 `n225` / 韩国KOSPI `ks11` / 富时新加坡海峡 `sti`；数据源东财优先，失败按腾讯 / 雅虎兜底，10 分钟缓存。

### 修复

1. 修复圆环扇区划不进去（谷歌 / 亚马逊等小扇区） —— 1) 头像层 pointer-events 拦截：F3 上层头像 mask 只影响视觉、不影响鼠标命中，整个头像方块盖住环带内缘一圈导致左上半圆扇区从内缘划过时无法 hover——修复为三层头像全部 `pointer-events-none`，头像上浮改由圆环容器统一判定（鼠标在中心洞里才触发）；2) 基础环无事件：持仓权重悬殊（如巴菲特 Apple 占大头）时环带大部分露出无事件的基础底色环，鼠标划过没有可命中的扇区——修复为扇区 hover 不再依赖每个 path 的事件，改为容器级几何判定：鼠标在环带任意位置（含基础环露出的细线区、内缘）按角度映射到对应扇区，悬停弹出与提示全部恢复正常。实测：左上半圆 105°/135°/200° 均正确触发对应扇区弹出，洞里头像上浮、环外复位，佩洛西 / 巴菲特数据均正常。
1. 「其他持仓」聚合段不参与最大持仓突出判定 —— 最大持仓段（向外突出 + 高亮）的判定改为排除「其他持仓」聚合段：聚合权重可能略高于真实最大持仓（如木头姐 ARK：其他持仓 19.1% > 特斯拉 19%），此前会抢走突出效果导致特斯拉不突出。修复后 `maxIdx` 只在真实持仓中取最大，实测木头姐圆环特斯拉扇区正确向外突出、其他持仓保持内侧不突出。
1. 名人持仓加密货币/贵金属图标走素材库（如特朗普持仓 BTC） —— `CelebLogo` 组件此前只查股票类型素材（`stockIcons`：市场:代码），素材库里 BTC 是 crypto 类型（`assetIcons`：按代码映射），导致特朗普持仓的比特币 ETF（code=BTC, market=US）匹配不到图标、走 CDN 兜底失败后显示首字母。修复：`CelebLogo` 增加 `assetIcons` 兜底（加密货币/贵金属按代码查询），实测特朗普持仓 BTC 行正确显示素材库比特币图标 `/uploads/asset/crypto/1785666703934-8a7ad4bd.svg`，圆环扇区 Logo 同步受益。
1. 名人持仓明细股票图标悬停相机上传（自动同步素材库） —— 名人持仓展示页的持仓明细每行股票图标新增悬停相机上传（仅管理员可传，非管理员只读）：上传后经 `/api/upload` 保存文件、自动 `POST /api/assets` 注册到素材库（type=stock, 市场:代码，`upsert` 幂等不堆积），再触发 `fire:assets-updated` 全局刷新——该股票在名人持仓 / 圆环 / 素材库全站生效，无需重复上传。实测：巴菲特持仓明细 8 行均有「上传 XX 图标（全站生效）」按钮，悬停相机遮罩淡入正常。
1. API 规范化 v1（面向 Swift / Android 移动端 + 全站） —— 新增统一 API 规范层 `/api/v1/**`（17 个路由）与规范文档 [`docs/api-spec.md`](docs/api-spec.md)：1) 统一响应信封 `{ code, message, data }`，错误码分段（40001 参数 / 40101 未登录 / 40103 密码错误 / 40301 权限 / 40401 不存在 / 42901 限流 / 50002 上游失败）；2) 认证支持 `Authorization: Bearer <token>`（登录返回 token，`getAuthUser` 兼容 Cookie 与 Bearer 双通道）；3) 分页统一 `?page=&pageSize=`，响应 `meta` 带 total；4) 新增 `/api/health` 健康检查、`/api/v1/overview` 资产总览（成本/市值/盈亏/市场分布）；5) v1 覆盖：认证、records CRUD、quotes/charts/kline/rates/search/indices/earnings、celebs(+详情+收益日线)、assets 分页/注册/改名/删除、upload、settings/public；6) 上传核心逻辑抽离 `lib/upload.ts`（SVG 消毒 / 文件落盘 / 头像替换），旧接口与新 v1 共用，冒烟 82/82 通过、旧接口零回归。
1. 设置-API 开发接口书图标链接 + /api-docs 文档页 —— 设置 → API 开发接口子页标题右侧与左侧竖排导航项右侧新增书本图标，点击新开 `/api-docs` 页面；新增 `/api-docs` 路由实时渲染 `docs/api-spec.md`（`lib/markdown.ts` 轻量 Markdown 渲染：标题/表格/代码块/列表/引用/行内代码/链接，内容先转义防 XSS），文档单一数据源、改动即同步，深浅色模式适配，顶部带「返回设置」。
1. API 文档页 Markdown 双模式编辑（分屏预览）+ 可折叠目录 —— `/api-docs` 升级为双模式：1) 阅读模式——左侧目录大纲（按一级章节分组，子项自动折叠，滚动自动展开当前章节、点击一级手动展开/收起、当前章节高亮），右上角「编辑文档」按钮（仅管理员显示）；2) 编辑模式——Obsidian 风格左右分屏：左 Markdown 源码编辑（深色等宽字体）、右实时预览（随输入即时渲染），顶部保存/取消（保存写回 `docs/api-spec.md` 经 `/api/api-docs` POST 接口，仅管理员、固定白名单路径、512KB 上限）。实测：初始目录 7 项全折叠、点击「路由清单」展开 6 子项、高亮跟随滚动、编辑模式 textarea+预览+保存均正常。

### 修复

1. 取消「苹果蓝」全局配色规范 —— 按用户要求取消 `AGENTS.md` 中的「苹果色块标准」章节：品牌色不再强制苹果蓝 `#0071e3`，按钮/胶囊 hover 的苹果「购买」风格（只改背景不改边框）与旧青色禁令一并解除；保留与颜色无关的独立约定——焦点无轮廓无光晕（白框问题历史修复）、深浅色适配需补 dark 覆盖、素材库图标全局缓存机制；「苹果风格开关（Toggle）」规范（绿/灰，名人管理为基准）不受影响继续生效。现有代码仍使用 brand=#0071e3，配色体系待后续统一调整。
1. 全站苹果蓝改为浅灰边框 + 白色块状 + hover 浅灰动画 —— 全站配色体系从苹果蓝切换为中性色：1) 品牌色板改为灰（brand.DEFAULT `#0071e3→#6b7280`、hover→`#e9ebee` 浅灰、light→`#f1f3f5`、deep→`#3f4652`）；2) 87 处实心蓝块改为「白底 + 浅灰边框 + 深色文字」（`border-edge-strong bg-white text-ink-2`，深色模式 `dark:bg-[#1c1c1e]`），含主按钮、tab/分页选中态、徽标、加号图标等；3) hover 统一为明显浅灰背景 `hover:bg-brand-hover`（浅色 #e9ebee、深色 `.dark` 覆盖 #262c37）+ `transition-all` 上浮/按压动画，替换原蓝色 hover（`hover:bg-brand-hover hover:text-white` / `active:#006EDB` 全部清除）；4) 蓝色阴影、输入框 focus 蓝光环、SVG `#0071e3` 图标全部改为中性灰/currentColor，全站蓝色残留清零；5) `AGENTS.md` 登记「中性色按钮标准」。实测：设置页保存按钮浅色 hover 浅灰背景、深色模式半透明白、transition all 动画正常，冒烟 82/82 通过。
1. 胶囊/筛选按钮 hover 改为黑色背景白字（浅色模式） —— 浅色模式下胶囊按钮（全部/美股/港股/A股筛选、分页页码与上一页/下一页、首页导航 tab、财报日历市场切换、附件管理分类等）hover 由浅灰改为纯黑背景 + 白色文字，选中态胶囊 hover 同步黑色（鼠标移开恢复白底选中）；`seg-active` 全局类补充 `.seg-active:hover` 黑色（深色模式保持半透明白）。实测：首页「全部」胶囊 hover 背景 rgb(0,0,0)、文字白色、圆角胶囊，与周围白底胶囊区分强烈，冒烟 82/82 通过。
1. 胶囊 hover 由黑色调整为淡黄色 #FFE38B —— 按用户参考色块将胶囊/筛选按钮 hover 从纯黑改为淡黄 `#FFE38B`（rgb 255,227,139）+ 黑色文字，覆盖全部/美股/港股/A股筛选、分页、首页导航 tab、财报日历、附件管理分类及 `seg-active` 选中态 hover（深色模式保持半透明白）。实测：首页「全部」胶囊 hover 背景 rgb(255,227,139)、文字黑色，与参考色块完全一致，视觉柔和且区分明显，冒烟 82/82 通过。
1. 胶囊 hover 再调整为橙色 #FF9828 —— 按用户参考图（暖色环形图，红色箭头指向橙色弧段）将胶囊/筛选按钮 hover 从淡黄改为橙色 `#FF9828`（rgb 255,152,40）+ 黑色文字，覆盖全部市场筛选、分页、首页导航 tab、财报日历、附件管理分类及 `seg-active` 选中态 hover（深色模式保持半透明白）。实测：首页「全部」胶囊 hover 背景 rgb(255,152,40)、黑色文字清晰，与参考图橙色一致、区分明显，冒烟 82/82 通过。
1. 财报日历市场/市值/类型/时段胶囊补橙色 hover —— 财报日历的市场切换（美股/港股/A股/日股/韩股）与 `PillGroup`（市值/类型/时段）未选中胶囊此前只有 `hover:text-ink`（文字变色无背景），鼠标划过无橙色效果。修复：未选中分支统一补 `hover:bg-[#FF9828] hover:text-black`，与全站胶囊橙色 hover 一致（选中态 `seg-active` hover 橙色已生效）。全站排查无其他同类遗漏；实测港股按钮 hover 背景 rgb(255,152,40)、黑字，冒烟 82/82 通过。
1. api-docs 换成 GitHub 风格渲染 —— API 规范文档页（含编辑模式实时预览）改为 GitHub Flavored Markdown 渲染风格：`globals.css` 新增 `.markdown-body` 规范（浅色：白底深灰字、h1/h2 下边框线、表格完整边框 + 灰色表头 + 斑马纹、代码块 `#f6f8fa` 浅灰背景 + 边框、引用左侧灰条、链接 `#0969da`；深色：GitHub Dark 配色 `#0d1117`/`#161b22`/`#30363d`/`#4493f8`）；`lib/markdown.ts` 渲染器改为输出纯语义 HTML（移除 Tailwind 色类，样式统一交给 `.markdown-body`）；阅读与编辑预览容器套用 `markdown-body`，深色模式背景对齐 GitHub Dark。实测浅色/深色均符合 GitHub README 渲染风格。
1. iOS App（SwiftUI）MVP 骨架 —— 新增 `ios/` 目录的 SwiftUI iOS App 骨架（技术栈：Swift 5.9 + SwiftUI + Async/Await + URLSession，对接后端 `/api/v1` 统一信封 + Bearer token）：XcodeGen 工程配置（`project.yml`）、`Info.plist`（ATS 开放局域网 HTTP）、Codable 模型（用户/总览/持仓/名人）、`APIClient`（登录 / 总览 / 持仓列表 / 登出）、登录页 + 资产总览 + 持仓列表三页、`AuthStore` 全局登录态；`ios/README.md` 含构建步骤（`brew install xcodegen` → `xcode-select` 切换 → `xcodegen generate`）。全部 Swift 文件语法检查通过；等待本机安装完整 Xcode 后 `xcodebuild` 构建到模拟器验证。
1. Fire iOS 工程落地 + App 图标 + 登录解码修复 —— 1) iOS 项目独立为「Fire ios」目录（软件名 Fire：工程名 / Bundle ID `com.fire.app` / 显示名 / 登录页标题全部改为 Fire）；安装完整 Xcode 26.6 + iOS 26.5 模拟器运行时，`xcodebuild` 构建通过并在 iPhone 17 模拟器运行（首启黑屏为模拟器显示缓存问题，重启模拟器解决；XcodeGen info 需用 properties 生成 Info.plist，缺失 UILaunchScreen 导致黑屏已修复）；2) 极简 App 图标（1024px，深蓝黑底 + 橙色 `#FF9828` 上升折线 + 白色终点圆点，无文字）接入 `Assets.xcassets`，actool 编译通过、主屏显示正常；3) 登录显示「HTTP 200」修复：后端 v1 登录只返回 user.id/username，Swift `User` 模型必填字段导致 Codable 解码失败，改为可选字段；macOS 编译网络层+模型真实调后端验证：登录 / `me()` / `overview()` 均解码成功（5 持仓、总市值 187381.5）。
1. 项目目录重组：fire-web / fire-ios 独立 —— 为让项目与对话更清晰，将根目录拆分为两个独立子项目：原 `fire` → `fire-web`（Web 全栈，662M 含 node_modules/data/素材/备份整体迁移，数据库与 1262 个素材完好，服务在 `fire-web` 下重启、端口 8124 正常）；原「Fire ios」→ `fire-ios`（SwiftUI，README 构建路径同步更新）。清理根目录空 `package-lock.json` 残留与 `.DS_Store`；新增根 README（两个子项目边界与常用命令）、`fire-web/README`（关键位置指引），约定 Web 改动只进 `fire-web`、iOS 改动只进 `fire-ios`。
1. 登录页改为长桥风格（左右分栏品牌卡） —— 登录页 `/login` 重构为长桥（Longbridge）风格：940px 大圆角卡片左右分栏——左侧深色品牌区（网格纹理背景、Fire Logo 橙色上升折线 + 标语「记录投资点滴」、特性列表、底部橙色折线装饰），右侧白色表单区（大字标题「登录」、登录/注册文字 Tab 橙色下划线选中、48px 输入框、黑色实心登录按钮、无账号去注册入口）；移动端隐藏左栏、顶部显示 Fire 品牌；深浅色模式均协调（深色下登录按钮反色白底黑字，符合 iOS 最佳实践），注册开关关闭时仅显示登录、开启时显示 Tab 与去注册。冒烟 82/82 通过。
1. 素材库股票图标补齐（记录漏收 + 图标获取三处根因修复） —— 修复素材库股票不全问题，三处根因：1) 记录漏收——东财 clist 翻页遇到 f20 市值缺失/解析失败（返回 0）时被误判为低于门槛提前截断，导致 A股缺 8 只、港股缺 5 只（如中国移动/海光信息等大票）；修复为缺失行跳过不截断、翻页上限 10→30，重新同步后 CN 409→418、HK 243→248；2) 图标错配——微牛搜索股票代码可能与指数冲突（000858 命中 CSI 500 指数、000001 命中 SSE 指数），精确匹配未限定 `template=stock` 导致大票图标缺失（五粮液）；修复为精确匹配必须 `template==='stock'`，代码搜不到时用中文名兜底；3) 图标被拒——微牛部分图标 CDN 返回 `application/octet-stream`（非 `image/*`），`downloadIcon` 误拒（中国移动等）；修复为接受 octet-stream 并按文件魔数校验/推断扩展名。另新增「仅补图标」模式（`POST /api/assets/sync` body `{ mode: 'icons' }`，只对 url 为空的存量记录重试下载，快速补漏）。实测：五粮液/中国移动等大票图标补齐，素材库 A股列表 46/46 图标加载成功、0 破损；剩余 160 只为微牛/parqet 确实无图标的冷门股（覆盖率约 88%），可后续加备用图标源。
1. 素材库股票数量扩容（美股 100 亿 / 港股·A股 150 亿 / 日韩前 300） —— 按用户决策扩大素材库股票覆盖：市值门槛下调（美股 300 亿→100 亿美元、港股/A股 500 亿→150 亿本地货币）、日韩由前 100 扩到前 300（companiesmarketcap 翻页拉取 + 代码去重，韩股受源收录上限为 162 只）。同步优化：已有图标的股票复用本地 URL 不重复下载，全量同步显著加快。实测总股票 3259 只（美股 1033 / A股 1284 / 港股 480 / 日股 300 / 韩股 162）。图标补漏遇到微牛搜索接口对该 IP 的持续限流（空结果风控）：为 `searchWebull` 增加 4 次指数退避重试、同步/补漏并发降到 2-3，并新增独立补漏脚本 `scripts/backfill-icons.mjs`（串行慢速、绕开 dev server 后台任务，`node scripts/backfill-icons.mjs [N]` 分段执行）；大票（五粮液/中国移动/平安银行/中国人保等）图标已补齐，剩余 345 只中小市值图标待微牛限流恢复后由脚本继续补（当前有图标 2914/3259，覆盖率 89.4%）。
1. 素材库美股手动添加股票市场列为空修复 —— 手动添加的美股（JPM / BRK_B / BRK_A 等，source=manual）board 为空，素材库「市场」列显示 —。修复：1) 为现有 9 只手动美股硬编码补精确归属市场（JPM/BRK/VOO/IVV/VTI/TLT → 纽交所、DJT → 纳斯达克）；2) 前端 `boardFallback` 兜底：美股 board 为空时显示「美股」（避免再出现空列，精确交易所待后台同步/backfill 补齐）。实测素材库美股列表市场列全部有值（TSM 纽交所、NVDA/AAPL 纳斯达克，无空值）。
1. 素材库市场/加密货币/贵金属统一为股票式表格展示 —— 素材库 market / crypto / metal 三个 tab 由管理型输入行改为与「股票图标」一致的微牛表格并简化：1) 市场 tab 精简为三列（序号 / 市场 / 操作），图标合入市场栏（名称左侧 36px 圆形图标悬停相机上传，与股票图标同尺寸），保留恢复默认；2) 加密货币 / 贵金属 tab 与股票图标完全一致：六列（序号 / 代码名称 / 市值 / 最新价 / 涨跌幅 / 操作），图标 36px 悬停相机上传，市值 / 价格 / 涨跌幅从 assets 数据填充（手动添加暂无数据时显示 —，后续同步可填充），保留恢复默认占位（₿ / 金）；3) 行悬停浅灰、深浅色适配。实测：市场 tab 三列简洁、图标 36px；crypto tab 六列齐全、风格与股票一致；冒烟 82/82 通过。
1. 素材库股票名称/代码显示顺序反转 + 市场图标上传修复 —— 1) 素材库股票图标与加密货币/贵金属的「代码 / 名称」列显示顺序反转：名称在前（粗体）、代码在后（灰色小字），与用户习惯一致（实测股票行：英伟达 / NVDA）；2) 修复市场图标点相机上传无反应：原 onClick 依赖 `fileRefs` 间接引用（ref 回调在 React 重渲染下可能未及时绑定导致 `click()` 静默失效），改为 DOM 就近查找（`e.currentTarget.closest("div")` 内 `querySelector input[type=file]`）触发，不依赖 ref 引用；market / crypto / metal 三处图标上传统一修复，类型断言 HTMLInputElement。冒烟 82/82 通过。
1. 素材库市场图标增加全球主要市场 + 移除恢复默认 —— 1) 市场图标 tab 由 5 个市场扩展为 14 个全球主要市场：美股/港股/A股/日股/韩股/新加坡/英国/德国/法国/澳大利亚/加拿大/印度/台湾/巴西（`MARKET_META` 补全名称与国旗，flagcdn 自动圆形国旗，UK 映射 gb 修复英国国旗 broken）；2) 修复 SG 显示韩国国旗 bug：SG 自定义图标记录误存了韩国图（与 KR 同 URL），删除该错误记录恢复新加坡默认国旗；3) 按用户要求移除 market / crypto / metal 三个 tab 的「恢复默认」按钮与操作列（market 精简为 序号/市场 两列，crypto/metal 为 序号/代码名称/市值/最新价/涨跌幅 五列），后续修改直接再次上传覆盖；4) 市场上传点击修复经 CDP 实测 `input.click` 触发成功（DOM 就近查找生效）。冒烟 82/82 通过。
1. 素材库市场筛选折叠为地球仪下拉 + 市值汇率补全 + 上传覆盖修复 —— 1) 股票图标 tab 市场筛选由 14 个胶囊改为默认显示前 5 个市场 + 地球仪圆形按钮，点击弹出下拉弹层（参考左图：圆角卡片、「选择市场」标题、国旗 + 名称垂直列表、选中深灰高亮 + 对勾、悬停浅灰、点击外部关闭），折叠的 9 个市场收纳进弹层，筛选栏不再拥挤；2) 市值货币转换补全：`MARKET_CURRENCY` 增加 SG/UK/DE/FR/AU/CA/IN/TW/BR → SGD/GBP/EUR/AUD/CAD/INR/TWD/BRL，服务端 `/api/rates` 拉取新增币种，`FALLBACK_RATES` 补兜底汇率，新增市场市值不再按 1:1 误算；3) 市场上传「成功却没更新」根因修复：`upsertAsset` 对 `source=manual` 的记录不更新 URL（防同步覆盖手动图标），但手动再次上传同样被挡住；`POST /api/assets` 带 id 时改为强制更新 url（与 name 一起），市场/加密货币/贵金属再次上传立即生效并保留 toast 提示。冒烟 82/82 通过。
1. 素材与头像命名规范 + 货币换算规范 + 误删恢复 —— 1) 素材文件命名规范（写入 `AGENTS.md`）：市场图标「中文名+市场码」（美股US.svg）、加密货币/贵金属「中文名+代码」（比特币BTC.svg）、股票「中文名+代码」（苹果AAPL.png），由 `lib/upload.ts` 的 `assetFilename` 统一生成（前端上传带 name/code/market）；历史随机名文件按规范重命名并更新 `assets.url`。2) 头像命名规范：按「登录名(UID编号)」（`Admin(UID1).jpeg`），`saveUpload` 头像分支统一生成，现有头像已重命名。3) 货币换算规范（写入 `AGENTS.md`）：新增市场必须同步 `MARKET_CURRENCY` / `FALLBACK_RATES` / `/api/rates` 币种，市值统一 cap ÷ 汇率。4) 事故恢复：清理脚本路径前缀 bug（`/public/uploads` vs `/uploads`）误删素材文件，已从 `data/backups/fire-20260808-003024` 备份恢复——market 5/5、crypto 39/39、metal 3/3 全部恢复，stock 备份内 1168 只旧图标恢复；剩余 1747 只为扩容新增股票图标，待微牛限流恢复后由 `backfill-icons.mjs` 补；`AGENTS.md` 补充「素材清理注意事项」（必须用 `cleanupOrphanFiles`、URL 先 decode、路径前缀 `/uploads`）防止再犯。
1. 头像命名改为 Admin(UID1)（去冒号）+ 修复扩展名缺小数点 —— 1) 头像命名由「登录名(UID:编号)」改为「登录名(UID编号)」（`Admin(UID:1)` → `Admin(UID1)`）：去掉冒号分隔，避免上传清洗规则把冒号转成横线（`demo(UID-2)`）导致命名不一致；`AGENTS.md` 头像命名规范同步更新为「登录名(UID编号)」。2) 修复头像上传扩展名缺小数点 bug：avatar 分支此前 `${stem}${ext}` 拼接少了点号（生成 `demo(UID-2)png` 这类无扩展名分隔的文件），改为 `${stem}.${ext}`。3) 现有文件与数据库同步：Admin 头像文件与 `users.avatar` 已更新为 `/uploads/avatar/Admin(UID1).jpeg`，测试残留文件按新规范重命名为 `demo(UID2).png`。
1. 素材文件命名全量规范化（market/crypto/metal/stock）+ 名称数据修复 —— 1) 新增项目内工具 `scripts/rename-assets.mjs`（支持 `--dry-run`，只 `fs.rename` 不删除、URL 先 decode、路径前缀 `/uploads`，防误删），把全部存量素材按 `AGENTS.md` 命名规范迁移：market 5 个时间戳随机名 → 美股US.svg/港股HK.svg/A股CN.svg/日股JP.svg/韩股KR.svg（name 同步为中文标签）；crypto 39 个 → 名称+代码（比特币BTC.svg、AaveAAVE.svg 等）；metal 3 个 → 黄金GOLD.png/白银SILVER.svg/铂金PLAT.svg；stock 各市场 73 个历史时间戳文件 → 名称+代码（苹果AAPL.png、伯克希尔BRK.B.png 等），0 冲突。2) 名称数据修复：28 行 HTML 实体解码（MS&amp;AD → MS&AD，修复显示与文件名）；13 个 A 股 XD/C 前缀名修正为规范全名（XD新华保→新华保险、XD上汽集→上汽集团、C长鑫→长鑫科技、C嘉立创→嘉立创 等，无图标行仅修正名称）；6 条未编码 URL 规范化（encodeURIComponent 存库）；2 条缺扩展名 URL 修复（平安银行000001png → 平安银行000001.png）。3) 验证：素材磁盘 1215 文件与数据库完全对应、0 素材孤儿文件（清理 2 个 .DS_Store）、重命名后 URL 全部 200、tsc 无错误、冒烟 82/82 通过；剩余 1747 只股票图标仍缺（微牛限流，待 `backfill-icons.mjs` 回填，回填脚本已按规范命名）。
1. 素材库市场筛选「全部」图标更换 + 「更多市场」弹层重定位美化 —— 1) 「全部」按钮图标由地球改为九宫格（4 个圆角方块），与「更多市场」地球圆形按钮区分，不再同形重复；2) 「更多市场」下拉弹层原为相对整个筛选容器 `right-0` 定位，导致点击后出现在容器最右缘、离按钮很远——改为包裹在按钮的 `relative` 容器内、右对齐按钮展开（面板向左延伸，空间充裕不会溢出屏幕），并新增：指向按钮的小三角、`popover-in` 淡入缩放动画、标题「更多市场」+ 折叠数量徽标、打开时按钮高亮为橙色，选中态与悬停样式保留。tsc 无错误、`/library` 页面编译加载正常。
1. 素材库市场图标名称可编辑 + 加密货币/贵金属行情补全 —— 1) 市场图标 tab：每行新增铅笔按钮编辑市场名称（行内编辑 Enter 保存 / Esc 取消），保存写入 `settings.marketLabels` 全局生效（首页标签、持仓市场 chips 同步）并同步素材库 `assets.name`；行布局恢复为与加密货币 tab 一致的自然样式——序号 + 图标 + 名称/市场码两行左对齐（图标在文字左侧），铅笔保留在名称后。2) 加密货币 / 贵金属 市值 / 最新价 / 涨跌幅补全：新增 `lib/assetQuotes.ts`——39 个加密货币走 CoinGecko `simple/price`（含市值 / 现价 / 24h 涨跌幅，出站走 `proxyFetch` 代理优先），贵金属 GOLD/SILVER/PLAT 走 companiesmarketcap 全球资产榜，行情缓存到内存 + `data/asset-quotes-cache.json`（crypto 5 分钟 / metal 6 小时），`GET /api/assets` 读取时合并 + 过期后台刷新不阻塞请求；crypto / metal tab 数据未就绪时 2.2 秒静默重取一次（无骨架闪烁）。3) 验证：39 币种 + 3 贵金属实测返回真实行情（如 BTC、ETH、黄金 `$4401` / 白银 `$63.8` / 铂金 `$1757`），tsc 无错误、冒烟 82/82 通过。
1. 加密货币/贵金属 市值、最新价、涨跌幅 排序 —— 素材库 crypto / metal 两个 tab 的 市值 / 最新价 / 涨跌幅 表头改为可点击排序按钮（复用股票图标 tab 的排序机制：点击切换升/降序，当前排序列高亮 + 箭头指示，无数据行排最后），排序后序号跟随当前顺序重新编号，排序状态同步到 URL（`?sort=&dir=`），刷新 / 分享保持。tsc 无错误、`/library` 页面编译加载正常。
1. 素材库新增钯金（贵金属补全四大主流） —— 贵金属由 3 种补全为 4 种主流产品：新增 钯金 PALLAD（黄金 / 白银 / 铂金 / 钯金 四大贵金属齐全）。图标从 companiesmarketcap 下载并本地化保存（`public/uploads/asset/metal/钯金PALLAD.png`，符合命名规范），`assets` 表新增 `metal:PALLAD` 记录，行情走既有 CMC 全球资产榜数据源（实测 现价 `$1383` / 市值 `$2420 亿` / 24h `+0.41%`）；前端 `BASE_ASSETS` 同步加入，贵金属 tab 直接展示。铑 / 铱 / 钌 / 锇 等小众贵金属暂无免费行情源，如需后续接入单独处理。tsc 无错误、页面编译加载正常。
1. 素材库「搜索即添加」重塑 + 名称/代码列序修正 —— 1) 移除底部笨重的「新增股票图标」表单（市场下拉 + 代码/名称/图片链接输入 + 上传/保存），改为搜索即添加：在搜索框输入后如素材库无匹配，空态显示「未找到 XX」+「+ 添加」按钮，点击拉取腾讯行情源联想结果（名称 / 代码 / 市场），候选列表逐条「添加」；选中后新增服务端接口 `POST /api/assets/add-by-search` 自动解析并尽力下载本地图标（复用 `stockSync.resolveIcon`，微牛 / parqet，失败留空前端首字母兜底），写入素材库（source=manual）并刷新。实测 BYND「别样肉客」搜索 → 添加 → 图标自动下载为 `别样肉客BYND.png`（命名规范）全流程通过。2) 股票图标 tab 表头「代码 / 名称」改为「名称 / 代码」（与行内名称在前、代码在后一致），加密货币 / 贵金属表头与搜索框占位同步。tsc 无错误、`/library` 编译加载正常。
1. 加密货币/贵金属 搜索框 + 搜索即添加（CoinGecko） —— 1) 加密货币 / 贵金属 tab 顶部新增搜索栏（搜索名称 / 代码 + 当前数量统计），列表按关键词本地过滤；2) 搜索无本地匹配时同样显示「未找到 XX」+「+ 添加」：加密货币候选走新增服务端接口 `GET /api/assets/search?type=crypto`（CoinGecko 搜索，返回 名称/代码/大图图标，走代理），贵金属候选为内置四大贵金属按名称/代码匹配；3) `POST /api/assets/add-by-search` 扩展支持 crypto/metal：传入 `iconUrl` 自动下载本地化（crypto → `public/uploads/asset/crypto/名称代码.扩展名`，metal 同理，market=ASSET，source=manual），并修复下载命名的扩展名缺小数点 bug；4) 实测：搜 `dogwifhat` → 候选 WIF → 添加 → 图标自动下载为 `dogwifhatWIF.jpg`（规范命名），测试数据已清理；贵金属搜「钯」→ 钯金 PALLAD。tsc 无错误、`/library` crypto/metal 页编译加载正常。
1. 素材库搜索增强：东财兜底 + 北交所前缀修复 + 手动添加兜底 + 添加提速 —— 1) 搜索源兜底：腾讯联想无结果时自动切东方财富搜索联想（按全称 / 简称 / 代码，覆盖 A股 / 港股 / 美股 / 北交所），解决改名股 / 中文全称搜不到——实测「西安旅游」→ 000610 \*ST西旅、「曙光数创」→ 920808 均可搜索并添加；2) 修复北交所代码前缀：920xxx / 4xx / 8xx 此前被误判为沪市（sh920808 无行情），改为 bj 前缀，`toTencentSymbol` / `minuteQueryCodes`（分时走势）/ 东财候选三处全局生效，北交所现价与迷你走势正常；3) 手动添加兜底：行情源也无候选时，股票 tab 显示「可手动添加」表单（市场 + 代码 + 名称，名称默认搜索词），保存仍走 `add-by-search`（图标尽力下载）；4) 添加提速：`add-by-search` 图标下载加 7 秒超时保护，微牛限流重试不再阻塞添加（实测 30s → 7s，超时留空后续可上传或回填）；5) 顺带修复 `lib/versions.ts` 条目缺逗号导致的 tsc 语法错误。tsc 无错误、`/library` 编译加载正常。
1. 素材库新增「券商图标」tab（市场图标后） —— 素材库 tabs 在市场图标后新增「券商图标」：券商列表来自 设置 → 股票设置 → 券商管理（原分组管理）中的分组，每行 序号 + 图标（悬停相机上传）+ 券商名称；`assets` 新增 broker 类型（id=`broker:{分组ID}`、market=GROUP、code=分组ID、name=券商名），图标按券商名称命名存放 `public/uploads/asset/broker/`（长桥证劵.png，已写入 `AGENTS.md` 命名规范），上传后全局生效；`/api/assets` 与 `/api/upload` 支持 broker；`rename-assets.mjs` 适配 broker 文件夹（名称无代码后缀）；附件管理素材库分支自动出现「broker」目录。实测：上传长桥证劵图标 → 注册 broker 素材 → 查询返回，命名规范、测试数据已清理。
1. 「分组」全站更名「券商」+ 券商图标全局显示 —— 1) 文案更名：设置 → 股票设置 的「分组管理」改为「券商管理」（添加分组 → 添加券商、分组名称 → 券商名称、删除分组 → 删除券商、提示文案同步），我的持仓 / 行情 编辑股票表单的「分组」字段改为「券商」，行情列表表头「分组」改为「券商」，财报日历「特别关注」分组提示同步为券商；2) 券商图标显示：`GroupSelect` 由原生 select 重写为自定义下拉（选项带券商图标 + 首字母兜底 + 选中对勾 + 点击外部关闭），我的持仓 / 行情编辑表单传入 `brokerIcons`；行情列表分组标签前显示券商图标；券商管理卡片由标签占位改为显示券商图标（素材库 broker 素材，无则首字母兜底）；`useAssetIcons` 新增 `brokerIcons`（券商名称 → 图标）供全局复用。tsc 无错误、`/library` /holdings /records /settings 编译加载正常。
1. 修复券商图标上传后不显示（分组 ID 大小写匹配 bug） —— 根因：券商素材存库时 code 被 `toUpperCase`（g178… → G178…），而前端按分组 id 精确 `===` 匹配全部落空，导致图标不显示、再次上传也找不到已有行无法强制更新 URL。修复：素材库券商 tab（`brokerIconOf` / `saveBrokerIcon` 查找）与券商管理（`SettingsView.brokerIconOf`）统一改为大小写不敏感匹配（`toLowerCase` 比对）；并在保存分组设置时同步素材库 broker 素材名称（`UPPER(code)=分组ID`，券商改名后按名称映射的图标仍全局生效）。实测：IBKR 两次上传换图 URL 正确覆盖更新，测试数据已清理；用户已上传的长桥证劵图标文件与记录完好，刷新即可显示。tsc 无错误、相关页面编译加载正常。
1. 修复券商分组被清空（保存空列表误清记录）+ 数据恢复 + 防误清保护 —— 根因：保存「券商管理」时若提交空 / 未加载完成的券商列表，服务端会把被移除分组的全部持仓记录分组清空（`clearRecordGroup`），导致我的持仓所有记录的券商丢失（设置里的 3 个券商仍在）。修复：1) 数据恢复——Admin 持仓按规则恢复：美股 8 条 → 长桥证劵、港股 5 条 + A股 3 条 → 华泰证劵；2) 防误清——券商管理保存按钮在券商列表加载完成前禁用（显示「加载中…」），且券商列表为空而当前已有券商时保存需二次确认（“将删除全部券商并清空所有持仓记录的券商”），避免异步加载竞态或误操作清空；3) 排查全局：仅「保存分组设置」会触发记录分组清空（rename/clear 同步），已由上述保护覆盖，其他设置保存均为局部字段不涉及记录。tsc 无错误、设置页编译加载正常。
1. 券商数据规范化（面向 iOS / Android） —— 1) 新增 `lib/brokers.ts` 统一券商数据模型 `{ id, name, icon }`：列表 = 设置券商分组（`site_settings.groups`，顺序即展示顺序）+ 素材库券商图标（`assets` type=broker，code=分组ID，大小写不敏感匹配），并抽出 `syncRecordGroups`（改名/删除同步持仓记录）供设置路由与 v1 共用；2) 新增 v1 券商接口：`GET /api/v1/brokers`（登录可读列表）、`POST /api/v1/brokers`（管理员全量保存，覆盖式）、`DELETE /api/v1/brokers?id=`（管理员删除并同步清空对应持仓记录的券商）；3) 更新 `docs/api-spec.md` 新增「券商 Brokers」章节（模型字段 / 响应示例 / 请求示例 / 增删改约定，图标走素材库上传自动合并）与路由清单，`AGENTS.md` 写入券商数据规范；4) 实测：GET 返回 3 券商含图标（长桥证劵 / IBKR / 华泰证劵），POST 原样保存 code 0，文档内容接口已含新章节，`/api-docs` 渲染正常。tsc 无错误。
1. 素材库券商图标：主流券商候选一键添加 + 行内删除 —— 1) 券商图标 tab 新增「新增主流券商」区：A股（华泰证券 / 东方财富 / 同花顺 / 中信证券 / 国泰君安 / 招商证券 等 15 家）、美股（IBKR / 嘉信理财 / 罗宾汉 / 富途 / 老虎 / 微牛 / 第一证券 等 12 家）、港股（富途 / 老虎 / 华盛 / 辉立 / 耀才 / 中银国际 / 海通国际 / 招证国际 等 15 家）分类切换，点击名称一键添加（生成分组 ID 写入券商管理，图标随后在行内上传），已存在（忽略证劵/证券差异）显示 ✓ 置灰；2) 每行右侧新增删除按钮（垃圾桶，hover 红，confirm 确认），删除走 `v1 DELETE /api/v1/brokers`：移除券商 + 同步清空该券商名下持仓记录 + 删除图标素材（assets 行与本地文件）；3) 实测：添加「同花顺」→ 4 家，删除 → 恢复 3 家，数据无损；页面编译正常。tsc 无错误。
1. 修复主流券商候选重复逻辑（IBKR = 盈透证券） —— 候选列表曾把同一券商的不同写法当作两个条目（如美股同时列出 IBKR 与 盈透证券、嘉信理财与 Schwab 等），且去重只比对字面名称，已添加 IBKR 后仍可再添加「盈透证券」造成重复。修复：`MAIN_BROKERS` 改为「规范名 + aliases」结构——同一券商只保留一个规范名（盈透证券 / 嘉信理财 / 罗宾汉 / 微牛证券 等），英文名与常见写法（IBKR / Interactive Brokers / Schwab / Charles Schwab / 嘉信 / Robinhood / Webull / Futu / Tiger / Firstrade 等）放 `aliases`；新增 `brokerNameKey`（证劵/证券 + 大小写归一化）与 `brokerExists`（规范名 + 别名集合比对），候选 ✓ 置灰与添加拦截统一走该逻辑，跨市场同名（富途 / 老虎 / 微牛）同样只允许添加一次；全表排查无其他同类重复（中金/CICC、亿创/E*TRADE、德美利/TD Ameritrade 等已归并）。`AGENTS.md` 写入候选去重规范。tsc 无错误、页面编译正常。
1. 修复新增主流券商候选深色模式下文字不可见 —— 深色模式下候选券商按钮（如微牛证券）文字呈深灰、与深色背景对比度极低（截图标红确认），已存在/选中态与 hover 也有相同隐患。修复：候选按钮深色模式显式浅色文字（`dark:text-[#e5e7eb]`，已存在态 `dark:text-[#99a3b2]`），hover 深色显式橙底黑字（`dark:hover:bg-[#FF9828] dark:hover:text-black`），不再依赖全局 `text-ink-2` 覆盖；全站排查 `hover:text-black` 均搭配橙色底（黑字在橙底可见），无其他同类隐患。tsc 无错误、`/library` 编译加载正常。
1. 券商别名（IBKR 改为盈透证券）+ 券商行可拖动排序 —— 1) 券商模型新增 `alias` 别名：IBKR 分组更名为「盈透证券」（持仓记录与 broker 素材名称同步），现有券商补齐别名（长桥证劵 → Longbridge、盈透证券 → IBKR、华泰证劵 → HTSC），素材库券商行改为两行显示（名称粗体 + 别名小字灰色，与股票图标「英伟达 / NVDA」同款），无别名不显示第二行；`GroupConfig` 增加 alias，券商管理卡片新增别名输入框，v1 `GET/POST /api/v1/brokers` 返回/接收 alias，`docs/api-spec.md` 与 `AGENTS.md` 同步（模型含 alias、请求响应示例更新）；2) 素材库券商行可拖动排序：行首拖动手柄 + 整行 draggable，松开后自动把新顺序 POST 到 v1 保存（toast「券商顺序已保存」），顺序即展示顺序（券商管理 / iOS 列表一致）。实测：v1 返回 长桥证劵(Longbridge) / 盈透证券(IBKR, 含图标) / 华泰证劵(HTSC)，页面编译正常。tsc 无错误。
1. 修复券商分组被迁移脚本覆盖丢失（数据恢复） —— 别名迁移脚本误用硬编码 3 个券商覆盖 `site_settings.groups`，导致用户此前通过素材库添加的券商分组（同花顺 / 东方财富 / 老虎证券 / 嘉信理财 / 罗宾汉 / 富途证券 等 6 个）从列表消失（broker 图标素材仍在）。修复：1) 从 `assets`（type=broker）与 `records.group_name` 反推被删分组，恢复完整券商列表 9 个——长桥证劵 / 盈透证券 / 华泰证劵 / 罗宾汉 / 富途证券 / 老虎证券 / 东方财富 / 同花顺 / 嘉信理财，别名补齐（Longbridge / IBKR / HTSC / Robinhood / Futu / Tiger / 东财 / Schwab）；2) 罗宾汉 / 富途证券（覆盖后重新添加的新分组 ID）的图标素材从旧分组 ID 迁移到新分组 ID，9 个券商图标全部恢复；3) `AGENTS.md` 写入「券商数据只读基准」：修改 groups 必须基于当前完整列表增量操作、禁止硬编码覆盖，恢复时可从素材与记录反推。实测：v1 返回 9 券商全部带图标，页面编译正常。

---

## v0.1.3 · 2026-08-07

> 图片文件唯一化：替换 / 删除素材时后台同步删除旧文件，并清理历史孤立文件，uploads 目录不再不断堆积（ico / 背景 / Logo / 头像 / 股票图标统一生效）。

### 新功能

1. 名人收益分析改为日线（CSV 存附件名人文件夹） —— 收益分析 K 线图由月粒度改为日粒度：服务端生成近 5 年日线（1826 天，每天一点，月序列插值平滑），持久化为 CSV（`data/attachments/{名人}/收益/收益日线.csv`，列 date / 组合收益 / 各对比指数），新增 `/api/celebs/returns` 接口读取；前端按区间取日线（近3月 92 天 / 近1年 365 天 / 近5年 1826 天，区间起点归零），悬停与 X 轴显示具体日期（年/月/日）。实测 1 年区间 365 点、轴标签 2025/08/08。
1. 名人收益曲线改为富途形态（涨跌有致 + 锚点精确对齐） —— 收益分析 K 线不再单调向上：改用「控制点样条（Catmull-Rom）+ AR(1) 均值回归噪声 + 锚点精确对齐」生成近 5 年日线——曲线含 2022 年回撤（组合抗跌、标普 -24.27% 对应参考图 -24.24%）、近 3 年 / 近 1 年锚点、1 年窗口中部深 V、末段冲高回落（巴菲特 5 年最高 84.05 / 终点 76，比例对应富途 85.7 / 77.06）；近 1 年 / 近 5 年终点与顶部数值完全一致（+26.54% / +76.00%），噪声在锚点处归零、无放大失真；CSV 升级 v3（版本标记强制重新生成）；X 轴只保留首尾日期（富途样式）；基准指数 5 年终点改用加法公式（负收益的名人不再爆值）。
1. 附件管理素材库分支：市值排序 + 删除同步 —— 附件管理素材库分支新增：1) 市值列可点击排序（按换算后的美元市值升序/降序切换）；2) 每行新增删除按钮（灰→悬停红，confirm 确认），删除后通过 `/api/assets` 同步移除素材库记录并刷新附件管理（素材库新增/删除与附件管理双向同步，实时读取 + `assets-updated` 事件）。实测：删除测试素材后素材库无记录、附件管理列表同步消失。
1. 附件管理分门别类（财报文件 / 素材库） —— 附件管理新增分类 tab：1) 财报文件——原有 13F 目录树；2) 素材库——按 股票（美股/港股/A股/日股/韩股子分支）/ 加密货币 / 贵金属 / 市场 分组展示，实时读取素材库数据，本地文件发生变化自动同步；支持图片预览（lightbox 大图）与修改名称（同步素材库 `assets.name`，`/api/assets` 主动改名强制生效）。实测 1358 个素材全部渲染、改名后素材库同步更新。
1. 素材库新增搜索与归属市场列 —— 1) 市场筛选行新增搜索框：全部搜全部、美股搜美股等（按当前市场过滤代码 / 名称，关键字写入 `?q=` 防刷新）；2) 表格新增「归属市场」列（可点击排序）：美股按交易所显示纳斯达克 / 纽交所 / 美交所（东方财富 `f13`），A股按代码规则显示科创板 / 创业板 / 北交所 / 沪主板 / 深主板，港股显示主板 / 创业板；同步与 backfill 接口补齐存量数据（实测更新 1154 行，NVDA→纳斯达克、TSM→纽交所、688825→科创板、300750→创业板、00700→主板）。

### 修复

1. 恢复名人持仓 F2 版本 —— 用户对 mask 实心化调整不满意，按约定用 `docs/celebs-ring/celebs-ring-F2.tsx` 整体替换 `CelebsView.tsx` 恢复：头像置顶于扇形之上（圆环在人物之下、悬停块状不覆盖头像）、mask 58% 起淡出。
1. 名人持仓头像身体实心化（领带下方不再透明） —— 透明头像的 mask 底部淡出范围收窄（58% 起 → 78% 起，底部仅 3% 柔化），西装/衬衫主体保持不透明，完整遮挡圆环内圈；领带下方不再透出后面的扇形与 Logo，观感与参考图（身体实心、遮挡圆环）一致。
1. 命名名人持仓 F2 版本并备份 —— 将当前版本（头像置顶于扇形之上：圆环统一在人物之下、悬停块状不覆盖头像）命名为「名人持仓 F2」，备份至 `docs/celebs-ring/celebs-ring-F2.tsx`，并在 `AGENTS.md` 登记恢复方法。
1. 名人持仓圆环：头像置顶于扇形之上（悬停块状不再覆盖人物） —— 修复悬停扇区（左上/右上等）弹出时覆盖人物头像的问题：圆环 SVG 统一渲染在人物之下（不再按透明检测分支压在人物之上），头像始终位于最顶层、完整可见（含肩膀衣领），悬停弹出的块状从头像后面穿过；与参考图「头像在扇形之上、扇形从后面经过」效果一致。
1. 恢复名人持仓 F1 版本 —— 用户对 F2（额头置顶于扇形之上）不满意，按约定用 `docs/celebs-ring/celebs-ring-F1.tsx` 整体替换 `CelebsView.tsx` 恢复改造前版本：圆环恢复条件层级（透明头像在上 / 非透明在下）、头像尺寸 0.7/0.66、上移 3px（悬停 5px）、mask 58% 起淡出；实测头像尺寸 97px 确认恢复到位。
1. 名人持仓圆环：额头置顶于扇形之上（F2） —— 圆环统一改为压在人物之上（不再依赖透明检测分支）：头部从洞里探出、额头贴近/压住上方扇形，肩膀/衣领被圆环扇形明显遮挡；透明头像尺寸 0.70→0.74、上移 3→6px（悬停 8px）、肩膀 mask 淡出起点 58%→52%。改造前版本已备份为「名人持仓 F1」（`docs/celebs-ring/celebs-ring-F1.tsx`），不理想可随时恢复。
1. 确立「苹果风格」规范（以名人管理开关为基准） —— 将名人持仓-名人管理的启用开关确立为「苹果风格」基准并写入 `AGENTS.md` 项目规范：开启苹果绿 `#34c759`、关闭浅灰 `#e9e9ea`（深色 `#3a3a3c`）、圆点必须内联纯白（禁止 `bg-white` 类，防深色模式被 `.dark .bg-white` 覆盖）、36×20 基准；动画规范：轨道颜色 300ms ease-out、圆点滑动 300ms + iOS 缓动 `cubic-bezier(.32,.72,0,1)`、乐观更新不抖动、禁止额外弹跳缩放闪烁。此后用户说「苹果风格」即指该样式，全站开关类控件统一遵守。
1. 修复开关圆点深色模式变深（统一纯白） —— 全局暗黑规则 `.dark .bg-white` 会把开关白色圆点覆盖成深色（#151a26），导致素材库 CDN、注册、自动备份开关在深色模式下圆点发暗、与名人持仓-名人管理开关不一致。修复：三处开关圆点改为内联白色（`style backgroundColor #fff`），深浅色模式均为纯白；关闭轨道统一 `#e9e9ea` / 深色 `#3a3a3c`。实测深色模式下圆点 rgb(255,255,255)。
1. 全站开关统一苹果 iOS 风格配色 —— 素材库 CDN 通道开关、网站设置注册开关、数据库增强自动备份开关统一为苹果 iOS 开关配色：开启苹果绿 `#34c759`，关闭浅灰 `#e9e9ea`（深色模式 `#3a3a3c`），白色圆点 + 平滑过渡，与名人持仓-名人管理启用开关完全一致。实测关闭色 rgb(233,233,234)。
1. 素材库加载骨架（消除「暂无股票图标」误提示）+ CDN 开关苹果风格 —— 1) 素材库进入时 assets 尚未加载完成会误显示「暂无股票图标，可点击右上角设置同步」，现改为加载中显示骨架屏（表头 + 10 行占位），加载完成后才显示列表、真正为空才显示提示；2) 素材库设置弹窗 CDN 图标通道开关改为苹果 iOS 风格：开启为苹果绿 `#34c759` + 白色圆点，关闭为浅灰轨道（深色模式 `#3a3a3c`），平滑过渡。
1. 版本弹窗版本 tab 加变更数徽标 —— 设置 → 关于 → 版本弹窗的每个版本号右上角新增圆形数字徽标，显示该版本变更总数（如 v0.1.3 右上角圆内 14），选中态白底蓝字、未选中蓝底白字，一目了然。
1. 设置页化繁为简（删除多余文字提示） —— 站点信息 / 网站形象卡片及全站设置卡片、子页面删除副标题说明文字（desc）、保存后的行内提示块（保留 Toast 反馈）、注册开关等说明小字；`SettingsHeader` 的 `desc` 改为可选。实测设置页仅保留卡片标题与表单字段，界面更简洁。
1. 收益分析悬停十字光标顺滑优化（零 React 重渲染逐帧跟随） —— 修复鼠标快速来回滑动时十字光标又肉又卡的问题：原实现每次 mousemove 都触发 React 重渲染（365~1826 点 polyline 每帧重建）+ 150ms left/top 过渡追赶。现改为 `React.memo` 冻结图表（悬停移动零重渲染，实测 400 次滑动 SVG polyline 节点不变、0 次 DOM 变更）+ ref 直接更新十字线 / 圆点 / 日期 / 数值 / 浮窗 + `requestAnimationFrame` 合并到每帧一次，光标逐帧精确跟随鼠标；进入 / 离开时仅一次状态更新（主线变粗 + 200ms 淡入淡出），来回滑动平滑无感。
1. 修复站点信息被测试覆盖丢失（冒烟不再写站点设置 + 恢复备份） —— 根因：冒烟测试「保存网站设置」会把站点标题 / 域名 / 图标 / 背景覆盖成测试占位值（如 `/uploads/ico/test.png`），而「替换删旧文件」逻辑把用户真实图标 / 背景文件删除；测试中途中断时末尾还原未执行，残留测试值。修复：1) 冒烟测试移除对 site settings 的全部写操作（不再污染用户数据），首页标题断言改为对比备份值（82/82 通过且测试后设置保持不变）；2) settings 保存删旧文件逻辑加保护——新值为本地但文件不存在（测试占位 / 异常写入）时不删旧文件；3) 孤立文件清理增加 24 小时安全期，防误删刚上传未保存的文件；4) 从每日备份恢复用户原始的网站图标 / 背景文件与站点设置（ico/background 文件恢复并可访问，标题域名保持用户当前值）。
1. 修复手动保存覆盖市值 + 附件管理刷新秒出 —— 1) `upsertAsset` 的市值 / 价格 / 涨跌幅改为仅在传入有效值时更新：此前素材库手动添加 / 编辑（不带市值）会把同步股票的市值覆盖为 0（如平安银行 000001 因此无市值）；backfill 接口同时补归属市场与市值，实测 000001 恢复 2187 亿人民币、A股全部有市值。2) 附件管理素材库分支增加 60 秒本地缓存秒出（刷新先显示上次数据再后台拉最新），接口本身本地约 40ms。
1. 修复港股/A股/日韩市值无数据（市场-货币映射） —— 市值换算误用 `rates[市场代码]`（如 HK），而汇率键是货币代码（HKD/CNY/JPY/KRW），导致只有美股有市值。修复：增加市场→货币映射（US→USD / HK→HKD / CN→CNY / JP→JPY / KR→KRW），本地货币市值 ÷ 对应汇率换算为美元。实测：腾讯控股 00700 市值 $554.8B（港币 4352 亿 ÷ 7.8446），素材库与附件管理同步修复。
1. 素材库市场列右对齐统一（附件管理同步） —— 市场列表头与数据行内容改为右对齐，与市值 / 最新价 / 涨跌幅三列对齐方式统一；列宽调整：序号 36 / 代码名称自适应 / 市值 80 / 最新价 72 / 涨跌幅 72 / 市场 140，市场列空间充足、与涨跌幅不再拥挤；附件管理素材库分支同步。实测表头与行右边缘一致。
1. 分页精简（移除首页/末页按钮） —— 智能分页去掉「首页 / 末页」按钮（与页码 1 / 尾页功能重复），保留 上一页 / 页码（首尾 + 当前附近 + 省略号）/ 下一页 / 「共 N 页 · 跳至」输入框，控件更简洁；素材库与附件管理统一生效。
1. 素材库市场列加市场图标并拉开间距（附件管理同步） —— 素材库股票列表「市场」列加入市场图标（素材库市场图标，与筛选栏一致），列宽 84→98px 拉开与涨跌幅的间距；附件管理素材库分支同步更新（股票条目市场列显示市场图标）。实测列宽：序号 20 / 代码名称 215 / 市值 92 / 最新价 84 / 涨跌幅 84 / 市场 98（含图标）。
1. 分页优化 / 表头调整 / 市值货币换算 / 附件管理分页 —— 1) 新增智能分页组件（Pagination）：页码首尾 + 当前页附近 + 省略号，含首页 / 上一页 / 下一页 / 末页 / 跳转输入框，替换原先只有「上一页 / 下一页」的分页，素材库与附件管理素材库分支统一使用；2) 素材库表头「归属市场」改名「市场」，并与市值列互换位置（顺序：序号 / 代码与名称 / 市值 / 最新价 / 涨跌幅 / 市场）；3) 市值按汇率统一换算为美元显示（本地货币市值 ÷ `/api/rates` 汇率，避免港股/A股/日韩市值直接混排）；4) 附件管理素材库分支每页 20 条分页展示（原一次性渲染 1358 条）。实测：素材库 68 页智能页码、附件管理 20 条/页、NVDA 市值 $5.29T 正确换算。
1. 素材库文件分类归档（市场 / 加密货币 / 贵金属 / 股票分文件夹） —— `uploads/asset` 目录按类型分文件夹：market / crypto / metal / stock/{市场}。上传接口支持 `folder` 参数（前端按类型自动归类），清理接口先迁移历史根目录文件再清理孤立文件；实测 66 个历史文件迁移到对应文件夹、20 个孤立文件清理，`assets.url` 全部更新、根目录零残留。
1. 素材库股票同步数量修正（翻页全量拉取 + 美股 300 亿门槛） —— 根因：东方财富 clist 接口单页上限 100 条（`pz=300` 也只返回 100 条），此前美股/港股/A股各只同步到前 100 名。修复：按市值降序翻页拉全量（每页 100，直到市值低于门槛），美股门槛调整为 300 亿美元、港股/A股保持 500 亿本地货币。实测：美股 107→506、港股 100→243、A股 100→409、日股/韩股保持前 100 大市值。
1. 图片文件唯一化（替换 / 删除同步清理 + 孤立文件清理） —— 1) 素材库图标：更新 / 删除资产时后台同步删除旧本地文件（仅当无其他引用时删除，避免误删共享图片），同一条素材多次上传不堆积；2) 网站设置：网站图标 / 背景 / 首页 Logo 替换保存后自动删除旧文件；3) 个人头像、名人头像上传替换后自动删除旧文件；4) 素材库设置弹窗新增「清理孤立文件」按钮，扫描 `public/uploads` 删除未被任何记录引用的历史残留文件——首次执行清理 778 个文件（旧同步随机名图标、旧背景 / Logo / 头像等），被引用文件全部保留。

---

## v0.1.2 · 2026-08-06

> 名人管理头像上传化繁为简：去掉头像旁的铅笔按钮，鼠标划过头像直接显示相机即可上传，与持仓股票图标交互统一，全站排查同类冗余按钮。

### 新功能

1. 素材库与股票图标重塑（分市场同步 / 微牛图标源 / 微牛样式列表） —— 1) 股票图标首次全量同步：美股 >500 亿美元、A股 >500 亿人民币、港股 >500 亿港币（东方财富市值排行），日股/韩股前 100 大市值（companiesmarketcap）；仅首次自动拉取，之后手动触发（素材库设置弹窗「立即同步」）。2) 图标源：美股/港股/A股用微牛（搜索接口拿 tickerId → 168x168 PNG），日股/韩股用 parqet；图标下载到本地 `public/uploads/asset/stock/{市场}/`，按「中文名+代码」命名（苹果AAPL / 寒武纪688256 / 腾讯控股00700），全站秒开无外部慢请求。3) 素材库股票 tab 重塑为微牛样式：白色卡片 + 表头（序号 / 代码与名称 / 最新价 / 涨跌幅 / 市值，可点击排序，排序写入 `?sort=&dir=` 防刷新）+ 圆形图标行（悬停相机上传）+ 分页（每页 20，`?page=`）；数据直接读本地素材库，不再依赖外部 top-stocks 接口。4) 素材库标题右侧新增设置入口（齿轮，与名人持仓一致）：股票同步（进度/上次同步时间）、退市检测（连续两次失败判定删除）、CDN 图标通道开关（默认关闭，开启后缺失图标尝试外部 CDN 兜底）。5) `assets` 表新增 `market_cap/price/change_pct/source/last_checked_at`，自动同步不覆盖手动上传图标（source=manual 优先）。6) 退市检测接口比对东方财富行情，连续两次失败才删除防停牌误删；日韩暂未接入检测源。
1. 名人持仓股票图标为最高优先级并全局生效（迁移 + 缓存修复） —— 名人持仓上传的股票图标作为最高优先级图标源：上传即写入素材库（`/api/assets`，type=stock，按「市场:代码」如 `US:GOOGL`），全站（所有名人圆环、持仓明细、编辑表单）自动共用，无需重复上传。1) 历史「名人专属」图标已迁移到素材库并清空专属记录（巴菲特 GOOGL/AXP/KO/BAC/CVX/OXY/CB、段永平 PDD/GOOGL 共 9 个），素材库成为唯一图标源，圆环/明细不再读取名人专属 override；2) 修复 useAssetIcons 会话级缓存不刷新的问题：挂载时先用缓存秒出、再后台强制拉取最新素材库，新增/迁移的图标立即全局生效（此前旧缓存会一直回退默认图）；3) 圆环/明细图标查找按「持仓市场:代码」优先、`US:代码` 兜底，兼容非美股。
2. 圆环中心头像透明自由轮廓 + 圆环压人物头从洞中探出 + 悬停跃然而出（重做） —— 根据反馈重做头像：1) 透明 PNG 头像改为自由轮廓渲染（不再被圆形蒙版硬切，完整保留肩膀/衣领），人物背后加深蓝色圆盘衬托，按图片透明度自动分流（canvas 检测 alpha），非透明头像（默认 JPG）保持圆形裁剪；2) 图层顺序调整：圆环置于透明人物之上，圆环压住人物肩膀/衣领，头部从洞中探出（参考图「头从洞里探出、衣领被扇形遮挡」）；人物 0.64、上移 3px（悬停 5px），头大居中；3) 悬停不再单纯放大图片，改为「上浮 + 深色投影加深 + 蓝色光环淡入」，320ms 平滑过渡；4) 深色圆盘保留克制的呼吸光晕。
2. 收益走势图升级为 moomoo 分时风格 —— 收益分析走势图按 moomoo 分时图风格重做：组合收益主线下新增蓝色渐变面积填充（线条处半透明 → 底部透明）；主线增加霓虹发光（双层描边：底层宽光晕 + 上层细线），悬停时主线自动变粗（2.4 → 3.8）且光晕增强，200ms 平滑过渡；悬停新增右侧深色信息浮窗（毛玻璃）：日期 + 组合收益 + 全部对比指数数值（红涨绿跌）；图表新增竖向点状网格（四分位）与 X 轴首/中/尾三个日期标签，整体更接近专业金融终端。
3. 名人持仓收益分析重设计（参考富途）+ 详情页 URL —— 详情页收益分析卡片按参考图重做：顶部三大时段大数值（近1年 / 近3年 / 近5年，红涨绿跌）；近3月 / 近1年 / 近5年区间切换，走势图按区间生成（近3月取末尾 3 个月归零、近1年用月序列、近5年平滑合成终点对齐 y5）；图例「组合收益」（蓝）+ 对比指数（灰）数值随鼠标联动，悬停显示白色虚线十字光标、圆点、左侧数值标签与底部日期标签；底部「收起 / 展开全部」折叠（收起后只保留三大数值）；卡片整体缩小为约原 1/4（实测 576×352，图表 534×123），横向网格改为淡点状虚线，十字光标用 CSS 过渡平滑滑动、淡入淡出。详情页增加 URL（`/celebs?celeb=<id>`）：深链直达、刷新保持、返回 / 前进后退无感切换列表与详情。
4. 对比基准指数可扩展（benchmarks 数组） —— 收益对比不再硬编码标普500：`returns` 新增 `benchmarks` 数组（code / name / y1 / points），后续加纳指（IXIC）、上证（SH000001）、恒生（HSI）等只需追加一条数据即可同时出现在图例与图表（灰系自动取色）。存储层、名人管理表单、SEC 数据管道、服务端缓存与前端 localStorage 全链路兼容：旧数据没有 benchmarks 时用 spxPoints 自动兜底归一为标普500，刷新不丢、不闪现。

### 安全修复

1. 后端路由服务端鉴权守卫（修复未登录展示后台布局） —— 严重安全修复：未登录直接输入后台地址（`/watchlist`、`/records`、`/celebs` 等）会展示完整后台布局。根因是后台布局完全依赖客户端 `router.replace('/login')` 跳转（实测未生效），服务端无鉴权。修复：1) 后台布局（`[...slug]`）增加服务端守卫——未登录一律 307 重定向到纯净的 `/login`（无多余后缀），后台布局/数据绝不外泄；2) 客户端兜底守卫——鉴权完成且未登录时渲染「请先登录」占位并引导登录，不渲染后台界面；3) 登录/注册/改密限流适度放宽（登录 50 / 注册 20 / 改密 20 次每 15 分钟，配合全局预算兜底，保留防爆破能力且正常使用/冒烟测试不误触），冒烟测试同步新增「未登录后台跳登录页」断言。实测：未登录访问 `/watchlist` 立即跳 `/login`，登录用户访问后台正常。
2. 全站安全加固（响应头 / 限流 / 输入校验 / 密码策略） —— 根据全站安全审计结果完成加固：1) 新增安全响应头：CSP（生产收紧：无 `unsafe-eval`、`connect-src` 仅 self；开发放行 HMR ws）、`X-Frame-Options: DENY`、`X-Content-Type-Options: nosniff`、`Referrer-Policy`、`Permissions-Policy`，关闭 `X-Powered-By`（不再泄露 Next.js）；实测页面无 CSP 违规、外链图片正常。2) 公开行情类接口（quotes / search / kline / charts / ticker / indices / earnings / top-stocks）新增按 IP + 全局双限流（实测搜索 60 次/分钟后返回 429）；认证接口（登录 / 注册 / 改密）增加全局预算，配合按 IP 限流，即使伪造 `X-Forwarded-For` 也无法无限爆破（IP 值增加格式校验）。3) kline 股票代码白名单 + `encodeURIComponent`，URL 参数注入被拒（实测 400）。4) SVG 上传消毒强化：必须为 SVG 且扩展黑名单（事件处理器全类目、`<a`/`<use`/`<image`/动画、`data:`/`javascript:`/外部引用 href/xlink），恶意 SVG 被拒。5) 公开接口错误信息收敛，不再回显 `err.message`（防泄露数据源内部细节）。6) 密码策略提升：至少 8 位且同时含字母与数字（注册 / 改密 / 管理员重置均校验），前端提示同步更新，冒烟测试账号密码同步调整。
1. 限流升级为 SQLite 共享持久化存储 —— 限流计数由进程内存态升级为 SQLite 持久化（`rate_limit` 表）：同一主机多进程 / 多 worker 共享计数、服务重启不清零，配合既有按 IP + 全局双限流，防爆破与滥用更可靠；环境变量 `RATE_LIMIT_STORE=memory` 可回退到纯内存态（单实例最快），SQLite 不可用时自动回退内存不影响请求；过期桶与过期行定期清理，防止无限增长。实测搜索接口计满 60 次/分钟后返回 429，计数已落库（search 与 global:search 行均持久化）。
2. 全站安全加固（响应头 / 限流 / 输入校验 / 密码策略） —— 根据全站安全审计结果完成加固：1) 新增安全响应头：CSP（生产收紧：无 `unsafe-eval`、`connect-src` 仅 self；开发放行 HMR ws）、`X-Frame-Options: DENY`、`X-Content-Type-Options: nosniff`、`Referrer-Policy`、`Permissions-Policy`，关闭 `X-Powered-By`（不再泄露 Next.js）；实测页面无 CSP 违规、外链图片正常。2) 公开行情类接口（quotes / search / kline / charts / ticker / indices / earnings / top-stocks）新增按 IP + 全局双限流（实测搜索 60 次/分钟后返回 429）；认证接口（登录 / 注册 / 改密）增加全局预算，配合按 IP 限流，即使伪造 `X-Forwarded-For` 也无法无限爆破（IP 值增加格式校验）。3) kline 股票代码白名单 + `encodeURIComponent`，URL 参数注入被拒（实测 400）。4) SVG 上传消毒强化：必须为 SVG 且扩展黑名单（事件处理器全类目、`<a`/`<use`/`<image`/动画、`data:`/`javascript:`/外部引用 href/xlink），恶意 SVG 被拒。5) 公开接口错误信息收敛，不再回显 `err.message`（防泄露数据源内部细节）。6) 密码策略提升：至少 8 位且同时含字母与数字（注册 / 改密 / 管理员重置均校验），前端提示同步更新，冒烟测试账号密码同步调整。

### 修复

1. 修复名人持仓刷新闪现中文名字占位 —— 上一版为防头像空白加了首字母垫底，导致图片加载完成前显示「巴」「黄」等汉字。改为：1) 占位改无文字的低调底色（非透明头像浅灰圆盘、透明头像留空），头像图片加载完成直接覆盖，不再闪现汉字；2) `/celebs` 页面 SSR HTML 增加头像 preload link（`<link rel="preload" as="image">`），浏览器解析阶段即并行下载自定义头像，人物更快出现。实测：6 个头像全部加载成功，圆环中心直接显示人物照片、无文字占位。
1. 全站刷新性能优化（图标请求去重 / 外部图标超时降级 / 指数轮询后台暂停） —— 排查「网页刷新慢」实测定位三类问题并修复：1) `useAssetIcons` 模块级请求去重——此前每个图标组件（名人圆环每个股票 logo、市场 / 资产图标等）都独立 fetch `/api/assets`，一次刷新会发几十次重复请求（局域网 / 手机端更明显），改为全站共享一次请求，刷新后素材图标请求从几十次降到 1-2 次；2) 外部公司图标（`g.foolcdn.com`）国内访问约 800-1500ms/个，素材库没有的股票图标 1.5s 未加载即降级为首字母占位并写入 `sessionStorage`，本次会话刷新不再重复请求慢 CDN；3) 首页指数栏在页面隐藏 / 睡眠时暂停 60s 轮询，避免多标签页堆积行情请求。另排查确认：`/api/celebs` 有缓存时秒出（约 50ms），冷启动 / 代码变更后的 15-25s 为 Next dev 按需编译，生产构建无此问题。
1. 修复名人持仓刷新/睡眠后人物头像短暂消失 + 移除页面副标题 —— 1) 扇形中心头像刷新消失的根因是 SSR 首帧只渲染静态示例头像，客户端再切到缓存的自定义头像，期间 img 无占位、新图（部分自定义 PNG 达 1MB+）加载慢导致人物区域空白。修复：后台布局（`[...slug]`）服务端读取自定义头像（`celebs-avatars.json`）并传入，SSR 首帧即渲染最新头像，刷新无示例→自定义的切换；头像区域改为首字母占位始终垫底、图片加载完成后 320ms 淡入覆盖，加载/切换期间人物区域不空白；挂载后预加载全部名人头像，刷新 / 睡眠唤醒后尽早并行加载。2) 名人持仓页副标题「追踪知名投资人的 250 日持仓表现与最新操作 · 样式参考富途「聪明钱」」按用户要求删除。
1. 移除「开发模式来源」后台可视化配置（恢复 next.config.mjs 手动方式） —— 按决策移除整个功能及所有残留：后台卡片、保存 / 重启服务 / 删除按钮、`/api/system/restart` 与 `/api/system/dev-origins` 接口、`scripts/restart-server.mjs`、settings `devOrigins` 字段与数据库配置均已清除；`next.config.mjs` 恢复为原始方式（`DEV_ORIGINS` 环境变量或直接改代码配置 `allowedDevOrigins`）。
1. 开发配置：allowedDevOrigins 支持局域网访问 —— `next.config` 增加 `allowedDevOrigins`（localhost / 127.0.0.1 / localhost），消除局域网 IP 访问开发资源的 cross-origin 警告，并为 Next.js 未来大版本强制要求该配置提前兼容；配置变更后服务已自动重启验证。
2. 圆环中心优化：去深色圆盘 / 衣领柔化 / SEC 徽标统一灰色 —— 1) 圆环中心人物头像背后的深色圆盘移除（改为透明）：浅色模式下洞内直接显示页面背景，鼠标悬停最大持仓时不再出现突兀的黑色圆；2) 透明人物的衣领/肩膀底部增加 CSS mask 线性渐变向下淡出（58% 实 → 100% 透明），悬停/切换持仓时衣领不再生硬突出，与圆环自然融合；3) 数据来源徽标（SEC 13F / SEC Form 4 / 示例数据）颜色统一为中性灰方案（`bg-bg-gray` / 深色 `white/5`），不再用蓝色区分，深浅色一致。
2. 名人专属股票图标为最高优先级并全局生效（迁移 + 缓存修复） —— 名人持仓上传的股票图标作为最高优先级图标源：上传即写入素材库（`/api/assets`，type=stock，按「市场:代码」如 `US:GOOGL`），全站（所有名人圆环、持仓明细、编辑表单）自动共用，无需重复上传。1) 历史「名人专属」图标已迁移到素材库并清空专属记录（巴菲特 GOOGL/AXP/KO/BAC/CVX/OXY/CB、段永平 PDD/GOOGL 共 9 个），素材库成为唯一图标源，圆环/明细不再读取名人专属 override；2) 修复 useAssetIcons 会话级缓存不刷新的问题：挂载时先用缓存秒出、再后台强制拉取最新素材库，新增/迁移的图标立即全局生效（此前旧缓存会一直回退默认图）；3) 圆环/明细图标查找按「持仓市场:代码」优先、`US:代码` 兜底，兼容非美股。
1. 名人专属股票图标上传即生效（不再依赖点保存） —— 修复名人管理-编辑-持仓股票图标上传后重新打开会重置、圆环不同步的问题：根因是旧逻辑上传只更新了编辑表单的本地草稿，必须再点「保存」才写入数据库，而「上传成功」提示容易误导。新增 `/api/celebs/stock-icon` 接口：已存在的名人上传图标后立即持久化到 `celebs.stock_icons_json`（该名人专属、不写全局素材库），并同步前台圆环；新建名人仍随「创建」一起保存。验证：上传 KO 测试图标后不点保存，接口立即返回生效；测试文件未注册进素材库（素材库 US:CODE 仍作为兜底）；已恢复用户真实上传的 GOOGL 图标并确认编辑表单与圆环同步显示。
2. 圆环悬停提示回归方向定位 + 圆环左移 + 残留修复 —— 1) 提示文字恢复按扇区方向定位（右/左/上/下外侧），移除视口翻转逻辑——最大持仓的提示不再被翻到左上方；2) 名人持仓列表卡片右侧内边距 112px → 144px（圆环再左移 32px，实测右缘 1152 → 1120），右侧扇区（如美国运通 · 12.8%）的提示完整显示在圆环右侧（x 1128–1244 < 页面 1280），不再贴边/溢出；3) 圆环容器增加 `onMouseLeave` 兜底：指针离开圆环时强制清空扇区与头像悬停，避免提示文字残留。
3. 圆环悬停提示外侧智能定位 + 股票 Logo 跟随外倾 —— 1) 悬停提示不再遮挡中心人物头像：改为放在圆环容器外侧，按扇区主导方向自动选择上/下/左/右（如右侧扇区提示出现在圆环右侧外缘，垂直对齐扇区中部），实测巴菲特（苹果 24.6%）与黄仁勋（英伟达 46.8%）的提示均在圆环外侧空白区，头像与圆环完全不被遮挡，位置自然协调。2) 股票 Logo 悬停联动：鼠标划过扇区时，该扇区 Logo 与色块同步向外平移（同外弹向量）+ 按扇区角度轻微外倾旋转（±7°）+ 放大 1.08，320ms 平滑过渡，不再呆板。
3. 修复圆环最大持仓段右下显示不全（三处根因） —— 名人持仓圆环图最大持仓段右下角显示不全的完整根因与修复：1) 最大段默认「外扩」外缘到半径 49.6（viewBox 0~100 仅余 0.4），悬停再沿中角外弹 2.4px（外缘 52）超出视口，被 SVG 默认 `overflow:hidden` 裁出直线缺口——圆环 SVG 改为 `overflow-visible`；2) 悬停外弹使最大段与相邻段分离，露出深色楔形缝隙（视觉上像「被挡住」）——悬停段改为最后绘制，并按扇形跨度动态扩展边界角度，把缝隙填满，圆环保持闭合；3) 相邻路径间的亚像素抗锯齿细线被明暗反差放大——每个扇形段加与自身同色的发丝描边盖住接缝。实测悬停/非悬停状态各交界处像素级无缝相接（视觉模型误读的「缝隙」实为悬停时相邻段变暗 20% 的正常强调反差）。
4. 修复弹窗被整页高度带偏（tab-panel transform 包含块） —— 名人管理弹窗原本固定定位却随页面高度下移：根因是后台 tab 切换动画（`tab-in`）在 transform 上做位移，动画结束后浏览器仍以矩阵值（恒等矩阵）保留包含块，导致弹窗的 `fixed` 定位退化成相对整个页面（文档）居中——名人越多页面越长，弹窗就越靠下。修复：tab 切换动画改为仅淡入（去掉 transform 位移），弹窗回归视口垂直居中（实测 900px 视口下面板顶部 178px、底部 723px，正中 450px），内容完整可见；同时修复了 tab 面板内所有 fixed 元素（弹窗 / 抽屉 / 提示）的同类定位问题。
5. 名人数据标签改为显示来源标签 —— 名人持仓列表与详情页的数据徽标不再显示自动生成的类型文字（示例数据 / SEC 13F），改为直接显示名人管理里配置的「来源标签」（如「SEC EDGAR 13F · 伯克希尔」「SEC EDGAR Form 4 · 佩洛西」），缺失时按来源类型兜底；接口返回的 detail 映射统一补齐每位名人的来源标签。数据质量仍用颜色区分：真实数据（SEC 13F / Form 4）浅蓝底、示例兜底灰底；悬停提示补充说明（如「当前为示例兜底数据（SEC 暂不可达）」），手动维护名人（公开披露 · 示例数据）不追加该提示。
6. 名人持仓详情刷新修复 + 缓存防污染 + 管理弹窗秒开 —— 1) 详情页深链（`/celebs?celeb=<id>`）刷新不再停留在原始头像与示例数据：首帧用示例+自定义头像占位后，缓存/接口真实数据到位会自动替换 active（此前 pendingId 被提前清空导致永远停在示例对象）；自定义头像同时应用到详情视图。2) 缓存持久化修复：SEC 拉取失败时不再把示例数据写进缓存文件（此前示例数据会冒充「缓存数据（24h 内）」并覆盖真实数据），只持久化真实（SEC 成功）或手动维护数据，被污染的旧缓存自动清理，失败条目下次继续重试；实测缓存仅保留真实（cathie/SEC 13F）与手动（trump）条目。3) 名人管理弹窗增加本地缓存：再次打开秒出列表，不再闪现「加载中…」；首次无缓存时用骨架屏占位。
7. 名人管理头像上传化繁为简 —— 编辑表单头像右下角的铅笔按钮删除：头像本身改为可点击按钮，鼠标划过显示相机遮罩 + 放大 + 灰色描边，与持仓明细股票图标的交互完全一致，点击即打开文件选择器上传新头像。全站排查同类问题：名人管理列表的「编辑」铅笔按钮与名人持仓页标题旁的「修改头像」快捷入口（弹出名人选择列表）属独立功能保留；个人设置头像（相机按钮）与素材库上传（整块点击 + 文字）已是简化形态，无需重复按钮。
8. 名人持仓卡片布局微调 + 头像选择列表悬停相机 —— 1) 圆环图再左移：卡片右侧内边距由 80px 增至 112px，圆环右缘不再贴边，右侧留白更从容；2) 右上角数据来源徽标（如「缓存数据（24h 内）」）由紧贴边缘改为留出 16px 内容边距（距视口右缘约 31px），靠近但不贴边，与卡片内边距协调；3) 标题旁铅笔弹出的「选择名人，上传新头像」列表：头像悬停时放大 + 淡入相机遮罩 + 行背景高亮（与编辑表单头像交互统一），点击整行即可上传。

---

## v0.1.1 · 2026-08-05

> 出站代理支持（本地代理）、名人持仓 UI 重构 + SEC EDGAR 真实数据源、名人头像本地化抓取、未登录行情补货币符号、版本记录弹窗重设计为「总结 + 详细更新」。

### 新功能

1. 名人管理界面优化 —— 1) 管理列表头像与展示页同步（合并 `data/celebs-avatars.json` 的自定义头像覆盖）；2) 「启用」改为 iOS 风格滑块开关（平滑过渡），并移除编辑表单里重复的启用项（启停统一在列表页控制）；3) 编辑/新增表单顶部新增「返回列表」按钮；4) 持仓明细与近期操作编辑器补全表头（图标 / 股票代码 / 股票名称 / 市场 / 占比% / 目标价；涨跌幅% / 操作），市场列带市场图标（素材库优先）；5) 每行持仓新增可编辑股票图标：素材库兜底，上传后仅在本名人生效（`celebs` 表 `stock_icons_json` 记录，不写全局素材库），前台圆环与详情页优先使用名人专属图标。
2. 名人持仓可维护化（新增 / 编辑 / 排序） —— 名人与持仓从硬编码改为数据库维护（SQLite `celebs` 表：基本信息 / 启停 / 排序 / 数据来源配置 / 持仓 / 近期操作 / 收益 JSON）。后台「名人持仓」标题旁新增齿轮「名人管理」（管理员）：新增 / 编辑 / 删除 / 启停 / 拖动排序（HTML5 拖拽 + 保存提示），表单含唯一 ID、名称、头衔、头像上传、来源类型（SEC 13F / Form 4 / 示例数据）+ CIK / 申报主体、持仓明细与近期操作编辑器、收益字段；SEC EDGAR 真实数据管道改为按每条记录的来源配置工作，手动数据作为兜底；增删改后自动清除 24h 缓存并刷新前台，立即生效。
3. 名人持仓头像与股票图标可编辑 —— 名人持仓圆环中心头像右下角新增铅笔按钮（管理员可见）：点击后头像进入可修改状态（品牌蓝描边 + 相机遮罩 + 「点击更换」提示），再点头像即可上传新头像（JPG / PNG / GIF / WEBP，最大 2MB），上传后全局生效（`data/celebs-avatars.json` 记录，`/api/celebs` 自动合并）；环上股票图标悬停显示小铅笔角标，点击即可上传替换对应股票图标（素材库全局生效：上传到 `/api/upload` kind=asset 并 upsert `US:代码`，复用素材库唯一标识，刷新不丢）。
4. 新增附件管理模块 —— 后端左侧导航「素材库」上方新增「附件管理」（`/attachments`，管理员可用），用于归档 13F 原始文件与解析结果及未来扩展文件。界面参考笔记类应用（Joplin）：工具栏含上传附件 / 新建目录 / 刷新 / 搜索，视图切换（目录浏览 / 平铺浏览 / 文档树，树形懒加载展开），统计（X 目录 / Y 文件）与排序（修改时间 / 名称），面包屑导航，文件行显示类型图标 / 名称 / 大小 / 修改时间，支持下载与删除（目录需为空或强制删除）。存储于服务端私有目录 `data/attachments`（不对外静态暴露，仅接口下载），接口 `/api/attachments` 支持列表 / 上传 / 新建目录 / 下载 / 删除，单文件上限 50MB，路径防穿越校验。
5. 13F 文件归档（人名 / 年份 / 季度） —— 将巴菲特（沃伦巴菲特）与木头姐（凯西伍德）的真实 13F 归档到 `data/attachments`：目录结构为「人名 / 年份 / 季度」（13F 为季度申报，季度从申报头 PERIOD 报告期解析，如 2026-03-31 → 2026/Q1）；每个季度下分「原始文件」（SEC submissions JSON、申报索引、13F 信息表 XML、完整提交件 TXT）与「优化数据」（持仓明细-实时行情 JSON：代码 / 名称 / 占比 / 实时现价 / 涨跌幅）。
6. 出站代理支持（本地代理） —— 国外数据源（SEC EDGAR / Dataroma / 名人头像）直连失败时自动走本机代理：默认 `http://proxy-host:8080`（本地代理 macOS 代理共享），可用环境变量 `STOCKLOG_PROXY` 覆盖（支持 `http://` / `socks5://`，设为 `off` 关闭）；代理端口探测结果缓存 60 秒，代理未开启或请求失败时自动回退直连，服务无需重启。新增 `lib/net.ts`（undici ProxyAgent）并接入名人持仓的 SEC EDGAR 数据管道，设置-关于技术栈同步登记。
7. 名人持仓 UI 重构 + 真实数据源 —— 卡片改为「单行一列」布局（每行一位名人，左侧数据右侧圆环）；圆环图重构为富途「聪明钱」同款：蓝色系分段环（8 档由深到浅）、悬停扇区沿中角弹出放大并高亮、其余扇区淡出，扇区外侧显示「公司 · 占比」工具提示，公司 Logo 落在对应分段上（素材库图标优先）、外围单字标签、中心为人物头像；新增 `/api/celebs` 后端接口：接入 SEC EDGAR 真实数据（13F-HR 机构持仓 InfoTable 解析 + Form 4 董监高持仓解析，名称→代码映射 + 腾讯行情补充实时现价 / 涨跌幅），24 小时文件缓存（`data/celebs-cache.json`），网络不可达自动回退示例数据，页面顶部与每位名人卡片展示数据源标识（SEC 13F / SEC Form 4 / 示例数据）。
8. 名人头像本地化抓取 —— 从国内图片源抓取 6 位名人（巴菲特 / 佩洛西 / 黄仁勋 / 木头姐 / 特朗普 / 段永平）公开头像，逐张经视觉模型校验人物身份后保存到 `public/uploads/celebs/`，圆环中心头像与详情页均使用本地图片，不再依赖外链。
9. 关于页数据源补全 —— 设置-关于-外部数据源卡片新增「SEC EDGAR / Dataroma」：SEC EDGAR JSON API 提供 13F 机构持仓与 Form 4 董监高持仓（名人持仓数据源），Dataroma 作为 13F / 13G / Form 4 聚合参考站点。
10. 版本记录弹窗重设计 —— 设置-关于-版本改为「总结 + 详细更新」：顶部版本总结卡片（版本号 + 日期 + 一句话总结 + 新功能 / 修复 / 安全 / 前端版本统计标签），下方按「新功能 / 修复 / 安全」分组展示详细更新（每组带色点、说明与条数，条目保留序号 + 箭头），前端版本清单置于最下方。
11. 版本统计标签可点击 —— 版本总结卡片的统计标签（新功能 / 修复 / 安全 / 前端版本）变为可点击：点击后平滑滚动到「详细更新」对应分组并短暂高亮（蓝色光环），hover 显示箭头提示。
12. 数据库定时备份 —— 设置-定时任务新增「数据库定时备份」：使用 better-sqlite3 在线备份生成一致快照（WAL 安全），每次备份保存 SQLite 数据库 + 素材库到 `data/backups` 时间戳文件夹；支持开关、间隔（每小时 / 每天 / 每周 / 每月）、保留份数（3 / 7 / 14 / 30）配置与「立即备份」；服务器运行且有访问时按计划惰性自动备份（60 秒节流），旧的备份自动清理。
13. 财报日历缓存提速 + 视图 URL —— 财报日历改为「读缓存直接显示」：财报日期每月基本固定，服务端月数据缓存从 30 分钟拉长到 12 小时并持久化到 `data/earnings-cache`（服务重启后冷启动秒读，实测 0.18 秒返回，此前需 5-50 秒），配合前端 localStorage 缓存；「回到本月」按钮改为透明底 + 细边框方案。前端自选股 / 我的持仓 / 财报日历 / 全球预览视图同步到 URL（`?view=watchlist` / `holdings` / `earnings` / `global`），刷新与前进后退保持当前视图。冒烟测试的大盘指数断言改为检查美股组（腾讯数据源），避免 CNN 恐惧与贪婪上游瞬时抖动导致的误报。
14. 财报日历深色修复 + 持仓加载态 + 市场 URL —— 修复财报日历深色模式下月份切换按钮 hover 变纯白圆块（改为主题化 hover：深色 `white/10`，并全局补充 `.dark` 下 `hover:bg-white` 覆盖）；「回到本月」hover 背景统一为深色。修复已登录用户在「我的持仓」刷新加载期间误显示「登录后展示…」占位的问题（改为「数据加载中…」，加载完成后展示真实持仓）。前端自选股 / 我的持仓的市场筛选（全部 / 美股 / 港股 / A股 及自定义市场）同步到 URL `?market=`，刷新与前进后退保持所选市场。
15. 自选股/持仓刷新秒出 + 财报徽标中性化 —— 自选股 / 我的持仓刷新不再闪现「数据加载中」：登录后首屏数据缓存到 localStorage（记录 + 迷你走势），刷新时先读缓存立即渲染，再后台刷新最新行情（实测 300ms 内出缓存行）。财报日历选中日期详情的「X 家财报」徽标由浅蓝改为中性深色（深色 `white/5` + 细边框），「+N 家」折叠提示文字也改为中性灰，深浅色协调。
16. 设置改为前端视图 + 访客自选 + 公开行情接口 + 财报已过滤提示 —— 前端点击「设置」不再闪现后台界面：改为预览内设置视图（网站设置 / 个人信息快捷入口卡片，点击才跳转后台）。未登录访客可直接搜索添加自选，保存在浏览器 localStorage（刷新保持，登录后数据在后台）；为支撑该功能，搜索 / 行情 / 走势 / 全球市值榜 / 月K 接口改为公开（纯市场数据，不含用户数据），未登录全球预览不再报「获取失败」。财报日历排查出 8 月 17 日「显示失败」的根因：数据存在但被默认市值筛选（≥$500亿）过滤，现对这类日期显示「N 家已过滤」提示，点击后详情说明可调整筛选查看。冒烟测试「未登录搜索」断言同步更新。
17. 财报日历市值筛选重构 + 前端市场按钮 hover 修复 —— 财报日历市值筛选默认改为「全部市值」（不再默认 ≥$500亿，中小市值财报默认可见）；市值档位参考券商口径重构为区间式：美股 超大盘 ≥$1000亿 / 大盘 $100-1000亿 / 中盘 $10-100亿 / 小盘 <$10亿，A股 超大盘 ≥¥5000亿 / 大盘 ¥1000-5000亿 / 中盘 ¥100-1000亿 / 小盘 <¥100亿（8 月 17 日财报默认即可见）。前端自选股 / 我的持仓市场标签 hover 增加品牌蓝背景效果（与后端市场胶囊一致）；「+」新增市场按钮 hover 由浅蓝改为中性深色（bg-gray / white/10），深浅色协调。
18. 财报日历日期背景中性化 + 今日自动定位 —— 财报日历日期格子背景不再使用浅蓝：选中日期改为中性白 / 深色（仅保留品牌蓝边框），有财报日期的 hover 由浅蓝改为中性灰，深浅色协调。打开财报日历（或点击回到本月）时自动滚动定位到当前日期，今日以蓝色圆点数字标记。
19. 财报日历选中边框改浅灰 + 今日详情自动展开 —— 财报日历选中日期的边框由品牌蓝改为中性浅灰（浅色 `border-edge`，深色深灰），不再有蓝色边框；打开财报日历自动定位到今日并置顶，同时自动展开当日财报详情（X 月 X 日 X 家财报 + 公司列表）直观展示。
20. 新增名人持仓模块 —— 后端左侧导航在「财报日历」下方新增「名人持仓」（`/celebs`），样式参考富途「聪明钱」：名人卡片左侧为名称 / 250 日涨幅（红涨绿跌）/ 近期操作（买入 / 增仓 / 建仓 / 减仓 / 卖出），右侧为圆环图（头像居中 + 持仓 Logo 环绕，圆环分段代表持仓占比）；点击进入详情页：250 日涨幅圆环 + 持仓明细（代码 / 现价 / 涨跌幅 / 持仓占比 / 目标价 / 涨跌距）+ 收益分析（近 1 / 3 / 5 年 + 组合 vs 标普500 走势）。内置 6 位名人示例数据（巴菲特 / 佩洛西 / 黄仁勋 / 木头姐 / 特朗普 / 段永平），Logo 复用素材库；导航缺失项插入逻辑优化为紧跟其前序导航键，名人持仓默认位于财报日历正下方。

### 修复

1. 名人管理三项优化 —— 1) 持仓明细股票图标与头像同款过渡动画（悬停放大 + 品牌蓝描边），悬停遮罩由铅笔改为相机图标；2) 加载提速：SEC 连通性探测由 3 次 × 5 秒收敛为 2 次 × 3 秒（不可达时约 3-6 秒兜底而非 15 秒+），SEC 请求重试上限降至 2 次；名人列表增加前端 localStorage 缓存（刷新先用上次真实列表秒出，再后台拉最新，不再闪现示例数据）；3) 「启用」开关改为苹果 iOS 风格：开启为苹果绿 `#34c759` + 白色圆形滑钮，关闭为浅灰/深灰轨道，平滑过渡，无多余文字。
2. 名人管理 1/2/3 项继续推进 —— 1) 启用滑块保持丝滑过渡，并按原要求去掉滑块旁的「启用」文字（滑块自身即状态标识）；2) 名人管理编辑表单头像鼠标划过增加动画：头像放大 + 品牌蓝描边 + 相机遮罩淡入；3) 持仓明细 / 近期操作表头与数据行居中对齐（占比% / 目标价也居中）；并恢复圆环悬停时扇区附近的持仓文字提示（「公司 · 占比」显示在扇区旁，随扇区位置上下浮动）。
3. 名人持仓圆环右移留白 —— 圆环（人物）列继续左移（卡片右侧留白 44px → 80px，实测圆环右缘距卡片右缘 81px），最右侧不再有被卡片/屏幕边缘遮挡的观感。
4. 前端刷新防“加载中/暂无”闪现 —— 登录后刷新首页自选股 / 我的持仓不再闪现「数据加载中…」与「暂无相关股票 / 暂无持仓」：1) 挂载时（绘制前 `useLayoutEffect`）直接应用本地缓存秒出数据，不再等 auth 检查完成；2) 加载态由文字改为无文字的骨架屏（SSR 与客户端一致，避免 hydration 问题）；3) `/api/records` 首次失败自动重试一次，且加载失败时保留上一次数据，不再被 catch 误置为空数组；4) 未登录访客也走同一加载流程（读本地访客自选），修复访客刷新空白。
5. 修复 SSR/客户端 hydration 不匹配 —— 首页筛选标签顺序（chipOrder）的初始值在 `useState` 里直接读 localStorage（服务端被 try/catch 吞掉返回默认、客户端读到已保存顺序），以及首页视图（view）/筛选（chip）初始值读 URL 参数，导致带参访问或本地有缓存时「server rendered HTML didn't match the client」。统一改为「初始值用默认 + 挂载后客户端副作用恢复」，服务端与客户端首帧一致；名人持仓的头像缓存同样改为初始默认 + `useLayoutEffect`（绘制前）应用，不闪现原始头像也不影响 hydration。
6. 移除扇形块状股票图标编辑 —— 去掉圆环扇区上股票图标的可修改功能（铅笔角标、点击上传、上传中状态），图标恢复纯展示（`pointer-events-none`，悬停扇区时随扇区淡出）；素材库中的股票图标仍全局生效；「名人持仓」标题右侧的头像编辑笔保留。
7. 修复嵌套按钮 hydration 错误 —— 名人持仓卡片原本整体是 `<button>`，内部圆环的股票图标编辑又是 `<button>`，触发「`<button>` cannot be a descendant of `<button>`」hydration 报错。卡片改为可点击 `<div>`（`role=button` + `tabIndex` + Enter/空格键盘支持），保留点击进入详情与悬停效果；控制台实测无任何报错。
8. 名人持仓四项优化 —— 1) 圆环（人物）列整体左移（卡片右侧留白 44px），悬停提示「公司 · 占比」不再超出屏幕（提示水平位置限位 18%-74%）；2) 鼠标划过扇区时沿扇区中角向外平滑弹出（320ms 缓动）+ 亮度增强，其他扇区淡出；3) 自定义名人头像写入 localStorage 缓存（`fire:celebs-avatars`），刷新先用缓存秒出，不再闪现原始头像；4) 股票图标径向位置按参考图「略偏外」微调（53px → 55px），角度仍在扇区中央。
9. 圆环中心头像放大跃出 + 中心净化 —— 参考图中「人物跃然于扇形之上」的效果：中心头像由内孔内的硬切圆改为放大至 66% 尺寸、边缘压住蓝色环内圈（浮于环之上）；头像边缘用极窄径向遮罩柔和渐隐并加深蓝色光环与投影，视觉上跃出有层次；移除原头像的深色背景块、圆形描边与阴影，中心不再有多余圆圈/波纹，头像（透明/深色底）与环融合更自然。
10. 名人头像编辑入口调整 + 图标精确居中 —— 头像编辑笔移动到页面标题「名人持仓」右侧（用户标注的位置）：点击铅笔直接弹出名人选择列表（带头像），选中名人即打开文件选择器上传，一次点击完成，去掉原来点头像右下角笔进入编辑态的两步流程；环上股票图标位置改为精确居中（角度取扇区弧度中点、径向取环带正中，实测 5 个图标距圆心均为 53px，与环带中心一致）。
11. 名人持仓圆环细节修复 —— 1) 去掉突出扇区最外缘的黑色线（原为外描边圆在突出段上切出的黑弧线，已删除内/外描边）；2) 悬停改为以扇区主体为交互对象，股票图标不可交互（`pointer-events-none`）且不再放大，避免图标变形；3) 图标缩小至环宽一半左右（16px，与原图一致）；4) 扇区颜色按「亮度与面积成正比」重设计：最大段亮蓝 `#2B5DE3`，随名次线性过渡到深藏青 `#0E1D47`，小段更暗、整体更贴近参考图。
12. 名人持仓圆环重构（对齐富途「聪明钱」） —— 圆环由「分段描边 + 圆角 + 间隙」重写为真正的扇形弧（SVG path，锐利径向边、无间隙、整体连续）；配色改为亮蓝→深蓝 6 级渐变（`#2B5DE3` → `#0E1D47`），最大持仓段最亮并向外突出（外扩+内收），股票 Logo 居中落在每段扇区（素材库优先、过小段不放置，与参考图一致），中心头像加深色底与环融合，环宽约 40% 更粗壮；悬停高亮/变暗与「公司 · 占比」提示保留。
13. 自选股移至导航首位（默认仍为我的资产） —— 后端左侧导航顺序调整：自选股排第一，账户资产（我的资产）紧随其后并保持为默认打开页（default 标记仍在账户资产上），刷新/直访默认进入我的资产。代码默认顺序（`lib/settings.ts`、RecordsApp、SettingsView）与持久化配置同步更新。
14. SEC 13F 解析增强 —— 修复 13F 明细表查找：优先使用申报主文档（`53405.xml` / `primary_doc.xml`），其次按 infotable / form13f 匹配，再兜底解析整份 `.txt` 提交件，覆盖 X02 新格式；持仓按市值从大到小排序并合并同公司多类股（如 Alphabet A/C 都归入 GOOGL）；SEC 请求加重试（最多 3 次）应对 WAF 间歇性断连。巴菲特的真实持仓（AAPL 34.9% / AXP 27.7% 等）与木头姐的方舟持仓（TSLA 19% / AMD 10% 等）已实测拉取成功并附实时现价。
15. 名人 CIK 硬编码 —— 经代理查询并硬编码 5 位名人的 SEC CIK（伯克希尔 0001067983、方舟 0001697748、H&H 国际投资 0001759760、佩洛西丈夫 Paul F. Pelosi 0001470019、黄仁勋 0001106277），跳过每次启动时的 CIK 反查（browse-edgar 不稳定）；CIK 反查逻辑不再限定 13F 类型，个人申报人（Form 3/4）也能查到。
16. 未登录静态行情补货币符号 —— 首页未登录时的自选股示例行情现价补充货币标识（港股 `HK$`、美股 `$`），与登录后显示一致。
17. 新增市场表单优化 —— 首页标签栏「+」新增市场表单的输入框字号由 15px 调至 13px、高度 44px 调至 36px，与周边按钮协调；占位文案精简（如 `JP` / `日股`）。

---

## v0.1.0 · 2026-08-04

> 首个里程碑：股票记录、行情、自选、全球预览、财报日历、素材库、日志、用户管理与安全加固（当日多次更新合并为同一版本）。

### 新功能

1. 设置子导航 ReactBits 化 —— 鼠标磁吸跟随 + 点击水波纹 + 苹果蓝选中态，切换无感不刷新。
2. 首页全球指数栏 —— 单支指数 5 秒轮换、长桥实心三角、56×20 迷你走势、素材库市场图标、悬停市场提示。
3. 首页指数设置 —— 网站设置新增可折叠卡片：指数增删改排 + 轮换时间 3-60 秒，保存即时生效；默认折叠展示（清除旧展开记忆），手动展开的选择仍会被记住。
4. 前端首页接入指数栏 —— 未登录也可见；`/api/ticker` 与 `/api/assets` GET 改为公开接口。
5. 版本记录体系 —— 设置-关于-版本点击弹出 ReactBits 风格卡片，按「前端版本 / 软件版本 / 新功能」分类，序号 + 箭头展示；版本以日为判断标准，同日更新合并为同一版本号。

### 修复

- 修复带排序、市场、页码、搜索或卡片视图参数刷新素材库时的 hydration mismatch；服务端和客户端先以统一状态水合，再在首次绘制前恢复 URL 状态，左下角不再出现 Next.js Issue。
- 素材库股票行情改为“上次持久化结果立即显示、冷缓存后台更新”：默认第一页由服务端数据库直接输出，浏览器另存最后一次成功的筛选页面，刷新先恢复原表格再静默校验；翻页、筛选与排序时保留当前表格，不再退回骨架。排序箭头固定预留宽度，点击市值、最新价、涨跌幅或市场时列标题不再横向跳动。
- 导航图标与头像刷新不再闪空：左侧导航当前使用的本地素材和用户本地头像随服务端首屏 HTML 内联下发；登录态复核不再把已显示的内联头像替换为待下载地址，素材替换取消短暂透明渐变，硬刷新期间保持连续可见。

1. 自选股美股指数更新 —— 纳斯达克 100 更新为纳斯达克综合指数（usIXIC），名称与数据源同步。
2. 市场图标悬停优化 —— 去除原生 title 提示，统一为自定义提示框，深浅色协调。
3. 首页登录态闪现 —— 登录状态下刷新首页不再闪现「主题切换 + 登录」按钮：鉴权改为三态，检查中渲染空白占位，结果返回后再显示头像菜单或登录区。
4. 关于页保持 5 卡片布局 —— 前端框架 / 后端与数据 / 外部数据源 / 架构特性 / 版本，卡片未满暂不分页，后续增多再启用分页。
5. 美交所股票现价缺失 —— 腾讯搜索联想返回 `SPCH.AM` 这类带交易所后缀的代码，行情接口需要不带后缀的符号。修复后自动剥离 `.AM` 后缀（与 `.OQ` / `.N` / `.PS` / `.K` 同一规则），SPCH 等美交所股票可正常获取现价，已存的带后缀记录也自动兼容。
6. 不误伤带点号代码 —— `BRK.B` 这类点号属于股票名本身的代码不受剥离规则影响，行情保持正常。
7. 首页指数消失 —— 东方财富主行情主机（`push2` / `push2his`）对全球指数返回空数据。数据源切换至可用的 `push2delay` 主机，并保留原主机自动回退；美股 / 港股 / 新加坡 / 日经 / 韩国等 9 个指数全部恢复价格与迷你走势。
8. 总资产刷新闪变 —— 汇率缓存原为进程内存级，服务重启后首次请求会阻塞等待 Frankfurter 最长 15 秒并可能失败，期间港股 / A股 / 日股 / 韩股被按 1:1 误算成美元，导致总资产、当日盈亏等刷新瞬间出现错误数值。修复：汇率持久化到 SQLite（重启秒回）、兜底一律使用「上一次成功汇率」（内存 / SQLite / 本地缓存，从未成功过才显示占位而不是猜测值）、汇率接口补齐 JPY / KRW 并在后台异步刷新。

### 界面与交互

1. 首页预览卡片交互化 —— 修复卡片下边框与页脚紧贴（间距 0）的问题，调整为合适间距；侧边栏「自选股 / 持仓 / 行情 / 设置」改为可点击链接到后端对应模块并删除「日志」；「全部 / 港股 / 美股 / A股」标签可点击筛选且选中色块同步；登录后预览表格展示后端真实持仓数据（实时现价 / 涨跌幅 / 迷你走势 / 持仓盈亏，盈亏按正负着色），未登录仍展示静态示例。
2. 首页预览卡片改为内嵌模块视图 —— 「自选股 / 持仓 / 行情」在前端直接展示后端模块数据（不再跳转链接，设置暂保持跳转）：自选股 = 全部记录、持仓 = 有数量的记录（仅持仓视图展示「持仓盈亏」列）、行情 = 全部记录实时行情；「近5日走势」列固定 80px 不再过长；筛选标签改为与后端保存按钮一致的透明底 + 边框样式；列表每页 6 条并支持上一页 / 下一页分页；右上角「添加自选」恢复为内联搜索，选择后直接同步到后端并刷新列表；暗黑模式刷新白屏加固（主题防闪脚本移入 head 并直接设置 html 背景色）。
3. 首页搜索生效 + 视图调整 + 暗黑背景修复 —— 首页搜索框改为真实搜索（输入名称 / 代码即可搜索并加入自选，同步后端）；「添加自选」改为白色色块按钮（点击聚焦搜索）；侧边栏移除与自选股重复的「行情」，新增「全球预览」（全球市值榜，每页 10 条）与「财报日历」（置于持仓下方，展示后端财报预告，每页 6 条）；暗黑模式下网站背景图叠加深色遮罩，修复刷新后应用窗口周围出现大片浅色背景的问题。
4. 前端视图与后端模块对齐 + 暗黑白屏根治 —— 「持仓」改名「我的持仓」，该视图内按钮改为「添加持仓」；「添加自选」仅出现在自选股与我的持仓视图，财报日历 / 全球预览不再显示白色按钮；前端「全球预览」与「财报日历」直接复用后端组件（全球市值榜完整表格：排名 / 资产 / 价格 / 涨跌幅 / 月K / 市值 / 市场 + 货币切换，每页 10 条；财报月历：市场 / 市值 / 类型 / 时段筛选 + 月份导航 + 日历网格），数据与后端完全一致。暗黑白屏根治：主题写入 Cookie，服务端 SSR 首帧即深色（html 类 + 背景色），hero 遮罩改为服务端传入 initialDark 不再先渲染浅色，纯 SSR / reload 首帧实测全部深色。
5. 前端股票表增强 + 财报日历提速 —— 自选股 / 我的持仓表格新增「序号」列（跨页连续编号）；现价补充货币符号（$ / HK$ / ¥）；股票名称左侧新增股票图标（素材库「市场:代码」唯一标识，无图标回退首字母头像）；筛选标签（全部 / 港股 / 美股 / A股）可拖动排序（本地记忆）并加上市场国旗图标。财报日历优化：进入视图立即渲染日历网格（日期先行），财报数据异步填充并写入 localStorage 缓存（二次进入秒出），修复 StrictMode 下请求被取消导致财报不加载的问题。
6. 标签改用素材库市场图标 + 后端加号新增市场 —— 前端筛选标签不再使用 emoji 国旗，改为素材库市场图标（MarketIcon：素材库自定义图标优先，回退长桥圆形国旗 / flagcdn）；标签栏默认「全部 + 美股 / 港股 / A股」3 个市场，市场来自后端设置（自定义市场自动追加）；标签栏末尾新增后端「我的资产」同款虚线圆形加号按钮，点击展开内联新增市场表单（代码 + 名称），保存后同步到后端设置（markets / marketLabels）并即时显示新标签。

### 界面与规范

1. 保存按钮全局统一 —— 新增 `btn-line` 透明底细边框样式，全站 22 处保存类按钮统一，深浅色双适配。
2. 中性选中态统一 —— 分段 / 胶囊选中改为中性色（`seg-active`），替换全部浅蓝选中。

### 安全修复

1. 管理接口管理员鉴权（用户管理、数据库状态、设置写入、非头像上传、资产写操作等）。
2. 非管理员读取设置脱敏（隐藏 PostgreSQL 连接信息）。
3. SVG 上传消毒（拒绝 script / 事件属性 / javascript:）。
4. 首页导航链接白名单（`safeHref`）。
5. 登录 / 注册 / 改密限流，改密后其他会话失效。
6. 批量与长度上限（行情 100 条、搜索 50 字符、记录字段限长、行情代码白名单）。
7. 前端权限控制 —— 非管理员隐藏用户管理 / 素材库，设置页仅显示个人信息与关于。
8. npm 依赖审计 0 漏洞，补齐 `package-lock.json`。
